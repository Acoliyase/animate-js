# babel-plugin-jsx-source-attrs

Injects a custom attribute (default: `data-source-location`) into every JSX element, showing the source file, line, and column.

## Installation
```bash
npm install --save-dev babel-plugin-jsx-source-attrs
```

