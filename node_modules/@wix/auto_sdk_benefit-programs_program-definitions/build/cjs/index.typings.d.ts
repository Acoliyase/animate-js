import { NonNullablePaths } from '@wix/sdk-types';

interface ProgramDefinition {
    /**
     * Program definition ID.
     * @format GUID
     * @immutable
     * @readonly
     */
    _id?: string | null;
    /**
     * Revision number, which increments by 1 each time the program definition is updated.
     * To prevent conflicting changes, the current revision must be passed when updating the program definition.
     *
     * Ignored when creating a program definition.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the program definition was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the program definition was updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Program definition name.
     * @maxLength 64
     */
    displayName?: string | null;
    /**
     * Namespace for your app or site's benefit programs. Namespaces allow you to distinguish between entities that you created and entities that other apps created.
     * @immutable
     * @readonly
     * @minLength 1
     * @maxLength 20
     */
    namespace?: string | null;
    /**
     * Custom field data for the program definition object.
     *
     * [Extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions) must be configured in the app dashboard before they can be accessed with API calls.
     */
    extendedFields?: ExtendedFields;
    /**
     * ID for the program definition defined by you. You can use `externalId` to filter queries.
     * @format GUID
     * @immutable
     * @readonly
     */
    externalId?: string | null;
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
interface CreateProgramDefinitionRequest {
    /** Program definition to create. */
    programDefinition: ProgramDefinition;
}
interface CreateProgramDefinitionResponse {
    /** Created program definition. */
    programDefinition?: ProgramDefinition;
}
interface UpdateProgramDefinitionRequest {
    /** Program definition to update. */
    programDefinition: ProgramDefinition;
}
interface UpdateProgramDefinitionResponse {
    /** Updated program definition. */
    programDefinition?: ProgramDefinition;
}
interface DeleteProgramDefinitionRequest {
    /**
     * ID of the program definition to delete.
     * @format GUID
     */
    programDefinitionId: string;
    /** Determines when the changes to this program definition will be applied to associated programs and pools. */
    cascadeType?: CascadeWithLiterals;
}
declare enum Cascade {
    /** Unknown cascade. */
    UNKNOWN_CASCADE = "UNKNOWN_CASCADE",
    /** Changes are applied to existing programs and pools when they are next renewed. */
    NEXT_RENEWAL = "NEXT_RENEWAL",
    /** Changes are applied to associated programs and pools immediately. */
    IMMEDIATELY = "IMMEDIATELY",
    /** Changes are not applied to existing associated programs and pools. They are only applied to future programs and pools. */
    FUTURE_PROVISIONS = "FUTURE_PROVISIONS"
}
/** @enumType */
type CascadeWithLiterals = Cascade | 'UNKNOWN_CASCADE' | 'NEXT_RENEWAL' | 'IMMEDIATELY' | 'FUTURE_PROVISIONS';
interface DeleteProgramDefinitionResponse {
}
interface GetProgramDefinitionRequest {
    /**
     * ID of the program definition to retrieve.
     * @format GUID
     */
    programDefinitionId: string;
}
interface GetProgramDefinitionResponse {
    /** Retrieved program definition. */
    programDefinition?: ProgramDefinition;
}
interface GetProgramDefinitionByExternalIdAndNamespaceRequest {
    /**
     * Namespace for your app or site's benefit programs. Namespaces allow you to distinguish between entities that you created and entities that other apps created.
     * @minLength 1
     * @maxLength 20
     */
    namespace: string;
    /**
     * Program definition external ID.
     * @format GUID
     */
    externalId: string;
}
interface GetProgramDefinitionByExternalIdAndNamespaceResponse {
    /** Retrieved program definition. */
    programDefinition?: ProgramDefinition;
}
interface QueryProgramDefinitionsRequest {
    /** Filter, sort, and paging to apply to the query. */
    query?: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     * See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language)
     * for more information.
     */
    filter?: Record<string, any> | null;
    /**
     * List of sort objects.
     * @maxSize 5
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /**
     * Sort order. Use `ASC` for ascending order or `DESC` for descending order.
     *
     * Default: `ASC`
     */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    /** Ascending sort order. */
    ASC = "ASC",
    /** Descending sort order. */
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Maximum number of items to return.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface QueryProgramDefinitionsResponse {
    /** List of retrieved program definitions. */
    programDefinitions?: ProgramDefinition[];
    /** Metadata for the paginated results. */
    metadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Cursor strings that point to the next page, previous page, or both. */
    cursors?: Cursors;
    /**
     * Whether there are more pages to retrieve following the current page.
     *
     * + `true`: Another page of results can be retrieved.
     * + `false`: This is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface UpsertProgramDefinitionRequest {
    /** Program definition to be created or updated. */
    programDefinition?: ProgramDefinition;
}
interface UpsertProgramDefinitionResponse {
    /** Created or updated program definition. */
    programDefinition?: ProgramDefinition;
}
interface GetOrCreateProgramDefinitionRequest {
    /**
     * Application sub-module that represents the source of the program definition. Specified by the external developer. This namespace is used to handle and retrieve program definition and associated programs.
     * @minLength 1
     * @maxLength 20
     */
    namespace?: string;
    /**
     * External program definition ID.
     * @format GUID
     */
    externalId?: string;
}
interface GetOrCreateProgramDefinitionResponse {
    /** The created or updated program definition. */
    programDefinition?: ProgramDefinition;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
/**
 * Creates a program definition.
 * @param programDefinition - Program definition to create.
 * @public
 * @documentationMaturity preview
 * @requiredField programDefinition
 * @requiredField programDefinition.externalId
 * @requiredField programDefinition.namespace
 * @permissionId BENEFIT_PROGRAMS.PROGRAM_DEFINITION_CREATE
 * @applicableIdentity APP
 * @returns Created program definition.
 * @fqn wix.benefit_programs.v1.program_definition.ProgramDefinitionService.CreateProgramDefinition
 */
declare function createProgramDefinition(programDefinition: NonNullablePaths<ProgramDefinition, `externalId` | `namespace`, 2>): Promise<ProgramDefinition>;
/**
 * Updates a program definition.
 *
 * Each time the program definition is updated,
 * `revision` increments by 1.
 * The current `revision` must be passed when updating the program definition.
 * This ensures you're working with the latest program definition
 * and prevents unintended overwrites.
 * @param _id - Program definition ID.
 * @public
 * @documentationMaturity preview
 * @requiredField _id
 * @requiredField programDefinition
 * @requiredField programDefinition.revision
 * @permissionId BENEFIT_PROGRAMS.PROGRAM_DEFINITION_UPDATE
 * @applicableIdentity APP
 * @returns Updated program definition.
 * @fqn wix.benefit_programs.v1.program_definition.ProgramDefinitionService.UpdateProgramDefinition
 */
declare function updateProgramDefinition(_id: string, programDefinition: NonNullablePaths<UpdateProgramDefinition, `revision`, 2>): Promise<ProgramDefinition>;
interface UpdateProgramDefinition {
    /**
     * Program definition ID.
     * @format GUID
     * @immutable
     * @readonly
     */
    _id?: string | null;
    /**
     * Revision number, which increments by 1 each time the program definition is updated.
     * To prevent conflicting changes, the current revision must be passed when updating the program definition.
     *
     * Ignored when creating a program definition.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the program definition was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the program definition was updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Program definition name.
     * @maxLength 64
     */
    displayName?: string | null;
    /**
     * Namespace for your app or site's benefit programs. Namespaces allow you to distinguish between entities that you created and entities that other apps created.
     * @immutable
     * @readonly
     * @minLength 1
     * @maxLength 20
     */
    namespace?: string | null;
    /**
     * Custom field data for the program definition object.
     *
     * [Extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions) must be configured in the app dashboard before they can be accessed with API calls.
     */
    extendedFields?: ExtendedFields;
    /**
     * ID for the program definition defined by you. You can use `externalId` to filter queries.
     * @format GUID
     * @immutable
     * @readonly
     */
    externalId?: string | null;
}
/**
 * Deletes a program definition.
 * @param programDefinitionId - ID of the program definition to delete.
 * @public
 * @documentationMaturity preview
 * @requiredField programDefinitionId
 * @permissionId BENEFIT_PROGRAMS.PROGRAM_DEFINITION_DELETE
 * @applicableIdentity APP
 * @fqn wix.benefit_programs.v1.program_definition.ProgramDefinitionService.DeleteProgramDefinition
 */
declare function deleteProgramDefinition(programDefinitionId: string, options?: DeleteProgramDefinitionOptions): Promise<void>;
interface DeleteProgramDefinitionOptions {
    /** Determines when the changes to this program definition will be applied to associated programs and pools. */
    cascadeType?: CascadeWithLiterals;
}
/**
 * Retrieves a program definition.
 * @param programDefinitionId - ID of the program definition to retrieve.
 * @public
 * @documentationMaturity preview
 * @requiredField programDefinitionId
 * @permissionId BENEFIT_PROGRAMS.PROGRAM_DEFINITION_READ
 * @applicableIdentity APP
 * @returns Retrieved program definition.
 * @fqn wix.benefit_programs.v1.program_definition.ProgramDefinitionService.GetProgramDefinition
 */
declare function getProgramDefinition(programDefinitionId: string): Promise<ProgramDefinition>;
/**
 * Retrieves a program definition with the specified external ID and namespace.
 * @param namespace - Namespace for your app or site's benefit programs. Namespaces allow you to distinguish between entities that you created and entities that other apps created.
 * @public
 * @documentationMaturity preview
 * @requiredField namespace
 * @requiredField options
 * @requiredField options.externalId
 * @permissionId BENEFIT_PROGRAMS.PROGRAM_DEFINITION_READ
 * @applicableIdentity APP
 * @fqn wix.benefit_programs.v1.program_definition.ProgramDefinitionService.GetProgramDefinitionByExternalIdAndNamespace
 */
declare function getProgramDefinitionByExternalIdAndNamespace(namespace: string, options: NonNullablePaths<GetProgramDefinitionByExternalIdAndNamespaceOptions, `externalId`, 2>): Promise<GetProgramDefinitionByExternalIdAndNamespaceResponse>;
interface GetProgramDefinitionByExternalIdAndNamespaceOptions {
    /**
     * Program definition external ID.
     * @format GUID
     */
    externalId: string;
}
/**
 * Creates a query to retrieve a list of program definitions.
 *
 * The Query Program Definitions method builds a query to retrieve a list of program definitions and returns a `ProgramDefinitionsQueryBuilder` object.
 *
 * The returned object contains the query definition, which is used to run the query using the `find()` method.
 *
 * You can refine the query by chaining `ProgramDefinitionsQueryBuilder` methods onto the query. `ProgramDefinitionsQueryBuilder` methods enable you to filter, sort, and control the results that Query Program Definitions returns.
 *
 * Query Program Definitions has a default paging limit of 50, which you can override.
 *
 *  For a full description of the item object, see the object returned for the `items` property in `ProgramDefinitionsQueryResult`.
 * @public
 * @documentationMaturity preview
 * @permissionId BENEFIT_PROGRAMS.PROGRAM_DEFINITION_READ
 * @applicableIdentity APP
 * @fqn wix.benefit_programs.v1.program_definition.ProgramDefinitionService.QueryProgramDefinitions
 */
declare function queryProgramDefinitions(): ProgramDefinitionsQueryBuilder;
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface ProgramDefinitionsQueryResult extends QueryCursorResult {
    items: ProgramDefinition[];
    query: ProgramDefinitionsQueryBuilder;
    next: () => Promise<ProgramDefinitionsQueryResult>;
    prev: () => Promise<ProgramDefinitionsQueryResult>;
}
interface ProgramDefinitionsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    eq: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: any) => ProgramDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ne: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: any) => ProgramDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ge: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: any) => ProgramDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    gt: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: any) => ProgramDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    le: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: any) => ProgramDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    lt: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: any) => ProgramDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `string`.
     * @param string - String to compare against. Case-insensitive.
     * @documentationMaturity preview
     */
    startsWith: (propertyName: '_id' | 'namespace' | 'externalId', value: string) => ProgramDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `values`.
     * @param values - List of values to compare against.
     * @documentationMaturity preview
     */
    hasSome: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: any[]) => ProgramDefinitionsQueryBuilder;
    /** @documentationMaturity preview */
    in: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: any) => ProgramDefinitionsQueryBuilder;
    /** @documentationMaturity preview */
    exists: (propertyName: '_id' | '_createdDate' | 'namespace' | 'externalId', value: boolean) => ProgramDefinitionsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    ascending: (...propertyNames: Array<'_id' | '_createdDate' | 'namespace' | 'externalId'>) => ProgramDefinitionsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    descending: (...propertyNames: Array<'_id' | '_createdDate' | 'namespace' | 'externalId'>) => ProgramDefinitionsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object.
     * @documentationMaturity preview
     */
    limit: (limit: number) => ProgramDefinitionsQueryBuilder;
    /** @param cursor - A pointer to specific record
     * @documentationMaturity preview
     */
    skipTo: (cursor: string) => ProgramDefinitionsQueryBuilder;
    /** @documentationMaturity preview */
    find: () => Promise<ProgramDefinitionsQueryResult>;
}

export { type ActionEvent, Cascade, type CascadeWithLiterals, type CreateProgramDefinitionRequest, type CreateProgramDefinitionResponse, type CursorPaging, type CursorPagingMetadata, type CursorQuery, type CursorQueryPagingMethodOneOf, type Cursors, type DeleteProgramDefinitionOptions, type DeleteProgramDefinitionRequest, type DeleteProgramDefinitionResponse, type DomainEvent, type DomainEventBodyOneOf, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type ExtendedFields, type GetOrCreateProgramDefinitionRequest, type GetOrCreateProgramDefinitionResponse, type GetProgramDefinitionByExternalIdAndNamespaceOptions, type GetProgramDefinitionByExternalIdAndNamespaceRequest, type GetProgramDefinitionByExternalIdAndNamespaceResponse, type GetProgramDefinitionRequest, type GetProgramDefinitionResponse, type IdentificationData, type IdentificationDataIdOneOf, type MessageEnvelope, type ProgramDefinition, type ProgramDefinitionsQueryBuilder, type ProgramDefinitionsQueryResult, type QueryProgramDefinitionsRequest, type QueryProgramDefinitionsResponse, type RestoreInfo, SortOrder, type SortOrderWithLiterals, type Sorting, type UpdateProgramDefinition, type UpdateProgramDefinitionRequest, type UpdateProgramDefinitionResponse, type UpsertProgramDefinitionRequest, type UpsertProgramDefinitionResponse, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, createProgramDefinition, deleteProgramDefinition, getProgramDefinition, getProgramDefinitionByExternalIdAndNamespace, queryProgramDefinitions, updateProgramDefinition };
