import { prettyDOM } from '@testing-library/dom';
import { isJsdomReactUniDriver } from '../isJsdomReactUniDriver';
export const printDom = async (uni) => {
    if (isJsdomReactUniDriver(uni)) {
        console.log(prettyDOM(await uni.unwrap()) || '');
    }
    else {
        throw new Error('Unsupported adapter type. jsdomReactUniDriver expected');
    }
};
//# sourceMappingURL=printDom.js.map