import { type UniDriver } from '@wix/unidriver-core';
import { type TestAppProps } from '../TestAppProps';
export type E2ESetupParams = {
    story: string;
};
export type UnitSetupProps = TestAppProps;
export type SetupData = E2ESetupParams | UnitSetupProps;
export type SetupFn<T = SetupData, P = any, K = object> = (data: T, ctx?: P) => ({
    driver: UniDriver;
    tearDown: () => unknown;
} & K) | Promise<{
    driver: UniDriver;
    tearDown: () => unknown;
} & K>;
export type TestSuiteParams<T = SetupData, P = any> = {
    isStorybook?: boolean;
    setup: SetupFn<T, P>;
};
//# sourceMappingURL=types.d.ts.map