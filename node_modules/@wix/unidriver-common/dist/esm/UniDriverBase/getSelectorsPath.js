export const getSelectorsPath = async (ctx, acc = []) => {
    if (ctx.parent) {
        await getSelectorsPath(ctx.parent, acc);
    }
    if (ctx.selector) {
        acc.push(await ctx.selector);
    }
    else if (ctx.idx !== undefined) {
        acc.push(`:nth-child(${String(ctx.idx + 1)})`);
    }
    return acc;
};
//# sourceMappingURL=getSelectorsPath.js.map