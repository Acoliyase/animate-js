import { UniDriverClass } from './UniDriverClass';
import { createUniDriverParams } from '../../utils/createUniDriverParams';
export const testUniDriver = (params) => {
    return new UniDriverClass(createUniDriverParams(params, async (base, selector) => {
        return base.querySelector(selector);
    }));
};
//# sourceMappingURL=testUniDriver.js.map