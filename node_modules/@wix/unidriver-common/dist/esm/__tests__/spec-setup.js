"use strict";
beforeAll(() => {
    // https://github.com/testing-library/user-event/issues/1115#issuecomment-2495876991
    // This fixes the issue with user-event not working with vitest fake timers
    vi.stubGlobal('jest', {
        advanceTimersByTime: vi.advanceTimersByTime.bind(vi),
    });
});
afterEach(() => {
    vi.useRealTimers();
    vi.clearAllMocks();
});
//# sourceMappingURL=spec-setup.js.map