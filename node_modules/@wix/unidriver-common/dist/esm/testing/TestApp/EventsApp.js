import React, { useState } from 'react';
export const EventsApp = () => {
    const [keyboardEvents, setKeyboardEvents] = useState([]);
    const [mouseEvents, setMouseEvents] = useState([]);
    const onMouseEvent = (e) => {
        setMouseEvents([...mouseEvents, e.type]);
    };
    return (React.createElement("div", { className: "events-container" },
        React.createElement("div", { className: "mouse-events" },
            React.createElement("button", { onMouseDown: onMouseEvent, onMouseLeave: onMouseEvent, onMouseMove: onMouseEvent, onMouseUp: onMouseEvent, style: { height: 100, width: 100 } }, "Mouse Events"),
            React.createElement("div", null, mouseEvents.map((event) => {
                return (React.createElement("div", { className: "mouse-event-data", key: event },
                    React.createElement("span", { className: "event-type" }, event)));
            }))),
        React.createElement("div", { className: "keyboard-events" },
            React.createElement("input", { defaultValue: "", onKeyDown: (e) => {
                    setKeyboardEvents([...keyboardEvents, { key: e.key, keyCode: e.keyCode }]);
                } }),
            React.createElement("div", null, keyboardEvents.map((event, idx) => {
                // on FF Meta key press ends up named "OS", so we normalize it (https://stackoverflow.com/questions/39292111/can-firefox-detect-metakey)
                const normalizedKey = event.key === 'OS' ? 'Meta' : event.key;
                return (React.createElement("div", { className: "keyboard-event-data", key: idx },
                    React.createElement("span", { className: "event-key", style: 
                        // `whiteSpace: pre` is needed for tests that run in browsers + Space key
                        { whiteSpace: 'pre' } }, normalizedKey),
                    React.createElement("span", { className: "event-keycode" }, event.keyCode)));
            })))));
};
//# sourceMappingURL=EventsApp.js.map