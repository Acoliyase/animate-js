export const runTest = async (setup, ctx, initializationData, test) => {
    const { tearDown, ...rest } = await setup(initializationData, ctx);
    try {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-argument
        await test(rest);
        await tearDown();
    }
    catch (e) {
        await tearDown();
        throw e;
    }
};
//# sourceMappingURL=runTest.js.map