import { isLocator } from './isLocator';
export const createUniDriverParams = (params, querySelector) => {
    if (typeof params === 'function') {
        const config = params();
        if (isLocator(config)) {
            return {
                base: params,
                selector: undefined,
            };
        }
        return {
            selector: config.selector,
            base: async () => querySelector(await config.root, config.selector),
        };
    }
    if (isLocator(params)) {
        return {
            base: params,
            selector: undefined,
        };
    }
    return {
        selector: params.selector,
        base: async () => querySelector(await params.root, params.selector),
    };
};
//# sourceMappingURL=createUniDriverParams.js.map