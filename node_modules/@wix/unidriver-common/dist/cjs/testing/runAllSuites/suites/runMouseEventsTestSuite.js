"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.runMouseEventsTestSuite = void 0;
const runTest_1 = require("../utils/runTest");
const runMouseEventsTestSuite = (params) => {
    const { isStorybook, setup } = params;
    describe('mouse events', () => {
        it('press works', async ({ mount }) => {
            await (0, runTest_1.runTest)(setup, { mount }, isStorybook ? { story: 'base' } : { items: [] }, async ({ driver }) => {
                const eventsComp = driver.$('.mouse-events button');
                await eventsComp.mouse.press();
                expect(await driver.$$('.mouse-event-data .event-type').text()).toContain('mousedown');
            });
        });
        it('with deep nested selector press works', async ({ mount }) => {
            await (0, runTest_1.runTest)(setup, { mount }, isStorybook ? { story: 'base' } : { items: [] }, async ({ driver }) => {
                const eventsComp = driver.$('.mouse-events').$('button');
                await eventsComp.mouse.press();
                expect(await driver.$$('.mouse-event-data .event-type').text()).toContain('mousedown');
            });
        });
        it('release works', async ({ mount }) => {
            await (0, runTest_1.runTest)(setup, { mount }, isStorybook ? { story: 'base' } : { items: [] }, async ({ driver }) => {
                const eventsComp = driver.$('.mouse-events button');
                await eventsComp.mouse.release();
                expect(await driver.$$('.mouse-event-data .event-type').text()).toContain('mouseup');
            });
        });
        it('with deep nested selector release works', async ({ mount }) => {
            await (0, runTest_1.runTest)(setup, { mount }, isStorybook ? { story: 'base' } : { items: [] }, async ({ driver }) => {
                const eventsComp = driver.$('.mouse-events').$('button');
                await eventsComp.mouse.release();
                expect(await driver.$$('.mouse-event-data .event-type').text()).toContain('mouseup');
            });
        });
        it('move works', async ({ mount }) => {
            await (0, runTest_1.runTest)(setup, { mount }, isStorybook ? { story: 'base' } : { items: [] }, async ({ driver }) => {
                const eventsComp = driver.$('.mouse-events button');
                await eventsComp.mouse.moveTo(eventsComp);
                expect(await driver.$$('.mouse-event-data .event-type').text()).toContain('mousemove');
            });
        });
        it('with deep nested selector move works', async ({ mount }) => {
            await (0, runTest_1.runTest)(setup, { mount }, isStorybook ? { story: 'base' } : { items: [] }, async ({ driver }) => {
                const eventsComp = driver.$('.mouse-events').$('button');
                await eventsComp.mouse.moveTo(eventsComp);
                expect(await driver.$$('.mouse-event-data .event-type').text()).toContain('mousemove');
            });
        });
        it('leave works', async ({ mount }) => {
            await (0, runTest_1.runTest)(setup, { mount }, isStorybook ? { story: 'base' } : { items: [] }, async ({ driver }) => {
                const eventsComp = driver.$('.mouse-events').$('button');
                await eventsComp.mouse.leave();
                expect(await driver.$$('.mouse-event-data .event-type').text()).toContain('mouseleave');
            });
        });
    });
};
exports.runMouseEventsTestSuite = runMouseEventsTestSuite;
//# sourceMappingURL=runMouseEventsTestSuite.js.map