import { NonNullablePaths } from '@wix/sdk-types';

interface ManualTaxMapping {
    /**
     * Manual tax mapping ID.
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * Revision number, which increments by 1 each time the manual tax mapping is updated.
     * To prevent conflicting changes, the current revision must be passed when updating the manual tax mapping.
     *
     * Ignored when creating a manual tax mapping.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the manual tax mapping was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the manual tax mapping was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Tax group ID. Tax groups define collections of items or services that share the same tax treatment.
     * @format GUID
     */
    taxGroupId?: string | null;
    /**
     * Tax region ID. Tax regions represent geographical areas where specific tax rules apply.
     * @format GUID
     */
    taxRegionId?: string | null;
    /**
     * Tax rate to apply for this tax group and region combination. Specified as a decimal value (for example, `0.080000` for 8%).
     * @decimalValue options { maxScale:6, gte:0 }
     */
    taxRate?: string;
    /**
     * Description of the manual tax mapping. Use this field to add context or notes about the tax rate.
     * @maxLength 1000
     */
    description?: string | null;
    /**
     * Type of tax being applied, such as `VAT`, `GST`, or `Sales Tax`. This depends on the jurisdiction's tax laws.
     * @maxLength 200
     */
    taxType?: string;
    /**
     * Name of the tax being applied. For example, `STATE TAX` or `GST`.
     * @maxLength 200
     */
    taxName?: string;
    /**
     * Name of the jurisdiction where this tax rate applies. For example, `California` or `United Kingdom`.
     * @maxLength 200
     */
    jurisdiction?: string;
    /** Type of jurisdiction where this tax rate applies, such as country, state, county, city, or special district. */
    jurisdictionType?: JurisdictionTypeWithLiterals;
    /**
     * Tax exemption details for user-defined exempt groups.
     * @maxSize 50
     */
    exemptRates?: ExemptDetails[];
    /** Extended fields for storing additional custom data. */
    extendedFields?: ExtendedFields;
}
/** Type of jurisdiction that taxes were calculated for. For example, `"State"`, `"County"`, `"City"`, `"Special"`, etc. */
declare enum JurisdictionType {
    UNDEFINED = "UNDEFINED",
    COUNTRY = "COUNTRY",
    STATE = "STATE",
    COUNTY = "COUNTY",
    CITY = "CITY",
    SPECIAL = "SPECIAL"
}
/** @enumType */
type JurisdictionTypeWithLiterals = JurisdictionType | 'UNDEFINED' | 'COUNTRY' | 'STATE' | 'COUNTY' | 'CITY' | 'SPECIAL';
interface ExemptDetails {
    /**
     * Tax override rate for given tax mapping.
     * @decimalValue options { maxScale:6, gte:0 }
     */
    taxRate?: string;
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
interface CreateManualTaxMappingRequest {
    /** Manual tax mapping to create. */
    manualTaxMapping: ManualTaxMapping;
}
interface CreateManualTaxMappingResponse {
    /** Created manual tax mapping. */
    manualTaxMapping?: ManualTaxMapping;
}
interface BulkCreateManualTaxMappingsRequest {
    /**
     * Manual tax mappings to create.
     * @minSize 1
     * @maxSize 100
     */
    manualTaxMappings: ManualTaxMapping[];
    /**
     * Whether to return the created manual tax mapping entities in the response.
     *
     * Default: `false`
     */
    returnEntity?: boolean;
}
interface BulkCreateManualTaxMappingsResponse {
    /**
     * Results of the bulk create operation, including created manual tax mappings and metadata.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkManualTaxMappingResult[];
    /** Metadata for the bulk create operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface ItemMetadata {
    /**
     * Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.
     * @format GUID
     */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action for this item was successful. When `false`, the `error` field is returned. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkManualTaxMappingResult {
    /** Metadata for the individual create operation. */
    itemMetadata?: ItemMetadata;
    /** Created manual tax mapping. Only present if `returnEntity` was set to `true` in the request. */
    item?: ManualTaxMapping;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface GetManualTaxMappingRequest {
    /**
     * Manual tax mapping ID.
     * @format GUID
     */
    manualTaxMappingId: string;
}
interface GetManualTaxMappingResponse {
    /** Retrieved manual tax mapping. */
    manualTaxMapping?: ManualTaxMapping;
}
interface UpdateManualTaxMappingRequest {
    /** Manual tax mapping to update. */
    manualTaxMapping: ManualTaxMapping;
}
interface UpdateManualTaxMappingResponse {
    /** Updated manual tax mapping. */
    manualTaxMapping?: ManualTaxMapping;
}
interface DeleteManualTaxMappingRequest {
    /**
     * Manual tax mapping ID.
     * @format GUID
     */
    manualTaxMappingId: string;
}
interface DeleteManualTaxMappingResponse {
}
interface QueryManualTaxMappingsRequest {
    /** Query options. */
    query?: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     *
     * Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object.
     *
     * Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
     * @maxSize 5
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Maximum number of items to return in the results.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface QueryManualTaxMappingsResponse {
    /**
     * Manual tax mappings that match the query criteria.
     * @maxSize 100
     */
    manualTaxMappings?: ManualTaxMapping[];
    /** Pagination metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in current page. */
    count?: number | null;
    /** Cursor strings that point to the next page, previous page, or both. */
    cursors?: Cursors;
    /**
     * Whether there are more pages to retrieve following the current page.
     *
     * + `true`: Another page of results can be retrieved.
     * + `false`: This is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface UpdateExtendedFieldsRequest {
    /** ID of the entity to update. */
    _id: string;
    /** Identifier for the app whose extended fields are being updated. */
    namespace: string;
    /** Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured. */
    namespaceData: Record<string, any> | null;
}
interface UpdateExtendedFieldsResponse {
    /** Updated manual tax mapping. */
    manualTaxMapping?: ManualTaxMapping;
}
interface BulkRestoreManualTaxMappingsRequest {
    /**
     * Manual tax mapping IDs to restore.
     * @format GUID
     * @minSize 1
     * @maxSize 200
     */
    manualTaxMappingIds?: string[];
}
interface BulkRestoreManualTaxMappingsResponse {
    /**
     * Restored manual tax mappings
     * @minSize 1
     * @maxSize 200
     */
    results?: BulkRestoreManualTaxMappingsResult[];
}
interface BulkRestoreManualTaxMappingsResult {
    itemMetadata?: ItemMetadata;
}
interface BulkAssignExemptOverridesRequest {
    /**
     * List of assign overrides.
     * @maxSize 50
     */
    assignOverrides?: AssignOverride[];
    /**
     * Whether to return the updated manual tax mapping entities in the response.
     * Default: `false`
     */
    returnEntity?: boolean;
}
interface AssignOverride {
    /**
     * ID of the manual tax mapping to which the exempt group override will be assigned.
     * @format GUID
     */
    manualTaxMappingId?: string;
    /**
     * List of exempt group overrides to assign to the manual tax mapping.
     * @minSize 1
     * @maxSize 50
     */
    exemptGroupOverrides?: ExemptGroupIdOverride[];
}
interface ExemptGroupIdOverride {
    /**
     * ID of the tax exempt group.
     * @format GUID
     */
    exemptGroupId?: string;
    /**
     * Override tax rate for the exempt group in the context of the manual tax mapping.
     * @format DECIMAL_VALUE
     * @decimalValue options { maxScale:3 }
     */
    rate?: string;
}
interface BulkAssignExemptOverridesResponse {
    /**
     * List of updated manual tax mappings.
     * @maxSize 50
     */
    results?: BulkExemptOverrideResult[];
    /** Metadata for the bulk assign operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkExemptOverrideResult {
    /** Metadata for the individual assign operation. */
    itemMetadata?: ItemMetadata;
    /** Updated manual tax mapping. Only present if `returnEntity` was set to `true` in the request. */
    item?: ManualTaxMapping;
}
interface BulkUnassignExemptOverridesRequest {
    /**
     * List of unassign overrides.
     * @maxSize 50
     */
    unassignOverrides: UnassignOverride[];
    /**
     * Whether to return the updated manual tax mapping entities in the response.
     * Default: `false`
     */
    returnEntity?: boolean;
}
interface UnassignOverride {
    /**
     * ID of the manual tax mapping from which the exempt group override will be unassigned.
     * @format GUID
     */
    manualTaxMappingId?: string;
    /**
     * List of exempt group IDs to unassign from the manual tax mapping.
     * @format GUID
     * @minSize 1
     * @maxSize 50
     */
    exemptGroupIds?: string[];
}
interface BulkUnassignExemptOverridesResponse {
    /**
     * List of updated manual tax mappings.
     * @maxSize 10
     */
    results?: BulkExemptOverrideResult[];
    /** Metadata for the bulk assign operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface ListManualTaxMappingsRequest {
}
interface ListManualTaxMappingsResponse {
    /**
     * List of manual tax mappings.
     * @maxSize 1000
     */
    manualTaxMappings?: ManualTaxMapping[];
    /** Pagination metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface Empty {
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
interface ManualTaxMappingCreatedEnvelope {
    entity: ManualTaxMapping;
    metadata: EventMetadata;
}
/**
 * Triggered when a manual tax mapping is created.
 * @permissionScope Manage Stores - all permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.MANAGE-STORES
 * @permissionScope Manage Stores
 * @permissionScopeId SCOPE.STORES.MANAGE-STORES
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope Manage eCommerce - all permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.MANAGE-ECOM
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-STORES.MANAGE-ORDERS
 * @permissionId BILLING.MANUAL_TAX_MAPPING_READ
 * @webhook
 * @eventType wix.billing.v1.manual_tax_mapping_created
 * @slug created
 */
declare function onManualTaxMappingCreated(handler: (event: ManualTaxMappingCreatedEnvelope) => void | Promise<void>): void;
interface ManualTaxMappingDeletedEnvelope {
    entity: ManualTaxMapping;
    metadata: EventMetadata;
}
/**
 * Triggered when a manual tax mapping is deleted.
 * @permissionScope Manage Stores - all permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.MANAGE-STORES
 * @permissionScope Manage Stores
 * @permissionScopeId SCOPE.STORES.MANAGE-STORES
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope Manage eCommerce - all permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.MANAGE-ECOM
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-STORES.MANAGE-ORDERS
 * @permissionId BILLING.MANUAL_TAX_MAPPING_READ
 * @webhook
 * @eventType wix.billing.v1.manual_tax_mapping_deleted
 * @slug deleted
 */
declare function onManualTaxMappingDeleted(handler: (event: ManualTaxMappingDeletedEnvelope) => void | Promise<void>): void;
interface ManualTaxMappingUpdatedEnvelope {
    entity: ManualTaxMapping;
    metadata: EventMetadata;
}
/**
 * Triggered when a manual tax mapping is updated.
 * @permissionScope Manage Stores - all permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.MANAGE-STORES
 * @permissionScope Manage Stores
 * @permissionScopeId SCOPE.STORES.MANAGE-STORES
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope Manage eCommerce - all permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.MANAGE-ECOM
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-STORES.MANAGE-ORDERS
 * @permissionId BILLING.MANUAL_TAX_MAPPING_READ
 * @webhook
 * @eventType wix.billing.v1.manual_tax_mapping_updated
 * @slug updated
 */
declare function onManualTaxMappingUpdated(handler: (event: ManualTaxMappingUpdatedEnvelope) => void | Promise<void>): void;
/**
 * Creates a manual tax mapping for a specific tax group and region combination.
 *
 * You must specify the tax group ID and tax region ID. The tax group and tax region
 * must already exist before creating the mapping.
 * To create multiple manual tax mappings in a single request, use [Bulk Create Manual Tax Mappings](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/extensions/tax/manual-tax-mappings/bulk-create-manual-tax-mappings).
 * @param manualTaxMapping - Manual tax mapping to create.
 * @public
 * @requiredField manualTaxMapping
 * @requiredField manualTaxMapping.taxGroupId
 * @requiredField manualTaxMapping.taxRate
 * @requiredField manualTaxMapping.taxRegionId
 * @permissionId BILLING.MANUAL_TAX_MAPPING_CREATE
 * @applicableIdentity APP
 * @returns Created manual tax mapping.
 * @fqn wix.billing.v1.ManualTaxMappingService.CreateManualTaxMapping
 */
declare function createManualTaxMapping(manualTaxMapping: NonNullablePaths<ManualTaxMapping, `taxGroupId` | `taxRate` | `taxRegionId`, 2>): Promise<NonNullablePaths<ManualTaxMapping, `taxRate` | `taxType` | `taxName` | `jurisdiction` | `jurisdictionType` | `exemptRates` | `exemptRates.${number}.taxRate`, 4>>;
/**
 * Creates up to 100 manual tax mappings in a single request.
 *
 * Use this method when you need to set up tax mappings for multiple tax group and region combinations at once,
 * such as when initially configuring tax rates for a new business or importing mappings from an external system.
 * @param manualTaxMappings - Manual tax mappings to create.
 * @public
 * @requiredField manualTaxMappings
 * @requiredField manualTaxMappings.taxGroupId
 * @requiredField manualTaxMappings.taxRate
 * @requiredField manualTaxMappings.taxRegionId
 * @permissionId BILLING.MANUAL_TAX_MAPPING_CREATE
 * @applicableIdentity APP
 * @fqn wix.billing.v1.ManualTaxMappingService.BulkCreateManualTaxMappings
 */
declare function bulkCreateManualTaxMappings(manualTaxMappings: NonNullablePaths<ManualTaxMapping, `taxGroupId` | `taxRate` | `taxRegionId`, 2>[], options?: BulkCreateManualTaxMappingsOptions): Promise<NonNullablePaths<BulkCreateManualTaxMappingsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.taxRate` | `results.${number}.item.taxType` | `results.${number}.item.taxName` | `results.${number}.item.jurisdiction` | `results.${number}.item.jurisdictionType` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkCreateManualTaxMappingsOptions {
    /**
     * Whether to return the created manual tax mapping entities in the response.
     *
     * Default: `false`
     */
    returnEntity?: boolean;
}
/**
 * Retrieves a manual tax mapping by ID.
 *
 * Use this method to get details about a specific manual tax mapping, including its tax rate,
 * jurisdiction information, and associated tax group and region.
 * @param manualTaxMappingId - Manual tax mapping ID.
 * @public
 * @requiredField manualTaxMappingId
 * @permissionId BILLING.MANUAL_TAX_MAPPING_READ
 * @applicableIdentity APP
 * @returns Retrieved manual tax mapping.
 * @fqn wix.billing.v1.ManualTaxMappingService.GetManualTaxMapping
 */
declare function getManualTaxMapping(manualTaxMappingId: string): Promise<NonNullablePaths<ManualTaxMapping, `taxRate` | `taxType` | `taxName` | `jurisdiction` | `jurisdictionType` | `exemptRates` | `exemptRates.${number}.taxRate`, 4>>;
/**
 * Updates an existing manual tax mapping.
 *
 * Use this method to modify the tax rate or other details of an existing manual tax mapping.
 * Each time the manual tax mapping is updated, `revision` increments by 1.
 * The current `revision` must be passed when updating the manual tax mapping.
 * This ensures you're working with the latest manual tax mapping and prevents unintended overwrites.
 * @param _id - Manual tax mapping ID.
 * @public
 * @requiredField _id
 * @requiredField manualTaxMapping
 * @requiredField manualTaxMapping.revision
 * @permissionId BILLING.MANUAL_TAX_MAPPING_UPDATE
 * @applicableIdentity APP
 * @returns Updated manual tax mapping.
 * @fqn wix.billing.v1.ManualTaxMappingService.UpdateManualTaxMapping
 */
declare function updateManualTaxMapping(_id: string, manualTaxMapping: NonNullablePaths<UpdateManualTaxMapping, `revision`, 2>): Promise<NonNullablePaths<ManualTaxMapping, `taxRate` | `taxType` | `taxName` | `jurisdiction` | `jurisdictionType` | `exemptRates` | `exemptRates.${number}.taxRate`, 4>>;
interface UpdateManualTaxMapping {
    /**
     * Manual tax mapping ID.
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * Revision number, which increments by 1 each time the manual tax mapping is updated.
     * To prevent conflicting changes, the current revision must be passed when updating the manual tax mapping.
     *
     * Ignored when creating a manual tax mapping.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the manual tax mapping was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the manual tax mapping was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Tax group ID. Tax groups define collections of items or services that share the same tax treatment.
     * @format GUID
     */
    taxGroupId?: string | null;
    /**
     * Tax region ID. Tax regions represent geographical areas where specific tax rules apply.
     * @format GUID
     */
    taxRegionId?: string | null;
    /**
     * Tax rate to apply for this tax group and region combination. Specified as a decimal value (for example, `0.080000` for 8%).
     * @decimalValue options { maxScale:6, gte:0 }
     */
    taxRate?: string;
    /**
     * Description of the manual tax mapping. Use this field to add context or notes about the tax rate.
     * @maxLength 1000
     */
    description?: string | null;
    /**
     * Type of tax being applied, such as `VAT`, `GST`, or `Sales Tax`. This depends on the jurisdiction's tax laws.
     * @maxLength 200
     */
    taxType?: string;
    /**
     * Name of the tax being applied. For example, `STATE TAX` or `GST`.
     * @maxLength 200
     */
    taxName?: string;
    /**
     * Name of the jurisdiction where this tax rate applies. For example, `California` or `United Kingdom`.
     * @maxLength 200
     */
    jurisdiction?: string;
    /** Type of jurisdiction where this tax rate applies, such as country, state, county, city, or special district. */
    jurisdictionType?: JurisdictionTypeWithLiterals;
    /**
     * Tax exemption details for user-defined exempt groups.
     * @maxSize 50
     */
    exemptRates?: ExemptDetails[];
    /** Extended fields for storing additional custom data. */
    extendedFields?: ExtendedFields;
}
/**
 * Deletes a manual tax mapping.
 *
 * This permanently removes the custom tax rate mapping.
 * After deletion, no tax will be calculated for the associated tax group and region combination.
 * @param manualTaxMappingId - Manual tax mapping ID.
 * @public
 * @requiredField manualTaxMappingId
 * @permissionId BILLING.MANUAL_TAX_MAPPING_DELETE
 * @applicableIdentity APP
 * @fqn wix.billing.v1.ManualTaxMappingService.DeleteManualTaxMapping
 */
declare function deleteManualTaxMapping(manualTaxMappingId: string): Promise<void>;
/**
 * Retrieves a list of manual tax mappings based on the provided query criteria.
 *
 * Use this method to find manual tax mappings by tax group, tax region, or other filter criteria.
 * You can also sort and paginate the results. This is useful for managing large numbers of tax mappings
 * or finding mappings that apply to specific regions or tax groups.
 *
 * To learn how to query manual tax mappings, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language).
 * @public
 * @permissionId BILLING.MANUAL_TAX_MAPPING_READ
 * @applicableIdentity APP
 * @fqn wix.billing.v1.ManualTaxMappingService.QueryManualTaxMappings
 */
declare function queryManualTaxMappings(): ManualTaxMappingsQueryBuilder;
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface ManualTaxMappingsQueryResult extends QueryCursorResult {
    items: ManualTaxMapping[];
    query: ManualTaxMappingsQueryBuilder;
    next: () => Promise<ManualTaxMappingsQueryResult>;
    prev: () => Promise<ManualTaxMappingsQueryResult>;
}
interface ManualTaxMappingsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    eq: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'jurisdictionType' | 'exemptRates.taxExemptGroupId', value: any) => ManualTaxMappingsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ne: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'jurisdictionType' | 'exemptRates.taxExemptGroupId', value: any) => ManualTaxMappingsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ge: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'exemptRates.taxExemptGroupId', value: any) => ManualTaxMappingsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    gt: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'exemptRates.taxExemptGroupId', value: any) => ManualTaxMappingsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    le: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'exemptRates.taxExemptGroupId', value: any) => ManualTaxMappingsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    lt: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'exemptRates.taxExemptGroupId', value: any) => ManualTaxMappingsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `string`.
     * @param string - String to compare against. Case-insensitive.
     */
    startsWith: (propertyName: '_id' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'exemptRates.taxExemptGroupId', value: string) => ManualTaxMappingsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `values`.
     * @param values - List of values to compare against.
     */
    hasSome: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'jurisdictionType' | 'exemptRates.taxExemptGroupId', value: any[]) => ManualTaxMappingsQueryBuilder;
    in: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'jurisdictionType' | 'exemptRates.taxExemptGroupId', value: any) => ManualTaxMappingsQueryBuilder;
    exists: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'taxGroupId' | 'taxRegionId' | 'taxRate' | 'description' | 'taxType' | 'taxName' | 'jurisdiction' | 'jurisdictionType' | 'exemptRates.taxExemptGroupId', value: boolean) => ManualTaxMappingsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    ascending: (...propertyNames: Array<'_id' | '_createdDate' | '_updatedDate' | 'taxRate' | 'taxType' | 'taxName' | 'jurisdiction' | 'jurisdictionType'>) => ManualTaxMappingsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    descending: (...propertyNames: Array<'_id' | '_createdDate' | '_updatedDate' | 'taxRate' | 'taxType' | 'taxName' | 'jurisdiction' | 'jurisdictionType'>) => ManualTaxMappingsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object. */
    limit: (limit: number) => ManualTaxMappingsQueryBuilder;
    /** @param cursor - A pointer to specific record */
    skipTo: (cursor: string) => ManualTaxMappingsQueryBuilder;
    find: () => Promise<ManualTaxMappingsQueryResult>;
}
/**
 * @hidden
 * @fqn wix.billing.v1.ManualTaxMappingService.QueryManualTaxMappings
 * @requiredField query
 */
declare function typedQueryManualTaxMappings(query: CursorQuery): Promise<NonNullablePaths<QueryManualTaxMappingsResponse, `manualTaxMappings` | `manualTaxMappings.${number}.taxRate` | `manualTaxMappings.${number}.taxType` | `manualTaxMappings.${number}.taxName` | `manualTaxMappings.${number}.jurisdiction` | `manualTaxMappings.${number}.jurisdictionType`, 4>>;
/**
 * Updates extended fields of a manual tax mapping.
 *
 * Use this method to modify extended field data associated with a manual tax mapping.
 * Unlike the standard update operation, this does not increment the revision number.
 * @param _id - ID of the entity to update.
 * @param namespace - Identifier for the app whose extended fields are being updated.
 * @public
 * @requiredField _id
 * @requiredField namespace
 * @requiredField options
 * @requiredField options.namespaceData
 * @permissionId BILLING.MANUAL_TAX_MAPPING_UPDATE
 * @applicableIdentity APP
 * @fqn wix.billing.v1.ManualTaxMappingService.UpdateExtendedFields
 */
declare function updateExtendedFields(_id: string, namespace: string, options: NonNullablePaths<UpdateExtendedFieldsOptions, `namespaceData`, 2>): Promise<NonNullablePaths<UpdateExtendedFieldsResponse, `manualTaxMapping.taxRate` | `manualTaxMapping.taxType` | `manualTaxMapping.taxName` | `manualTaxMapping.jurisdiction` | `manualTaxMapping.jurisdictionType` | `manualTaxMapping.exemptRates` | `manualTaxMapping.exemptRates.${number}.taxRate`, 5>>;
interface UpdateExtendedFieldsOptions {
    /** Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured. */
    namespaceData: Record<string, any> | null;
}
/**
 * Assigns tax exempt overrides to multiple manual tax mappings in a single request.
 * @public
 * @requiredField options.assignOverrides.exemptGroupOverrides
 * @requiredField options.assignOverrides.exemptGroupOverrides.exemptGroupId
 * @requiredField options.assignOverrides.exemptGroupOverrides.rate
 * @requiredField options.assignOverrides.manualTaxMappingId
 * @permissionId BILLING.MANUAL_TAX_MAPPING_UPDATE
 * @applicableIdentity APP
 * @fqn wix.billing.v1.ManualTaxMappingService.BulkAssignExemptOverrides
 */
declare function bulkAssignExemptOverrides(options?: NonNullablePaths<BulkAssignExemptOverridesOptions, `assignOverrides.${number}.exemptGroupOverrides` | `assignOverrides.${number}.exemptGroupOverrides.${number}.exemptGroupId` | `assignOverrides.${number}.exemptGroupOverrides.${number}.rate` | `assignOverrides.${number}.manualTaxMappingId`, 6>): Promise<NonNullablePaths<BulkAssignExemptOverridesResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.taxRate` | `results.${number}.item.taxType` | `results.${number}.item.taxName` | `results.${number}.item.jurisdiction` | `results.${number}.item.jurisdictionType` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkAssignExemptOverridesOptions {
    /**
     * List of assign overrides.
     * @maxSize 50
     */
    assignOverrides?: AssignOverride[];
    /**
     * Whether to return the updated manual tax mapping entities in the response.
     * Default: `false`
     */
    returnEntity?: boolean;
}
/**
 * Unassigns tax exempt overrides to multiple manual tax mappings in a single request.
 * @param unassignOverrides - List of unassign overrides.
 * @public
 * @requiredField unassignOverrides
 * @requiredField unassignOverrides.manualTaxMappingId
 * @permissionId BILLING.MANUAL_TAX_MAPPING_UPDATE
 * @applicableIdentity APP
 * @fqn wix.billing.v1.ManualTaxMappingService.BulkUnassignExemptOverrides
 */
declare function bulkUnassignExemptOverrides(unassignOverrides: NonNullablePaths<UnassignOverride, `manualTaxMappingId`, 2>[], options?: BulkUnassignExemptOverridesOptions): Promise<NonNullablePaths<BulkUnassignExemptOverridesResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.taxRate` | `results.${number}.item.taxType` | `results.${number}.item.taxName` | `results.${number}.item.jurisdiction` | `results.${number}.item.jurisdictionType` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkUnassignExemptOverridesOptions {
    /**
     * Whether to return the updated manual tax mapping entities in the response.
     * Default: `false`
     */
    returnEntity?: boolean;
}
/**
 * Retrieves up to 1000 manual tax mappings.
 * The returned manual tax mappings are sorted by the `createdDate` field in ascending order.
 * @public
 * @permissionId BILLING.MANUAL_TAX_MAPPING_READ
 * @applicableIdentity APP
 * @fqn wix.billing.v1.ManualTaxMappingService.ListManualTaxMappings
 */
declare function listManualTaxMappings(): Promise<NonNullablePaths<ListManualTaxMappingsResponse, `manualTaxMappings` | `manualTaxMappings.${number}.taxRate` | `manualTaxMappings.${number}.taxType` | `manualTaxMappings.${number}.taxName` | `manualTaxMappings.${number}.jurisdiction` | `manualTaxMappings.${number}.jurisdictionType`, 4>>;

export { type ActionEvent, type ApplicationError, type AssignOverride, type BaseEventMetadata, type BulkActionMetadata, type BulkAssignExemptOverridesOptions, type BulkAssignExemptOverridesRequest, type BulkAssignExemptOverridesResponse, type BulkCreateManualTaxMappingsOptions, type BulkCreateManualTaxMappingsRequest, type BulkCreateManualTaxMappingsResponse, type BulkExemptOverrideResult, type BulkManualTaxMappingResult, type BulkRestoreManualTaxMappingsRequest, type BulkRestoreManualTaxMappingsResponse, type BulkRestoreManualTaxMappingsResult, type BulkUnassignExemptOverridesOptions, type BulkUnassignExemptOverridesRequest, type BulkUnassignExemptOverridesResponse, type CreateManualTaxMappingRequest, type CreateManualTaxMappingResponse, type CursorPaging, type CursorPagingMetadata, type CursorQuery, type CursorQueryPagingMethodOneOf, type Cursors, type DeleteManualTaxMappingRequest, type DeleteManualTaxMappingResponse, type DomainEvent, type DomainEventBodyOneOf, type Empty, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type EventMetadata, type ExemptDetails, type ExemptGroupIdOverride, type ExtendedFields, type GetManualTaxMappingRequest, type GetManualTaxMappingResponse, type IdentificationData, type IdentificationDataIdOneOf, type ItemMetadata, JurisdictionType, type JurisdictionTypeWithLiterals, type ListManualTaxMappingsRequest, type ListManualTaxMappingsResponse, type ManualTaxMapping, type ManualTaxMappingCreatedEnvelope, type ManualTaxMappingDeletedEnvelope, type ManualTaxMappingUpdatedEnvelope, type ManualTaxMappingsQueryBuilder, type ManualTaxMappingsQueryResult, type MessageEnvelope, type QueryManualTaxMappingsRequest, type QueryManualTaxMappingsResponse, type RestoreInfo, SortOrder, type SortOrderWithLiterals, type Sorting, type UnassignOverride, type UpdateExtendedFieldsOptions, type UpdateExtendedFieldsRequest, type UpdateExtendedFieldsResponse, type UpdateManualTaxMapping, type UpdateManualTaxMappingRequest, type UpdateManualTaxMappingResponse, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, bulkAssignExemptOverrides, bulkCreateManualTaxMappings, bulkUnassignExemptOverrides, createManualTaxMapping, deleteManualTaxMapping, getManualTaxMapping, listManualTaxMappings, onManualTaxMappingCreated, onManualTaxMappingDeleted, onManualTaxMappingUpdated, queryManualTaxMappings, typedQueryManualTaxMappings, updateExtendedFields, updateManualTaxMapping };
