import { NonNullablePaths } from '@wix/sdk-types';

interface AvailabilityException {
    /**
     * Availability exception ID.
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * Revision number, which increments by 1 each time the availability exception is updated.
     * To prevent conflicting changes, the current revision must be specified when updating the availability exception.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the availability exception was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the availability exception was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.
     * @readonly
     */
    _updatedDate?: Date | null;
    /** The start time of the availability exception in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format. */
    startTime?: Date | null;
    /** The end time of the availability exception in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format. */
    endTime?: Date | null;
    /**
     * Whether the exception makes the [`start_time`, `end_time`] range available.
     * If `true`, the exception makes the restaurant available for online ordering during the time range. If `false`, the exception makes the restaurant unavailable for ordering during the time range.
     * Currently, only `false` is supported.
     * @readonly
     */
    available?: boolean | null;
    /**
     * Exception name.
     * @maxLength 500
     */
    name?: string | null;
    /** Fulfillment methods for which this exception applies. */
    affectedFulfillmentMethods?: AffectedFulfillmentMethods;
    /**
     * ID of the restaurant operation this exception is associated with. (See the Restaurants Operations API for more information.)
     * @format GUID
     */
    operationId?: string | null;
    /** Extended fields. */
    extendedFields?: ExtendedFields;
    /**
     * Business location ID ([SDK](https://dev.wix.com/docs/sdk/backend-modules/restaurants/wix-restaurants-new/about-business-locations) | [REST](https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/about-business-locations)) of the operation this availability exception applies to.
     * @format GUID
     * @readonly
     */
    businessLocationId?: string | null;
    /** Tags ([SDK](https://dev.wix.com/docs/sdk/backend-modules/tags/tags/introduction) | [REST](https://dev.wix.com/docs/rest/business-management/tags/introduction)) used to classify and sort different types of availability exceptions. */
    tags?: Tags;
}
interface AffectedFulfillmentMethods extends AffectedFulfillmentMethodsAffectedMethodsOptionsOneOf {
    /** Settings for specific fulfillment methods. */
    specificFulfillmentMethodsOptions?: SpecificFulfillmentMethodsImpactScope;
    /** The type of the fulfillment methods this exception applies to. */
    affectedMethods?: AffectedMethodsWithLiterals;
}
/** @oneof */
interface AffectedFulfillmentMethodsAffectedMethodsOptionsOneOf {
    /** Settings for specific fulfillment methods. */
    specificFulfillmentMethodsOptions?: SpecificFulfillmentMethodsImpactScope;
}
/** The scope the exception applied. */
declare enum AffectedMethods {
    /** Unknown impact scope. */
    UNKNOWN_AFFECTED_METHODS = "UNKNOWN_AFFECTED_METHODS",
    /** All fulfillment methods of this operation applied for this exception. */
    ALL_FULFILLMENT_METHODS = "ALL_FULFILLMENT_METHODS",
    /** Specific fulfillment methods applied for this exception. */
    SPECIFIC_FULFILLMENT_METHODS = "SPECIFIC_FULFILLMENT_METHODS",
    /** All pickup fulfillment methods applied for this exception. */
    ALL_PICKUP_FULFILLMENT_METHODS = "ALL_PICKUP_FULFILLMENT_METHODS",
    /** All delivery fulfillment methods applied for this exception. */
    ALL_DELIVERY_FULFILLMENT_METHODS = "ALL_DELIVERY_FULFILLMENT_METHODS"
}
/** @enumType */
type AffectedMethodsWithLiterals = AffectedMethods | 'UNKNOWN_AFFECTED_METHODS' | 'ALL_FULFILLMENT_METHODS' | 'SPECIFIC_FULFILLMENT_METHODS' | 'ALL_PICKUP_FULFILLMENT_METHODS' | 'ALL_DELIVERY_FULFILLMENT_METHODS';
/** Details of the specific fulfillment methods this exception applies to. */
interface SpecificFulfillmentMethodsImpactScope {
    /**
     * IDs of the fulfillment methods this exception applies to.
     * @format GUID
     * @maxSize 500
     */
    fulfillmentMethodIds?: string[] | null;
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
declare enum Type {
    /** Unspecified Availability exception type. */
    UNSPECIFIED_AVAILABILITY_EXCEPTION_TYPE = "UNSPECIFIED_AVAILABILITY_EXCEPTION_TYPE",
    /** Ordering paused. */
    ORDERING_PAUSED = "ORDERING_PAUSED",
    /** The restaurant may be closed completely or only partially. For example, a restaurant may be closed only for pickup orders, only for delivery orders, or only for orders from a specific delivery area. */
    CLOSURE = "CLOSURE"
}
/** @enumType */
type TypeWithLiterals = Type | 'UNSPECIFIED_AVAILABILITY_EXCEPTION_TYPE' | 'ORDERING_PAUSED' | 'CLOSURE';
/**
 * Common object for tags.
 * Should be use as in this example:
 * message Foo {
 * string id = 1;
 * ...
 * Tags tags = 5
 * }
 * example of taggable entity
 * {
 * id: "123"
 * tags: {
 * tags: {
 * tag_ids:["11","22"]
 * },
 * private_tags: {
 * tag_ids: ["33", "44"]
 * }
 * }
 * }
 */
interface Tags {
    /** Tags that require an additional permission in order to access them, normally not given to site members or visitors. */
    privateTags?: TagList;
    /** Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors. */
    tags?: TagList;
}
interface TagList {
    /**
     * List of tag IDs
     * @maxSize 100
     * @maxLength 5
     */
    tagIds?: string[];
}
interface CreateAvailabilityExceptionRequest {
    /** Availability exception details. */
    availabilityException: AvailabilityException;
}
interface CreateAvailabilityExceptionResponse {
    /** The created availability exception. */
    availabilityException?: AvailabilityException;
}
interface GetAvailabilityExceptionRequest {
    /**
     * ID of the availability exception to retrieve.
     * @format GUID
     */
    availabilityExceptionId: string;
}
interface GetAvailabilityExceptionResponse {
    /** The requested availability exception. */
    availabilityException?: AvailabilityException;
}
interface UpdateAvailabilityExceptionRequest {
    /** Availability exception to update. */
    availabilityException: AvailabilityException;
}
interface UpdateAvailabilityExceptionResponse {
    /** Updated availability exception. */
    availabilityException?: AvailabilityException;
}
interface DeleteAvailabilityExceptionRequest {
    /**
     * ID of the availability exception to delete.
     * @format GUID
     */
    availabilityExceptionId: string;
}
interface DeleteAvailabilityExceptionResponse {
}
interface QueryAvailabilityExceptionsRequest {
    /** WQL expression. */
    query?: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object in the following format:
     * `"filter" : {
     * "fieldName1": "value1",
     * "fieldName2":{"$operator":"value2"}
     * }`
     * Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the following format:
     * `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`
     * @maxSize 4
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 64
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * You can get the relevant cursor token
     * from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface QueryAvailabilityExceptionsResponse {
    /** List of availability exceptions. */
    availabilityExceptions?: AvailabilityException[];
    /** Paging metadata */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    cursors?: Cursors;
    /**
     * Indicates if there are more results after the current page.
     * If `true`, another page of results can be retrieved.
     * If `false`, this is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor pointing to next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface BulkCreateAvailabilityExceptionsRequest {
    /**
     * Availability exception details.
     * @minSize 1
     * @maxSize 20
     */
    availabilityExceptions?: AvailabilityException[];
    /** Whether the full availability exception entity is returned. Defaults to `true`. */
    returnEntity?: boolean;
}
interface BulkCreateAvailabilityExceptionsResponse {
    /** Information about the created availability exception. */
    results?: BulkCreateAvailabilityExceptionsResult[];
    /** Metadata for the API call. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkCreateAvailabilityExceptionsResult {
    /** Metadata for availability exception creation. */
    itemMetadata?: ItemMetadata;
    /** Created availability exception. */
    availabilityException?: AvailabilityException;
}
interface ItemMetadata {
    /**
     * Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).
     * @format GUID
     */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface BulkUpdateAvailabilityExceptionsRequest {
    /**
     * Availability exceptions entities to update.
     * @minSize 1
     * @maxSize 100
     */
    availabilityExceptions: MaskedAvailabilityException[];
    /** Whether to receive the entity in the response. */
    returnEntity?: boolean;
}
interface MaskedAvailabilityException {
    /** Availability exception entity to update. */
    availabilityException?: AvailabilityException;
    /** Explicit list of fields to update. */
    mask?: string[];
}
interface BulkUpdateAvailabilityExceptionsResponse {
    /** Information about the updated availability exceptions. */
    results?: BulkAvailabilityExceptionResult[];
    /** Bulk Update Availability Exceptions metadata. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkAvailabilityExceptionResult {
    /** Metadata of the availability exception update. */
    itemMetadata?: ItemMetadata;
    /** Availability exception (present when return_entity is true). */
    item?: AvailabilityException;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface Empty {
}
interface BulkUpdateAvailabilityExceptionTagsRequest {
    /**
     * IDs of the availability exceptions to update tags for.
     * @format GUID
     * @maxSize 100
     */
    availabilityExceptionIds: string[];
    /** Tags to assign to the availability exceptions. */
    assignTags?: Tags;
    /** Tags to unassign from the availability exceptions. */
    unassignTags?: Tags;
}
interface BulkUpdateAvailabilityExceptionTagsResponse {
    /**
     * Results of the bulk update.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateAvailabilityExceptionTagsResult[];
    /** Metadata for the bulk update. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUpdateAvailabilityExceptionTagsResult {
    /** Metadata for the updated availability exception. */
    itemMetadata?: ItemMetadata;
}
interface BulkUpdateAvailabilityExceptionTagsByFilterRequest {
    /** Filter that determines which availability exceptions to update tags for. */
    filter: Record<string, any> | null;
    /** Tags to assign to the availability exceptions. */
    assignTags?: Tags;
    /** Tags to unassign from the availability exceptions. */
    unassignTags?: Tags;
}
interface BulkUpdateAvailabilityExceptionTagsByFilterResponse {
    /**
     * Job ID. Pass this ID to Get Async Job ([SDK](https://dev.wix.com/docs/sdk/backend-modules/async-jobs/get-async-job) | [REST](https://dev.wix.com/docs/rest/business-management/async-job/get-async-job)) to track the job's status.
     * @format GUID
     */
    jobId?: string;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
/** @docsIgnore */
type BulkUpdateAvailabilityExceptionTagsApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type BulkUpdateAvailabilityExceptionTagsByFilterApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
interface AvailabilityExceptionCreatedEnvelope {
    entity: AvailabilityException;
    metadata: EventMetadata;
}
/**
 * Triggered when an availability exception is created.
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_READ
 * @webhook
 * @eventType wix.restaurants.v1.availability_exception_created
 * @slug created
 * @documentationMaturity preview
 */
declare function onAvailabilityExceptionCreated(handler: (event: AvailabilityExceptionCreatedEnvelope) => void | Promise<void>): void;
interface AvailabilityExceptionDeletedEnvelope {
    entity: AvailabilityException;
    metadata: EventMetadata;
}
/**
 * Triggered when an availability exception is deleted.
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_READ
 * @webhook
 * @eventType wix.restaurants.v1.availability_exception_deleted
 * @serviceIdentifier wix.restaurants.v1.AvailabilityExceptionsService
 * @slug deleted
 * @documentationMaturity preview
 */
declare function onAvailabilityExceptionDeleted(handler: (event: AvailabilityExceptionDeletedEnvelope) => void | Promise<void>): void;
interface AvailabilityExceptionUpdatedEnvelope {
    entity: AvailabilityException;
    metadata: EventMetadata;
}
/**
 * Triggered when an availability exception is updated.
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_READ
 * @webhook
 * @eventType wix.restaurants.v1.availability_exception_updated
 * @slug updated
 * @documentationMaturity preview
 */
declare function onAvailabilityExceptionUpdated(handler: (event: AvailabilityExceptionUpdatedEnvelope) => void | Promise<void>): void;
/**
 * Creates a new availability exception.
 * @param availabilityException - Availability exception details.
 * @public
 * @documentationMaturity preview
 * @requiredField availabilityException
 * @requiredField availabilityException.affectedFulfillmentMethods
 * @requiredField availabilityException.affectedFulfillmentMethods.affectedMethods
 * @requiredField availabilityException.available
 * @requiredField availabilityException.endTime
 * @requiredField availabilityException.operationId
 * @requiredField availabilityException.startTime
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_CREATE
 * @applicableIdentity APP
 * @returns The created availability exception.
 * @fqn wix.restaurants.v1.AvailabilityExceptionsService.CreateAvailabilityException
 */
declare function createAvailabilityException(availabilityException: NonNullablePaths<AvailabilityException, `affectedFulfillmentMethods` | `affectedFulfillmentMethods.affectedMethods` | `available` | `endTime` | `operationId` | `startTime`, 3>): Promise<NonNullablePaths<AvailabilityException, `affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds` | `affectedFulfillmentMethods.affectedMethods` | `tags.privateTags.tagIds`, 4>>;
/**
 * Retrieves an availability exception.
 * @param availabilityExceptionId - ID of the availability exception to retrieve.
 * @public
 * @documentationMaturity preview
 * @requiredField availabilityExceptionId
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_READ
 * @applicableIdentity APP
 * @returns The requested availability exception.
 * @fqn wix.restaurants.v1.AvailabilityExceptionsService.GetAvailabilityException
 */
declare function getAvailabilityException(availabilityExceptionId: string): Promise<NonNullablePaths<AvailabilityException, `affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds` | `affectedFulfillmentMethods.affectedMethods` | `tags.privateTags.tagIds`, 4>>;
/**
 * Updates an availability exception.
 *
 * Each time the availability exception is updated, `revision` increments by 1.
 * The current `revision` must be passed when updating the availability exception.
 * This ensures you're working with the latest availability exception and prevents unintended overwrites.
 * @param _id - Availability exception ID.
 * @public
 * @documentationMaturity preview
 * @requiredField _id
 * @requiredField availabilityException
 * @requiredField availabilityException.revision
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_UPDATE
 * @applicableIdentity APP
 * @returns Updated availability exception.
 * @fqn wix.restaurants.v1.AvailabilityExceptionsService.UpdateAvailabilityException
 */
declare function updateAvailabilityException(_id: string, availabilityException: NonNullablePaths<UpdateAvailabilityException, `revision`, 2>): Promise<NonNullablePaths<AvailabilityException, `affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds` | `affectedFulfillmentMethods.affectedMethods` | `tags.privateTags.tagIds`, 4>>;
interface UpdateAvailabilityException {
    /**
     * Availability exception ID.
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * Revision number, which increments by 1 each time the availability exception is updated.
     * To prevent conflicting changes, the current revision must be specified when updating the availability exception.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the availability exception was created in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the availability exception was last updated in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format.
     * @readonly
     */
    _updatedDate?: Date | null;
    /** The start time of the availability exception in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format. */
    startTime?: Date | null;
    /** The end time of the availability exception in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#combined_date_and_time_representations) format. */
    endTime?: Date | null;
    /**
     * Whether the exception makes the [`start_time`, `end_time`] range available.
     * If `true`, the exception makes the restaurant available for online ordering during the time range. If `false`, the exception makes the restaurant unavailable for ordering during the time range.
     * Currently, only `false` is supported.
     * @readonly
     */
    available?: boolean | null;
    /**
     * Exception name.
     * @maxLength 500
     */
    name?: string | null;
    /** Fulfillment methods for which this exception applies. */
    affectedFulfillmentMethods?: AffectedFulfillmentMethods;
    /**
     * ID of the restaurant operation this exception is associated with. (See the Restaurants Operations API for more information.)
     * @format GUID
     */
    operationId?: string | null;
    /** Extended fields. */
    extendedFields?: ExtendedFields;
    /**
     * Business location ID ([SDK](https://dev.wix.com/docs/sdk/backend-modules/restaurants/wix-restaurants-new/about-business-locations) | [REST](https://dev.wix.com/docs/rest/business-solutions/restaurants/wix-restaurants-new/about-business-locations)) of the operation this availability exception applies to.
     * @format GUID
     * @readonly
     */
    businessLocationId?: string | null;
    /** Tags ([SDK](https://dev.wix.com/docs/sdk/backend-modules/tags/tags/introduction) | [REST](https://dev.wix.com/docs/rest/business-management/tags/introduction)) used to classify and sort different types of availability exceptions. */
    tags?: Tags;
}
/**
 * Deletes an availability exception.
 * @param availabilityExceptionId - ID of the availability exception to delete.
 * @public
 * @documentationMaturity preview
 * @requiredField availabilityExceptionId
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_DELETE
 * @applicableIdentity APP
 * @fqn wix.restaurants.v1.AvailabilityExceptionsService.DeleteAvailabilityException
 */
declare function deleteAvailabilityException(availabilityExceptionId: string): Promise<void>;
/**
 * Creates a query to retrieve a list of availability exceptions.
 *
 * The `queryAvailabilityExceptions()` function builds a query to retrieve a list of availability exceptions and returns an `AvailabilityExceptionsQueryBuilder` object.
 *
 * The returned object contains the query definition, which is used to run the query using the [`find()`](/availability-exceptions/availability-exceptions-query-builder/find) function.
 *
 * You can refine the query by chaining `AvailabilityExceptionsQueryBuilder` functions onto the query. `AvailabilityExceptionsQueryBuilder` functions enable you to filter, sort, and control the results that `queryAvailabilityExceptions()` returns.
 *
 * `queryAvailabilityExceptions()` runs with the following `AvailabilityExceptionsQueryBuilder` defaults, which you can override:
 *
 * - [`limit(50)`](/availability-exceptions/availability-exceptions-query-builder/limit)
 * - [`ascending('_id')`](/availability-exceptions/availability-exceptions-query-builder/ascending)
 *
 * The following `AvailabilityExceptionsQueryBuilder` functions are supported for `queryAvailabilityExceptions()`. For a full description of the availability exception object, see the object returned for the [`items`](/availability-exceptions/availability-exceptions-query-result/items) property in `AvailabilityExceptionsQueryResult`.
 * @public
 * @documentationMaturity preview
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_READ
 * @applicableIdentity APP
 * @fqn wix.restaurants.v1.AvailabilityExceptionsService.QueryAvailabilityExceptions
 */
declare function queryAvailabilityExceptions(): AvailabilityExceptionsQueryBuilder;
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface AvailabilityExceptionsQueryResult extends QueryCursorResult {
    items: AvailabilityException[];
    query: AvailabilityExceptionsQueryBuilder;
    next: () => Promise<AvailabilityExceptionsQueryResult>;
    prev: () => Promise<AvailabilityExceptionsQueryResult>;
}
interface AvailabilityExceptionsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    eq: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'available' | 'name' | 'affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds' | 'affectedFulfillmentMethods.affectedMethods', value: any) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ne: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'available' | 'name' | 'affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds' | 'affectedFulfillmentMethods.affectedMethods', value: any) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ge: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'name', value: any) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    gt: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'name', value: any) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    le: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'name', value: any) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    lt: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'name', value: any) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `string`.
     * @param string - String to compare against. Case-insensitive.
     * @documentationMaturity preview
     */
    startsWith: (propertyName: '_id' | 'name', value: string) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `values`.
     * @param values - List of values to compare against.
     * @documentationMaturity preview
     */
    hasSome: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'available' | 'name' | 'affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds' | 'affectedFulfillmentMethods.affectedMethods', value: any[]) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `values`.
     * @param values - List of values to compare against.
     * @documentationMaturity preview
     */
    hasAll: (propertyName: 'affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds', value: any[]) => AvailabilityExceptionsQueryBuilder;
    /** @documentationMaturity preview */
    in: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'available' | 'name' | 'affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds' | 'affectedFulfillmentMethods.affectedMethods', value: any) => AvailabilityExceptionsQueryBuilder;
    /** @documentationMaturity preview */
    exists: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'available' | 'name' | 'affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds' | 'affectedFulfillmentMethods.affectedMethods', value: boolean) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    ascending: (...propertyNames: Array<'_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'available' | 'name' | 'affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds' | 'affectedFulfillmentMethods.affectedMethods'>) => AvailabilityExceptionsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    descending: (...propertyNames: Array<'_id' | '_createdDate' | '_updatedDate' | 'startTime' | 'endTime' | 'available' | 'name' | 'affectedFulfillmentMethods.specificFulfillmentMethodsOptions.fulfillmentMethodIds' | 'affectedFulfillmentMethods.affectedMethods'>) => AvailabilityExceptionsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object.
     * @documentationMaturity preview
     */
    limit: (limit: number) => AvailabilityExceptionsQueryBuilder;
    /** @param cursor - A pointer to specific record
     * @documentationMaturity preview
     */
    skipTo: (cursor: string) => AvailabilityExceptionsQueryBuilder;
    /** @documentationMaturity preview */
    find: () => Promise<AvailabilityExceptionsQueryResult>;
}
/**
 * Updates multiple availability exceptions.
 * @param availabilityExceptions - Availability exceptions entities to update.
 * @public
 * @documentationMaturity preview
 * @requiredField availabilityExceptions
 * @requiredField availabilityExceptions.availabilityException._id
 * @requiredField availabilityExceptions.availabilityException.revision
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_UPDATE
 * @applicableIdentity APP
 * @fqn wix.restaurants.v1.AvailabilityExceptionsService.BulkUpdateAvailabilityExceptions
 */
declare function bulkUpdateAvailabilityExceptions(availabilityExceptions: NonNullablePaths<MaskedAvailabilityException, `availabilityException._id` | `availabilityException.revision`, 3>[], options?: BulkUpdateAvailabilityExceptionsOptions): Promise<NonNullablePaths<BulkUpdateAvailabilityExceptionsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.affectedFulfillmentMethods.affectedMethods` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkUpdateAvailabilityExceptionsOptions {
    /** Whether to receive the entity in the response. */
    returnEntity?: boolean;
}
/**
 * Synchronously update tags on multiple availability exceptions.
 * If you specify a tag in both `assignTags` and `unassignTags`, it is assigned.
 * @param availabilityExceptionIds - IDs of the availability exceptions to update tags for.
 * @public
 * @documentationMaturity preview
 * @requiredField availabilityExceptionIds
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_UPDATE_TAGS
 * @applicableIdentity APP
 * @fqn wix.restaurants.v1.AvailabilityExceptionsService.BulkUpdateAvailabilityExceptionTags
 */
declare function bulkUpdateAvailabilityExceptionTags(availabilityExceptionIds: string[], options?: BulkUpdateAvailabilityExceptionTagsOptions): Promise<NonNullablePaths<BulkUpdateAvailabilityExceptionTagsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6> & {
    __applicationErrorsType?: BulkUpdateAvailabilityExceptionTagsApplicationErrors;
}>;
interface BulkUpdateAvailabilityExceptionTagsOptions {
    /** Tags to assign to the availability exceptions. */
    assignTags?: Tags;
    /** Tags to unassign from the availability exceptions. */
    unassignTags?: Tags;
}
/**
 * Asynchronously update tags on multiple availability exceptions according to the specified filter.
 * If a filter isn't specified, this method updates all availability exceptions.
 * If you specify a tag in both `assignTags` and `unassignTags`, it is assigned.
 * @param filter - Filter that determines which availability exceptions to update tags for.
 * @public
 * @documentationMaturity preview
 * @requiredField filter
 * @permissionId RESTAURANTS.AVAILABILITY_EXCEPTION_UPDATE_TAGS
 * @applicableIdentity APP
 * @fqn wix.restaurants.v1.AvailabilityExceptionsService.BulkUpdateAvailabilityExceptionTagsByFilter
 */
declare function bulkUpdateAvailabilityExceptionTagsByFilter(filter: Record<string, any>, options?: BulkUpdateAvailabilityExceptionTagsByFilterOptions): Promise<NonNullablePaths<BulkUpdateAvailabilityExceptionTagsByFilterResponse, `jobId`, 2> & {
    __applicationErrorsType?: BulkUpdateAvailabilityExceptionTagsByFilterApplicationErrors;
}>;
interface BulkUpdateAvailabilityExceptionTagsByFilterOptions {
    /** Tags to assign to the availability exceptions. */
    assignTags?: Tags;
    /** Tags to unassign from the availability exceptions. */
    unassignTags?: Tags;
}

export { type ActionEvent, type AffectedFulfillmentMethods, type AffectedFulfillmentMethodsAffectedMethodsOptionsOneOf, AffectedMethods, type AffectedMethodsWithLiterals, type ApplicationError, type AvailabilityException, type AvailabilityExceptionCreatedEnvelope, type AvailabilityExceptionDeletedEnvelope, type AvailabilityExceptionUpdatedEnvelope, type AvailabilityExceptionsQueryBuilder, type AvailabilityExceptionsQueryResult, type BaseEventMetadata, type BulkActionMetadata, type BulkAvailabilityExceptionResult, type BulkCreateAvailabilityExceptionsRequest, type BulkCreateAvailabilityExceptionsResponse, type BulkCreateAvailabilityExceptionsResult, type BulkUpdateAvailabilityExceptionTagsApplicationErrors, type BulkUpdateAvailabilityExceptionTagsByFilterApplicationErrors, type BulkUpdateAvailabilityExceptionTagsByFilterOptions, type BulkUpdateAvailabilityExceptionTagsByFilterRequest, type BulkUpdateAvailabilityExceptionTagsByFilterResponse, type BulkUpdateAvailabilityExceptionTagsOptions, type BulkUpdateAvailabilityExceptionTagsRequest, type BulkUpdateAvailabilityExceptionTagsResponse, type BulkUpdateAvailabilityExceptionTagsResult, type BulkUpdateAvailabilityExceptionsOptions, type BulkUpdateAvailabilityExceptionsRequest, type BulkUpdateAvailabilityExceptionsResponse, type CreateAvailabilityExceptionRequest, type CreateAvailabilityExceptionResponse, type CursorPaging, type CursorPagingMetadata, type CursorQuery, type CursorQueryPagingMethodOneOf, type Cursors, type DeleteAvailabilityExceptionRequest, type DeleteAvailabilityExceptionResponse, type DomainEvent, type DomainEventBodyOneOf, type Empty, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type EventMetadata, type ExtendedFields, type GetAvailabilityExceptionRequest, type GetAvailabilityExceptionResponse, type IdentificationData, type IdentificationDataIdOneOf, type ItemMetadata, type MaskedAvailabilityException, type MessageEnvelope, type QueryAvailabilityExceptionsRequest, type QueryAvailabilityExceptionsResponse, type RestoreInfo, SortOrder, type SortOrderWithLiterals, type Sorting, type SpecificFulfillmentMethodsImpactScope, type TagList, type Tags, Type, type TypeWithLiterals, type UpdateAvailabilityException, type UpdateAvailabilityExceptionRequest, type UpdateAvailabilityExceptionResponse, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, bulkUpdateAvailabilityExceptionTags, bulkUpdateAvailabilityExceptionTagsByFilter, bulkUpdateAvailabilityExceptions, createAvailabilityException, deleteAvailabilityException, getAvailabilityException, onAvailabilityExceptionCreated, onAvailabilityExceptionDeleted, onAvailabilityExceptionUpdated, queryAvailabilityExceptions, updateAvailabilityException };
