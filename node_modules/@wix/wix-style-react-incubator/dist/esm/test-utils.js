export function move(items, activeId, overId) {
  if (!overId) {
    return;
  }
  const activeIndex = items.findIndex(_ref => {
    let {
      id
    } = _ref;
    return id === activeId;
  });
  const overIndex = items.findIndex(_ref2 => {
    let {
      id
    } = _ref2;
    return id === overId;
  });
  if (activeIndex === null || overIndex === null) {
    return;
  }
  const newItems = items.slice();
  newItems.splice(overIndex < 0 ? newItems.length + overIndex : overIndex, 0, newItems.splice(activeIndex, 1)[0]);
  return newItems;
}
//# sourceMappingURL=test-utils.js.map