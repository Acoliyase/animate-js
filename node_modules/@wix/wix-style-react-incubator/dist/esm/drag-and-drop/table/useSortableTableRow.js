import { useSortable } from '@dnd-kit/sortable';
export function useSortableTableRow(_ref) {
  let {
    id,
    disabled
  } = _ref;
  return useSortable({
    id,
    transition: {
      duration: 250,
      easing: 'cubic-bezier(0.33, 1, 0.68, 1)'
    },
    disabled
  });
}
//# sourceMappingURL=useSortableTableRow.js.map