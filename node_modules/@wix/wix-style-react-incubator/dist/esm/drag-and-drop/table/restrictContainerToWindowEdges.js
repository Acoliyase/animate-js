import { restrictToBoundingRect } from '@dnd-kit/modifiers';
export const restrictContainerToWindowEdges = _ref => {
  let {
    transform,
    windowRect,
    scrollableAncestorRects,
    draggingNodeRect
  } = _ref;
  const containerRect = scrollableAncestorRects[0];
  if (!containerRect || !windowRect || !draggingNodeRect) {
    return transform;
  }
  return restrictToBoundingRect(transform, {
    ...draggingNodeRect,
    left: containerRect.left,
    right: containerRect.right
  }, windowRect);
};
//# sourceMappingURL=restrictContainerToWindowEdges.js.map