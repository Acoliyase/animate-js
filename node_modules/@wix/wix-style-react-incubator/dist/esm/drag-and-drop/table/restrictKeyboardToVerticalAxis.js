export const restrictKeyboardToVerticalAxis = _ref => {
  let {
    activatorEvent,
    transform
  } = _ref;
  if (activatorEvent instanceof KeyboardEvent) {
    return {
      ...transform,
      x: 0
    };
  }
  return transform;
};
//# sourceMappingURL=restrictKeyboardToVerticalAxis.js.map