import { NonNullablePaths } from '@wix/sdk-types';

interface Step extends StepStepTypeOptionsOneOf {
    /** options of video step. */
    videoOptions?: VideoOptions;
    /** options of quiz step. */
    quizOptions?: QuizOptions;
    /**
     * Program Task ID
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * Represents the current state of an item. Each time the item is modified, its `revision` changes by the server. for an update operation to succeed, you MUST pass the latest revision
     * @readonly
     */
    revision?: string | null;
    /**
     * Represents the time this Task was created
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Represents the time this Task was last updated
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * id of the owning section.
     * @format GUID
     */
    sectionId?: string;
    /** title, description and media content. */
    description?: Description;
    /** field for manual ordering of steps */
    ordering?: number;
    /** Content type of a step, can be article, quiz or video. */
    stepType?: StepTypeWithLiterals;
    /** extensible field */
    extendedFields?: ExtendedFields;
    /**
     * id of the owning program.
     * @format GUID
     * @readonly
     */
    programId?: string;
}
/** @oneof */
interface StepStepTypeOptionsOneOf {
    /** options of video step. */
    videoOptions?: VideoOptions;
    /** options of quiz step. */
    quizOptions?: QuizOptions;
}
interface WixVideo {
    /**
     * Id of Wix Video.
     * @format GUID
     */
    _id?: string;
    /** Is Wix Video paid or not. */
    paid?: boolean;
}
interface VideoResolution {
    /** Video URL. */
    url?: string;
    /** Video height. */
    height?: number;
    /** Video width. */
    width?: number;
    /** Video format for example, mp4, hls. */
    format?: string;
}
interface FocalPoint {
    /** X-coordinate of the focal point. */
    x?: number;
    /** Y-coordinate of the focal point. */
    y?: number;
    /** crop by height */
    height?: number | null;
    /** crop by width */
    width?: number | null;
}
interface Description extends DescriptionMediaOneOf {
    /** WixMedia image */
    image?: string;
    /** WixMedia video */
    video?: string;
    /** @maxLength 250 */
    title?: string;
    /** @maxLength 10000000 */
    details?: string | null;
}
/** @oneof */
interface DescriptionMediaOneOf {
    /** WixMedia image */
    image?: string;
    /** WixMedia video */
    video?: string;
}
declare enum StepType {
    /** text-based step. */
    ARTICLE = "ARTICLE",
    /** step with video content. */
    VIDEO = "VIDEO",
    /** step containing quiz. */
    QUIZ = "QUIZ"
}
/** @enumType */
type StepTypeWithLiterals = StepType | 'ARTICLE' | 'VIDEO' | 'QUIZ';
interface VideoOptions extends VideoOptionsVideoSourceOneOf {
    /** Video from Wix Video product. */
    wixVideo?: WixVideo;
    /** Video uploaded via Media Manager. */
    video?: string;
    /** How many percent you need to watch to complete step. */
    requiredCompletionPercentage?: number;
    /** Cover image of video. */
    coverImage?: string;
    /** Duration of video in milliseconds. */
    durationInMilliseconds?: number | null;
    /** Shows if video is deleted or not (only for videos uploaded via Media Manager). */
    deleted?: boolean;
}
/** @oneof */
interface VideoOptionsVideoSourceOneOf {
    /** Video from Wix Video product. */
    wixVideo?: WixVideo;
    /** Video uploaded via Media Manager. */
    video?: string;
}
interface QuizOptions {
    /**
     * quiz id
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * number of questions inside the quiz
     * @readonly
     */
    totalQuestions?: number;
    /** percentage of right answers to complete the quiz. */
    passingGrade?: number | null;
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
interface UpdateDocumentsEvent extends UpdateDocumentsEventOperationOneOf {
    /** insert/update documents */
    update?: DocumentUpdateOperation;
    /** delete by document ids */
    deleteByIds?: DeleteByIdsOperation;
    /** delete documents matching filter */
    deleteByFilter?: DeleteByFilterOperation;
    /** update documents matching filter */
    updateByFilter?: UpdateByFilterOperation;
    /** update only existing documents */
    updateExisting?: UpdateExistingOperation;
    /**
     * application which owns documents
     * @minLength 2
     */
    appDefId?: string | null;
    /**
     * type of the documents
     * @minLength 2
     */
    documentType?: string | null;
    /**
     * language of the documents
     * @minLength 2
     */
    language?: string | null;
    /**
     * site documents belong to
     * @minLength 2
     */
    msId?: string | null;
}
/** @oneof */
interface UpdateDocumentsEventOperationOneOf {
    /** insert/update documents */
    update?: DocumentUpdateOperation;
    /** delete by document ids */
    deleteByIds?: DeleteByIdsOperation;
    /** delete documents matching filter */
    deleteByFilter?: DeleteByFilterOperation;
    /** update documents matching filter */
    updateByFilter?: UpdateByFilterOperation;
    /** update only existing documents */
    updateExisting?: UpdateExistingOperation;
}
interface DocumentUpdateOperation {
    /** documents to index or update */
    documents?: IndexDocument[];
}
interface IndexDocument {
    /** data bag with non-searchable fields (url, image) */
    payload?: DocumentPayload;
    /** what type of users should documents be visible to */
    exposure?: EnumWithLiterals;
    /** document with mandatory fields (id, title, description) and with fields specific to the type of the document */
    document?: Record<string, any> | null;
    /** what member groups is the document exposed to. Used only with GROUP_PROTECTED exposure */
    permittedMemberGroups?: string[];
    /** if true SEO is disabled for this document */
    seoHidden?: boolean | null;
    /** if true the page is a lightbox popup */
    isPopup?: boolean | null;
}
interface DocumentPayload {
    /** url of the page representing the document */
    url?: string | null;
    /** image which represents the document */
    documentImage?: DocumentImage;
}
interface DocumentImage {
    /** the name of the image */
    name?: string;
    /** the width of the image */
    width?: number;
    /** the height of the image */
    height?: number;
}
declare enum Enum {
    /** Default value. Means that permission not set */
    UNKNOWN = "UNKNOWN",
    /** Protected exposure. Exposed to members and owners */
    PROTECTED = "PROTECTED",
    /** Private exposure. Exposed to owners */
    PRIVATE = "PRIVATE",
    /** Public exposure. Visible to everyone */
    PUBLIC = "PUBLIC",
    /** Used for partial updates, to state that exposure is not changing */
    UNCHANGED = "UNCHANGED",
    /** Protected to members of permitted groups and owners */
    GROUP_PROTECTED = "GROUP_PROTECTED"
}
/** @enumType */
type EnumWithLiterals = Enum | 'UNKNOWN' | 'PROTECTED' | 'PRIVATE' | 'PUBLIC' | 'UNCHANGED' | 'GROUP_PROTECTED';
interface DeleteByIdsOperation {
    /** ids of the documents to delete */
    documentIds?: string[];
}
interface DeleteByFilterOperation {
    /** documents matching this filter wil be deleted. only filterable documents defined in document_type can be used for filtering */
    filter?: Record<string, any> | null;
}
interface UpdateByFilterOperation {
    /** documents matching this filter will be updated */
    filter?: Record<string, any> | null;
    /** partial document to apply */
    document?: IndexDocument;
}
interface UpdateExistingOperation {
    /** documents to update */
    documents?: IndexDocument[];
}
interface StepUpdated {
    /** Step before update. */
    previous?: Step;
    /** Step after update. */
    current?: Step;
    /**
     * Program id in which step is contained.
     * @format GUID
     */
    programId?: string;
}
interface StepMoved {
    /**
     * target section id
     * @format GUID
     */
    sectionId?: string;
    /**
     * what step to move
     * @format GUID
     */
    stepId?: string;
    /** new step position */
    newPosition?: number;
    /**
     * program id where move is happening
     * @format GUID
     */
    programId?: string;
    /**
     * section id before move
     * @format GUID
     */
    prevSectionId?: string;
}
interface CreateStepRequest {
    /** Step to be created. */
    step: Step;
    /**
     * Program id in which step is to be created.
     * @format GUID
     */
    programId?: string;
}
interface CreateStepResponse {
    /** The created Step. */
    step?: Step;
}
interface BulkCreateStepRequest {
    /**
     * List of steps you want to create
     * @minSize 1
     * @maxSize 100
     */
    steps: Step[];
    /** Whether to return entities in response or not. */
    returnEntity?: boolean;
    /**
     * Program id in which steps are to be created.
     * @format GUID
     */
    programId?: string;
}
interface BulkCreateStepResponse {
    /** Results of bulk action. */
    results?: BulkStepResult[];
    /** Metadata about whole bulk action. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkStepResult {
    /** Metadata of item. */
    itemMetadata?: ItemMetadata;
    /** Item itself (empty if return_entity in request is false). */
    item?: Step;
    /** Action type of bulk action (INSERT OR UPDATE) for that endpoint. */
    action?: BulkActionTypeWithLiterals;
}
interface ItemMetadata {
    /** Item ID. Should always be available, unless it's impossible (for example, when failing to create an item). */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
declare enum BulkActionType {
    UNKNOWN_ACTION_TYPE = "UNKNOWN_ACTION_TYPE",
    INSERT = "INSERT",
    UPDATE = "UPDATE",
    DELETE = "DELETE"
}
/** @enumType */
type BulkActionTypeWithLiterals = BulkActionType | 'UNKNOWN_ACTION_TYPE' | 'INSERT' | 'UPDATE' | 'DELETE';
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface BulkCreateStepMigrationRequest {
    /**
     * List of steps you want to create
     * @minSize 1
     * @maxSize 100
     */
    steps: Step[];
    /**
     * Program in which steps are to be created.
     * @format GUID
     */
    programId?: string;
}
interface BulkCreateStepMigrationResponse {
    /** Results of bulk action. */
    results?: BulkStepResult[];
    /** Metadata about whole bulk action. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface GetStepRequest {
    /**
     * ID of the Step to retrieve.
     * @format GUID
     */
    stepId: string;
    /** Whenever to return description only with title or with details as well */
    descriptionFieldSet?: DescriptionFieldSetWithLiterals;
}
declare enum DescriptionFieldSet {
    STANDARD = "STANDARD",
    EXTENDED = "EXTENDED"
}
/** @enumType */
type DescriptionFieldSetWithLiterals = DescriptionFieldSet | 'STANDARD' | 'EXTENDED';
interface GetStepResponse {
    /** The requested Step. */
    step?: Step;
}
interface UpdateStepRequest {
    /** Step to be updated, may be partial. */
    step: Step;
}
interface UpdateStepResponse {
    /** Updated Step. */
    step?: Step;
}
interface BulkUpdateStepsRequest {
    /**
     * List of steps you want to update
     * @minSize 1
     * @maxSize 100
     */
    steps: MaskedStep[];
    /** set to `true` if you wish to receive back the created steps in the response */
    returnEntity?: boolean;
}
interface MaskedStep {
    /** Step to be updated, may be partial */
    step?: Step;
    /** Explicit list of fields to update. */
    fieldMask?: string[];
}
interface BulkUpdateStepsResponse {
    /**
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateStepResult[];
    /** metadata */
    bulkActionMetadata?: CommonBulkActionMetadata;
}
interface CommonItemMetadata {
    /**
     * Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).
     * @format GUID
     */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface BulkUpdateStepResult {
    /** item metadata */
    itemMetadata?: CommonItemMetadata;
    /** Only exists if `returnEntity` was set to true in the request */
    item?: Step;
}
interface CommonBulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface DeleteStepRequest {
    /**
     * Id of the Step to delete.
     * @format GUID
     */
    stepId: string;
}
interface DeleteStepResponse {
}
interface QueryStepsRequest {
    /** WQL expression. */
    query?: CursorQuery;
    /** Whenever to return description only with title or with details as well */
    descriptionFieldSet?: DescriptionFieldSetWithLiterals;
    /**
     * Program id to query steps
     * @maxLength 36
     */
    programId?: string;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object in the following format:
     * `"filter" : {
     * "fieldName1": "value1",
     * "fieldName2":{"$operator":"value2"}
     * }`
     * Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the following format:
     * `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Maximum number of items to return in the results.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface QueryStepsResponse {
    /** List of Steps. */
    steps?: Step[];
    /** Paging metadata */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in current page. */
    count?: number | null;
    /** Cursor strings that point to the next page, previous page, or both. */
    cursors?: Cursors;
    /**
     * Whether there are more pages to retrieve following the current page.
     *
     * + `true`: Another page of results can be retrieved.
     * + `false`: This is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface QueryStepsWithoutPagingRequest {
    /** WQL expression. Cursor paging is not supported in this endpoint. */
    query?: CursorQuery;
    /** Whenever to return description only with title or with details as well */
    descriptionFieldSet?: DescriptionFieldSetWithLiterals;
    /**
     * Program id to query steps
     * @maxLength 36
     */
    programId?: string;
}
interface QueryStepsWithoutPagingResponse {
    /** List of Steps. */
    steps?: Step[];
}
interface ListAllProgramStepsRequest {
    /**
     * program id of requested steps
     * @format GUID
     */
    programId?: string;
    /** Whenever to return description only with title or with details as well */
    descriptionFieldSet?: DescriptionFieldSetWithLiterals;
}
interface ListAllProgramStepsResponse {
    /** List of Steps. */
    steps?: Step[];
}
interface DeleteAllProgramStepsRequest {
    /**
     * Program id to delete steps from
     * @format GUID
     */
    programId?: string;
}
interface DeleteAllProgramStepsResponse {
    /** Total count of steps in the program */
    stepsTotal?: number;
    /** Count of successfully deleted steps */
    successfulCount?: number;
    /** Count of steps that failed to be deleted */
    failedCount?: number;
}
interface BulkCloneStepRequest {
    /**
     * id of program which was cloned
     * @format GUID
     */
    prototypeProgramId?: string;
    /**
     * id of program which was cloned
     * @format GUID
     */
    clonedProgramId?: string;
    /** mapping of original to cloned section ids */
    prototypeIdToClonedId?: Record<string, string>;
}
interface BulkCloneStepResponse {
    /**
     * ids of newly created steps
     * @format GUID
     * @maxSize 100
     */
    createdStepIds?: string[];
}
interface MoveStepRequest {
    /**
     * what step to move
     * @format GUID
     */
    stepId: string;
    /**
     * target section (keep the same or move step to a different one)
     * @format GUID
     */
    sectionId: string;
    /**
     * insert after this step id (can be empty if it's the first position in the section)
     * @format GUID
     */
    afterStepId?: string | null;
    revision?: string;
}
interface MoveStepResponse {
    /** Moved step with updated ordering. */
    step?: Step;
}
interface CloneStepRequest {
    /**
     * what step to clone
     * @format GUID
     */
    stepId: string;
}
interface CloneStepResponse {
    /** created step. */
    step?: Step;
}
interface RestoreStepFromTrashRequest {
    /**
     * step id to restore
     * @format GUID
     */
    stepId?: string;
}
interface RestoreStepFromTrashResponse {
    /** restored step */
    step?: Step;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface Empty {
}
interface SearchIndexingNotification {
    /** new state of indexing for the site specified in ms_id */
    indexState?: StateWithLiterals;
    /** type of the document the notification is targeted for. Applies to all types if not provided */
    documentType?: string | null;
    /** languaInternalDocumentUpdateByFilterOperationge the notification is targeted for. Applies to all languages if not provided */
    language?: string | null;
    /**
     * site for which notification is targeted
     * @minLength 2
     */
    msId?: string | null;
}
declare enum State {
    /** default state */
    Unknown = "Unknown",
    /** metasite does not require site search indexing */
    Off = "Off",
    /** metasite requires site search indexing */
    On = "On"
}
/** @enumType */
type StateWithLiterals = State | 'Unknown' | 'Off' | 'On';
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
/**
 * Creates a Step.
 * @param step - Step to be created.
 * @public
 * @documentationMaturity preview
 * @requiredField step
 * @permissionId ONLINE_PROGRAMS.STEP_CREATE
 * @applicableIdentity APP
 * @returns The created Step.
 * @fqn wix.onlineprograms.steps.v3.StepsService.CreateStep
 */
declare function createStep(step: Step, options?: CreateStepOptions): Promise<NonNullablePaths<Step, `videoOptions.wixVideo._id` | `videoOptions.wixVideo.paid` | `videoOptions.requiredCompletionPercentage` | `videoOptions.deleted` | `quizOptions._id` | `quizOptions.totalQuestions` | `sectionId` | `description.title` | `ordering` | `stepType` | `programId`, 4>>;
interface CreateStepOptions {
    /**
     * Program id in which step is to be created.
     * @format GUID
     */
    programId?: string;
}
/**
 * Creates steps in bulk.
 * @param steps - List of steps you want to create
 * @public
 * @documentationMaturity preview
 * @requiredField steps
 * @permissionId ONLINE_PROGRAMS.STEP_CREATE
 * @applicableIdentity APP
 * @fqn wix.onlineprograms.steps.v3.StepsService.BulkCreateStep
 */
declare function bulkCreateStep(steps: Step[], options?: BulkCreateStepOptions): Promise<NonNullablePaths<BulkCreateStepResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.videoOptions.requiredCompletionPercentage` | `results.${number}.item.videoOptions.deleted` | `results.${number}.item.quizOptions._id` | `results.${number}.item.quizOptions.totalQuestions` | `results.${number}.item.sectionId` | `results.${number}.item.description.title` | `results.${number}.item.ordering` | `results.${number}.item.stepType` | `results.${number}.item.programId` | `results.${number}.action` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkCreateStepOptions {
    /** Whether to return entities in response or not. */
    returnEntity?: boolean;
    /**
     * Program id in which steps are to be created.
     * @format GUID
     */
    programId?: string;
}
/**
 * Internal endpoint to create steps in bulk for migration. Need to supply full entities which will be inserted in db as is.
 * @param steps - List of steps you want to create
 * @public
 * @documentationMaturity preview
 * @requiredField steps
 * @permissionId ONLINE_PROGRAMS.STEP_CREATE
 * @applicableIdentity APP
 * @fqn wix.onlineprograms.steps.v3.StepsService.BulkCreateStepMigration
 */
declare function bulkCreateStepMigration(steps: Step[], options?: BulkCreateStepMigrationOptions): Promise<NonNullablePaths<BulkCreateStepMigrationResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.videoOptions.requiredCompletionPercentage` | `results.${number}.item.videoOptions.deleted` | `results.${number}.item.quizOptions._id` | `results.${number}.item.quizOptions.totalQuestions` | `results.${number}.item.sectionId` | `results.${number}.item.description.title` | `results.${number}.item.ordering` | `results.${number}.item.stepType` | `results.${number}.item.programId` | `results.${number}.action` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkCreateStepMigrationOptions {
    /**
     * Program in which steps are to be created.
     * @format GUID
     */
    programId?: string;
}
/**
 * Retrieves a Step.
 * @param stepId - ID of the Step to retrieve.
 * @public
 * @documentationMaturity preview
 * @requiredField stepId
 * @permissionId ONLINE_PROGRAMS.STEP_READ
 * @applicableIdentity APP
 * @returns The requested Step.
 * @fqn wix.onlineprograms.steps.v3.StepsService.GetStep
 */
declare function getStep(stepId: string, options?: GetStepOptions): Promise<NonNullablePaths<Step, `videoOptions.wixVideo._id` | `videoOptions.wixVideo.paid` | `videoOptions.requiredCompletionPercentage` | `videoOptions.deleted` | `quizOptions._id` | `quizOptions.totalQuestions` | `sectionId` | `description.title` | `ordering` | `stepType` | `programId`, 4>>;
interface GetStepOptions {
    /** Whenever to return description only with title or with details as well */
    descriptionFieldSet?: DescriptionFieldSetWithLiterals;
}
/**
 * Update Step.
 * @param _id - Program Task ID
 * @public
 * @documentationMaturity preview
 * @requiredField _id
 * @requiredField step
 * @requiredField step.revision
 * @permissionId ONLINE_PROGRAMS.STEP_UPDATE
 * @applicableIdentity APP
 * @returns Updated Step.
 * @fqn wix.onlineprograms.steps.v3.StepsService.UpdateStep
 */
declare function updateStep(_id: string, step: NonNullablePaths<UpdateStep, `revision`, 2>): Promise<NonNullablePaths<Step, `videoOptions.wixVideo._id` | `videoOptions.wixVideo.paid` | `videoOptions.requiredCompletionPercentage` | `videoOptions.deleted` | `quizOptions._id` | `quizOptions.totalQuestions` | `sectionId` | `description.title` | `ordering` | `stepType` | `programId`, 4>>;
interface UpdateStep {
    /** options of video step. */
    videoOptions?: VideoOptions;
    /** options of quiz step. */
    quizOptions?: QuizOptions;
    /**
     * Program Task ID
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * Represents the current state of an item. Each time the item is modified, its `revision` changes by the server. for an update operation to succeed, you MUST pass the latest revision
     * @readonly
     */
    revision?: string | null;
    /**
     * Represents the time this Task was created
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Represents the time this Task was last updated
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * id of the owning section.
     * @format GUID
     */
    sectionId?: string;
    /** title, description and media content. */
    description?: Description;
    /** field for manual ordering of steps */
    ordering?: number;
    /** Content type of a step, can be article, quiz or video. */
    stepType?: StepTypeWithLiterals;
    /** extensible field */
    extendedFields?: ExtendedFields;
    /**
     * id of the owning program.
     * @format GUID
     * @readonly
     */
    programId?: string;
}
/**
 * Update multiple Steps in a single request. Works synchronously.
 * @param steps - List of steps you want to update
 * @public
 * @documentationMaturity preview
 * @requiredField steps
 * @requiredField steps.step.revision
 * @permissionId ONLINE_PROGRAMS.STEP_UPDATE
 * @applicableIdentity APP
 * @fqn wix.onlineprograms.steps.v3.StepsService.BulkUpdateSteps
 */
declare function bulkUpdateSteps(steps: NonNullablePaths<MaskedStep, `step.revision`, 3>[], options?: BulkUpdateStepsOptions): Promise<NonNullablePaths<BulkUpdateStepsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.videoOptions.requiredCompletionPercentage` | `results.${number}.item.videoOptions.deleted` | `results.${number}.item.quizOptions._id` | `results.${number}.item.quizOptions.totalQuestions` | `results.${number}.item.sectionId` | `results.${number}.item.description.title` | `results.${number}.item.ordering` | `results.${number}.item.stepType` | `results.${number}.item.programId` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkUpdateStepsOptions {
    /** set to `true` if you wish to receive back the created steps in the response */
    returnEntity?: boolean;
}
/**
 * Deletes a Step.
 * Deleting a Step permanently removes them from the Step List.
 * @param stepId - Id of the Step to delete.
 * @public
 * @documentationMaturity preview
 * @requiredField stepId
 * @permissionId ONLINE_PROGRAMS.STEP_DELETE
 * @applicableIdentity APP
 * @fqn wix.onlineprograms.steps.v3.StepsService.DeleteStep
 */
declare function deleteStep(stepId: string): Promise<void>;
/**
 * Query steps.
 * @public
 * @documentationMaturity preview
 * @permissionId ONLINE_PROGRAMS.STEP_READ
 * @applicableIdentity APP
 * @fqn wix.onlineprograms.steps.v3.StepsService.QuerySteps
 */
declare function querySteps(options?: QueryStepsOptions): StepsQueryBuilder;
interface QueryStepsOptions {
    /** Whenever to return description only with title or with details as well */
    descriptionFieldSet?: DescriptionFieldSetWithLiterals | undefined;
    /**
     * Program id to query steps
     * @maxLength 36
     */
    programId?: string | undefined;
}
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface StepsQueryResult extends QueryCursorResult {
    items: Step[];
    query: StepsQueryBuilder;
    next: () => Promise<StepsQueryResult>;
    prev: () => Promise<StepsQueryResult>;
}
interface StepsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    eq: (propertyName: '_id' | 'sectionId', value: any) => StepsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ne: (propertyName: '_id', value: any) => StepsQueryBuilder;
    /** @documentationMaturity preview */
    in: (propertyName: '_id' | 'sectionId', value: any) => StepsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    ascending: (...propertyNames: Array<'_id' | '_createdDate'>) => StepsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    descending: (...propertyNames: Array<'_id' | '_createdDate'>) => StepsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object.
     * @documentationMaturity preview
     */
    limit: (limit: number) => StepsQueryBuilder;
    /** @param cursor - A pointer to specific record
     * @documentationMaturity preview
     */
    skipTo: (cursor: string) => StepsQueryBuilder;
    /** @documentationMaturity preview */
    find: () => Promise<StepsQueryResult>;
}
/**
 * Change Step position.
 * @param stepId - what step to move
 * @param sectionId - target section (keep the same or move step to a different one)
 * @public
 * @documentationMaturity preview
 * @requiredField sectionId
 * @requiredField stepId
 * @permissionId ONLINE_PROGRAMS.STEP_UPDATE
 * @applicableIdentity APP
 * @fqn wix.onlineprograms.steps.v3.StepsService.MoveStep
 */
declare function moveStep(stepId: string, sectionId: string, options?: MoveStepOptions): Promise<NonNullablePaths<MoveStepResponse, `step.videoOptions.wixVideo._id` | `step.videoOptions.wixVideo.paid` | `step.videoOptions.requiredCompletionPercentage` | `step.videoOptions.deleted` | `step.quizOptions._id` | `step.quizOptions.totalQuestions` | `step.sectionId` | `step.description.title` | `step.ordering` | `step.stepType` | `step.programId`, 5>>;
interface MoveStepOptions {
    /**
     * insert after this step id (can be empty if it's the first position in the section)
     * @format GUID
     */
    afterStepId?: string | null;
    revision?: string;
}
/**
 * Clone Step.
 * @param stepId - what step to clone
 * @public
 * @documentationMaturity preview
 * @requiredField stepId
 * @permissionId ONLINE_PROGRAMS.STEP_CREATE
 * @applicableIdentity APP
 * @fqn wix.onlineprograms.steps.v3.StepsService.CloneStep
 */
declare function cloneStep(stepId: string): Promise<NonNullablePaths<CloneStepResponse, `step.videoOptions.wixVideo._id` | `step.videoOptions.wixVideo.paid` | `step.videoOptions.requiredCompletionPercentage` | `step.videoOptions.deleted` | `step.quizOptions._id` | `step.quizOptions.totalQuestions` | `step.sectionId` | `step.description.title` | `step.ordering` | `step.stepType` | `step.programId`, 5>>;

export { type ActionEvent, type ApplicationError, type BulkActionMetadata, BulkActionType, type BulkActionTypeWithLiterals, type BulkCloneStepRequest, type BulkCloneStepResponse, type BulkCreateStepMigrationOptions, type BulkCreateStepMigrationRequest, type BulkCreateStepMigrationResponse, type BulkCreateStepOptions, type BulkCreateStepRequest, type BulkCreateStepResponse, type BulkStepResult, type BulkUpdateStepResult, type BulkUpdateStepsOptions, type BulkUpdateStepsRequest, type BulkUpdateStepsResponse, type CloneStepRequest, type CloneStepResponse, type CommonBulkActionMetadata, type CommonItemMetadata, type CreateStepOptions, type CreateStepRequest, type CreateStepResponse, type CursorPaging, type CursorPagingMetadata, type CursorQuery, type CursorQueryPagingMethodOneOf, type Cursors, type DeleteAllProgramStepsRequest, type DeleteAllProgramStepsResponse, type DeleteByFilterOperation, type DeleteByIdsOperation, type DeleteStepRequest, type DeleteStepResponse, type Description, DescriptionFieldSet, type DescriptionFieldSetWithLiterals, type DescriptionMediaOneOf, type DocumentImage, type DocumentPayload, type DocumentUpdateOperation, type DomainEvent, type DomainEventBodyOneOf, type Empty, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, Enum, type EnumWithLiterals, type ExtendedFields, type FocalPoint, type GetStepOptions, type GetStepRequest, type GetStepResponse, type IdentificationData, type IdentificationDataIdOneOf, type IndexDocument, type ItemMetadata, type ListAllProgramStepsRequest, type ListAllProgramStepsResponse, type MaskedStep, type MessageEnvelope, type MoveStepOptions, type MoveStepRequest, type MoveStepResponse, type QueryStepsOptions, type QueryStepsRequest, type QueryStepsResponse, type QueryStepsWithoutPagingRequest, type QueryStepsWithoutPagingResponse, type QuizOptions, type RestoreInfo, type RestoreStepFromTrashRequest, type RestoreStepFromTrashResponse, type SearchIndexingNotification, SortOrder, type SortOrderWithLiterals, type Sorting, State, type StateWithLiterals, type Step, type StepMoved, type StepStepTypeOptionsOneOf, StepType, type StepTypeWithLiterals, type StepUpdated, type StepsQueryBuilder, type StepsQueryResult, type UpdateByFilterOperation, type UpdateDocumentsEvent, type UpdateDocumentsEventOperationOneOf, type UpdateExistingOperation, type UpdateStep, type UpdateStepRequest, type UpdateStepResponse, type VideoOptions, type VideoOptionsVideoSourceOneOf, type VideoResolution, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, type WixVideo, bulkCreateStep, bulkCreateStepMigration, bulkUpdateSteps, cloneStep, createStep, deleteStep, getStep, moveStep, querySteps, updateStep };
