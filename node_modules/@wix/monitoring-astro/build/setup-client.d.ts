interface LogParams {
    method: 'debug' | 'error' | 'info' | 'log' | 'warn';
    timestamp?: number;
    stack?: string;
}

declare global {
    interface Window {
        __wixMonitoring?: {
            originalMethods: Record<LogParams['method'], (...args: unknown[]) => void>;
            queue: Array<{
                args: unknown[];
                method: LogParams['method'];
                timestamp: number;
                stack: string;
            }>;
        };
    }
}
declare const getInlineMonitoringSnippet: () => string;
declare function setupClientMonitoring(): void;

export { getInlineMonitoringSnippet, setupClientMonitoring };
