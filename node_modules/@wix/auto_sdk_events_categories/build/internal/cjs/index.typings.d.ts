import { NonNullablePaths } from '@wix/sdk-types';

/**
 * A Category is a classification object that groups related events on a site so you can organize and display them by themes, venues, or other facets.
 *
 * You can manage Categories, assign events to them, and use them to control the selection and order of events across different pages and widgets.
 *
 * Read more about [Categories](https://support.wix.com/en/article/creating-and-displaying-event-categories).
 */
interface Category {
    /**
     * Category ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * Category name.
     * @minLength 1
     * @maxLength 30
     */
    name?: string;
    /**
     * Date and time when category was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * The total number of draft and published events assigned to the category.
     * @readonly
     */
    counts?: CategoryCounts;
    /**
     * Category state. Possible values:
     * - `MANUAL`: Category is created manually by the user.
     * - `AUTO`: Category is created automatically.
     * - `RECURRING_EVENT`: Category is created automatically when publishing recurring events.
     * - `HIDDEN`: Category can't be seen.
     *
     * Default: `MANUAL`.
     *
     * **Note:** The WIX_EVENTS.MANAGE_AUTO_CATEGORIES permission is required to use states other than `MANUAL`.
     * @maxSize 3
     */
    states?: StateWithLiterals[];
}
interface CategoryCounts {
    /** Total number of published events assigned to the category. Deleted events are excluded. */
    assignedEventsCount?: number | null;
    /** Total number of draft events assigned to the category. */
    assignedDraftEventsCount?: number | null;
}
declare enum State {
    /** Created manually by the user. */
    MANUAL = "MANUAL",
    /** Created automatically. */
    AUTO = "AUTO",
    /** Created when publishing recurring events. */
    RECURRING_EVENT = "RECURRING_EVENT",
    /** Category is hidden. */
    HIDDEN = "HIDDEN"
}
/** @enumType */
type StateWithLiterals = State | 'MANUAL' | 'AUTO' | 'RECURRING_EVENT' | 'HIDDEN';
interface CreateCategoryRequest {
    /** Category to create. */
    category: Category;
}
interface CreateCategoryResponse {
    /** Created category. */
    category?: Category;
}
interface BulkCreateCategoryRequest {
    /**
     * Categories to create.
     * @minSize 1
     * @maxSize 10
     */
    categories: Category[];
}
interface BulkCreateCategoryResponse {
    /** Bulk create results. */
    results?: BulkCategoryResult[];
    /** Metadata of results. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkCategoryResult {
    /** Metadata. */
    itemMetadata?: ItemMetadata;
    /** Created categories. */
    item?: Category;
}
interface ItemMetadata {
    /** Item ID. Should always be available, unless it's impossible (for example, when failing to create an item). */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details due to exceeding the threshold for detailed failures. */
    undetailedFailures?: number;
}
interface UpdateCategoryRequest {
    /** Category to update. */
    category: Category;
}
interface UpdateCategoryResponse {
    /** Updated category. */
    category?: Category;
}
interface DeleteCategoryRequest {
    /**
     * ID of category to be deleted.
     * @format GUID
     */
    categoryId: string;
}
interface DeleteCategoryResponse {
}
interface QueryCategoriesRequest {
    /** Options to use when querying categories. See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language) for more details. */
    query: QueryV2;
    /**
     * Predefined sets of fields to return.
     * - `COUNTS`: Returns `assignedEventsCount`.
     * @maxSize 1
     */
    fieldset?: CategoryFieldsetWithLiterals[];
}
interface QueryV2 extends QueryV2PagingMethodOneOf {
    /**
     * Pointer to page of results using offset.
     * See [Pagination](https://dev.wix.com/api/rest/getting-started/pagination).
     */
    paging?: Paging;
    /** Filter. See [supported fields and operators](https://dev.wix.com/api/rest/wix-events/wix-events/filter-and-sort#wix-events_wix-events_filter-and-sort_list-query-events). */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the form [{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]
     * See [supported fields](https://dev.wix.com/api/rest/wix-events/wix-events/filter-and-sort#wix-events_wix-events_filter-and-sort_list-query-events).
     * @maxSize 20
     */
    sort?: Sorting[];
}
/** @oneof */
interface QueryV2PagingMethodOneOf {
    /**
     * Pointer to page of results using offset.
     * See [Pagination](https://dev.wix.com/api/rest/getting-started/pagination).
     */
    paging?: Paging;
}
interface Sorting {
    /**
     * Name of the field to sort by
     * @maxLength 100
     */
    fieldName?: string;
    /** Sort order (ASC/DESC). Defaults to ASC */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface Paging {
    /**
     * Number of items to load per page.
     * @max 1000
     */
    limit?: number | null;
    /** Number of items to skip in the current sort order. */
    offset?: number | null;
}
declare enum CategoryFieldset {
    /** Include `assignedEventsCount` in the response. */
    COUNTS = "COUNTS"
}
/** @enumType */
type CategoryFieldsetWithLiterals = CategoryFieldset | 'COUNTS';
interface QueryCategoriesResponse {
    /** List of categories. */
    categories?: Category[];
    /** Metadata for the paginated results. */
    metaData?: PagingMetadataV2;
}
interface PagingMetadataV2 {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    offset?: number | null;
    /** Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set. */
    total?: number | null;
    /** Flag that indicates the server failed to calculate the `total` field. */
    tooManyToCount?: boolean | null;
    /** Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used. */
    cursors?: Cursors;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface AssignEventsRequest {
    /**
     * ID of category to which events should be assigned.
     * @format GUID
     */
    categoryId: string;
    /**
     * A list of events IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    eventId: string[];
}
interface AssignEventsResponse {
}
interface BulkAssignEventsRequest {
    /**
     * A list of category IDs to which events should be assigned.
     * @format GUID
     * @minSize 1
     * @maxSize 10
     */
    categoryId: string[];
    /**
     * A list of events IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 1
     */
    eventId: string[];
}
interface BulkAssignEventsResponse {
    /** Bulk assign results. */
    results?: BulkCategoryResult[];
    /** Metadata of results. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkAssignEventsAsyncRequest {
    /**
     * Category IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 10
     */
    categoryId: string[];
    /**
     * Criteria that must be met for an event to be considered for the bulk assign. Supported filters for this API:
     * - `_id`: [`eq()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/eq)
     * - `name`: [`eq()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/eq)
     */
    filter: Record<string, any> | null;
}
interface BulkAssignEventsAsyncResponse {
}
interface UnassignEventsRequest {
    /**
     * Category ID.
     * @format GUID
     */
    categoryId: string;
    /**
     * A list of events IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    eventId: string[];
}
interface UnassignEventsResponse {
}
interface BulkUnassignEventsRequest {
    /**
     * A list of category IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 10
     */
    categoryId: string[];
    /**
     * A list of events IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 1
     */
    eventId?: string[];
}
interface BulkUnassignEventsResponse {
    /** Results. */
    results?: BulkCategoryResult[];
    /** Metadata of results. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUnassignEventsAsyncRequest {
    /**
     * Category ID.
     * @format GUID
     * @minSize 1
     * @maxSize 10
     */
    categoryId: string[];
    /**
     * Criteria that must be met for an event to be considered for the bulk assign. Supported filters for this API:
     * - `_id`: [`eq()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/eq)
     * - `name`: [`eq()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/eq)
     */
    filter: Record<string, any> | null;
}
interface BulkUnassignEventsAsyncResponse {
}
interface ListEventCategoriesRequest {
    /**
     * Event ID.
     * @format GUID
     */
    eventId: string;
}
interface ListEventCategoriesResponse {
    /** A list of categories. */
    categories?: Category[];
}
interface ReorderCategoryEventsRequest extends ReorderCategoryEventsRequestReferenceEventOneOf {
    /**
     * Move the event before defined `eventId`.
     * @format GUID
     */
    beforeEventId?: string;
    /**
     * Move the event after defined `eventId`.
     * @format GUID
     */
    afterEventId?: string;
    /**
     * Category ID.
     * @format GUID
     */
    categoryId: string;
    /**
     * Event ID.
     * @format GUID
     */
    eventId?: string;
}
/** @oneof */
interface ReorderCategoryEventsRequestReferenceEventOneOf {
    /**
     * Move the event before defined `eventId`.
     * @format GUID
     */
    beforeEventId?: string;
    /**
     * Move the event after defined `eventId`.
     * @format GUID
     */
    afterEventId?: string;
}
interface ReorderCategoryEventsResponse {
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
/** @docsIgnore */
type ReorderCategoryEventsApplicationErrors = {
    code?: 'INVALID_REORDER_INSTRUCTION';
    description?: string;
    data?: Record<string, any>;
};
/**
 * Creates a category.
 * @public
 * @requiredField category
 * @param category - Category to create.
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @returns Created category.
 * @fqn wix.events.categories.CategoryManagement.CreateCategory
 */
declare function createCategory(category: Category): Promise<NonNullablePaths<Category, `_id` | `name` | `states`, 2>>;
/**
 * Creates multiple categories at once.
 * @param categories - Categories to create.
 * @public
 * @requiredField categories
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.BulkCreateCategory
 */
declare function bulkCreateCategory(categories: Category[]): Promise<NonNullablePaths<BulkCreateCategoryResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item._id` | `results.${number}.item.name` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
/**
 * Updates an existing category.
 * @param _id - Category ID.
 * @public
 * @requiredField _id
 * @requiredField category
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @returns Updated category.
 * @fqn wix.events.categories.CategoryManagement.UpdateCategory
 */
declare function updateCategory(_id: string, category: UpdateCategory): Promise<NonNullablePaths<Category, `_id` | `name` | `states`, 2>>;
interface UpdateCategory {
    /**
     * Category ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * Category name.
     * @minLength 1
     * @maxLength 30
     */
    name?: string;
    /**
     * Date and time when category was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * The total number of draft and published events assigned to the category.
     * @readonly
     */
    counts?: CategoryCounts;
    /**
     * Category state. Possible values:
     * - `MANUAL`: Category is created manually by the user.
     * - `AUTO`: Category is created automatically.
     * - `RECURRING_EVENT`: Category is created automatically when publishing recurring events.
     * - `HIDDEN`: Category can't be seen.
     *
     * Default: `MANUAL`.
     *
     * **Note:** The WIX_EVENTS.MANAGE_AUTO_CATEGORIES permission is required to use states other than `MANUAL`.
     * @maxSize 3
     */
    states?: StateWithLiterals[];
}
/**
 * Deletes a category.
 * @param categoryId - ID of category to be deleted.
 * @public
 * @requiredField categoryId
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.DeleteCategory
 */
declare function deleteCategory(categoryId: string): Promise<void>;
/**
 * Creates a query to retrieve a list of categories.
 *
 *
 * The `queryCategories()` function builds a query to retrieve a list of categories and returns a `CategoriesQueryBuilder` object.
 *
 * The returned object contains the query definition, which is typically used to run the query using the [`find()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/find) function.
 *
 * You can refine the query by chaining `CategoriesQueryBuilder` functions onto the query. `CategoriesQueryBuilder` functions enable you to sort, filter, and control the results `queryCategories()` returns.
 *
 * `queryCategories()` runs with these `CategoriesQueryBuilder` defaults, which you can override:
 *
 *  - [`limit(50)`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/limit)
 * @public
 * @param options - Options to use when querying categories.
 * @permissionId WIX_EVENTS.READ_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.QueryCategories
 */
declare function queryCategories(options?: QueryCategoriesOptions): CategoriesQueryBuilder;
interface QueryCategoriesOptions {
    /**
     * Predefined sets of fields to return.
     * - `COUNTS`: Returns `assignedEventsCount`.
     * @maxSize 1
     */
    fieldset?: CategoryFieldsetWithLiterals[] | undefined;
}
interface QueryOffsetResult {
    currentPage: number | undefined;
    totalPages: number | undefined;
    totalCount: number | undefined;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface CategoriesQueryResult extends QueryOffsetResult {
    items: Category[];
    query: CategoriesQueryBuilder;
    next: () => Promise<CategoriesQueryResult>;
    prev: () => Promise<CategoriesQueryResult>;
}
interface CategoriesQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    eq: (propertyName: '_id' | 'name', value: any) => CategoriesQueryBuilder;
    /** @param propertyName - Property whose value is compared with `values`.
     * @param values - List of values to compare against.
     */
    hasSome: (propertyName: string, value: any[]) => CategoriesQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    ascending: (...propertyNames: Array<'_id' | 'name' | '_createdDate'>) => CategoriesQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    descending: (...propertyNames: Array<'_id' | 'name' | '_createdDate'>) => CategoriesQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object. */
    limit: (limit: number) => CategoriesQueryBuilder;
    /** @param skip - Number of items to skip in the query results before returning the results. */
    skip: (skip: number) => CategoriesQueryBuilder;
    find: () => Promise<CategoriesQueryResult>;
}
/**
 * Assigns events to a single category.
 * @param categoryId - ID of category to which events should be assigned.
 * @param eventId - A list of events IDs.
 * @public
 * @requiredField categoryId
 * @requiredField eventId
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.AssignEvents
 */
declare function assignEvents(categoryId: string, eventId: string[]): Promise<void>;
/**
 * Assigns events to multiple categories at once.
 * @param categoryId - A list of category IDs to which events should be assigned.
 * @public
 * @requiredField categoryId
 * @requiredField options
 * @requiredField options.eventId
 * @param options - Options to use when assigning events to multiple categories.
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.BulkAssignEvents
 */
declare function bulkAssignEvents(categoryId: string[], options: NonNullablePaths<BulkAssignEventsOptions, `eventId`, 2>): Promise<NonNullablePaths<BulkAssignEventsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item._id` | `results.${number}.item.name` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkAssignEventsOptions {
    /**
     * A list of events IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 1
     */
    eventId: string[];
}
/**
 * Assigns events that match given filter criteria to multiple categories.
 *
 * Unlike the [`bulkAssignEvents()`](https://www.wix.com/velo/reference/wix-events-v2/categories/bulkassignevents) function, this function can handle numerous requests and is less prone to failures.
 *
 * However, the events will not be instantly assigned to the categories (as with `bulkAssignEvents()`), but rather after some time. In this case, if try to [`listEventCategories`](https://www.wix.com/velo/reference/wix-events-v2/categories/listeventcategories) or [`queryCategories`](https://www.wix.com/velo/reference/wix-events-v2/categories/querycategories), you might not get the correct response.
 * @param categoryId - Category IDs.
 * @public
 * @requiredField categoryId
 * @requiredField options
 * @requiredField options.filter
 * @param options - Options to use when assigning events to multiple categories.
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.BulkAssignEventsAsync
 */
declare function bulkAssignEventsAsync(categoryId: string[], options: NonNullablePaths<BulkAssignEventsAsyncOptions, `filter`, 2>): Promise<void>;
interface BulkAssignEventsAsyncOptions {
    /**
     * Criteria that must be met for an event to be considered for the bulk assign. Supported filters for this API:
     * - `_id`: [`eq()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/eq)
     * - `name`: [`eq()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/eq)
     */
    filter: Record<string, any> | null;
}
/**
 * Unassigns events from a category.
 * @param categoryId - Category ID.
 * @param eventId - A list of events IDs.
 * @public
 * @requiredField categoryId
 * @requiredField eventId
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.UnassignEvents
 */
declare function unassignEvents(categoryId: string, eventId: string[]): Promise<void>;
/**
 * Unassigns events from multiple categories at once.
 * @param categoryId - A list of category IDs.
 * @public
 * @requiredField categoryId
 * @param options - Options to use when removing events from multiple categories.
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.BulkUnassignEvents
 */
declare function bulkUnassignEvents(categoryId: string[], options?: BulkUnassignEventsOptions): Promise<NonNullablePaths<BulkUnassignEventsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item._id` | `results.${number}.item.name` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkUnassignEventsOptions {
    /**
     * A list of events IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 1
     */
    eventId?: string[];
}
/**
 * Removes events that match given filter criteria from multiple categories.
 *
 * Unlike the [`bulkUnassignEvents()`](https://www.wix.com/velo/reference/wix-events-v2/categories/bulkunassignevents) function, this function can handle numerous requests and is less prone to failures.
 *
 * However, the events will not be instantly removed from the categories (as with `bulkUnassignEvents()`), but rather after some time. In this case, if try to [`listEventCategories`](https://www.wix.com/velo/reference/wix-events-v2/categories/listeventcategories) or [`queryCategories`](https://www.wix.com/velo/reference/wix-events-v2/categories/querycategories), you might not get the correct response.
 * @param categoryId - Category ID.
 * @public
 * @requiredField categoryId
 * @requiredField options
 * @requiredField options.filter
 * @param options - Options to use when removing events from multiple categories.
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.BulkUnassignEventsAsync
 */
declare function bulkUnassignEventsAsync(categoryId: string[], options: NonNullablePaths<BulkUnassignEventsAsyncOptions, `filter`, 2>): Promise<void>;
interface BulkUnassignEventsAsyncOptions {
    /**
     * Criteria that must be met for an event to be considered for the bulk assign. Supported filters for this API:
     * - `_id`: [`eq()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/eq)
     * - `name`: [`eq()`](https://www.wix.com/velo/reference/wix-events-v2/categories/categoriesquerybuilder/eq)
     */
    filter: Record<string, any> | null;
}
/**
 * Retrieves a list of categories that are not in the `HIDDEN` state.
 * @param eventId - Event ID.
 * @public
 * @requiredField eventId
 * @permissionId WIX_EVENTS.READ_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.ListEventCategories
 */
declare function listEventCategories(eventId: string): Promise<NonNullablePaths<ListEventCategoriesResponse, `categories` | `categories.${number}._id` | `categories.${number}.name`, 4>>;
/**
 * Change the order of events that are assigned to the same category on the Events Widget.
 *
 *
 * For more information see [this article](https://support.wix.com/en/article/creating-and-displaying-event-categories)
 * @param categoryId - Category ID.
 * @public
 * @requiredField categoryId
 * @param options - Options to use when reordering events.
 * @permissionId WIX_EVENTS.MANAGE_CATEGORIES
 * @applicableIdentity APP
 * @fqn wix.events.categories.CategoryManagement.ReorderCategoryEvents
 */
declare function reorderCategoryEvents(categoryId: string, options?: ReorderCategoryEventsOptions): Promise<void & {
    __applicationErrorsType?: ReorderCategoryEventsApplicationErrors;
}>;
interface ReorderCategoryEventsOptions extends ReorderCategoryEventsOptionsReferenceEventOneOf {
    /**
     * Event ID.
     * @format GUID
     */
    eventId?: string;
    /**
     * Move the event before defined `eventId`.
     * @format GUID
     */
    beforeEventId?: string;
    /**
     * Move the event after defined `eventId`.
     * @format GUID
     */
    afterEventId?: string;
}
/** @oneof */
interface ReorderCategoryEventsOptionsReferenceEventOneOf {
    /**
     * Move the event before defined `eventId`.
     * @format GUID
     */
    beforeEventId?: string;
    /**
     * Move the event after defined `eventId`.
     * @format GUID
     */
    afterEventId?: string;
}

export { type ActionEvent, type ApplicationError, type AssignEventsRequest, type AssignEventsResponse, type BulkActionMetadata, type BulkAssignEventsAsyncOptions, type BulkAssignEventsAsyncRequest, type BulkAssignEventsAsyncResponse, type BulkAssignEventsOptions, type BulkAssignEventsRequest, type BulkAssignEventsResponse, type BulkCategoryResult, type BulkCreateCategoryRequest, type BulkCreateCategoryResponse, type BulkUnassignEventsAsyncOptions, type BulkUnassignEventsAsyncRequest, type BulkUnassignEventsAsyncResponse, type BulkUnassignEventsOptions, type BulkUnassignEventsRequest, type BulkUnassignEventsResponse, type CategoriesQueryBuilder, type CategoriesQueryResult, type Category, type CategoryCounts, CategoryFieldset, type CategoryFieldsetWithLiterals, type CreateCategoryRequest, type CreateCategoryResponse, type Cursors, type DeleteCategoryRequest, type DeleteCategoryResponse, type DomainEvent, type DomainEventBodyOneOf, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type IdentificationData, type IdentificationDataIdOneOf, type ItemMetadata, type ListEventCategoriesRequest, type ListEventCategoriesResponse, type MessageEnvelope, type Paging, type PagingMetadataV2, type QueryCategoriesOptions, type QueryCategoriesRequest, type QueryCategoriesResponse, type QueryV2, type QueryV2PagingMethodOneOf, type ReorderCategoryEventsApplicationErrors, type ReorderCategoryEventsOptions, type ReorderCategoryEventsOptionsReferenceEventOneOf, type ReorderCategoryEventsRequest, type ReorderCategoryEventsRequestReferenceEventOneOf, type ReorderCategoryEventsResponse, type RestoreInfo, SortOrder, type SortOrderWithLiterals, type Sorting, State, type StateWithLiterals, type UnassignEventsRequest, type UnassignEventsResponse, type UpdateCategory, type UpdateCategoryRequest, type UpdateCategoryResponse, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, assignEvents, bulkAssignEvents, bulkAssignEventsAsync, bulkCreateCategory, bulkUnassignEvents, bulkUnassignEventsAsync, createCategory, deleteCategory, listEventCategories, queryCategories, reorderCategoryEvents, unassignEvents, updateCategory };
