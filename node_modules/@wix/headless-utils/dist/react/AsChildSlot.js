import { jsx as _jsx, Fragment as _Fragment } from "react/jsx-runtime";
import React from 'react';
import { Slot } from '@radix-ui/react-slot';
export const AsChildSlot = React.forwardRef((props, ref) => {
    const { asChild, children, customElement, customElementProps = {}, content, ...restProps } = props;
    if (asChild && customElement) {
        // Handle string
        if (typeof customElement === 'string') {
            return (_jsx(Slot, { ref: ref, ...restProps, children: _jsx("span", { children: customElement }) }));
        }
        // Handle React element pattern
        if (React.isValidElement(customElement)) {
            return (_jsx(Slot, { ...restProps, children: React.cloneElement(customElement, {
                    ref,
                    ...(content !== undefined ? { children: content } : {}),
                }) }));
        }
        // Handle render function pattern
        if (typeof customElement === 'function') {
            return (_jsx(Slot, { ...restProps, children: customElement(customElementProps, ref) }));
        }
        // Handle render object pattern
        if (typeof customElement === 'object' && 'render' in customElement) {
            return (_jsx(Slot, { ...restProps, children: customElement.render(customElementProps, ref) }));
        }
    }
    if (!children) {
        return null;
    }
    if (typeof children === 'string') {
        return (_jsx(Slot, { ref: ref, ...restProps, children: _jsx("div", { children: children }) }));
    }
    if (typeof children === 'function') {
        return _jsx(Slot, { ...restProps, children: children({}, ref) });
    }
    if (typeof children === 'object' && 'render' in children) {
        return (_jsx(Slot, { ...restProps, children: children.render(customElementProps, ref) }));
    }
    return (_jsx(Slot, { ref: ref, ...restProps, children: React.Children.count(children) > 1 ? _jsx(_Fragment, { children: children }) : children }));
});
