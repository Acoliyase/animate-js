import { NonNullablePaths } from '@wix/sdk-types';

/** PacingOrder is the main entity of OrderPacingService */
interface PacingOrder {
}
interface GetCurrentDelayRequest {
    /**
     * the location id of the restaurant
     * @format GUID
     */
    locationId?: string | null;
    /**
     * the max acceptable delay time in minutes
     * @min 480
     * @max 2880
     */
    maxAcceptableDelayTimeInMinutes?: number | null;
}
interface GetCurrentDelayResponse {
    /** The current delay */
    delay?: Delay;
}
interface Delay {
    /** The duration in minutes */
    durationInMinutes?: number;
}
interface GetCurrentDelayWithValidityPeriodRequest {
    /**
     * the location id of the restaurant
     * @format GUID
     */
    locationId?: string | null;
    /**
     * the max acceptable delay time in minutes
     * @min 480
     * @max 2880
     */
    maxAcceptableDelayTimeInMinutes?: number | null;
}
interface GetCurrentDelayWithValidityPeriodResponse {
    /** The current delay */
    delay?: Delay;
    /** The validity period of the delay in minutes */
    validityPeriodInMinutes?: number | null;
}
interface ListBusyBlocksRequest {
    /**
     * the location id of the restaurant
     * @format GUID
     */
    locationId?: string | null;
    /** the start time of the desired window */
    from: Date | null;
    /** the end time of the desired window */
    until: Date | null;
    /** the time it takes the order to be supplied to the user */
    orderFulfillmentTimeInMinutes: number | null;
}
interface ListBusyBlocksResponse {
    /** The retrieved busy blocks arranged by increasing time */
    busyBlocks?: BusyBlock[];
}
interface BusyBlock {
    /** the start time of the block */
    startTime?: Date | null;
    /** the end time of the block */
    endTime?: Date | null;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface Empty {
}
/**
 * Get the current delay the restaurant is experiencing
 * @public
 * @documentationMaturity preview
 * @permissionId WIX_RESTAURANTS.ORDER_PACING_READ
 * @applicableIdentity APP
 * @fqn com.wixpress.restaurants.order.pacing.OrderPacingService.GetCurrentDelay
 */
declare function getCurrentDelay(options?: GetCurrentDelayOptions): Promise<NonNullablePaths<GetCurrentDelayResponse, `delay.durationInMinutes`, 3>>;
interface GetCurrentDelayOptions {
    /**
     * the location id of the restaurant
     * @format GUID
     */
    locationId?: string | null;
    /**
     * the max acceptable delay time in minutes
     * @min 480
     * @max 2880
     */
    maxAcceptableDelayTimeInMinutes?: number | null;
}
/**
 * Get the current delay the restaurant is experiencing with a validity period in minutes
 * @public
 * @documentationMaturity preview
 * @permissionId WIX_RESTAURANTS.ORDER_PACING_READ
 * @applicableIdentity APP
 * @fqn com.wixpress.restaurants.order.pacing.OrderPacingService.GetCurrentDelayWithValidityPeriod
 */
declare function getCurrentDelayWithValidityPeriod(options?: GetCurrentDelayWithValidityPeriodOptions): Promise<NonNullablePaths<GetCurrentDelayWithValidityPeriodResponse, `delay.durationInMinutes`, 3>>;
interface GetCurrentDelayWithValidityPeriodOptions {
    /**
     * the location id of the restaurant
     * @format GUID
     */
    locationId?: string | null;
    /**
     * the max acceptable delay time in minutes
     * @min 480
     * @max 2880
     */
    maxAcceptableDelayTimeInMinutes?: number | null;
}
/**
 * Get the restaurant block levels in a time window, 15 min each
 * @public
 * @documentationMaturity preview
 * @requiredField options.from
 * @requiredField options.orderFulfillmentTimeInMinutes
 * @requiredField options.until
 * @permissionId WIX_RESTAURANTS.ORDER_PACING_READ
 * @applicableIdentity APP
 * @fqn com.wixpress.restaurants.order.pacing.OrderPacingService.ListBusyBlocks
 */
declare function listBusyBlocks(options?: NonNullablePaths<ListBusyBlocksOptions, `from` | `orderFulfillmentTimeInMinutes` | `until`, 2>): Promise<NonNullablePaths<ListBusyBlocksResponse, `busyBlocks`, 2>>;
interface ListBusyBlocksOptions {
    /**
     * the location id of the restaurant
     * @format GUID
     */
    locationId?: string | null;
    /** the start time of the desired window */
    from: Date | null;
    /** the end time of the desired window */
    until: Date | null;
    /** the time it takes the order to be supplied to the user */
    orderFulfillmentTimeInMinutes: number | null;
}

export { type ActionEvent, type BusyBlock, type Delay, type DomainEvent, type DomainEventBodyOneOf, type Empty, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type GetCurrentDelayOptions, type GetCurrentDelayRequest, type GetCurrentDelayResponse, type GetCurrentDelayWithValidityPeriodOptions, type GetCurrentDelayWithValidityPeriodRequest, type GetCurrentDelayWithValidityPeriodResponse, type ListBusyBlocksOptions, type ListBusyBlocksRequest, type ListBusyBlocksResponse, type PacingOrder, type RestoreInfo, getCurrentDelay, getCurrentDelayWithValidityPeriod, listBusyBlocks };
