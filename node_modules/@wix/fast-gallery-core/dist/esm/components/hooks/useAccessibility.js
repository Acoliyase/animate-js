import { useState, useEffect } from 'react';
export const useAccessibility = (isAccessibilityEnabled) => {
    const [isAccessible, setAccessibilityState] = useState(false);
    useEffect(() => {
        if (!isAccessibilityEnabled || isAccessible) {
            return;
        }
        const handleKeyDown = (event) => {
            if (event.key === 'Tab') {
                event.preventDefault();
                setAccessibilityState(true);
            }
        };
        window.addEventListener('keydown', handleKeyDown, { once: true });
        return () => {
            window.removeEventListener('keydown', handleKeyDown);
        };
    }, [isAccessibilityEnabled, isAccessible]);
    return {
        isAccessible,
    };
};
//# sourceMappingURL=useAccessibility.js.map