/* eslint-disable react-hooks/rules-of-hooks */
import React from 'react';
import { useAccessibility } from '../hooks/useAccessibility';
export const WithAccessibility = (Gallery) => (props) => {
    const isAccessibilityEnabled = !props.disableAccessibilityHelper;
    const { isAccessible } = useAccessibility(isAccessibilityEnabled);
    return React.createElement(Gallery, { ...props, isAccessible: isAccessible });
};
//# sourceMappingURL=WithAccessibility.js.map