"use strict";

exports.__esModule = true;
exports.useAccessibility = void 0;
var _react = require("react");
const useAccessibility = isAccessibilityEnabled => {
  const [isAccessible, setAccessibilityState] = (0, _react.useState)(false);
  (0, _react.useEffect)(() => {
    if (!isAccessibilityEnabled || isAccessible) {
      return;
    }
    const handleKeyDown = event => {
      if (event.key === 'Tab') {
        event.preventDefault();
        setAccessibilityState(true);
      }
    };
    window.addEventListener('keydown', handleKeyDown, {
      once: true
    });
    return () => {
      window.removeEventListener('keydown', handleKeyDown);
    };
  }, [isAccessibilityEnabled, isAccessible]);
  return {
    isAccessible
  };
};
exports.useAccessibility = useAccessibility;
//# sourceMappingURL=useAccessibility.js.map