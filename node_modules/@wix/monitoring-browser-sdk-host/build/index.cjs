"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var index_exports = {};
__export(index_exports, {
  getMonitoringClientFunction: () => getMonitoringClientFunction
});
module.exports = __toCommonJS(index_exports);

// src/monitoring-client.ts
var import_monitoring = require("@wix/monitoring");
var import_monitoring_browser_sentry = require("@wix/monitoring-browser-sentry");
var import_monitoring_browser_panorama = require("@wix/monitoring-browser-panorama");
var isSentryConfig = (monitoringConfig) => monitoringConfig?.type === "SENTRY";
var isPanoramaConfig = (monitoringConfig) => monitoringConfig?.type === "PANORAMA";
var getMonitoringClientFunction = ({
  sentrySDK,
  sentryTransport,
  panoramaClient,
  monitoringConfig,
  hostContext
}) => {
  if (isSentryConfig(monitoringConfig)) {
    const { sentryOptions } = monitoringConfig;
    return () => (0, import_monitoring_browser_sentry.createSentryBrowserClient)({
      dsn: sentryOptions?.dsn,
      hostContext,
      sentrySDK,
      transport: sentryTransport
    });
  } else if (isPanoramaConfig(monitoringConfig)) {
    return () => (0, import_monitoring_browser_panorama.createPanoramaBrowserClient)({ panoramaClient });
  }
  return () => (0, import_monitoring.createFallbackMonitoringClient)(
    "Invalid monitoring configuration. Please check the monitoring setup of your application."
  );
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getMonitoringClientFunction
});
