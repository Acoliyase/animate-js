"use strict";
/**
 * @wix/consent-policy-manager - Wix Consent Policy SDK
 *
 * This package provides a type-safe interface for managing site visitor cookie preferences
 * and 3rd-party data transfers for GDPR and CCPA compliance by proxying to the browser's
 * window.consentPolicyManager methods.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Gets the site visitor's consent policy regarding allowed cookies and 3rd-party data transfers for GDPR or CCPA compliance.
 *
 * Retrieves the site visitor's consent policy details, including which cookies are allowed and whether data transfer to 3rd parties is permitted.
 *
 * @returns Promise that resolves to the current consent policy details
 * @throws Error if the consent policy manager is not available or if an error occurs
 *
 * @example
 * ```typescript
 * import {
 *   getCurrentConsentPolicy,
 *   setConsentPolicy,
 *   resetConsentPolicy,
 *   publishPolicyUpdateRequestedEvent
 * } from '@wix/consent-policy-manager';
 *
 * // Get current policy (synchronous)
 * const policyDetails = getCurrentConsentPolicy();
 * console.log('Current policy:', policyDetails.policy);
 *
 * // Set policy using callbacks
 * setConsentPolicy(
 *   { essential: true, functional: true, analytics: false, advertising: false, dataToThirdParty: false },
 *   (policyDetails) => console.log('Policy updated:', policyDetails),
 *   (error) => console.error('Error:', error)
 * );
 *
 * // Set policy using async/await (callbacks optional)
 * try {
 *   await setConsentPolicy({
 *     essential: true,
 *     functional: true,
 *     analytics: false,
 *     advertising: false,
 *     dataToThirdParty: false
 *   });
 *   console.log('Policy updated successfully');
 * } catch (error) {
 *   console.error('Error:', error);
 * }
 * ```
 */
__exportStar(require("./consent-policy.js"), exports);
//# sourceMappingURL=index.js.map