import React from 'react';
import type { Menu, Section } from '../services/types.js';
import { type AsChildChildren } from '@wix/headless-utils/react';
export interface MenuRootProps {
    children: React.ReactNode;
    menu?: Menu;
}
export interface MenuNameProps {
    /** Whether to render as a child component */
    asChild?: boolean;
    /** Custom render function when using asChild */
    children?: AsChildChildren<{
        name: string;
    }>;
    /** CSS classes to apply to the default element */
    className?: string;
}
export interface MenuDescriptionProps {
    /** Whether to render as a child component */
    asChild?: boolean;
    /** Custom render function when using asChild */
    children?: AsChildChildren<{
        description: string;
    }>;
    /** CSS classes to apply to the default element */
    className?: string;
}
export interface MenuSectionsProps {
    children: (props: {
        sections: Section[];
    }) => React.ReactNode;
}
export interface MenuSectionsRepeaterProps {
    children: React.ReactNode;
}
export declare function Root(props: MenuRootProps): import("react/jsx-runtime").JSX.Element | null;
/**
 * Displays the menu name with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Menu.Name className="text-2xl font-semibold" />
 *
 * // asChild with primitive
 * <Menu.Name asChild>
 *   <h2 className="text-2xl font-semibold" />
 * </Menu.Name>
 *
 * // asChild with react component
 * <Menu.Name asChild>
 *   {React.forwardRef(({name, ...props}, ref) => (
 *     <h2 ref={ref} {...props} className="text-2xl font-semibold">
 *       {name}
 *     </h2>
 *   ))}
 * </Menu.Name>
 * ```
 */
export declare const Name: React.ForwardRefExoticComponent<MenuNameProps & React.RefAttributes<HTMLElement>>;
/**
 * Displays the menu description with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Menu.Description className="text-secondary-foreground" />
 *
 * // asChild with primitive
 * <Menu.Description asChild>
 *   <p className="text-secondary-foreground" />
 * </Menu.Description>
 *
 * // asChild with react component
 * <Menu.Description asChild>
 *   {React.forwardRef(({description, ...props}, ref) => (
 *     <p ref={ref} {...props} className="text-secondary-foreground">
 *       {description}
 *     </p>
 *   ))}
 * </Menu.Description>
 * ```
 */
export declare const Description: React.ForwardRefExoticComponent<MenuDescriptionProps & React.RefAttributes<HTMLElement>>;
export declare const SectionsRepeater: React.ForwardRefExoticComponent<MenuSectionsRepeaterProps & React.RefAttributes<HTMLElement>>;
/**
 * Menu namespace containing all menu components
 * following the compound component pattern: MenuComponent.Root, MenuComponent.Name, MenuComponent.Description, etc.
 */
export declare const MenuComponent: {
    /** Menu root component */
    readonly Root: typeof Root;
    /** Menu name component */
    readonly Name: React.ForwardRefExoticComponent<MenuNameProps & React.RefAttributes<HTMLElement>>;
    /** Menu description component */
    readonly Description: React.ForwardRefExoticComponent<MenuDescriptionProps & React.RefAttributes<HTMLElement>>;
    /** Menu sections repeater component */
    readonly SectionsRepeater: React.ForwardRefExoticComponent<MenuSectionsRepeaterProps & React.RefAttributes<HTMLElement>>;
};
