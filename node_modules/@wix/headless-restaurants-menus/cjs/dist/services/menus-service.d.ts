import type { Menu, Section, Variant, Label, Modifier, EnhancedItem, EnhancedModifierGroup, Location } from './types.js';
import { type Signal, type ReadOnlySignal } from '@wix/services-definitions/core-services/signals';
export interface MenusServiceConfig {
    autoLoad?: boolean;
    menus?: Menu[];
    sections?: Section[];
    items?: EnhancedItem[];
    variants?: Variant[];
    labels?: Label[];
    modifierGroups?: EnhancedModifierGroup[];
    modifiers?: Modifier[];
}
export interface MenusServiceAPI {
    menus: Signal<Menu[]>;
    filteredMenus: ReadOnlySignal<Menu[]>;
    sections: Signal<Section[]>;
    items: Signal<EnhancedItem[]>;
    variants: Signal<Variant[]>;
    labels: Signal<Label[]>;
    modifierGroups: Signal<EnhancedModifierGroup[]>;
    modifiers: Signal<Modifier[]>;
    locations: Signal<Location[]>;
    loading: Signal<boolean>;
    error: Signal<string | null>;
    selectedMenu: Signal<Menu | null>;
    selectedLocation: Signal<string | null>;
}
export declare const MenusServiceDefinition: string & {
    __api: MenusServiceAPI;
    __config: MenusServiceConfig;
    isServiceDefinition?: boolean;
} & MenusServiceAPI;
export declare const getLocations: (menus: Menu[]) => Location[];
export declare const MenusService: import("@wix/services-definitions").ServiceFactory<string & {
    __api: MenusServiceAPI;
    __config: MenusServiceConfig;
    isServiceDefinition?: boolean;
} & MenusServiceAPI, MenusServiceConfig>;
export declare function loadMenusServiceConfig(): Promise<MenusServiceConfig>;
