import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { CoreVariant, VariantName, VariantPrice } from './core/index.js';
import { AsChildSlot } from '@wix/headless-utils/react';
import { TestIds } from './TestIds.js';
export function Root(props) {
    if (!props.variant) {
        return null;
    }
    return _jsx(CoreVariant, { variant: props.variant, children: props.children });
}
/**
 * Displays the variant name with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Variant.Name className="text-lg font-semibold" />
 *
 * // asChild with primitive
 * <Variant.Name asChild>
 *   <h3 className="text-lg font-semibold" />
 * </Variant.Name>
 *
 * // asChild with react component
 * <Variant.Name asChild>
 *   {React.forwardRef(({name, ...props}, ref) => (
 *     <h3 ref={ref} {...props} className="text-lg font-semibold">
 *       {name}
 *     </h3>
 *   ))}
 * </Variant.Name>
 * ```
 */
export const Name = React.forwardRef((props, ref) => {
    const { asChild, children, className, ...otherProps } = props;
    return (_jsx(VariantName, { children: ({ name }) => {
            return (_jsx(AsChildSlot, { ref: ref, asChild: asChild, className: className, "data-testid": TestIds.variantName, customElement: children, customElementProps: { name }, content: name, ...otherProps, children: _jsx("p", { children: name }) }));
        } }));
});
/**
 * Displays the variant price with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Variant.Price className="text-lg font-bold text-primary" />
 *
 * // asChild with primitive
 * <Variant.Price asChild>
 *   <span className="text-lg font-bold text-primary" />
 * </Variant.Price>
 *
 * // asChild with react component
 * <Variant.Price asChild>
 *   {React.forwardRef(({price, formattedPrice, hasPrice, ...props}, ref) => (
 *     <span ref={ref} {...props} className="text-lg font-bold text-primary">
 *       {formattedPrice || price || null}
 *     </span>
 *   ))}
 * </Variant.Price>
 * ```
 */
export const Price = React.forwardRef((props, ref) => {
    const { asChild, children, className, ...otherProps } = props;
    return (_jsx(VariantPrice, { children: ({ price, formattedPrice, hasPrice }) => {
            if (!hasPrice && !formattedPrice && !price) {
                return null;
            }
            const displayPrice = formattedPrice || price;
            return (_jsx(AsChildSlot, { ref: ref, asChild: asChild, className: className, "data-testid": TestIds.variantPrice, customElement: children, customElementProps: { price, formattedPrice, hasPrice }, content: displayPrice, ...otherProps, children: _jsx("p", { children: displayPrice }) }));
        } }));
});
Name.displayName = 'Variant.Name';
Price.displayName = 'Variant.Price';
/**
 * Variant namespace containing all variant components
 * following the compound component pattern: VariantComponent.Root, VariantComponent.Name, VariantComponent.Price
 */
export const VariantComponent = {
    /** Variant root component */
    Root,
    /** Variant name component */
    Name,
    /** Variant price component */
    Price,
};
