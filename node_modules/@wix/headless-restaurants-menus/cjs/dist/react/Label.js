import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { WixMediaImage } from '@wix/headless-media/react';
import { AsChildSlot } from '@wix/headless-utils/react';
import { TestIds } from './TestIds.js';
import { CoreLabel, LabelName, LabelIcon } from './core/index.js';
export const Root = (props) => {
    if (!props.label) {
        return null;
    }
    return _jsx(CoreLabel, { label: props.label, children: props.children });
};
/**
 * Displays the label name with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Label.Name className="text-xs" />
 *
 * // asChild with primitive
 * <Label.Name asChild>
 *   <span className="text-xs" />
 * </Label.Name>
 *
 * // asChild with react component
 * <Label.Name asChild>
 *   {React.forwardRef(({name, ...props}, ref) => (
 *     <span ref={ref} {...props} className="text-xs">
 *       {name}
 *     </span>
 *   ))}
 * </Label.Name>
 * ```
 */
export const Name = React.forwardRef((props, ref) => {
    const { asChild, children, className, ...otherProps } = props;
    return (_jsx(LabelName, { children: ({ name }) => {
            return (_jsx(AsChildSlot, { ref: ref, asChild: asChild, className: className, "data-testid": TestIds.labelName, customElement: children, customElementProps: { name }, content: name, ...otherProps, children: _jsx("p", { children: name }) }));
        } }));
});
Name.displayName = 'Label.Name';
/**
 * Displays the label icon with customizable rendering following the documented API.
 * Provides the actual icon element when available.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Label.Icon className="w-4 h-4 object-contain" />
 *
 * // asChild with primitive
 * <Label.Icon asChild>
 *   <img className="w-4 h-4 object-contain" />
 * </Label.Icon>
 *
 * // asChild with custom component
 * <Label.Icon asChild>
 *   {React.forwardRef(({hasIcon, icon, altText, ...props}, ref) => (
 *     <div ref={ref} {...props} className="w-4 h-4">
 *       {hasIcon && <img src={icon} alt={altText} />}
 *     </div>
 *   ))}
 * </Label.Icon>
 *
 * // Custom render function
 * <Label.Icon>
 *   {({ hasIcon, icon, altText }) => (
 *     <div className="w-4 h-4 flex-shrink-0">
 *       {hasIcon && (
 *         <WixMediaImage media={{ image: icon }} alt={altText} width={16} height={16} />
 *       )}
 *     </div>
 *   )}
 * </Label.Icon>
 * ```
 */
export const Icon = React.forwardRef((props, ref) => {
    const { asChild, children, ...otherProps } = props;
    return (_jsx(LabelIcon, { children: ({ icon, hasIcon, altText }) => {
            if (asChild && children) {
                return children({ hasIcon, icon, altText }, ref);
            }
            return (_jsx(WixMediaImage, { ref: ref, media: { image: icon ?? '' }, isShape: true, alt: altText, "data-testid": TestIds.labelIcon, ...otherProps }));
        } }));
});
Icon.displayName = 'Label.Icon';
/**
 * Label namespace containing all label components
 * following the compound component pattern: LabelComponent.Root, LabelComponent.Name, LabelComponent.Icon
 */
export const LabelComponent = {
    /** Label root component */
    Root,
    /** Label name component */
    Name,
    /** Label icon component */
    Icon,
};
