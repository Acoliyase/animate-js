import React from 'react';
import type { Section } from '../services/types.js';
import { type AsChildChildren } from '@wix/headless-utils/react';
export interface SectionRootProps {
    children: React.ReactNode;
    section?: Section;
}
export interface SectionNameProps {
    /** Whether to render as a child component */
    asChild?: boolean;
    /** Custom render function when using asChild */
    children?: AsChildChildren<{
        name: string;
    }>;
    /** CSS classes to apply to the default element */
    className?: string;
}
export interface SectionDescriptionProps {
    /** Whether to render as a child component */
    asChild?: boolean;
    /** Custom render function when using asChild */
    children?: AsChildChildren<{
        description: string;
    }>;
    /** CSS classes to apply to the default element */
    className?: string;
}
export interface SectionItemsRepeaterProps {
    children: React.ReactNode;
}
export declare function Root(props: SectionRootProps): import("react/jsx-runtime").JSX.Element | null;
/**
 * Displays the section name with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Section.Name className="text-xl font-medium" />
 *
 * // asChild with primitive
 * <Section.Name asChild>
 *   <h3 className="text-xl font-medium" />
 * </Section.Name>
 *
 * // asChild with react component
 * <Section.Name asChild>
 *   {React.forwardRef(({name, ...props}, ref) => (
 *     <h3 ref={ref} {...props} className="text-xl font-medium">
 *       {name}
 *     </h3>
 *   ))}
 * </Section.Name>
 * ```
 */
export declare const Name: React.ForwardRefExoticComponent<SectionNameProps & React.RefAttributes<HTMLElement>>;
/**
 * Displays the section description with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Section.Description className="text-secondary-foreground" />
 *
 * // asChild with primitive
 * <Section.Description asChild>
 *   <p className="text-secondary-foreground" />
 * </Section.Description>
 *
 * // asChild with react component
 * <Section.Description asChild>
 *   {React.forwardRef(({description, ...props}, ref) => (
 *     <p ref={ref} {...props} className="text-secondary-foreground">
 *       {description}
 *     </p>
 *   ))}
 * </Section.Description>
 * ```
 */
export declare const Description: React.ForwardRefExoticComponent<SectionDescriptionProps & React.RefAttributes<HTMLElement>>;
export declare const ItemsRepeater: React.ForwardRefExoticComponent<SectionItemsRepeaterProps & React.RefAttributes<HTMLElement>>;
/**
 * Section namespace containing all section components
 * following the compound component pattern: SectionComponent.Root, SectionComponent.Name, SectionComponent.Description, etc.
 */
export declare const SectionComponent: {
    /** Section root component */
    readonly Root: typeof Root;
    /** Section name component */
    readonly Name: React.ForwardRefExoticComponent<SectionNameProps & React.RefAttributes<HTMLElement>>;
    /** Section description component */
    readonly Description: React.ForwardRefExoticComponent<SectionDescriptionProps & React.RefAttributes<HTMLElement>>;
    /** Section items repeater component */
    readonly ItemsRepeater: React.ForwardRefExoticComponent<SectionItemsRepeaterProps & React.RefAttributes<HTMLElement>>;
};
