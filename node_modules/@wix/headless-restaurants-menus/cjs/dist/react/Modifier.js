import { jsx as _jsx } from "react/jsx-runtime";
import React from 'react';
import { AsChildSlot } from '@wix/headless-utils/react';
import { TestIds } from './TestIds.js';
import { CoreModifier, ModifierName, ModifierPrice } from './core/index.js';
export const Root = (props) => {
    if (!props.modifier) {
        return null;
    }
    return (_jsx(CoreModifier, { modifier: props.modifier, children: props.children }));
};
/**
 * Displays the modifier name with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Modifier.Name className="text-sm" />
 *
 * // asChild with primitive
 * <Modifier.Name asChild>
 *   <span className="text-sm" />
 * </Modifier.Name>
 *
 * // asChild with react component
 * <Modifier.Name asChild>
 *   {React.forwardRef(({name, ...props}, ref) => (
 *     <span ref={ref} {...props} className="text-sm">
 *       {name}
 *     </span>
 *   ))}
 * </Modifier.Name>
 * ```
 */
export const Name = React.forwardRef((props, ref) => {
    const { asChild, children, className, ...otherProps } = props;
    return (_jsx(ModifierName, { children: ({ name }) => {
            return (_jsx(AsChildSlot, { ref: ref, asChild: asChild, className: className, "data-testid": TestIds.modifierName, customElement: children, customElementProps: { name }, content: name, ...otherProps, children: _jsx("p", { children: name }) }));
        } }));
});
/**
 * Displays the modifier price with customizable rendering following the documented API.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Modifier.Price className="text-sm font-medium" />
 *
 * // asChild with primitive
 * <Modifier.Price asChild>
 *   <span className="text-sm font-medium" />
 * </Modifier.Price>
 *
 * // asChild with react component
 * <Modifier.Price asChild>
 *   {React.forwardRef(({price, hasAdditionalCharge, formattedAdditionalCharge, ...props}, ref) => (
 *     <span ref={ref} {...props} className="text-sm font-medium">
 *       {hasAdditionalCharge ? formattedAdditionalCharge : `$${price.toFixed(2)}`}
 *     </span>
 *   ))}
 * </Modifier.Price>
 * ```
 */
export const Price = React.forwardRef((props, ref) => {
    const { asChild, children, className, ...otherProps } = props;
    return (_jsx(ModifierPrice, { children: ({ hasAdditionalCharge, formattedAdditionalCharge, additionalCharge, }) => {
            return (_jsx(AsChildSlot, { ref: ref, asChild: asChild, className: className, "data-testid": TestIds.modifierPrice, customElement: children, customElementProps: {
                    hasAdditionalCharge,
                    formattedAdditionalCharge,
                    additionalCharge,
                }, content: formattedAdditionalCharge ?? additionalCharge, ...otherProps, children: _jsx("p", { children: formattedAdditionalCharge ?? additionalCharge }) }));
        } }));
});
Name.displayName = 'Modifier.Name';
Price.displayName = 'Modifier.Price';
/**
 * Modifier namespace containing all modifier components
 * following the compound component pattern: ModifierComponent.Root, ModifierComponent.Name, ModifierComponent.Price
 */
export const ModifierComponent = {
    /** Modifier root component */
    Root,
    /** Modifier name component */
    Name,
    /** Modifier price component */
    Price,
};
