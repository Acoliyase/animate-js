import { CloudProvider } from "./constants.js";
import dotenv from 'dotenv';
class CloudProviderFetcher {
    constructor(envPath = '.env.local') {
        dotenv.config({ path: envPath });
    }
    getCloudProvider() {
        const cloudProvider = process.env.WIX_CLOUD_PROVIDER;
        if (cloudProvider === CloudProvider.KUBERNETES) {
            return CloudProvider.KUBERNETES;
        }
        return CloudProvider.CLOUD_FLARE;
    }
}
export { CloudProviderFetcher };
