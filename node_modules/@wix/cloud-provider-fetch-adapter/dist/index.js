import { AdapterFactory } from './AdapterFactory.js';
import { CloudProviderFetchIntegration } from './CloudProviderFetchIntegration.js';
import { CloudProviderFetcher } from './CloudProviderFetcher.js';
const createIntegration = (options) => {
    const cloudProviderFetchIntegration = new CloudProviderFetchIntegration(new CloudProviderFetcher(), AdapterFactory);
    return cloudProviderFetchIntegration.createIntegration(options);
};
export default createIntegration;
