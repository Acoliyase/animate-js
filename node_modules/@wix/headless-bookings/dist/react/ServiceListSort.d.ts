import React from 'react';
import { Sort as SortPrimitive } from '@wix/headless-components/react';
export interface ServiceListSortProps {
    children: (props: {
        currentSort: {
            fieldName: string;
            order: 'ASC' | 'DESC';
        }[];
        sortOptions: SortPrimitive.SortOption[];
        setSort: (sort: {
            fieldName: string;
            order: 'ASC' | 'DESC';
        }[]) => void;
    }) => React.ReactNode;
}
export declare const ServiceListSort: (props: ServiceListSortProps) => React.ReactNode;
export interface ServiceListSeparateSortProps {
    children: (props: {
        currentSort: {
            fieldName: string;
            order: 'ASC' | 'DESC';
        }[];
        sortFieldOptions: Array<{
            fieldName: string;
            label: string;
        }>;
        sortOrderOptions: Array<{
            order: 'ASC' | 'DESC';
            label: string;
        }>;
        setSort: (sort: {
            fieldName: string;
            order: 'ASC' | 'DESC';
        }[]) => void;
    }) => React.ReactNode;
}
export declare function ServiceListSeparateSort(props: ServiceListSeparateSortProps): React.ReactNode;
