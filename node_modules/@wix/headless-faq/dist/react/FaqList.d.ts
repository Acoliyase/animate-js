import React from 'react';
import { type AsChildChildren } from '@wix/headless-utils/react';
import { type FaqServiceConfig, type FaqCategory } from '../services/index.js';
/**
 * Props for FaqList Root component
 */
export interface FaqListRootProps {
    children: React.ReactNode;
    category: FaqCategory;
    className?: string;
    /** FAQ service config for this category's FAQs */
    faqConfig?: FaqServiceConfig;
}
/**
 * Root component that provides service context for a FAQ category and its FAQs.
 * Automatically loads FAQs for the category.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <FaqList.Root category={category}>
 *   <FaqList.Name />
 *   <FaqList.Items>
 *     <FaqList.FaqRepeater>
 *       <Faq.Name />
 *       <Faq.Answer />
 *     </FaqList.FaqRepeater>
 *   </FaqList.Items>
 * </FaqList.Root>
 * ```
 */
export declare function Root(props: FaqListRootProps): React.ReactNode;
/**
 * Props for FaqList Name component
 */
export interface NameProps {
    /** Whether to render as a child component */
    asChild?: boolean;
    /** Custom render function when using asChild */
    children?: AsChildChildren<{
        name: string;
    }>;
    /** CSS classes to apply to the default element */
    className?: string;
}
/**
 * Displays the FAQ category name.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <FaqList.Name className="text-2xl font-bold" />
 *
 * // asChild with primitive
 * <FaqList.Name asChild>
 *   <h2 className="text-2xl font-bold" />
 * </FaqList.Name>
 *
 * // asChild with react component
 * <FaqList.Name asChild>
 *   {React.forwardRef(({name, ...props}, ref) => (
 *     <h2 ref={ref} {...props} className="text-2xl font-bold">
 *       {name}
 *     </h2>
 *   ))}
 * </FaqList.Name>
 * ```
 */
export declare const Name: React.ForwardRefExoticComponent<NameProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for FaqList Items component
 */
export interface ItemsProps {
    children: React.ReactNode;
    emptyState?: React.ReactNode;
}
/**
 * Container for the list of FAQs with empty state support.
 * Follows List Container Level pattern.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <FaqList.Items emptyState={<div>No FAQs available</div>}>
 *   <FaqList.FaqRepeater>
 *     <Faq.Name />
 *     <Faq.Answer />
 *   </FaqList.FaqRepeater>
 * </FaqList.Items>
 *
 * // Simple container usage
 * <FaqList.Items emptyState={<div>No FAQs</div>}>
 *   <div className="faqs-container">
 *     <FaqList.FaqRepeater>
 *       <Faq.Name />
 *       <Faq.Answer />
 *     </FaqList.FaqRepeater>
 *   </div>
 * </FaqList.Items>
 * ```
 */
export declare const Items: React.ForwardRefExoticComponent<ItemsProps & React.RefAttributes<HTMLDivElement>>;
/**
 * Props for FaqList FaqRepeater component
 */
export interface FaqRepeaterProps {
    children: React.ReactNode;
}
/**
 * Repeater component that renders Faq.Root for each FAQ entry.
 * Follows Repeater Level pattern.
 *
 * @component
 * @example
 * ```tsx
 * <FaqList.FaqRepeater>
 *   <Faq.Name />
 *   <Faq.Answer />
 * </FaqList.FaqRepeater>
 * ```
 */
export declare const FaqRepeater: React.ForwardRefExoticComponent<FaqRepeaterProps & React.RefAttributes<HTMLElement>>;
//# sourceMappingURL=FaqList.d.ts.map