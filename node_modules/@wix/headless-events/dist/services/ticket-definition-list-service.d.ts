import { type Signal } from '@wix/services-definitions/core-services/signals';
import { type TicketDefinition } from './ticket-definition-service.js';
import { type Event } from './event-service.js';
export interface TicketDefinitionListServiceAPI {
    ticketDefinitions: Signal<TicketDefinition[]>;
    selectedQuantities: Signal<TicketReservationQuantity[]>;
    setQuantity: (params: {
        ticketDefinitionId: string;
        quantity?: number;
        priceOverride?: string;
        pricingOptionId?: string;
    }) => void;
    getMaxQuantity: (ticketDefinitionId: string) => number;
    getCurrentQuantity: (ticketDefinitionId: string, pricingOptionId?: string) => number;
    getCurrentPriceOverride: (ticketDefinitionId: string) => string | undefined;
    isSoldOut: (ticketDefinitionId: string) => boolean;
}
export interface TicketReservationQuantity {
    ticketDefinitionId: string;
    quantity?: number;
    priceOverride?: string;
    pricingOptionId?: string;
}
export interface TicketReservationTotals {
    currency: string;
    subtotal: number;
    tax: number;
    fee: number;
    total: number;
    formattedSubtotal: string;
    formattedTax: string;
    formattedFee: string;
    formattedTotal: string;
}
export interface TicketDefinitionListServiceConfig {
    ticketDefinitions: TicketDefinition[];
}
export declare const TicketDefinitionListServiceDefinition: string & {
    __api: TicketDefinitionListServiceAPI;
    __config: TicketDefinitionListServiceConfig;
    isServiceDefinition?: boolean;
} & TicketDefinitionListServiceAPI;
export declare const TicketDefinitionListService: import("@wix/services-definitions").ServiceFactory<string & {
    __api: TicketDefinitionListServiceAPI;
    __config: TicketDefinitionListServiceConfig;
    isServiceDefinition?: boolean;
} & TicketDefinitionListServiceAPI, TicketDefinitionListServiceConfig>;
export declare function loadTicketDefinitionListServiceConfig({ eventId, }: LoadTicketDefinitionListServiceConfigParams): Promise<TicketDefinitionListServiceConfig>;
export declare function getTicketReservationTotals(event: Event, ticketDefinitions: TicketDefinition[], selectedQuantities: TicketReservationQuantity[], locale: Intl.LocalesArgument): TicketReservationTotals;
interface LoadTicketDefinitionListServiceConfigParams {
    eventId: string;
}
export {};
