import { Signal } from '@wix/services-definitions/core-services/signals';
import { orders } from '@wix/events';
export type Order = orders.Order;
export interface OrderServiceAPI {
    order: Signal<Order>;
    isPolling: Signal<boolean>;
}
export interface OrderServiceConfig {
    order: Order;
    eventId: string;
    orderNumber: string;
}
export declare const OrderServiceDefinition: string & {
    __api: OrderServiceAPI;
    __config: OrderServiceConfig;
    isServiceDefinition?: boolean;
} & OrderServiceAPI;
export declare const OrderService: import("@wix/services-definitions").ServiceFactory<string & {
    __api: OrderServiceAPI;
    __config: OrderServiceConfig;
    isServiceDefinition?: boolean;
} & OrderServiceAPI, OrderServiceConfig>;
export declare function loadOrderServiceConfig({ eventId, orderNumber, }: LoadOrderServiceConfigParams): Promise<OrderServiceConfig>;
export declare function isOrderReady(order: Order): boolean;
interface LoadOrderServiceConfigParams {
    eventId: string;
    orderNumber: string;
}
export {};
