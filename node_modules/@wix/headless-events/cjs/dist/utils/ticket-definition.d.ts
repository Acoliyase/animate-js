import { wixEventsV2 } from '@wix/events';
import { type TicketDefinition } from '../services/ticket-definition-service.js';
export declare const getTicketDefinitionCurrency: (ticketDefinition: TicketDefinition) => string;
export declare const getTicketDefinitionTax: (taxSettings: wixEventsV2.TaxSettings, price: number, currency: string, locale: Intl.LocalesArgument) => {
    name: string;
    rate: number;
    included: boolean;
    taxableValue: number;
    taxValue: number;
    formattedTaxValue: string;
};
export declare const getTicketDefinitionFee: (taxSettings: wixEventsV2.TaxSettings | undefined, price: number, currency: string, guestPricing: boolean, locale: Intl.LocalesArgument) => {
    rate: number;
    value: number;
    formattedValue: string;
};
export declare const isTicketDefinitionAvailable: (ticketDefinition: TicketDefinition) => boolean;
