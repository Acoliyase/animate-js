import { jsx as _jsx } from "react/jsx-runtime";
import { useService, WixServices } from '@wix/services-manager-react';
import { createServicesMap } from '@wix/services-manager';
import { ScheduleItemTagService, ScheduleItemTagServiceDefinition, } from '../../services/schedule-item-tag-service.js';
/**
 * ScheduleItemTag Root core component that provides tag service data.
 *
 * @component
 */
export function Root(props) {
    const { children, tag } = props;
    const tagServiceConfig = {
        tag,
    };
    return (_jsx(WixServices, { servicesMap: createServicesMap().addService(ScheduleItemTagServiceDefinition, ScheduleItemTagService, tagServiceConfig), children: children }));
}
/**
 * ScheduleItemTag Tag core component that provides tag data.
 *
 * @component
 */
export function Tag(props) {
    const tagService = useService(ScheduleItemTagServiceDefinition);
    const tag = tagService.tag.get();
    return props.children({ tag });
}
