"use strict";

exports.__esModule = true;
exports.assertProp = assertProp;
function assertProp(value, propName, componentName) {
  if (process.env.NODE_ENV !== 'production') {
    if (value == null) {
      throw new Error(`${propName} prop was not passed, make sure to propagate all props to ${componentName}`);
    }
  }
}
//# sourceMappingURL=assertProp.js.map