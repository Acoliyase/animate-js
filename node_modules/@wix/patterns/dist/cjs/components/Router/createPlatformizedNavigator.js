"use strict";

exports.__esModule = true;
exports.createPlatformizedNavigator = void 0;
var _reactRouterDom = require("react-router-dom");
const createPlatformizedNavigator = ({
  pageLocation,
  container
}) => {
  return {
    createHref: _reactRouterDom.createPath,
    push: to => {
      const relativeUrl = typeof to === 'string' ? to : (0, _reactRouterDom.createPath)(to);
      container.navigateTo({
        pageId: pageLocation.pageId,
        relativeUrl
      });
    },
    replace: () => {
      throw new Error('not yet supported');
    },
    go: () => {
      throw new Error('not yet supported');
    }
  };
};
exports.createPlatformizedNavigator = createPlatformizedNavigator;
//# sourceMappingURL=createPlatformizedNavigator.js.map