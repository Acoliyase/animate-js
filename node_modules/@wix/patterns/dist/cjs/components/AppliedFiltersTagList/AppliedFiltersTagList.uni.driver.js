"use strict";

exports.__esModule = true;
exports.AppliedFiltersTagListUniDriver = AppliedFiltersTagListUniDriver;
var _unidriver = require("@wix/design-system/dist/testkit/unidriver");
function AppliedFiltersTagListUniDriver(base, body) {
  const tagList = (0, _unidriver.TagListUniDriver)(base.$('[data-hook="active-filter-tags"]'), body);
  const getAppliedFilterTag = (filterName, id) => (0, _unidriver.TagUniDriver)(base.$(`[data-hook="${filterName}${id ? `-${id}` : ''}"]`), body);
  const appliedFilterTagExists = (filterName, id) => getAppliedFilterTag(filterName, id).exists();
  const getAppliedFilterTagText = (filterName, id) => getAppliedFilterTag(filterName, id).getLabel();
  const removeAppliedFilterTag = (filterName, id) => getAppliedFilterTag(filterName, id).removeTag();
  return {
    ...tagList,
    getAppliedFilterTagText,
    removeAppliedFilterTag,
    appliedFilterTagExists,
    getAppliedFilterTag
  };
}
//# sourceMappingURL=AppliedFiltersTagList.uni.driver.js.map