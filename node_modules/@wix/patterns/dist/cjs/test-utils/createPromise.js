"use strict";

exports.__esModule = true;
exports.createPromise = createPromise;
exports.createPromiseFn = createPromiseFn;
function createPromise() {
  let resolver = () => {};
  const promise = new Promise(_resolver => resolver = _resolver);
  return {
    resolver,
    promise
  };
}
function createPromiseFn() {
  const {
    resolver,
    promise
  } = createPromise();
  return {
    resolver,
    fn: () => promise
  };
}
//# sourceMappingURL=createPromise.js.map