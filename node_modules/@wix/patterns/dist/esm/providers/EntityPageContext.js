import { createContext, useContext } from 'react';
import { assertContextValue } from '../createContext';
const context = createContext(null);
export const entityPageContext = context;
export const EntityPageContextProvider = context.Provider;
export const EntityPageContextConsumer = context.Consumer;
export const useEntityPageContext = () => {
    const value = useContext(entityPageContext);
    assertContextValue('EntityPageProvider', value);
    return value;
};
//# sourceMappingURL=EntityPageContext.js.map