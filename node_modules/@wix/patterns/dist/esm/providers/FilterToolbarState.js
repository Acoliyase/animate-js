import { useContext } from 'react';
import { ToolbarCollectionContext } from '../components/ToolbarCollectionContext';
const fallbackState = {
    getPendingFilter: (filter) => filter,
};
export function useFilterToolbarState() {
    const value = useContext(ToolbarCollectionContext);
    if (value == null) {
        return fallbackState;
    }
    return value;
}
//# sourceMappingURL=FilterToolbarState.js.map