import { CairoProvider as CairoProviderBase, } from '@wix/bex-core/react';
import { configure } from '@wix/bex-utils/react-dom';
import React, { useState } from 'react';
import { version as cairoVersion } from '../version';
import '@wix/design-system/styles.global.css';
import '../styles.global.css';
import * as wixDesignSystemVersion from '../wix-design-system-version';
import { minimalRequiredWdsRuntimeCheck } from '../minimalRequiredWdsRuntimeCheck';
configure();
if (process.env.NODE_ENV !== 'production') {
    minimalRequiredWdsRuntimeCheck({
        required: wixDesignSystemVersion.required,
    });
}
export function CairoProvider({ value: valueProp, children, ...rest }) {
    const [value] = useState(() => ({
        ...valueProp,
        environment: {
            ...valueProp.environment,
            uiLibVersion: cairoVersion,
        },
    }));
    return (React.createElement(CairoProviderBase, { ...rest, value: value }, children));
}
//# sourceMappingURL=CairoProvider.js.map