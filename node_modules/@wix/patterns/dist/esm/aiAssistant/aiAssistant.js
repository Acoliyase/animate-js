import { fqdnToString, isFqdnValid, } from '@wix/bex-core';
export function createWixPatternsCreationEventName(collection) {
    if (!collection.fqdn || (collection.fqdn && !isFqdnValid(collection.fqdn))) {
        throw new Error('FQDN is invalid. Please provide a valid FQDN in the collection hook state (use<YourComponentName>Collection)');
    }
    return `ITEM_CREATED_${fqdnToString(collection.fqdn)}`;
}
//# sourceMappingURL=aiAssistant.js.map