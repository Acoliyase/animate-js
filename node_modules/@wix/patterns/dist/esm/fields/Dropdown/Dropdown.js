import React, { useMemo } from 'react';
import { FormField, Dropdown as DropdownWSR, } from '@wix/design-system';
import { Field } from '../Field';
import { dataHooks } from '../dataHooks';
export const Dropdown = ({ dataHook, schema, initialValue, onChange, }) => {
    const options = useMemo(() => schema.options?.map((o) => ({ id: o, value: o })) || [], [schema]);
    return (React.createElement(Field, { dataHook: dataHook, type: schema.type },
        React.createElement(FormField, { label: schema.label, dataHook: dataHooks.Label },
            React.createElement(DropdownWSR, { dataHook: dataHooks.Input, options: options, initialSelectedId: initialValue, onSelect: ({ id }) => onChange(id) }))));
};
//# sourceMappingURL=Dropdown.js.map