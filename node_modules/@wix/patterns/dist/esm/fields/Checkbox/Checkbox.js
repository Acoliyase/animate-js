import React, { useState } from 'react';
import { Checkbox as CheckboxWSR } from '@wix/design-system';
import { Field } from '../Field';
import { dataHooks } from '../dataHooks';
export const Checkbox = ({ dataHook, schema, initialValue, onChange, }) => {
    const [value, setValue] = useState(initialValue);
    return (React.createElement(Field, { dataHook: dataHook, type: schema.type },
        React.createElement(CheckboxWSR, { dataHook: dataHooks.Input, checked: value, onChange: (e) => {
                setValue(e.target.checked);
                onChange(e.target.checked);
            } }, schema.label)));
};
//# sourceMappingURL=Checkbox.js.map