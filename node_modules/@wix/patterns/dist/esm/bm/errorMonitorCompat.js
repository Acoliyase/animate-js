import { useErrorMonitor, useSentry, } from '@wix/yoshi-flow-bm';
export function useErrorMonitorCompat() {
    let sentry = null;
    let errorMonitor = null;
    try {
        // eslint-disable-next-line react-hooks/rules-of-hooks
        errorMonitor = useErrorMonitor();
    }
    catch (e) { }
    try {
        // eslint-disable-next-line react-hooks/rules-of-hooks
        sentry = useSentry();
    }
    catch (e) { }
    return (errorMonitor ?? sentry);
}
export function getErrorMonitorCompat(flowAPI) {
    let sentry = null;
    let errorMonitor = null;
    try {
        errorMonitor = flowAPI.errorMonitor;
    }
    catch (e) { }
    try {
        sentry = flowAPI.sentry;
    }
    catch (e) { }
    return (errorMonitor ?? sentry);
}
//# sourceMappingURL=errorMonitorCompat.js.map