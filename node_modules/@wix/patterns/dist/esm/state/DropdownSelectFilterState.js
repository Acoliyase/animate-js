import { SelectState, } from '@wix/bex-core';
import { DropdownSelectState } from './DropdownSelectState';
import { action, makeObservable } from 'mobx';
export class DropdownSelectFilterState {
    constructor(params) {
        this.select = new SelectState({
            keyGetter: (value) => value.key,
        });
        this.dropdownSelect = new DropdownSelectState({
            select: this.select,
            container: params.container,
            toolbar: params.toolbar,
            filter: params.filter,
        });
        makeObservable(this, {
            init: action.bound,
        });
    }
    init() {
        const { dropdownSelect } = this;
        const dispose = dropdownSelect.init();
        return () => {
            dispose();
        };
    }
}
//# sourceMappingURL=DropdownSelectFilterState.js.map