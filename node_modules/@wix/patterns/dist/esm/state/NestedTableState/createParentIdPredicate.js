export function createParentIdPredicate(state) {
    const parentFilterName = state?.parentFilter?.name;
    return (query) => {
        const parentId = parentFilterName
            ? query.filters?.[parentFilterName]
            : undefined;
        return (item) => {
            const itemParentId = state?.levelDescriptor?.parentKey(item);
            if (parentId &&
                parentId.length &&
                !parentId.some((p) => p === 'null' ? itemParentId == null : itemParentId === p)) {
                return false;
            }
            return true;
        };
    };
}
//# sourceMappingURL=createParentIdPredicate.js.map