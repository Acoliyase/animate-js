import { DateRangePickerState } from './DateRangePickerState';
import { action, makeObservable } from 'mobx';
export class DateRangeFilterState {
    constructor(params) {
        this.dateRangePicker = new DateRangePickerState({
            toolbar: params.toolbar,
            translate: params.translate,
            filter: params.filter,
            filterByTime: params.filterByTime,
            mode: params.mode,
        });
        makeObservable(this, {
            init: action.bound,
        });
    }
    init() {
        const { dateRangePicker } = this;
        const dispose = dateRangePicker.init();
        return () => {
            dispose();
        };
    }
}
//# sourceMappingURL=DateRangeFilterState.js.map