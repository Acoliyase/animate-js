import React, { useEffect, useState } from 'react';
import { Page } from '@wix/design-system';
import { useFormPageContext } from '../../providers/FormPageContext';
export function FormFooter({ FooterElement, scrollableRef, }) {
    const [scrollable, setScrollable] = useState();
    const pageState = useFormPageContext();
    useEffect(() => {
        const pageStatus = pageState?.initTask?.status;
        if (!pageStatus.isLoading && !pageStatus.isIdle) {
            setScrollable(scrollableRef?.scrollHeight > scrollableRef?.clientHeight);
        }
    }, [pageState?.initTask?.status]);
    return (React.createElement(Page.Footer, null,
        React.createElement(Page.Footer.End, null, scrollable && React.createElement(FooterElement, null))));
}
//# sourceMappingURL=FormFooter.js.map