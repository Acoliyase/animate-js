import { computed, makeObservable, observable } from 'mobx';
export class AdditionalStepState {
    constructor(params) {
        this.isLoading = false;
        this.additionalData = undefined;
        this.collection = params.collection;
        makeObservable(this, {
            isLoading: observable.ref,
            selectedKeyedItem: computed,
            additionalData: observable.ref,
        });
    }
    get selectedKeyedItem() {
        return this.collection.bulkSelect.selectedKeyedItems?.[0];
    }
}
//# sourceMappingURL=AdditionalStepState.js.map