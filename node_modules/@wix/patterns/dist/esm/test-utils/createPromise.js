export function createPromise() {
    let resolver = () => { };
    const promise = new Promise((_resolver) => (resolver = _resolver));
    return {
        resolver,
        promise,
    };
}
export function createPromiseFn() {
    const { resolver, promise } = createPromise();
    return {
        resolver,
        fn: () => promise,
    };
}
//# sourceMappingURL=createPromise.js.map