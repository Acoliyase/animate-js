import React from 'react';
import { Card, DatePicker, FormField } from '@wix/design-system';
import { Controller } from 'react-hook-form';
function ExtensionWidget({ form, entity, }) {
    return (React.createElement(Card, null,
        React.createElement(Card.Header, { title: "Extension Widget" }),
        React.createElement(Card.Divider, null),
        React.createElement(Card.Content, null,
            React.createElement(Controller, { control: form.control, name: "createdDate", defaultValue: entity?.createdDate || undefined, rules: {
                    validate: (value) => {
                        return value?.getDay() !== 6
                            ? true
                            : `${value.toLocaleString('en', {
                                weekday: 'long',
                            })} is Shabbat, please choose a different day.`;
                    },
                }, render: ({ field: { onChange, onBlur, value, ref }, fieldState: { invalid, error }, }) => (React.createElement(FormField, { label: "Created At", status: invalid ? 'error' : undefined, statusMessage: error?.message },
                    React.createElement(DatePicker, { onClose: onBlur, onChange: onChange, value: value, inputProps: { inputRef: ref } }))) }))));
}
export default ExtensionWidget;
//# sourceMappingURL=ExtensionWidget.js.map