import { FilterRenderingContextProvider } from '../FilterRenderingContext';
import React, { useState } from 'react';
import { WixStyleReactDefaultsOverrideProvider } from '@wix/design-system';
export function CollectionToolbarsProvider({ children, }) {
    const [filterRenderingContext] = useState(() => ({
        location: 'toolbar',
    }));
    const [wsrDefaults] = useState(() => ({
        input: {
            size: 'small',
        },
        button: {
            size: 'small',
        },
    }));
    return (React.createElement(FilterRenderingContextProvider, { value: filterRenderingContext },
        React.createElement(WixStyleReactDefaultsOverrideProvider, { value: wsrDefaults }, children)));
}
//# sourceMappingURL=CollectionToolbarsProvider.js.map