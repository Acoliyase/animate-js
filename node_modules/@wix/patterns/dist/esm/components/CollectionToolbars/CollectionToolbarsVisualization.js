import { createElement } from 'react';
import { observer } from 'mobx-react-lite';
import { isValidElement } from '../isValidElement';
function _CollectionToolbarsVisualization(props) {
    const { visualization, state } = props;
    return isValidElement(visualization)
        ? visualization
        : createElement(visualization, { state });
}
export const CollectionToolbarsVisualization = observer(_CollectionToolbarsVisualization);
//# sourceMappingURL=CollectionToolbarsVisualization.js.map