import React from 'react';
export function FilterTagContent({ filterProps, values, }) {
    const filterName = filterProps?.toolbarTagProps?.label ??
        filterProps?.accordionItemProps?.label;
    return (React.createElement(React.Fragment, null,
        filterName && (React.createElement("span", { style: { fontWeight: 530 } }, `${filterName}: `)),
        values
            .map(({ filter, value }) => filter.itemName(value))
            .join(', ')));
}
//# sourceMappingURL=FilterTagContent.js.map