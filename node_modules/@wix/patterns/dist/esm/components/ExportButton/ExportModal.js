import { Modal } from '@wix/design-system';
import React, { useState } from 'react';
import { useToolbarCollectionContext } from '../ToolbarCollectionContext';
import { ModalState } from '@wix/bex-core';
import { ExportButtonModalLayout } from './ExportButtonModalLayout';
import { observer } from 'mobx-react-lite';
function _ExportModal(props) {
    const table = useToolbarCollectionContext();
    const [exportModalState] = useState(() => (table.exportState ??= new ModalState()));
    return (React.createElement(Modal, { onRequestClose: exportModalState.close, isOpen: exportModalState.isOpen, dataHook: "export-modal" },
        React.createElement(ExportButtonModalLayout, { ...props, exportModalState: exportModalState })));
}
export const ExportModal = observer(_ExportModal);
//# sourceMappingURL=ExportModal.js.map