import { Box, LinearProgressBar } from '@wix/design-system';
import React, { cloneElement } from 'react';
import { observer } from 'mobx-react-lite';
function _ExportStepError({ state, children, }) {
    const { counter } = state;
    return (React.createElement(Box, { gap: "SP2", direction: "vertical", paddingBottom: "SP1" },
        React.createElement(LinearProgressBar, { error: true, showProgressIndication: true, value: counter }),
        children &&
            cloneElement(children, {
                dataHook: 'export-error-message',
            })));
}
export const ExportStepError = observer(_ExportStepError);
//# sourceMappingURL=ExportStepError.js.map