import { Box, RadioGroup, Text } from '@wix/design-system';
import React from 'react';
import { observer } from 'mobx-react-lite';
import { useToolbarCollectionContext } from '../ToolbarCollectionContext';
import { RadioGroupSafe } from './RadioGroupSafe';
function _ExportStepConfig({ state, exportToCSVMessage, }) {
    const { selectedExportOption, disabledRadios, totalForExportMap, translate: t, } = state;
    const table = useToolbarCollectionContext();
    return (React.createElement(Box, { gap: "SP4", direction: "vertical", paddingBottom: "SP1" },
        React.createElement(RadioGroupSafe, { dataHook: "export-group", onChange: (val) => state.onExportTypeChange(val), value: selectedExportOption, disabledRadios: disabledRadios },
            React.createElement(RadioGroup.Radio, { value: "all" }, t('cairo.export.allItems')),
            React.createElement(RadioGroup.Radio, { value: "filtered" },
                t('cairo.export.filteredItems'),
                ' ',
                disabledRadios.indexOf('filtered') === -1
                    ? totalForExportMap.filtered != null &&
                        `(${totalForExportMap.filtered})`
                    : '(0)'),
            table.selectionProps.value.showSelection && (React.createElement(RadioGroup.Radio, { value: "selected" },
                t('cairo.export.selectedItems'),
                ' ',
                disabledRadios.indexOf('selected') === -1
                    ? totalForExportMap.selected != null &&
                        `(${totalForExportMap.selected})`
                    : '(0)'))),
        React.createElement(Text, { dataHook: "export-csv-message", widows: true }, exportToCSVMessage || t('cairo.export.exportToCSVMessage'))));
}
export const ExportStepConfig = observer(_ExportStepConfig);
//# sourceMappingURL=ExportStepConfig.js.map