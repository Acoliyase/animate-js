import { SectionHeader, Text, CounterBadge, Box, } from '@wix/design-system';
import React from 'react';
import { CollectionSectionHeaderActionBar } from './CollectionSectionHeaderActionBar';
export const CollectionSectionHeader = ({ section, skin, dataHook, biComponentType, }) => {
    const title = (React.createElement(Box, { verticalAlign: "middle", gap: "SP1" },
        React.createElement(Text, { size: "small", weight: "normal" }, section.title),
        section.badge?.visible && (React.createElement(CounterBadge, { truncate: false, skin: section.badge.skin || 'light', dataHook: "section-header-badge" },
            section.count,
            section.hasMore ? '+' : ''))));
    return (React.createElement(SectionHeader, { dataHook: dataHook, title: title, skin: skin, divider: "none", size: "small", suffix: React.createElement(CollectionSectionHeaderActionBar, { section: section, biComponentType: biComponentType }) }));
};
//# sourceMappingURL=CollectionSectionHeader.js.map