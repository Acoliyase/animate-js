import { Modal } from '@wix/design-system';
import React from 'react';
import { observer } from 'mobx-react-lite';
function _ModalWrapper({ state, ...rest }) {
    return (React.createElement("div", { onClick: (e) => e.stopPropagation() },
        React.createElement(Modal, { isOpen: state.isOpen, onRequestClose: state.close, ...rest })));
}
export const ModalWrapper = observer(_ModalWrapper);
//# sourceMappingURL=CollectionModal.js.map