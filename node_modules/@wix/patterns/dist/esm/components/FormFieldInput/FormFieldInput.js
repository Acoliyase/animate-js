import React from 'react';
import { Checkbox, DateInput, DateTime, Dropdown, LongText, Number, ShortText, } from '../../fields';
import { Url } from '../../fields/Url';
export const EDITABLE_INPUT_ELEMENTS = {
    dropdown: Dropdown,
    checkbox: Checkbox,
    shortText: ShortText,
    longText: LongText,
    date: DateInput,
    dateTime: DateTime,
    integer: Number,
    decimal: Number,
    url: Url,
};
const INPUT_ELEMENTS = {
    ...EDITABLE_INPUT_ELEMENTS,
    files: () => null,
    multiSelect: () => null, // not supported
};
export const FormFieldInput = ({ dataHook, state, schema, initialValue, onChange, inputRef, }) => {
    if (INPUT_ELEMENTS.hasOwnProperty(schema.type)) {
        const FieldInput = INPUT_ELEMENTS[schema.type];
        return (React.createElement(FieldInput, { dataHook: dataHook, state: state, initialValue: initialValue, schema: schema, onChange: (newValue) => onChange(newValue), inputRef: inputRef }));
    }
    return null;
};
//# sourceMappingURL=FormFieldInput.js.map