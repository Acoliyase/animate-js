import React, { useCallback } from 'react';
import { Box, Button, Divider, TextButton } from '@wix/design-system';
import { useWixPatternsContainer } from '@wix/bex-core/react';
import { observer } from 'mobx-react-lite';
const _Footer = ({ state }) => {
    const { translate: t } = useWixPatternsContainer();
    const closePopover = useCallback(() => {
        state.closePopover();
    }, [state]);
    const applyChanges = useCallback(() => {
        state.applyChanges();
    }, [state]);
    const handleManageTagsCtaClick = useCallback(() => {
        state.handleManageTagsCtaClick();
    }, [state]);
    return (React.createElement(Box, { height: "54px", width: "100%", direction: "vertical" },
        React.createElement(Divider, null),
        React.createElement(Box, { height: "100%", align: "space-between", verticalAlign: "middle", padding: "0 SP3" },
            !state.isLoading && !state.isError && (React.createElement(TextButton, { dataHook: "bulk-assign-tags-manage-tags-button", size: "small", onClick: handleManageTagsCtaClick }, t('cairo.tags.collectionPage.popover.manage.CTA'))),
            React.createElement(Box, { marginLeft: "auto" }, state.isDirty ? (React.createElement(Button, { onClick: applyChanges, dataHook: "bulk-assign-tags-apply-button" }, t('cairo.tags.collectionPage.applyChanges.CTA'))) : (React.createElement(Button, { onClick: closePopover, dataHook: "bulk-assign-tags-close-button" }, t('cairo.tags.collectionPage.popover.close.CTA')))))));
};
export const Footer = observer(_Footer);
//# sourceMappingURL=Footer.js.map