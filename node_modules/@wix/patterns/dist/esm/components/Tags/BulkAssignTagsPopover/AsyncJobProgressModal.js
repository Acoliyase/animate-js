import { observer } from 'mobx-react-lite';
import React from 'react';
import { Box, LinearProgressBar, Text } from '@wix/design-system';
import { useWixPatternsContainer } from '@wix/bex-core/react';
import { CustomActionModal } from '../../ActionModal';
export const _AsyncJobProgressModal = ({ state, }) => {
    const { asyncJobProgressModalState, closeAsyncJobProgressModal, updatedItemsPercentage, } = state.bulkAssignAsyncState;
    const { translate: t } = useWixPatternsContainer();
    return (React.createElement(CustomActionModal, { state: asyncJobProgressModalState, title: t('cairo.tags.bulkItemUpdate.updatingModal.title'), primaryButtonText: t('cairo.tags.bulkItemUpdate.updatingModal.CTA'), primaryButtonOnClick: closeAsyncJobProgressModal, onCloseButtonClick: closeAsyncJobProgressModal, dataHook: "async-job-progress-modal-layout", content: React.createElement(Box, { direction: "vertical", width: "552px", gap: "SP2", paddingTop: "SP1", paddingBottom: "SP1" },
            React.createElement(LinearProgressBar, { dataHook: "async-job-progress-modal-progress-bar", value: updatedItemsPercentage, showProgressIndication: true }),
            React.createElement(Text, { dataHook: "async-job-progress-modal-body-text" }, t('cairo.tags.bulkItemUpdate.updatingModal.body'))) }));
};
export const AsyncJobProgressModal = observer(_AsyncJobProgressModal);
//# sourceMappingURL=AsyncJobProgressModal.js.map