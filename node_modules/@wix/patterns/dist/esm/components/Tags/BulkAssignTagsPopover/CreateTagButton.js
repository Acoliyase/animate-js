import React, { useCallback } from 'react';
import { TextButton, Box } from '@wix/design-system';
import { Add as AddIcon } from '@wix/wix-ui-icons-common';
import { useWixPatternsContainer } from '@wix/bex-core/react';
import { TAG_MAX_LENGTH } from '@wix/bex-core';
export const CreateTagButton = ({ state, searchValue, }) => {
    const { translate: t } = useWixPatternsContainer();
    const createAndAssignTag = useCallback((tagName) => {
        state.createAndAssignTag(tagName);
    }, [state]);
    return (React.createElement(Box, { height: "18px", width: "100%" },
        React.createElement(TextButton, { dataHook: "bulk-assign-tags-create-tag-button", prefixIcon: React.createElement(AddIcon, null), onClick: () => createAndAssignTag(searchValue), size: "small", ellipsis: true }, t('cairo.tags.collectionPage.popover.create.CTA', {
            tagName: searchValue.slice(0, TAG_MAX_LENGTH),
        }))));
};
//# sourceMappingURL=CreateTagButton.js.map