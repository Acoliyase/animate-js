import { Box, Tooltip } from '@wix/design-system';
import React from 'react';
export function DisabledTooltip(props) {
    const { children, width, ...rest } = props;
    const { props: { disabled }, } = children;
    let child = children;
    if (disabled) {
        child = (React.createElement(Tooltip, { ...rest },
            React.createElement(Box, { direction: "horizontal", width: width }, child)));
    }
    return child;
}
//# sourceMappingURL=DisabledTooltip.js.map