import { baseUniDriverFactory } from '../../unidriver';
import { ButtonUniDriver, TooltipUniDriver, } from '@wix/design-system/dist/testkit/unidriver';
export function DisabledTooltipButtonUniDriver(base, body, { dataHook }) {
    const buttonBase = () => base.$(`[data-hook="${dataHook}"]`);
    return {
        ...baseUniDriverFactory(buttonBase()),
        ...ButtonUniDriver(buttonBase(), body),
        hover: () => buttonBase().hover(),
        getTooltipText: () => TooltipUniDriver(base.$(`[data-hook="${dataHook}-disabled-tooltip"]`), body).getTooltipText(),
    };
}
//# sourceMappingURL=DisabledTooltipButton.uni.driver.js.map