import { Children, isValidElement, } from 'react';
export function createChildSlot(name) {
    const Slot = () => null;
    Slot.displayName = name;
    Slot.isElementOf = (el) => el.type === Slot;
    Slot.findElement = (children) => children != null
        ? Children.toArray(children).find((child) => isValidElement(child) && Slot.isElementOf(child))
        : undefined;
    return Slot;
}
//# sourceMappingURL=createChildSlot.js.map