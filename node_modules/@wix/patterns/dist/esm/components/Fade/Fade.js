import { Transition } from 'react-transition-group';
import React, { useMemo } from 'react';
import { st, classes } from './Fade.st.css.js';
export function Fade({ children, timeout, ...rest }) {
    const { type: Component, props: childProps } = children;
    const style = useMemo(() => ({
        transitionDuration: `${timeout}ms`,
        ...childProps.style,
    }), [timeout, childProps.style]);
    return (React.createElement(Transition, { timeout: timeout, ...rest }, (status) => {
        const className = st(classes.root, {
            exit: status === 'exited' || status === 'exiting',
        }, childProps.className, `fade-${status}`);
        return (React.createElement(Component, { ...childProps, className: className, style: style }));
    }));
}
//# sourceMappingURL=Fade.js.map