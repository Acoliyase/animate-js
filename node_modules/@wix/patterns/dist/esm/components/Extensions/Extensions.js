import React from 'react';
import { observer } from 'mobx-react-lite';
import { Box } from '@wix/design-system';
function _Extensions(params) {
    const { state, dataHook, ...rest } = params;
    const { items } = state;
    return (React.createElement(Box, { direction: "vertical", gap: "SP4", dataHook: dataHook }, items.map(({ Extension: _Extension, componentId }) => {
        const Extension = _Extension;
        return React.createElement(Extension, { ...rest, key: componentId });
    })));
}
export const Extensions = observer(_Extensions);
//# sourceMappingURL=Extensions.js.map