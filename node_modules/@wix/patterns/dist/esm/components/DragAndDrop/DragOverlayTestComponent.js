import React from 'react';
import { observer } from 'mobx-react-lite';
function _DragOverlayTestComponent(props) {
    const { wrapperElement: Wrapper, style, children, className, zIndex, state, } = props;
    if (state.activeId == null) {
        return null;
    }
    let child = children;
    if (Wrapper) {
        child = React.createElement(Wrapper, null, children);
    }
    return (React.createElement("div", { className: className, style: {
            position: 'fixed',
            ...style,
            zIndex,
        } }, child));
}
export const DragOverlayTestComponent = observer(_DragOverlayTestComponent);
//# sourceMappingURL=DragOverlayTestComponent.js.map