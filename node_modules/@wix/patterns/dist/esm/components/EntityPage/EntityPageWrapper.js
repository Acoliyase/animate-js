import React from 'react';
import { observer } from 'mobx-react-lite';
import { EntityPagePlain } from './EntityPagePlain';
import { PageErrorState } from '../PageErrorState';
import { useWixPatternsContainer } from '@wix/bex-core/react';
function _EntityPageWrapper({ state, ...rest }) {
    const { translate: t } = useWixPatternsContainer();
    if (state.initTask.status.isError) {
        return (React.createElement(PageErrorState, { title: t('cairo.page.loadError.failedState.title'), subtitle: t('cairo.page.loadError.failedState.description'), retryText: t('cairo.page.loadError.failedState.CTA'), onRetry: () => state.retryFetch(), ...rest }));
    }
    return React.createElement(EntityPagePlain, { state: state, ...rest });
}
export const EntityPageWrapper = observer(_EntityPageWrapper);
//# sourceMappingURL=EntityPageWrapper.js.map