import { Page } from '@wix/design-system';
import React, { useState } from 'react';
import { observer } from 'mobx-react-lite';
import { useEntityPageChild } from '../../hooks';
import { useIsMobile } from '../../hooks/useIsMobile';
import { st, classes } from './EntityPagePlain.st.css.js';
function _EntityPagePlain({ state, children, ...rest }) {
    const [scrollableRef, setScrollableRef] = useState(null);
    const childrenArr = useEntityPageChild(children, scrollableRef);
    const isMobile = useIsMobile();
    return (React.createElement(Page, { className: st(classes.root, { mobile: isMobile }), minWidth: isMobile ? 0 : undefined, scrollableContentRef: setScrollableRef, ...rest }, childrenArr));
}
export const EntityPagePlain = observer(_EntityPagePlain);
//# sourceMappingURL=EntityPagePlain.js.map