import React from 'react';
import { useEntityPageContext } from '../../providers';
import { observer } from 'mobx-react-lite';
import { FormPageActionsBar } from '../FormPage';
export function _EntityPageActionsBar(props) {
    const state = useEntityPageContext();
    return (React.createElement(FormPageActionsBar, { onCancel: state.onCancel.bind(state), onSave: state.handleSubmit.bind(state), actionsBarConfig: state.actionsBarConfig, ...props }));
}
export const EntityPageActionsBar = observer(_EntityPageActionsBar);
//# sourceMappingURL=EntityPageActionsBar.js.map