import { createContext } from '../createContext';
import { useContext } from 'react';
const { Provider, Consumer, Context } = createContext('FilterContextProvider');
export const FilterContext = Context;
export const FilterContextProvider = Provider;
export const FilterContextConsumer = Consumer;
export const useFilterContext = () => useContext(FilterContext);
//# sourceMappingURL=FilterContext.js.map