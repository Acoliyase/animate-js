import React from 'react';
import { Box } from '@wix/design-system';
import { observer } from 'mobx-react-lite';
export function _CollectionPageHeader(props) {
    const { state, children, ...rest } = props;
    const { initTask, result: { total }, } = state;
    return (React.createElement(Box, { direction: "horizontal", ...rest },
        React.createElement(Box, { paddingRight: "9px" }, children),
        initTask.status.isSuccess && (React.createElement(Box, { dataHook: "page-header-total-count", color: "D40" }, total))));
}
export const CollectionPageHeader = observer(_CollectionPageHeader);
//# sourceMappingURL=CollectionPageHeader.js.map