import React from 'react';
import { Box, CounterBadge } from '@wix/design-system';
export const CounterBadgeButton = (props) => {
    const { children, count = 0, dataHook = 'counter-badge-count' } = props;
    return (React.createElement(Box, { position: "relative" },
        children,
        count > 0 && (React.createElement(Box, { position: "absolute", top: 3, right: 3, transform: "translate3d(50%, -50%, 0)", pointerEvents: "none" },
            React.createElement(CounterBadge, { dataHook: dataHook, skin: "neutralStandard" }, count)))));
};
//# sourceMappingURL=CounterBadgeButton.js.map