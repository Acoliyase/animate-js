import { baseUniDriverFactory } from '../../unidriver';
import ErrorCardUniDriver from '../ErrorCard/ErrorCard.uni.driver';
import SkeletonCardUniDriver from '../SkeletonCard/SkeletonCard.uni.driver';
export default (base, body) => {
    const getSkeletonCard = () => SkeletonCardUniDriver(base, body);
    return {
        ...baseUniDriverFactory(base),
        showsSkeletonHeader: () => getSkeletonCard().showsSkeletonHeader(),
        getChildByDataHook: (dataHook) => base.$(`[data-hook="${dataHook}"]`),
        getErrorState: () => ErrorCardUniDriver(base, body),
    };
};
//# sourceMappingURL=CardContainer.uni.driver.js.map