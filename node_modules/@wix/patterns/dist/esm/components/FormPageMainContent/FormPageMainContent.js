import React from 'react';
import { Box, Cell } from '@wix/design-system';
import { useIsMobile } from '../../hooks/useIsMobile';
export function FormPageMainContent(props) {
    const { children, ...rest } = props;
    const isMobile = useIsMobile();
    return (React.createElement(Cell, { span: isMobile ? 12 : undefined, ...rest },
        React.createElement(Box, { direction: "vertical", gap: isMobile ? 'SP2' : 'SP4' }, children)));
}
FormPageMainContent.displayName = 'FormPageMainContent';
//# sourceMappingURL=FormPageMainContent.js.map