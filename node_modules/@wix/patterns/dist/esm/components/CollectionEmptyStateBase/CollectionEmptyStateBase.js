import React from 'react';
import { EmptyState } from '@wix/design-system';
import { observer } from 'mobx-react-lite';
import { useEmptyStateTexts } from '../../hooks';
function _CollectionEmptyStateBase(props) {
    const { component: Component = EmptyState } = props;
    const { title, subtitle } = useEmptyStateTexts(props);
    return React.createElement(Component, { title: title, subtitle: subtitle, ...props });
}
export const CollectionEmptyStateBase = observer(_CollectionEmptyStateBase);
//# sourceMappingURL=CollectionEmptyStateBase.js.map