import { Page } from '@wix/design-system';
import React, { Children } from 'react';
import { FormPageProvider } from './FormPageProvider';
const FormPageContent = ({ children, ...rest }) => {
    // WSR <Page /> expects a flattened children array
    const childrenArr = Children.toArray(children);
    return React.createElement(Page, { ...rest }, childrenArr);
};
export function FormPage({ state, ...rest }) {
    return (React.createElement(FormPageProvider, { state: state },
        React.createElement(FormPageContent, { ...rest })));
}
//# sourceMappingURL=FormPage.js.map