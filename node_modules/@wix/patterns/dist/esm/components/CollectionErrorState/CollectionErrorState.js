import React from 'react';
import { Box, EmptyState, TextButton } from '@wix/design-system';
import { useWixPatternsContainer } from '@wix/bex-core/react';
const RequestId = React.lazy(() => import(/* webpackChunkName: "RequestId" */ '../RequestId').then((module) => ({
    default: module.RequestId,
})));
export const CollectionErrorState = ({ dataHook = 'collection-error-state', title, subtitle, action, requestId, }) => {
    const { environment } = useWixPatternsContainer();
    return (React.createElement(Box, { direction: "vertical", align: "center", width: "100%", padding: "SP7 0" },
        React.createElement(EmptyState, { dataHook: dataHook, theme: "page-no-border", title: title, subtitle: subtitle }, action && (React.createElement(TextButton, { dataHook: "collection-error-state-action-button", prefixIcon: action?.prefixIcon, onClick: action?.onClick }, action?.text))),
        requestId && (React.createElement(React.Suspense, { fallback: null },
            React.createElement(Box, { align: "center", padding: "SP2", position: "absolute", bottom: "0" },
                React.createElement(RequestId, { value: requestId, locale: environment.language }))))));
};
//# sourceMappingURL=CollectionErrorState.js.map