import { Box, Table } from '@wix/design-system';
import React from 'react';
import { ToolbarCollectionContentAutoSizer } from '../ToolbarCollectionContentAutoSizer';
import { classes, st } from '../common.st.css.js';
import { useTableRoundedCornersEnabled } from '../../hooks/useTableRoundedCornersEnabled';
export function CollectionTableContentCard(props) {
    const { children } = props;
    const roundedCornersEnabled = useTableRoundedCornersEnabled();
    return (
    // wrapping with Card is WSR way to make the sticky table header not to add extra border between the header and the content https://www.docs.wixdesignsystem.com/?path=/story/components-lists-table--table#Sticky_Toolbar_&_Sub-toolbar
    React.createElement(Box, { direction: "vertical", backgroundColor: "D80", borderRadius: "0px 0px 8px 8px", overflow: roundedCornersEnabled ? 'hidden' : undefined, className: st(classes.column, classes.fullHeight) },
        React.createElement(ToolbarCollectionContentAutoSizer, null,
            React.createElement(Table.Content, { titleBarVisible: false, dataHook: "table-content" }),
            children)));
}
//# sourceMappingURL=CollectionTableContentCard.js.map