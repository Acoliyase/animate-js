import { Delete } from '@wix/wix-ui-icons-common';
import React from 'react';
export function deleteSecondaryAction(props) {
    const { optimisticActions, actionCellAPI: { openConfirmDeleteModal, collection, keyedItem }, ...deleteManyParams } = props;
    const { translate: t } = collection;
    return {
        dataHook: 'collection-delete-action',
        text: t('cairo.delete.button'),
        icon: React.createElement(Delete, null),
        onClick: () => {
            openConfirmDeleteModal({
                primaryButtonOnClick: () => {
                    optimisticActions.deleteOne(keyedItem.item, deleteManyParams);
                },
            });
        },
    };
}
//# sourceMappingURL=deleteSecondaryAction.js.map