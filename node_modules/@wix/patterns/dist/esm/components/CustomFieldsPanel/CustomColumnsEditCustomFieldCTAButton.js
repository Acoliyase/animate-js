import React from 'react';
import { useToolbarCollectionContext } from '../ToolbarCollectionContext';
import { Box } from '@wix/design-system';
import { ManageFieldsPopoverMenu } from './ManageFieldsPopoverMenu';
export function CustomColumnsEditCustomFieldCTAButton({ field, }) {
    const toolbar = useToolbarCollectionContext();
    const state = toolbar.collectionDataExtension?.customFieldsPanel;
    if (!state || state.dataExtension.disableUserDefinedWriting) {
        return null;
    }
    return (React.createElement(Box, null,
        React.createElement(ManageFieldsPopoverMenu, { state: state, id: field.id, origin: "Custom columns panel", location: "Customize columns action cell" }),
        React.createElement(Box, { paddingInlineEnd: "12px" })));
}
//# sourceMappingURL=CustomColumnsEditCustomFieldCTAButton.js.map