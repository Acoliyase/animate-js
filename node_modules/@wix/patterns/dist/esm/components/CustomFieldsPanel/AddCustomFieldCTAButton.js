import { AddItem } from '@wix/design-system';
import React from 'react';
import { useWixPatternsContainer } from '@wix/bex-core/react';
import { CustomFieldsDisabledHint } from '../CustomFields';
import { observer } from 'mobx-react-lite';
function _AddCustomFieldCTAButton({ state, onClick, dataHook = 'data-extension-add-new', }) {
    const container = useWixPatternsContainer();
    const { translate: t } = container;
    if (state.dataExtension.disableUserDefinedWriting) {
        return null;
    }
    return (React.createElement(CustomFieldsDisabledHint, { disabled: state.dataExtension.disableAddCustomField, content: state.dataExtension.disableAddCustomFieldContent },
        React.createElement(AddItem, { dataHook: dataHook, onClick: onClick }, t('cairo.customFields.addCustomField.CTA'))));
}
export const AddCustomFieldCTAButton = observer(_AddCustomFieldCTAButton);
//# sourceMappingURL=AddCustomFieldCTAButton.js.map