import { observer } from 'mobx-react-lite';
import React, { useCallback } from 'react';
import { CustomFieldItem } from './CustomFieldItem';
import { Box } from '@wix/design-system';
function _CustomFieldsList(props) {
    const { state } = props;
    const renderItem = useCallback((field, index, array) => {
        return (React.createElement(CustomFieldItem, { state: state, key: field.id, field: field, showDivider: index < array.length - 1 }));
    }, []);
    return (React.createElement(Box, { direction: "vertical", dataHook: "custom-fields-list", backgroundColor: "D60" }, state.dataExtension.availableUserCustomFields.map(renderItem)));
}
export const CustomFieldsList = observer(_CustomFieldsList);
//# sourceMappingURL=CustomFieldsList.js.map