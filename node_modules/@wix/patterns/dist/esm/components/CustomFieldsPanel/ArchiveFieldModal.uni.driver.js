import { MessageModalLayoutUniDriver } from '@wix/design-system/dist/testkit/unidriver';
export function ArchiveFieldModalUniDriver(_base, body) {
    const getArchiveFieldModal = () => MessageModalLayoutUniDriver(body.$('[data-hook="archive-custom-field-modal"]'), body);
    const exists = () => getArchiveFieldModal().exists();
    const clickConfirm = () => getArchiveFieldModal()
        .getPrimaryButtonDriver()
        .then((driver) => driver.click());
    const clickClose = () => getArchiveFieldModal().clickCloseButton();
    return {
        exists,
        clickConfirm,
        clickClose,
    };
}
//# sourceMappingURL=ArchiveFieldModal.uni.driver.js.map