export function assertComponentType(element, displayName, propName) {
    if (element === true ||
        (element &&
            element.type.displayName !== displayName)) {
        throw new Error(`Prop "${propName}" must be a <${displayName} /> element`);
    }
}
//# sourceMappingURL=assertComponentType.js.map