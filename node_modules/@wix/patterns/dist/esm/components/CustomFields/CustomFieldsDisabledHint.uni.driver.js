import { baseUniDriverFactory } from '../../unidriver';
import { AddItemUniDriver, TextUniDriver, } from '@wix/design-system/dist/testkit/unidriver';
export function CustomFieldsDisabledHintUniDriver(base, body, { dataHook }) {
    const buttonBase = () => base.$(`[data-hook="${dataHook}"]`);
    return {
        ...baseUniDriverFactory(base),
        ...AddItemUniDriver(buttonBase(), body),
        isDisabled: async () => {
            return ((await buttonBase().$('button').exists()) &&
                (await buttonBase().$('button')._prop('disabled')) === true);
        },
        getHintText: () => TextUniDriver(base.$(`[data-hook="${dataHook}-disabled-hint"]`), body).getText(),
    };
}
//# sourceMappingURL=CustomFieldsDisabledHint.uni.driver.js.map