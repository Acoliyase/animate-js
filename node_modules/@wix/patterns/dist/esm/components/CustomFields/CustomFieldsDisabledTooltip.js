import { DisabledTooltip, } from '../DisabledTooltip';
import React from 'react';
import { observer } from 'mobx-react-lite';
function _CustomFieldsDisabledTooltip(props) {
    const { children, disabled, content, ...rest } = props;
    const { key, props: componentProps, type: Component } = children;
    const { dataHook } = componentProps;
    return (React.createElement(DisabledTooltip, { dataHook: dataHook ? `${dataHook}-disabled-tooltip` : undefined, content: content, appendTo: "window", maxWidth: "226px", ...rest },
        React.createElement(Component, { key: key, ...componentProps, disabled: disabled })));
}
export const CustomFieldsDisabledTooltip = observer(_CustomFieldsDisabledTooltip);
//# sourceMappingURL=CustomFieldsDisabledTooltip.js.map