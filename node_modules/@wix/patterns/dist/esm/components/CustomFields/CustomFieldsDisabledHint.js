import { Text, Box } from '@wix/design-system';
import React from 'react';
import { observer } from 'mobx-react-lite';
function _CustomFieldsDisabledHint(props) {
    const { children, disabled, content } = props;
    const { key, props: componentProps, type: Component } = children;
    const { dataHook } = componentProps;
    return (React.createElement(Box, { direction: "vertical", align: "center", gap: "SP1" },
        React.createElement(Component, { key: key, ...componentProps, disabled: disabled }),
        disabled && (React.createElement(Text, { dataHook: dataHook ? `${dataHook}-disabled-hint` : undefined, size: "small", skin: "disabled" }, content))));
}
export const CustomFieldsDisabledHint = observer(_CustomFieldsDisabledHint);
//# sourceMappingURL=CustomFieldsDisabledHint.js.map