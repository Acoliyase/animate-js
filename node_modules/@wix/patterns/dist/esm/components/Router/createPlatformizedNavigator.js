import { createPath } from 'react-router-dom';
export const createPlatformizedNavigator = ({ pageLocation, container, }) => {
    return {
        createHref: createPath,
        push: (to) => {
            const relativeUrl = typeof to === 'string' ? to : createPath(to);
            container.navigateTo({
                pageId: pageLocation.pageId,
                relativeUrl,
            });
        },
        replace: () => {
            throw new Error('not yet supported');
        },
        go: () => {
            throw new Error('not yet supported');
        },
    };
};
//# sourceMappingURL=createPlatformizedNavigator.js.map