import React, { useState, useEffect, useRef, } from 'react';
import { Box, Loader } from '@wix/design-system';
import { observer } from 'mobx-react-lite';
const useElementWidth = (initialWidth = 0) => {
    const [width, setWidth] = useState(initialWidth);
    const ref = useRef(null);
    useEffect(() => {
        if (ref.current) {
            setWidth(ref.current.clientWidth);
        }
    }, [ref.current]);
    return [width, ref];
};
export const CustomFieldModalPrimaryButton = observer((props) => {
    const { state: { isSubmitting }, content, } = props;
    const [width, ref] = useElementWidth();
    return (React.createElement("div", { ref: ref }, isSubmitting ? (React.createElement(Box, { verticalAlign: "middle", align: "center", width: width },
        React.createElement(Loader, { dataHook: "custom-field-modal-status-submitting", size: "tiny" }))) : (content)));
});
//# sourceMappingURL=CustomFieldModalPrimaryButton.js.map