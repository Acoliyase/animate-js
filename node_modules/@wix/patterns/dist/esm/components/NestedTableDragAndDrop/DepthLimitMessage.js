import React from 'react';
import { observer } from 'mobx-react-lite';
import { Tag } from '@wix/design-system';
import { BlockSmall } from '@wix/wix-ui-icons-common';
function _DepthLimitMessage(props) {
    const { state } = props;
    const { nestedState } = state;
    const { levels, container: { translate: t }, } = nestedState;
    return (React.createElement(Tag, { id: "nested-table-dnd-depth-limit-message", dataHook: "nested-table-dnd-depth-limit-message", theme: "light", thumb: React.createElement(BlockSmall, null), removable: false }, t('cairo.collectionPage.nestedLimit.tooltip', {
        subCatsAmount: levels.length,
    })));
}
export const DepthLimitMessage = observer(_DepthLimitMessage);
//# sourceMappingURL=DepthLimitMessage.js.map