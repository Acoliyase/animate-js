import { Box } from '@wix/design-system';
import React from 'react';
import { RowActionsColumnSlot, } from '../CollectionListItem';
export function CollectionListHeader({ element }) {
    const { type: CollectionListItemComponent, props: collectionListItemProps } = element;
    const transformColumn = (columnElement) => {
        const { props } = columnElement;
        const overrides = RowActionsColumnSlot.isElementOf(columnElement)
            ? {
                width: 100,
                align: 'right',
            }
            : {};
        const { title, width, align } = {
            ...props,
            ...overrides,
        };
        return {
            title,
            width,
            align,
            value: React.createElement(Box, null, props.title),
        };
    };
    return (React.createElement(CollectionListItemComponent, { ...collectionListItemProps, transformColumn: transformColumn }));
}
//# sourceMappingURL=CollectionListHeader.js.map