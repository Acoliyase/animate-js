import React, { useMemo } from 'react';
import { Box, Text } from '@wix/design-system';
import { formatBytes } from './formatBytes';
import { THUMB_WIDTH, SMALL_THUMB_WIDTH } from './resolveFileByMediaType';
export function FileDetails({ file, showDetails }) {
    const fileSize = useMemo(() => formatBytes(file.sizeInBytes), [file.sizeInBytes]);
    return (React.createElement(Box, { gap: "4px", direction: "vertical", dataHook: `file-details-${file.mediaId}`, maxWidth: `calc(100% - ${showDetails ? THUMB_WIDTH + 60 : SMALL_THUMB_WIDTH + 10}px)`, paddingRight: !showDetails ? 'SP2' : '0' },
        React.createElement(Text, { ellipsis: true, size: "small", dataHook: `file-details-name-${file.mediaId}`, paddingRight: "SP2" }, file.displayName),
        showDetails && (React.createElement(Box, { verticalAlign: "middle" },
            React.createElement(Text, { paddingRight: "SP2", size: "tiny", secondary: true, ellipsis: true, dataHook: `file-details-size-${file.mediaId}` },
                fileSize,
                " ",
                file.mediaType)))));
}
//# sourceMappingURL=FileDetails.js.map