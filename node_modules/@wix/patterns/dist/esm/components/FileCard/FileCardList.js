import React from 'react';
import { FilePreview } from './FilePreview';
import { Box } from '@wix/design-system';
export const FileCardList = ({ files, showDownloadCTA, showDetails, }) => {
    if (!files) {
        return null;
    }
    return (React.createElement(Box, { direction: "vertical" }, files.map((file) => (React.createElement(FilePreview, { file: file, key: file.mediaId, showDownloadCTA: showDownloadCTA, showDetails: showDetails })))));
};
//# sourceMappingURL=FileCardList.js.map