import React from 'react';
import { Box } from '@wix/design-system';
import { Thumbnail } from './Thumbnail';
import { FileDetails } from './FileDetails';
import { DownloadButton } from './DownloadButton';
export const FileCard = ({ file, showDownloadCTA = false, showDetails = false, }) => {
    // const downloadIconWidth = showDownloadCTA ? 36 : 0;
    return (React.createElement(Box, { verticalAlign: "middle", align: "left", gap: showDetails ? 'SP2' : 'SP1', paddingBlockEnd: 1, maxWidth: "100%", width: "100%", height: "100%", dataHook: `file-card-${file.mediaId}` },
        React.createElement(Thumbnail, { file: file, showDetails: showDetails }),
        React.createElement(FileDetails, { file: file, showDetails: showDetails }),
        showDownloadCTA && !file.private ? React.createElement(DownloadButton, { file: file }) : null));
};
//# sourceMappingURL=FileCard.js.map