import React from 'react';
import { Box, IconButton, Loader, Tooltip } from '@wix/design-system';
import { DownloadImportSmall } from '@wix/wix-ui-icons-common';
import { useDownloadFile } from './useDownloadFile';
import { useWixPatternsContainer } from '@wix/bex-core/react';
export function DownloadButton({ file }) {
    const { downloadFile, isDownloading } = useDownloadFile({ file });
    const { translate: t } = useWixPatternsContainer();
    return (React.createElement(Box, { paddingInlineEnd: 4, marginLeft: "auto", dataHook: `download-button-${file.mediaId}` }, isDownloading ? (React.createElement(Loader, { size: "tiny" })) : (React.createElement(Tooltip, { content: t('cairo.customFields.fileUpload.download.cta') },
        React.createElement(IconButton, { priority: "secondary", size: "small", onClick: (e) => {
                e.stopPropagation();
                downloadFile();
            } },
            React.createElement(DownloadImportSmall, null))))));
}
//# sourceMappingURL=DownloadButton.js.map