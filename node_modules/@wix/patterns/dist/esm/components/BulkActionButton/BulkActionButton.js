import { Button } from '@wix/design-system';
import React from 'react';
import { DisabledTooltip } from '../DisabledTooltip';
export function BulkActionButton(props) {
    const { tooltipText, tooltipProps, ...rest } = props;
    return (React.createElement(DisabledTooltip, { content: tooltipText, dataHook: "bulk-action-button-tooltip", ...tooltipProps },
        React.createElement(Button, { dataHook: "bulk-action-button", skin: "standard", priority: "secondary", ...rest })));
}
//# sourceMappingURL=BulkActionButton.js.map