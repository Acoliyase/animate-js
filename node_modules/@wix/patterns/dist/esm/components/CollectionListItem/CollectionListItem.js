import { TableListItem, } from '@wix/design-system';
import React, { Children } from 'react';
import { RowActionsColumnSlot, RowColumnSlot, } from './CollectionListChildSlot';
import { assertProp } from '../../utils/assertProp';
export function CollectionListItem(props) {
    assertProp(props.transformColumn, 'transformColumn', 'CollectionListItem');
    // assertProp(props.className, 'className', 'CollectionListItem');
    const { children = [], transformColumn, ...rest } = props;
    const options = Children.map(children, (element) => {
        const { width, align, value } = transformColumn?.(element) ?? {
            ...element.props,
            value: element.props.children,
        };
        return {
            width,
            align,
            value,
        };
    });
    return React.createElement(TableListItem, { ...rest, options: options });
}
CollectionListItem.Column = RowColumnSlot;
CollectionListItem.ActionsColumn = RowActionsColumnSlot;
//# sourceMappingURL=CollectionListItem.js.map