import React, { Children, isValidElement } from 'react';
import { Card, EmptyState as WSREmptyState, TextButton, } from '@wix/design-system';
export function ErrorCard({ children, title, subtitle, action, ...rest }) {
    const childrenArray = Children.toArray(children);
    return (React.createElement(Card, { ...rest },
        childrenArray.filter((el) => isValidElement(el) &&
            el.type?.displayName !== 'Card.Content'),
        React.createElement(Card.Content, null,
            React.createElement(WSREmptyState, { dataHook: "error-card-state", title: title, subtitle: subtitle },
                React.createElement(TextButton, { dataHook: "error-card-state-action-button", prefixIcon: action?.prefixIcon, onClick: action?.onClick }, action?.text)))));
}
//# sourceMappingURL=ErrorCard.js.map