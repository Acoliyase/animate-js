import React, { forwardRef } from 'react';
import { DragOverlay, } from '@wix/wix-style-react-incubator/dnd-kit/core';
import { createPortal } from 'react-dom';
import { restrictToWindowEdges } from '@wix/wix-style-react-incubator/dnd-kit/modifiers';
import { GridDragOverlayCardItem } from './GridDragOverlayCardItem';
import { observer } from 'mobx-react-lite';
import { usePageContext } from '@wix/bex-core/react';
const DraggableCardOverlayWrapper = forwardRef((props, ref) => (React.createElement("div", { "data-hook": "draggable-card-overlay", ref: ref, ...props })));
function _DraggableCardOverlay(props) {
    const { state, renderItem } = props;
    const { _dnd } = state;
    const { modalsContainerRef } = usePageContext();
    return createPortal(React.createElement(DragOverlay, { dropAnimation: _dnd?.dropAnimation, modifiers: [restrictToWindowEdges], wrapperElement: DraggableCardOverlayWrapper },
        React.createElement(GridDragOverlayCardItem, { renderItem: renderItem, state: state })), modalsContainerRef?.current ?? document.body);
}
export const DraggableCardOverlay = observer(_DraggableCardOverlay);
//# sourceMappingURL=DraggableCardOverlay.js.map