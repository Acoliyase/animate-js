import { Text } from '@wix/design-system';
import React from 'react';
import { observer } from 'mobx-react-lite';
function _FilterAccordionTitle(props) {
    const { state, filterProps } = props;
    const { accordionItemProps } = filterProps;
    const filter = state.getPendingFilter(props.filter);
    return (React.createElement(Text, { dataHook: `accordion-item-title-${filter.name}`, ellipsis: true, weight: "normal" }, `${accordionItemProps?.label ?? ''}${filter.size > 0 ? ` (${filter.size})` : ''}`));
}
export const FilterAccordionTitle = observer(_FilterAccordionTitle);
//# sourceMappingURL=FilterAccordionTitle.js.map