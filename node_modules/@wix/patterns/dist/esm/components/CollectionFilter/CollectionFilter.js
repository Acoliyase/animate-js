import { useEffect, useState } from 'react';
import { useFilterRenderingContext } from '../FilterRenderingContext';
import { CollectionFilterState } from './CollectionFilterState';
export function CollectionFilter(props) {
    const { filter, children, state: toolbarFiltersState } = props;
    const { location } = useFilterRenderingContext();
    const [state] = useState(() => new CollectionFilterState({
        filter,
        toolbarFiltersState,
        location,
    }));
    useEffect(() => state.init(), []);
    return children;
}
//# sourceMappingURL=CollectionFilter.js.map