import React from 'react';
import { observer } from 'mobx-react-lite';
import { InlineSkeletonContent } from './Skeletons';
import { CustomFieldsViewWidgetContent } from './CustomFieldsViewWidgetContent';
import { EmptyStateViewContent } from './EmptyStateViewContent';
import { ErrorStateContent } from './ErrorStateContent';
const _CustomFieldsViewWidgetInline = ({ dataHook, state, container, areAllCustomFieldsEmpty, extendedFields, userFieldsTitle, }) => {
    const { translate: t } = container;
    const getContentElement = () => {
        switch (true) {
            case state.isLoading: {
                return React.createElement(InlineSkeletonContent, null);
            }
            case state.isError:
                return React.createElement(ErrorStateContent, { state: state });
            case areAllCustomFieldsEmpty:
                return (React.createElement(EmptyStateViewContent, { title: t('cairo.customFields.entityPage.emptyState.title'), subtitle: t('cairo.customFields.entityPage.emptyState.subtitle') }));
            default:
                return (React.createElement(CustomFieldsViewWidgetContent, { state: state, extendedFields: extendedFields, userFieldsTitle: userFieldsTitle }));
        }
    };
    return React.createElement("div", { "data-hook": dataHook }, getContentElement());
};
export const CustomFieldsViewWidgetInline = observer(_CustomFieldsViewWidgetInline);
//# sourceMappingURL=CustomFieldsViewWidgetInline.js.map