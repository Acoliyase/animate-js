import React from 'react';
import { EmptyState, TextButton } from '@wix/design-system';
import { observer } from 'mobx-react-lite';
import { Refresh } from '@wix/wix-ui-icons-common';
const _ErrorStateContent = ({ state }) => {
    const { container: { translate: t }, } = state;
    return (React.createElement(EmptyState, { dataHook: "error-state", theme: "page-no-border", title: t('cairo.customFields.technical.error.title'), subtitle: t('cairo.technical.error.text') },
        React.createElement(TextButton, { dataHook: "retry-button", prefixIcon: React.createElement(Refresh, null), onClick: () => state.retry() }, t('cairo.error.tryAgain.cta'))));
};
export const ErrorStateContent = observer(_ErrorStateContent);
//# sourceMappingURL=ErrorStateContent.js.map