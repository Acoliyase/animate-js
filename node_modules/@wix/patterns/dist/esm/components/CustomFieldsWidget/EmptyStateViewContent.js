import React from 'react';
import { EmptyState, Text } from '@wix/design-system';
import { observer } from 'mobx-react-lite';
function _EmptyStateViewContent({ title, subtitle, }) {
    return (React.createElement(EmptyState, { dataHook: "empty-state", title: title, subtitle: React.createElement(Text, { dataHook: "empty-state-subtitle", size: "small", secondary: true, widows: true }, subtitle) }));
}
export const EmptyStateViewContent = observer(_EmptyStateViewContent);
//# sourceMappingURL=EmptyStateViewContent.js.map