import React from 'react';
import { observer } from 'mobx-react-lite';
import { Refresh as RefreshIcon } from '@wix/wix-ui-icons-common';
import { LoadingErrorState } from '../ErrorState';
import { CustomFieldsWidgetContentLazy } from './CustomFieldsWidgetContentLazy';
import { InlineSkeletonContent } from './Skeletons';
const _CustomFieldsWidgetInline = (props) => {
    const { state, emptyStateOverrides, onChange, dataHook } = props;
    const { translate: t } = state.dataExtension.container;
    const renderContent = () => {
        if (state.isLoading) {
            return React.createElement(InlineSkeletonContent, null);
        }
        if (state.isError) {
            return (React.createElement(LoadingErrorState, { dataHook: "custom-fields-widget-inline-error", title: t('cairo.customFields.technical.error.title'), action: {
                    prefixIcon: React.createElement(RefreshIcon, null),
                    text: t('cairo.toast.retry'),
                    onClick: state.retry,
                } }));
        }
        return (React.createElement(CustomFieldsWidgetContentLazy, { emptyStateOverrides: emptyStateOverrides, onChange: onChange }));
    };
    return React.createElement("div", { "data-hook": dataHook }, renderContent());
};
export const CustomFieldsWidgetInline = observer(_CustomFieldsWidgetInline);
//# sourceMappingURL=CustomFieldsWidgetInline.js.map