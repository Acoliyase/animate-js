import React from 'react';
import { Box, Button } from '@wix/design-system';
import { observer } from 'mobx-react-lite';
import { Add } from '@wix/wix-ui-icons-common';
import { ManageFieldsCTAButton } from '../CustomFieldsPanel/ManageFieldsCTAButton';
import { CustomFieldsDisabledTooltip } from '../CustomFields';
const _ActionButtons = ({ state }) => {
    const { container: { translate: t }, } = state;
    return (React.createElement(Box, { gap: "SP2" },
        React.createElement(CustomFieldsDisabledTooltip, { disabled: state.dataExtension.disableAddCustomField, content: state.dataExtension.disableAddCustomFieldContent },
            React.createElement(Button, { dataHook: "add-custom-field-btn", prefixIcon: React.createElement(Add, null), size: "small", priority: "secondary", onClick: () => {
                    state.customFieldsPanel.openCustomFieldModal({
                        origin: 'Custom fields widget',
                    });
                } }, t('cairo.customFields.addCustomField.CTA'))),
        React.createElement(ManageFieldsCTAButton, { state: state, origin: "Custom fields widget" })));
};
export const ActionButtons = observer(_ActionButtons);
//# sourceMappingURL=ActionButtons.js.map