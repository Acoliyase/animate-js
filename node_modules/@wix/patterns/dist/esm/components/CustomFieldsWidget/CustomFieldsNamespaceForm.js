import React, { useEffect } from 'react';
import { observer } from 'mobx-react-lite';
import { CustomFieldsForm } from '../CustomFieldsForm';
import { useCustomFieldsFormState } from './useCustomFieldsFormState';
function _CustomFieldsNamespaceForm(props) {
    const { state: allNamespacesFormState, onChange, namespace } = props;
    const state = useCustomFieldsFormState({
        state: allNamespacesFormState,
        namespace,
    });
    useEffect(() => state.init(), []);
    if (!state.writeableFields.length) {
        return null;
    }
    return React.createElement(CustomFieldsForm, { state: state, onChange: onChange });
}
export const CustomFieldsNamespaceForm = observer(_CustomFieldsNamespaceForm);
//# sourceMappingURL=CustomFieldsNamespaceForm.js.map