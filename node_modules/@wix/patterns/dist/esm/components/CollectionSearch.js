import { observer } from 'mobx-react-lite';
import { Search } from '@wix/design-system';
import React from 'react';
import { useCollectionContext } from '@wix/bex-core/react';
export const CollectionSearch = observer(function CollectionSearch({ onSearchChange, onSearchClick, onSearchBlur, visibility, ...rest }) {
    const collection = useCollectionContext();
    const { query: { search }, translate: t, } = collection;
    return (React.createElement(Search, { dataHook: "collection-search", onChange: (e) => {
            const value = e.target.value;
            collection.search(e.target.value, { clearResult: true });
            onSearchChange?.(value);
        }, value: search.value, onClear: () => {
            const value = '';
            collection.clearSearch();
            onSearchChange?.(value);
        }, placeholder: t('cairo.searchPlaceholder', { defaultValue: '' }), onInputClicked: () => onSearchClick?.(), textOverflow: "ellipsis", onBlur: (e) => {
            const value = e.target.value;
            onSearchBlur?.(value);
        }, ...rest }));
});
//# sourceMappingURL=CollectionSearch.js.map