import React from 'react';
import { observer } from 'mobx-react-lite';
import { AddItemEmptyState } from '../Illustration/AddItemEmptyState';
import { useWixPatternsContainer } from '@wix/bex-core/react';
import { EmptyStateBase } from './EmptyStateBase';
import { NewItemButton } from './NewItemButton';
function _CollectionEmptyState(props) {
    const { translate: t } = useWixPatternsContainer();
    const { addNewCta } = props;
    return (React.createElement(EmptyStateBase, { dataHook: "collection-empty-state", title: t('cairo.emptyState.additem.title'), subtitle: t('cairo.emptyState.additem.text'), image: React.createElement(AddItemEmptyState, { "data-hook": "empty-state-new-item-illustration" }), ...props, cta: addNewCta && React.createElement(NewItemButton, { ...addNewCta }) }));
}
export const CollectionEmptyState = observer(_CollectionEmptyState);
//# sourceMappingURL=CollectionEmptyState.js.map