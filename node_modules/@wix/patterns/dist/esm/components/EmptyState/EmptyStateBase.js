import React from 'react';
import { Box, EmptyState } from '@wix/design-system';
import { observer } from 'mobx-react-lite';
function _EmptyStateBase(props) {
    const { children, customCta, title, subtitle, cta, image, ...rest } = props;
    let child;
    if (cta || customCta) {
        child = (React.createElement(Box, { gap: "SP5", verticalAlign: "middle" },
            customCta,
            cta));
    }
    if (children) {
        child = (React.createElement(React.Fragment, null,
            child,
            children));
    }
    return (React.createElement(EmptyState, { title: title, subtitle: subtitle, theme: "page-no-border", image: image ? React.createElement("div", { style: { display: 'flex' } }, image) : undefined, ...rest }, child));
}
export const EmptyStateBase = observer(_EmptyStateBase);
//# sourceMappingURL=EmptyStateBase.js.map