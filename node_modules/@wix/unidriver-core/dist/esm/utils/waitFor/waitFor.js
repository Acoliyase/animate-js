import { awaiter } from '../awaiter/awaiter';
export const waitFor = async (fn, paramsOrTimeout) => {
    return awaiter(fn, {
        ...(typeof paramsOrTimeout === 'number' ? { timeout: paramsOrTimeout } : paramsOrTimeout),
        awaiterName: 'waitFor',
        expect: true,
    });
};
//# sourceMappingURL=waitFor.js.map