import { Locator, Page } from 'playwright';
import { UniDriver } from '@wix/unidriver-core';
import { BaseUniDriver } from '../base-driver';
interface DriverFactoryOptions {
    dataHook: string;
}
export declare function playwrightUniTestkitFactoryCreator<T extends BaseUniDriver>(driverFactory: (base: UniDriver, body: UniDriver, options: DriverFactoryOptions) => T): (arg: {
    dataHook: string;
    page: Page;
    wrapper?: Locator;
}) => Promise<T>;
export {};
//# sourceMappingURL=playwright.d.ts.map