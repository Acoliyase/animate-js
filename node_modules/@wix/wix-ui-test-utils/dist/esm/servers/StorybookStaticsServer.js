import express from 'express';
const storybookPath = './storybook-static';
export class StorybookStaticsServer {
    constructor() {
        this.server = null;
    }
    start({ port }) {
        return new Promise((resolve) => {
            this.server = express()
                .use('/', express.static(storybookPath))
                .listen(port, resolve);
        });
    }
    stop() {
        return new Promise((resolve) => {
            this.server && this.server.close(resolve);
        });
    }
}
//# sourceMappingURL=StorybookStaticsServer.js.map