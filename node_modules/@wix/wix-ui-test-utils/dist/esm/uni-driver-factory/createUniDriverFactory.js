import { createRoot } from 'react-dom/client';
import { jsdomReactUniDriver } from '@wix/unidriver-jsdom-react-legacy';
function componentFactory(Component) {
    const wrapperDiv = document.createElement('div');
    const root = createRoot(wrapperDiv);
    root.render(Component);
    const base = jsdomReactUniDriver(wrapperDiv.childNodes[0]);
    return base;
}
export function createUniDriverFactory(driverFactory) {
    const body = jsdomReactUniDriver(document.body);
    return (Component) => driverFactory(componentFactory(Component), body);
}
//# sourceMappingURL=createUniDriverFactory.js.map