import { CreateTagRequest as CreateTagRequest$1, CreateTagResponse as CreateTagResponse$1, GetTagByLabelRequest as GetTagByLabelRequest$1, GetTagByLabelResponse as GetTagByLabelResponse$1, GetTagRequest as GetTagRequest$1, GetTagResponse as GetTagResponse$1, GetTagBySlugRequest as GetTagBySlugRequest$1, GetTagBySlugResponse as GetTagBySlugResponse$1, QueryTagsRequest as QueryTagsRequest$1, QueryTagsResponse as QueryTagsResponse$1, DeleteTagRequest as DeleteTagRequest$1, DeleteTagResponse as DeleteTagResponse$1 } from './index.typings.mjs';
import '@wix/sdk-types';

interface BlogTag {
    /**
     * Tag ID.
     * @readonly
     * @format GUID
     */
    id?: string;
    /**
     * Tag label.
     *
     * A blog can't have two tags with the same label.
     * @maxLength 100
     */
    label?: string;
    /**
     * Part of a tag's URL that refers to a specific tag.
     *
     * For example, `'https:/example.com/tags/{my-tag-slug}'`
     * @maxLength 100
     */
    slug?: string;
    /**
     * Date the tag was created.
     * @readonly
     */
    createdDate?: Date | null;
    /**
     * Date the tag was last updated.
     * @readonly
     */
    updatedDate?: Date | null;
    /**
     * Number of posts with this tag. Includes unpublished draft posts.
     * @readonly
     */
    postCount?: number;
    /**
     * Tag URL.
     *
     * The `url` directs you to a page that lists every post with the specified tag.
     * @readonly
     */
    url?: PageUrl;
    /**
     * Number of published posts with this tag.
     * @readonly
     */
    publishedPostCount?: number;
    /**
     * ID of the tag's translations when [Wix Multilingual](https://support.wix.com/en/article/wix-multilingual-translating-your-blog) is installed on a site.
     *
     * All translations of a single tag share the same `translationId`.
     * @readonly
     * @format GUID
     */
    translationId?: string | null;
    /**
     * Tag language.
     *
     * 2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
     * @immutable
     * @format LANGUAGE_TAG
     */
    language?: string | null;
}
interface PageUrl {
    /** The base URL. For premium sites, this is the domain. For free sites, this is the site URL. For example, `mysite.wixsite.com/mysite`. */
    base?: string;
    /** The relative path for the page within the site. For example, `/product-page/a-product`. */
    path?: string;
}
/**
 * The SEO schema object contains data about different types of meta tags. It makes sure that the information about your page is presented properly to search engines.
 * The search engines use this information for ranking purposes, or to display snippets in the search results.
 * This data will override other sources of tags (for example patterns) and will be included in the <head> section of the HTML document, while not being displayed on the page itself.
 */
interface SeoSchema {
    /** SEO tag information. */
    tags?: Tag[];
    /** SEO general settings. */
    settings?: Settings;
}
interface Keyword {
    /** Keyword value. */
    term?: string;
    /** Whether the keyword is the main focus keyword. */
    isMain?: boolean;
    /**
     * The source that added the keyword terms to the SEO settings.
     * @maxLength 1000
     */
    origin?: string | null;
}
interface Tag {
    /**
     * SEO tag type.
     *
     *
     * Supported values: `title`, `meta`, `script`, `link`.
     */
    type?: string;
    /**
     * A `{"key": "value"}` pair object where each SEO tag property (`"name"`, `"content"`, `"rel"`, `"href"`) contains a value.
     * For example: `{"name": "description", "content": "the description itself"}`.
     */
    props?: Record<string, any> | null;
    /** SEO tag metadata. For example, `{"height": 300, "width": 240}`. */
    meta?: Record<string, any> | null;
    /** SEO tag inner content. For example, `<title> inner content </title>`. */
    children?: string;
    /** Whether the tag is a [custom tag](https://support.wix.com/en/article/adding-additional-meta-tags-to-your-sites-pages). */
    custom?: boolean;
    /** Whether the tag is disabled. If the tag is disabled, people can't find your page when searching for this phrase in search engines. */
    disabled?: boolean;
}
interface Settings {
    /**
     * Whether the [automatical redirect visits](https://support.wix.com/en/article/customizing-your-pages-seo-settings-in-the-seo-panel) from the old URL to the new one is enabled.
     *
     *
     * Default: `false` (automatical redirect is enabled).
     */
    preventAutoRedirect?: boolean;
    /**
     * User-selected keyword terms for a specific page.
     * @maxSize 5
     */
    keywords?: Keyword[];
}
interface InitialTagsCopied {
    /** Number of tags copied. */
    count?: number;
}
interface GetOrCreateTagRequest {
    /**
     * Tag name. Unique per blog.
     * @minLength 1
     * @maxLength 100
     */
    label?: string;
    /**
     * Tag language.
     * @format LANGUAGE_TAG
     */
    language?: string | null;
    /**
     * List of tag fields to be included if entities are present in the response.
     * Base fieldset, which is default, will return all core tag properties.
     * Example: When URL fieldset is selected, returned tag will have a set of base properties and tag url.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
}
declare enum Field {
    UNKNOWN = "UNKNOWN",
    /** Includes Tag URL when present. */
    URL = "URL"
}
/** @enumType */
type FieldWithLiterals = Field | 'UNKNOWN' | 'URL';
interface GetOrCreateTagResponse {
    /** Tag info. */
    tag?: BlogTag;
}
interface CreateTagRequest {
    /**
     * Tag label. The label for each tag in a blog must be unique.
     * @minLength 1
     * @maxLength 200
     */
    label: string;
    /**
     * Tag language.
     *
     * 2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
     * @format LANGUAGE_TAG
     */
    language?: string | null;
    /**
     * Preferred tag slug. For example, `'tag-slug'`.
     * @maxLength 100
     */
    slug?: string | null;
    /**
     * List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
     * the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
     * only the tag's base fields are returned.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
}
interface CreateTagResponse {
    /** Created tag. */
    tag?: BlogTag;
}
interface UpdateTagRequest {
    /** Tag info to update. */
    tag?: BlogTag;
    /**
     * List of tag fields to be included if entities are present in the response.
     * Base fieldset, which is default, will return all core tag properties.
     * Example: When URL fieldset is selected, returned tag will have a set of base properties and tag url.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
}
interface UpdateTagResponse {
    /** Updated tag. */
    tag?: BlogTag;
}
interface BulkCreateTagsRequest {
    /**
     * Tags to create.
     * @minSize 1
     * @maxSize 50
     */
    tags?: BlogTag[];
    /** Whether to return the full created tag entities in the response. */
    returnFullEntity?: boolean;
    /**
     * List of tag fields to be included if entities are present in the response.
     * Base fieldset, which is default, will return all core tag properties.
     * Example: When URL fieldset is selected, returned tag will have a set of base properties and tag url.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
}
interface BulkCreateTagsResponse {
    /** Tags created by bulk action. */
    results?: BulkTagResult[];
    /** Bulk action metadata. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkTagResult {
    /** Bulk actions metadata for tag. */
    itemMetadata?: ItemMetadata;
    /** Optional created tag. */
    item?: BlogTag;
}
interface ItemMetadata {
    /** Item ID. Should always be available, unless it's impossible (for example, when failing to create an item). */
    id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface GetTagsCountByLanguageRequest {
}
interface GetTagsCountByLanguageResponse {
    /** The language and it's count. */
    tagsLanguageCount?: TagLanguageCount[];
}
interface TagLanguageCount {
    /**
     * 2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
     * @format LANGUAGE_TAG
     */
    languageCode?: string | null;
    /** The amount of tags for the language. */
    tagCount?: number | null;
}
interface GetTagByLabelRequest {
    /**
     * Tag label.
     * @minLength 1
     * @maxLength 100
     */
    label: string;
    /**
     * Tag language.
     *
     * 2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
     * If omitted, tags in all languages are returned.
     * @format LANGUAGE_TAG
     */
    language?: string | null;
    /**
     * List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
     * the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
     * only the tag's base fields are returned.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
}
interface GetTagByLabelResponse {
    /** Retrieved tag. */
    tag?: BlogTag;
}
interface GetTagRequest {
    /**
     * ID of tag to retrieve.
     * @minLength 1
     * @maxLength 38
     */
    tagId: string;
    /**
     * List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
     * the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
     * only the tag's base fields are returned.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
}
interface GetTagResponse {
    /** Retrieved tag. */
    tag?: BlogTag;
}
interface GetTagBySlugRequest {
    /**
     * Slug of the tag to retrieve.
     * @minLength 1
     * @maxLength 100
     */
    slug: string;
    /**
     * List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
     * the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
     * only the tag's base fields are returned.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
    /**
     * 2-or-4-letter language code in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
     * Language of the tag to retrieve.
     * @format LANGUAGE_TAG
     */
    language?: string | null;
}
interface GetTagBySlugResponse {
    /** Retrieved tag. */
    tag?: BlogTag;
}
interface QueryTagsRequest {
    /** Query options. */
    query?: PlatformQuery;
    /**
     * List of additional tag fields to include in the response. For example, use the `URL` fieldset to retrieve the url field in
     * the response in addition to the tag's base fields. Base fields don’t include any of the supported fieldset values. By default
     * only the tag's base fields are returned.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
}
interface PlatformQuery extends PlatformQueryPagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: CommonPaging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CommonCursorPaging;
    /**
     * Filter object in the following format:
     * `"filter" : {
     * "fieldName1": "value1",
     * "fieldName2":{"$operator":"value2"}
     * }`
     * Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the following format:
     * `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`
     * @maxSize 3
     */
    sort?: Sorting[];
}
/** @oneof */
interface PlatformQueryPagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: CommonPaging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CommonCursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CommonPaging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /** Number of items to skip in the current sort order. */
    offset?: number | null;
}
interface CommonCursorPaging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * You can get the relevant cursor token
     * from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 2000
     */
    cursor?: string | null;
}
interface QueryTagsResponse {
    /** List of tags. */
    tags?: BlogTag[];
    /** Details on the paged set of results returned. */
    pagingMetadata?: PagingMetadataV2;
}
interface PagingMetadataV2 {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    offset?: number | null;
    /** Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set. */
    total?: number | null;
    /** Flag that indicates the server failed to calculate the `total` field. */
    tooManyToCount?: boolean | null;
    /** Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used. */
    cursors?: Cursors;
}
interface Cursors {
    /**
     * Cursor pointing to next page in the list of results.
     * @maxLength 2000
     */
    next?: string | null;
    /**
     * Cursor pointing to previous page in the list of results.
     * @maxLength 2000
     */
    prev?: string | null;
}
interface ListTagsRequest extends ListTagsRequestPagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: Paging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Sorting options. For a list of sortable fields, see [Field Support for Filtering and Sorting](https://dev.wix.com/api/rest/community/blog/filter-and-sort).
     * @maxSize 3
     */
    sort?: Sorting[];
    /**
     * List of tag fields to be included if entities are present in the response.
     * Base fieldset, which is default, will return all core tag properties.
     * Example: When URL fieldset is selected, returned tag will have a set of base properties and tag url.
     * @maxSize 10
     */
    fieldsets?: FieldWithLiterals[];
}
/** @oneof */
interface ListTagsRequestPagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: Paging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `sort`. */
    cursorPaging?: CursorPaging;
}
interface Paging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /** Number of items to skip in the current sort order. */
    offset?: number | null;
}
interface CursorPaging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * You can get the relevant cursor token
     * from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 2000
     */
    cursor?: string | null;
}
interface ListTagsResponse {
    /** List of tags. */
    tags?: BlogTag[];
    /** Details on the paged set of results returned. */
    metaData?: MetaData;
}
interface MetaData {
    /** Number of items returned in this response. */
    count?: number;
    /** Requested offset. */
    offset?: number;
    /** Total number of items that match the query. */
    total?: number;
    /**
     * Pointer to the next or previous page in the list of results.
     * @maxLength 2000
     */
    cursor?: string | null;
}
interface BulkDeleteTagRequest {
    /**
     * Tag IDs.
     * @minSize 1
     * @maxSize 100
     * @maxLength 38
     */
    tagIds?: string[];
    /** Should delete bypassing the trash-bin. */
    permanent?: boolean;
}
interface BulkDeleteTagResponse {
}
interface DeleteTagRequest {
    /**
     * Tag ID.
     * @maxLength 38
     */
    tagId: string;
}
interface DeleteTagResponse {
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entityAsJson?: string;
    /** Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity */
    restoreInfo?: RestoreInfo;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntityAsJson?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntityAsJson?: string | null;
}
interface ActionEvent {
    bodyAsJson?: string;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';

type __PublicMethodMetaInfo<K = string, M = unknown, T = unknown, S = unknown, Q = unknown, R = unknown> = {
    getUrl: (context: any) => string;
    httpMethod: K;
    path: string;
    pathParams: M;
    __requestType: T;
    __originalRequestType: S;
    __responseType: Q;
    __originalResponseType: R;
};
declare function createTag(): __PublicMethodMetaInfo<'POST', {}, CreateTagRequest$1, CreateTagRequest, CreateTagResponse$1, CreateTagResponse>;
declare function getTagByLabel(): __PublicMethodMetaInfo<'GET', {
    label: string;
}, GetTagByLabelRequest$1, GetTagByLabelRequest, GetTagByLabelResponse$1, GetTagByLabelResponse>;
declare function getTag(): __PublicMethodMetaInfo<'GET', {
    tagId: string;
}, GetTagRequest$1, GetTagRequest, GetTagResponse$1, GetTagResponse>;
declare function getTagBySlug(): __PublicMethodMetaInfo<'GET', {
    slug: string;
}, GetTagBySlugRequest$1, GetTagBySlugRequest, GetTagBySlugResponse$1, GetTagBySlugResponse>;
declare function queryTags(): __PublicMethodMetaInfo<'POST', {}, QueryTagsRequest$1, QueryTagsRequest, QueryTagsResponse$1, QueryTagsResponse>;
declare function deleteTag(): __PublicMethodMetaInfo<'DELETE', {
    tagId: string;
}, DeleteTagRequest$1, DeleteTagRequest, DeleteTagResponse$1, DeleteTagResponse>;

export { type ActionEvent as ActionEventOriginal, type ApplicationError as ApplicationErrorOriginal, type BlogTag as BlogTagOriginal, type BulkActionMetadata as BulkActionMetadataOriginal, type BulkCreateTagsRequest as BulkCreateTagsRequestOriginal, type BulkCreateTagsResponse as BulkCreateTagsResponseOriginal, type BulkDeleteTagRequest as BulkDeleteTagRequestOriginal, type BulkDeleteTagResponse as BulkDeleteTagResponseOriginal, type BulkTagResult as BulkTagResultOriginal, type CommonCursorPaging as CommonCursorPagingOriginal, type CommonPaging as CommonPagingOriginal, type CreateTagRequest as CreateTagRequestOriginal, type CreateTagResponse as CreateTagResponseOriginal, type CursorPaging as CursorPagingOriginal, type Cursors as CursorsOriginal, type DeleteTagRequest as DeleteTagRequestOriginal, type DeleteTagResponse as DeleteTagResponseOriginal, type DomainEventBodyOneOf as DomainEventBodyOneOfOriginal, type DomainEvent as DomainEventOriginal, type EntityCreatedEvent as EntityCreatedEventOriginal, type EntityDeletedEvent as EntityDeletedEventOriginal, type EntityUpdatedEvent as EntityUpdatedEventOriginal, Field as FieldOriginal, type FieldWithLiterals as FieldWithLiteralsOriginal, type GetOrCreateTagRequest as GetOrCreateTagRequestOriginal, type GetOrCreateTagResponse as GetOrCreateTagResponseOriginal, type GetTagByLabelRequest as GetTagByLabelRequestOriginal, type GetTagByLabelResponse as GetTagByLabelResponseOriginal, type GetTagBySlugRequest as GetTagBySlugRequestOriginal, type GetTagBySlugResponse as GetTagBySlugResponseOriginal, type GetTagRequest as GetTagRequestOriginal, type GetTagResponse as GetTagResponseOriginal, type GetTagsCountByLanguageRequest as GetTagsCountByLanguageRequestOriginal, type GetTagsCountByLanguageResponse as GetTagsCountByLanguageResponseOriginal, type IdentificationDataIdOneOf as IdentificationDataIdOneOfOriginal, type IdentificationData as IdentificationDataOriginal, type InitialTagsCopied as InitialTagsCopiedOriginal, type ItemMetadata as ItemMetadataOriginal, type Keyword as KeywordOriginal, type ListTagsRequest as ListTagsRequestOriginal, type ListTagsRequestPagingMethodOneOf as ListTagsRequestPagingMethodOneOfOriginal, type ListTagsResponse as ListTagsResponseOriginal, type MessageEnvelope as MessageEnvelopeOriginal, type MetaData as MetaDataOriginal, type PageUrl as PageUrlOriginal, type PagingMetadataV2 as PagingMetadataV2Original, type Paging as PagingOriginal, type PlatformQuery as PlatformQueryOriginal, type PlatformQueryPagingMethodOneOf as PlatformQueryPagingMethodOneOfOriginal, type QueryTagsRequest as QueryTagsRequestOriginal, type QueryTagsResponse as QueryTagsResponseOriginal, type RestoreInfo as RestoreInfoOriginal, type SeoSchema as SeoSchemaOriginal, type Settings as SettingsOriginal, SortOrder as SortOrderOriginal, type SortOrderWithLiterals as SortOrderWithLiteralsOriginal, type Sorting as SortingOriginal, type TagLanguageCount as TagLanguageCountOriginal, type Tag as TagOriginal, type UpdateTagRequest as UpdateTagRequestOriginal, type UpdateTagResponse as UpdateTagResponseOriginal, WebhookIdentityType as WebhookIdentityTypeOriginal, type WebhookIdentityTypeWithLiterals as WebhookIdentityTypeWithLiteralsOriginal, type __PublicMethodMetaInfo, createTag, deleteTag, getTag, getTagByLabel, getTagBySlug, queryTags };
