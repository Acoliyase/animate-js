import { jsx as _jsx } from "react/jsx-runtime";
import { PlanPaywallService, PlanPaywallServiceDefinition, } from '../../services/index.js';
import { WixServices, useService } from '@wix/services-manager-react';
import { createServicesMap } from '@wix/services-manager';
export function Root({ children, planPaywallServiceConfig }) {
    return (_jsx(WixServices, { servicesMap: createServicesMap().addService(PlanPaywallServiceDefinition, PlanPaywallService, planPaywallServiceConfig), children: children }));
}
export function Paywall({ children }) {
    const { isLoadingSignal, errorSignal, hasAccessSignal } = useService(PlanPaywallServiceDefinition);
    const isLoading = isLoadingSignal.get();
    const error = errorSignal.get();
    const hasAccess = hasAccessSignal.get();
    return children({ isLoading, error, hasAccess });
}
