# @wix/headless-node
Headless express API.

Host usage:

```js
import { headlessNode } from '@wix/headless-node';
import { createClient } from '@wix/sdk';

const sdkClient = createClient({
  host: headlessNode.host(),
});
```
