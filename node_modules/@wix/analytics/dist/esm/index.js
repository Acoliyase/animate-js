/**
 * @wix/analytics - Wix Analytics event tracking SDK
 *
 * This package provides a type-safe interface for tracking Wix Analytics events
 * by proxying to the browser's window.wixAnalytics.trackEvent method.
 */
/**
 * Track a Wix Analytics event with type-safe event data
 * @param eventName - Name of the event to track (supports standard Wix events and custom events)
 * @param eventData - Event data typed according to the event name
 *
 * @example
 * ```typescript
 * import { trackEvent } from '@wix/analytics';
 *
 * // Track a standard Wix event with typed parameters
 * trackEvent('AddToCart', {
 *   name: 'Example Product',
 *   price: 29.99,
 *   currency: 'USD',
 *   category: 'Electronics',
 *   quantity: 1
 * });
 *
 * // Track a custom event
 * trackEvent('custom_button_click', {
 *   eventCategory: 'user_interaction',
 *   eventAction: 'click',
 *   eventLabel: 'header_cta'
 * });
 *
 * // Events with no parameters
 * trackEvent('CompleteRegistration');
 * ```
 */
export * from "./analytics.js";
//# sourceMappingURL=index.js.map