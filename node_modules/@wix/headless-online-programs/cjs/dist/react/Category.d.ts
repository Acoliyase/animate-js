import React from 'react';
import { categories } from '@wix/online-programs';
import { AsChildChildren } from '@wix/headless-utils/react';
/**
 * Props for Category.Root component
 */
interface CategoryRootProps {
    children: React.ReactNode;
    category: categories.Category;
}
/**
 * Root component that provides the Category context to its children.
 *
 * @order 1
 * @component
 * @example
 * ```tsx
 * import { Category } from '@wix/online-programs/components';
 *
 * function CategoryCard(props) {
 *  const { category } = props;
 *
 *   return (
 *    <Category.Root category={category}>
 *      <Category.Label />
 *      <Category.Id />
 *    </Category.Root>
 *   );
 * }
 * ```
 */
declare function Root(props: CategoryRootProps): React.ReactNode;
declare namespace Root {
    var displayName: string;
}
/**
 * Props for Category.Id component
 */
interface CategoryIdProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        id: string;
    }>;
    className?: string;
}
/**
 * Props for Category.Label component
 */
interface CategoryLabelProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        label: string;
    }>;
    className?: string;
}
/**
 * Compound component for Category with all sub-components
 */
export declare const Category: {
    readonly Root: typeof Root;
    readonly Id: React.ForwardRefExoticComponent<CategoryIdProps & React.RefAttributes<HTMLElement>>;
    readonly Label: React.ForwardRefExoticComponent<CategoryLabelProps & React.RefAttributes<HTMLElement>>;
};
export {};
