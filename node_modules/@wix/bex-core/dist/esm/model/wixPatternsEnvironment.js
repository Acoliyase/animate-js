export function wixPatternsEnvironment(environment) {
    const { language, artifactId = 'cairo-default-artifact-id', componentId = artifactId, componentName = componentId, uiLibVersion, providerType, msid, } = environment;
    return {
        language,
        artifactId,
        componentId,
        componentName,
        uiLibVersion,
        providerType,
        msid,
    };
}
//# sourceMappingURL=wixPatternsEnvironment.js.map