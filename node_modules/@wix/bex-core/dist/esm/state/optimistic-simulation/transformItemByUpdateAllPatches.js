export const transformItemByUpdateAllPatches = (patches) => (item) => {
    return patches.reduce((current, { selector, patch, actionType }) => {
        if (!selector(item) || actionType !== 'updateAll') {
            return current;
        }
        return {
            ...item,
            ...patch(item),
        };
    }, item);
};
//# sourceMappingURL=transformItemByUpdateAllPatches.js.map