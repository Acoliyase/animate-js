export function refreshFilter(filter, value, options) {
    filter.events.emit('beforeRefresh', {
        ...options,
        clickedValueKey: filter._toArray(value).map(filter.itemKey).join(','),
        actionType: 'add',
    });
    filter.changeValue(value, { ...options, emitEvents: ['refresh'] });
}
//# sourceMappingURL=refreshFilter.js.map