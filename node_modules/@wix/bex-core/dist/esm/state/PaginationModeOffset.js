import { computed, makeObservable } from 'mobx';
export class PaginationModeOffset {
    constructor(params) {
        this.collection = params.collection;
        makeObservable(this, {
            fetchedLastPage: computed,
        });
    }
    get fetchedLastPage() {
        const { totalPages, result: { pages, hasNext }, } = this.collection;
        return hasNext != null ? !hasNext : pages.length >= totalPages;
    }
    setCursor() { }
    get id() {
        return PaginationModeOffset.id;
    }
}
PaginationModeOffset.id = 'offset';
//# sourceMappingURL=PaginationModeOffset.js.map