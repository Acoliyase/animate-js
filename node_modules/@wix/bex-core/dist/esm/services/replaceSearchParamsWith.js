export function replaceSearchParamsWith(history, cb) {
    const searchParams = new URLSearchParams(history.location.search);
    cb(searchParams);
    const search = searchParams.toString();
    history.replace({
        ...history.location,
        search: search ? `?${search}` : '',
    });
}
//# sourceMappingURL=replaceSearchParamsWith.js.map