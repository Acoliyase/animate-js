export function addEventListener(events, event, listener) {
    events.on(event, listener);
    return () => {
        events.off(event, listener);
    };
}
//# sourceMappingURL=addEventListener.js.map