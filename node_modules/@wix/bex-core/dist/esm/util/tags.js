export const getTranslatedErrorMessage = ({ errorCode, visualType, container, }) => {
    let translationKey;
    switch (errorCode) {
        case 'TAGS_REACHED_LIMIT': {
            translationKey =
                visualType === 'toast'
                    ? 'cairo.tags.popover.creationLimitReached.error'
                    : 'cairo.tags.creationLimitReached.error';
            break;
        }
        default:
            translationKey =
                visualType === 'toast'
                    ? 'cairo.tags.collectionPage.toast.create.error'
                    : 'cairo.tags.manageTags.modal.toast.create.error';
    }
    return container.translate(translationKey);
};
//# sourceMappingURL=tags.js.map