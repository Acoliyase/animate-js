"use strict";

exports.__esModule = true;
exports.fixDomainForBI = fixDomainForBI;
function fixDomainForBI(url) {
  try {
    const urlObj = new URL(url);
    urlObj.hostname = urlObj.hostname.replace(/_/g, '.'); // Replace underscores
    return urlObj.toString();
  } catch (e) {
    return 'https://invalid.url';
  }
}
//# sourceMappingURL=fixDomainForBI.js.map