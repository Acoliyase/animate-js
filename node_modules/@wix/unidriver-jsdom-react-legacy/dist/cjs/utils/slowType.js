"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.slowType = void 0;
const test_utils_1 = require("react-dom/test-utils");
const actCompat_1 = require("../actCompat");
const slowType = async (base, value, delayMs) => {
    const { name, type } = base;
    let currentValue = '';
    // eslint-disable-next-line @typescript-eslint/prefer-for-of
    for (let i = 0; i < value.length; i++) {
        currentValue += value[i];
        /* eslint-disable @typescript-eslint/no-misused-promises */
        await (0, actCompat_1.act)(async () => {
            test_utils_1.Simulate.change(base, {
                target: { name, type, value: currentValue },
            });
        });
        await (0, actCompat_1.act)(async () => {
            await new Promise((resolve) => setTimeout(resolve, delayMs));
        });
    }
};
exports.slowType = slowType;
//# sourceMappingURL=slowType.js.map