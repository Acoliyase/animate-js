export const contextToWaitError = (context) => {
    const contextToSelectorString = (ctx) => {
        const maybeIndexDescription = typeof context.idx === 'number' && context.idx >= 0 ? ` at index #${String(context.idx)}` : '';
        const maybeParentDescription = ctx.parent ? `${contextToSelectorString(ctx.parent)} => ` : '';
        // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
        return `${maybeParentDescription}${String(context.selector)}${maybeIndexDescription}`;
    };
    return `Timeout waiting for element: ${contextToSelectorString(context)}`;
};
//# sourceMappingURL=contextToWaitError.js.map