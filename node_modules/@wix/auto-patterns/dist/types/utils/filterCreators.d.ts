import { FilterOptions } from '../hooks/useFilters';
import { SchemaConfig } from '../types';
interface FilterOption {
    value: string;
    label: string;
}
interface BaseFilterParams {
    id: string;
    fieldId: string;
    commonFilterProps: Record<string, any>;
}
export declare const createDateTimeFilter: ({ id, fieldId, commonFilterProps, config, }: BaseFilterParams & {
    config?: {
        mode?: "ONLY_PREDEFINED" | "ONLY_CUSTOM" | "COMBINE";
        presets?: string[];
        includeTime?: boolean;
    };
}) => FilterOptions;
export declare const createBooleanFilter: ({ id, fieldId, commonFilterProps, config, }: BaseFilterParams & {
    config?: {
        trueLabel?: string;
        falseLabel?: string;
    };
}) => FilterOptions;
export declare const createNumberFilter: ({ id, fieldId, commonFilterProps, config, }: BaseFilterParams & {
    config?: {
        min?: number;
        max?: number;
        allowedDecimals?: boolean;
    };
}) => FilterOptions;
export declare const createEnumFilter: ({ id, fieldId, commonFilterProps, config, }: BaseFilterParams & {
    config: {
        options?: FilterOption[];
        selectionMode?: "single" | "multiple";
        optionType?: "select" | "radio" | "checkbox" | "inlineCheckbox";
    };
}) => FilterOptions;
export declare const createReferenceFilter: ({ id, fieldId, commonFilterProps, config, additionalParams, }: BaseFilterParams & {
    config?: {
        selectionMode?: "single" | "multiple";
    };
    additionalParams: {
        referencedSchema: SchemaConfig;
    };
}) => FilterOptions;
export {};
//# sourceMappingURL=filterCreators.d.ts.map