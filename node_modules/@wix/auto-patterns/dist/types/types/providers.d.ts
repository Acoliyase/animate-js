import { SchemaConfig } from './types';
import { CollectionConfig } from '@wix/patterns';
export interface SchemaProviderConfig {
    id: string;
}
export interface DataProviderConfig {
    id: string;
}
export type ExternalSchemaProvider = (collectionId: string, context: any) => Promise<SchemaConfig>;
export type ExternalDataProvider = (collectionId: string, context: any) => CollectionConfig<any, any>['fetchData'];
//# sourceMappingURL=providers.d.ts.map