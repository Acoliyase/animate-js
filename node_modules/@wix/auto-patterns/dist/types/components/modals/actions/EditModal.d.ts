import React from 'react';
import { UpdateActionModal } from '../../../types';
import { ActionModalProps } from './types';
export interface EditModalProps extends ActionModalProps {
    modalParams: UpdateActionModal;
    actionParams: {
        item: any;
    };
}
export declare const EditModal: ({ sdk, modalParams, actionParams, }: EditModalProps) => React.JSX.Element;
//# sourceMappingURL=EditModal.d.ts.map