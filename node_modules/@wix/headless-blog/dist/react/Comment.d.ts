import type { comments } from '@wix/comments';
import { type AsChildChildren } from '@wix/headless-utils/react';
import type { members } from '@wix/members';
import React from 'react';
import { type CommentWithResolvedFields } from '../services/comments-service.js';
export * as Form from './CommentForm.js';
export interface CommentContentProps {
    asChild?: boolean;
    className?: string;
    children?: AsChildChildren<{
        content: any;
    }> | React.ReactNode;
}
interface CommentContextValue {
    comment: CommentWithResolvedFields;
    replies: CommentWithResolvedFields[];
    deleteComment: () => Promise<void>;
    currentMemberId: string | undefined;
}
export declare const CommentContext: React.Context<CommentContextValue | null>;
/**
 * Hook to access the current comment context.
 * Must be used within a Comment.Root or Comment.ReplyItemRepeater component.
 *
 * @returns The comment context containing comment data, replies, and delete function
 * @throws Error if used outside of Comment.Root
 */
export declare function useCommentContext(): CommentContextValue;
type RootProps = {
    comment: CommentWithResolvedFields;
    children: React.ReactNode | AsChildChildren<{
        comment: CommentWithResolvedFields;
        replies: CommentWithResolvedFields[];
    }>;
    asChild?: boolean;
    className?: string;
    currentMemberId: string | undefined;
};
/**
 * Root component for rendering a single comment.
 * Provides comment context to all child components.
 *
 * @component
 * @example
 * ```tsx
 * <Comment.Root comment={comment}>
 *   <Comment.Author />
 *   <Comment.Content />
 *   <Comment.CommentDate />
 * </Comment.Root>
 * ```
 */
export declare const Root: React.ForwardRefExoticComponent<RootProps & React.RefAttributes<HTMLDivElement>>;
/**
 * Displays the comment content with rich text support.
 *
 * @component
 * @example
 * ```tsx
 * // Custom rendering with asChild
 * <Comment.Content asChild>
 *   {({ content }) => (
 *     <div className="comment-content">
 *       <RicosViewer content={content} />
 *     </div>
 *   )}
 * </Comment.Content>
 * ```
 */
export declare const Content: React.ForwardRefExoticComponent<CommentContentProps & React.RefAttributes<HTMLElement>>;
export interface AuthorProps {
    asChild?: boolean;
    className?: string;
    children?: AsChildChildren<{
        author: members.Member | null | undefined;
    }> | React.ReactNode;
}
/**
 * Displays the comment author's name.
 *
 * @component
 * @example
 * ```tsx
 * // Default rendering
 * <Comment.Author />
 *
 * // Custom rendering with asChild
 * <Comment.Author asChild>
 *   {({ author }) => author?.profile?.nickname ?? 'Unknown'} />}
 * </Comment.Author>
 * ```
 */
export declare const Author: React.ForwardRefExoticComponent<AuthorProps & React.RefAttributes<HTMLElement>>;
export interface CommentDateProps {
    asChild?: boolean;
    className?: string;
    locale?: Intl.LocalesArgument;
    children?: AsChildChildren<{
        commentDate: string;
    }> | React.ReactNode;
}
/**
 * Displays the comment date in a formatted way.
 *
 * @component
 * @example
 * ```tsx
 * // Default rendering
 * <Comment.CommentDate />
 *
 * // With custom locale
 * <Comment.CommentDate locale="en-US" />
 *
 * // Custom rendering with asChild
 * <Comment.CommentDate asChild>
 *   {({ commentDate }) => <RelativeDate date={commentDate} />}
 * </Comment.CommentDate>
 * ```
 */
export declare const CommentDate: React.ForwardRefExoticComponent<CommentDateProps & React.RefAttributes<HTMLElement>>;
export interface OwnerProps {
    children?: React.ReactNode;
}
/**
 * Renders if the current member is the owner of the comment.
 *
 * @component
 * @example
 * ```tsx
 * <Comment.Owner>
 *   <Comment.Action.Delete />
 * </Comment.Owner>
 * ```
 */
export declare const Owner: {
    (props: OwnerProps): React.ReactNode;
    displayName: string;
};
export interface StatusProps {
    asChild?: boolean;
    className?: string;
    children?: AsChildChildren<{
        status: comments.Comment['status'];
        isPending: boolean;
    }> | React.ReactNode;
}
/**
 * Displays or provides access to the comment status (PUBLISHED, PENDING, DELETED, etc.).
 *
 * @component
 * @example
 * ```tsx
 * // Default rendering
 * <Comment.Status />
 *
 * // Conditional rendering with asChild
 * <Comment.Status asChild>
 *   {({ status }) => (
 *     status === 'PENDING' ? <span>Awaiting approval</span> : null
 *   )}
 * </Comment.Status>
 * ```
 */
export declare const Status: React.ForwardRefExoticComponent<StatusProps & React.RefAttributes<HTMLElement>>;
export interface ReplyItemsProps {
    className?: string;
    children: React.ReactNode;
}
/**
 * Container for comment replies with empty state support.
 * Follows List Container Level pattern from architecture rules.
 * Always renders if there are total replies, even if no replies are loaded initially.
 *
 * @component
 * @example
 * ```tsx
 * <Comment.ReplyItems>
 *   <Comment.ReplyItemRepeater>
 *     <Comment.Author />
 *     <Comment.Content />
 *   </Comment.ReplyItemRepeater>
 *   <Comment.LoadMoreReplies />
 * </Comment.ReplyItems>
 * ```
 */
export declare const ReplyItems: React.ForwardRefExoticComponent<ReplyItemsProps & React.RefAttributes<HTMLElement>>;
export interface ReplyItemRepeaterProps {
    children: React.ReactNode;
}
/**
 * Repeater component that renders all replies to a comment.
 * Follows Repeater Level pattern from architecture rules.
 * Note: Repeater components do NOT support asChild as per architecture rules.
 *
 * @component
 * @example
 * ```tsx
 * <Comment.ReplyItems>
 *   <Comment.ReplyItemRepeater>
 *     <Comment.Author />
 *     <Comment.Content />
 *   </Comment.ReplyItemRepeater>
 * </Comment.ReplyItems>
 * ```
 */
export declare const ReplyItemRepeater: React.ForwardRefExoticComponent<ReplyItemRepeaterProps & React.RefAttributes<HTMLElement>>;
export interface ParentCommentProps extends Omit<RootProps, 'comment' | 'currentMemberId'> {
}
/**
 * Displays the parent comment when the current comment is a reply to another reply.
 * Only renders if the parent is not a top-level comment.
 *
 * @component
 * @example
 * ```tsx
 * // Default rendering
 * <Comment.ParentComment>
 *   <Comment.Author />
 *   <Comment.Content />
 * </Comment.ParentComment>
 *
 * // Custom rendering with asChild
 * <Comment.ParentComment asChild>
 *   {({ comment: parentComment }) => (
 *     <div>
 *       Replying to: <CommentPreview comment={parentComment} />
 *     </div>
 *   )}
 * </Comment.ParentComment>
 * ```
 */
export declare const ParentComment: React.ForwardRefExoticComponent<ParentCommentProps & React.RefAttributes<HTMLDivElement>>;
export interface LoadMoreRepliesProps {
    asChild?: boolean;
    className?: string;
    loadingState?: React.ReactNode;
    children?: AsChildChildren<{
        isLoading: boolean;
        hasNextPage: boolean;
        loadNextPage: () => Promise<void>;
    }> | React.ReactNode;
}
/**
 * Button to load more replies when there are additional pages.
 * Only renders when there are more replies to load.
 *
 * @component
 * @example
 * ```tsx
 * // Default rendering
 * <Comment.LoadMoreReplies />
 *
 * // Custom rendering with asChild
 * <Comment.LoadMoreReplies asChild>
 *   {({ hasNextPage, isLoading, loadNextPage }) => (
 *     <button onClick={loadNextPage} disabled={isLoading}>
 *       {isLoading ? 'Loading...' : 'Show more replies'}
 *     </button>
 *   )}
 * </Comment.LoadMoreReplies>
 * ```
 */
export declare const LoadMoreReplies: React.ForwardRefExoticComponent<LoadMoreRepliesProps & React.RefAttributes<HTMLElement>>;
export interface DeleteActionProps {
    /** If callback returns false, the delete action will not be performed. */
    onDelete?: () => boolean;
    asChild?: boolean;
    className?: string;
    children?: React.ReactNode | AsChildChildren<{
        deleteComment: () => Promise<void>;
    }>;
}
export declare const Action: {
    Delete: React.ForwardRefExoticComponent<DeleteActionProps & React.RefAttributes<HTMLButtonElement>>;
};
//# sourceMappingURL=Comment.d.ts.map