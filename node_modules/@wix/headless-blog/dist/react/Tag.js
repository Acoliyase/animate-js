import { jsx as _jsx } from "react/jsx-runtime";
import { AsChildSlot } from '@wix/headless-utils/react';
import React from 'react';
import { useTagRepeaterContext } from './Post.js';
export const Label = React.forwardRef((props, ref) => {
    const { asChild, children, className } = props;
    const { tag } = useTagRepeaterContext();
    if (!tag?.label)
        return null;
    const attributes = {
        'data-testid': "blog-tag-label" /* TestIds.blogTagLabel */,
    };
    return (_jsx(AsChildSlot, { ref: ref, asChild: asChild, className: className, ...attributes, customElement: children, customElementProps: { tag }, content: tag.label, children: _jsx("span", { children: tag.label }) }));
});
Label.displayName = 'Blog.Tag.Label';
