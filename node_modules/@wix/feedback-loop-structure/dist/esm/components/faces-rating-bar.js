import { z } from 'zod';
const FacesRatingBar = 'FacesRatingBar';
export const FacesRatingBarSchema = z.object({
    type: z.literal(FacesRatingBar),
    questionId: z.string(),
    label: z.string(),
    tooltips: z
        .tuple([z.string(), z.string(), z.string(), z.string(), z.string()])
        .optional(),
});
//# sourceMappingURL=faces-rating-bar.js.map