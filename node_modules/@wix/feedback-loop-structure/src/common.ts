export const FORM_CLOSE_EVENT = '$$FeedbackFormClose';
export const GLOBAL_STRUCTURE_KEY = '__structure';
export const GLOBAL_METADATA_KEY = '__metadata';
export const GLOBAL_LANGUAGE_KEY = '__language';
export const GLOBAL_DEBUG_KEY = '__debug';

export enum FormCloseReason {
  Timeout = 'timeout',
  Submitted = 'submitted',
  Closed = 'closed',
}

export interface IFrameNotShownEvent {
  reason: 'not-shown';
}

export type IFrameCloseEvent =
  | {
      reason: FormCloseReason.Closed;
    }
  | {
      reason: FormCloseReason.Submitted;
    };
