"use strict";

exports.__esModule = true;
exports.registerHeightReporter = void 0;
const registerHeightReporter = (bridge, container) => {
  const update = () => bridge.updateHeight(container.offsetHeight);
  const resizeObserver = new ResizeObserver(update);
  resizeObserver.observe(container);
  return () => resizeObserver.disconnect();
};
exports.registerHeightReporter = registerHeightReporter;
//# sourceMappingURL=registerHeightReporter.js.map