import { NonNullablePaths } from '@wix/sdk-types';

interface Rule extends RuleValueOneOf, RuleRequirementsOneOf, RuleConditionsOneOf, RuleConditionTypeOptionsOneOf, RuleTaxesOneOf {
    /**
     * Fixed fee. Must hold a positive value.
     * @deprecated Fixed fee. Must hold a positive value.
     * @replacedBy fixed_fee
     * @targetRemovalDate 2024-02-15
     */
    amount?: CommonMoney;
    /**
     * Percentage fee. For example, `5` represents a 5% fee applied to the order's total.
     * @decimalValue options { maxScale:2, gte:0, lte:100 }
     * @deprecated
     * @replacedBy percentage_fee
     * @targetRemovalDate 2024-02-15
     */
    percentage?: string | null;
    /** Fixed fee. Must hold a positive value. */
    fixedFee?: Money;
    /**
     * Percentage fee. For example, 5 represents a 5% fee applied to the order's total.
     *
     * Min: `0`.
     *
     * Max: `100`.
     * @decimalValue options { maxScale:2, gte:0, lte:100 }
     */
    percentageFee?: string | null;
    /**
     * Single condition that must be met for the rule to be applied to an order.
     * @deprecated Single condition that must be met for the rule to be applied to an order.
     * @replacedBy condition_options
     * @targetRemovalDate 2024-02-15
     */
    condition?: Condition;
    /**
     * Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order.
     * @deprecated Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order.
     * @replacedBy condition_tree_options
     * @targetRemovalDate 2024-02-15
     */
    conditionTree?: ConditionTree;
    /** Single condition that must be met for the rule to be applied to an order. */
    conditionOptions?: Condition;
    /** Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order. */
    conditionTreeOptions?: ConditionTree;
    /** Percentage value to apply as a custom tax rate. Range: [0-100]. */
    customTaxRate?: string | null;
    /**
     * Tax group ID. Internal only.
     * @format GUID
     */
    taxGroupId?: string | null;
    /**
     * Rule ID.
     * @readonly
     */
    _id?: string | null;
    /**
     * ID of the restaurant’s [location](/locations/introduction).
     * @format GUID
     */
    locationId?: string | null;
    /**
     * Rule name.
     * @maxLength 50
     * @minLength 1
     */
    name?: string | null;
    /**
     * Date and time the rule was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the rule was updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Percentage value to apply as a custom tax rate. For example, `5` represents a 5% fee applied to the order's total. <br />
     * Min: '0'. <br />
     * Max: `100`.
     * @decimalValue options { maxScale:2, gte:0, lte:100 }
     * @deprecated
     * @replacedBy taxes
     * @targetRemovalDate 2023-09-16
     */
    taxRate?: string | null;
    /**
     * Specifies the type of condition.
     * @deprecated Specifies the type of condition.
     * @replacedBy condition_type
     * @targetRemovalDate 2024-02-15
     */
    conditionsType?: ConditionsTypeWithLiterals;
    /** Specifies the type of condition. */
    conditionType?: ConditionTypeWithLiterals;
    /** Whether the rule is enabled. If `true`, the rule is evaluated when service fees are calculated. If `false`, the rule will not be evaluated when service fees are calculated. */
    enabled?: boolean | null;
    /**
     * Revision number. Increments by 1 each time the rule is updated.
     * To prevent conflicting changes, the existing `revision` must be used when updating a rule.
     * @readonly
     */
    revision?: string | null;
    /**
     * DEPRECATED. Defines the app that the rule is connected to.
     * @maxLength 100
     * @minLength 1
     * @deprecated DEPRECATED. Defines the app that the rule is connected to.
     * @replacedBy app_id
     * @targetRemovalDate 2023-02-19
     */
    label?: string | null;
    /**
     * Defines the app that the rule is connected to.
     * @format GUID
     */
    appId?: string | null;
    /**
     * Rounding strategy to apply to fee and tax calculation.
     *
     * Supported values:
     *
     * - `"HALF_UP"`: Rounds up any number exactly halfway between two integers. For example, `2.5` rounds to `3`, and `3.5` and rounds to `4`.
     * - `"HALF_EVEN"`: Rounds such numbers to the nearest even integer. For example, `2.5` rounds to `2`, but `3.5` rounds to `4`.
     * - `"UNKNOWN_ROUNDING_STRATEGY"`
     */
    roundingStrategy?: RoundingStrategyWithLiterals;
    /** Data Extensions */
    extendedFields?: ExtendedFields;
    /** Tags ([SDK](https://dev.wix.com/docs/sdk/backend-modules/tags/tags/introduction) | [REST](https://dev.wix.com/docs/rest/business-management/tags/introduction)) used to classify and sort different types of rules. */
    tags?: Tags;
}
/** @oneof */
interface RuleValueOneOf {
    /**
     * Fixed fee. Must hold a positive value.
     * @deprecated Fixed fee. Must hold a positive value.
     * @replacedBy fixed_fee
     * @targetRemovalDate 2024-02-15
     */
    amount?: CommonMoney;
    /**
     * Percentage fee. For example, `5` represents a 5% fee applied to the order's total.
     * @decimalValue options { maxScale:2, gte:0, lte:100 }
     * @deprecated
     * @replacedBy percentage_fee
     * @targetRemovalDate 2024-02-15
     */
    percentage?: string | null;
}
/** @oneof */
interface RuleRequirementsOneOf {
    /** Fixed fee. Must hold a positive value. */
    fixedFee?: Money;
    /**
     * Percentage fee. For example, 5 represents a 5% fee applied to the order's total.
     *
     * Min: `0`.
     *
     * Max: `100`.
     * @decimalValue options { maxScale:2, gte:0, lte:100 }
     */
    percentageFee?: string | null;
}
/** @oneof */
interface RuleConditionsOneOf {
    /**
     * Single condition that must be met for the rule to be applied to an order.
     * @deprecated Single condition that must be met for the rule to be applied to an order.
     * @replacedBy condition_options
     * @targetRemovalDate 2024-02-15
     */
    condition?: Condition;
    /**
     * Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order.
     * @deprecated Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order.
     * @replacedBy condition_tree_options
     * @targetRemovalDate 2024-02-15
     */
    conditionTree?: ConditionTree;
}
/** @oneof */
interface RuleConditionTypeOptionsOneOf {
    /** Single condition that must be met for the rule to be applied to an order. */
    conditionOptions?: Condition;
    /** Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order. */
    conditionTreeOptions?: ConditionTree;
}
/** @oneof */
interface RuleTaxesOneOf {
    /** Percentage value to apply as a custom tax rate. Range: [0-100]. */
    customTaxRate?: string | null;
    /**
     * Tax group ID. Internal only.
     * @format GUID
     */
    taxGroupId?: string | null;
}
/**
 * Money.
 * Default format to use. Sufficiently compliant with majority of standards: w3c, ISO 4217, ISO 20022, ISO 8583:2003.
 */
interface CommonMoney {
    /**
     * Monetary amount. Decimal string with a period as a decimal separator (e.g., 3.99). Optionally, a single (-), to indicate that the amount is negative.
     * @format DECIMAL_VALUE
     */
    value?: string;
    /**
     * Currency code. Must be valid ISO 4217 currency code (e.g., USD).
     * @format CURRENCY
     */
    currency?: string;
    /** Monetary amount. Decimal string in local format (e.g., 1 000,30). Optionally, a single (-), to indicate that the amount is negative. */
    formattedValue?: string | null;
}
/**
 * Money.
 * Default format to use. Sufficiently compliant with majority of standards: w3c, ISO 4217, ISO 20022, ISO 8583:2003.
 */
interface Money {
    /**
     * Monetary amount. Decimal string with a period as a decimal separator (e.g., 3.99). Optionally, a single (-), to indicate that the amount is negative.
     * @format DECIMAL_VALUE
     */
    value?: string;
    /**
     * Currency code. Must be valid ISO 4217 currency code (e.g., USD).
     * @format CURRENCY
     */
    currency?: string;
    /** Monetary amount. Decimal string in local format (e.g., 1 000,30). Optionally, a single (-), to indicate that the amount is negative. */
    formattedValue?: string | null;
}
interface Condition extends ConditionValueOneOf {
    /**
     * Contains a numeric value and an operation.
     *
     * Required if `expectedFieldType` is `NUMBER`.
     */
    number?: _Number;
    /**
     * Contains an array of strings to compare with the value of the specified field. If the value of the field matches a string in the array, the condition is considered met.
     *
     * Required if `expectedFieldType` is `LIST`.
     */
    list?: List;
    /**
     * Path of the field in the order entity that this condition will evaluate. For example, `priceSummary.subtotal`.
     * @maxLength 1000
     * @deprecated
     * @replacedBy order_field_path
     * @targetRemovalDate 2024-02-15
     */
    path?: string;
    /**
     * Path of the field in the order entity that this condition will evaluate. For example, `priceSummary.subtotal`.
     * @maxLength 1000
     */
    orderFieldPath?: string;
    /**
     * Type of the field specified in `orderFieldPath`. For example, the type of `priceSummary.subtotal` is `"NUMBER"`).
     * @deprecated Type of the field specified in `orderFieldPath`. For example, the type of `priceSummary.subtotal` is `"NUMBER"`).
     * @replacedBy expected_field_type
     * @targetRemovalDate 2024-02-15
     */
    expectedType?: ExpectedType;
    /** Type of the field specified in `orderFieldPath`. For example, the type of `priceSummary.subtotal` is `"NUMBER"`). */
    expectedFieldType?: ExpectedFieldTypeWithLiterals;
}
/** @oneof */
interface ConditionValueOneOf {
    /**
     * Contains a numeric value and an operation.
     *
     * Required if `expectedFieldType` is `NUMBER`.
     */
    number?: _Number;
    /**
     * Contains an array of strings to compare with the value of the specified field. If the value of the field matches a string in the array, the condition is considered met.
     *
     * Required if `expectedFieldType` is `LIST`.
     */
    list?: List;
}
interface ExpectedType {
    /** Type of the field specified in `orderFieldPath`. */
    value?: ValueWithLiterals;
}
declare enum Value {
    /** Represents a number value. */
    NUMBER = "NUMBER",
    /** Represents a list of strings values - Not supported yet. */
    LIST = "LIST",
    /** Represents a string value. */
    STRING = "STRING"
}
/** @enumType */
type ValueWithLiterals = Value | 'NUMBER' | 'LIST' | 'STRING';
declare enum ExpectedFieldType {
    /** Unknown expected field type */
    UNKNOWN_EXPECTED_FIELD_TYPE = "UNKNOWN_EXPECTED_FIELD_TYPE",
    /** Represents a number value. */
    NUMBER = "NUMBER",
    /** Represents a list of strings values - Not supported yet. */
    LIST = "LIST",
    /** Represents a string value. */
    STRING = "STRING"
}
/** @enumType */
type ExpectedFieldTypeWithLiterals = ExpectedFieldType | 'UNKNOWN_EXPECTED_FIELD_TYPE' | 'NUMBER' | 'LIST' | 'STRING';
interface _Number {
    /** Numeric value to compare with the value of the specified field. */
    value?: number;
    /**
     * Operation to use.
     *
     * Supported values:
     *
     * - `"EQ"`: Equal to.
     * - `"LT"`: Strictly less than.
     * - `"LE"`: Less than or equal to.
     * - `"GT"`: Strictly greater than.
     * - `"GE"`: Greater than or equal to.
     */
    operation?: OperationWithLiterals;
}
declare enum Operation {
    /** Equal to. */
    EQ = "EQ",
    /** Strictly less than. */
    LT = "LT",
    /** Less than or equal to. */
    LE = "LE",
    /** Strictly greater than. */
    GT = "GT",
    /** Greater than or equal to. */
    GE = "GE"
}
/** @enumType */
type OperationWithLiterals = Operation | 'EQ' | 'LT' | 'LE' | 'GT' | 'GE';
interface List {
    /**
     * Array of string values to compare with the value of the field. If the value of the fields matches a string in the array, the condition is considered met.
     * @minSize 1
     * @maxSize 100
     * @minLength 1
     * @maxLength 100
     */
    values?: string[];
}
/** Used to represent a logical condition in the form of a tree structure. */
interface ConditionTree extends ConditionTreeLeftConditionNodeOneOf, ConditionTreeRightConditionNodeOneOf {
    /** Single condition that must be met for the rule to be applied to an order. */
    leftCondition?: Condition;
    /** Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order. */
    leftConditionsTree?: ConditionTree;
    /** Single condition that must be met for the rule to be applied to an order. */
    rightCondition?: Condition;
    /** Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order. */
    rightConditionsTree?: ConditionTree;
    /** Specifies the logical operator to use when combining the evaluation of the left and right conditions. */
    operator?: OperatorWithLiterals;
}
/** @oneof */
interface ConditionTreeLeftConditionNodeOneOf {
    /** Single condition that must be met for the rule to be applied to an order. */
    leftCondition?: Condition;
    /** Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order. */
    leftConditionsTree?: ConditionTree;
}
/** @oneof */
interface ConditionTreeRightConditionNodeOneOf {
    /** Single condition that must be met for the rule to be applied to an order. */
    rightCondition?: Condition;
    /** Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order. */
    rightConditionsTree?: ConditionTree;
}
declare enum Operator {
    /** The condition is true if both the left and right sides are true. */
    AND = "AND",
    /** The condition is true if either the left or right side is true. */
    OR = "OR"
}
/** @enumType */
type OperatorWithLiterals = Operator | 'AND' | 'OR';
declare enum ConditionsType {
    /** Indicates that the Rule has no conditions specified. */
    NO_CONDITIONS = "NO_CONDITIONS",
    /** Indicates that the Rule has a single Condition. */
    CONDITION = "CONDITION",
    /** Indicates that the Rule has a complex condition specified as a ConditionTree. */
    CONDITION_TREE = "CONDITION_TREE"
}
/** @enumType */
type ConditionsTypeWithLiterals = ConditionsType | 'NO_CONDITIONS' | 'CONDITION' | 'CONDITION_TREE';
declare enum ConditionType {
    /** Indicates that the Rule has no conditions specified. */
    UNDEFINED_CONDITION_TYPE = "UNDEFINED_CONDITION_TYPE",
    /** Indicates that the Rule has a single Condition. */
    CONDITION = "CONDITION",
    /** Indicates that the Rule has a complex condition specified as a ConditionTree. */
    CONDITION_TREE = "CONDITION_TREE"
}
/** @enumType */
type ConditionTypeWithLiterals = ConditionType | 'UNDEFINED_CONDITION_TYPE' | 'CONDITION' | 'CONDITION_TREE';
declare enum RoundingStrategy {
    /** Unknown rounding strategy */
    UNKNOWN_ROUNDING_STRATEGY = "UNKNOWN_ROUNDING_STRATEGY",
    /** Half-up rounding strategy - relevant for fee and percentage fee calculation. */
    HALF_UP = "HALF_UP",
    /** Half-even rounding strategy - relevant for fee and percentage fee calculation. */
    HALF_EVEN = "HALF_EVEN"
}
/** @enumType */
type RoundingStrategyWithLiterals = RoundingStrategy | 'UNKNOWN_ROUNDING_STRATEGY' | 'HALF_UP' | 'HALF_EVEN';
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
/**
 * Common object for tags.
 * Should be use as in this example:
 * message Foo {
 * string id = 1;
 * ...
 * Tags tags = 5
 * }
 * example of taggable entity
 * {
 * id: "123"
 * tags: {
 * tags: {
 * tag_ids:["11","22"]
 * },
 * private_tags: {
 * tag_ids: ["33", "44"]
 * }
 * }
 * }
 */
interface Tags {
    /** Tags that require an additional permission in order to access them, normally not given to site members or visitors. */
    privateTags?: TagList;
    /** Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors. */
    tags?: TagList;
}
interface TagList {
    /**
     * List of tag IDs
     * @maxSize 100
     * @maxLength 5
     */
    tagIds?: string[];
}
interface CalculateServiceFeesRequest {
    /** Order information needed to evaluate the rules and calculate the relevant fees. */
    order: Order;
    /**
     * DEPRECATED. Defines the app that the rule is connected to.
     * @maxLength 100
     * @minLength 1
     * @deprecated DEPRECATED. Defines the app that the rule is connected to.
     * @replacedBy app_id
     * @targetRemovalDate 2023-02-19
     */
    label?: string | null;
    /**
     * Defines the app that the rule is connected to.
     * @format GUID
     */
    appId?: string | null;
}
interface Order {
    /**
     * Order ID.
     * @readonly
     */
    _id?: string;
    /**
     * ID of the site's location.
     * @format GUID
     */
    locationId?: string | null;
    /**
     * Currency used for pricing on the site.
     * @format CURRENCY
     */
    currency?: string | null;
    /** Information about the price of the order. */
    priceSummary?: PriceSummary;
    /** Order's shipping information. */
    shippingInfo?: ShippingInformation;
    /** Platform on which the order was placed. */
    platform?: Platform;
    /** Order's locale. */
    locale?: Locale;
}
interface PriceSummary {
    /**
     * Subtotal of the order.
     * @decimalValue options { maxScale:3 }
     */
    subtotal?: string;
}
interface ShippingInformation {
    /** Information about the type of delivery. For example, pick-up. */
    logistics?: DeliveryLogistics;
}
interface DeliveryLogistics {
    /**
     * Type of delivery.
     *
     * Supported values:
     * - `"UNSPECIFIED_FULFILLMENT_TYPE"`
     * - `"PICKUP"`
     * - `"DELIVERY"`
     * - `"DINE_IN"`
     * - `"CURBSIDE_PICKUP"`
     */
    type?: TypeWithLiterals;
}
declare enum Type {
    /** Missing type due to an error. */
    UNSPECIFIED_FULFILLMENT_TYPE = "UNSPECIFIED_FULFILLMENT_TYPE",
    /** Pickup */
    PICKUP = "PICKUP",
    /** Delivery */
    DELIVERY = "DELIVERY",
    /** Dine-in */
    DINE_IN = "DINE_IN",
    /** Curbside-pickup */
    CURBSIDE_PICKUP = "CURBSIDE_PICKUP"
}
/** @enumType */
type TypeWithLiterals = Type | 'UNSPECIFIED_FULFILLMENT_TYPE' | 'PICKUP' | 'DELIVERY' | 'DINE_IN' | 'CURBSIDE_PICKUP';
interface Platform {
    /**
     * Platform on which the order was placed.
     *
     * Supported values:
     * - `"SITE"`
     * - `"MOBILE_SITE"`
     * - `"MOBILE_APP"`
     */
    value?: PlatformValueWithLiterals;
}
declare enum PlatformValue {
    /** Site */
    SITE = "SITE",
    /** Mobile site */
    MOBILE_SITE = "MOBILE_SITE",
    /** Mobile app */
    MOBILE_APP = "MOBILE_APP"
}
/** @enumType */
type PlatformValueWithLiterals = PlatformValue | 'SITE' | 'MOBILE_SITE' | 'MOBILE_APP';
interface Locale {
    /**
     * Locale in [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
     * Typically, this is a lowercase 2-letter language code,
     * followed by a hyphen, followed by an uppercase 2-letter country code.
     * For example, `en-US` for U.S. English, and `de-DE` for Germany German.
     */
    languageCode?: string | null;
    /** 2-letter country code in [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements) format. */
    country?: string | null;
}
interface CalculateServiceFeesResponse {
    /** A list of calculated fees based on rules evaluation. */
    calculatedFees?: CalculatedFee[];
}
interface CalculatedFee {
    /**
     * The ID of the rule that was used to calculate the fee.
     * @format GUID
     */
    ruleId?: string;
    /**
     * The name of the rule that was used to calculate the fee.
     * @readonly
     */
    name?: string;
    /** Fee amount. */
    fee?: Money;
    /** Tax amount. */
    tax?: Money;
    /**
     * Tax group ID. This is an alternative to calculating the tax amount manually.
     * @format GUID
     */
    taxGroupId?: string | null;
}
interface CreateRuleRequest {
    /** Rule to create. */
    rule: Rule;
}
interface CreateRuleResponse {
    /** Created rule */
    rule?: Rule;
}
interface GetRuleRequest {
    /**
     * ID of the rule to retrieve.
     * @format GUID
     */
    ruleId: string;
}
interface GetRuleResponse {
    /** The retrieved rule. */
    rule?: Rule;
}
interface UpdateRuleRequest {
    /** Rule to update. */
    rule: Rule;
}
interface UpdateRuleResponse {
    /** Updated rule. */
    rule?: Rule;
}
interface DeleteRuleRequest {
    /**
     * ID of the rule to delete.
     * @format GUID
     */
    ruleId: string;
}
interface DeleteRuleResponse {
}
interface ListRulesRequest {
    /**
     * Retrieve only rule that apply at the [location](https://dev.wix.com/docs/rest/api-reference/business-tools/locations/location-object) with this ID. If this field is `null`, the rules will not be filtered by location.
     * @format GUID
     */
    locationId?: string | null;
    /**
     * DEPRECATED. Defines the app that the rule is connected to.
     * @maxLength 100
     * @minLength 1
     * @deprecated DEPRECATED. Defines the app that the rule is connected to.
     * @replacedBy app_id
     * @targetRemovalDate 2023-02-19
     */
    label?: string | null;
    /**
     * Defines the app that the rule is connected to.
     * @format GUID
     */
    appId?: string | null;
}
interface ListRulesResponse {
    /** Array containing the retrieved rules. */
    rules?: Rule[];
}
interface QueryRulesRequest {
    /** Query options. */
    query: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     * See [API Query Language](https://dev.wix.com/api/rest/getting-started/api-query-language)
     * for more information.
     *
     * For a detailed list of supported filters, see
     * [Supported Filters](/supported-filters-and-sorting).
     */
    filter?: Record<string, any> | null;
    /** Sort object. */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /**
     * Sort order.
     *
     * Supported values:
     * - `"ASC"`
     * - `"DESC"`
     */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Number of items to load. <br />
     * Default: `50`
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * You can get the relevant cursor token
     * from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     */
    cursor?: string | null;
}
interface QueryRulesResponse {
    /** The retrieved rules. */
    rules?: Rule[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    cursors?: Cursors;
    /**
     * Indicates if there are more results after the current page.
     * If `true`, another page of results can be retrieved.
     * If `false`, this is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /** Cursor pointing to next page in the list of results. */
    next?: string | null;
    /** Cursor pointing to previous page in the list of results. */
    prev?: string | null;
}
interface BulkCreateRulesRequest {
    /** Rules to create. */
    rules: Rule[];
    /**
     * Whether the full rule entity is returned. <br />
     * Default: `false`.
     */
    returnFullEntity?: boolean;
}
interface BulkCreateRulesResponse {
    /** The created rules. Omitted if `returnFullEntity` is set to false. */
    results?: BulkRuleResult[];
    /** Bulk Create Rule metadata. */
    bulkActionMetadata?: CommonBulkActionMetadata;
}
interface BulkRuleResult {
    /** Metadata of the rule. */
    itemMetadata?: CommonItemMetadata;
    /** The created rule. */
    rule?: Rule;
}
interface CommonItemMetadata {
    /** Item ID. Should always be available, unless it's impossible (for example, when failing to create an item). */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface CommonBulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface BulkUpdateRulesRequest {
    /** Masked rules to update. */
    rules: MaskedRule[];
    /**
     * Whether the full rule entity is returned. <br />
     * Default: `false`.
     */
    returnFullEntity?: boolean;
}
interface MaskedRule {
    /** Rule to update. */
    rule?: Rule;
    /** Explicit list of fields to update */
    mask?: string[];
}
interface BulkUpdateRulesResponse {
    /** The updated rules. Omitted if `returnFullEntity` is set to false. */
    results?: BulkRuleResult[];
    /** Bulk Update Rule metadata. */
    bulkActionMetadata?: CommonBulkActionMetadata;
}
interface BulkDeleteRulesRequest {
    /**
     * IDs of the rules to delete.
     * @format GUID
     */
    ruleIds: string[];
}
interface BulkDeleteRulesResponse {
    /** Information about the deleted rules. */
    results?: BulkRuleResult[];
    /** Bulk Delete Rule metadata. */
    bulkActionMetadata?: CommonBulkActionMetadata;
}
interface BulkUpdateRuleTagsRequest {
    /**
     * IDs of the rules to update tags for.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    ruleIds: string[];
    /** Tags to assign to the rules. */
    assignTags?: Tags;
    /** Tags to unassign from the rules. */
    unassignTags?: Tags;
}
interface BulkUpdateRuleTagsResponse {
    /**
     * Results of the bulk update.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateRuleTagsResult[];
    /** Metadata for the bulk update. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface ItemMetadata {
    /**
     * Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).
     * @format GUID
     */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface BulkUpdateRuleTagsResult {
    /** Metadata for the updated rule. */
    itemMetadata?: ItemMetadata;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface BulkUpdateRuleTagsByFilterRequest {
    /** Filter that determines which rules to update tags for. */
    filter: Record<string, any> | null;
    /** Tags to assign to the rules. */
    assignTags?: Tags;
    /** Tags to unassign from the rules. */
    unassignTags?: Tags;
}
interface BulkUpdateRuleTagsByFilterResponse {
    /**
     * Job ID. Pass this ID to Get Async Job ([SDK](https://dev.wix.com/docs/sdk/backend-modules/async-jobs/get-async-job) | [REST](https://dev.wix.com/docs/rest/business-management/async-job/get-async-job)) to track the job's status.
     * @format GUID
     */
    jobId?: string;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface Empty {
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
/** @docsIgnore */
type BulkUpdateRuleTagsApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type BulkUpdateRuleTagsByFilterApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at 16:00 and then again at 16:01, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
interface RuleCreatedEnvelope {
    entity: Rule;
    metadata: EventMetadata;
}
/** @webhook
 * @eventType wix.service.fees.v1.rule_created
 * @serviceIdentifier com.wixpress.service.fees.v1.ServiceFeesRules
 * @slug created
 * @documentationMaturity preview
 */
declare function onRuleCreated(handler: (event: RuleCreatedEnvelope) => void | Promise<void>): void;
interface RuleDeletedEnvelope {
    metadata: EventMetadata;
}
/** @webhook
 * @eventType wix.service.fees.v1.rule_deleted
 * @serviceIdentifier com.wixpress.service.fees.v1.ServiceFeesRules
 * @slug deleted
 * @documentationMaturity preview
 */
declare function onRuleDeleted(handler: (event: RuleDeletedEnvelope) => void | Promise<void>): void;
interface RuleUpdatedEnvelope {
    entity: Rule;
    metadata: EventMetadata;
}
/** @webhook
 * @eventType wix.service.fees.v1.rule_updated
 * @serviceIdentifier com.wixpress.service.fees.v1.ServiceFeesRules
 * @slug updated
 * @documentationMaturity preview
 */
declare function onRuleUpdated(handler: (event: RuleUpdatedEnvelope) => void | Promise<void>): void;
/**
 * The `calculateServiceFees()` function returns a Promise that resolves to an array containing the calculated fees.
 *
 *
 * The specified order information is evaluated against all rules created for the site. If the rule conditions are met, the service fee set in the rule is applied. Otherwise, no service fee is added.
 * @param order - Order information needed to evaluate the rules and calculate the relevant fees.
 * @public
 * @documentationMaturity preview
 * @requiredField order
 * @requiredField order.currency
 * @requiredField order.priceSummary
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_READ
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesCalculate.CalculateServiceFees
 */
declare function calculateServiceFees(order: NonNullablePaths<Order, `currency` | `priceSummary`, 2>, options?: CalculateServiceFeesOptions): Promise<NonNullablePaths<CalculateServiceFeesResponse, `calculatedFees` | `calculatedFees.${number}.ruleId` | `calculatedFees.${number}.name` | `calculatedFees.${number}.fee.value` | `calculatedFees.${number}.fee.currency`, 5>>;
interface CalculateServiceFeesOptions {
    /**
     * DEPRECATED. Defines the app that the rule is connected to.
     * @maxLength 100
     * @minLength 1
     * @deprecated DEPRECATED. Defines the app that the rule is connected to.
     * @replacedBy app_id
     * @targetRemovalDate 2023-02-19
     */
    label?: string | null;
    /**
     * Defines the app that the rule is connected to.
     * @format GUID
     */
    appId?: string | null;
}
/**
 * The `createRule()` function returns a Promise that resolves to the created rule.
 *
 *
 * To create multiple rules at once, use the `bulkCreateRules()` function.
 * @param rule - Rule to create.
 * @public
 * @documentationMaturity preview
 * @requiredField rule
 * @requiredField rule.enabled
 * @requiredField rule.name
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_CREATE
 * @applicableIdentity APP
 * @returns Created rule
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.CreateRule
 */
declare function createRule(rule: NonNullablePaths<Rule, `enabled` | `name`, 2>): Promise<NonNullablePaths<Rule, `amount.value` | `amount.currency` | `fixedFee.value` | `fixedFee.currency` | `condition.number.value` | `condition.number.operation` | `condition.list.values` | `condition.path` | `condition.orderFieldPath` | `condition.expectedType.value` | `condition.expectedFieldType` | `conditionTree.operator` | `conditionsType` | `conditionType` | `roundingStrategy` | `tags.privateTags.tagIds`, 4>>;
/**
 * The `createRule()` function returns a Promise that resolves to the retrieved rule.
 * @param ruleId - ID of the rule to retrieve.
 * @public
 * @documentationMaturity preview
 * @requiredField ruleId
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_READ
 * @applicableIdentity APP
 * @returns The retrieved rule.
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.GetRule
 */
declare function getRule(ruleId: string): Promise<NonNullablePaths<Rule, `amount.value` | `amount.currency` | `fixedFee.value` | `fixedFee.currency` | `condition.number.value` | `condition.number.operation` | `condition.list.values` | `condition.path` | `condition.orderFieldPath` | `condition.expectedType.value` | `condition.expectedFieldType` | `conditionTree.operator` | `conditionsType` | `conditionType` | `roundingStrategy` | `tags.privateTags.tagIds`, 4>>;
/**
 * The `updateRule()` function returns a Promise that resolves to the updated rule.
 *
 *
 * Each time the task is updated, `revision` increments by 1. The existing `revision` must be included when updating the task. This ensures you're working with the latest task and prevents unintended overwrites.
 *
 * To update multiple rules at once, use the `bulkUpdateRules()` function.
 * @param _id - Rule ID.
 * @public
 * @documentationMaturity preview
 * @requiredField _id
 * @requiredField rule
 * @requiredField rule.revision
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_UPDATE
 * @applicableIdentity APP
 * @returns Updated rule.
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.UpdateRule
 */
declare function updateRule(_id: string, rule: NonNullablePaths<UpdateRule, `revision`, 2>): Promise<NonNullablePaths<Rule, `amount.value` | `amount.currency` | `fixedFee.value` | `fixedFee.currency` | `condition.number.value` | `condition.number.operation` | `condition.list.values` | `condition.path` | `condition.orderFieldPath` | `condition.expectedType.value` | `condition.expectedFieldType` | `conditionTree.operator` | `conditionsType` | `conditionType` | `roundingStrategy` | `tags.privateTags.tagIds`, 4>>;
interface UpdateRule {
    /**
     * Fixed fee. Must hold a positive value.
     * @deprecated Fixed fee. Must hold a positive value.
     * @replacedBy fixed_fee
     * @targetRemovalDate 2024-02-15
     */
    amount?: CommonMoney;
    /**
     * Percentage fee. For example, `5` represents a 5% fee applied to the order's total.
     * @decimalValue options { maxScale:2, gte:0, lte:100 }
     * @deprecated
     * @replacedBy percentage_fee
     * @targetRemovalDate 2024-02-15
     */
    percentage?: string | null;
    /** Fixed fee. Must hold a positive value. */
    fixedFee?: Money;
    /**
     * Percentage fee. For example, 5 represents a 5% fee applied to the order's total.
     *
     * Min: `0`.
     *
     * Max: `100`.
     * @decimalValue options { maxScale:2, gte:0, lte:100 }
     */
    percentageFee?: string | null;
    /**
     * Single condition that must be met for the rule to be applied to an order.
     * @deprecated Single condition that must be met for the rule to be applied to an order.
     * @replacedBy condition_options
     * @targetRemovalDate 2024-02-15
     */
    condition?: Condition;
    /**
     * Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order.
     * @deprecated Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order.
     * @replacedBy condition_tree_options
     * @targetRemovalDate 2024-02-15
     */
    conditionTree?: ConditionTree;
    /** Single condition that must be met for the rule to be applied to an order. */
    conditionOptions?: Condition;
    /** Binary tree of conditions. Use the operator to indicate whether only one or both conditions must be met in order for a service fee rule to be applied to an order. */
    conditionTreeOptions?: ConditionTree;
    /** Percentage value to apply as a custom tax rate. Range: [0-100]. */
    customTaxRate?: string | null;
    /**
     * Tax group ID. Internal only.
     * @format GUID
     */
    taxGroupId?: string | null;
    /**
     * Rule ID.
     * @readonly
     */
    _id?: string | null;
    /**
     * ID of the restaurant’s [location](/locations/introduction).
     * @format GUID
     */
    locationId?: string | null;
    /**
     * Rule name.
     * @maxLength 50
     * @minLength 1
     */
    name?: string | null;
    /**
     * Date and time the rule was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the rule was updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Percentage value to apply as a custom tax rate. For example, `5` represents a 5% fee applied to the order's total. <br />
     * Min: '0'. <br />
     * Max: `100`.
     * @decimalValue options { maxScale:2, gte:0, lte:100 }
     * @deprecated
     * @replacedBy taxes
     * @targetRemovalDate 2023-09-16
     */
    taxRate?: string | null;
    /**
     * Specifies the type of condition.
     * @deprecated Specifies the type of condition.
     * @replacedBy condition_type
     * @targetRemovalDate 2024-02-15
     */
    conditionsType?: ConditionsTypeWithLiterals;
    /** Specifies the type of condition. */
    conditionType?: ConditionTypeWithLiterals;
    /** Whether the rule is enabled. If `true`, the rule is evaluated when service fees are calculated. If `false`, the rule will not be evaluated when service fees are calculated. */
    enabled?: boolean | null;
    /**
     * Revision number. Increments by 1 each time the rule is updated.
     * To prevent conflicting changes, the existing `revision` must be used when updating a rule.
     * @readonly
     */
    revision?: string | null;
    /**
     * DEPRECATED. Defines the app that the rule is connected to.
     * @maxLength 100
     * @minLength 1
     * @deprecated DEPRECATED. Defines the app that the rule is connected to.
     * @replacedBy app_id
     * @targetRemovalDate 2023-02-19
     */
    label?: string | null;
    /**
     * Defines the app that the rule is connected to.
     * @format GUID
     */
    appId?: string | null;
    /**
     * Rounding strategy to apply to fee and tax calculation.
     *
     * Supported values:
     *
     * - `"HALF_UP"`: Rounds up any number exactly halfway between two integers. For example, `2.5` rounds to `3`, and `3.5` and rounds to `4`.
     * - `"HALF_EVEN"`: Rounds such numbers to the nearest even integer. For example, `2.5` rounds to `2`, but `3.5` rounds to `4`.
     * - `"UNKNOWN_ROUNDING_STRATEGY"`
     */
    roundingStrategy?: RoundingStrategyWithLiterals;
    /** Data Extensions */
    extendedFields?: ExtendedFields;
    /** Tags ([SDK](https://dev.wix.com/docs/sdk/backend-modules/tags/tags/introduction) | [REST](https://dev.wix.com/docs/rest/business-management/tags/introduction)) used to classify and sort different types of rules. */
    tags?: Tags;
}
/**
 * The `deleteRule()` function returns a Promise that resolves to void.
 *
 *
 * To delete multiple rules at once, use the `bulkDeleteRules()` function.
 * @param ruleId - ID of the rule to delete.
 * @public
 * @documentationMaturity preview
 * @requiredField ruleId
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_DELETE
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.DeleteRule
 */
declare function deleteRule(ruleId: string): Promise<void>;
/**
 * The `listRules()` function returns a Promise that resolves to an array of the retrieved rules.
 *
 *
 * You can filter by location or app that the rules are associated with.
 * @public
 * @documentationMaturity preview
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_READ
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.ListRules
 */
declare function listRules(options?: ListRulesOptions): Promise<NonNullablePaths<ListRulesResponse, `rules` | `rules.${number}.amount.value` | `rules.${number}.amount.currency` | `rules.${number}.fixedFee.value` | `rules.${number}.fixedFee.currency` | `rules.${number}.condition.number.value` | `rules.${number}.condition.number.operation` | `rules.${number}.condition.path` | `rules.${number}.condition.orderFieldPath` | `rules.${number}.condition.expectedType.value` | `rules.${number}.condition.expectedFieldType` | `rules.${number}.conditionTree.operator` | `rules.${number}.conditionsType` | `rules.${number}.conditionType` | `rules.${number}.roundingStrategy`, 6>>;
interface ListRulesOptions {
    /**
     * Retrieve only rule that apply at the [location](https://dev.wix.com/docs/rest/api-reference/business-tools/locations/location-object) with this ID. If this field is `null`, the rules will not be filtered by location.
     * @format GUID
     */
    locationId?: string | null;
    /**
     * DEPRECATED. Defines the app that the rule is connected to.
     * @maxLength 100
     * @minLength 1
     * @deprecated DEPRECATED. Defines the app that the rule is connected to.
     * @replacedBy app_id
     * @targetRemovalDate 2023-02-19
     */
    label?: string | null;
    /**
     * Defines the app that the rule is connected to.
     * @format GUID
     */
    appId?: string | null;
}
/**
 * Creates a query to retrieve a list of rules.
 *
 *
 * The `queryRules()` function builds a query to retrieve a list of up to 1,000 rules and returns a `RulesQueryBuilder` object.
 *
 * The returned object contains the query definition which is typically used to run the query using the [`find()`](/service-fees/rules-query-builder/find) function.
 *
 * You can refine the query by chaining `RulesQueryBuilder` functions onto the query. `RulesQueryBuilder` functions enable you to sort, filter, and control the results that `queryRules()` returns. The functions that are chained to `queryRules()` are applied in the order they are called.
 *
 * `queryRules()` runs with the following `RulesQueryBuilder` defaults that you can override:
 *
 * * [`limit(50)`](/service-fees/rules-query-builder/limit)
 * * [`ascending('entityId')`](/service-fees/rules-query-builder/ascending)
 *
 * The following `QueryRulesBuilder` functions are supported for the `queryRules()` function. For a full description of the Rules object, see the object returned for the [`items`](/service-fees/rules-query-result/items) property in `RulesQueryResult`.
 * @public
 * @documentationMaturity preview
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_READ
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.QueryRules
 */
declare function queryRules(): RulesQueryBuilder;
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface RulesQueryResult extends QueryCursorResult {
    items: Rule[];
    query: RulesQueryBuilder;
    next: () => Promise<RulesQueryResult>;
    prev: () => Promise<RulesQueryResult>;
}
interface RulesQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    eq: (propertyName: '_id' | 'locationId' | 'appId', value: any) => RulesQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ne: (propertyName: '_id' | 'locationId' | 'appId', value: any) => RulesQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ge: (propertyName: '_id' | 'locationId' | 'appId', value: any) => RulesQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    gt: (propertyName: '_id' | 'locationId' | 'appId', value: any) => RulesQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    le: (propertyName: '_id' | 'locationId' | 'appId', value: any) => RulesQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    lt: (propertyName: '_id' | 'locationId' | 'appId', value: any) => RulesQueryBuilder;
    /** @param propertyName - Property whose value is compared with `string`.
     * @param string - String to compare against. Case-insensitive.
     * @documentationMaturity preview
     */
    startsWith: (propertyName: '_id' | 'locationId' | 'appId', value: string) => RulesQueryBuilder;
    /** @param propertyName - Property whose value is compared with `values`.
     * @param values - List of values to compare against.
     * @documentationMaturity preview
     */
    hasSome: (propertyName: '_id' | 'locationId' | 'appId', value: any[]) => RulesQueryBuilder;
    /** @documentationMaturity preview */
    in: (propertyName: '_id' | 'locationId' | 'appId', value: any) => RulesQueryBuilder;
    /** @documentationMaturity preview */
    exists: (propertyName: '_id' | 'locationId' | 'appId', value: boolean) => RulesQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    ascending: (...propertyNames: Array<'_id' | 'locationId' | 'appId'>) => RulesQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    descending: (...propertyNames: Array<'_id' | 'locationId' | 'appId'>) => RulesQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object.
     * @documentationMaturity preview
     */
    limit: (limit: number) => RulesQueryBuilder;
    /** @param cursor - A pointer to specific record
     * @documentationMaturity preview
     */
    skipTo: (cursor: string) => RulesQueryBuilder;
    /** @documentationMaturity preview */
    find: () => Promise<RulesQueryResult>;
}
/**
 * The `bulkCreateRules()` function returns a Promise that resolves to the created rules.
 *
 *
 * To create only one rule, use the `createRule()` function.
 * @param rules - Rules to create.
 * @public
 * @documentationMaturity preview
 * @requiredField rules
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_CREATE
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.BulkCreateRules
 */
declare function bulkCreateRules(rules: Rule[], options?: BulkCreateRulesOptions): Promise<NonNullablePaths<BulkCreateRulesResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.rule.amount.value` | `results.${number}.rule.amount.currency` | `results.${number}.rule.fixedFee.value` | `results.${number}.rule.fixedFee.currency` | `results.${number}.rule.condition.path` | `results.${number}.rule.condition.orderFieldPath` | `results.${number}.rule.condition.expectedFieldType` | `results.${number}.rule.conditionTree.operator` | `results.${number}.rule.conditionsType` | `results.${number}.rule.conditionType` | `results.${number}.rule.roundingStrategy` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkCreateRulesOptions {
    /**
     * Whether the full rule entity is returned. <br />
     * Default: `false`.
     */
    returnFullEntity?: boolean;
}
/**
 * The `bulkUpdateRules()` function returns a Promise that resolves to the updated rules.
 *
 *
 * Each time the task is updated, `revision` increments by 1. The existing `revision` must be included when updating the task. This ensures you're working with the latest task and prevents unintended overwrites.
 *
 * To update only one rule, use the `updateRule()` function.
 * @param rules - Masked rules to update.
 * @public
 * @documentationMaturity preview
 * @requiredField rules
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_UPDATE
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.BulkUpdateRules
 */
declare function bulkUpdateRules(rules: MaskedRule[], options?: BulkUpdateRulesOptions): Promise<NonNullablePaths<BulkUpdateRulesResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.rule.amount.value` | `results.${number}.rule.amount.currency` | `results.${number}.rule.fixedFee.value` | `results.${number}.rule.fixedFee.currency` | `results.${number}.rule.condition.path` | `results.${number}.rule.condition.orderFieldPath` | `results.${number}.rule.condition.expectedFieldType` | `results.${number}.rule.conditionTree.operator` | `results.${number}.rule.conditionsType` | `results.${number}.rule.conditionType` | `results.${number}.rule.roundingStrategy` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
interface BulkUpdateRulesOptions {
    /**
     * Whether the full rule entity is returned. <br />
     * Default: `false`.
     */
    returnFullEntity?: boolean;
}
/**
 * The `bulkDeleteRules()` function returns a Promise that resolves to the deleted rules.
 *
 *
 * To delete only one rule, use the `deleteRule()` function.
 * @param ruleIds - IDs of the rules to delete.
 * @public
 * @documentationMaturity preview
 * @requiredField ruleIds
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_DELETE
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.BulkDeleteRules
 */
declare function bulkDeleteRules(ruleIds: string[]): Promise<NonNullablePaths<BulkDeleteRulesResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.rule.amount.value` | `results.${number}.rule.amount.currency` | `results.${number}.rule.fixedFee.value` | `results.${number}.rule.fixedFee.currency` | `results.${number}.rule.condition.path` | `results.${number}.rule.condition.orderFieldPath` | `results.${number}.rule.condition.expectedFieldType` | `results.${number}.rule.conditionTree.operator` | `results.${number}.rule.conditionsType` | `results.${number}.rule.conditionType` | `results.${number}.rule.roundingStrategy` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6>>;
/**
 * Synchronously update tags on multiple rules.
 * If you specify a tag in both `assignTags` and `unassignTags`, it is assigned.
 * @param ruleIds - IDs of the rules to update tags for.
 * @public
 * @documentationMaturity preview
 * @requiredField ruleIds
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_UPDATE_TAGS
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.BulkUpdateRuleTags
 */
declare function bulkUpdateRuleTags(ruleIds: string[], options?: BulkUpdateRuleTagsOptions): Promise<NonNullablePaths<BulkUpdateRuleTagsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 6> & {
    __applicationErrorsType?: BulkUpdateRuleTagsApplicationErrors;
}>;
interface BulkUpdateRuleTagsOptions {
    /** Tags to assign to the rules. */
    assignTags?: Tags;
    /** Tags to unassign from the rules. */
    unassignTags?: Tags;
}
/**
 * Asynchronously update tags on multiple rules according to the specified filter.
 * If a filter isn't specified, this method updates all rules.
 * If you specify a tag in both `assignTags` and `unassignTags`, it is assigned.
 * @param filter - Filter that determines which rules to update tags for.
 * @public
 * @documentationMaturity preview
 * @requiredField filter
 * @permissionId WIX_RESTAURANTS.SERVICE_FEES_RULES_UPDATE_TAGS
 * @applicableIdentity APP
 * @fqn com.wixpress.service.fees.v1.ServiceFeesRules.BulkUpdateRuleTagsByFilter
 */
declare function bulkUpdateRuleTagsByFilter(filter: Record<string, any>, options?: BulkUpdateRuleTagsByFilterOptions): Promise<NonNullablePaths<BulkUpdateRuleTagsByFilterResponse, `jobId`, 2> & {
    __applicationErrorsType?: BulkUpdateRuleTagsByFilterApplicationErrors;
}>;
interface BulkUpdateRuleTagsByFilterOptions {
    /** Tags to assign to the rules. */
    assignTags?: Tags;
    /** Tags to unassign from the rules. */
    unassignTags?: Tags;
}

export { type ActionEvent, type ApplicationError, type BaseEventMetadata, type BulkActionMetadata, type BulkCreateRulesOptions, type BulkCreateRulesRequest, type BulkCreateRulesResponse, type BulkDeleteRulesRequest, type BulkDeleteRulesResponse, type BulkRuleResult, type BulkUpdateRuleTagsApplicationErrors, type BulkUpdateRuleTagsByFilterApplicationErrors, type BulkUpdateRuleTagsByFilterOptions, type BulkUpdateRuleTagsByFilterRequest, type BulkUpdateRuleTagsByFilterResponse, type BulkUpdateRuleTagsOptions, type BulkUpdateRuleTagsRequest, type BulkUpdateRuleTagsResponse, type BulkUpdateRuleTagsResult, type BulkUpdateRulesOptions, type BulkUpdateRulesRequest, type BulkUpdateRulesResponse, type CalculateServiceFeesOptions, type CalculateServiceFeesRequest, type CalculateServiceFeesResponse, type CalculatedFee, type CommonBulkActionMetadata, type CommonItemMetadata, type CommonMoney, type Condition, type ConditionTree, type ConditionTreeLeftConditionNodeOneOf, type ConditionTreeRightConditionNodeOneOf, ConditionType, type ConditionTypeWithLiterals, type ConditionValueOneOf, ConditionsType, type ConditionsTypeWithLiterals, type CreateRuleRequest, type CreateRuleResponse, type CursorPaging, type CursorPagingMetadata, type CursorQuery, type CursorQueryPagingMethodOneOf, type Cursors, type DeleteRuleRequest, type DeleteRuleResponse, type DeliveryLogistics, type DomainEvent, type DomainEventBodyOneOf, type Empty, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type EventMetadata, ExpectedFieldType, type ExpectedFieldTypeWithLiterals, type ExpectedType, type ExtendedFields, type GetRuleRequest, type GetRuleResponse, type IdentificationData, type IdentificationDataIdOneOf, type ItemMetadata, type List, type ListRulesOptions, type ListRulesRequest, type ListRulesResponse, type Locale, type MaskedRule, type MessageEnvelope, type Money, Operation, type OperationWithLiterals, Operator, type OperatorWithLiterals, type Order, type Platform, PlatformValue, type PlatformValueWithLiterals, type PriceSummary, type QueryRulesRequest, type QueryRulesResponse, type RestoreInfo, RoundingStrategy, type RoundingStrategyWithLiterals, type Rule, type RuleConditionTypeOptionsOneOf, type RuleConditionsOneOf, type RuleCreatedEnvelope, type RuleDeletedEnvelope, type RuleRequirementsOneOf, type RuleTaxesOneOf, type RuleUpdatedEnvelope, type RuleValueOneOf, type RulesQueryBuilder, type RulesQueryResult, type ShippingInformation, SortOrder, type SortOrderWithLiterals, type Sorting, type TagList, type Tags, Type, type TypeWithLiterals, type UpdateRule, type UpdateRuleRequest, type UpdateRuleResponse, Value, type ValueWithLiterals, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, type _Number, bulkCreateRules, bulkDeleteRules, bulkUpdateRuleTags, bulkUpdateRuleTagsByFilter, bulkUpdateRules, calculateServiceFees, createRule, deleteRule, getRule, listRules, onRuleCreated, onRuleDeleted, onRuleUpdated, queryRules, updateRule };
