import { Node_Type } from "./../../ricos-schema/legacy/wix/rich_content/v1/index.js";
const caption = (...args) => {
  const captionData = args.find((a) => "textStyle" in a);
  const style = args.find((a) => "paddingTop" in a || "paddingBottom" in a);
  const nodeBuilders = args.filter(
    (a) => "type" in a && a.type === "text_node"
  );
  return {
    type: "block_node",
    // @ts-expect-error
    node: (getId) => ({
      id: getId(),
      type: Node_Type.CAPTION,
      ...nodeBuilders.length && {
        nodes: nodeBuilders.map((nb) => nb.node())
      },
      ...captionData && { captionData },
      ...style && { style }
    })
  };
};
export {
  caption
};
//# sourceMappingURL=caption.builder.js.map
