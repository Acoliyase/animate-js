import { Node_Type } from "./../../ricos-schema/legacy/wix/rich_content/v1/index.js";
const cellConverter = {
  toPm: {
    types: [Node_Type.LAYOUT_CELL],
    // @ts-expect-error $TSFixMe
    convert: (node, visit) => {
      return {
        type: Node_Type.LAYOUT_CELL,
        attrs: {
          colSpan: node.layoutCellData.colSpan,
          id: node.id
        },
        content: visit(node)
      };
    }
  },
  fromPm: {
    types: [Node_Type.LAYOUT_CELL],
    // @ts-expect-error $TSFixMe
    convert: (node, visit) => {
      const { attrs = {} } = node;
      const { id, colSpan } = attrs;
      return {
        type: Node_Type.LAYOUT_CELL,
        id,
        nodes: visit(node),
        layoutCellData: {
          colSpan
        }
      };
    }
  }
};
export {
  cellConverter
};
//# sourceMappingURL=cell-converter.js.map
