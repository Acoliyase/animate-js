import { dataHook } from "./../../wix-rich-content-common/index.js";
import { TEXT_BUTTON_BOLD } from "./data-hooks.js";
const createBoldTestkit = () => {
  return {
    content: {},
    toolbars: {
      createTextButtonToolbars: (getTextButtonToolbarBase) => {
        const getButton = () => getTextButtonToolbarBase().$(dataHook(TEXT_BUTTON_BOLD));
        return {
          click: () => getButton().click(),
          getElement: () => getTextButtonToolbarBase().get(dataHook(TEXT_BUTTON_BOLD)),
          focus: async () => {
            const button = await getButton().unwrap();
            return button.focus();
          }
        };
      }
    }
  };
};
export {
  createBoldTestkit
};
//# sourceMappingURL=bold.testkit.js.map
