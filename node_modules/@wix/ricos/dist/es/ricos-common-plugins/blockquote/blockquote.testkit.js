import { dataHook } from "./../../wix-rich-content-common/index.js";
import { TEXT_BUTTON_BLOCKQUOTE } from "./data-hooks.js";
const createBlockquoteTestkit = () => {
  return {
    content: {},
    toolbars: {
      createTextButtonToolbars: (getTextButtonBase) => ({
        isActive: async () => {
          const active = getTextButtonBase().$(dataHook([TEXT_BUTTON_BLOCKQUOTE, "active"]));
          return active.exists();
        },
        isDisabled: async () => {
          const disabled = getTextButtonBase().$(dataHook([TEXT_BUTTON_BLOCKQUOTE, "disabled"]));
          return disabled.exists();
        },
        click: async () => {
          const button = getTextButtonBase().$(dataHook(TEXT_BUTTON_BLOCKQUOTE));
          await button.click();
        }
      })
    }
  };
};
export {
  createBlockquoteTestkit
};
//# sourceMappingURL=blockquote.testkit.js.map
