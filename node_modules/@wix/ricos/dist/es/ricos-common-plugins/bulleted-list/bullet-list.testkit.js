import { dataHook } from "./../../wix-rich-content-common/index.js";
import { TEXT_BUTTON_BULLETED_LIST } from "./data-hooks.js";
const createBulletListTestkit = (getBase) => {
  return {
    content: {
      atIndex: (index) => {
        return {
          getStyles: async () => {
            const el = (await getBase().getAll("li")).at(index);
            const style = await el._prop("style");
            return style;
          }
        };
      }
    },
    toolbars: {
      createTextButtonToolbars: (getTextButtonBase) => ({
        click: async () => {
          (await getTextButtonBase().get(dataHook(TEXT_BUTTON_BULLETED_LIST))).click();
        }
      })
    }
  };
};
export {
  createBulletListTestkit
};
//# sourceMappingURL=bullet-list.testkit.js.map
