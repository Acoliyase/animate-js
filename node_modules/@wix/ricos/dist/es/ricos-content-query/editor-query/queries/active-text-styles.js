const NAME = "activeTextStyles";
const activeTextStyles = ({ editorQuery }) => (textStyleName) => {
  const selectedNodesTextStyles = editorQuery.selection().doc.descendants((node) => node.isTextBlock()).map((node) => {
    return editorQuery.styles().getComputedTextStyle(node, textStyleName);
  });
  const activeTextStyles2 = selectedNodesTextStyles.filter(isDefined);
  return activeTextStyles2;
};
const isDefined = (x) => x !== void 0 && x !== null;
var active_text_styles_default = { NAME, query: activeTextStyles };
export {
  active_text_styles_default as default
};
//# sourceMappingURL=active-text-styles.js.map
