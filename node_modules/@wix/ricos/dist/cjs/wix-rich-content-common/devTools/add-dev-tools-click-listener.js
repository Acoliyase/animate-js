"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var add_dev_tools_click_listener_exports = {};
__export(add_dev_tools_click_listener_exports, {
  addDevToolsClickListener: () => addDevToolsClickListener
});
module.exports = __toCommonJS(add_dev_tools_click_listener_exports);
const addDevToolsClickListener = (ref, enableDevTools, editor) => {
  let clickListener = null;
  const keyDownHandler = (e) => {
    if (e.metaKey && e.shiftKey && e.altKey && !clickListener) {
      clickListener = (e2) => {
        if (e2.detail === 2) {
          enableDevTools();
          if (editor && typeof window !== "undefined") {
            Promise.resolve().then(() => __toESM(require(
              /* webpackChunkName: "ricos_prosemirror-dev-tools" */
              "prosemirror-dev-tools"
            ))).then(
              ({ applyDevTools }) => {
                applyDevTools(editor.view, {});
              },
              (e3) => {
                console.error("Failed to load prosemirror-dev-tools", e3);
              }
            );
          }
        }
      };
      ref?.addEventListener("click", clickListener, { capture: true });
    }
  };
  const keyUpHandler = (e) => {
    if (!e.metaKey || !e.shiftKey || !e.altKey) {
      if (clickListener) {
        ref?.removeEventListener("click", clickListener);
        clickListener = null;
      }
    }
  };
  document.addEventListener("keydown", keyDownHandler);
  document.addEventListener("keyup", keyUpHandler);
  return () => {
    document.removeEventListener("keydown", keyDownHandler);
    document.removeEventListener("keyup", keyUpHandler);
    if (clickListener) {
      ref?.removeEventListener("click", clickListener);
    }
  };
};
//# sourceMappingURL=add-dev-tools-click-listener.js.map
