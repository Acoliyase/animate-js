"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var autofix_api_exports = {};
__export(autofix_api_exports, {
  fixDocument: () => fixDocument
});
module.exports = __toCommonJS(autofix_api_exports);
var import_normalize_ricos_document = require("./../../../../ricos-content/normalize-ricos-document.js");
const fixDocument = async (content, plugins) => {
  const response = await fetch(
    `https://apps.wix.com/_serverless/ricos-playground-services/fix-document`,
    {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({ content, enabledPlugins: plugins })
    }
  );
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  const result = await response.json();
  return (0, import_normalize_ricos_document.normalizeRicosDocument)(result.content);
};
//# sourceMappingURL=autofix-api.js.map
