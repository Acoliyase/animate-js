"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var cellEditingDecorations_exports = {};
__export(cellEditingDecorations_exports, {
  cellEditingDecorations: () => cellEditingDecorations
});
module.exports = __toCommonJS(cellEditingDecorations_exports);
var import_view = require("@tiptap/pm/view");
var import_table_scss = __toESM(require("../plugins/table-scss.js"));
const cellEditingDecorations = ($cell) => {
  const pos = $cell.pos + 1;
  const backgroundColor = $cell.nodeAfter.attrs.cellStyle?.backgroundColor || "white";
  const decorations = [];
  const editCellRight = document.createElement("div");
  editCellRight.classList.add(import_table_scss.default.editCellR);
  editCellRight.style.backgroundColor = backgroundColor;
  decorations.push(import_view.Decoration.widget(pos, editCellRight));
  const editCellLeft = document.createElement("div");
  editCellLeft.classList.add(import_table_scss.default.editCellL);
  editCellLeft.style.backgroundColor = backgroundColor;
  decorations.push(import_view.Decoration.widget(pos, editCellLeft));
  const editCellBottom = document.createElement("div");
  editCellBottom.classList.add(import_table_scss.default.editCellB);
  decorations.push(import_view.Decoration.widget(pos, editCellBottom));
  const editCellTop = document.createElement("div");
  editCellTop.classList.add(import_table_scss.default.editCellT);
  decorations.push(import_view.Decoration.widget(pos, editCellTop));
  return decorations;
};
//# sourceMappingURL=cellEditingDecorations.js.map
