"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var builder_old_exports = {};
__export(builder_old_exports, {
  createBuilder: () => createBuilder
});
module.exports = __toCommonJS(builder_old_exports);
var import_function = require("fp-ts-esm/function");
var import_lodash = __toESM(require("lodash"));
var import_generateRandomId = require("../converters/generateRandomId.js");
var import_RicosContentBuilder = require("../RicosContentAPI/RicosContentBuilder.js");
var import_consts = require("./consts.js");
var import_addImage = require("./nativeElements/addImage.js");
const extractBuilderMethods = ({ RicosContentBuilder: _, ...rest }) => rest;
const notify = (content, callback) => {
  callback?.(content);
  return createBuilder(content, callback);
};
const applyBuilderPattern = (content, callback) => (methods) => Object.entries(methods).reduce(
  (prev, [key, func]) => ({
    ...prev,
    [key]: import_lodash.default.flow(
      (0, import_consts.applyContent)(content),
      func,
      (newContent) => notify(newContent, callback)
    )
  }),
  {}
);
const createBuilder = (content, callback) => {
  const originalMethods = (0, import_function.pipe)(import_generateRandomId.generateId, import_RicosContentBuilder.setupContentBuilder, extractBuilderMethods);
  const adapterMethods = (0, import_function.pipe)(originalMethods, applyBuilderPattern(content, callback));
  return {
    ...adapterMethods,
    addImage: import_lodash.default.flow(
      import_addImage.addImage,
      (0, import_consts.applyContent)(content),
      originalMethods.addImage,
      (newContent) => notify(newContent, callback)
    ),
    get: () => content
  };
};
//# sourceMappingURL=builder-old.js.map
