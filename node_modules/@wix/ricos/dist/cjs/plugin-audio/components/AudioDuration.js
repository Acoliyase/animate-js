"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var AudioDuration_exports = {};
__export(AudioDuration_exports, {
  default: () => AudioDuration_default
});
module.exports = __toCommonJS(AudioDuration_exports);
var import_classnames = __toESM(require("classnames"));
var import_react = __toESM(require("react"));
var import_audio_scss = __toESM(require("../statics/styles/audio-scss.js"));
const Duration = ({ seconds }) => {
  return /* @__PURE__ */ import_react.default.createElement("time", { dateTime: `P${Math.round(seconds)}S` }, format(seconds));
};
function format(seconds) {
  const date = new Date(seconds * 1e3);
  const hh = date.getUTCHours();
  const mm = date.getUTCMinutes();
  const ss = pad(date.getUTCSeconds());
  if (hh) {
    return `${hh}:${pad(mm)}:${ss}`;
  }
  return `${mm}:${ss}`;
}
function pad(string) {
  return ("0" + string).slice(-2);
}
const AudioDuration = ({ played, duration, isLoading }) => {
  return /* @__PURE__ */ import_react.default.createElement("div", { className: (0, import_classnames.default)(import_audio_scss.default.audioDuration, { [import_audio_scss.default.isLoading]: isLoading }) }, isLoading ? "--:--" : /* @__PURE__ */ import_react.default.createElement(Duration, { seconds: played > 0 ? played * duration : duration }));
};
var AudioDuration_default = AudioDuration;
//# sourceMappingURL=AudioDuration.js.map
