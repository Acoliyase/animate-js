import { runAllSuites } from '@wix/unidriver-common/testing';
import { setup } from './setup';
import { makePlaywrightCompatible } from './makePlaywrightCompatible';
import { test } from '@playwright/test';
import { playwrightUniDriver } from '../playwrightUniDriver';
makePlaywrightCompatible();
describe('Common Test Suite', () => {
    runAllSuites({ setup, isStorybook: true });
});
test('Playwright types compatibility test', async ({ page }) => {
    playwrightUniDriver({
        root: page,
        selector: 'body',
    });
    playwrightUniDriver({
        root: page.locator('body'),
        selector: 'div',
    });
});
//# sourceMappingURL=tests.spec.js.map