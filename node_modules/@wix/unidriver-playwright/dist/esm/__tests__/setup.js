import { TestApp } from '@wix/unidriver-common/testing';
import { playwrightUniDriver } from '../playwrightUniDriver';
import * as stories from '../stories/TestApp.stories';
import React from 'react';
export const setup = async (params, ctx) => {
    const args = Object.values(stories).find((meta) => {
        return meta.name === params.story;
    })?.args;
    const component = await ctx?.mount(React.createElement(TestApp, { ...args }));
    if (!component) {
        throw new Error('Failed to mount component');
    }
    await component.locator('.todo-app').isVisible();
    const driver = playwrightUniDriver(component);
    const tearDown = () => {
        return undefined;
    };
    return { driver, tearDown };
};
//# sourceMappingURL=setup.js.map