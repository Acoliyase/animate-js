"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.enterText = void 0;
const enterText = async (base, value, options) => {
    const { disabled, readonly } = await base.evaluate((_base) => {
        return {
            disabled: _base.disabled,
            readonly: _base.readOnly,
        };
    });
    const { clear, delay } = options;
    if (disabled || readonly) {
        return;
    }
    if (clear) {
        await base.fill('');
        await base.pressSequentially(value, { delay });
    }
    else {
        const originalValue = await base.evaluate((_base) => {
            return (_base.value ?? '');
        });
        await base.fill('');
        await base.pressSequentially(originalValue + value, { delay });
    }
};
exports.enterText = enterText;
//# sourceMappingURL=enterText.js.map