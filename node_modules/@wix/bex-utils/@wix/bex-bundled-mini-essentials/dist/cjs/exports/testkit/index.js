"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// ../../node_modules/lodash/lodash.js
var require_lodash = __commonJS({
  "../../node_modules/lodash/lodash.js"(exports2, module2) {
    "use strict";
    (function() {
      var undefined2;
      var VERSION = "4.17.21";
      var LARGE_ARRAY_SIZE = 200;
      var CORE_ERROR_TEXT = "Unsupported core-js use. Try https://npms.io/search?q=ponyfill.", FUNC_ERROR_TEXT = "Expected a function", INVALID_TEMPL_VAR_ERROR_TEXT = "Invalid `variable` option passed into `_.template`";
      var HASH_UNDEFINED = "__lodash_hash_undefined__";
      var MAX_MEMOIZE_SIZE = 500;
      var PLACEHOLDER = "__lodash_placeholder__";
      var CLONE_DEEP_FLAG = 1, CLONE_FLAT_FLAG = 2, CLONE_SYMBOLS_FLAG = 4;
      var COMPARE_PARTIAL_FLAG = 1, COMPARE_UNORDERED_FLAG = 2;
      var WRAP_BIND_FLAG = 1, WRAP_BIND_KEY_FLAG = 2, WRAP_CURRY_BOUND_FLAG = 4, WRAP_CURRY_FLAG = 8, WRAP_CURRY_RIGHT_FLAG = 16, WRAP_PARTIAL_FLAG = 32, WRAP_PARTIAL_RIGHT_FLAG = 64, WRAP_ARY_FLAG = 128, WRAP_REARG_FLAG = 256, WRAP_FLIP_FLAG = 512;
      var DEFAULT_TRUNC_LENGTH = 30, DEFAULT_TRUNC_OMISSION = "...";
      var HOT_COUNT = 800, HOT_SPAN = 16;
      var LAZY_FILTER_FLAG = 1, LAZY_MAP_FLAG = 2, LAZY_WHILE_FLAG = 3;
      var INFINITY = 1 / 0, MAX_SAFE_INTEGER = 9007199254740991, MAX_INTEGER = 17976931348623157e292, NAN = 0 / 0;
      var MAX_ARRAY_LENGTH = 4294967295, MAX_ARRAY_INDEX = MAX_ARRAY_LENGTH - 1, HALF_MAX_ARRAY_LENGTH = MAX_ARRAY_LENGTH >>> 1;
      var wrapFlags = [
        ["ary", WRAP_ARY_FLAG],
        ["bind", WRAP_BIND_FLAG],
        ["bindKey", WRAP_BIND_KEY_FLAG],
        ["curry", WRAP_CURRY_FLAG],
        ["curryRight", WRAP_CURRY_RIGHT_FLAG],
        ["flip", WRAP_FLIP_FLAG],
        ["partial", WRAP_PARTIAL_FLAG],
        ["partialRight", WRAP_PARTIAL_RIGHT_FLAG],
        ["rearg", WRAP_REARG_FLAG]
      ];
      var argsTag = "[object Arguments]", arrayTag = "[object Array]", asyncTag = "[object AsyncFunction]", boolTag = "[object Boolean]", dateTag = "[object Date]", domExcTag = "[object DOMException]", errorTag = "[object Error]", funcTag = "[object Function]", genTag = "[object GeneratorFunction]", mapTag = "[object Map]", numberTag = "[object Number]", nullTag = "[object Null]", objectTag = "[object Object]", promiseTag = "[object Promise]", proxyTag = "[object Proxy]", regexpTag = "[object RegExp]", setTag = "[object Set]", stringTag = "[object String]", symbolTag = "[object Symbol]", undefinedTag = "[object Undefined]", weakMapTag = "[object WeakMap]", weakSetTag = "[object WeakSet]";
      var arrayBufferTag = "[object ArrayBuffer]", dataViewTag = "[object DataView]", float32Tag = "[object Float32Array]", float64Tag = "[object Float64Array]", int8Tag = "[object Int8Array]", int16Tag = "[object Int16Array]", int32Tag = "[object Int32Array]", uint8Tag = "[object Uint8Array]", uint8ClampedTag = "[object Uint8ClampedArray]", uint16Tag = "[object Uint16Array]", uint32Tag = "[object Uint32Array]";
      var reEmptyStringLeading = /\b__p \+= '';/g, reEmptyStringMiddle = /\b(__p \+=) '' \+/g, reEmptyStringTrailing = /(__e\(.*?\)|\b__t\)) \+\n'';/g;
      var reEscapedHtml = /&(?:amp|lt|gt|quot|#39);/g, reUnescapedHtml = /[&<>"']/g, reHasEscapedHtml = RegExp(reEscapedHtml.source), reHasUnescapedHtml = RegExp(reUnescapedHtml.source);
      var reEscape = /<%-([\s\S]+?)%>/g, reEvaluate = /<%([\s\S]+?)%>/g, reInterpolate = /<%=([\s\S]+?)%>/g;
      var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, reIsPlainProp = /^\w*$/, rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
      var reRegExpChar = /[\\^$.*+?()[\]{}|]/g, reHasRegExpChar = RegExp(reRegExpChar.source);
      var reTrimStart = /^\s+/;
      var reWhitespace = /\s/;
      var reWrapComment = /\{(?:\n\/\* \[wrapped with .+\] \*\/)?\n?/, reWrapDetails = /\{\n\/\* \[wrapped with (.+)\] \*/, reSplitDetails = /,? & /;
      var reAsciiWord = /[^\x00-\x2f\x3a-\x40\x5b-\x60\x7b-\x7f]+/g;
      var reForbiddenIdentifierChars = /[()=,{}\[\]\/\s]/;
      var reEscapeChar = /\\(\\)?/g;
      var reEsTemplate = /\$\{([^\\}]*(?:\\.[^\\}]*)*)\}/g;
      var reFlags = /\w*$/;
      var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
      var reIsBinary = /^0b[01]+$/i;
      var reIsHostCtor = /^\[object .+?Constructor\]$/;
      var reIsOctal = /^0o[0-7]+$/i;
      var reIsUint = /^(?:0|[1-9]\d*)$/;
      var reLatin = /[\xc0-\xd6\xd8-\xf6\xf8-\xff\u0100-\u017f]/g;
      var reNoMatch = /($^)/;
      var reUnescapedString = /['\n\r\u2028\u2029\\]/g;
      var rsAstralRange = "\\ud800-\\udfff", rsComboMarksRange = "\\u0300-\\u036f", reComboHalfMarksRange = "\\ufe20-\\ufe2f", rsComboSymbolsRange = "\\u20d0-\\u20ff", rsComboRange = rsComboMarksRange + reComboHalfMarksRange + rsComboSymbolsRange, rsDingbatRange = "\\u2700-\\u27bf", rsLowerRange = "a-z\\xdf-\\xf6\\xf8-\\xff", rsMathOpRange = "\\xac\\xb1\\xd7\\xf7", rsNonCharRange = "\\x00-\\x2f\\x3a-\\x40\\x5b-\\x60\\x7b-\\xbf", rsPunctuationRange = "\\u2000-\\u206f", rsSpaceRange = " \\t\\x0b\\f\\xa0\\ufeff\\n\\r\\u2028\\u2029\\u1680\\u180e\\u2000\\u2001\\u2002\\u2003\\u2004\\u2005\\u2006\\u2007\\u2008\\u2009\\u200a\\u202f\\u205f\\u3000", rsUpperRange = "A-Z\\xc0-\\xd6\\xd8-\\xde", rsVarRange = "\\ufe0e\\ufe0f", rsBreakRange = rsMathOpRange + rsNonCharRange + rsPunctuationRange + rsSpaceRange;
      var rsApos = "['\u2019]", rsAstral = "[" + rsAstralRange + "]", rsBreak = "[" + rsBreakRange + "]", rsCombo = "[" + rsComboRange + "]", rsDigits = "\\d+", rsDingbat = "[" + rsDingbatRange + "]", rsLower = "[" + rsLowerRange + "]", rsMisc = "[^" + rsAstralRange + rsBreakRange + rsDigits + rsDingbatRange + rsLowerRange + rsUpperRange + "]", rsFitz = "\\ud83c[\\udffb-\\udfff]", rsModifier = "(?:" + rsCombo + "|" + rsFitz + ")", rsNonAstral = "[^" + rsAstralRange + "]", rsRegional = "(?:\\ud83c[\\udde6-\\uddff]){2}", rsSurrPair = "[\\ud800-\\udbff][\\udc00-\\udfff]", rsUpper = "[" + rsUpperRange + "]", rsZWJ = "\\u200d";
      var rsMiscLower = "(?:" + rsLower + "|" + rsMisc + ")", rsMiscUpper = "(?:" + rsUpper + "|" + rsMisc + ")", rsOptContrLower = "(?:" + rsApos + "(?:d|ll|m|re|s|t|ve))?", rsOptContrUpper = "(?:" + rsApos + "(?:D|LL|M|RE|S|T|VE))?", reOptMod = rsModifier + "?", rsOptVar = "[" + rsVarRange + "]?", rsOptJoin = "(?:" + rsZWJ + "(?:" + [rsNonAstral, rsRegional, rsSurrPair].join("|") + ")" + rsOptVar + reOptMod + ")*", rsOrdLower = "\\d*(?:1st|2nd|3rd|(?![123])\\dth)(?=\\b|[A-Z_])", rsOrdUpper = "\\d*(?:1ST|2ND|3RD|(?![123])\\dTH)(?=\\b|[a-z_])", rsSeq = rsOptVar + reOptMod + rsOptJoin, rsEmoji = "(?:" + [rsDingbat, rsRegional, rsSurrPair].join("|") + ")" + rsSeq, rsSymbol = "(?:" + [rsNonAstral + rsCombo + "?", rsCombo, rsRegional, rsSurrPair, rsAstral].join("|") + ")";
      var reApos = RegExp(rsApos, "g");
      var reComboMark = RegExp(rsCombo, "g");
      var reUnicode = RegExp(rsFitz + "(?=" + rsFitz + ")|" + rsSymbol + rsSeq, "g");
      var reUnicodeWord = RegExp([
        rsUpper + "?" + rsLower + "+" + rsOptContrLower + "(?=" + [rsBreak, rsUpper, "$"].join("|") + ")",
        rsMiscUpper + "+" + rsOptContrUpper + "(?=" + [rsBreak, rsUpper + rsMiscLower, "$"].join("|") + ")",
        rsUpper + "?" + rsMiscLower + "+" + rsOptContrLower,
        rsUpper + "+" + rsOptContrUpper,
        rsOrdUpper,
        rsOrdLower,
        rsDigits,
        rsEmoji
      ].join("|"), "g");
      var reHasUnicode = RegExp("[" + rsZWJ + rsAstralRange + rsComboRange + rsVarRange + "]");
      var reHasUnicodeWord = /[a-z][A-Z]|[A-Z]{2}[a-z]|[0-9][a-zA-Z]|[a-zA-Z][0-9]|[^a-zA-Z0-9 ]/;
      var contextProps = [
        "Array",
        "Buffer",
        "DataView",
        "Date",
        "Error",
        "Float32Array",
        "Float64Array",
        "Function",
        "Int8Array",
        "Int16Array",
        "Int32Array",
        "Map",
        "Math",
        "Object",
        "Promise",
        "RegExp",
        "Set",
        "String",
        "Symbol",
        "TypeError",
        "Uint8Array",
        "Uint8ClampedArray",
        "Uint16Array",
        "Uint32Array",
        "WeakMap",
        "_",
        "clearTimeout",
        "isFinite",
        "parseInt",
        "setTimeout"
      ];
      var templateCounter = -1;
      var typedArrayTags = {};
      typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
      typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
      var cloneableTags = {};
      cloneableTags[argsTag] = cloneableTags[arrayTag] = cloneableTags[arrayBufferTag] = cloneableTags[dataViewTag] = cloneableTags[boolTag] = cloneableTags[dateTag] = cloneableTags[float32Tag] = cloneableTags[float64Tag] = cloneableTags[int8Tag] = cloneableTags[int16Tag] = cloneableTags[int32Tag] = cloneableTags[mapTag] = cloneableTags[numberTag] = cloneableTags[objectTag] = cloneableTags[regexpTag] = cloneableTags[setTag] = cloneableTags[stringTag] = cloneableTags[symbolTag] = cloneableTags[uint8Tag] = cloneableTags[uint8ClampedTag] = cloneableTags[uint16Tag] = cloneableTags[uint32Tag] = true;
      cloneableTags[errorTag] = cloneableTags[funcTag] = cloneableTags[weakMapTag] = false;
      var deburredLetters = {
        // Latin-1 Supplement block.
        "\xC0": "A",
        "\xC1": "A",
        "\xC2": "A",
        "\xC3": "A",
        "\xC4": "A",
        "\xC5": "A",
        "\xE0": "a",
        "\xE1": "a",
        "\xE2": "a",
        "\xE3": "a",
        "\xE4": "a",
        "\xE5": "a",
        "\xC7": "C",
        "\xE7": "c",
        "\xD0": "D",
        "\xF0": "d",
        "\xC8": "E",
        "\xC9": "E",
        "\xCA": "E",
        "\xCB": "E",
        "\xE8": "e",
        "\xE9": "e",
        "\xEA": "e",
        "\xEB": "e",
        "\xCC": "I",
        "\xCD": "I",
        "\xCE": "I",
        "\xCF": "I",
        "\xEC": "i",
        "\xED": "i",
        "\xEE": "i",
        "\xEF": "i",
        "\xD1": "N",
        "\xF1": "n",
        "\xD2": "O",
        "\xD3": "O",
        "\xD4": "O",
        "\xD5": "O",
        "\xD6": "O",
        "\xD8": "O",
        "\xF2": "o",
        "\xF3": "o",
        "\xF4": "o",
        "\xF5": "o",
        "\xF6": "o",
        "\xF8": "o",
        "\xD9": "U",
        "\xDA": "U",
        "\xDB": "U",
        "\xDC": "U",
        "\xF9": "u",
        "\xFA": "u",
        "\xFB": "u",
        "\xFC": "u",
        "\xDD": "Y",
        "\xFD": "y",
        "\xFF": "y",
        "\xC6": "Ae",
        "\xE6": "ae",
        "\xDE": "Th",
        "\xFE": "th",
        "\xDF": "ss",
        // Latin Extended-A block.
        "\u0100": "A",
        "\u0102": "A",
        "\u0104": "A",
        "\u0101": "a",
        "\u0103": "a",
        "\u0105": "a",
        "\u0106": "C",
        "\u0108": "C",
        "\u010A": "C",
        "\u010C": "C",
        "\u0107": "c",
        "\u0109": "c",
        "\u010B": "c",
        "\u010D": "c",
        "\u010E": "D",
        "\u0110": "D",
        "\u010F": "d",
        "\u0111": "d",
        "\u0112": "E",
        "\u0114": "E",
        "\u0116": "E",
        "\u0118": "E",
        "\u011A": "E",
        "\u0113": "e",
        "\u0115": "e",
        "\u0117": "e",
        "\u0119": "e",
        "\u011B": "e",
        "\u011C": "G",
        "\u011E": "G",
        "\u0120": "G",
        "\u0122": "G",
        "\u011D": "g",
        "\u011F": "g",
        "\u0121": "g",
        "\u0123": "g",
        "\u0124": "H",
        "\u0126": "H",
        "\u0125": "h",
        "\u0127": "h",
        "\u0128": "I",
        "\u012A": "I",
        "\u012C": "I",
        "\u012E": "I",
        "\u0130": "I",
        "\u0129": "i",
        "\u012B": "i",
        "\u012D": "i",
        "\u012F": "i",
        "\u0131": "i",
        "\u0134": "J",
        "\u0135": "j",
        "\u0136": "K",
        "\u0137": "k",
        "\u0138": "k",
        "\u0139": "L",
        "\u013B": "L",
        "\u013D": "L",
        "\u013F": "L",
        "\u0141": "L",
        "\u013A": "l",
        "\u013C": "l",
        "\u013E": "l",
        "\u0140": "l",
        "\u0142": "l",
        "\u0143": "N",
        "\u0145": "N",
        "\u0147": "N",
        "\u014A": "N",
        "\u0144": "n",
        "\u0146": "n",
        "\u0148": "n",
        "\u014B": "n",
        "\u014C": "O",
        "\u014E": "O",
        "\u0150": "O",
        "\u014D": "o",
        "\u014F": "o",
        "\u0151": "o",
        "\u0154": "R",
        "\u0156": "R",
        "\u0158": "R",
        "\u0155": "r",
        "\u0157": "r",
        "\u0159": "r",
        "\u015A": "S",
        "\u015C": "S",
        "\u015E": "S",
        "\u0160": "S",
        "\u015B": "s",
        "\u015D": "s",
        "\u015F": "s",
        "\u0161": "s",
        "\u0162": "T",
        "\u0164": "T",
        "\u0166": "T",
        "\u0163": "t",
        "\u0165": "t",
        "\u0167": "t",
        "\u0168": "U",
        "\u016A": "U",
        "\u016C": "U",
        "\u016E": "U",
        "\u0170": "U",
        "\u0172": "U",
        "\u0169": "u",
        "\u016B": "u",
        "\u016D": "u",
        "\u016F": "u",
        "\u0171": "u",
        "\u0173": "u",
        "\u0174": "W",
        "\u0175": "w",
        "\u0176": "Y",
        "\u0177": "y",
        "\u0178": "Y",
        "\u0179": "Z",
        "\u017B": "Z",
        "\u017D": "Z",
        "\u017A": "z",
        "\u017C": "z",
        "\u017E": "z",
        "\u0132": "IJ",
        "\u0133": "ij",
        "\u0152": "Oe",
        "\u0153": "oe",
        "\u0149": "'n",
        "\u017F": "s"
      };
      var htmlEscapes = {
        "&": "&amp;",
        "<": "&lt;",
        ">": "&gt;",
        '"': "&quot;",
        "'": "&#39;"
      };
      var htmlUnescapes = {
        "&amp;": "&",
        "&lt;": "<",
        "&gt;": ">",
        "&quot;": '"',
        "&#39;": "'"
      };
      var stringEscapes = {
        "\\": "\\",
        "'": "'",
        "\n": "n",
        "\r": "r",
        "\u2028": "u2028",
        "\u2029": "u2029"
      };
      var freeParseFloat = parseFloat, freeParseInt = parseInt;
      var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
      var freeSelf = typeof self == "object" && self && self.Object === Object && self;
      var root = freeGlobal || freeSelf || Function("return this")();
      var freeExports = typeof exports2 == "object" && exports2 && !exports2.nodeType && exports2;
      var freeModule = freeExports && typeof module2 == "object" && module2 && !module2.nodeType && module2;
      var moduleExports = freeModule && freeModule.exports === freeExports;
      var freeProcess = moduleExports && freeGlobal.process;
      var nodeUtil = function() {
        try {
          var types = freeModule && freeModule.require && freeModule.require("util").types;
          if (types) {
            return types;
          }
          return freeProcess && freeProcess.binding && freeProcess.binding("util");
        } catch (e) {
        }
      }();
      var nodeIsArrayBuffer = nodeUtil && nodeUtil.isArrayBuffer, nodeIsDate = nodeUtil && nodeUtil.isDate, nodeIsMap = nodeUtil && nodeUtil.isMap, nodeIsRegExp = nodeUtil && nodeUtil.isRegExp, nodeIsSet = nodeUtil && nodeUtil.isSet, nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
      function apply(func, thisArg, args) {
        switch (args.length) {
          case 0:
            return func.call(thisArg);
          case 1:
            return func.call(thisArg, args[0]);
          case 2:
            return func.call(thisArg, args[0], args[1]);
          case 3:
            return func.call(thisArg, args[0], args[1], args[2]);
        }
        return func.apply(thisArg, args);
      }
      function arrayAggregator(array, setter, iteratee, accumulator) {
        var index = -1, length = array == null ? 0 : array.length;
        while (++index < length) {
          var value = array[index];
          setter(accumulator, value, iteratee(value), array);
        }
        return accumulator;
      }
      function arrayEach(array, iteratee) {
        var index = -1, length = array == null ? 0 : array.length;
        while (++index < length) {
          if (iteratee(array[index], index, array) === false) {
            break;
          }
        }
        return array;
      }
      function arrayEachRight(array, iteratee) {
        var length = array == null ? 0 : array.length;
        while (length--) {
          if (iteratee(array[length], length, array) === false) {
            break;
          }
        }
        return array;
      }
      function arrayEvery(array, predicate) {
        var index = -1, length = array == null ? 0 : array.length;
        while (++index < length) {
          if (!predicate(array[index], index, array)) {
            return false;
          }
        }
        return true;
      }
      function arrayFilter(array, predicate) {
        var index = -1, length = array == null ? 0 : array.length, resIndex = 0, result = [];
        while (++index < length) {
          var value = array[index];
          if (predicate(value, index, array)) {
            result[resIndex++] = value;
          }
        }
        return result;
      }
      function arrayIncludes(array, value) {
        var length = array == null ? 0 : array.length;
        return !!length && baseIndexOf(array, value, 0) > -1;
      }
      function arrayIncludesWith(array, value, comparator) {
        var index = -1, length = array == null ? 0 : array.length;
        while (++index < length) {
          if (comparator(value, array[index])) {
            return true;
          }
        }
        return false;
      }
      function arrayMap(array, iteratee) {
        var index = -1, length = array == null ? 0 : array.length, result = Array(length);
        while (++index < length) {
          result[index] = iteratee(array[index], index, array);
        }
        return result;
      }
      function arrayPush(array, values) {
        var index = -1, length = values.length, offset = array.length;
        while (++index < length) {
          array[offset + index] = values[index];
        }
        return array;
      }
      function arrayReduce(array, iteratee, accumulator, initAccum) {
        var index = -1, length = array == null ? 0 : array.length;
        if (initAccum && length) {
          accumulator = array[++index];
        }
        while (++index < length) {
          accumulator = iteratee(accumulator, array[index], index, array);
        }
        return accumulator;
      }
      function arrayReduceRight(array, iteratee, accumulator, initAccum) {
        var length = array == null ? 0 : array.length;
        if (initAccum && length) {
          accumulator = array[--length];
        }
        while (length--) {
          accumulator = iteratee(accumulator, array[length], length, array);
        }
        return accumulator;
      }
      function arraySome(array, predicate) {
        var index = -1, length = array == null ? 0 : array.length;
        while (++index < length) {
          if (predicate(array[index], index, array)) {
            return true;
          }
        }
        return false;
      }
      var asciiSize = baseProperty("length");
      function asciiToArray(string) {
        return string.split("");
      }
      function asciiWords(string) {
        return string.match(reAsciiWord) || [];
      }
      function baseFindKey(collection, predicate, eachFunc) {
        var result;
        eachFunc(collection, function(value, key, collection2) {
          if (predicate(value, key, collection2)) {
            result = key;
            return false;
          }
        });
        return result;
      }
      function baseFindIndex(array, predicate, fromIndex, fromRight) {
        var length = array.length, index = fromIndex + (fromRight ? 1 : -1);
        while (fromRight ? index-- : ++index < length) {
          if (predicate(array[index], index, array)) {
            return index;
          }
        }
        return -1;
      }
      function baseIndexOf(array, value, fromIndex) {
        return value === value ? strictIndexOf(array, value, fromIndex) : baseFindIndex(array, baseIsNaN, fromIndex);
      }
      function baseIndexOfWith(array, value, fromIndex, comparator) {
        var index = fromIndex - 1, length = array.length;
        while (++index < length) {
          if (comparator(array[index], value)) {
            return index;
          }
        }
        return -1;
      }
      function baseIsNaN(value) {
        return value !== value;
      }
      function baseMean(array, iteratee) {
        var length = array == null ? 0 : array.length;
        return length ? baseSum(array, iteratee) / length : NAN;
      }
      function baseProperty(key) {
        return function(object) {
          return object == null ? undefined2 : object[key];
        };
      }
      function basePropertyOf(object) {
        return function(key) {
          return object == null ? undefined2 : object[key];
        };
      }
      function baseReduce(collection, iteratee, accumulator, initAccum, eachFunc) {
        eachFunc(collection, function(value, index, collection2) {
          accumulator = initAccum ? (initAccum = false, value) : iteratee(accumulator, value, index, collection2);
        });
        return accumulator;
      }
      function baseSortBy(array, comparer) {
        var length = array.length;
        array.sort(comparer);
        while (length--) {
          array[length] = array[length].value;
        }
        return array;
      }
      function baseSum(array, iteratee) {
        var result, index = -1, length = array.length;
        while (++index < length) {
          var current = iteratee(array[index]);
          if (current !== undefined2) {
            result = result === undefined2 ? current : result + current;
          }
        }
        return result;
      }
      function baseTimes(n, iteratee) {
        var index = -1, result = Array(n);
        while (++index < n) {
          result[index] = iteratee(index);
        }
        return result;
      }
      function baseToPairs(object, props) {
        return arrayMap(props, function(key) {
          return [key, object[key]];
        });
      }
      function baseTrim(string) {
        return string ? string.slice(0, trimmedEndIndex(string) + 1).replace(reTrimStart, "") : string;
      }
      function baseUnary(func) {
        return function(value) {
          return func(value);
        };
      }
      function baseValues(object, props) {
        return arrayMap(props, function(key) {
          return object[key];
        });
      }
      function cacheHas(cache, key) {
        return cache.has(key);
      }
      function charsStartIndex(strSymbols, chrSymbols) {
        var index = -1, length = strSymbols.length;
        while (++index < length && baseIndexOf(chrSymbols, strSymbols[index], 0) > -1) {
        }
        return index;
      }
      function charsEndIndex(strSymbols, chrSymbols) {
        var index = strSymbols.length;
        while (index-- && baseIndexOf(chrSymbols, strSymbols[index], 0) > -1) {
        }
        return index;
      }
      function countHolders(array, placeholder) {
        var length = array.length, result = 0;
        while (length--) {
          if (array[length] === placeholder) {
            ++result;
          }
        }
        return result;
      }
      var deburrLetter = basePropertyOf(deburredLetters);
      var escapeHtmlChar = basePropertyOf(htmlEscapes);
      function escapeStringChar(chr) {
        return "\\" + stringEscapes[chr];
      }
      function getValue(object, key) {
        return object == null ? undefined2 : object[key];
      }
      function hasUnicode(string) {
        return reHasUnicode.test(string);
      }
      function hasUnicodeWord(string) {
        return reHasUnicodeWord.test(string);
      }
      function iteratorToArray(iterator) {
        var data, result = [];
        while (!(data = iterator.next()).done) {
          result.push(data.value);
        }
        return result;
      }
      function mapToArray(map) {
        var index = -1, result = Array(map.size);
        map.forEach(function(value, key) {
          result[++index] = [key, value];
        });
        return result;
      }
      function overArg(func, transform) {
        return function(arg) {
          return func(transform(arg));
        };
      }
      function replaceHolders(array, placeholder) {
        var index = -1, length = array.length, resIndex = 0, result = [];
        while (++index < length) {
          var value = array[index];
          if (value === placeholder || value === PLACEHOLDER) {
            array[index] = PLACEHOLDER;
            result[resIndex++] = index;
          }
        }
        return result;
      }
      function setToArray(set3) {
        var index = -1, result = Array(set3.size);
        set3.forEach(function(value) {
          result[++index] = value;
        });
        return result;
      }
      function setToPairs(set3) {
        var index = -1, result = Array(set3.size);
        set3.forEach(function(value) {
          result[++index] = [value, value];
        });
        return result;
      }
      function strictIndexOf(array, value, fromIndex) {
        var index = fromIndex - 1, length = array.length;
        while (++index < length) {
          if (array[index] === value) {
            return index;
          }
        }
        return -1;
      }
      function strictLastIndexOf(array, value, fromIndex) {
        var index = fromIndex + 1;
        while (index--) {
          if (array[index] === value) {
            return index;
          }
        }
        return index;
      }
      function stringSize(string) {
        return hasUnicode(string) ? unicodeSize(string) : asciiSize(string);
      }
      function stringToArray(string) {
        return hasUnicode(string) ? unicodeToArray(string) : asciiToArray(string);
      }
      function trimmedEndIndex(string) {
        var index = string.length;
        while (index-- && reWhitespace.test(string.charAt(index))) {
        }
        return index;
      }
      var unescapeHtmlChar = basePropertyOf(htmlUnescapes);
      function unicodeSize(string) {
        var result = reUnicode.lastIndex = 0;
        while (reUnicode.test(string)) {
          ++result;
        }
        return result;
      }
      function unicodeToArray(string) {
        return string.match(reUnicode) || [];
      }
      function unicodeWords(string) {
        return string.match(reUnicodeWord) || [];
      }
      var runInContext = function runInContext2(context) {
        context = context == null ? root : _.defaults(root.Object(), context, _.pick(root, contextProps));
        var Array2 = context.Array, Date2 = context.Date, Error2 = context.Error, Function2 = context.Function, Math2 = context.Math, Object2 = context.Object, RegExp2 = context.RegExp, String2 = context.String, TypeError2 = context.TypeError;
        var arrayProto = Array2.prototype, funcProto = Function2.prototype, objectProto = Object2.prototype;
        var coreJsData = context["__core-js_shared__"];
        var funcToString = funcProto.toString;
        var hasOwnProperty = objectProto.hasOwnProperty;
        var idCounter = 0;
        var maskSrcKey = function() {
          var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
          return uid ? "Symbol(src)_1." + uid : "";
        }();
        var nativeObjectToString = objectProto.toString;
        var objectCtorString = funcToString.call(Object2);
        var oldDash = root._;
        var reIsNative = RegExp2(
          "^" + funcToString.call(hasOwnProperty).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
        );
        var Buffer2 = moduleExports ? context.Buffer : undefined2, Symbol2 = context.Symbol, Uint8Array2 = context.Uint8Array, allocUnsafe = Buffer2 ? Buffer2.allocUnsafe : undefined2, getPrototype = overArg(Object2.getPrototypeOf, Object2), objectCreate = Object2.create, propertyIsEnumerable = objectProto.propertyIsEnumerable, splice = arrayProto.splice, spreadableSymbol = Symbol2 ? Symbol2.isConcatSpreadable : undefined2, symIterator = Symbol2 ? Symbol2.iterator : undefined2, symToStringTag = Symbol2 ? Symbol2.toStringTag : undefined2;
        var defineProperty = function() {
          try {
            var func = getNative(Object2, "defineProperty");
            func({}, "", {});
            return func;
          } catch (e) {
          }
        }();
        var ctxClearTimeout = context.clearTimeout !== root.clearTimeout && context.clearTimeout, ctxNow = Date2 && Date2.now !== root.Date.now && Date2.now, ctxSetTimeout = context.setTimeout !== root.setTimeout && context.setTimeout;
        var nativeCeil = Math2.ceil, nativeFloor = Math2.floor, nativeGetSymbols = Object2.getOwnPropertySymbols, nativeIsBuffer = Buffer2 ? Buffer2.isBuffer : undefined2, nativeIsFinite = context.isFinite, nativeJoin = arrayProto.join, nativeKeys = overArg(Object2.keys, Object2), nativeMax = Math2.max, nativeMin = Math2.min, nativeNow = Date2.now, nativeParseInt = context.parseInt, nativeRandom = Math2.random, nativeReverse = arrayProto.reverse;
        var DataView = getNative(context, "DataView"), Map2 = getNative(context, "Map"), Promise2 = getNative(context, "Promise"), Set2 = getNative(context, "Set"), WeakMap = getNative(context, "WeakMap"), nativeCreate = getNative(Object2, "create");
        var metaMap = WeakMap && new WeakMap();
        var realNames = {};
        var dataViewCtorString = toSource(DataView), mapCtorString = toSource(Map2), promiseCtorString = toSource(Promise2), setCtorString = toSource(Set2), weakMapCtorString = toSource(WeakMap);
        var symbolProto = Symbol2 ? Symbol2.prototype : undefined2, symbolValueOf = symbolProto ? symbolProto.valueOf : undefined2, symbolToString = symbolProto ? symbolProto.toString : undefined2;
        function lodash(value) {
          if (isObjectLike(value) && !isArray3(value) && !(value instanceof LazyWrapper)) {
            if (value instanceof LodashWrapper) {
              return value;
            }
            if (hasOwnProperty.call(value, "__wrapped__")) {
              return wrapperClone(value);
            }
          }
          return new LodashWrapper(value);
        }
        var baseCreate = /* @__PURE__ */ function() {
          function object() {
          }
          return function(proto) {
            if (!isObject4(proto)) {
              return {};
            }
            if (objectCreate) {
              return objectCreate(proto);
            }
            object.prototype = proto;
            var result2 = new object();
            object.prototype = undefined2;
            return result2;
          };
        }();
        function baseLodash() {
        }
        function LodashWrapper(value, chainAll) {
          this.__wrapped__ = value;
          this.__actions__ = [];
          this.__chain__ = !!chainAll;
          this.__index__ = 0;
          this.__values__ = undefined2;
        }
        lodash.templateSettings = {
          /**
           * Used to detect `data` property values to be HTML-escaped.
           *
           * @memberOf _.templateSettings
           * @type {RegExp}
           */
          "escape": reEscape,
          /**
           * Used to detect code to be evaluated.
           *
           * @memberOf _.templateSettings
           * @type {RegExp}
           */
          "evaluate": reEvaluate,
          /**
           * Used to detect `data` property values to inject.
           *
           * @memberOf _.templateSettings
           * @type {RegExp}
           */
          "interpolate": reInterpolate,
          /**
           * Used to reference the data object in the template text.
           *
           * @memberOf _.templateSettings
           * @type {string}
           */
          "variable": "",
          /**
           * Used to import variables into the compiled template.
           *
           * @memberOf _.templateSettings
           * @type {Object}
           */
          "imports": {
            /**
             * A reference to the `lodash` function.
             *
             * @memberOf _.templateSettings.imports
             * @type {Function}
             */
            "_": lodash
          }
        };
        lodash.prototype = baseLodash.prototype;
        lodash.prototype.constructor = lodash;
        LodashWrapper.prototype = baseCreate(baseLodash.prototype);
        LodashWrapper.prototype.constructor = LodashWrapper;
        function LazyWrapper(value) {
          this.__wrapped__ = value;
          this.__actions__ = [];
          this.__dir__ = 1;
          this.__filtered__ = false;
          this.__iteratees__ = [];
          this.__takeCount__ = MAX_ARRAY_LENGTH;
          this.__views__ = [];
        }
        function lazyClone() {
          var result2 = new LazyWrapper(this.__wrapped__);
          result2.__actions__ = copyArray(this.__actions__);
          result2.__dir__ = this.__dir__;
          result2.__filtered__ = this.__filtered__;
          result2.__iteratees__ = copyArray(this.__iteratees__);
          result2.__takeCount__ = this.__takeCount__;
          result2.__views__ = copyArray(this.__views__);
          return result2;
        }
        function lazyReverse() {
          if (this.__filtered__) {
            var result2 = new LazyWrapper(this);
            result2.__dir__ = -1;
            result2.__filtered__ = true;
          } else {
            result2 = this.clone();
            result2.__dir__ *= -1;
          }
          return result2;
        }
        function lazyValue() {
          var array = this.__wrapped__.value(), dir = this.__dir__, isArr = isArray3(array), isRight = dir < 0, arrLength = isArr ? array.length : 0, view = getView(0, arrLength, this.__views__), start = view.start, end = view.end, length = end - start, index = isRight ? end : start - 1, iteratees = this.__iteratees__, iterLength = iteratees.length, resIndex = 0, takeCount = nativeMin(length, this.__takeCount__);
          if (!isArr || !isRight && arrLength == length && takeCount == length) {
            return baseWrapperValue(array, this.__actions__);
          }
          var result2 = [];
          outer:
            while (length-- && resIndex < takeCount) {
              index += dir;
              var iterIndex = -1, value = array[index];
              while (++iterIndex < iterLength) {
                var data = iteratees[iterIndex], iteratee2 = data.iteratee, type = data.type, computed = iteratee2(value);
                if (type == LAZY_MAP_FLAG) {
                  value = computed;
                } else if (!computed) {
                  if (type == LAZY_FILTER_FLAG) {
                    continue outer;
                  } else {
                    break outer;
                  }
                }
              }
              result2[resIndex++] = value;
            }
          return result2;
        }
        LazyWrapper.prototype = baseCreate(baseLodash.prototype);
        LazyWrapper.prototype.constructor = LazyWrapper;
        function Hash(entries) {
          var index = -1, length = entries == null ? 0 : entries.length;
          this.clear();
          while (++index < length) {
            var entry = entries[index];
            this.set(entry[0], entry[1]);
          }
        }
        function hashClear() {
          this.__data__ = nativeCreate ? nativeCreate(null) : {};
          this.size = 0;
        }
        function hashDelete(key) {
          var result2 = this.has(key) && delete this.__data__[key];
          this.size -= result2 ? 1 : 0;
          return result2;
        }
        function hashGet(key) {
          var data = this.__data__;
          if (nativeCreate) {
            var result2 = data[key];
            return result2 === HASH_UNDEFINED ? undefined2 : result2;
          }
          return hasOwnProperty.call(data, key) ? data[key] : undefined2;
        }
        function hashHas(key) {
          var data = this.__data__;
          return nativeCreate ? data[key] !== undefined2 : hasOwnProperty.call(data, key);
        }
        function hashSet(key, value) {
          var data = this.__data__;
          this.size += this.has(key) ? 0 : 1;
          data[key] = nativeCreate && value === undefined2 ? HASH_UNDEFINED : value;
          return this;
        }
        Hash.prototype.clear = hashClear;
        Hash.prototype["delete"] = hashDelete;
        Hash.prototype.get = hashGet;
        Hash.prototype.has = hashHas;
        Hash.prototype.set = hashSet;
        function ListCache(entries) {
          var index = -1, length = entries == null ? 0 : entries.length;
          this.clear();
          while (++index < length) {
            var entry = entries[index];
            this.set(entry[0], entry[1]);
          }
        }
        function listCacheClear() {
          this.__data__ = [];
          this.size = 0;
        }
        function listCacheDelete(key) {
          var data = this.__data__, index = assocIndexOf(data, key);
          if (index < 0) {
            return false;
          }
          var lastIndex = data.length - 1;
          if (index == lastIndex) {
            data.pop();
          } else {
            splice.call(data, index, 1);
          }
          --this.size;
          return true;
        }
        function listCacheGet(key) {
          var data = this.__data__, index = assocIndexOf(data, key);
          return index < 0 ? undefined2 : data[index][1];
        }
        function listCacheHas(key) {
          return assocIndexOf(this.__data__, key) > -1;
        }
        function listCacheSet(key, value) {
          var data = this.__data__, index = assocIndexOf(data, key);
          if (index < 0) {
            ++this.size;
            data.push([key, value]);
          } else {
            data[index][1] = value;
          }
          return this;
        }
        ListCache.prototype.clear = listCacheClear;
        ListCache.prototype["delete"] = listCacheDelete;
        ListCache.prototype.get = listCacheGet;
        ListCache.prototype.has = listCacheHas;
        ListCache.prototype.set = listCacheSet;
        function MapCache(entries) {
          var index = -1, length = entries == null ? 0 : entries.length;
          this.clear();
          while (++index < length) {
            var entry = entries[index];
            this.set(entry[0], entry[1]);
          }
        }
        function mapCacheClear() {
          this.size = 0;
          this.__data__ = {
            "hash": new Hash(),
            "map": new (Map2 || ListCache)(),
            "string": new Hash()
          };
        }
        function mapCacheDelete(key) {
          var result2 = getMapData(this, key)["delete"](key);
          this.size -= result2 ? 1 : 0;
          return result2;
        }
        function mapCacheGet(key) {
          return getMapData(this, key).get(key);
        }
        function mapCacheHas(key) {
          return getMapData(this, key).has(key);
        }
        function mapCacheSet(key, value) {
          var data = getMapData(this, key), size2 = data.size;
          data.set(key, value);
          this.size += data.size == size2 ? 0 : 1;
          return this;
        }
        MapCache.prototype.clear = mapCacheClear;
        MapCache.prototype["delete"] = mapCacheDelete;
        MapCache.prototype.get = mapCacheGet;
        MapCache.prototype.has = mapCacheHas;
        MapCache.prototype.set = mapCacheSet;
        function SetCache(values2) {
          var index = -1, length = values2 == null ? 0 : values2.length;
          this.__data__ = new MapCache();
          while (++index < length) {
            this.add(values2[index]);
          }
        }
        function setCacheAdd(value) {
          this.__data__.set(value, HASH_UNDEFINED);
          return this;
        }
        function setCacheHas(value) {
          return this.__data__.has(value);
        }
        SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
        SetCache.prototype.has = setCacheHas;
        function Stack(entries) {
          var data = this.__data__ = new ListCache(entries);
          this.size = data.size;
        }
        function stackClear() {
          this.__data__ = new ListCache();
          this.size = 0;
        }
        function stackDelete(key) {
          var data = this.__data__, result2 = data["delete"](key);
          this.size = data.size;
          return result2;
        }
        function stackGet(key) {
          return this.__data__.get(key);
        }
        function stackHas(key) {
          return this.__data__.has(key);
        }
        function stackSet(key, value) {
          var data = this.__data__;
          if (data instanceof ListCache) {
            var pairs = data.__data__;
            if (!Map2 || pairs.length < LARGE_ARRAY_SIZE - 1) {
              pairs.push([key, value]);
              this.size = ++data.size;
              return this;
            }
            data = this.__data__ = new MapCache(pairs);
          }
          data.set(key, value);
          this.size = data.size;
          return this;
        }
        Stack.prototype.clear = stackClear;
        Stack.prototype["delete"] = stackDelete;
        Stack.prototype.get = stackGet;
        Stack.prototype.has = stackHas;
        Stack.prototype.set = stackSet;
        function arrayLikeKeys(value, inherited) {
          var isArr = isArray3(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result2 = skipIndexes ? baseTimes(value.length, String2) : [], length = result2.length;
          for (var key in value) {
            if ((inherited || hasOwnProperty.call(value, key)) && !(skipIndexes && // Safari 9 has enumerable `arguments.length` in strict mode.
            (key == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
            isBuff && (key == "offset" || key == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
            isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || // Skip index properties.
            isIndex(key, length)))) {
              result2.push(key);
            }
          }
          return result2;
        }
        function arraySample(array) {
          var length = array.length;
          return length ? array[baseRandom(0, length - 1)] : undefined2;
        }
        function arraySampleSize(array, n) {
          return shuffleSelf(copyArray(array), baseClamp(n, 0, array.length));
        }
        function arrayShuffle(array) {
          return shuffleSelf(copyArray(array));
        }
        function assignMergeValue(object, key, value) {
          if (value !== undefined2 && !eq(object[key], value) || value === undefined2 && !(key in object)) {
            baseAssignValue(object, key, value);
          }
        }
        function assignValue(object, key, value) {
          var objValue = object[key];
          if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) || value === undefined2 && !(key in object)) {
            baseAssignValue(object, key, value);
          }
        }
        function assocIndexOf(array, key) {
          var length = array.length;
          while (length--) {
            if (eq(array[length][0], key)) {
              return length;
            }
          }
          return -1;
        }
        function baseAggregator(collection, setter, iteratee2, accumulator) {
          baseEach(collection, function(value, key, collection2) {
            setter(accumulator, value, iteratee2(value), collection2);
          });
          return accumulator;
        }
        function baseAssign(object, source) {
          return object && copyObject(source, keys(source), object);
        }
        function baseAssignIn(object, source) {
          return object && copyObject(source, keysIn(source), object);
        }
        function baseAssignValue(object, key, value) {
          if (key == "__proto__" && defineProperty) {
            defineProperty(object, key, {
              "configurable": true,
              "enumerable": true,
              "value": value,
              "writable": true
            });
          } else {
            object[key] = value;
          }
        }
        function baseAt(object, paths) {
          var index = -1, length = paths.length, result2 = Array2(length), skip = object == null;
          while (++index < length) {
            result2[index] = skip ? undefined2 : get3(object, paths[index]);
          }
          return result2;
        }
        function baseClamp(number, lower, upper) {
          if (number === number) {
            if (upper !== undefined2) {
              number = number <= upper ? number : upper;
            }
            if (lower !== undefined2) {
              number = number >= lower ? number : lower;
            }
          }
          return number;
        }
        function baseClone(value, bitmask, customizer, key, object, stack) {
          var result2, isDeep = bitmask & CLONE_DEEP_FLAG, isFlat = bitmask & CLONE_FLAT_FLAG, isFull = bitmask & CLONE_SYMBOLS_FLAG;
          if (customizer) {
            result2 = object ? customizer(value, key, object, stack) : customizer(value);
          }
          if (result2 !== undefined2) {
            return result2;
          }
          if (!isObject4(value)) {
            return value;
          }
          var isArr = isArray3(value);
          if (isArr) {
            result2 = initCloneArray(value);
            if (!isDeep) {
              return copyArray(value, result2);
            }
          } else {
            var tag = getTag(value), isFunc = tag == funcTag || tag == genTag;
            if (isBuffer(value)) {
              return cloneBuffer(value, isDeep);
            }
            if (tag == objectTag || tag == argsTag || isFunc && !object) {
              result2 = isFlat || isFunc ? {} : initCloneObject(value);
              if (!isDeep) {
                return isFlat ? copySymbolsIn(value, baseAssignIn(result2, value)) : copySymbols(value, baseAssign(result2, value));
              }
            } else {
              if (!cloneableTags[tag]) {
                return object ? value : {};
              }
              result2 = initCloneByTag(value, tag, isDeep);
            }
          }
          stack || (stack = new Stack());
          var stacked = stack.get(value);
          if (stacked) {
            return stacked;
          }
          stack.set(value, result2);
          if (isSet(value)) {
            value.forEach(function(subValue) {
              result2.add(baseClone(subValue, bitmask, customizer, subValue, value, stack));
            });
          } else if (isMap(value)) {
            value.forEach(function(subValue, key2) {
              result2.set(key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
            });
          }
          var keysFunc = isFull ? isFlat ? getAllKeysIn : getAllKeys : isFlat ? keysIn : keys;
          var props = isArr ? undefined2 : keysFunc(value);
          arrayEach(props || value, function(subValue, key2) {
            if (props) {
              key2 = subValue;
              subValue = value[key2];
            }
            assignValue(result2, key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
          });
          return result2;
        }
        function baseConforms(source) {
          var props = keys(source);
          return function(object) {
            return baseConformsTo(object, source, props);
          };
        }
        function baseConformsTo(object, source, props) {
          var length = props.length;
          if (object == null) {
            return !length;
          }
          object = Object2(object);
          while (length--) {
            var key = props[length], predicate = source[key], value = object[key];
            if (value === undefined2 && !(key in object) || !predicate(value)) {
              return false;
            }
          }
          return true;
        }
        function baseDelay(func, wait, args) {
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          return setTimeout2(function() {
            func.apply(undefined2, args);
          }, wait);
        }
        function baseDifference(array, values2, iteratee2, comparator) {
          var index = -1, includes2 = arrayIncludes, isCommon = true, length = array.length, result2 = [], valuesLength = values2.length;
          if (!length) {
            return result2;
          }
          if (iteratee2) {
            values2 = arrayMap(values2, baseUnary(iteratee2));
          }
          if (comparator) {
            includes2 = arrayIncludesWith;
            isCommon = false;
          } else if (values2.length >= LARGE_ARRAY_SIZE) {
            includes2 = cacheHas;
            isCommon = false;
            values2 = new SetCache(values2);
          }
          outer:
            while (++index < length) {
              var value = array[index], computed = iteratee2 == null ? value : iteratee2(value);
              value = comparator || value !== 0 ? value : 0;
              if (isCommon && computed === computed) {
                var valuesIndex = valuesLength;
                while (valuesIndex--) {
                  if (values2[valuesIndex] === computed) {
                    continue outer;
                  }
                }
                result2.push(value);
              } else if (!includes2(values2, computed, comparator)) {
                result2.push(value);
              }
            }
          return result2;
        }
        var baseEach = createBaseEach(baseForOwn);
        var baseEachRight = createBaseEach(baseForOwnRight, true);
        function baseEvery(collection, predicate) {
          var result2 = true;
          baseEach(collection, function(value, index, collection2) {
            result2 = !!predicate(value, index, collection2);
            return result2;
          });
          return result2;
        }
        function baseExtremum(array, iteratee2, comparator) {
          var index = -1, length = array.length;
          while (++index < length) {
            var value = array[index], current = iteratee2(value);
            if (current != null && (computed === undefined2 ? current === current && !isSymbol(current) : comparator(current, computed))) {
              var computed = current, result2 = value;
            }
          }
          return result2;
        }
        function baseFill(array, value, start, end) {
          var length = array.length;
          start = toInteger(start);
          if (start < 0) {
            start = -start > length ? 0 : length + start;
          }
          end = end === undefined2 || end > length ? length : toInteger(end);
          if (end < 0) {
            end += length;
          }
          end = start > end ? 0 : toLength(end);
          while (start < end) {
            array[start++] = value;
          }
          return array;
        }
        function baseFilter(collection, predicate) {
          var result2 = [];
          baseEach(collection, function(value, index, collection2) {
            if (predicate(value, index, collection2)) {
              result2.push(value);
            }
          });
          return result2;
        }
        function baseFlatten(array, depth, predicate, isStrict, result2) {
          var index = -1, length = array.length;
          predicate || (predicate = isFlattenable);
          result2 || (result2 = []);
          while (++index < length) {
            var value = array[index];
            if (depth > 0 && predicate(value)) {
              if (depth > 1) {
                baseFlatten(value, depth - 1, predicate, isStrict, result2);
              } else {
                arrayPush(result2, value);
              }
            } else if (!isStrict) {
              result2[result2.length] = value;
            }
          }
          return result2;
        }
        var baseFor = createBaseFor();
        var baseForRight = createBaseFor(true);
        function baseForOwn(object, iteratee2) {
          return object && baseFor(object, iteratee2, keys);
        }
        function baseForOwnRight(object, iteratee2) {
          return object && baseForRight(object, iteratee2, keys);
        }
        function baseFunctions(object, props) {
          return arrayFilter(props, function(key) {
            return isFunction2(object[key]);
          });
        }
        function baseGet(object, path) {
          path = castPath(path, object);
          var index = 0, length = path.length;
          while (object != null && index < length) {
            object = object[toKey(path[index++])];
          }
          return index && index == length ? object : undefined2;
        }
        function baseGetAllKeys(object, keysFunc, symbolsFunc) {
          var result2 = keysFunc(object);
          return isArray3(object) ? result2 : arrayPush(result2, symbolsFunc(object));
        }
        function baseGetTag(value) {
          if (value == null) {
            return value === undefined2 ? undefinedTag : nullTag;
          }
          return symToStringTag && symToStringTag in Object2(value) ? getRawTag(value) : objectToString(value);
        }
        function baseGt(value, other) {
          return value > other;
        }
        function baseHas(object, key) {
          return object != null && hasOwnProperty.call(object, key);
        }
        function baseHasIn(object, key) {
          return object != null && key in Object2(object);
        }
        function baseInRange(number, start, end) {
          return number >= nativeMin(start, end) && number < nativeMax(start, end);
        }
        function baseIntersection(arrays, iteratee2, comparator) {
          var includes2 = comparator ? arrayIncludesWith : arrayIncludes, length = arrays[0].length, othLength = arrays.length, othIndex = othLength, caches = Array2(othLength), maxLength = Infinity, result2 = [];
          while (othIndex--) {
            var array = arrays[othIndex];
            if (othIndex && iteratee2) {
              array = arrayMap(array, baseUnary(iteratee2));
            }
            maxLength = nativeMin(array.length, maxLength);
            caches[othIndex] = !comparator && (iteratee2 || length >= 120 && array.length >= 120) ? new SetCache(othIndex && array) : undefined2;
          }
          array = arrays[0];
          var index = -1, seen = caches[0];
          outer:
            while (++index < length && result2.length < maxLength) {
              var value = array[index], computed = iteratee2 ? iteratee2(value) : value;
              value = comparator || value !== 0 ? value : 0;
              if (!(seen ? cacheHas(seen, computed) : includes2(result2, computed, comparator))) {
                othIndex = othLength;
                while (--othIndex) {
                  var cache = caches[othIndex];
                  if (!(cache ? cacheHas(cache, computed) : includes2(arrays[othIndex], computed, comparator))) {
                    continue outer;
                  }
                }
                if (seen) {
                  seen.push(computed);
                }
                result2.push(value);
              }
            }
          return result2;
        }
        function baseInverter(object, setter, iteratee2, accumulator) {
          baseForOwn(object, function(value, key, object2) {
            setter(accumulator, iteratee2(value), key, object2);
          });
          return accumulator;
        }
        function baseInvoke(object, path, args) {
          path = castPath(path, object);
          object = parent(object, path);
          var func = object == null ? object : object[toKey(last(path))];
          return func == null ? undefined2 : apply(func, object, args);
        }
        function baseIsArguments(value) {
          return isObjectLike(value) && baseGetTag(value) == argsTag;
        }
        function baseIsArrayBuffer(value) {
          return isObjectLike(value) && baseGetTag(value) == arrayBufferTag;
        }
        function baseIsDate(value) {
          return isObjectLike(value) && baseGetTag(value) == dateTag;
        }
        function baseIsEqual(value, other, bitmask, customizer, stack) {
          if (value === other) {
            return true;
          }
          if (value == null || other == null || !isObjectLike(value) && !isObjectLike(other)) {
            return value !== value && other !== other;
          }
          return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
        }
        function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
          var objIsArr = isArray3(object), othIsArr = isArray3(other), objTag = objIsArr ? arrayTag : getTag(object), othTag = othIsArr ? arrayTag : getTag(other);
          objTag = objTag == argsTag ? objectTag : objTag;
          othTag = othTag == argsTag ? objectTag : othTag;
          var objIsObj = objTag == objectTag, othIsObj = othTag == objectTag, isSameTag = objTag == othTag;
          if (isSameTag && isBuffer(object)) {
            if (!isBuffer(other)) {
              return false;
            }
            objIsArr = true;
            objIsObj = false;
          }
          if (isSameTag && !objIsObj) {
            stack || (stack = new Stack());
            return objIsArr || isTypedArray(object) ? equalArrays(object, other, bitmask, customizer, equalFunc, stack) : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
          }
          if (!(bitmask & COMPARE_PARTIAL_FLAG)) {
            var objIsWrapped = objIsObj && hasOwnProperty.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty.call(other, "__wrapped__");
            if (objIsWrapped || othIsWrapped) {
              var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
              stack || (stack = new Stack());
              return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
            }
          }
          if (!isSameTag) {
            return false;
          }
          stack || (stack = new Stack());
          return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
        }
        function baseIsMap(value) {
          return isObjectLike(value) && getTag(value) == mapTag;
        }
        function baseIsMatch(object, source, matchData, customizer) {
          var index = matchData.length, length = index, noCustomizer = !customizer;
          if (object == null) {
            return !length;
          }
          object = Object2(object);
          while (index--) {
            var data = matchData[index];
            if (noCustomizer && data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) {
              return false;
            }
          }
          while (++index < length) {
            data = matchData[index];
            var key = data[0], objValue = object[key], srcValue = data[1];
            if (noCustomizer && data[2]) {
              if (objValue === undefined2 && !(key in object)) {
                return false;
              }
            } else {
              var stack = new Stack();
              if (customizer) {
                var result2 = customizer(objValue, srcValue, key, object, source, stack);
              }
              if (!(result2 === undefined2 ? baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG, customizer, stack) : result2)) {
                return false;
              }
            }
          }
          return true;
        }
        function baseIsNative(value) {
          if (!isObject4(value) || isMasked(value)) {
            return false;
          }
          var pattern = isFunction2(value) ? reIsNative : reIsHostCtor;
          return pattern.test(toSource(value));
        }
        function baseIsRegExp(value) {
          return isObjectLike(value) && baseGetTag(value) == regexpTag;
        }
        function baseIsSet(value) {
          return isObjectLike(value) && getTag(value) == setTag;
        }
        function baseIsTypedArray(value) {
          return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
        }
        function baseIteratee(value) {
          if (typeof value == "function") {
            return value;
          }
          if (value == null) {
            return identity;
          }
          if (typeof value == "object") {
            return isArray3(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
          }
          return property(value);
        }
        function baseKeys(object) {
          if (!isPrototype(object)) {
            return nativeKeys(object);
          }
          var result2 = [];
          for (var key in Object2(object)) {
            if (hasOwnProperty.call(object, key) && key != "constructor") {
              result2.push(key);
            }
          }
          return result2;
        }
        function baseKeysIn(object) {
          if (!isObject4(object)) {
            return nativeKeysIn(object);
          }
          var isProto = isPrototype(object), result2 = [];
          for (var key in object) {
            if (!(key == "constructor" && (isProto || !hasOwnProperty.call(object, key)))) {
              result2.push(key);
            }
          }
          return result2;
        }
        function baseLt(value, other) {
          return value < other;
        }
        function baseMap(collection, iteratee2) {
          var index = -1, result2 = isArrayLike(collection) ? Array2(collection.length) : [];
          baseEach(collection, function(value, key, collection2) {
            result2[++index] = iteratee2(value, key, collection2);
          });
          return result2;
        }
        function baseMatches(source) {
          var matchData = getMatchData(source);
          if (matchData.length == 1 && matchData[0][2]) {
            return matchesStrictComparable(matchData[0][0], matchData[0][1]);
          }
          return function(object) {
            return object === source || baseIsMatch(object, source, matchData);
          };
        }
        function baseMatchesProperty(path, srcValue) {
          if (isKey(path) && isStrictComparable(srcValue)) {
            return matchesStrictComparable(toKey(path), srcValue);
          }
          return function(object) {
            var objValue = get3(object, path);
            return objValue === undefined2 && objValue === srcValue ? hasIn(object, path) : baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG);
          };
        }
        function baseMerge(object, source, srcIndex, customizer, stack) {
          if (object === source) {
            return;
          }
          baseFor(source, function(srcValue, key) {
            stack || (stack = new Stack());
            if (isObject4(srcValue)) {
              baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);
            } else {
              var newValue = customizer ? customizer(safeGet(object, key), srcValue, key + "", object, source, stack) : undefined2;
              if (newValue === undefined2) {
                newValue = srcValue;
              }
              assignMergeValue(object, key, newValue);
            }
          }, keysIn);
        }
        function baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {
          var objValue = safeGet(object, key), srcValue = safeGet(source, key), stacked = stack.get(srcValue);
          if (stacked) {
            assignMergeValue(object, key, stacked);
            return;
          }
          var newValue = customizer ? customizer(objValue, srcValue, key + "", object, source, stack) : undefined2;
          var isCommon = newValue === undefined2;
          if (isCommon) {
            var isArr = isArray3(srcValue), isBuff = !isArr && isBuffer(srcValue), isTyped = !isArr && !isBuff && isTypedArray(srcValue);
            newValue = srcValue;
            if (isArr || isBuff || isTyped) {
              if (isArray3(objValue)) {
                newValue = objValue;
              } else if (isArrayLikeObject(objValue)) {
                newValue = copyArray(objValue);
              } else if (isBuff) {
                isCommon = false;
                newValue = cloneBuffer(srcValue, true);
              } else if (isTyped) {
                isCommon = false;
                newValue = cloneTypedArray(srcValue, true);
              } else {
                newValue = [];
              }
            } else if (isPlainObject(srcValue) || isArguments(srcValue)) {
              newValue = objValue;
              if (isArguments(objValue)) {
                newValue = toPlainObject(objValue);
              } else if (!isObject4(objValue) || isFunction2(objValue)) {
                newValue = initCloneObject(srcValue);
              }
            } else {
              isCommon = false;
            }
          }
          if (isCommon) {
            stack.set(srcValue, newValue);
            mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
            stack["delete"](srcValue);
          }
          assignMergeValue(object, key, newValue);
        }
        function baseNth(array, n) {
          var length = array.length;
          if (!length) {
            return;
          }
          n += n < 0 ? length : 0;
          return isIndex(n, length) ? array[n] : undefined2;
        }
        function baseOrderBy(collection, iteratees, orders) {
          if (iteratees.length) {
            iteratees = arrayMap(iteratees, function(iteratee2) {
              if (isArray3(iteratee2)) {
                return function(value) {
                  return baseGet(value, iteratee2.length === 1 ? iteratee2[0] : iteratee2);
                };
              }
              return iteratee2;
            });
          } else {
            iteratees = [identity];
          }
          var index = -1;
          iteratees = arrayMap(iteratees, baseUnary(getIteratee()));
          var result2 = baseMap(collection, function(value, key, collection2) {
            var criteria = arrayMap(iteratees, function(iteratee2) {
              return iteratee2(value);
            });
            return { "criteria": criteria, "index": ++index, "value": value };
          });
          return baseSortBy(result2, function(object, other) {
            return compareMultiple(object, other, orders);
          });
        }
        function basePick(object, paths) {
          return basePickBy(object, paths, function(value, path) {
            return hasIn(object, path);
          });
        }
        function basePickBy(object, paths, predicate) {
          var index = -1, length = paths.length, result2 = {};
          while (++index < length) {
            var path = paths[index], value = baseGet(object, path);
            if (predicate(value, path)) {
              baseSet(result2, castPath(path, object), value);
            }
          }
          return result2;
        }
        function basePropertyDeep(path) {
          return function(object) {
            return baseGet(object, path);
          };
        }
        function basePullAll(array, values2, iteratee2, comparator) {
          var indexOf2 = comparator ? baseIndexOfWith : baseIndexOf, index = -1, length = values2.length, seen = array;
          if (array === values2) {
            values2 = copyArray(values2);
          }
          if (iteratee2) {
            seen = arrayMap(array, baseUnary(iteratee2));
          }
          while (++index < length) {
            var fromIndex = 0, value = values2[index], computed = iteratee2 ? iteratee2(value) : value;
            while ((fromIndex = indexOf2(seen, computed, fromIndex, comparator)) > -1) {
              if (seen !== array) {
                splice.call(seen, fromIndex, 1);
              }
              splice.call(array, fromIndex, 1);
            }
          }
          return array;
        }
        function basePullAt(array, indexes) {
          var length = array ? indexes.length : 0, lastIndex = length - 1;
          while (length--) {
            var index = indexes[length];
            if (length == lastIndex || index !== previous) {
              var previous = index;
              if (isIndex(index)) {
                splice.call(array, index, 1);
              } else {
                baseUnset(array, index);
              }
            }
          }
          return array;
        }
        function baseRandom(lower, upper) {
          return lower + nativeFloor(nativeRandom() * (upper - lower + 1));
        }
        function baseRange(start, end, step, fromRight) {
          var index = -1, length = nativeMax(nativeCeil((end - start) / (step || 1)), 0), result2 = Array2(length);
          while (length--) {
            result2[fromRight ? length : ++index] = start;
            start += step;
          }
          return result2;
        }
        function baseRepeat(string, n) {
          var result2 = "";
          if (!string || n < 1 || n > MAX_SAFE_INTEGER) {
            return result2;
          }
          do {
            if (n % 2) {
              result2 += string;
            }
            n = nativeFloor(n / 2);
            if (n) {
              string += string;
            }
          } while (n);
          return result2;
        }
        function baseRest(func, start) {
          return setToString(overRest(func, start, identity), func + "");
        }
        function baseSample(collection) {
          return arraySample(values(collection));
        }
        function baseSampleSize(collection, n) {
          var array = values(collection);
          return shuffleSelf(array, baseClamp(n, 0, array.length));
        }
        function baseSet(object, path, value, customizer) {
          if (!isObject4(object)) {
            return object;
          }
          path = castPath(path, object);
          var index = -1, length = path.length, lastIndex = length - 1, nested = object;
          while (nested != null && ++index < length) {
            var key = toKey(path[index]), newValue = value;
            if (key === "__proto__" || key === "constructor" || key === "prototype") {
              return object;
            }
            if (index != lastIndex) {
              var objValue = nested[key];
              newValue = customizer ? customizer(objValue, key, nested) : undefined2;
              if (newValue === undefined2) {
                newValue = isObject4(objValue) ? objValue : isIndex(path[index + 1]) ? [] : {};
              }
            }
            assignValue(nested, key, newValue);
            nested = nested[key];
          }
          return object;
        }
        var baseSetData = !metaMap ? identity : function(func, data) {
          metaMap.set(func, data);
          return func;
        };
        var baseSetToString = !defineProperty ? identity : function(func, string) {
          return defineProperty(func, "toString", {
            "configurable": true,
            "enumerable": false,
            "value": constant(string),
            "writable": true
          });
        };
        function baseShuffle(collection) {
          return shuffleSelf(values(collection));
        }
        function baseSlice(array, start, end) {
          var index = -1, length = array.length;
          if (start < 0) {
            start = -start > length ? 0 : length + start;
          }
          end = end > length ? length : end;
          if (end < 0) {
            end += length;
          }
          length = start > end ? 0 : end - start >>> 0;
          start >>>= 0;
          var result2 = Array2(length);
          while (++index < length) {
            result2[index] = array[index + start];
          }
          return result2;
        }
        function baseSome(collection, predicate) {
          var result2;
          baseEach(collection, function(value, index, collection2) {
            result2 = predicate(value, index, collection2);
            return !result2;
          });
          return !!result2;
        }
        function baseSortedIndex(array, value, retHighest) {
          var low = 0, high = array == null ? low : array.length;
          if (typeof value == "number" && value === value && high <= HALF_MAX_ARRAY_LENGTH) {
            while (low < high) {
              var mid = low + high >>> 1, computed = array[mid];
              if (computed !== null && !isSymbol(computed) && (retHighest ? computed <= value : computed < value)) {
                low = mid + 1;
              } else {
                high = mid;
              }
            }
            return high;
          }
          return baseSortedIndexBy(array, value, identity, retHighest);
        }
        function baseSortedIndexBy(array, value, iteratee2, retHighest) {
          var low = 0, high = array == null ? 0 : array.length;
          if (high === 0) {
            return 0;
          }
          value = iteratee2(value);
          var valIsNaN = value !== value, valIsNull = value === null, valIsSymbol = isSymbol(value), valIsUndefined = value === undefined2;
          while (low < high) {
            var mid = nativeFloor((low + high) / 2), computed = iteratee2(array[mid]), othIsDefined = computed !== undefined2, othIsNull = computed === null, othIsReflexive = computed === computed, othIsSymbol = isSymbol(computed);
            if (valIsNaN) {
              var setLow = retHighest || othIsReflexive;
            } else if (valIsUndefined) {
              setLow = othIsReflexive && (retHighest || othIsDefined);
            } else if (valIsNull) {
              setLow = othIsReflexive && othIsDefined && (retHighest || !othIsNull);
            } else if (valIsSymbol) {
              setLow = othIsReflexive && othIsDefined && !othIsNull && (retHighest || !othIsSymbol);
            } else if (othIsNull || othIsSymbol) {
              setLow = false;
            } else {
              setLow = retHighest ? computed <= value : computed < value;
            }
            if (setLow) {
              low = mid + 1;
            } else {
              high = mid;
            }
          }
          return nativeMin(high, MAX_ARRAY_INDEX);
        }
        function baseSortedUniq(array, iteratee2) {
          var index = -1, length = array.length, resIndex = 0, result2 = [];
          while (++index < length) {
            var value = array[index], computed = iteratee2 ? iteratee2(value) : value;
            if (!index || !eq(computed, seen)) {
              var seen = computed;
              result2[resIndex++] = value === 0 ? 0 : value;
            }
          }
          return result2;
        }
        function baseToNumber(value) {
          if (typeof value == "number") {
            return value;
          }
          if (isSymbol(value)) {
            return NAN;
          }
          return +value;
        }
        function baseToString(value) {
          if (typeof value == "string") {
            return value;
          }
          if (isArray3(value)) {
            return arrayMap(value, baseToString) + "";
          }
          if (isSymbol(value)) {
            return symbolToString ? symbolToString.call(value) : "";
          }
          var result2 = value + "";
          return result2 == "0" && 1 / value == -INFINITY ? "-0" : result2;
        }
        function baseUniq(array, iteratee2, comparator) {
          var index = -1, includes2 = arrayIncludes, length = array.length, isCommon = true, result2 = [], seen = result2;
          if (comparator) {
            isCommon = false;
            includes2 = arrayIncludesWith;
          } else if (length >= LARGE_ARRAY_SIZE) {
            var set4 = iteratee2 ? null : createSet(array);
            if (set4) {
              return setToArray(set4);
            }
            isCommon = false;
            includes2 = cacheHas;
            seen = new SetCache();
          } else {
            seen = iteratee2 ? [] : result2;
          }
          outer:
            while (++index < length) {
              var value = array[index], computed = iteratee2 ? iteratee2(value) : value;
              value = comparator || value !== 0 ? value : 0;
              if (isCommon && computed === computed) {
                var seenIndex = seen.length;
                while (seenIndex--) {
                  if (seen[seenIndex] === computed) {
                    continue outer;
                  }
                }
                if (iteratee2) {
                  seen.push(computed);
                }
                result2.push(value);
              } else if (!includes2(seen, computed, comparator)) {
                if (seen !== result2) {
                  seen.push(computed);
                }
                result2.push(value);
              }
            }
          return result2;
        }
        function baseUnset(object, path) {
          path = castPath(path, object);
          object = parent(object, path);
          return object == null || delete object[toKey(last(path))];
        }
        function baseUpdate(object, path, updater, customizer) {
          return baseSet(object, path, updater(baseGet(object, path)), customizer);
        }
        function baseWhile(array, predicate, isDrop, fromRight) {
          var length = array.length, index = fromRight ? length : -1;
          while ((fromRight ? index-- : ++index < length) && predicate(array[index], index, array)) {
          }
          return isDrop ? baseSlice(array, fromRight ? 0 : index, fromRight ? index + 1 : length) : baseSlice(array, fromRight ? index + 1 : 0, fromRight ? length : index);
        }
        function baseWrapperValue(value, actions) {
          var result2 = value;
          if (result2 instanceof LazyWrapper) {
            result2 = result2.value();
          }
          return arrayReduce(actions, function(result3, action) {
            return action.func.apply(action.thisArg, arrayPush([result3], action.args));
          }, result2);
        }
        function baseXor(arrays, iteratee2, comparator) {
          var length = arrays.length;
          if (length < 2) {
            return length ? baseUniq(arrays[0]) : [];
          }
          var index = -1, result2 = Array2(length);
          while (++index < length) {
            var array = arrays[index], othIndex = -1;
            while (++othIndex < length) {
              if (othIndex != index) {
                result2[index] = baseDifference(result2[index] || array, arrays[othIndex], iteratee2, comparator);
              }
            }
          }
          return baseUniq(baseFlatten(result2, 1), iteratee2, comparator);
        }
        function baseZipObject(props, values2, assignFunc) {
          var index = -1, length = props.length, valsLength = values2.length, result2 = {};
          while (++index < length) {
            var value = index < valsLength ? values2[index] : undefined2;
            assignFunc(result2, props[index], value);
          }
          return result2;
        }
        function castArrayLikeObject(value) {
          return isArrayLikeObject(value) ? value : [];
        }
        function castFunction(value) {
          return typeof value == "function" ? value : identity;
        }
        function castPath(value, object) {
          if (isArray3(value)) {
            return value;
          }
          return isKey(value, object) ? [value] : stringToPath(toString(value));
        }
        var castRest = baseRest;
        function castSlice(array, start, end) {
          var length = array.length;
          end = end === undefined2 ? length : end;
          return !start && end >= length ? array : baseSlice(array, start, end);
        }
        var clearTimeout2 = ctxClearTimeout || function(id) {
          return root.clearTimeout(id);
        };
        function cloneBuffer(buffer, isDeep) {
          if (isDeep) {
            return buffer.slice();
          }
          var length = buffer.length, result2 = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);
          buffer.copy(result2);
          return result2;
        }
        function cloneArrayBuffer(arrayBuffer) {
          var result2 = new arrayBuffer.constructor(arrayBuffer.byteLength);
          new Uint8Array2(result2).set(new Uint8Array2(arrayBuffer));
          return result2;
        }
        function cloneDataView(dataView, isDeep) {
          var buffer = isDeep ? cloneArrayBuffer(dataView.buffer) : dataView.buffer;
          return new dataView.constructor(buffer, dataView.byteOffset, dataView.byteLength);
        }
        function cloneRegExp(regexp) {
          var result2 = new regexp.constructor(regexp.source, reFlags.exec(regexp));
          result2.lastIndex = regexp.lastIndex;
          return result2;
        }
        function cloneSymbol(symbol) {
          return symbolValueOf ? Object2(symbolValueOf.call(symbol)) : {};
        }
        function cloneTypedArray(typedArray, isDeep) {
          var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
          return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
        }
        function compareAscending(value, other) {
          if (value !== other) {
            var valIsDefined = value !== undefined2, valIsNull = value === null, valIsReflexive = value === value, valIsSymbol = isSymbol(value);
            var othIsDefined = other !== undefined2, othIsNull = other === null, othIsReflexive = other === other, othIsSymbol = isSymbol(other);
            if (!othIsNull && !othIsSymbol && !valIsSymbol && value > other || valIsSymbol && othIsDefined && othIsReflexive && !othIsNull && !othIsSymbol || valIsNull && othIsDefined && othIsReflexive || !valIsDefined && othIsReflexive || !valIsReflexive) {
              return 1;
            }
            if (!valIsNull && !valIsSymbol && !othIsSymbol && value < other || othIsSymbol && valIsDefined && valIsReflexive && !valIsNull && !valIsSymbol || othIsNull && valIsDefined && valIsReflexive || !othIsDefined && valIsReflexive || !othIsReflexive) {
              return -1;
            }
          }
          return 0;
        }
        function compareMultiple(object, other, orders) {
          var index = -1, objCriteria = object.criteria, othCriteria = other.criteria, length = objCriteria.length, ordersLength = orders.length;
          while (++index < length) {
            var result2 = compareAscending(objCriteria[index], othCriteria[index]);
            if (result2) {
              if (index >= ordersLength) {
                return result2;
              }
              var order = orders[index];
              return result2 * (order == "desc" ? -1 : 1);
            }
          }
          return object.index - other.index;
        }
        function composeArgs(args, partials, holders, isCurried) {
          var argsIndex = -1, argsLength = args.length, holdersLength = holders.length, leftIndex = -1, leftLength = partials.length, rangeLength = nativeMax(argsLength - holdersLength, 0), result2 = Array2(leftLength + rangeLength), isUncurried = !isCurried;
          while (++leftIndex < leftLength) {
            result2[leftIndex] = partials[leftIndex];
          }
          while (++argsIndex < holdersLength) {
            if (isUncurried || argsIndex < argsLength) {
              result2[holders[argsIndex]] = args[argsIndex];
            }
          }
          while (rangeLength--) {
            result2[leftIndex++] = args[argsIndex++];
          }
          return result2;
        }
        function composeArgsRight(args, partials, holders, isCurried) {
          var argsIndex = -1, argsLength = args.length, holdersIndex = -1, holdersLength = holders.length, rightIndex = -1, rightLength = partials.length, rangeLength = nativeMax(argsLength - holdersLength, 0), result2 = Array2(rangeLength + rightLength), isUncurried = !isCurried;
          while (++argsIndex < rangeLength) {
            result2[argsIndex] = args[argsIndex];
          }
          var offset = argsIndex;
          while (++rightIndex < rightLength) {
            result2[offset + rightIndex] = partials[rightIndex];
          }
          while (++holdersIndex < holdersLength) {
            if (isUncurried || argsIndex < argsLength) {
              result2[offset + holders[holdersIndex]] = args[argsIndex++];
            }
          }
          return result2;
        }
        function copyArray(source, array) {
          var index = -1, length = source.length;
          array || (array = Array2(length));
          while (++index < length) {
            array[index] = source[index];
          }
          return array;
        }
        function copyObject(source, props, object, customizer) {
          var isNew = !object;
          object || (object = {});
          var index = -1, length = props.length;
          while (++index < length) {
            var key = props[index];
            var newValue = customizer ? customizer(object[key], source[key], key, object, source) : undefined2;
            if (newValue === undefined2) {
              newValue = source[key];
            }
            if (isNew) {
              baseAssignValue(object, key, newValue);
            } else {
              assignValue(object, key, newValue);
            }
          }
          return object;
        }
        function copySymbols(source, object) {
          return copyObject(source, getSymbols(source), object);
        }
        function copySymbolsIn(source, object) {
          return copyObject(source, getSymbolsIn(source), object);
        }
        function createAggregator(setter, initializer) {
          return function(collection, iteratee2) {
            var func = isArray3(collection) ? arrayAggregator : baseAggregator, accumulator = initializer ? initializer() : {};
            return func(collection, setter, getIteratee(iteratee2, 2), accumulator);
          };
        }
        function createAssigner(assigner) {
          return baseRest(function(object, sources) {
            var index = -1, length = sources.length, customizer = length > 1 ? sources[length - 1] : undefined2, guard = length > 2 ? sources[2] : undefined2;
            customizer = assigner.length > 3 && typeof customizer == "function" ? (length--, customizer) : undefined2;
            if (guard && isIterateeCall(sources[0], sources[1], guard)) {
              customizer = length < 3 ? undefined2 : customizer;
              length = 1;
            }
            object = Object2(object);
            while (++index < length) {
              var source = sources[index];
              if (source) {
                assigner(object, source, index, customizer);
              }
            }
            return object;
          });
        }
        function createBaseEach(eachFunc, fromRight) {
          return function(collection, iteratee2) {
            if (collection == null) {
              return collection;
            }
            if (!isArrayLike(collection)) {
              return eachFunc(collection, iteratee2);
            }
            var length = collection.length, index = fromRight ? length : -1, iterable = Object2(collection);
            while (fromRight ? index-- : ++index < length) {
              if (iteratee2(iterable[index], index, iterable) === false) {
                break;
              }
            }
            return collection;
          };
        }
        function createBaseFor(fromRight) {
          return function(object, iteratee2, keysFunc) {
            var index = -1, iterable = Object2(object), props = keysFunc(object), length = props.length;
            while (length--) {
              var key = props[fromRight ? length : ++index];
              if (iteratee2(iterable[key], key, iterable) === false) {
                break;
              }
            }
            return object;
          };
        }
        function createBind(func, bitmask, thisArg) {
          var isBind = bitmask & WRAP_BIND_FLAG, Ctor = createCtor(func);
          function wrapper() {
            var fn = this && this !== root && this instanceof wrapper ? Ctor : func;
            return fn.apply(isBind ? thisArg : this, arguments);
          }
          return wrapper;
        }
        function createCaseFirst(methodName) {
          return function(string) {
            string = toString(string);
            var strSymbols = hasUnicode(string) ? stringToArray(string) : undefined2;
            var chr = strSymbols ? strSymbols[0] : string.charAt(0);
            var trailing = strSymbols ? castSlice(strSymbols, 1).join("") : string.slice(1);
            return chr[methodName]() + trailing;
          };
        }
        function createCompounder(callback) {
          return function(string) {
            return arrayReduce(words(deburr(string).replace(reApos, "")), callback, "");
          };
        }
        function createCtor(Ctor) {
          return function() {
            var args = arguments;
            switch (args.length) {
              case 0:
                return new Ctor();
              case 1:
                return new Ctor(args[0]);
              case 2:
                return new Ctor(args[0], args[1]);
              case 3:
                return new Ctor(args[0], args[1], args[2]);
              case 4:
                return new Ctor(args[0], args[1], args[2], args[3]);
              case 5:
                return new Ctor(args[0], args[1], args[2], args[3], args[4]);
              case 6:
                return new Ctor(args[0], args[1], args[2], args[3], args[4], args[5]);
              case 7:
                return new Ctor(args[0], args[1], args[2], args[3], args[4], args[5], args[6]);
            }
            var thisBinding = baseCreate(Ctor.prototype), result2 = Ctor.apply(thisBinding, args);
            return isObject4(result2) ? result2 : thisBinding;
          };
        }
        function createCurry(func, bitmask, arity) {
          var Ctor = createCtor(func);
          function wrapper() {
            var length = arguments.length, args = Array2(length), index = length, placeholder = getHolder(wrapper);
            while (index--) {
              args[index] = arguments[index];
            }
            var holders = length < 3 && args[0] !== placeholder && args[length - 1] !== placeholder ? [] : replaceHolders(args, placeholder);
            length -= holders.length;
            if (length < arity) {
              return createRecurry(
                func,
                bitmask,
                createHybrid,
                wrapper.placeholder,
                undefined2,
                args,
                holders,
                undefined2,
                undefined2,
                arity - length
              );
            }
            var fn = this && this !== root && this instanceof wrapper ? Ctor : func;
            return apply(fn, this, args);
          }
          return wrapper;
        }
        function createFind(findIndexFunc) {
          return function(collection, predicate, fromIndex) {
            var iterable = Object2(collection);
            if (!isArrayLike(collection)) {
              var iteratee2 = getIteratee(predicate, 3);
              collection = keys(collection);
              predicate = function(key) {
                return iteratee2(iterable[key], key, iterable);
              };
            }
            var index = findIndexFunc(collection, predicate, fromIndex);
            return index > -1 ? iterable[iteratee2 ? collection[index] : index] : undefined2;
          };
        }
        function createFlow(fromRight) {
          return flatRest(function(funcs) {
            var length = funcs.length, index = length, prereq = LodashWrapper.prototype.thru;
            if (fromRight) {
              funcs.reverse();
            }
            while (index--) {
              var func = funcs[index];
              if (typeof func != "function") {
                throw new TypeError2(FUNC_ERROR_TEXT);
              }
              if (prereq && !wrapper && getFuncName(func) == "wrapper") {
                var wrapper = new LodashWrapper([], true);
              }
            }
            index = wrapper ? index : length;
            while (++index < length) {
              func = funcs[index];
              var funcName = getFuncName(func), data = funcName == "wrapper" ? getData(func) : undefined2;
              if (data && isLaziable(data[0]) && data[1] == (WRAP_ARY_FLAG | WRAP_CURRY_FLAG | WRAP_PARTIAL_FLAG | WRAP_REARG_FLAG) && !data[4].length && data[9] == 1) {
                wrapper = wrapper[getFuncName(data[0])].apply(wrapper, data[3]);
              } else {
                wrapper = func.length == 1 && isLaziable(func) ? wrapper[funcName]() : wrapper.thru(func);
              }
            }
            return function() {
              var args = arguments, value = args[0];
              if (wrapper && args.length == 1 && isArray3(value)) {
                return wrapper.plant(value).value();
              }
              var index2 = 0, result2 = length ? funcs[index2].apply(this, args) : value;
              while (++index2 < length) {
                result2 = funcs[index2].call(this, result2);
              }
              return result2;
            };
          });
        }
        function createHybrid(func, bitmask, thisArg, partials, holders, partialsRight, holdersRight, argPos, ary2, arity) {
          var isAry = bitmask & WRAP_ARY_FLAG, isBind = bitmask & WRAP_BIND_FLAG, isBindKey = bitmask & WRAP_BIND_KEY_FLAG, isCurried = bitmask & (WRAP_CURRY_FLAG | WRAP_CURRY_RIGHT_FLAG), isFlip = bitmask & WRAP_FLIP_FLAG, Ctor = isBindKey ? undefined2 : createCtor(func);
          function wrapper() {
            var length = arguments.length, args = Array2(length), index = length;
            while (index--) {
              args[index] = arguments[index];
            }
            if (isCurried) {
              var placeholder = getHolder(wrapper), holdersCount = countHolders(args, placeholder);
            }
            if (partials) {
              args = composeArgs(args, partials, holders, isCurried);
            }
            if (partialsRight) {
              args = composeArgsRight(args, partialsRight, holdersRight, isCurried);
            }
            length -= holdersCount;
            if (isCurried && length < arity) {
              var newHolders = replaceHolders(args, placeholder);
              return createRecurry(
                func,
                bitmask,
                createHybrid,
                wrapper.placeholder,
                thisArg,
                args,
                newHolders,
                argPos,
                ary2,
                arity - length
              );
            }
            var thisBinding = isBind ? thisArg : this, fn = isBindKey ? thisBinding[func] : func;
            length = args.length;
            if (argPos) {
              args = reorder(args, argPos);
            } else if (isFlip && length > 1) {
              args.reverse();
            }
            if (isAry && ary2 < length) {
              args.length = ary2;
            }
            if (this && this !== root && this instanceof wrapper) {
              fn = Ctor || createCtor(fn);
            }
            return fn.apply(thisBinding, args);
          }
          return wrapper;
        }
        function createInverter(setter, toIteratee) {
          return function(object, iteratee2) {
            return baseInverter(object, setter, toIteratee(iteratee2), {});
          };
        }
        function createMathOperation(operator, defaultValue) {
          return function(value, other) {
            var result2;
            if (value === undefined2 && other === undefined2) {
              return defaultValue;
            }
            if (value !== undefined2) {
              result2 = value;
            }
            if (other !== undefined2) {
              if (result2 === undefined2) {
                return other;
              }
              if (typeof value == "string" || typeof other == "string") {
                value = baseToString(value);
                other = baseToString(other);
              } else {
                value = baseToNumber(value);
                other = baseToNumber(other);
              }
              result2 = operator(value, other);
            }
            return result2;
          };
        }
        function createOver(arrayFunc) {
          return flatRest(function(iteratees) {
            iteratees = arrayMap(iteratees, baseUnary(getIteratee()));
            return baseRest(function(args) {
              var thisArg = this;
              return arrayFunc(iteratees, function(iteratee2) {
                return apply(iteratee2, thisArg, args);
              });
            });
          });
        }
        function createPadding(length, chars) {
          chars = chars === undefined2 ? " " : baseToString(chars);
          var charsLength = chars.length;
          if (charsLength < 2) {
            return charsLength ? baseRepeat(chars, length) : chars;
          }
          var result2 = baseRepeat(chars, nativeCeil(length / stringSize(chars)));
          return hasUnicode(chars) ? castSlice(stringToArray(result2), 0, length).join("") : result2.slice(0, length);
        }
        function createPartial(func, bitmask, thisArg, partials) {
          var isBind = bitmask & WRAP_BIND_FLAG, Ctor = createCtor(func);
          function wrapper() {
            var argsIndex = -1, argsLength = arguments.length, leftIndex = -1, leftLength = partials.length, args = Array2(leftLength + argsLength), fn = this && this !== root && this instanceof wrapper ? Ctor : func;
            while (++leftIndex < leftLength) {
              args[leftIndex] = partials[leftIndex];
            }
            while (argsLength--) {
              args[leftIndex++] = arguments[++argsIndex];
            }
            return apply(fn, isBind ? thisArg : this, args);
          }
          return wrapper;
        }
        function createRange(fromRight) {
          return function(start, end, step) {
            if (step && typeof step != "number" && isIterateeCall(start, end, step)) {
              end = step = undefined2;
            }
            start = toFinite(start);
            if (end === undefined2) {
              end = start;
              start = 0;
            } else {
              end = toFinite(end);
            }
            step = step === undefined2 ? start < end ? 1 : -1 : toFinite(step);
            return baseRange(start, end, step, fromRight);
          };
        }
        function createRelationalOperation(operator) {
          return function(value, other) {
            if (!(typeof value == "string" && typeof other == "string")) {
              value = toNumber(value);
              other = toNumber(other);
            }
            return operator(value, other);
          };
        }
        function createRecurry(func, bitmask, wrapFunc, placeholder, thisArg, partials, holders, argPos, ary2, arity) {
          var isCurry = bitmask & WRAP_CURRY_FLAG, newHolders = isCurry ? holders : undefined2, newHoldersRight = isCurry ? undefined2 : holders, newPartials = isCurry ? partials : undefined2, newPartialsRight = isCurry ? undefined2 : partials;
          bitmask |= isCurry ? WRAP_PARTIAL_FLAG : WRAP_PARTIAL_RIGHT_FLAG;
          bitmask &= ~(isCurry ? WRAP_PARTIAL_RIGHT_FLAG : WRAP_PARTIAL_FLAG);
          if (!(bitmask & WRAP_CURRY_BOUND_FLAG)) {
            bitmask &= ~(WRAP_BIND_FLAG | WRAP_BIND_KEY_FLAG);
          }
          var newData = [
            func,
            bitmask,
            thisArg,
            newPartials,
            newHolders,
            newPartialsRight,
            newHoldersRight,
            argPos,
            ary2,
            arity
          ];
          var result2 = wrapFunc.apply(undefined2, newData);
          if (isLaziable(func)) {
            setData(result2, newData);
          }
          result2.placeholder = placeholder;
          return setWrapToString(result2, func, bitmask);
        }
        function createRound(methodName) {
          var func = Math2[methodName];
          return function(number, precision) {
            number = toNumber(number);
            precision = precision == null ? 0 : nativeMin(toInteger(precision), 292);
            if (precision && nativeIsFinite(number)) {
              var pair = (toString(number) + "e").split("e"), value = func(pair[0] + "e" + (+pair[1] + precision));
              pair = (toString(value) + "e").split("e");
              return +(pair[0] + "e" + (+pair[1] - precision));
            }
            return func(number);
          };
        }
        var createSet = !(Set2 && 1 / setToArray(new Set2([, -0]))[1] == INFINITY) ? noop : function(values2) {
          return new Set2(values2);
        };
        function createToPairs(keysFunc) {
          return function(object) {
            var tag = getTag(object);
            if (tag == mapTag) {
              return mapToArray(object);
            }
            if (tag == setTag) {
              return setToPairs(object);
            }
            return baseToPairs(object, keysFunc(object));
          };
        }
        function createWrap(func, bitmask, thisArg, partials, holders, argPos, ary2, arity) {
          var isBindKey = bitmask & WRAP_BIND_KEY_FLAG;
          if (!isBindKey && typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          var length = partials ? partials.length : 0;
          if (!length) {
            bitmask &= ~(WRAP_PARTIAL_FLAG | WRAP_PARTIAL_RIGHT_FLAG);
            partials = holders = undefined2;
          }
          ary2 = ary2 === undefined2 ? ary2 : nativeMax(toInteger(ary2), 0);
          arity = arity === undefined2 ? arity : toInteger(arity);
          length -= holders ? holders.length : 0;
          if (bitmask & WRAP_PARTIAL_RIGHT_FLAG) {
            var partialsRight = partials, holdersRight = holders;
            partials = holders = undefined2;
          }
          var data = isBindKey ? undefined2 : getData(func);
          var newData = [
            func,
            bitmask,
            thisArg,
            partials,
            holders,
            partialsRight,
            holdersRight,
            argPos,
            ary2,
            arity
          ];
          if (data) {
            mergeData(newData, data);
          }
          func = newData[0];
          bitmask = newData[1];
          thisArg = newData[2];
          partials = newData[3];
          holders = newData[4];
          arity = newData[9] = newData[9] === undefined2 ? isBindKey ? 0 : func.length : nativeMax(newData[9] - length, 0);
          if (!arity && bitmask & (WRAP_CURRY_FLAG | WRAP_CURRY_RIGHT_FLAG)) {
            bitmask &= ~(WRAP_CURRY_FLAG | WRAP_CURRY_RIGHT_FLAG);
          }
          if (!bitmask || bitmask == WRAP_BIND_FLAG) {
            var result2 = createBind(func, bitmask, thisArg);
          } else if (bitmask == WRAP_CURRY_FLAG || bitmask == WRAP_CURRY_RIGHT_FLAG) {
            result2 = createCurry(func, bitmask, arity);
          } else if ((bitmask == WRAP_PARTIAL_FLAG || bitmask == (WRAP_BIND_FLAG | WRAP_PARTIAL_FLAG)) && !holders.length) {
            result2 = createPartial(func, bitmask, thisArg, partials);
          } else {
            result2 = createHybrid.apply(undefined2, newData);
          }
          var setter = data ? baseSetData : setData;
          return setWrapToString(setter(result2, newData), func, bitmask);
        }
        function customDefaultsAssignIn(objValue, srcValue, key, object) {
          if (objValue === undefined2 || eq(objValue, objectProto[key]) && !hasOwnProperty.call(object, key)) {
            return srcValue;
          }
          return objValue;
        }
        function customDefaultsMerge(objValue, srcValue, key, object, source, stack) {
          if (isObject4(objValue) && isObject4(srcValue)) {
            stack.set(srcValue, objValue);
            baseMerge(objValue, srcValue, undefined2, customDefaultsMerge, stack);
            stack["delete"](srcValue);
          }
          return objValue;
        }
        function customOmitClone(value) {
          return isPlainObject(value) ? undefined2 : value;
        }
        function equalArrays(array, other, bitmask, customizer, equalFunc, stack) {
          var isPartial = bitmask & COMPARE_PARTIAL_FLAG, arrLength = array.length, othLength = other.length;
          if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
            return false;
          }
          var arrStacked = stack.get(array);
          var othStacked = stack.get(other);
          if (arrStacked && othStacked) {
            return arrStacked == other && othStacked == array;
          }
          var index = -1, result2 = true, seen = bitmask & COMPARE_UNORDERED_FLAG ? new SetCache() : undefined2;
          stack.set(array, other);
          stack.set(other, array);
          while (++index < arrLength) {
            var arrValue = array[index], othValue = other[index];
            if (customizer) {
              var compared = isPartial ? customizer(othValue, arrValue, index, other, array, stack) : customizer(arrValue, othValue, index, array, other, stack);
            }
            if (compared !== undefined2) {
              if (compared) {
                continue;
              }
              result2 = false;
              break;
            }
            if (seen) {
              if (!arraySome(other, function(othValue2, othIndex) {
                if (!cacheHas(seen, othIndex) && (arrValue === othValue2 || equalFunc(arrValue, othValue2, bitmask, customizer, stack))) {
                  return seen.push(othIndex);
                }
              })) {
                result2 = false;
                break;
              }
            } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
              result2 = false;
              break;
            }
          }
          stack["delete"](array);
          stack["delete"](other);
          return result2;
        }
        function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
          switch (tag) {
            case dataViewTag:
              if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
                return false;
              }
              object = object.buffer;
              other = other.buffer;
            case arrayBufferTag:
              if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array2(object), new Uint8Array2(other))) {
                return false;
              }
              return true;
            case boolTag:
            case dateTag:
            case numberTag:
              return eq(+object, +other);
            case errorTag:
              return object.name == other.name && object.message == other.message;
            case regexpTag:
            case stringTag:
              return object == other + "";
            case mapTag:
              var convert = mapToArray;
            case setTag:
              var isPartial = bitmask & COMPARE_PARTIAL_FLAG;
              convert || (convert = setToArray);
              if (object.size != other.size && !isPartial) {
                return false;
              }
              var stacked = stack.get(object);
              if (stacked) {
                return stacked == other;
              }
              bitmask |= COMPARE_UNORDERED_FLAG;
              stack.set(object, other);
              var result2 = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
              stack["delete"](object);
              return result2;
            case symbolTag:
              if (symbolValueOf) {
                return symbolValueOf.call(object) == symbolValueOf.call(other);
              }
          }
          return false;
        }
        function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
          var isPartial = bitmask & COMPARE_PARTIAL_FLAG, objProps = getAllKeys(object), objLength = objProps.length, othProps = getAllKeys(other), othLength = othProps.length;
          if (objLength != othLength && !isPartial) {
            return false;
          }
          var index = objLength;
          while (index--) {
            var key = objProps[index];
            if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) {
              return false;
            }
          }
          var objStacked = stack.get(object);
          var othStacked = stack.get(other);
          if (objStacked && othStacked) {
            return objStacked == other && othStacked == object;
          }
          var result2 = true;
          stack.set(object, other);
          stack.set(other, object);
          var skipCtor = isPartial;
          while (++index < objLength) {
            key = objProps[index];
            var objValue = object[key], othValue = other[key];
            if (customizer) {
              var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
            }
            if (!(compared === undefined2 ? objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack) : compared)) {
              result2 = false;
              break;
            }
            skipCtor || (skipCtor = key == "constructor");
          }
          if (result2 && !skipCtor) {
            var objCtor = object.constructor, othCtor = other.constructor;
            if (objCtor != othCtor && ("constructor" in object && "constructor" in other) && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) {
              result2 = false;
            }
          }
          stack["delete"](object);
          stack["delete"](other);
          return result2;
        }
        function flatRest(func) {
          return setToString(overRest(func, undefined2, flatten), func + "");
        }
        function getAllKeys(object) {
          return baseGetAllKeys(object, keys, getSymbols);
        }
        function getAllKeysIn(object) {
          return baseGetAllKeys(object, keysIn, getSymbolsIn);
        }
        var getData = !metaMap ? noop : function(func) {
          return metaMap.get(func);
        };
        function getFuncName(func) {
          var result2 = func.name + "", array = realNames[result2], length = hasOwnProperty.call(realNames, result2) ? array.length : 0;
          while (length--) {
            var data = array[length], otherFunc = data.func;
            if (otherFunc == null || otherFunc == func) {
              return data.name;
            }
          }
          return result2;
        }
        function getHolder(func) {
          var object = hasOwnProperty.call(lodash, "placeholder") ? lodash : func;
          return object.placeholder;
        }
        function getIteratee() {
          var result2 = lodash.iteratee || iteratee;
          result2 = result2 === iteratee ? baseIteratee : result2;
          return arguments.length ? result2(arguments[0], arguments[1]) : result2;
        }
        function getMapData(map2, key) {
          var data = map2.__data__;
          return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
        }
        function getMatchData(object) {
          var result2 = keys(object), length = result2.length;
          while (length--) {
            var key = result2[length], value = object[key];
            result2[length] = [key, value, isStrictComparable(value)];
          }
          return result2;
        }
        function getNative(object, key) {
          var value = getValue(object, key);
          return baseIsNative(value) ? value : undefined2;
        }
        function getRawTag(value) {
          var isOwn = hasOwnProperty.call(value, symToStringTag), tag = value[symToStringTag];
          try {
            value[symToStringTag] = undefined2;
            var unmasked = true;
          } catch (e) {
          }
          var result2 = nativeObjectToString.call(value);
          if (unmasked) {
            if (isOwn) {
              value[symToStringTag] = tag;
            } else {
              delete value[symToStringTag];
            }
          }
          return result2;
        }
        var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
          if (object == null) {
            return [];
          }
          object = Object2(object);
          return arrayFilter(nativeGetSymbols(object), function(symbol) {
            return propertyIsEnumerable.call(object, symbol);
          });
        };
        var getSymbolsIn = !nativeGetSymbols ? stubArray : function(object) {
          var result2 = [];
          while (object) {
            arrayPush(result2, getSymbols(object));
            object = getPrototype(object);
          }
          return result2;
        };
        var getTag = baseGetTag;
        if (DataView && getTag(new DataView(new ArrayBuffer(1))) != dataViewTag || Map2 && getTag(new Map2()) != mapTag || Promise2 && getTag(Promise2.resolve()) != promiseTag || Set2 && getTag(new Set2()) != setTag || WeakMap && getTag(new WeakMap()) != weakMapTag) {
          getTag = function(value) {
            var result2 = baseGetTag(value), Ctor = result2 == objectTag ? value.constructor : undefined2, ctorString = Ctor ? toSource(Ctor) : "";
            if (ctorString) {
              switch (ctorString) {
                case dataViewCtorString:
                  return dataViewTag;
                case mapCtorString:
                  return mapTag;
                case promiseCtorString:
                  return promiseTag;
                case setCtorString:
                  return setTag;
                case weakMapCtorString:
                  return weakMapTag;
              }
            }
            return result2;
          };
        }
        function getView(start, end, transforms) {
          var index = -1, length = transforms.length;
          while (++index < length) {
            var data = transforms[index], size2 = data.size;
            switch (data.type) {
              case "drop":
                start += size2;
                break;
              case "dropRight":
                end -= size2;
                break;
              case "take":
                end = nativeMin(end, start + size2);
                break;
              case "takeRight":
                start = nativeMax(start, end - size2);
                break;
            }
          }
          return { "start": start, "end": end };
        }
        function getWrapDetails(source) {
          var match = source.match(reWrapDetails);
          return match ? match[1].split(reSplitDetails) : [];
        }
        function hasPath(object, path, hasFunc) {
          path = castPath(path, object);
          var index = -1, length = path.length, result2 = false;
          while (++index < length) {
            var key = toKey(path[index]);
            if (!(result2 = object != null && hasFunc(object, key))) {
              break;
            }
            object = object[key];
          }
          if (result2 || ++index != length) {
            return result2;
          }
          length = object == null ? 0 : object.length;
          return !!length && isLength(length) && isIndex(key, length) && (isArray3(object) || isArguments(object));
        }
        function initCloneArray(array) {
          var length = array.length, result2 = new array.constructor(length);
          if (length && typeof array[0] == "string" && hasOwnProperty.call(array, "index")) {
            result2.index = array.index;
            result2.input = array.input;
          }
          return result2;
        }
        function initCloneObject(object) {
          return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
        }
        function initCloneByTag(object, tag, isDeep) {
          var Ctor = object.constructor;
          switch (tag) {
            case arrayBufferTag:
              return cloneArrayBuffer(object);
            case boolTag:
            case dateTag:
              return new Ctor(+object);
            case dataViewTag:
              return cloneDataView(object, isDeep);
            case float32Tag:
            case float64Tag:
            case int8Tag:
            case int16Tag:
            case int32Tag:
            case uint8Tag:
            case uint8ClampedTag:
            case uint16Tag:
            case uint32Tag:
              return cloneTypedArray(object, isDeep);
            case mapTag:
              return new Ctor();
            case numberTag:
            case stringTag:
              return new Ctor(object);
            case regexpTag:
              return cloneRegExp(object);
            case setTag:
              return new Ctor();
            case symbolTag:
              return cloneSymbol(object);
          }
        }
        function insertWrapDetails(source, details) {
          var length = details.length;
          if (!length) {
            return source;
          }
          var lastIndex = length - 1;
          details[lastIndex] = (length > 1 ? "& " : "") + details[lastIndex];
          details = details.join(length > 2 ? ", " : " ");
          return source.replace(reWrapComment, "{\n/* [wrapped with " + details + "] */\n");
        }
        function isFlattenable(value) {
          return isArray3(value) || isArguments(value) || !!(spreadableSymbol && value && value[spreadableSymbol]);
        }
        function isIndex(value, length) {
          var type = typeof value;
          length = length == null ? MAX_SAFE_INTEGER : length;
          return !!length && (type == "number" || type != "symbol" && reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length);
        }
        function isIterateeCall(value, index, object) {
          if (!isObject4(object)) {
            return false;
          }
          var type = typeof index;
          if (type == "number" ? isArrayLike(object) && isIndex(index, object.length) : type == "string" && index in object) {
            return eq(object[index], value);
          }
          return false;
        }
        function isKey(value, object) {
          if (isArray3(value)) {
            return false;
          }
          var type = typeof value;
          if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
            return true;
          }
          return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object2(object);
        }
        function isKeyable(value) {
          var type = typeof value;
          return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
        }
        function isLaziable(func) {
          var funcName = getFuncName(func), other = lodash[funcName];
          if (typeof other != "function" || !(funcName in LazyWrapper.prototype)) {
            return false;
          }
          if (func === other) {
            return true;
          }
          var data = getData(other);
          return !!data && func === data[0];
        }
        function isMasked(func) {
          return !!maskSrcKey && maskSrcKey in func;
        }
        var isMaskable = coreJsData ? isFunction2 : stubFalse;
        function isPrototype(value) {
          var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
          return value === proto;
        }
        function isStrictComparable(value) {
          return value === value && !isObject4(value);
        }
        function matchesStrictComparable(key, srcValue) {
          return function(object) {
            if (object == null) {
              return false;
            }
            return object[key] === srcValue && (srcValue !== undefined2 || key in Object2(object));
          };
        }
        function memoizeCapped(func) {
          var result2 = memoize(func, function(key) {
            if (cache.size === MAX_MEMOIZE_SIZE) {
              cache.clear();
            }
            return key;
          });
          var cache = result2.cache;
          return result2;
        }
        function mergeData(data, source) {
          var bitmask = data[1], srcBitmask = source[1], newBitmask = bitmask | srcBitmask, isCommon = newBitmask < (WRAP_BIND_FLAG | WRAP_BIND_KEY_FLAG | WRAP_ARY_FLAG);
          var isCombo = srcBitmask == WRAP_ARY_FLAG && bitmask == WRAP_CURRY_FLAG || srcBitmask == WRAP_ARY_FLAG && bitmask == WRAP_REARG_FLAG && data[7].length <= source[8] || srcBitmask == (WRAP_ARY_FLAG | WRAP_REARG_FLAG) && source[7].length <= source[8] && bitmask == WRAP_CURRY_FLAG;
          if (!(isCommon || isCombo)) {
            return data;
          }
          if (srcBitmask & WRAP_BIND_FLAG) {
            data[2] = source[2];
            newBitmask |= bitmask & WRAP_BIND_FLAG ? 0 : WRAP_CURRY_BOUND_FLAG;
          }
          var value = source[3];
          if (value) {
            var partials = data[3];
            data[3] = partials ? composeArgs(partials, value, source[4]) : value;
            data[4] = partials ? replaceHolders(data[3], PLACEHOLDER) : source[4];
          }
          value = source[5];
          if (value) {
            partials = data[5];
            data[5] = partials ? composeArgsRight(partials, value, source[6]) : value;
            data[6] = partials ? replaceHolders(data[5], PLACEHOLDER) : source[6];
          }
          value = source[7];
          if (value) {
            data[7] = value;
          }
          if (srcBitmask & WRAP_ARY_FLAG) {
            data[8] = data[8] == null ? source[8] : nativeMin(data[8], source[8]);
          }
          if (data[9] == null) {
            data[9] = source[9];
          }
          data[0] = source[0];
          data[1] = newBitmask;
          return data;
        }
        function nativeKeysIn(object) {
          var result2 = [];
          if (object != null) {
            for (var key in Object2(object)) {
              result2.push(key);
            }
          }
          return result2;
        }
        function objectToString(value) {
          return nativeObjectToString.call(value);
        }
        function overRest(func, start, transform2) {
          start = nativeMax(start === undefined2 ? func.length - 1 : start, 0);
          return function() {
            var args = arguments, index = -1, length = nativeMax(args.length - start, 0), array = Array2(length);
            while (++index < length) {
              array[index] = args[start + index];
            }
            index = -1;
            var otherArgs = Array2(start + 1);
            while (++index < start) {
              otherArgs[index] = args[index];
            }
            otherArgs[start] = transform2(array);
            return apply(func, this, otherArgs);
          };
        }
        function parent(object, path) {
          return path.length < 2 ? object : baseGet(object, baseSlice(path, 0, -1));
        }
        function reorder(array, indexes) {
          var arrLength = array.length, length = nativeMin(indexes.length, arrLength), oldArray = copyArray(array);
          while (length--) {
            var index = indexes[length];
            array[length] = isIndex(index, arrLength) ? oldArray[index] : undefined2;
          }
          return array;
        }
        function safeGet(object, key) {
          if (key === "constructor" && typeof object[key] === "function") {
            return;
          }
          if (key == "__proto__") {
            return;
          }
          return object[key];
        }
        var setData = shortOut(baseSetData);
        var setTimeout2 = ctxSetTimeout || function(func, wait) {
          return root.setTimeout(func, wait);
        };
        var setToString = shortOut(baseSetToString);
        function setWrapToString(wrapper, reference, bitmask) {
          var source = reference + "";
          return setToString(wrapper, insertWrapDetails(source, updateWrapDetails(getWrapDetails(source), bitmask)));
        }
        function shortOut(func) {
          var count = 0, lastCalled = 0;
          return function() {
            var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
            lastCalled = stamp;
            if (remaining > 0) {
              if (++count >= HOT_COUNT) {
                return arguments[0];
              }
            } else {
              count = 0;
            }
            return func.apply(undefined2, arguments);
          };
        }
        function shuffleSelf(array, size2) {
          var index = -1, length = array.length, lastIndex = length - 1;
          size2 = size2 === undefined2 ? length : size2;
          while (++index < size2) {
            var rand = baseRandom(index, lastIndex), value = array[rand];
            array[rand] = array[index];
            array[index] = value;
          }
          array.length = size2;
          return array;
        }
        var stringToPath = memoizeCapped(function(string) {
          var result2 = [];
          if (string.charCodeAt(0) === 46) {
            result2.push("");
          }
          string.replace(rePropName, function(match, number, quote, subString) {
            result2.push(quote ? subString.replace(reEscapeChar, "$1") : number || match);
          });
          return result2;
        });
        function toKey(value) {
          if (typeof value == "string" || isSymbol(value)) {
            return value;
          }
          var result2 = value + "";
          return result2 == "0" && 1 / value == -INFINITY ? "-0" : result2;
        }
        function toSource(func) {
          if (func != null) {
            try {
              return funcToString.call(func);
            } catch (e) {
            }
            try {
              return func + "";
            } catch (e) {
            }
          }
          return "";
        }
        function updateWrapDetails(details, bitmask) {
          arrayEach(wrapFlags, function(pair) {
            var value = "_." + pair[0];
            if (bitmask & pair[1] && !arrayIncludes(details, value)) {
              details.push(value);
            }
          });
          return details.sort();
        }
        function wrapperClone(wrapper) {
          if (wrapper instanceof LazyWrapper) {
            return wrapper.clone();
          }
          var result2 = new LodashWrapper(wrapper.__wrapped__, wrapper.__chain__);
          result2.__actions__ = copyArray(wrapper.__actions__);
          result2.__index__ = wrapper.__index__;
          result2.__values__ = wrapper.__values__;
          return result2;
        }
        function chunk(array, size2, guard) {
          if (guard ? isIterateeCall(array, size2, guard) : size2 === undefined2) {
            size2 = 1;
          } else {
            size2 = nativeMax(toInteger(size2), 0);
          }
          var length = array == null ? 0 : array.length;
          if (!length || size2 < 1) {
            return [];
          }
          var index = 0, resIndex = 0, result2 = Array2(nativeCeil(length / size2));
          while (index < length) {
            result2[resIndex++] = baseSlice(array, index, index += size2);
          }
          return result2;
        }
        function compact(array) {
          var index = -1, length = array == null ? 0 : array.length, resIndex = 0, result2 = [];
          while (++index < length) {
            var value = array[index];
            if (value) {
              result2[resIndex++] = value;
            }
          }
          return result2;
        }
        function concat() {
          var length = arguments.length;
          if (!length) {
            return [];
          }
          var args = Array2(length - 1), array = arguments[0], index = length;
          while (index--) {
            args[index - 1] = arguments[index];
          }
          return arrayPush(isArray3(array) ? copyArray(array) : [array], baseFlatten(args, 1));
        }
        var difference = baseRest(function(array, values2) {
          return isArrayLikeObject(array) ? baseDifference(array, baseFlatten(values2, 1, isArrayLikeObject, true)) : [];
        });
        var differenceBy = baseRest(function(array, values2) {
          var iteratee2 = last(values2);
          if (isArrayLikeObject(iteratee2)) {
            iteratee2 = undefined2;
          }
          return isArrayLikeObject(array) ? baseDifference(array, baseFlatten(values2, 1, isArrayLikeObject, true), getIteratee(iteratee2, 2)) : [];
        });
        var differenceWith = baseRest(function(array, values2) {
          var comparator = last(values2);
          if (isArrayLikeObject(comparator)) {
            comparator = undefined2;
          }
          return isArrayLikeObject(array) ? baseDifference(array, baseFlatten(values2, 1, isArrayLikeObject, true), undefined2, comparator) : [];
        });
        function drop(array, n, guard) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return [];
          }
          n = guard || n === undefined2 ? 1 : toInteger(n);
          return baseSlice(array, n < 0 ? 0 : n, length);
        }
        function dropRight(array, n, guard) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return [];
          }
          n = guard || n === undefined2 ? 1 : toInteger(n);
          n = length - n;
          return baseSlice(array, 0, n < 0 ? 0 : n);
        }
        function dropRightWhile(array, predicate) {
          return array && array.length ? baseWhile(array, getIteratee(predicate, 3), true, true) : [];
        }
        function dropWhile(array, predicate) {
          return array && array.length ? baseWhile(array, getIteratee(predicate, 3), true) : [];
        }
        function fill(array, value, start, end) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return [];
          }
          if (start && typeof start != "number" && isIterateeCall(array, value, start)) {
            start = 0;
            end = length;
          }
          return baseFill(array, value, start, end);
        }
        function findIndex(array, predicate, fromIndex) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return -1;
          }
          var index = fromIndex == null ? 0 : toInteger(fromIndex);
          if (index < 0) {
            index = nativeMax(length + index, 0);
          }
          return baseFindIndex(array, getIteratee(predicate, 3), index);
        }
        function findLastIndex(array, predicate, fromIndex) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return -1;
          }
          var index = length - 1;
          if (fromIndex !== undefined2) {
            index = toInteger(fromIndex);
            index = fromIndex < 0 ? nativeMax(length + index, 0) : nativeMin(index, length - 1);
          }
          return baseFindIndex(array, getIteratee(predicate, 3), index, true);
        }
        function flatten(array) {
          var length = array == null ? 0 : array.length;
          return length ? baseFlatten(array, 1) : [];
        }
        function flattenDeep(array) {
          var length = array == null ? 0 : array.length;
          return length ? baseFlatten(array, INFINITY) : [];
        }
        function flattenDepth(array, depth) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return [];
          }
          depth = depth === undefined2 ? 1 : toInteger(depth);
          return baseFlatten(array, depth);
        }
        function fromPairs(pairs) {
          var index = -1, length = pairs == null ? 0 : pairs.length, result2 = {};
          while (++index < length) {
            var pair = pairs[index];
            result2[pair[0]] = pair[1];
          }
          return result2;
        }
        function head(array) {
          return array && array.length ? array[0] : undefined2;
        }
        function indexOf(array, value, fromIndex) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return -1;
          }
          var index = fromIndex == null ? 0 : toInteger(fromIndex);
          if (index < 0) {
            index = nativeMax(length + index, 0);
          }
          return baseIndexOf(array, value, index);
        }
        function initial(array) {
          var length = array == null ? 0 : array.length;
          return length ? baseSlice(array, 0, -1) : [];
        }
        var intersection = baseRest(function(arrays) {
          var mapped = arrayMap(arrays, castArrayLikeObject);
          return mapped.length && mapped[0] === arrays[0] ? baseIntersection(mapped) : [];
        });
        var intersectionBy = baseRest(function(arrays) {
          var iteratee2 = last(arrays), mapped = arrayMap(arrays, castArrayLikeObject);
          if (iteratee2 === last(mapped)) {
            iteratee2 = undefined2;
          } else {
            mapped.pop();
          }
          return mapped.length && mapped[0] === arrays[0] ? baseIntersection(mapped, getIteratee(iteratee2, 2)) : [];
        });
        var intersectionWith = baseRest(function(arrays) {
          var comparator = last(arrays), mapped = arrayMap(arrays, castArrayLikeObject);
          comparator = typeof comparator == "function" ? comparator : undefined2;
          if (comparator) {
            mapped.pop();
          }
          return mapped.length && mapped[0] === arrays[0] ? baseIntersection(mapped, undefined2, comparator) : [];
        });
        function join(array, separator) {
          return array == null ? "" : nativeJoin.call(array, separator);
        }
        function last(array) {
          var length = array == null ? 0 : array.length;
          return length ? array[length - 1] : undefined2;
        }
        function lastIndexOf(array, value, fromIndex) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return -1;
          }
          var index = length;
          if (fromIndex !== undefined2) {
            index = toInteger(fromIndex);
            index = index < 0 ? nativeMax(length + index, 0) : nativeMin(index, length - 1);
          }
          return value === value ? strictLastIndexOf(array, value, index) : baseFindIndex(array, baseIsNaN, index, true);
        }
        function nth(array, n) {
          return array && array.length ? baseNth(array, toInteger(n)) : undefined2;
        }
        var pull = baseRest(pullAll);
        function pullAll(array, values2) {
          return array && array.length && values2 && values2.length ? basePullAll(array, values2) : array;
        }
        function pullAllBy(array, values2, iteratee2) {
          return array && array.length && values2 && values2.length ? basePullAll(array, values2, getIteratee(iteratee2, 2)) : array;
        }
        function pullAllWith(array, values2, comparator) {
          return array && array.length && values2 && values2.length ? basePullAll(array, values2, undefined2, comparator) : array;
        }
        var pullAt = flatRest(function(array, indexes) {
          var length = array == null ? 0 : array.length, result2 = baseAt(array, indexes);
          basePullAt(array, arrayMap(indexes, function(index) {
            return isIndex(index, length) ? +index : index;
          }).sort(compareAscending));
          return result2;
        });
        function remove(array, predicate) {
          var result2 = [];
          if (!(array && array.length)) {
            return result2;
          }
          var index = -1, indexes = [], length = array.length;
          predicate = getIteratee(predicate, 3);
          while (++index < length) {
            var value = array[index];
            if (predicate(value, index, array)) {
              result2.push(value);
              indexes.push(index);
            }
          }
          basePullAt(array, indexes);
          return result2;
        }
        function reverse(array) {
          return array == null ? array : nativeReverse.call(array);
        }
        function slice2(array, start, end) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return [];
          }
          if (end && typeof end != "number" && isIterateeCall(array, start, end)) {
            start = 0;
            end = length;
          } else {
            start = start == null ? 0 : toInteger(start);
            end = end === undefined2 ? length : toInteger(end);
          }
          return baseSlice(array, start, end);
        }
        function sortedIndex(array, value) {
          return baseSortedIndex(array, value);
        }
        function sortedIndexBy(array, value, iteratee2) {
          return baseSortedIndexBy(array, value, getIteratee(iteratee2, 2));
        }
        function sortedIndexOf(array, value) {
          var length = array == null ? 0 : array.length;
          if (length) {
            var index = baseSortedIndex(array, value);
            if (index < length && eq(array[index], value)) {
              return index;
            }
          }
          return -1;
        }
        function sortedLastIndex(array, value) {
          return baseSortedIndex(array, value, true);
        }
        function sortedLastIndexBy(array, value, iteratee2) {
          return baseSortedIndexBy(array, value, getIteratee(iteratee2, 2), true);
        }
        function sortedLastIndexOf(array, value) {
          var length = array == null ? 0 : array.length;
          if (length) {
            var index = baseSortedIndex(array, value, true) - 1;
            if (eq(array[index], value)) {
              return index;
            }
          }
          return -1;
        }
        function sortedUniq(array) {
          return array && array.length ? baseSortedUniq(array) : [];
        }
        function sortedUniqBy(array, iteratee2) {
          return array && array.length ? baseSortedUniq(array, getIteratee(iteratee2, 2)) : [];
        }
        function tail(array) {
          var length = array == null ? 0 : array.length;
          return length ? baseSlice(array, 1, length) : [];
        }
        function take(array, n, guard) {
          if (!(array && array.length)) {
            return [];
          }
          n = guard || n === undefined2 ? 1 : toInteger(n);
          return baseSlice(array, 0, n < 0 ? 0 : n);
        }
        function takeRight(array, n, guard) {
          var length = array == null ? 0 : array.length;
          if (!length) {
            return [];
          }
          n = guard || n === undefined2 ? 1 : toInteger(n);
          n = length - n;
          return baseSlice(array, n < 0 ? 0 : n, length);
        }
        function takeRightWhile(array, predicate) {
          return array && array.length ? baseWhile(array, getIteratee(predicate, 3), false, true) : [];
        }
        function takeWhile(array, predicate) {
          return array && array.length ? baseWhile(array, getIteratee(predicate, 3)) : [];
        }
        var union = baseRest(function(arrays) {
          return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true));
        });
        var unionBy = baseRest(function(arrays) {
          var iteratee2 = last(arrays);
          if (isArrayLikeObject(iteratee2)) {
            iteratee2 = undefined2;
          }
          return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true), getIteratee(iteratee2, 2));
        });
        var unionWith = baseRest(function(arrays) {
          var comparator = last(arrays);
          comparator = typeof comparator == "function" ? comparator : undefined2;
          return baseUniq(baseFlatten(arrays, 1, isArrayLikeObject, true), undefined2, comparator);
        });
        function uniq(array) {
          return array && array.length ? baseUniq(array) : [];
        }
        function uniqBy(array, iteratee2) {
          return array && array.length ? baseUniq(array, getIteratee(iteratee2, 2)) : [];
        }
        function uniqWith(array, comparator) {
          comparator = typeof comparator == "function" ? comparator : undefined2;
          return array && array.length ? baseUniq(array, undefined2, comparator) : [];
        }
        function unzip(array) {
          if (!(array && array.length)) {
            return [];
          }
          var length = 0;
          array = arrayFilter(array, function(group) {
            if (isArrayLikeObject(group)) {
              length = nativeMax(group.length, length);
              return true;
            }
          });
          return baseTimes(length, function(index) {
            return arrayMap(array, baseProperty(index));
          });
        }
        function unzipWith(array, iteratee2) {
          if (!(array && array.length)) {
            return [];
          }
          var result2 = unzip(array);
          if (iteratee2 == null) {
            return result2;
          }
          return arrayMap(result2, function(group) {
            return apply(iteratee2, undefined2, group);
          });
        }
        var without = baseRest(function(array, values2) {
          return isArrayLikeObject(array) ? baseDifference(array, values2) : [];
        });
        var xor = baseRest(function(arrays) {
          return baseXor(arrayFilter(arrays, isArrayLikeObject));
        });
        var xorBy = baseRest(function(arrays) {
          var iteratee2 = last(arrays);
          if (isArrayLikeObject(iteratee2)) {
            iteratee2 = undefined2;
          }
          return baseXor(arrayFilter(arrays, isArrayLikeObject), getIteratee(iteratee2, 2));
        });
        var xorWith = baseRest(function(arrays) {
          var comparator = last(arrays);
          comparator = typeof comparator == "function" ? comparator : undefined2;
          return baseXor(arrayFilter(arrays, isArrayLikeObject), undefined2, comparator);
        });
        var zip = baseRest(unzip);
        function zipObject(props, values2) {
          return baseZipObject(props || [], values2 || [], assignValue);
        }
        function zipObjectDeep(props, values2) {
          return baseZipObject(props || [], values2 || [], baseSet);
        }
        var zipWith = baseRest(function(arrays) {
          var length = arrays.length, iteratee2 = length > 1 ? arrays[length - 1] : undefined2;
          iteratee2 = typeof iteratee2 == "function" ? (arrays.pop(), iteratee2) : undefined2;
          return unzipWith(arrays, iteratee2);
        });
        function chain(value) {
          var result2 = lodash(value);
          result2.__chain__ = true;
          return result2;
        }
        function tap(value, interceptor) {
          interceptor(value);
          return value;
        }
        function thru(value, interceptor) {
          return interceptor(value);
        }
        var wrapperAt = flatRest(function(paths) {
          var length = paths.length, start = length ? paths[0] : 0, value = this.__wrapped__, interceptor = function(object) {
            return baseAt(object, paths);
          };
          if (length > 1 || this.__actions__.length || !(value instanceof LazyWrapper) || !isIndex(start)) {
            return this.thru(interceptor);
          }
          value = value.slice(start, +start + (length ? 1 : 0));
          value.__actions__.push({
            "func": thru,
            "args": [interceptor],
            "thisArg": undefined2
          });
          return new LodashWrapper(value, this.__chain__).thru(function(array) {
            if (length && !array.length) {
              array.push(undefined2);
            }
            return array;
          });
        });
        function wrapperChain() {
          return chain(this);
        }
        function wrapperCommit() {
          return new LodashWrapper(this.value(), this.__chain__);
        }
        function wrapperNext() {
          if (this.__values__ === undefined2) {
            this.__values__ = toArray(this.value());
          }
          var done = this.__index__ >= this.__values__.length, value = done ? undefined2 : this.__values__[this.__index__++];
          return { "done": done, "value": value };
        }
        function wrapperToIterator() {
          return this;
        }
        function wrapperPlant(value) {
          var result2, parent2 = this;
          while (parent2 instanceof baseLodash) {
            var clone2 = wrapperClone(parent2);
            clone2.__index__ = 0;
            clone2.__values__ = undefined2;
            if (result2) {
              previous.__wrapped__ = clone2;
            } else {
              result2 = clone2;
            }
            var previous = clone2;
            parent2 = parent2.__wrapped__;
          }
          previous.__wrapped__ = value;
          return result2;
        }
        function wrapperReverse() {
          var value = this.__wrapped__;
          if (value instanceof LazyWrapper) {
            var wrapped = value;
            if (this.__actions__.length) {
              wrapped = new LazyWrapper(this);
            }
            wrapped = wrapped.reverse();
            wrapped.__actions__.push({
              "func": thru,
              "args": [reverse],
              "thisArg": undefined2
            });
            return new LodashWrapper(wrapped, this.__chain__);
          }
          return this.thru(reverse);
        }
        function wrapperValue() {
          return baseWrapperValue(this.__wrapped__, this.__actions__);
        }
        var countBy = createAggregator(function(result2, value, key) {
          if (hasOwnProperty.call(result2, key)) {
            ++result2[key];
          } else {
            baseAssignValue(result2, key, 1);
          }
        });
        function every(collection, predicate, guard) {
          var func = isArray3(collection) ? arrayEvery : baseEvery;
          if (guard && isIterateeCall(collection, predicate, guard)) {
            predicate = undefined2;
          }
          return func(collection, getIteratee(predicate, 3));
        }
        function filter(collection, predicate) {
          var func = isArray3(collection) ? arrayFilter : baseFilter;
          return func(collection, getIteratee(predicate, 3));
        }
        var find = createFind(findIndex);
        var findLast = createFind(findLastIndex);
        function flatMap(collection, iteratee2) {
          return baseFlatten(map(collection, iteratee2), 1);
        }
        function flatMapDeep(collection, iteratee2) {
          return baseFlatten(map(collection, iteratee2), INFINITY);
        }
        function flatMapDepth(collection, iteratee2, depth) {
          depth = depth === undefined2 ? 1 : toInteger(depth);
          return baseFlatten(map(collection, iteratee2), depth);
        }
        function forEach(collection, iteratee2) {
          var func = isArray3(collection) ? arrayEach : baseEach;
          return func(collection, getIteratee(iteratee2, 3));
        }
        function forEachRight(collection, iteratee2) {
          var func = isArray3(collection) ? arrayEachRight : baseEachRight;
          return func(collection, getIteratee(iteratee2, 3));
        }
        var groupBy = createAggregator(function(result2, value, key) {
          if (hasOwnProperty.call(result2, key)) {
            result2[key].push(value);
          } else {
            baseAssignValue(result2, key, [value]);
          }
        });
        function includes(collection, value, fromIndex, guard) {
          collection = isArrayLike(collection) ? collection : values(collection);
          fromIndex = fromIndex && !guard ? toInteger(fromIndex) : 0;
          var length = collection.length;
          if (fromIndex < 0) {
            fromIndex = nativeMax(length + fromIndex, 0);
          }
          return isString(collection) ? fromIndex <= length && collection.indexOf(value, fromIndex) > -1 : !!length && baseIndexOf(collection, value, fromIndex) > -1;
        }
        var invokeMap = baseRest(function(collection, path, args) {
          var index = -1, isFunc = typeof path == "function", result2 = isArrayLike(collection) ? Array2(collection.length) : [];
          baseEach(collection, function(value) {
            result2[++index] = isFunc ? apply(path, value, args) : baseInvoke(value, path, args);
          });
          return result2;
        });
        var keyBy = createAggregator(function(result2, value, key) {
          baseAssignValue(result2, key, value);
        });
        function map(collection, iteratee2) {
          var func = isArray3(collection) ? arrayMap : baseMap;
          return func(collection, getIteratee(iteratee2, 3));
        }
        function orderBy(collection, iteratees, orders, guard) {
          if (collection == null) {
            return [];
          }
          if (!isArray3(iteratees)) {
            iteratees = iteratees == null ? [] : [iteratees];
          }
          orders = guard ? undefined2 : orders;
          if (!isArray3(orders)) {
            orders = orders == null ? [] : [orders];
          }
          return baseOrderBy(collection, iteratees, orders);
        }
        var partition = createAggregator(function(result2, value, key) {
          result2[key ? 0 : 1].push(value);
        }, function() {
          return [[], []];
        });
        function reduce(collection, iteratee2, accumulator) {
          var func = isArray3(collection) ? arrayReduce : baseReduce, initAccum = arguments.length < 3;
          return func(collection, getIteratee(iteratee2, 4), accumulator, initAccum, baseEach);
        }
        function reduceRight(collection, iteratee2, accumulator) {
          var func = isArray3(collection) ? arrayReduceRight : baseReduce, initAccum = arguments.length < 3;
          return func(collection, getIteratee(iteratee2, 4), accumulator, initAccum, baseEachRight);
        }
        function reject(collection, predicate) {
          var func = isArray3(collection) ? arrayFilter : baseFilter;
          return func(collection, negate(getIteratee(predicate, 3)));
        }
        function sample(collection) {
          var func = isArray3(collection) ? arraySample : baseSample;
          return func(collection);
        }
        function sampleSize(collection, n, guard) {
          if (guard ? isIterateeCall(collection, n, guard) : n === undefined2) {
            n = 1;
          } else {
            n = toInteger(n);
          }
          var func = isArray3(collection) ? arraySampleSize : baseSampleSize;
          return func(collection, n);
        }
        function shuffle(collection) {
          var func = isArray3(collection) ? arrayShuffle : baseShuffle;
          return func(collection);
        }
        function size(collection) {
          if (collection == null) {
            return 0;
          }
          if (isArrayLike(collection)) {
            return isString(collection) ? stringSize(collection) : collection.length;
          }
          var tag = getTag(collection);
          if (tag == mapTag || tag == setTag) {
            return collection.size;
          }
          return baseKeys(collection).length;
        }
        function some(collection, predicate, guard) {
          var func = isArray3(collection) ? arraySome : baseSome;
          if (guard && isIterateeCall(collection, predicate, guard)) {
            predicate = undefined2;
          }
          return func(collection, getIteratee(predicate, 3));
        }
        var sortBy = baseRest(function(collection, iteratees) {
          if (collection == null) {
            return [];
          }
          var length = iteratees.length;
          if (length > 1 && isIterateeCall(collection, iteratees[0], iteratees[1])) {
            iteratees = [];
          } else if (length > 2 && isIterateeCall(iteratees[0], iteratees[1], iteratees[2])) {
            iteratees = [iteratees[0]];
          }
          return baseOrderBy(collection, baseFlatten(iteratees, 1), []);
        });
        var now2 = ctxNow || function() {
          return root.Date.now();
        };
        function after(n, func) {
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          n = toInteger(n);
          return function() {
            if (--n < 1) {
              return func.apply(this, arguments);
            }
          };
        }
        function ary(func, n, guard) {
          n = guard ? undefined2 : n;
          n = func && n == null ? func.length : n;
          return createWrap(func, WRAP_ARY_FLAG, undefined2, undefined2, undefined2, undefined2, n);
        }
        function before(n, func) {
          var result2;
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          n = toInteger(n);
          return function() {
            if (--n > 0) {
              result2 = func.apply(this, arguments);
            }
            if (n <= 1) {
              func = undefined2;
            }
            return result2;
          };
        }
        var bind = baseRest(function(func, thisArg, partials) {
          var bitmask = WRAP_BIND_FLAG;
          if (partials.length) {
            var holders = replaceHolders(partials, getHolder(bind));
            bitmask |= WRAP_PARTIAL_FLAG;
          }
          return createWrap(func, bitmask, thisArg, partials, holders);
        });
        var bindKey = baseRest(function(object, key, partials) {
          var bitmask = WRAP_BIND_FLAG | WRAP_BIND_KEY_FLAG;
          if (partials.length) {
            var holders = replaceHolders(partials, getHolder(bindKey));
            bitmask |= WRAP_PARTIAL_FLAG;
          }
          return createWrap(key, bitmask, object, partials, holders);
        });
        function curry(func, arity, guard) {
          arity = guard ? undefined2 : arity;
          var result2 = createWrap(func, WRAP_CURRY_FLAG, undefined2, undefined2, undefined2, undefined2, undefined2, arity);
          result2.placeholder = curry.placeholder;
          return result2;
        }
        function curryRight(func, arity, guard) {
          arity = guard ? undefined2 : arity;
          var result2 = createWrap(func, WRAP_CURRY_RIGHT_FLAG, undefined2, undefined2, undefined2, undefined2, undefined2, arity);
          result2.placeholder = curryRight.placeholder;
          return result2;
        }
        function debounce(func, wait, options) {
          var lastArgs, lastThis, maxWait, result2, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          wait = toNumber(wait) || 0;
          if (isObject4(options)) {
            leading = !!options.leading;
            maxing = "maxWait" in options;
            maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
            trailing = "trailing" in options ? !!options.trailing : trailing;
          }
          function invokeFunc(time) {
            var args = lastArgs, thisArg = lastThis;
            lastArgs = lastThis = undefined2;
            lastInvokeTime = time;
            result2 = func.apply(thisArg, args);
            return result2;
          }
          function leadingEdge(time) {
            lastInvokeTime = time;
            timerId = setTimeout2(timerExpired, wait);
            return leading ? invokeFunc(time) : result2;
          }
          function remainingWait(time) {
            var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime, timeWaiting = wait - timeSinceLastCall;
            return maxing ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke) : timeWaiting;
          }
          function shouldInvoke(time) {
            var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime;
            return lastCallTime === undefined2 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
          }
          function timerExpired() {
            var time = now2();
            if (shouldInvoke(time)) {
              return trailingEdge(time);
            }
            timerId = setTimeout2(timerExpired, remainingWait(time));
          }
          function trailingEdge(time) {
            timerId = undefined2;
            if (trailing && lastArgs) {
              return invokeFunc(time);
            }
            lastArgs = lastThis = undefined2;
            return result2;
          }
          function cancel() {
            if (timerId !== undefined2) {
              clearTimeout2(timerId);
            }
            lastInvokeTime = 0;
            lastArgs = lastCallTime = lastThis = timerId = undefined2;
          }
          function flush() {
            return timerId === undefined2 ? result2 : trailingEdge(now2());
          }
          function debounced() {
            var time = now2(), isInvoking = shouldInvoke(time);
            lastArgs = arguments;
            lastThis = this;
            lastCallTime = time;
            if (isInvoking) {
              if (timerId === undefined2) {
                return leadingEdge(lastCallTime);
              }
              if (maxing) {
                clearTimeout2(timerId);
                timerId = setTimeout2(timerExpired, wait);
                return invokeFunc(lastCallTime);
              }
            }
            if (timerId === undefined2) {
              timerId = setTimeout2(timerExpired, wait);
            }
            return result2;
          }
          debounced.cancel = cancel;
          debounced.flush = flush;
          return debounced;
        }
        var defer3 = baseRest(function(func, args) {
          return baseDelay(func, 1, args);
        });
        var delay = baseRest(function(func, wait, args) {
          return baseDelay(func, toNumber(wait) || 0, args);
        });
        function flip(func) {
          return createWrap(func, WRAP_FLIP_FLAG);
        }
        function memoize(func, resolver) {
          if (typeof func != "function" || resolver != null && typeof resolver != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          var memoized = function() {
            var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
            if (cache.has(key)) {
              return cache.get(key);
            }
            var result2 = func.apply(this, args);
            memoized.cache = cache.set(key, result2) || cache;
            return result2;
          };
          memoized.cache = new (memoize.Cache || MapCache)();
          return memoized;
        }
        memoize.Cache = MapCache;
        function negate(predicate) {
          if (typeof predicate != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          return function() {
            var args = arguments;
            switch (args.length) {
              case 0:
                return !predicate.call(this);
              case 1:
                return !predicate.call(this, args[0]);
              case 2:
                return !predicate.call(this, args[0], args[1]);
              case 3:
                return !predicate.call(this, args[0], args[1], args[2]);
            }
            return !predicate.apply(this, args);
          };
        }
        function once(func) {
          return before(2, func);
        }
        var overArgs = castRest(function(func, transforms) {
          transforms = transforms.length == 1 && isArray3(transforms[0]) ? arrayMap(transforms[0], baseUnary(getIteratee())) : arrayMap(baseFlatten(transforms, 1), baseUnary(getIteratee()));
          var funcsLength = transforms.length;
          return baseRest(function(args) {
            var index = -1, length = nativeMin(args.length, funcsLength);
            while (++index < length) {
              args[index] = transforms[index].call(this, args[index]);
            }
            return apply(func, this, args);
          });
        });
        var partial = baseRest(function(func, partials) {
          var holders = replaceHolders(partials, getHolder(partial));
          return createWrap(func, WRAP_PARTIAL_FLAG, undefined2, partials, holders);
        });
        var partialRight = baseRest(function(func, partials) {
          var holders = replaceHolders(partials, getHolder(partialRight));
          return createWrap(func, WRAP_PARTIAL_RIGHT_FLAG, undefined2, partials, holders);
        });
        var rearg = flatRest(function(func, indexes) {
          return createWrap(func, WRAP_REARG_FLAG, undefined2, undefined2, undefined2, indexes);
        });
        function rest(func, start) {
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          start = start === undefined2 ? start : toInteger(start);
          return baseRest(func, start);
        }
        function spread(func, start) {
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          start = start == null ? 0 : nativeMax(toInteger(start), 0);
          return baseRest(function(args) {
            var array = args[start], otherArgs = castSlice(args, 0, start);
            if (array) {
              arrayPush(otherArgs, array);
            }
            return apply(func, this, otherArgs);
          });
        }
        function throttle(func, wait, options) {
          var leading = true, trailing = true;
          if (typeof func != "function") {
            throw new TypeError2(FUNC_ERROR_TEXT);
          }
          if (isObject4(options)) {
            leading = "leading" in options ? !!options.leading : leading;
            trailing = "trailing" in options ? !!options.trailing : trailing;
          }
          return debounce(func, wait, {
            "leading": leading,
            "maxWait": wait,
            "trailing": trailing
          });
        }
        function unary(func) {
          return ary(func, 1);
        }
        function wrap(value, wrapper) {
          return partial(castFunction(wrapper), value);
        }
        function castArray() {
          if (!arguments.length) {
            return [];
          }
          var value = arguments[0];
          return isArray3(value) ? value : [value];
        }
        function clone(value) {
          return baseClone(value, CLONE_SYMBOLS_FLAG);
        }
        function cloneWith(value, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return baseClone(value, CLONE_SYMBOLS_FLAG, customizer);
        }
        function cloneDeep(value) {
          return baseClone(value, CLONE_DEEP_FLAG | CLONE_SYMBOLS_FLAG);
        }
        function cloneDeepWith(value, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return baseClone(value, CLONE_DEEP_FLAG | CLONE_SYMBOLS_FLAG, customizer);
        }
        function conformsTo(object, source) {
          return source == null || baseConformsTo(object, source, keys(source));
        }
        function eq(value, other) {
          return value === other || value !== value && other !== other;
        }
        var gt = createRelationalOperation(baseGt);
        var gte = createRelationalOperation(function(value, other) {
          return value >= other;
        });
        var isArguments = baseIsArguments(/* @__PURE__ */ function() {
          return arguments;
        }()) ? baseIsArguments : function(value) {
          return isObjectLike(value) && hasOwnProperty.call(value, "callee") && !propertyIsEnumerable.call(value, "callee");
        };
        var isArray3 = Array2.isArray;
        var isArrayBuffer = nodeIsArrayBuffer ? baseUnary(nodeIsArrayBuffer) : baseIsArrayBuffer;
        function isArrayLike(value) {
          return value != null && isLength(value.length) && !isFunction2(value);
        }
        function isArrayLikeObject(value) {
          return isObjectLike(value) && isArrayLike(value);
        }
        function isBoolean(value) {
          return value === true || value === false || isObjectLike(value) && baseGetTag(value) == boolTag;
        }
        var isBuffer = nativeIsBuffer || stubFalse;
        var isDate = nodeIsDate ? baseUnary(nodeIsDate) : baseIsDate;
        function isElement(value) {
          return isObjectLike(value) && value.nodeType === 1 && !isPlainObject(value);
        }
        function isEmpty2(value) {
          if (value == null) {
            return true;
          }
          if (isArrayLike(value) && (isArray3(value) || typeof value == "string" || typeof value.splice == "function" || isBuffer(value) || isTypedArray(value) || isArguments(value))) {
            return !value.length;
          }
          var tag = getTag(value);
          if (tag == mapTag || tag == setTag) {
            return !value.size;
          }
          if (isPrototype(value)) {
            return !baseKeys(value).length;
          }
          for (var key in value) {
            if (hasOwnProperty.call(value, key)) {
              return false;
            }
          }
          return true;
        }
        function isEqual3(value, other) {
          return baseIsEqual(value, other);
        }
        function isEqualWith(value, other, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          var result2 = customizer ? customizer(value, other) : undefined2;
          return result2 === undefined2 ? baseIsEqual(value, other, undefined2, customizer) : !!result2;
        }
        function isError(value) {
          if (!isObjectLike(value)) {
            return false;
          }
          var tag = baseGetTag(value);
          return tag == errorTag || tag == domExcTag || typeof value.message == "string" && typeof value.name == "string" && !isPlainObject(value);
        }
        function isFinite2(value) {
          return typeof value == "number" && nativeIsFinite(value);
        }
        function isFunction2(value) {
          if (!isObject4(value)) {
            return false;
          }
          var tag = baseGetTag(value);
          return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
        }
        function isInteger(value) {
          return typeof value == "number" && value == toInteger(value);
        }
        function isLength(value) {
          return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
        }
        function isObject4(value) {
          var type = typeof value;
          return value != null && (type == "object" || type == "function");
        }
        function isObjectLike(value) {
          return value != null && typeof value == "object";
        }
        var isMap = nodeIsMap ? baseUnary(nodeIsMap) : baseIsMap;
        function isMatch(object, source) {
          return object === source || baseIsMatch(object, source, getMatchData(source));
        }
        function isMatchWith(object, source, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return baseIsMatch(object, source, getMatchData(source), customizer);
        }
        function isNaN2(value) {
          return isNumber3(value) && value != +value;
        }
        function isNative(value) {
          if (isMaskable(value)) {
            throw new Error2(CORE_ERROR_TEXT);
          }
          return baseIsNative(value);
        }
        function isNull(value) {
          return value === null;
        }
        function isNil(value) {
          return value == null;
        }
        function isNumber3(value) {
          return typeof value == "number" || isObjectLike(value) && baseGetTag(value) == numberTag;
        }
        function isPlainObject(value) {
          if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
            return false;
          }
          var proto = getPrototype(value);
          if (proto === null) {
            return true;
          }
          var Ctor = hasOwnProperty.call(proto, "constructor") && proto.constructor;
          return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
        }
        var isRegExp = nodeIsRegExp ? baseUnary(nodeIsRegExp) : baseIsRegExp;
        function isSafeInteger(value) {
          return isInteger(value) && value >= -MAX_SAFE_INTEGER && value <= MAX_SAFE_INTEGER;
        }
        var isSet = nodeIsSet ? baseUnary(nodeIsSet) : baseIsSet;
        function isString(value) {
          return typeof value == "string" || !isArray3(value) && isObjectLike(value) && baseGetTag(value) == stringTag;
        }
        function isSymbol(value) {
          return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag;
        }
        var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
        function isUndefined(value) {
          return value === undefined2;
        }
        function isWeakMap(value) {
          return isObjectLike(value) && getTag(value) == weakMapTag;
        }
        function isWeakSet(value) {
          return isObjectLike(value) && baseGetTag(value) == weakSetTag;
        }
        var lt = createRelationalOperation(baseLt);
        var lte = createRelationalOperation(function(value, other) {
          return value <= other;
        });
        function toArray(value) {
          if (!value) {
            return [];
          }
          if (isArrayLike(value)) {
            return isString(value) ? stringToArray(value) : copyArray(value);
          }
          if (symIterator && value[symIterator]) {
            return iteratorToArray(value[symIterator]());
          }
          var tag = getTag(value), func = tag == mapTag ? mapToArray : tag == setTag ? setToArray : values;
          return func(value);
        }
        function toFinite(value) {
          if (!value) {
            return value === 0 ? value : 0;
          }
          value = toNumber(value);
          if (value === INFINITY || value === -INFINITY) {
            var sign = value < 0 ? -1 : 1;
            return sign * MAX_INTEGER;
          }
          return value === value ? value : 0;
        }
        function toInteger(value) {
          var result2 = toFinite(value), remainder = result2 % 1;
          return result2 === result2 ? remainder ? result2 - remainder : result2 : 0;
        }
        function toLength(value) {
          return value ? baseClamp(toInteger(value), 0, MAX_ARRAY_LENGTH) : 0;
        }
        function toNumber(value) {
          if (typeof value == "number") {
            return value;
          }
          if (isSymbol(value)) {
            return NAN;
          }
          if (isObject4(value)) {
            var other = typeof value.valueOf == "function" ? value.valueOf() : value;
            value = isObject4(other) ? other + "" : other;
          }
          if (typeof value != "string") {
            return value === 0 ? value : +value;
          }
          value = baseTrim(value);
          var isBinary = reIsBinary.test(value);
          return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
        }
        function toPlainObject(value) {
          return copyObject(value, keysIn(value));
        }
        function toSafeInteger(value) {
          return value ? baseClamp(toInteger(value), -MAX_SAFE_INTEGER, MAX_SAFE_INTEGER) : value === 0 ? value : 0;
        }
        function toString(value) {
          return value == null ? "" : baseToString(value);
        }
        var assign = createAssigner(function(object, source) {
          if (isPrototype(source) || isArrayLike(source)) {
            copyObject(source, keys(source), object);
            return;
          }
          for (var key in source) {
            if (hasOwnProperty.call(source, key)) {
              assignValue(object, key, source[key]);
            }
          }
        });
        var assignIn = createAssigner(function(object, source) {
          copyObject(source, keysIn(source), object);
        });
        var assignInWith = createAssigner(function(object, source, srcIndex, customizer) {
          copyObject(source, keysIn(source), object, customizer);
        });
        var assignWith = createAssigner(function(object, source, srcIndex, customizer) {
          copyObject(source, keys(source), object, customizer);
        });
        var at = flatRest(baseAt);
        function create3(prototype, properties) {
          var result2 = baseCreate(prototype);
          return properties == null ? result2 : baseAssign(result2, properties);
        }
        var defaults2 = baseRest(function(object, sources) {
          object = Object2(object);
          var index = -1;
          var length = sources.length;
          var guard = length > 2 ? sources[2] : undefined2;
          if (guard && isIterateeCall(sources[0], sources[1], guard)) {
            length = 1;
          }
          while (++index < length) {
            var source = sources[index];
            var props = keysIn(source);
            var propsIndex = -1;
            var propsLength = props.length;
            while (++propsIndex < propsLength) {
              var key = props[propsIndex];
              var value = object[key];
              if (value === undefined2 || eq(value, objectProto[key]) && !hasOwnProperty.call(object, key)) {
                object[key] = source[key];
              }
            }
          }
          return object;
        });
        var defaultsDeep = baseRest(function(args) {
          args.push(undefined2, customDefaultsMerge);
          return apply(mergeWith, undefined2, args);
        });
        function findKey(object, predicate) {
          return baseFindKey(object, getIteratee(predicate, 3), baseForOwn);
        }
        function findLastKey(object, predicate) {
          return baseFindKey(object, getIteratee(predicate, 3), baseForOwnRight);
        }
        function forIn(object, iteratee2) {
          return object == null ? object : baseFor(object, getIteratee(iteratee2, 3), keysIn);
        }
        function forInRight(object, iteratee2) {
          return object == null ? object : baseForRight(object, getIteratee(iteratee2, 3), keysIn);
        }
        function forOwn(object, iteratee2) {
          return object && baseForOwn(object, getIteratee(iteratee2, 3));
        }
        function forOwnRight(object, iteratee2) {
          return object && baseForOwnRight(object, getIteratee(iteratee2, 3));
        }
        function functions(object) {
          return object == null ? [] : baseFunctions(object, keys(object));
        }
        function functionsIn(object) {
          return object == null ? [] : baseFunctions(object, keysIn(object));
        }
        function get3(object, path, defaultValue) {
          var result2 = object == null ? undefined2 : baseGet(object, path);
          return result2 === undefined2 ? defaultValue : result2;
        }
        function has(object, path) {
          return object != null && hasPath(object, path, baseHas);
        }
        function hasIn(object, path) {
          return object != null && hasPath(object, path, baseHasIn);
        }
        var invert = createInverter(function(result2, value, key) {
          if (value != null && typeof value.toString != "function") {
            value = nativeObjectToString.call(value);
          }
          result2[value] = key;
        }, constant(identity));
        var invertBy = createInverter(function(result2, value, key) {
          if (value != null && typeof value.toString != "function") {
            value = nativeObjectToString.call(value);
          }
          if (hasOwnProperty.call(result2, value)) {
            result2[value].push(key);
          } else {
            result2[value] = [key];
          }
        }, getIteratee);
        var invoke = baseRest(baseInvoke);
        function keys(object) {
          return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
        }
        function keysIn(object) {
          return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
        }
        function mapKeys(object, iteratee2) {
          var result2 = {};
          iteratee2 = getIteratee(iteratee2, 3);
          baseForOwn(object, function(value, key, object2) {
            baseAssignValue(result2, iteratee2(value, key, object2), value);
          });
          return result2;
        }
        function mapValues(object, iteratee2) {
          var result2 = {};
          iteratee2 = getIteratee(iteratee2, 3);
          baseForOwn(object, function(value, key, object2) {
            baseAssignValue(result2, key, iteratee2(value, key, object2));
          });
          return result2;
        }
        var merge = createAssigner(function(object, source, srcIndex) {
          baseMerge(object, source, srcIndex);
        });
        var mergeWith = createAssigner(function(object, source, srcIndex, customizer) {
          baseMerge(object, source, srcIndex, customizer);
        });
        var omit = flatRest(function(object, paths) {
          var result2 = {};
          if (object == null) {
            return result2;
          }
          var isDeep = false;
          paths = arrayMap(paths, function(path) {
            path = castPath(path, object);
            isDeep || (isDeep = path.length > 1);
            return path;
          });
          copyObject(object, getAllKeysIn(object), result2);
          if (isDeep) {
            result2 = baseClone(result2, CLONE_DEEP_FLAG | CLONE_FLAT_FLAG | CLONE_SYMBOLS_FLAG, customOmitClone);
          }
          var length = paths.length;
          while (length--) {
            baseUnset(result2, paths[length]);
          }
          return result2;
        });
        function omitBy(object, predicate) {
          return pickBy(object, negate(getIteratee(predicate)));
        }
        var pick = flatRest(function(object, paths) {
          return object == null ? {} : basePick(object, paths);
        });
        function pickBy(object, predicate) {
          if (object == null) {
            return {};
          }
          var props = arrayMap(getAllKeysIn(object), function(prop) {
            return [prop];
          });
          predicate = getIteratee(predicate);
          return basePickBy(object, props, function(value, path) {
            return predicate(value, path[0]);
          });
        }
        function result(object, path, defaultValue) {
          path = castPath(path, object);
          var index = -1, length = path.length;
          if (!length) {
            length = 1;
            object = undefined2;
          }
          while (++index < length) {
            var value = object == null ? undefined2 : object[toKey(path[index])];
            if (value === undefined2) {
              index = length;
              value = defaultValue;
            }
            object = isFunction2(value) ? value.call(object) : value;
          }
          return object;
        }
        function set3(object, path, value) {
          return object == null ? object : baseSet(object, path, value);
        }
        function setWith(object, path, value, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return object == null ? object : baseSet(object, path, value, customizer);
        }
        var toPairs = createToPairs(keys);
        var toPairsIn = createToPairs(keysIn);
        function transform(object, iteratee2, accumulator) {
          var isArr = isArray3(object), isArrLike = isArr || isBuffer(object) || isTypedArray(object);
          iteratee2 = getIteratee(iteratee2, 4);
          if (accumulator == null) {
            var Ctor = object && object.constructor;
            if (isArrLike) {
              accumulator = isArr ? new Ctor() : [];
            } else if (isObject4(object)) {
              accumulator = isFunction2(Ctor) ? baseCreate(getPrototype(object)) : {};
            } else {
              accumulator = {};
            }
          }
          (isArrLike ? arrayEach : baseForOwn)(object, function(value, index, object2) {
            return iteratee2(accumulator, value, index, object2);
          });
          return accumulator;
        }
        function unset(object, path) {
          return object == null ? true : baseUnset(object, path);
        }
        function update(object, path, updater) {
          return object == null ? object : baseUpdate(object, path, castFunction(updater));
        }
        function updateWith(object, path, updater, customizer) {
          customizer = typeof customizer == "function" ? customizer : undefined2;
          return object == null ? object : baseUpdate(object, path, castFunction(updater), customizer);
        }
        function values(object) {
          return object == null ? [] : baseValues(object, keys(object));
        }
        function valuesIn(object) {
          return object == null ? [] : baseValues(object, keysIn(object));
        }
        function clamp(number, lower, upper) {
          if (upper === undefined2) {
            upper = lower;
            lower = undefined2;
          }
          if (upper !== undefined2) {
            upper = toNumber(upper);
            upper = upper === upper ? upper : 0;
          }
          if (lower !== undefined2) {
            lower = toNumber(lower);
            lower = lower === lower ? lower : 0;
          }
          return baseClamp(toNumber(number), lower, upper);
        }
        function inRange(number, start, end) {
          start = toFinite(start);
          if (end === undefined2) {
            end = start;
            start = 0;
          } else {
            end = toFinite(end);
          }
          number = toNumber(number);
          return baseInRange(number, start, end);
        }
        function random(lower, upper, floating) {
          if (floating && typeof floating != "boolean" && isIterateeCall(lower, upper, floating)) {
            upper = floating = undefined2;
          }
          if (floating === undefined2) {
            if (typeof upper == "boolean") {
              floating = upper;
              upper = undefined2;
            } else if (typeof lower == "boolean") {
              floating = lower;
              lower = undefined2;
            }
          }
          if (lower === undefined2 && upper === undefined2) {
            lower = 0;
            upper = 1;
          } else {
            lower = toFinite(lower);
            if (upper === undefined2) {
              upper = lower;
              lower = 0;
            } else {
              upper = toFinite(upper);
            }
          }
          if (lower > upper) {
            var temp = lower;
            lower = upper;
            upper = temp;
          }
          if (floating || lower % 1 || upper % 1) {
            var rand = nativeRandom();
            return nativeMin(lower + rand * (upper - lower + freeParseFloat("1e-" + ((rand + "").length - 1))), upper);
          }
          return baseRandom(lower, upper);
        }
        var camelCase = createCompounder(function(result2, word, index) {
          word = word.toLowerCase();
          return result2 + (index ? capitalize(word) : word);
        });
        function capitalize(string) {
          return upperFirst(toString(string).toLowerCase());
        }
        function deburr(string) {
          string = toString(string);
          return string && string.replace(reLatin, deburrLetter).replace(reComboMark, "");
        }
        function endsWith(string, target, position) {
          string = toString(string);
          target = baseToString(target);
          var length = string.length;
          position = position === undefined2 ? length : baseClamp(toInteger(position), 0, length);
          var end = position;
          position -= target.length;
          return position >= 0 && string.slice(position, end) == target;
        }
        function escape(string) {
          string = toString(string);
          return string && reHasUnescapedHtml.test(string) ? string.replace(reUnescapedHtml, escapeHtmlChar) : string;
        }
        function escapeRegExp(string) {
          string = toString(string);
          return string && reHasRegExpChar.test(string) ? string.replace(reRegExpChar, "\\$&") : string;
        }
        var kebabCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? "-" : "") + word.toLowerCase();
        });
        var lowerCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? " " : "") + word.toLowerCase();
        });
        var lowerFirst = createCaseFirst("toLowerCase");
        function pad(string, length, chars) {
          string = toString(string);
          length = toInteger(length);
          var strLength = length ? stringSize(string) : 0;
          if (!length || strLength >= length) {
            return string;
          }
          var mid = (length - strLength) / 2;
          return createPadding(nativeFloor(mid), chars) + string + createPadding(nativeCeil(mid), chars);
        }
        function padEnd(string, length, chars) {
          string = toString(string);
          length = toInteger(length);
          var strLength = length ? stringSize(string) : 0;
          return length && strLength < length ? string + createPadding(length - strLength, chars) : string;
        }
        function padStart(string, length, chars) {
          string = toString(string);
          length = toInteger(length);
          var strLength = length ? stringSize(string) : 0;
          return length && strLength < length ? createPadding(length - strLength, chars) + string : string;
        }
        function parseInt2(string, radix, guard) {
          if (guard || radix == null) {
            radix = 0;
          } else if (radix) {
            radix = +radix;
          }
          return nativeParseInt(toString(string).replace(reTrimStart, ""), radix || 0);
        }
        function repeat(string, n, guard) {
          if (guard ? isIterateeCall(string, n, guard) : n === undefined2) {
            n = 1;
          } else {
            n = toInteger(n);
          }
          return baseRepeat(toString(string), n);
        }
        function replace() {
          var args = arguments, string = toString(args[0]);
          return args.length < 3 ? string : string.replace(args[1], args[2]);
        }
        var snakeCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? "_" : "") + word.toLowerCase();
        });
        function split(string, separator, limit) {
          if (limit && typeof limit != "number" && isIterateeCall(string, separator, limit)) {
            separator = limit = undefined2;
          }
          limit = limit === undefined2 ? MAX_ARRAY_LENGTH : limit >>> 0;
          if (!limit) {
            return [];
          }
          string = toString(string);
          if (string && (typeof separator == "string" || separator != null && !isRegExp(separator))) {
            separator = baseToString(separator);
            if (!separator && hasUnicode(string)) {
              return castSlice(stringToArray(string), 0, limit);
            }
          }
          return string.split(separator, limit);
        }
        var startCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? " " : "") + upperFirst(word);
        });
        function startsWith(string, target, position) {
          string = toString(string);
          position = position == null ? 0 : baseClamp(toInteger(position), 0, string.length);
          target = baseToString(target);
          return string.slice(position, position + target.length) == target;
        }
        function template(string, options, guard) {
          var settings = lodash.templateSettings;
          if (guard && isIterateeCall(string, options, guard)) {
            options = undefined2;
          }
          string = toString(string);
          options = assignInWith({}, options, settings, customDefaultsAssignIn);
          var imports = assignInWith({}, options.imports, settings.imports, customDefaultsAssignIn), importsKeys = keys(imports), importsValues = baseValues(imports, importsKeys);
          var isEscaping, isEvaluating, index = 0, interpolate = options.interpolate || reNoMatch, source = "__p += '";
          var reDelimiters = RegExp2(
            (options.escape || reNoMatch).source + "|" + interpolate.source + "|" + (interpolate === reInterpolate ? reEsTemplate : reNoMatch).source + "|" + (options.evaluate || reNoMatch).source + "|$",
            "g"
          );
          var sourceURL = "//# sourceURL=" + (hasOwnProperty.call(options, "sourceURL") ? (options.sourceURL + "").replace(/\s/g, " ") : "lodash.templateSources[" + ++templateCounter + "]") + "\n";
          string.replace(reDelimiters, function(match, escapeValue, interpolateValue, esTemplateValue, evaluateValue, offset) {
            interpolateValue || (interpolateValue = esTemplateValue);
            source += string.slice(index, offset).replace(reUnescapedString, escapeStringChar);
            if (escapeValue) {
              isEscaping = true;
              source += "' +\n__e(" + escapeValue + ") +\n'";
            }
            if (evaluateValue) {
              isEvaluating = true;
              source += "';\n" + evaluateValue + ";\n__p += '";
            }
            if (interpolateValue) {
              source += "' +\n((__t = (" + interpolateValue + ")) == null ? '' : __t) +\n'";
            }
            index = offset + match.length;
            return match;
          });
          source += "';\n";
          var variable = hasOwnProperty.call(options, "variable") && options.variable;
          if (!variable) {
            source = "with (obj) {\n" + source + "\n}\n";
          } else if (reForbiddenIdentifierChars.test(variable)) {
            throw new Error2(INVALID_TEMPL_VAR_ERROR_TEXT);
          }
          source = (isEvaluating ? source.replace(reEmptyStringLeading, "") : source).replace(reEmptyStringMiddle, "$1").replace(reEmptyStringTrailing, "$1;");
          source = "function(" + (variable || "obj") + ") {\n" + (variable ? "" : "obj || (obj = {});\n") + "var __t, __p = ''" + (isEscaping ? ", __e = _.escape" : "") + (isEvaluating ? ", __j = Array.prototype.join;\nfunction print() { __p += __j.call(arguments, '') }\n" : ";\n") + source + "return __p\n}";
          var result2 = attempt(function() {
            return Function2(importsKeys, sourceURL + "return " + source).apply(undefined2, importsValues);
          });
          result2.source = source;
          if (isError(result2)) {
            throw result2;
          }
          return result2;
        }
        function toLower(value) {
          return toString(value).toLowerCase();
        }
        function toUpper2(value) {
          return toString(value).toUpperCase();
        }
        function trim(string, chars, guard) {
          string = toString(string);
          if (string && (guard || chars === undefined2)) {
            return baseTrim(string);
          }
          if (!string || !(chars = baseToString(chars))) {
            return string;
          }
          var strSymbols = stringToArray(string), chrSymbols = stringToArray(chars), start = charsStartIndex(strSymbols, chrSymbols), end = charsEndIndex(strSymbols, chrSymbols) + 1;
          return castSlice(strSymbols, start, end).join("");
        }
        function trimEnd(string, chars, guard) {
          string = toString(string);
          if (string && (guard || chars === undefined2)) {
            return string.slice(0, trimmedEndIndex(string) + 1);
          }
          if (!string || !(chars = baseToString(chars))) {
            return string;
          }
          var strSymbols = stringToArray(string), end = charsEndIndex(strSymbols, stringToArray(chars)) + 1;
          return castSlice(strSymbols, 0, end).join("");
        }
        function trimStart(string, chars, guard) {
          string = toString(string);
          if (string && (guard || chars === undefined2)) {
            return string.replace(reTrimStart, "");
          }
          if (!string || !(chars = baseToString(chars))) {
            return string;
          }
          var strSymbols = stringToArray(string), start = charsStartIndex(strSymbols, stringToArray(chars));
          return castSlice(strSymbols, start).join("");
        }
        function truncate(string, options) {
          var length = DEFAULT_TRUNC_LENGTH, omission = DEFAULT_TRUNC_OMISSION;
          if (isObject4(options)) {
            var separator = "separator" in options ? options.separator : separator;
            length = "length" in options ? toInteger(options.length) : length;
            omission = "omission" in options ? baseToString(options.omission) : omission;
          }
          string = toString(string);
          var strLength = string.length;
          if (hasUnicode(string)) {
            var strSymbols = stringToArray(string);
            strLength = strSymbols.length;
          }
          if (length >= strLength) {
            return string;
          }
          var end = length - stringSize(omission);
          if (end < 1) {
            return omission;
          }
          var result2 = strSymbols ? castSlice(strSymbols, 0, end).join("") : string.slice(0, end);
          if (separator === undefined2) {
            return result2 + omission;
          }
          if (strSymbols) {
            end += result2.length - end;
          }
          if (isRegExp(separator)) {
            if (string.slice(end).search(separator)) {
              var match, substring = result2;
              if (!separator.global) {
                separator = RegExp2(separator.source, toString(reFlags.exec(separator)) + "g");
              }
              separator.lastIndex = 0;
              while (match = separator.exec(substring)) {
                var newEnd = match.index;
              }
              result2 = result2.slice(0, newEnd === undefined2 ? end : newEnd);
            }
          } else if (string.indexOf(baseToString(separator), end) != end) {
            var index = result2.lastIndexOf(separator);
            if (index > -1) {
              result2 = result2.slice(0, index);
            }
          }
          return result2 + omission;
        }
        function unescape2(string) {
          string = toString(string);
          return string && reHasEscapedHtml.test(string) ? string.replace(reEscapedHtml, unescapeHtmlChar) : string;
        }
        var upperCase = createCompounder(function(result2, word, index) {
          return result2 + (index ? " " : "") + word.toUpperCase();
        });
        var upperFirst = createCaseFirst("toUpperCase");
        function words(string, pattern, guard) {
          string = toString(string);
          pattern = guard ? undefined2 : pattern;
          if (pattern === undefined2) {
            return hasUnicodeWord(string) ? unicodeWords(string) : asciiWords(string);
          }
          return string.match(pattern) || [];
        }
        var attempt = baseRest(function(func, args) {
          try {
            return apply(func, undefined2, args);
          } catch (e) {
            return isError(e) ? e : new Error2(e);
          }
        });
        var bindAll = flatRest(function(object, methodNames) {
          arrayEach(methodNames, function(key) {
            key = toKey(key);
            baseAssignValue(object, key, bind(object[key], object));
          });
          return object;
        });
        function cond(pairs) {
          var length = pairs == null ? 0 : pairs.length, toIteratee = getIteratee();
          pairs = !length ? [] : arrayMap(pairs, function(pair) {
            if (typeof pair[1] != "function") {
              throw new TypeError2(FUNC_ERROR_TEXT);
            }
            return [toIteratee(pair[0]), pair[1]];
          });
          return baseRest(function(args) {
            var index = -1;
            while (++index < length) {
              var pair = pairs[index];
              if (apply(pair[0], this, args)) {
                return apply(pair[1], this, args);
              }
            }
          });
        }
        function conforms(source) {
          return baseConforms(baseClone(source, CLONE_DEEP_FLAG));
        }
        function constant(value) {
          return function() {
            return value;
          };
        }
        function defaultTo(value, defaultValue) {
          return value == null || value !== value ? defaultValue : value;
        }
        var flow = createFlow();
        var flowRight = createFlow(true);
        function identity(value) {
          return value;
        }
        function iteratee(func) {
          return baseIteratee(typeof func == "function" ? func : baseClone(func, CLONE_DEEP_FLAG));
        }
        function matches(source) {
          return baseMatches(baseClone(source, CLONE_DEEP_FLAG));
        }
        function matchesProperty(path, srcValue) {
          return baseMatchesProperty(path, baseClone(srcValue, CLONE_DEEP_FLAG));
        }
        var method = baseRest(function(path, args) {
          return function(object) {
            return baseInvoke(object, path, args);
          };
        });
        var methodOf = baseRest(function(object, args) {
          return function(path) {
            return baseInvoke(object, path, args);
          };
        });
        function mixin(object, source, options) {
          var props = keys(source), methodNames = baseFunctions(source, props);
          if (options == null && !(isObject4(source) && (methodNames.length || !props.length))) {
            options = source;
            source = object;
            object = this;
            methodNames = baseFunctions(source, keys(source));
          }
          var chain2 = !(isObject4(options) && "chain" in options) || !!options.chain, isFunc = isFunction2(object);
          arrayEach(methodNames, function(methodName) {
            var func = source[methodName];
            object[methodName] = func;
            if (isFunc) {
              object.prototype[methodName] = function() {
                var chainAll = this.__chain__;
                if (chain2 || chainAll) {
                  var result2 = object(this.__wrapped__), actions = result2.__actions__ = copyArray(this.__actions__);
                  actions.push({ "func": func, "args": arguments, "thisArg": object });
                  result2.__chain__ = chainAll;
                  return result2;
                }
                return func.apply(object, arrayPush([this.value()], arguments));
              };
            }
          });
          return object;
        }
        function noConflict() {
          if (root._ === this) {
            root._ = oldDash;
          }
          return this;
        }
        function noop() {
        }
        function nthArg(n) {
          n = toInteger(n);
          return baseRest(function(args) {
            return baseNth(args, n);
          });
        }
        var over = createOver(arrayMap);
        var overEvery = createOver(arrayEvery);
        var overSome = createOver(arraySome);
        function property(path) {
          return isKey(path) ? baseProperty(toKey(path)) : basePropertyDeep(path);
        }
        function propertyOf(object) {
          return function(path) {
            return object == null ? undefined2 : baseGet(object, path);
          };
        }
        var range = createRange();
        var rangeRight = createRange(true);
        function stubArray() {
          return [];
        }
        function stubFalse() {
          return false;
        }
        function stubObject() {
          return {};
        }
        function stubString() {
          return "";
        }
        function stubTrue() {
          return true;
        }
        function times(n, iteratee2) {
          n = toInteger(n);
          if (n < 1 || n > MAX_SAFE_INTEGER) {
            return [];
          }
          var index = MAX_ARRAY_LENGTH, length = nativeMin(n, MAX_ARRAY_LENGTH);
          iteratee2 = getIteratee(iteratee2);
          n -= MAX_ARRAY_LENGTH;
          var result2 = baseTimes(length, iteratee2);
          while (++index < n) {
            iteratee2(index);
          }
          return result2;
        }
        function toPath(value) {
          if (isArray3(value)) {
            return arrayMap(value, toKey);
          }
          return isSymbol(value) ? [value] : copyArray(stringToPath(toString(value)));
        }
        function uniqueId(prefix) {
          var id = ++idCounter;
          return toString(prefix) + id;
        }
        var add = createMathOperation(function(augend, addend) {
          return augend + addend;
        }, 0);
        var ceil = createRound("ceil");
        var divide = createMathOperation(function(dividend, divisor) {
          return dividend / divisor;
        }, 1);
        var floor = createRound("floor");
        function max(array) {
          return array && array.length ? baseExtremum(array, identity, baseGt) : undefined2;
        }
        function maxBy(array, iteratee2) {
          return array && array.length ? baseExtremum(array, getIteratee(iteratee2, 2), baseGt) : undefined2;
        }
        function mean(array) {
          return baseMean(array, identity);
        }
        function meanBy(array, iteratee2) {
          return baseMean(array, getIteratee(iteratee2, 2));
        }
        function min(array) {
          return array && array.length ? baseExtremum(array, identity, baseLt) : undefined2;
        }
        function minBy(array, iteratee2) {
          return array && array.length ? baseExtremum(array, getIteratee(iteratee2, 2), baseLt) : undefined2;
        }
        var multiply = createMathOperation(function(multiplier, multiplicand) {
          return multiplier * multiplicand;
        }, 1);
        var round = createRound("round");
        var subtract = createMathOperation(function(minuend, subtrahend) {
          return minuend - subtrahend;
        }, 0);
        function sum(array) {
          return array && array.length ? baseSum(array, identity) : 0;
        }
        function sumBy(array, iteratee2) {
          return array && array.length ? baseSum(array, getIteratee(iteratee2, 2)) : 0;
        }
        lodash.after = after;
        lodash.ary = ary;
        lodash.assign = assign;
        lodash.assignIn = assignIn;
        lodash.assignInWith = assignInWith;
        lodash.assignWith = assignWith;
        lodash.at = at;
        lodash.before = before;
        lodash.bind = bind;
        lodash.bindAll = bindAll;
        lodash.bindKey = bindKey;
        lodash.castArray = castArray;
        lodash.chain = chain;
        lodash.chunk = chunk;
        lodash.compact = compact;
        lodash.concat = concat;
        lodash.cond = cond;
        lodash.conforms = conforms;
        lodash.constant = constant;
        lodash.countBy = countBy;
        lodash.create = create3;
        lodash.curry = curry;
        lodash.curryRight = curryRight;
        lodash.debounce = debounce;
        lodash.defaults = defaults2;
        lodash.defaultsDeep = defaultsDeep;
        lodash.defer = defer3;
        lodash.delay = delay;
        lodash.difference = difference;
        lodash.differenceBy = differenceBy;
        lodash.differenceWith = differenceWith;
        lodash.drop = drop;
        lodash.dropRight = dropRight;
        lodash.dropRightWhile = dropRightWhile;
        lodash.dropWhile = dropWhile;
        lodash.fill = fill;
        lodash.filter = filter;
        lodash.flatMap = flatMap;
        lodash.flatMapDeep = flatMapDeep;
        lodash.flatMapDepth = flatMapDepth;
        lodash.flatten = flatten;
        lodash.flattenDeep = flattenDeep;
        lodash.flattenDepth = flattenDepth;
        lodash.flip = flip;
        lodash.flow = flow;
        lodash.flowRight = flowRight;
        lodash.fromPairs = fromPairs;
        lodash.functions = functions;
        lodash.functionsIn = functionsIn;
        lodash.groupBy = groupBy;
        lodash.initial = initial;
        lodash.intersection = intersection;
        lodash.intersectionBy = intersectionBy;
        lodash.intersectionWith = intersectionWith;
        lodash.invert = invert;
        lodash.invertBy = invertBy;
        lodash.invokeMap = invokeMap;
        lodash.iteratee = iteratee;
        lodash.keyBy = keyBy;
        lodash.keys = keys;
        lodash.keysIn = keysIn;
        lodash.map = map;
        lodash.mapKeys = mapKeys;
        lodash.mapValues = mapValues;
        lodash.matches = matches;
        lodash.matchesProperty = matchesProperty;
        lodash.memoize = memoize;
        lodash.merge = merge;
        lodash.mergeWith = mergeWith;
        lodash.method = method;
        lodash.methodOf = methodOf;
        lodash.mixin = mixin;
        lodash.negate = negate;
        lodash.nthArg = nthArg;
        lodash.omit = omit;
        lodash.omitBy = omitBy;
        lodash.once = once;
        lodash.orderBy = orderBy;
        lodash.over = over;
        lodash.overArgs = overArgs;
        lodash.overEvery = overEvery;
        lodash.overSome = overSome;
        lodash.partial = partial;
        lodash.partialRight = partialRight;
        lodash.partition = partition;
        lodash.pick = pick;
        lodash.pickBy = pickBy;
        lodash.property = property;
        lodash.propertyOf = propertyOf;
        lodash.pull = pull;
        lodash.pullAll = pullAll;
        lodash.pullAllBy = pullAllBy;
        lodash.pullAllWith = pullAllWith;
        lodash.pullAt = pullAt;
        lodash.range = range;
        lodash.rangeRight = rangeRight;
        lodash.rearg = rearg;
        lodash.reject = reject;
        lodash.remove = remove;
        lodash.rest = rest;
        lodash.reverse = reverse;
        lodash.sampleSize = sampleSize;
        lodash.set = set3;
        lodash.setWith = setWith;
        lodash.shuffle = shuffle;
        lodash.slice = slice2;
        lodash.sortBy = sortBy;
        lodash.sortedUniq = sortedUniq;
        lodash.sortedUniqBy = sortedUniqBy;
        lodash.split = split;
        lodash.spread = spread;
        lodash.tail = tail;
        lodash.take = take;
        lodash.takeRight = takeRight;
        lodash.takeRightWhile = takeRightWhile;
        lodash.takeWhile = takeWhile;
        lodash.tap = tap;
        lodash.throttle = throttle;
        lodash.thru = thru;
        lodash.toArray = toArray;
        lodash.toPairs = toPairs;
        lodash.toPairsIn = toPairsIn;
        lodash.toPath = toPath;
        lodash.toPlainObject = toPlainObject;
        lodash.transform = transform;
        lodash.unary = unary;
        lodash.union = union;
        lodash.unionBy = unionBy;
        lodash.unionWith = unionWith;
        lodash.uniq = uniq;
        lodash.uniqBy = uniqBy;
        lodash.uniqWith = uniqWith;
        lodash.unset = unset;
        lodash.unzip = unzip;
        lodash.unzipWith = unzipWith;
        lodash.update = update;
        lodash.updateWith = updateWith;
        lodash.values = values;
        lodash.valuesIn = valuesIn;
        lodash.without = without;
        lodash.words = words;
        lodash.wrap = wrap;
        lodash.xor = xor;
        lodash.xorBy = xorBy;
        lodash.xorWith = xorWith;
        lodash.zip = zip;
        lodash.zipObject = zipObject;
        lodash.zipObjectDeep = zipObjectDeep;
        lodash.zipWith = zipWith;
        lodash.entries = toPairs;
        lodash.entriesIn = toPairsIn;
        lodash.extend = assignIn;
        lodash.extendWith = assignInWith;
        mixin(lodash, lodash);
        lodash.add = add;
        lodash.attempt = attempt;
        lodash.camelCase = camelCase;
        lodash.capitalize = capitalize;
        lodash.ceil = ceil;
        lodash.clamp = clamp;
        lodash.clone = clone;
        lodash.cloneDeep = cloneDeep;
        lodash.cloneDeepWith = cloneDeepWith;
        lodash.cloneWith = cloneWith;
        lodash.conformsTo = conformsTo;
        lodash.deburr = deburr;
        lodash.defaultTo = defaultTo;
        lodash.divide = divide;
        lodash.endsWith = endsWith;
        lodash.eq = eq;
        lodash.escape = escape;
        lodash.escapeRegExp = escapeRegExp;
        lodash.every = every;
        lodash.find = find;
        lodash.findIndex = findIndex;
        lodash.findKey = findKey;
        lodash.findLast = findLast;
        lodash.findLastIndex = findLastIndex;
        lodash.findLastKey = findLastKey;
        lodash.floor = floor;
        lodash.forEach = forEach;
        lodash.forEachRight = forEachRight;
        lodash.forIn = forIn;
        lodash.forInRight = forInRight;
        lodash.forOwn = forOwn;
        lodash.forOwnRight = forOwnRight;
        lodash.get = get3;
        lodash.gt = gt;
        lodash.gte = gte;
        lodash.has = has;
        lodash.hasIn = hasIn;
        lodash.head = head;
        lodash.identity = identity;
        lodash.includes = includes;
        lodash.indexOf = indexOf;
        lodash.inRange = inRange;
        lodash.invoke = invoke;
        lodash.isArguments = isArguments;
        lodash.isArray = isArray3;
        lodash.isArrayBuffer = isArrayBuffer;
        lodash.isArrayLike = isArrayLike;
        lodash.isArrayLikeObject = isArrayLikeObject;
        lodash.isBoolean = isBoolean;
        lodash.isBuffer = isBuffer;
        lodash.isDate = isDate;
        lodash.isElement = isElement;
        lodash.isEmpty = isEmpty2;
        lodash.isEqual = isEqual3;
        lodash.isEqualWith = isEqualWith;
        lodash.isError = isError;
        lodash.isFinite = isFinite2;
        lodash.isFunction = isFunction2;
        lodash.isInteger = isInteger;
        lodash.isLength = isLength;
        lodash.isMap = isMap;
        lodash.isMatch = isMatch;
        lodash.isMatchWith = isMatchWith;
        lodash.isNaN = isNaN2;
        lodash.isNative = isNative;
        lodash.isNil = isNil;
        lodash.isNull = isNull;
        lodash.isNumber = isNumber3;
        lodash.isObject = isObject4;
        lodash.isObjectLike = isObjectLike;
        lodash.isPlainObject = isPlainObject;
        lodash.isRegExp = isRegExp;
        lodash.isSafeInteger = isSafeInteger;
        lodash.isSet = isSet;
        lodash.isString = isString;
        lodash.isSymbol = isSymbol;
        lodash.isTypedArray = isTypedArray;
        lodash.isUndefined = isUndefined;
        lodash.isWeakMap = isWeakMap;
        lodash.isWeakSet = isWeakSet;
        lodash.join = join;
        lodash.kebabCase = kebabCase;
        lodash.last = last;
        lodash.lastIndexOf = lastIndexOf;
        lodash.lowerCase = lowerCase;
        lodash.lowerFirst = lowerFirst;
        lodash.lt = lt;
        lodash.lte = lte;
        lodash.max = max;
        lodash.maxBy = maxBy;
        lodash.mean = mean;
        lodash.meanBy = meanBy;
        lodash.min = min;
        lodash.minBy = minBy;
        lodash.stubArray = stubArray;
        lodash.stubFalse = stubFalse;
        lodash.stubObject = stubObject;
        lodash.stubString = stubString;
        lodash.stubTrue = stubTrue;
        lodash.multiply = multiply;
        lodash.nth = nth;
        lodash.noConflict = noConflict;
        lodash.noop = noop;
        lodash.now = now2;
        lodash.pad = pad;
        lodash.padEnd = padEnd;
        lodash.padStart = padStart;
        lodash.parseInt = parseInt2;
        lodash.random = random;
        lodash.reduce = reduce;
        lodash.reduceRight = reduceRight;
        lodash.repeat = repeat;
        lodash.replace = replace;
        lodash.result = result;
        lodash.round = round;
        lodash.runInContext = runInContext2;
        lodash.sample = sample;
        lodash.size = size;
        lodash.snakeCase = snakeCase;
        lodash.some = some;
        lodash.sortedIndex = sortedIndex;
        lodash.sortedIndexBy = sortedIndexBy;
        lodash.sortedIndexOf = sortedIndexOf;
        lodash.sortedLastIndex = sortedLastIndex;
        lodash.sortedLastIndexBy = sortedLastIndexBy;
        lodash.sortedLastIndexOf = sortedLastIndexOf;
        lodash.startCase = startCase;
        lodash.startsWith = startsWith;
        lodash.subtract = subtract;
        lodash.sum = sum;
        lodash.sumBy = sumBy;
        lodash.template = template;
        lodash.times = times;
        lodash.toFinite = toFinite;
        lodash.toInteger = toInteger;
        lodash.toLength = toLength;
        lodash.toLower = toLower;
        lodash.toNumber = toNumber;
        lodash.toSafeInteger = toSafeInteger;
        lodash.toString = toString;
        lodash.toUpper = toUpper2;
        lodash.trim = trim;
        lodash.trimEnd = trimEnd;
        lodash.trimStart = trimStart;
        lodash.truncate = truncate;
        lodash.unescape = unescape2;
        lodash.uniqueId = uniqueId;
        lodash.upperCase = upperCase;
        lodash.upperFirst = upperFirst;
        lodash.each = forEach;
        lodash.eachRight = forEachRight;
        lodash.first = head;
        mixin(lodash, function() {
          var source = {};
          baseForOwn(lodash, function(func, methodName) {
            if (!hasOwnProperty.call(lodash.prototype, methodName)) {
              source[methodName] = func;
            }
          });
          return source;
        }(), { "chain": false });
        lodash.VERSION = VERSION;
        arrayEach(["bind", "bindKey", "curry", "curryRight", "partial", "partialRight"], function(methodName) {
          lodash[methodName].placeholder = lodash;
        });
        arrayEach(["drop", "take"], function(methodName, index) {
          LazyWrapper.prototype[methodName] = function(n) {
            n = n === undefined2 ? 1 : nativeMax(toInteger(n), 0);
            var result2 = this.__filtered__ && !index ? new LazyWrapper(this) : this.clone();
            if (result2.__filtered__) {
              result2.__takeCount__ = nativeMin(n, result2.__takeCount__);
            } else {
              result2.__views__.push({
                "size": nativeMin(n, MAX_ARRAY_LENGTH),
                "type": methodName + (result2.__dir__ < 0 ? "Right" : "")
              });
            }
            return result2;
          };
          LazyWrapper.prototype[methodName + "Right"] = function(n) {
            return this.reverse()[methodName](n).reverse();
          };
        });
        arrayEach(["filter", "map", "takeWhile"], function(methodName, index) {
          var type = index + 1, isFilter = type == LAZY_FILTER_FLAG || type == LAZY_WHILE_FLAG;
          LazyWrapper.prototype[methodName] = function(iteratee2) {
            var result2 = this.clone();
            result2.__iteratees__.push({
              "iteratee": getIteratee(iteratee2, 3),
              "type": type
            });
            result2.__filtered__ = result2.__filtered__ || isFilter;
            return result2;
          };
        });
        arrayEach(["head", "last"], function(methodName, index) {
          var takeName = "take" + (index ? "Right" : "");
          LazyWrapper.prototype[methodName] = function() {
            return this[takeName](1).value()[0];
          };
        });
        arrayEach(["initial", "tail"], function(methodName, index) {
          var dropName = "drop" + (index ? "" : "Right");
          LazyWrapper.prototype[methodName] = function() {
            return this.__filtered__ ? new LazyWrapper(this) : this[dropName](1);
          };
        });
        LazyWrapper.prototype.compact = function() {
          return this.filter(identity);
        };
        LazyWrapper.prototype.find = function(predicate) {
          return this.filter(predicate).head();
        };
        LazyWrapper.prototype.findLast = function(predicate) {
          return this.reverse().find(predicate);
        };
        LazyWrapper.prototype.invokeMap = baseRest(function(path, args) {
          if (typeof path == "function") {
            return new LazyWrapper(this);
          }
          return this.map(function(value) {
            return baseInvoke(value, path, args);
          });
        });
        LazyWrapper.prototype.reject = function(predicate) {
          return this.filter(negate(getIteratee(predicate)));
        };
        LazyWrapper.prototype.slice = function(start, end) {
          start = toInteger(start);
          var result2 = this;
          if (result2.__filtered__ && (start > 0 || end < 0)) {
            return new LazyWrapper(result2);
          }
          if (start < 0) {
            result2 = result2.takeRight(-start);
          } else if (start) {
            result2 = result2.drop(start);
          }
          if (end !== undefined2) {
            end = toInteger(end);
            result2 = end < 0 ? result2.dropRight(-end) : result2.take(end - start);
          }
          return result2;
        };
        LazyWrapper.prototype.takeRightWhile = function(predicate) {
          return this.reverse().takeWhile(predicate).reverse();
        };
        LazyWrapper.prototype.toArray = function() {
          return this.take(MAX_ARRAY_LENGTH);
        };
        baseForOwn(LazyWrapper.prototype, function(func, methodName) {
          var checkIteratee = /^(?:filter|find|map|reject)|While$/.test(methodName), isTaker = /^(?:head|last)$/.test(methodName), lodashFunc = lodash[isTaker ? "take" + (methodName == "last" ? "Right" : "") : methodName], retUnwrapped = isTaker || /^find/.test(methodName);
          if (!lodashFunc) {
            return;
          }
          lodash.prototype[methodName] = function() {
            var value = this.__wrapped__, args = isTaker ? [1] : arguments, isLazy = value instanceof LazyWrapper, iteratee2 = args[0], useLazy = isLazy || isArray3(value);
            var interceptor = function(value2) {
              var result3 = lodashFunc.apply(lodash, arrayPush([value2], args));
              return isTaker && chainAll ? result3[0] : result3;
            };
            if (useLazy && checkIteratee && typeof iteratee2 == "function" && iteratee2.length != 1) {
              isLazy = useLazy = false;
            }
            var chainAll = this.__chain__, isHybrid = !!this.__actions__.length, isUnwrapped = retUnwrapped && !chainAll, onlyLazy = isLazy && !isHybrid;
            if (!retUnwrapped && useLazy) {
              value = onlyLazy ? value : new LazyWrapper(this);
              var result2 = func.apply(value, args);
              result2.__actions__.push({ "func": thru, "args": [interceptor], "thisArg": undefined2 });
              return new LodashWrapper(result2, chainAll);
            }
            if (isUnwrapped && onlyLazy) {
              return func.apply(this, args);
            }
            result2 = this.thru(interceptor);
            return isUnwrapped ? isTaker ? result2.value()[0] : result2.value() : result2;
          };
        });
        arrayEach(["pop", "push", "shift", "sort", "splice", "unshift"], function(methodName) {
          var func = arrayProto[methodName], chainName = /^(?:push|sort|unshift)$/.test(methodName) ? "tap" : "thru", retUnwrapped = /^(?:pop|shift)$/.test(methodName);
          lodash.prototype[methodName] = function() {
            var args = arguments;
            if (retUnwrapped && !this.__chain__) {
              var value = this.value();
              return func.apply(isArray3(value) ? value : [], args);
            }
            return this[chainName](function(value2) {
              return func.apply(isArray3(value2) ? value2 : [], args);
            });
          };
        });
        baseForOwn(LazyWrapper.prototype, function(func, methodName) {
          var lodashFunc = lodash[methodName];
          if (lodashFunc) {
            var key = lodashFunc.name + "";
            if (!hasOwnProperty.call(realNames, key)) {
              realNames[key] = [];
            }
            realNames[key].push({ "name": methodName, "func": lodashFunc });
          }
        });
        realNames[createHybrid(undefined2, WRAP_BIND_KEY_FLAG).name] = [{
          "name": "wrapper",
          "func": undefined2
        }];
        LazyWrapper.prototype.clone = lazyClone;
        LazyWrapper.prototype.reverse = lazyReverse;
        LazyWrapper.prototype.value = lazyValue;
        lodash.prototype.at = wrapperAt;
        lodash.prototype.chain = wrapperChain;
        lodash.prototype.commit = wrapperCommit;
        lodash.prototype.next = wrapperNext;
        lodash.prototype.plant = wrapperPlant;
        lodash.prototype.reverse = wrapperReverse;
        lodash.prototype.toJSON = lodash.prototype.valueOf = lodash.prototype.value = wrapperValue;
        lodash.prototype.first = lodash.prototype.head;
        if (symIterator) {
          lodash.prototype[symIterator] = wrapperToIterator;
        }
        return lodash;
      };
      var _ = runInContext();
      if (typeof define == "function" && typeof define.amd == "object" && define.amd) {
        root._ = _;
        define(function() {
          return _;
        });
      } else if (freeModule) {
        (freeModule.exports = _)._ = _;
        freeExports._ = _;
      } else {
        root._ = _;
      }
    }).call(exports2);
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/constants.js
var require_constants = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/constants.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.MAX_NGINX_URL_LENGTH = exports2.ORIGINAL_PROTOCOL_HEADER = exports2.ORIGINAL_HOST_HEADER = exports2.ORIGINAL_URL_HEADER = exports2.SSR_PROTOCOL = exports2.SSR_HOST = exports2.NO_SCENARIO_FOUND = void 0;
    exports2.NO_SCENARIO_FOUND = "NO_SCENARIO_FOUND";
    exports2.SSR_HOST = "www.wix.com";
    exports2.SSR_PROTOCOL = "http";
    exports2.ORIGINAL_URL_HEADER = "x-wix-metro-original-url";
    exports2.ORIGINAL_HOST_HEADER = "x-wix-metro-original-host";
    exports2.ORIGINAL_PROTOCOL_HEADER = "x-wix-metro-original-protocol";
    exports2.MAX_NGINX_URL_LENGTH = 9216;
  }
});

// ../../node_modules/@wix/headers/dist/esm/globals.js
function maybeGetDocument() {
  if (typeof document === "object") {
    return document;
  }
  return void 0;
}
function maybeGetGlobal() {
  if (typeof self === "object") {
    return self;
  } else {
    return global;
  }
}
var init_globals = __esm({
  "../../node_modules/@wix/headers/dist/esm/globals.js"() {
    "use strict";
  }
});

// ../../node_modules/@wix/headers/dist/esm/cookies.js
function readCookie(name) {
  const cookieValue = getCookieByName(name);
  if (cookieValue) {
    return cookieValue.split("=")[1];
  }
  return "";
}
function loadCookies() {
  const documentObj = maybeGetDocument();
  if (documentObj && documentObj.cookie) {
    return decodeURIComponent(documentObj.cookie).split(";");
  }
  return [];
}
function getCookieByName(name) {
  return loadCookies().filter((cookie) => name === cookie.split("=")[0].trim())[0];
}
var init_cookies = __esm({
  "../../node_modules/@wix/headers/dist/esm/cookies.js"() {
    "use strict";
    init_globals();
  }
});

// ../../node_modules/@wix/headers/dist/esm/headers/csrf.js
function csrf() {
  const value = readCookie(XSRF_TOKEN_COOKIE_NAME);
  return { [XSRF_HEADER_NAME]: value };
}
var XSRF_TOKEN_COOKIE_NAME, XSRF_HEADER_NAME;
var init_csrf = __esm({
  "../../node_modules/@wix/headers/dist/esm/headers/csrf.js"() {
    "use strict";
    init_cookies();
    XSRF_TOKEN_COOKIE_NAME = "XSRF-TOKEN";
    XSRF_HEADER_NAME = "x-xsrf-token";
  }
});

// ../../node_modules/@wix/headers/dist/esm/headers/x-wix-brand.js
function xWixBrand() {
  const value = extractBrand();
  return { [X_WIX_BRAND_NAME]: value };
}
function extractBrand() {
  const globalObj = maybeGetGlobal();
  if (globalObj && globalObj.commonConfig && typeof globalObj.commonConfig.brand === "string") {
    return globalObj.commonConfig.brand;
  }
  return "";
}
var X_WIX_BRAND_NAME;
var init_x_wix_brand = __esm({
  "../../node_modules/@wix/headers/dist/esm/headers/x-wix-brand.js"() {
    "use strict";
    init_globals();
    X_WIX_BRAND_NAME = "x-wix-brand";
  }
});

// ../../node_modules/@wix/headers/dist/esm/headers/consent-policy.js
function consentPolicy() {
  return tryResolveNativeAPI() || tryResolveJsSDK() || none();
}
function tryResolveJsSDK() {
  const globalObj = maybeGetGlobal();
  if (globalObj && globalObj.Wix && globalObj.Wix.Utils && typeof globalObj.Wix.Utils._getConsentPolicyHeader === "function") {
    return globalObj.Wix.Utils._getConsentPolicyHeader();
  }
  return void 0;
}
function tryResolveNativeAPI() {
  const globalObj = maybeGetGlobal();
  if (globalObj && globalObj.consentPolicyManager && typeof globalObj.consentPolicyManager._getConsentPolicyHeader === "function") {
    return globalObj.consentPolicyManager._getConsentPolicyHeader();
  }
  return void 0;
}
function none() {
  return {};
}
var init_consent_policy = __esm({
  "../../node_modules/@wix/headers/dist/esm/headers/consent-policy.js"() {
    "use strict";
    init_globals();
  }
});

// ../../node_modules/@wix/headers/dist/esm/headers/authorization.js
function authorization(signedInstance) {
  return { authorization: signedInstance };
}
var init_authorization = __esm({
  "../../node_modules/@wix/headers/dist/esm/headers/authorization.js"() {
    "use strict";
  }
});

// ../../node_modules/@wix/headers/dist/esm/utils.js
function isCI() {
  if (isNode()) {
    return !!process.env.TEAMCITY_VERSION || !!process.env.BUILDKITE;
  }
  return false;
}
function isNode() {
  return typeof process !== "undefined" && process.versions?.node != null;
}
var init_utils = __esm({
  "../../node_modules/@wix/headers/dist/esm/utils.js"() {
    "use strict";
  }
});

// ../../node_modules/@wix/headers/dist/esm/headers/artifact-id.js
function artifactId(override) {
  const artifactIdToUse = isNode() ? process.env.ARTIFACT_ID ?? process.env.APP_NAME ?? "" : "";
  return {
    "X-Wix-Client-Artifact-Id": override ?? (!isCI() ? artifactIdToUse : "")
  };
}
var init_artifact_id = __esm({
  "../../node_modules/@wix/headers/dist/esm/headers/artifact-id.js"() {
    "use strict";
    init_utils();
  }
});

// ../../node_modules/@wix/headers/dist/esm/headers/common-config.js
function extractCommonConfigValues() {
  const globalObj = maybeGetGlobal();
  const commonConfigObj = globalObj?.commonConfig;
  if (!commonConfigObj) {
    return null;
  }
  const res = {};
  let addedKeys = false;
  Object.keys(commonConfigObj).forEach((key) => {
    const newKey = MAP_KEYS[key];
    if (newKey) {
      res[newKey] = commonConfigObj[key];
      addedKeys = true;
    } else if (IGNORE_KEYS.indexOf(key) < 0 && typeof commonConfigObj[key] !== "function") {
      res[key] = commonConfigObj[key];
      addedKeys = true;
    }
  });
  return addedKeys ? res : null;
}
var IGNORE_KEYS, COMMON_CONFIG_NAME, MAP_KEYS, commonConfig;
var init_common_config = __esm({
  "../../node_modules/@wix/headers/dist/esm/headers/common-config.js"() {
    "use strict";
    init_globals();
    IGNORE_KEYS = ["consentPolicy", "consentPolicyHeader"];
    COMMON_CONFIG_NAME = "commonConfig";
    MAP_KEYS = {
      bsi: "BSI"
    };
    commonConfig = () => {
      const commonConfigValues = extractCommonConfigValues();
      const value = commonConfigValues ? JSON.stringify(commonConfigValues) : "";
      return { [COMMON_CONFIG_NAME]: encodeURIComponent(value) };
    };
  }
});

// ../../node_modules/base-64/base64.js
var require_base64 = __commonJS({
  "../../node_modules/base-64/base64.js"(exports2, module2) {
    "use strict";
    (function(root) {
      var freeExports = typeof exports2 == "object" && exports2;
      var freeModule = typeof module2 == "object" && module2 && module2.exports == freeExports && module2;
      var freeGlobal = typeof global == "object" && global;
      if (freeGlobal.global === freeGlobal || freeGlobal.window === freeGlobal) {
        root = freeGlobal;
      }
      var InvalidCharacterError = function(message) {
        this.message = message;
      };
      InvalidCharacterError.prototype = new Error();
      InvalidCharacterError.prototype.name = "InvalidCharacterError";
      var error = function(message) {
        throw new InvalidCharacterError(message);
      };
      var TABLE = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
      var REGEX_SPACE_CHARACTERS = /[\t\n\f\r ]/g;
      var decode = function(input) {
        input = String(input).replace(REGEX_SPACE_CHARACTERS, "");
        var length = input.length;
        if (length % 4 == 0) {
          input = input.replace(/==?$/, "");
          length = input.length;
        }
        if (length % 4 == 1 || // http://whatwg.org/C#alphanumeric-ascii-characters
        /[^+a-zA-Z0-9/]/.test(input)) {
          error(
            "Invalid character: the string to be decoded is not correctly encoded."
          );
        }
        var bitCounter = 0;
        var bitStorage;
        var buffer;
        var output = "";
        var position = -1;
        while (++position < length) {
          buffer = TABLE.indexOf(input.charAt(position));
          bitStorage = bitCounter % 4 ? bitStorage * 64 + buffer : buffer;
          if (bitCounter++ % 4) {
            output += String.fromCharCode(
              255 & bitStorage >> (-2 * bitCounter & 6)
            );
          }
        }
        return output;
      };
      var encode = function(input) {
        input = String(input);
        if (/[^\0-\xFF]/.test(input)) {
          error(
            "The string to be encoded contains characters outside of the Latin1 range."
          );
        }
        var padding = input.length % 3;
        var output = "";
        var position = -1;
        var a;
        var b;
        var c;
        var buffer;
        var length = input.length - padding;
        while (++position < length) {
          a = input.charCodeAt(position) << 16;
          b = input.charCodeAt(++position) << 8;
          c = input.charCodeAt(++position);
          buffer = a + b + c;
          output += TABLE.charAt(buffer >> 18 & 63) + TABLE.charAt(buffer >> 12 & 63) + TABLE.charAt(buffer >> 6 & 63) + TABLE.charAt(buffer & 63);
        }
        if (padding == 2) {
          a = input.charCodeAt(position) << 8;
          b = input.charCodeAt(++position);
          buffer = a + b;
          output += TABLE.charAt(buffer >> 10) + TABLE.charAt(buffer >> 4 & 63) + TABLE.charAt(buffer << 2 & 63) + "=";
        } else if (padding == 1) {
          buffer = input.charCodeAt(position);
          output += TABLE.charAt(buffer >> 2) + TABLE.charAt(buffer << 4 & 63) + "==";
        }
        return output;
      };
      var base642 = {
        "encode": encode,
        "decode": decode,
        "version": "1.0.0"
      };
      if (typeof define == "function" && typeof define.amd == "object" && define.amd) {
        define(function() {
          return base642;
        });
      } else if (freeExports && !freeExports.nodeType) {
        if (freeModule) {
          freeModule.exports = base642;
        } else {
          for (var key in base642) {
            base642.hasOwnProperty(key) && (freeExports[key] = base642[key]);
          }
        }
      } else {
        root.base64 = base642;
      }
    })(exports2);
  }
});

// ../../node_modules/@wix/headers/dist/esm/headers/linguist.js
function linguistHeader({ lang, locale, isPrimaryLanguage, signedInstance }) {
  if (isMultilingualOptionsAreValid({ lang, locale, isPrimaryLanguage })) {
    const instanceId = getInstanceIdFromSignedInstance(signedInstance);
    if (instanceId !== void 0) {
      return {
        [LINGUIST_HEADER_KEY]: [
          lang,
          locale,
          isPrimaryLanguage?.toString(),
          instanceId
        ].join("|")
      };
    }
  }
  return {};
}
function isMultilingualOptionsAreValid({ lang, locale, isPrimaryLanguage }) {
  return lang && locale && /^(true|false)$/.test(isPrimaryLanguage?.toString() || "");
}
function getInstanceIdFromSignedInstance(signedInstance) {
  try {
    const encodedInstance = signedInstance?.startsWith("wixcode") ? signedInstance?.split(".")[2] : signedInstance?.split(".")[1];
    if (encodedInstance) {
      return JSON.parse(import_base_64.default.decode(encodedInstance)).instanceId;
    }
  } catch (e) {
  }
}
var import_base_64, LINGUIST_HEADER_KEY;
var init_linguist = __esm({
  "../../node_modules/@wix/headers/dist/esm/headers/linguist.js"() {
    "use strict";
    import_base_64 = __toESM(require_base64());
    LINGUIST_HEADER_KEY = "x-wix-linguist";
  }
});

// ../../node_modules/@wix/headers/dist/esm/errors.js
var WixHeadersValidationError;
var init_errors = __esm({
  "../../node_modules/@wix/headers/dist/esm/errors.js"() {
    "use strict";
    WixHeadersValidationError = class extends Error {
      constructor(type, key, value) {
        super(`WixHeadersValidationError: expected ${key} to be ${type} but got ${JSON.stringify(value)}`);
      }
    };
  }
});

// ../../node_modules/@wix/headers/dist/esm/assert.js
function assertBoolean(key, value) {
  if (typeof value !== "boolean") {
    throw new WixHeadersValidationError("boolean", key, value);
  }
}
function assertString(key, value) {
  if (typeof value !== "string") {
    throw new WixHeadersValidationError("string", key, value);
  }
}
var init_assert = __esm({
  "../../node_modules/@wix/headers/dist/esm/assert.js"() {
    "use strict";
    init_errors();
  }
});

// ../../node_modules/@wix/headers/dist/esm/index.js
var esm_exports = {};
__export(esm_exports, {
  createHeaders: () => createHeaders
});
function createHeaders(opts = {}) {
  opts = { csrf: true, signedInstance: "", ...opts };
  verifyOptsOrThrow(opts);
  const headers = [
    xWixBrand(),
    consentPolicy(),
    authorization(opts.signedInstance),
    artifactId(opts.artifactId),
    commonConfig(),
    linguistHeader({
      signedInstance: opts.signedInstance,
      ...opts.multilingualOptions
    })
  ];
  if (opts.csrf) {
    headers.push(csrf());
  }
  return headers.filter((hdrs) => Object.values(hdrs).every((v) => v)).reduce((result, hdrs) => ({
    ...result,
    ...hdrs
  }), {});
}
function verifyOptsOrThrow(opts) {
  assertBoolean("opts.csrf", opts.csrf);
  assertString("opts.signedInstance", opts.signedInstance);
}
var init_esm = __esm({
  "../../node_modules/@wix/headers/dist/esm/index.js"() {
    "use strict";
    init_csrf();
    init_x_wix_brand();
    init_consent_policy();
    init_authorization();
    init_artifact_id();
    init_common_config();
    init_linguist();
    init_assert();
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/env-util.js
var require_env_util = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/env-util.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.isProduction = exports2.isNode = exports2.isWebWorker = exports2.isBrowser = void 0;
    function isBrowser() {
      return typeof window !== "undefined";
    }
    exports2.isBrowser = isBrowser;
    function isWebWorker() {
      var _a;
      return typeof self === "object" && ((_a = self === null || self === void 0 ? void 0 : self.constructor) === null || _a === void 0 ? void 0 : _a.name) === "DedicatedWorkerGlobalScope";
    }
    exports2.isWebWorker = isWebWorker;
    function isNode2() {
      var _a;
      return typeof process !== "undefined" && ((_a = process === null || process === void 0 ? void 0 : process.versions) === null || _a === void 0 ? void 0 : _a.node) != null;
    }
    exports2.isNode = isNode2;
    function isProduction() {
      return process.env.NODE_ENV === "production";
    }
    exports2.isProduction = isProduction;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/utils.js
var require_utils = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/utils.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.toSearchQueryParams = exports2.shouldAllowUnmocked = exports2.isHttpMockServerEnabled = exports2.getGlobalConfig = exports2.changeHost = exports2.extractHost = exports2.extractProtocol = exports2.resolveProtocol = exports2.resolveHost = exports2.extractProp = exports2.silent = exports2.isAbsoluteUrl = void 0;
    var constants_1 = require_constants();
    var env_util_1 = require_env_util();
    function isAbsoluteUrl(url) {
      if (typeof url !== "string") {
        throw new TypeError(`Expected a \`string\`, got \`${typeof url}\``);
      }
      return /(^https?:)|(^\/\/)/.test(url);
    }
    exports2.isAbsoluteUrl = isAbsoluteUrl;
    async function silent(fn) {
      try {
        await fn();
      } catch (_a) {
      }
    }
    exports2.silent = silent;
    function extractProp(data, path) {
      return path.split(".").filter((prop) => prop).reduce((result, prop) => result[prop], data);
    }
    exports2.extractProp = extractProp;
    function resolveHost2(opts) {
      if (opts.baseURL) {
        return extractHost(opts.baseURL);
      } else if (opts.isSSR) {
        return constants_1.SSR_HOST;
      } else if (typeof self !== "undefined" && !!self.location) {
        return self.location.host;
      } else {
        return "";
      }
    }
    exports2.resolveHost = resolveHost2;
    function resolveProtocol(opts) {
      if (opts.baseURL) {
        return extractProtocol(opts.baseURL);
      } else if (opts.isSSR) {
        return constants_1.SSR_PROTOCOL;
      } else if (typeof self !== "undefined" && !!self.location) {
        return self.location.protocol;
      } else {
        return "";
      }
    }
    exports2.resolveProtocol = resolveProtocol;
    function extractProtocol(url) {
      try {
        const asUrl = new URL(url);
        return asUrl.protocol;
      } catch (_a) {
        const hostAndProtocol = extractHostAndProtocolUsingRegExp(url);
        return hostAndProtocol.protocol;
      }
    }
    exports2.extractProtocol = extractProtocol;
    function extractHost(url) {
      let asUrl;
      try {
        asUrl = new URL(url);
      } catch (_a) {
        asUrl = new URL(`http:${url}`);
      }
      try {
        return asUrl.host;
      } catch (_b) {
        const hostAndProtocol = extractHostAndProtocolUsingRegExp(url);
        return hostAndProtocol.host;
      }
    }
    exports2.extractHost = extractHost;
    function extractHostAndProtocolUsingRegExp(url) {
      const regExp = new RegExp("^(.*:)//([A-Za-z0-9-.]+)(:[0-9]+)?(.*)$");
      try {
        const urlOptions = regExp.exec(url);
        return { protocol: urlOptions[1], host: urlOptions[2] };
      } catch (_a) {
        return { protocol: "http:", host: "" };
      }
    }
    function changeHost(urlOrPath, host) {
      let url;
      try {
        url = new URL(urlOrPath);
        url.host = host;
        url.protocol = "http";
      } catch (_a) {
        url = new URL(`http://${host}`);
        url.pathname = urlOrPath;
      }
      return url.toString();
    }
    exports2.changeHost = changeHost;
    function getGlobalConfig() {
      const defaultConfig = { httpMockServer: { enabled: false } };
      let globalConfig;
      if ((0, env_util_1.isNode)() && process.env.HTTP_CLIENT_GLOBAL_CONFIG) {
        try {
          globalConfig = JSON.parse(process.env.HTTP_CLIENT_GLOBAL_CONFIG);
        } catch (e) {
        }
      }
      if (!globalConfig) {
        if ((0, env_util_1.isBrowser)()) {
          globalConfig = window._httpClientGlobalConfig;
        } else if ((0, env_util_1.isWebWorker)()) {
        }
      }
      return globalConfig || defaultConfig;
    }
    exports2.getGlobalConfig = getGlobalConfig;
    function isHttpMockServerEnabled() {
      const globalConfig = getGlobalConfig();
      return globalConfig.httpMockServer.enabled;
    }
    exports2.isHttpMockServerEnabled = isHttpMockServerEnabled;
    function shouldAllowUnmocked() {
      const globalConfig = getGlobalConfig();
      return globalConfig.httpMockServer.allowUnmocked;
    }
    exports2.shouldAllowUnmocked = shouldAllowUnmocked;
    function toSearchQueryParams(params) {
      return Object.entries(params).map(([key, value]) => `${key}=${encodeURIComponent(value)}`).flat().join("&");
    }
    exports2.toSearchQueryParams = toSearchQueryParams;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/whitelist.json
var require_whitelist = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/whitelist.json"(exports2, module2) {
    module2.exports = [
      "wix.com",
      "editorx.com",
      "wix-code.com",
      "wixapps.net",
      "wixprod.net"
    ];
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/headers.js
var require_headers = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/headers.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.isWixDomain = exports2.composeHeaders = exports2.requestIdOrEmptyString = void 0;
    var tslib_1 = require("tslib");
    var headers_1 = (init_esm(), __toCommonJS(esm_exports));
    var utils_1 = require_utils();
    var whitelist_json_1 = tslib_1.__importDefault(require_whitelist());
    function requestIdOrEmptyString(response) {
      var _a;
      return ((_a = response === null || response === void 0 ? void 0 : response.headers) === null || _a === void 0 ? void 0 : _a["x-wix-request-id"]) || "";
    }
    exports2.requestIdOrEmptyString = requestIdOrEmptyString;
    var composeHeaders = ({ url, disableWixHeaders, includeWixHeaders, wixHeadersOpts }) => {
      const shouldAssignWixHeaders = includeWixHeaders || !disableWixHeaders && isWixDomain(url);
      return shouldAssignWixHeaders ? (0, headers_1.createHeaders)(wixHeadersOpts) : {};
    };
    exports2.composeHeaders = composeHeaders;
    function isWixDomain(url) {
      const isRelative = !(0, utils_1.isAbsoluteUrl)(url);
      if (isRelative) {
        return true;
      }
      const host = `.${(0, utils_1.extractHost)(url)}`;
      const isWhitelisted = whitelist_json_1.default.find((wixHost) => host.endsWith(`.${wixHost}`));
      return !!isWhitelisted;
    }
    exports2.isWixDomain = isWixDomain;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/error.js
var require_error = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/error.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.createHttpError = exports2.HttpError = exports2.axiosErrorFields = void 0;
    var headers_1 = require_headers();
    exports2.axiosErrorFields = [
      "code",
      "config",
      "request",
      "response",
      "toJSON",
      "__CANCEL__"
    ];
    var HttpError2 = class _HttpError extends Error {
      constructor(error) {
        super(error.message);
        this.isWixHttpError = true;
        Object.setPrototypeOf(this, _HttpError.prototype);
        exports2.axiosErrorFields.forEach((key) => {
          this[key] = error[key];
        });
      }
      get requestId() {
        return (0, headers_1.requestIdOrEmptyString)(this.response);
      }
    };
    exports2.HttpError = HttpError2;
    function createHttpError2(...args) {
      return new HttpError2(...args);
    }
    exports2.createHttpError = createHttpError2;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/bind.js
var require_bind = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/bind.js"(exports2, module2) {
    "use strict";
    module2.exports = function bind(fn, thisArg) {
      return function wrap() {
        var args = new Array(arguments.length);
        for (var i = 0; i < args.length; i++) {
          args[i] = arguments[i];
        }
        return fn.apply(thisArg, args);
      };
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/utils.js
var require_utils2 = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/utils.js"(exports2, module2) {
    "use strict";
    var bind = require_bind();
    var toString = Object.prototype.toString;
    function isArray3(val) {
      return toString.call(val) === "[object Array]";
    }
    function isUndefined(val) {
      return typeof val === "undefined";
    }
    function isBuffer(val) {
      return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor) && typeof val.constructor.isBuffer === "function" && val.constructor.isBuffer(val);
    }
    function isArrayBuffer(val) {
      return toString.call(val) === "[object ArrayBuffer]";
    }
    function isFormData(val) {
      return typeof FormData !== "undefined" && val instanceof FormData;
    }
    function isArrayBufferView(val) {
      var result;
      if (typeof ArrayBuffer !== "undefined" && ArrayBuffer.isView) {
        result = ArrayBuffer.isView(val);
      } else {
        result = val && val.buffer && val.buffer instanceof ArrayBuffer;
      }
      return result;
    }
    function isString(val) {
      return typeof val === "string";
    }
    function isNumber3(val) {
      return typeof val === "number";
    }
    function isObject4(val) {
      return val !== null && typeof val === "object";
    }
    function isPlainObject(val) {
      if (toString.call(val) !== "[object Object]") {
        return false;
      }
      var prototype = Object.getPrototypeOf(val);
      return prototype === null || prototype === Object.prototype;
    }
    function isDate(val) {
      return toString.call(val) === "[object Date]";
    }
    function isFile(val) {
      return toString.call(val) === "[object File]";
    }
    function isBlob(val) {
      return toString.call(val) === "[object Blob]";
    }
    function isFunction2(val) {
      return toString.call(val) === "[object Function]";
    }
    function isStream(val) {
      return isObject4(val) && isFunction2(val.pipe);
    }
    function isURLSearchParams(val) {
      return typeof URLSearchParams !== "undefined" && val instanceof URLSearchParams;
    }
    function trim(str) {
      return str.trim ? str.trim() : str.replace(/^\s+|\s+$/g, "");
    }
    function isStandardBrowserEnv() {
      if (typeof navigator !== "undefined" && (navigator.product === "ReactNative" || navigator.product === "NativeScript" || navigator.product === "NS")) {
        return false;
      }
      return typeof window !== "undefined" && typeof document !== "undefined";
    }
    function forEach(obj, fn) {
      if (obj === null || typeof obj === "undefined") {
        return;
      }
      if (typeof obj !== "object") {
        obj = [obj];
      }
      if (isArray3(obj)) {
        for (var i = 0, l = obj.length; i < l; i++) {
          fn.call(null, obj[i], i, obj);
        }
      } else {
        for (var key in obj) {
          if (Object.prototype.hasOwnProperty.call(obj, key)) {
            fn.call(null, obj[key], key, obj);
          }
        }
      }
    }
    function merge() {
      var result = {};
      function assignValue(val, key) {
        if (isPlainObject(result[key]) && isPlainObject(val)) {
          result[key] = merge(result[key], val);
        } else if (isPlainObject(val)) {
          result[key] = merge({}, val);
        } else if (isArray3(val)) {
          result[key] = val.slice();
        } else {
          result[key] = val;
        }
      }
      for (var i = 0, l = arguments.length; i < l; i++) {
        forEach(arguments[i], assignValue);
      }
      return result;
    }
    function extend(a, b, thisArg) {
      forEach(b, function assignValue(val, key) {
        if (thisArg && typeof val === "function") {
          a[key] = bind(val, thisArg);
        } else {
          a[key] = val;
        }
      });
      return a;
    }
    function stripBOM(content) {
      if (content.charCodeAt(0) === 65279) {
        content = content.slice(1);
      }
      return content;
    }
    module2.exports = {
      isArray: isArray3,
      isArrayBuffer,
      isBuffer,
      isFormData,
      isArrayBufferView,
      isString,
      isNumber: isNumber3,
      isObject: isObject4,
      isPlainObject,
      isUndefined,
      isDate,
      isFile,
      isBlob,
      isFunction: isFunction2,
      isStream,
      isURLSearchParams,
      isStandardBrowserEnv,
      forEach,
      merge,
      extend,
      trim,
      stripBOM
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/enhanceError.js
var require_enhanceError = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/enhanceError.js"(exports2, module2) {
    "use strict";
    module2.exports = function enhanceError(error, config, code, request, response) {
      error.config = config;
      if (code) {
        error.code = code;
      }
      error.request = request;
      error.response = response;
      error.isAxiosError = true;
      error.toJSON = function toJSON() {
        return {
          // Standard
          message: this.message,
          name: this.name,
          // Microsoft
          description: this.description,
          number: this.number,
          // Mozilla
          fileName: this.fileName,
          lineNumber: this.lineNumber,
          columnNumber: this.columnNumber,
          stack: this.stack,
          // Axios
          config: this.config,
          code: this.code
        };
      };
      return error;
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/createError.js
var require_createError = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/createError.js"(exports2, module2) {
    "use strict";
    var enhanceError = require_enhanceError();
    module2.exports = function createError(message, config, code, request, response) {
      var error = new Error(message);
      return enhanceError(error, config, code, request, response);
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/settle.js
var require_settle = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/settle.js"(exports2, module2) {
    "use strict";
    var createError = require_createError();
    module2.exports = function settle(resolve, reject, response) {
      var validateStatus = response.config.validateStatus;
      if (!response.status || !validateStatus || validateStatus(response.status)) {
        resolve(response);
      } else {
        reject(createError(
          "Request failed with status code " + response.status,
          response.config,
          null,
          response.request,
          response
        ));
      }
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/isAbsoluteURL.js
var require_isAbsoluteURL = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/isAbsoluteURL.js"(exports2, module2) {
    "use strict";
    module2.exports = function isAbsoluteURL(url) {
      return /^([a-z][a-z\d\+\-\.]*:)?\/\//i.test(url);
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/combineURLs.js
var require_combineURLs = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/combineURLs.js"(exports2, module2) {
    "use strict";
    module2.exports = function combineURLs2(baseURL, relativeURL) {
      return relativeURL ? baseURL.replace(/\/+$/, "") + "/" + relativeURL.replace(/^\/+/, "") : baseURL;
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/buildFullPath.js
var require_buildFullPath = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/buildFullPath.js"(exports2, module2) {
    "use strict";
    var isAbsoluteURL = require_isAbsoluteURL();
    var combineURLs2 = require_combineURLs();
    module2.exports = function buildFullPath(baseURL, requestedURL) {
      if (baseURL && !isAbsoluteURL(requestedURL)) {
        return combineURLs2(baseURL, requestedURL);
      }
      return requestedURL;
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/buildURL.js
var require_buildURL = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/buildURL.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    function encode(val) {
      return encodeURIComponent(val).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+").replace(/%5B/gi, "[").replace(/%5D/gi, "]");
    }
    module2.exports = function buildURL(url, params, paramsSerializer) {
      if (!params) {
        return url;
      }
      var serializedParams;
      if (paramsSerializer) {
        serializedParams = paramsSerializer(params);
      } else if (utils.isURLSearchParams(params)) {
        serializedParams = params.toString();
      } else {
        var parts = [];
        utils.forEach(params, function serialize(val, key) {
          if (val === null || typeof val === "undefined") {
            return;
          }
          if (utils.isArray(val)) {
            key = key + "[]";
          } else {
            val = [val];
          }
          utils.forEach(val, function parseValue(v) {
            if (utils.isDate(v)) {
              v = v.toISOString();
            } else if (utils.isObject(v)) {
              v = JSON.stringify(v);
            }
            parts.push(encode(key) + "=" + encode(v));
          });
        });
        serializedParams = parts.join("&");
      }
      if (serializedParams) {
        var hashmarkIndex = url.indexOf("#");
        if (hashmarkIndex !== -1) {
          url = url.slice(0, hashmarkIndex);
        }
        url += (url.indexOf("?") === -1 ? "?" : "&") + serializedParams;
      }
      return url;
    };
  }
});

// ../../node_modules/debug/node_modules/ms/index.js
var require_ms = __commonJS({
  "../../node_modules/debug/node_modules/ms/index.js"(exports2, module2) {
    "use strict";
    var s = 1e3;
    var m = s * 60;
    var h = m * 60;
    var d = h * 24;
    var w = d * 7;
    var y = d * 365.25;
    module2.exports = function(val, options) {
      options = options || {};
      var type = typeof val;
      if (type === "string" && val.length > 0) {
        return parse(val);
      } else if (type === "number" && isFinite(val)) {
        return options.long ? fmtLong(val) : fmtShort(val);
      }
      throw new Error(
        "val is not a non-empty string or a valid number. val=" + JSON.stringify(val)
      );
    };
    function parse(str) {
      str = String(str);
      if (str.length > 100) {
        return;
      }
      var match = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(
        str
      );
      if (!match) {
        return;
      }
      var n = parseFloat(match[1]);
      var type = (match[2] || "ms").toLowerCase();
      switch (type) {
        case "years":
        case "year":
        case "yrs":
        case "yr":
        case "y":
          return n * y;
        case "weeks":
        case "week":
        case "w":
          return n * w;
        case "days":
        case "day":
        case "d":
          return n * d;
        case "hours":
        case "hour":
        case "hrs":
        case "hr":
        case "h":
          return n * h;
        case "minutes":
        case "minute":
        case "mins":
        case "min":
        case "m":
          return n * m;
        case "seconds":
        case "second":
        case "secs":
        case "sec":
        case "s":
          return n * s;
        case "milliseconds":
        case "millisecond":
        case "msecs":
        case "msec":
        case "ms":
          return n;
        default:
          return void 0;
      }
    }
    function fmtShort(ms) {
      var msAbs = Math.abs(ms);
      if (msAbs >= d) {
        return Math.round(ms / d) + "d";
      }
      if (msAbs >= h) {
        return Math.round(ms / h) + "h";
      }
      if (msAbs >= m) {
        return Math.round(ms / m) + "m";
      }
      if (msAbs >= s) {
        return Math.round(ms / s) + "s";
      }
      return ms + "ms";
    }
    function fmtLong(ms) {
      var msAbs = Math.abs(ms);
      if (msAbs >= d) {
        return plural(ms, msAbs, d, "day");
      }
      if (msAbs >= h) {
        return plural(ms, msAbs, h, "hour");
      }
      if (msAbs >= m) {
        return plural(ms, msAbs, m, "minute");
      }
      if (msAbs >= s) {
        return plural(ms, msAbs, s, "second");
      }
      return ms + " ms";
    }
    function plural(ms, msAbs, n, name) {
      var isPlural = msAbs >= n * 1.5;
      return Math.round(ms / n) + " " + name + (isPlural ? "s" : "");
    }
  }
});

// ../../node_modules/debug/src/common.js
var require_common = __commonJS({
  "../../node_modules/debug/src/common.js"(exports2, module2) {
    "use strict";
    function setup(env3) {
      createDebug.debug = createDebug;
      createDebug.default = createDebug;
      createDebug.coerce = coerce;
      createDebug.disable = disable;
      createDebug.enable = enable;
      createDebug.enabled = enabled;
      createDebug.humanize = require_ms();
      createDebug.destroy = destroy;
      Object.keys(env3).forEach((key) => {
        createDebug[key] = env3[key];
      });
      createDebug.names = [];
      createDebug.skips = [];
      createDebug.formatters = {};
      function selectColor(namespace) {
        let hash = 0;
        for (let i = 0; i < namespace.length; i++) {
          hash = (hash << 5) - hash + namespace.charCodeAt(i);
          hash |= 0;
        }
        return createDebug.colors[Math.abs(hash) % createDebug.colors.length];
      }
      createDebug.selectColor = selectColor;
      function createDebug(namespace) {
        let prevTime;
        let enableOverride = null;
        let namespacesCache;
        let enabledCache;
        function debug(...args) {
          if (!debug.enabled) {
            return;
          }
          const self2 = debug;
          const curr = Number(/* @__PURE__ */ new Date());
          const ms = curr - (prevTime || curr);
          self2.diff = ms;
          self2.prev = prevTime;
          self2.curr = curr;
          prevTime = curr;
          args[0] = createDebug.coerce(args[0]);
          if (typeof args[0] !== "string") {
            args.unshift("%O");
          }
          let index = 0;
          args[0] = args[0].replace(/%([a-zA-Z%])/g, (match, format) => {
            if (match === "%%") {
              return "%";
            }
            index++;
            const formatter = createDebug.formatters[format];
            if (typeof formatter === "function") {
              const val = args[index];
              match = formatter.call(self2, val);
              args.splice(index, 1);
              index--;
            }
            return match;
          });
          createDebug.formatArgs.call(self2, args);
          const logFn = self2.log || createDebug.log;
          logFn.apply(self2, args);
        }
        debug.namespace = namespace;
        debug.useColors = createDebug.useColors();
        debug.color = createDebug.selectColor(namespace);
        debug.extend = extend;
        debug.destroy = createDebug.destroy;
        Object.defineProperty(debug, "enabled", {
          enumerable: true,
          configurable: false,
          get: () => {
            if (enableOverride !== null) {
              return enableOverride;
            }
            if (namespacesCache !== createDebug.namespaces) {
              namespacesCache = createDebug.namespaces;
              enabledCache = createDebug.enabled(namespace);
            }
            return enabledCache;
          },
          set: (v) => {
            enableOverride = v;
          }
        });
        if (typeof createDebug.init === "function") {
          createDebug.init(debug);
        }
        return debug;
      }
      function extend(namespace, delimiter) {
        const newDebug = createDebug(this.namespace + (typeof delimiter === "undefined" ? ":" : delimiter) + namespace);
        newDebug.log = this.log;
        return newDebug;
      }
      function enable(namespaces) {
        createDebug.save(namespaces);
        createDebug.namespaces = namespaces;
        createDebug.names = [];
        createDebug.skips = [];
        let i;
        const split = (typeof namespaces === "string" ? namespaces : "").split(/[\s,]+/);
        const len = split.length;
        for (i = 0; i < len; i++) {
          if (!split[i]) {
            continue;
          }
          namespaces = split[i].replace(/\*/g, ".*?");
          if (namespaces[0] === "-") {
            createDebug.skips.push(new RegExp("^" + namespaces.slice(1) + "$"));
          } else {
            createDebug.names.push(new RegExp("^" + namespaces + "$"));
          }
        }
      }
      function disable() {
        const namespaces = [
          ...createDebug.names.map(toNamespace),
          ...createDebug.skips.map(toNamespace).map((namespace) => "-" + namespace)
        ].join(",");
        createDebug.enable("");
        return namespaces;
      }
      function enabled(name) {
        if (name[name.length - 1] === "*") {
          return true;
        }
        let i;
        let len;
        for (i = 0, len = createDebug.skips.length; i < len; i++) {
          if (createDebug.skips[i].test(name)) {
            return false;
          }
        }
        for (i = 0, len = createDebug.names.length; i < len; i++) {
          if (createDebug.names[i].test(name)) {
            return true;
          }
        }
        return false;
      }
      function toNamespace(regexp) {
        return regexp.toString().substring(2, regexp.toString().length - 2).replace(/\.\*\?$/, "*");
      }
      function coerce(val) {
        if (val instanceof Error) {
          return val.stack || val.message;
        }
        return val;
      }
      function destroy() {
        console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
      }
      createDebug.enable(createDebug.load());
      return createDebug;
    }
    module2.exports = setup;
  }
});

// ../../node_modules/debug/src/browser.js
var require_browser = __commonJS({
  "../../node_modules/debug/src/browser.js"(exports2, module2) {
    "use strict";
    exports2.formatArgs = formatArgs;
    exports2.save = save;
    exports2.load = load;
    exports2.useColors = useColors;
    exports2.storage = localstorage();
    exports2.destroy = /* @__PURE__ */ (() => {
      let warned = false;
      return () => {
        if (!warned) {
          warned = true;
          console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
        }
      };
    })();
    exports2.colors = [
      "#0000CC",
      "#0000FF",
      "#0033CC",
      "#0033FF",
      "#0066CC",
      "#0066FF",
      "#0099CC",
      "#0099FF",
      "#00CC00",
      "#00CC33",
      "#00CC66",
      "#00CC99",
      "#00CCCC",
      "#00CCFF",
      "#3300CC",
      "#3300FF",
      "#3333CC",
      "#3333FF",
      "#3366CC",
      "#3366FF",
      "#3399CC",
      "#3399FF",
      "#33CC00",
      "#33CC33",
      "#33CC66",
      "#33CC99",
      "#33CCCC",
      "#33CCFF",
      "#6600CC",
      "#6600FF",
      "#6633CC",
      "#6633FF",
      "#66CC00",
      "#66CC33",
      "#9900CC",
      "#9900FF",
      "#9933CC",
      "#9933FF",
      "#99CC00",
      "#99CC33",
      "#CC0000",
      "#CC0033",
      "#CC0066",
      "#CC0099",
      "#CC00CC",
      "#CC00FF",
      "#CC3300",
      "#CC3333",
      "#CC3366",
      "#CC3399",
      "#CC33CC",
      "#CC33FF",
      "#CC6600",
      "#CC6633",
      "#CC9900",
      "#CC9933",
      "#CCCC00",
      "#CCCC33",
      "#FF0000",
      "#FF0033",
      "#FF0066",
      "#FF0099",
      "#FF00CC",
      "#FF00FF",
      "#FF3300",
      "#FF3333",
      "#FF3366",
      "#FF3399",
      "#FF33CC",
      "#FF33FF",
      "#FF6600",
      "#FF6633",
      "#FF9900",
      "#FF9933",
      "#FFCC00",
      "#FFCC33"
    ];
    function useColors() {
      if (typeof window !== "undefined" && window.process && (window.process.type === "renderer" || window.process.__nwjs)) {
        return true;
      }
      if (typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/(edge|trident)\/(\d+)/)) {
        return false;
      }
      let m;
      return typeof document !== "undefined" && document.documentElement && document.documentElement.style && document.documentElement.style.WebkitAppearance || // Is firebug? http://stackoverflow.com/a/398120/376773
      typeof window !== "undefined" && window.console && (window.console.firebug || window.console.exception && window.console.table) || // Is firefox >= v31?
      // https://developer.mozilla.org/en-US/docs/Tools/Web_Console#Styling_messages
      typeof navigator !== "undefined" && navigator.userAgent && (m = navigator.userAgent.toLowerCase().match(/firefox\/(\d+)/)) && parseInt(m[1], 10) >= 31 || // Double check webkit in userAgent just in case we are in a worker
      typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/applewebkit\/(\d+)/);
    }
    function formatArgs(args) {
      args[0] = (this.useColors ? "%c" : "") + this.namespace + (this.useColors ? " %c" : " ") + args[0] + (this.useColors ? "%c " : " ") + "+" + module2.exports.humanize(this.diff);
      if (!this.useColors) {
        return;
      }
      const c = "color: " + this.color;
      args.splice(1, 0, c, "color: inherit");
      let index = 0;
      let lastC = 0;
      args[0].replace(/%[a-zA-Z%]/g, (match) => {
        if (match === "%%") {
          return;
        }
        index++;
        if (match === "%c") {
          lastC = index;
        }
      });
      args.splice(lastC, 0, c);
    }
    exports2.log = console.debug || console.log || (() => {
    });
    function save(namespaces) {
      try {
        if (namespaces) {
          exports2.storage.setItem("debug", namespaces);
        } else {
          exports2.storage.removeItem("debug");
        }
      } catch (error) {
      }
    }
    function load() {
      let r;
      try {
        r = exports2.storage.getItem("debug");
      } catch (error) {
      }
      if (!r && typeof process !== "undefined" && "env" in process) {
        r = process.env.DEBUG;
      }
      return r;
    }
    function localstorage() {
      try {
        return localStorage;
      } catch (error) {
      }
    }
    module2.exports = require_common()(exports2);
    var { formatters } = module2.exports;
    formatters.j = function(v) {
      try {
        return JSON.stringify(v);
      } catch (error) {
        return "[UnexpectedJSONParseError]: " + error.message;
      }
    };
  }
});

// ../../node_modules/has-flag/index.js
var require_has_flag = __commonJS({
  "../../node_modules/has-flag/index.js"(exports2, module2) {
    "use strict";
    module2.exports = (flag, argv = process.argv) => {
      const prefix = flag.startsWith("-") ? "" : flag.length === 1 ? "-" : "--";
      const position = argv.indexOf(prefix + flag);
      const terminatorPosition = argv.indexOf("--");
      return position !== -1 && (terminatorPosition === -1 || position < terminatorPosition);
    };
  }
});

// ../../node_modules/supports-color/index.js
var require_supports_color = __commonJS({
  "../../node_modules/supports-color/index.js"(exports2, module2) {
    "use strict";
    var os = require("os");
    var tty = require("tty");
    var hasFlag = require_has_flag();
    var { env: env3 } = process;
    var flagForceColor;
    if (hasFlag("no-color") || hasFlag("no-colors") || hasFlag("color=false") || hasFlag("color=never")) {
      flagForceColor = 0;
    } else if (hasFlag("color") || hasFlag("colors") || hasFlag("color=true") || hasFlag("color=always")) {
      flagForceColor = 1;
    }
    function envForceColor() {
      if ("FORCE_COLOR" in env3) {
        if (env3.FORCE_COLOR === "true") {
          return 1;
        }
        if (env3.FORCE_COLOR === "false") {
          return 0;
        }
        return env3.FORCE_COLOR.length === 0 ? 1 : Math.min(Number.parseInt(env3.FORCE_COLOR, 10), 3);
      }
    }
    function translateLevel(level) {
      if (level === 0) {
        return false;
      }
      return {
        level,
        hasBasic: true,
        has256: level >= 2,
        has16m: level >= 3
      };
    }
    function supportsColor(haveStream, { streamIsTTY, sniffFlags = true } = {}) {
      const noFlagForceColor = envForceColor();
      if (noFlagForceColor !== void 0) {
        flagForceColor = noFlagForceColor;
      }
      const forceColor = sniffFlags ? flagForceColor : noFlagForceColor;
      if (forceColor === 0) {
        return 0;
      }
      if (sniffFlags) {
        if (hasFlag("color=16m") || hasFlag("color=full") || hasFlag("color=truecolor")) {
          return 3;
        }
        if (hasFlag("color=256")) {
          return 2;
        }
      }
      if (haveStream && !streamIsTTY && forceColor === void 0) {
        return 0;
      }
      const min = forceColor || 0;
      if (env3.TERM === "dumb") {
        return min;
      }
      if (process.platform === "win32") {
        const osRelease = os.release().split(".");
        if (Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) {
          return Number(osRelease[2]) >= 14931 ? 3 : 2;
        }
        return 1;
      }
      if ("CI" in env3) {
        if (["TRAVIS", "CIRCLECI", "APPVEYOR", "GITLAB_CI", "GITHUB_ACTIONS", "BUILDKITE", "DRONE"].some((sign) => sign in env3) || env3.CI_NAME === "codeship") {
          return 1;
        }
        return min;
      }
      if ("TEAMCITY_VERSION" in env3) {
        return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(env3.TEAMCITY_VERSION) ? 1 : 0;
      }
      if (env3.COLORTERM === "truecolor") {
        return 3;
      }
      if ("TERM_PROGRAM" in env3) {
        const version = Number.parseInt((env3.TERM_PROGRAM_VERSION || "").split(".")[0], 10);
        switch (env3.TERM_PROGRAM) {
          case "iTerm.app":
            return version >= 3 ? 3 : 2;
          case "Apple_Terminal":
            return 2;
        }
      }
      if (/-256(color)?$/i.test(env3.TERM)) {
        return 2;
      }
      if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(env3.TERM)) {
        return 1;
      }
      if ("COLORTERM" in env3) {
        return 1;
      }
      return min;
    }
    function getSupportLevel(stream, options = {}) {
      const level = supportsColor(stream, {
        streamIsTTY: stream && stream.isTTY,
        ...options
      });
      return translateLevel(level);
    }
    module2.exports = {
      supportsColor: getSupportLevel,
      stdout: getSupportLevel({ isTTY: tty.isatty(1) }),
      stderr: getSupportLevel({ isTTY: tty.isatty(2) })
    };
  }
});

// ../../node_modules/debug/src/node.js
var require_node = __commonJS({
  "../../node_modules/debug/src/node.js"(exports2, module2) {
    "use strict";
    var tty = require("tty");
    var util = require("util");
    exports2.init = init;
    exports2.log = log;
    exports2.formatArgs = formatArgs;
    exports2.save = save;
    exports2.load = load;
    exports2.useColors = useColors;
    exports2.destroy = util.deprecate(
      () => {
      },
      "Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`."
    );
    exports2.colors = [6, 2, 3, 4, 5, 1];
    try {
      const supportsColor = require_supports_color();
      if (supportsColor && (supportsColor.stderr || supportsColor).level >= 2) {
        exports2.colors = [
          20,
          21,
          26,
          27,
          32,
          33,
          38,
          39,
          40,
          41,
          42,
          43,
          44,
          45,
          56,
          57,
          62,
          63,
          68,
          69,
          74,
          75,
          76,
          77,
          78,
          79,
          80,
          81,
          92,
          93,
          98,
          99,
          112,
          113,
          128,
          129,
          134,
          135,
          148,
          149,
          160,
          161,
          162,
          163,
          164,
          165,
          166,
          167,
          168,
          169,
          170,
          171,
          172,
          173,
          178,
          179,
          184,
          185,
          196,
          197,
          198,
          199,
          200,
          201,
          202,
          203,
          204,
          205,
          206,
          207,
          208,
          209,
          214,
          215,
          220,
          221
        ];
      }
    } catch (error) {
    }
    exports2.inspectOpts = Object.keys(process.env).filter((key) => {
      return /^debug_/i.test(key);
    }).reduce((obj, key) => {
      const prop = key.substring(6).toLowerCase().replace(/_([a-z])/g, (_, k) => {
        return k.toUpperCase();
      });
      let val = process.env[key];
      if (/^(yes|on|true|enabled)$/i.test(val)) {
        val = true;
      } else if (/^(no|off|false|disabled)$/i.test(val)) {
        val = false;
      } else if (val === "null") {
        val = null;
      } else {
        val = Number(val);
      }
      obj[prop] = val;
      return obj;
    }, {});
    function useColors() {
      return "colors" in exports2.inspectOpts ? Boolean(exports2.inspectOpts.colors) : tty.isatty(process.stderr.fd);
    }
    function formatArgs(args) {
      const { namespace: name, useColors: useColors2 } = this;
      if (useColors2) {
        const c = this.color;
        const colorCode = "\x1B[3" + (c < 8 ? c : "8;5;" + c);
        const prefix = `  ${colorCode};1m${name} \x1B[0m`;
        args[0] = prefix + args[0].split("\n").join("\n" + prefix);
        args.push(colorCode + "m+" + module2.exports.humanize(this.diff) + "\x1B[0m");
      } else {
        args[0] = getDate() + name + " " + args[0];
      }
    }
    function getDate() {
      if (exports2.inspectOpts.hideDate) {
        return "";
      }
      return (/* @__PURE__ */ new Date()).toISOString() + " ";
    }
    function log(...args) {
      return process.stderr.write(util.formatWithOptions(exports2.inspectOpts, ...args) + "\n");
    }
    function save(namespaces) {
      if (namespaces) {
        process.env.DEBUG = namespaces;
      } else {
        delete process.env.DEBUG;
      }
    }
    function load() {
      return process.env.DEBUG;
    }
    function init(debug) {
      debug.inspectOpts = {};
      const keys = Object.keys(exports2.inspectOpts);
      for (let i = 0; i < keys.length; i++) {
        debug.inspectOpts[keys[i]] = exports2.inspectOpts[keys[i]];
      }
    }
    module2.exports = require_common()(exports2);
    var { formatters } = module2.exports;
    formatters.o = function(v) {
      this.inspectOpts.colors = this.useColors;
      return util.inspect(v, this.inspectOpts).split("\n").map((str) => str.trim()).join(" ");
    };
    formatters.O = function(v) {
      this.inspectOpts.colors = this.useColors;
      return util.inspect(v, this.inspectOpts);
    };
  }
});

// ../../node_modules/debug/src/index.js
var require_src = __commonJS({
  "../../node_modules/debug/src/index.js"(exports2, module2) {
    "use strict";
    if (typeof process === "undefined" || process.type === "renderer" || process.browser === true || process.__nwjs) {
      module2.exports = require_browser();
    } else {
      module2.exports = require_node();
    }
  }
});

// ../../node_modules/follow-redirects/debug.js
var require_debug = __commonJS({
  "../../node_modules/follow-redirects/debug.js"(exports2, module2) {
    "use strict";
    var debug;
    module2.exports = function() {
      if (!debug) {
        try {
          debug = require_src()("follow-redirects");
        } catch (error) {
        }
        if (typeof debug !== "function") {
          debug = function() {
          };
        }
      }
      debug.apply(null, arguments);
    };
  }
});

// ../../node_modules/follow-redirects/index.js
var require_follow_redirects = __commonJS({
  "../../node_modules/follow-redirects/index.js"(exports2, module2) {
    "use strict";
    var url = require("url");
    var URL2 = url.URL;
    var http = require("http");
    var https = require("https");
    var Writable = require("stream").Writable;
    var assert2 = require("assert");
    var debug = require_debug();
    var useNativeURL = false;
    try {
      assert2(new URL2());
    } catch (error) {
      useNativeURL = error.code === "ERR_INVALID_URL";
    }
    var preservedUrlFields = [
      "auth",
      "host",
      "hostname",
      "href",
      "path",
      "pathname",
      "port",
      "protocol",
      "query",
      "search",
      "hash"
    ];
    var events = ["abort", "aborted", "connect", "error", "socket", "timeout"];
    var eventHandlers = /* @__PURE__ */ Object.create(null);
    events.forEach(function(event) {
      eventHandlers[event] = function(arg1, arg2, arg3) {
        this._redirectable.emit(event, arg1, arg2, arg3);
      };
    });
    var InvalidUrlError = createErrorType(
      "ERR_INVALID_URL",
      "Invalid URL",
      TypeError
    );
    var RedirectionError = createErrorType(
      "ERR_FR_REDIRECTION_FAILURE",
      "Redirected request failed"
    );
    var TooManyRedirectsError = createErrorType(
      "ERR_FR_TOO_MANY_REDIRECTS",
      "Maximum number of redirects exceeded",
      RedirectionError
    );
    var MaxBodyLengthExceededError = createErrorType(
      "ERR_FR_MAX_BODY_LENGTH_EXCEEDED",
      "Request body larger than maxBodyLength limit"
    );
    var WriteAfterEndError = createErrorType(
      "ERR_STREAM_WRITE_AFTER_END",
      "write after end"
    );
    var destroy = Writable.prototype.destroy || noop;
    function RedirectableRequest(options, responseCallback) {
      Writable.call(this);
      this._sanitizeOptions(options);
      this._options = options;
      this._ended = false;
      this._ending = false;
      this._redirectCount = 0;
      this._redirects = [];
      this._requestBodyLength = 0;
      this._requestBodyBuffers = [];
      if (responseCallback) {
        this.on("response", responseCallback);
      }
      var self2 = this;
      this._onNativeResponse = function(response) {
        try {
          self2._processResponse(response);
        } catch (cause) {
          self2.emit("error", cause instanceof RedirectionError ? cause : new RedirectionError({ cause }));
        }
      };
      this._performRequest();
    }
    RedirectableRequest.prototype = Object.create(Writable.prototype);
    RedirectableRequest.prototype.abort = function() {
      destroyRequest(this._currentRequest);
      this._currentRequest.abort();
      this.emit("abort");
    };
    RedirectableRequest.prototype.destroy = function(error) {
      destroyRequest(this._currentRequest, error);
      destroy.call(this, error);
      return this;
    };
    RedirectableRequest.prototype.write = function(data, encoding, callback) {
      if (this._ending) {
        throw new WriteAfterEndError();
      }
      if (!isString(data) && !isBuffer(data)) {
        throw new TypeError("data should be a string, Buffer or Uint8Array");
      }
      if (isFunction2(encoding)) {
        callback = encoding;
        encoding = null;
      }
      if (data.length === 0) {
        if (callback) {
          callback();
        }
        return;
      }
      if (this._requestBodyLength + data.length <= this._options.maxBodyLength) {
        this._requestBodyLength += data.length;
        this._requestBodyBuffers.push({ data, encoding });
        this._currentRequest.write(data, encoding, callback);
      } else {
        this.emit("error", new MaxBodyLengthExceededError());
        this.abort();
      }
    };
    RedirectableRequest.prototype.end = function(data, encoding, callback) {
      if (isFunction2(data)) {
        callback = data;
        data = encoding = null;
      } else if (isFunction2(encoding)) {
        callback = encoding;
        encoding = null;
      }
      if (!data) {
        this._ended = this._ending = true;
        this._currentRequest.end(null, null, callback);
      } else {
        var self2 = this;
        var currentRequest = this._currentRequest;
        this.write(data, encoding, function() {
          self2._ended = true;
          currentRequest.end(null, null, callback);
        });
        this._ending = true;
      }
    };
    RedirectableRequest.prototype.setHeader = function(name, value) {
      this._options.headers[name] = value;
      this._currentRequest.setHeader(name, value);
    };
    RedirectableRequest.prototype.removeHeader = function(name) {
      delete this._options.headers[name];
      this._currentRequest.removeHeader(name);
    };
    RedirectableRequest.prototype.setTimeout = function(msecs, callback) {
      var self2 = this;
      function destroyOnTimeout(socket) {
        socket.setTimeout(msecs);
        socket.removeListener("timeout", socket.destroy);
        socket.addListener("timeout", socket.destroy);
      }
      function startTimer(socket) {
        if (self2._timeout) {
          clearTimeout(self2._timeout);
        }
        self2._timeout = setTimeout(function() {
          self2.emit("timeout");
          clearTimer();
        }, msecs);
        destroyOnTimeout(socket);
      }
      function clearTimer() {
        if (self2._timeout) {
          clearTimeout(self2._timeout);
          self2._timeout = null;
        }
        self2.removeListener("abort", clearTimer);
        self2.removeListener("error", clearTimer);
        self2.removeListener("response", clearTimer);
        self2.removeListener("close", clearTimer);
        if (callback) {
          self2.removeListener("timeout", callback);
        }
        if (!self2.socket) {
          self2._currentRequest.removeListener("socket", startTimer);
        }
      }
      if (callback) {
        this.on("timeout", callback);
      }
      if (this.socket) {
        startTimer(this.socket);
      } else {
        this._currentRequest.once("socket", startTimer);
      }
      this.on("socket", destroyOnTimeout);
      this.on("abort", clearTimer);
      this.on("error", clearTimer);
      this.on("response", clearTimer);
      this.on("close", clearTimer);
      return this;
    };
    [
      "flushHeaders",
      "getHeader",
      "setNoDelay",
      "setSocketKeepAlive"
    ].forEach(function(method) {
      RedirectableRequest.prototype[method] = function(a, b) {
        return this._currentRequest[method](a, b);
      };
    });
    ["aborted", "connection", "socket"].forEach(function(property) {
      Object.defineProperty(RedirectableRequest.prototype, property, {
        get: function() {
          return this._currentRequest[property];
        }
      });
    });
    RedirectableRequest.prototype._sanitizeOptions = function(options) {
      if (!options.headers) {
        options.headers = {};
      }
      if (options.host) {
        if (!options.hostname) {
          options.hostname = options.host;
        }
        delete options.host;
      }
      if (!options.pathname && options.path) {
        var searchPos = options.path.indexOf("?");
        if (searchPos < 0) {
          options.pathname = options.path;
        } else {
          options.pathname = options.path.substring(0, searchPos);
          options.search = options.path.substring(searchPos);
        }
      }
    };
    RedirectableRequest.prototype._performRequest = function() {
      var protocol = this._options.protocol;
      var nativeProtocol = this._options.nativeProtocols[protocol];
      if (!nativeProtocol) {
        throw new TypeError("Unsupported protocol " + protocol);
      }
      if (this._options.agents) {
        var scheme = protocol.slice(0, -1);
        this._options.agent = this._options.agents[scheme];
      }
      var request = this._currentRequest = nativeProtocol.request(this._options, this._onNativeResponse);
      request._redirectable = this;
      for (var event of events) {
        request.on(event, eventHandlers[event]);
      }
      this._currentUrl = /^\//.test(this._options.path) ? url.format(this._options) : (
        // When making a request to a proxy, […]
        // a client MUST send the target URI in absolute-form […].
        this._options.path
      );
      if (this._isRedirect) {
        var i = 0;
        var self2 = this;
        var buffers = this._requestBodyBuffers;
        (function writeNext(error) {
          if (request === self2._currentRequest) {
            if (error) {
              self2.emit("error", error);
            } else if (i < buffers.length) {
              var buffer = buffers[i++];
              if (!request.finished) {
                request.write(buffer.data, buffer.encoding, writeNext);
              }
            } else if (self2._ended) {
              request.end();
            }
          }
        })();
      }
    };
    RedirectableRequest.prototype._processResponse = function(response) {
      var statusCode = response.statusCode;
      if (this._options.trackRedirects) {
        this._redirects.push({
          url: this._currentUrl,
          headers: response.headers,
          statusCode
        });
      }
      var location2 = response.headers.location;
      if (!location2 || this._options.followRedirects === false || statusCode < 300 || statusCode >= 400) {
        response.responseUrl = this._currentUrl;
        response.redirects = this._redirects;
        this.emit("response", response);
        this._requestBodyBuffers = [];
        return;
      }
      destroyRequest(this._currentRequest);
      response.destroy();
      if (++this._redirectCount > this._options.maxRedirects) {
        throw new TooManyRedirectsError();
      }
      var requestHeaders;
      var beforeRedirect = this._options.beforeRedirect;
      if (beforeRedirect) {
        requestHeaders = Object.assign({
          // The Host header was set by nativeProtocol.request
          Host: response.req.getHeader("host")
        }, this._options.headers);
      }
      var method = this._options.method;
      if ((statusCode === 301 || statusCode === 302) && this._options.method === "POST" || // RFC7231§6.4.4: The 303 (See Other) status code indicates that
      // the server is redirecting the user agent to a different resource […]
      // A user agent can perform a retrieval request targeting that URI
      // (a GET or HEAD request if using HTTP) […]
      statusCode === 303 && !/^(?:GET|HEAD)$/.test(this._options.method)) {
        this._options.method = "GET";
        this._requestBodyBuffers = [];
        removeMatchingHeaders(/^content-/i, this._options.headers);
      }
      var currentHostHeader = removeMatchingHeaders(/^host$/i, this._options.headers);
      var currentUrlParts = parseUrl(this._currentUrl);
      var currentHost = currentHostHeader || currentUrlParts.host;
      var currentUrl = /^\w+:/.test(location2) ? this._currentUrl : url.format(Object.assign(currentUrlParts, { host: currentHost }));
      var redirectUrl = resolveUrl(location2, currentUrl);
      debug("redirecting to", redirectUrl.href);
      this._isRedirect = true;
      spreadUrlObject(redirectUrl, this._options);
      if (redirectUrl.protocol !== currentUrlParts.protocol && redirectUrl.protocol !== "https:" || redirectUrl.host !== currentHost && !isSubdomain(redirectUrl.host, currentHost)) {
        removeMatchingHeaders(/^(?:(?:proxy-)?authorization|cookie)$/i, this._options.headers);
      }
      if (isFunction2(beforeRedirect)) {
        var responseDetails = {
          headers: response.headers,
          statusCode
        };
        var requestDetails = {
          url: currentUrl,
          method,
          headers: requestHeaders
        };
        beforeRedirect(this._options, responseDetails, requestDetails);
        this._sanitizeOptions(this._options);
      }
      this._performRequest();
    };
    function wrap(protocols) {
      var exports3 = {
        maxRedirects: 21,
        maxBodyLength: 10 * 1024 * 1024
      };
      var nativeProtocols = {};
      Object.keys(protocols).forEach(function(scheme) {
        var protocol = scheme + ":";
        var nativeProtocol = nativeProtocols[protocol] = protocols[scheme];
        var wrappedProtocol = exports3[scheme] = Object.create(nativeProtocol);
        function request(input, options, callback) {
          if (isURL(input)) {
            input = spreadUrlObject(input);
          } else if (isString(input)) {
            input = spreadUrlObject(parseUrl(input));
          } else {
            callback = options;
            options = validateUrl(input);
            input = { protocol };
          }
          if (isFunction2(options)) {
            callback = options;
            options = null;
          }
          options = Object.assign({
            maxRedirects: exports3.maxRedirects,
            maxBodyLength: exports3.maxBodyLength
          }, input, options);
          options.nativeProtocols = nativeProtocols;
          if (!isString(options.host) && !isString(options.hostname)) {
            options.hostname = "::1";
          }
          assert2.equal(options.protocol, protocol, "protocol mismatch");
          debug("options", options);
          return new RedirectableRequest(options, callback);
        }
        function get3(input, options, callback) {
          var wrappedRequest = wrappedProtocol.request(input, options, callback);
          wrappedRequest.end();
          return wrappedRequest;
        }
        Object.defineProperties(wrappedProtocol, {
          request: { value: request, configurable: true, enumerable: true, writable: true },
          get: { value: get3, configurable: true, enumerable: true, writable: true }
        });
      });
      return exports3;
    }
    function noop() {
    }
    function parseUrl(input) {
      var parsed;
      if (useNativeURL) {
        parsed = new URL2(input);
      } else {
        parsed = validateUrl(url.parse(input));
        if (!isString(parsed.protocol)) {
          throw new InvalidUrlError({ input });
        }
      }
      return parsed;
    }
    function resolveUrl(relative, base) {
      return useNativeURL ? new URL2(relative, base) : parseUrl(url.resolve(base, relative));
    }
    function validateUrl(input) {
      if (/^\[/.test(input.hostname) && !/^\[[:0-9a-f]+\]$/i.test(input.hostname)) {
        throw new InvalidUrlError({ input: input.href || input });
      }
      if (/^\[/.test(input.host) && !/^\[[:0-9a-f]+\](:\d+)?$/i.test(input.host)) {
        throw new InvalidUrlError({ input: input.href || input });
      }
      return input;
    }
    function spreadUrlObject(urlObject, target) {
      var spread = target || {};
      for (var key of preservedUrlFields) {
        spread[key] = urlObject[key];
      }
      if (spread.hostname.startsWith("[")) {
        spread.hostname = spread.hostname.slice(1, -1);
      }
      if (spread.port !== "") {
        spread.port = Number(spread.port);
      }
      spread.path = spread.search ? spread.pathname + spread.search : spread.pathname;
      return spread;
    }
    function removeMatchingHeaders(regex, headers) {
      var lastValue;
      for (var header in headers) {
        if (regex.test(header)) {
          lastValue = headers[header];
          delete headers[header];
        }
      }
      return lastValue === null || typeof lastValue === "undefined" ? void 0 : String(lastValue).trim();
    }
    function createErrorType(code, message, baseClass) {
      function CustomError(properties) {
        Error.captureStackTrace(this, this.constructor);
        Object.assign(this, properties || {});
        this.code = code;
        this.message = this.cause ? message + ": " + this.cause.message : message;
      }
      CustomError.prototype = new (baseClass || Error)();
      Object.defineProperties(CustomError.prototype, {
        constructor: {
          value: CustomError,
          enumerable: false
        },
        name: {
          value: "Error [" + code + "]",
          enumerable: false
        }
      });
      return CustomError;
    }
    function destroyRequest(request, error) {
      for (var event of events) {
        request.removeListener(event, eventHandlers[event]);
      }
      request.on("error", noop);
      request.destroy(error);
    }
    function isSubdomain(subdomain, domain) {
      assert2(isString(subdomain) && isString(domain));
      var dot = subdomain.length - domain.length - 1;
      return dot > 0 && subdomain[dot] === "." && subdomain.endsWith(domain);
    }
    function isString(value) {
      return typeof value === "string" || value instanceof String;
    }
    function isFunction2(value) {
      return typeof value === "function";
    }
    function isBuffer(value) {
      return typeof value === "object" && "length" in value;
    }
    function isURL(value) {
      return URL2 && value instanceof URL2;
    }
    module2.exports = wrap({ http, https });
    module2.exports.wrap = wrap;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/package.json
var require_package = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/package.json"(exports2, module2) {
    module2.exports = {
      name: "axios",
      version: "0.21.4",
      description: "Promise based HTTP client for the browser and node.js",
      main: "index.js",
      scripts: {
        test: "grunt test",
        start: "node ./sandbox/server.js",
        build: "NODE_ENV=production grunt build",
        preversion: "npm test",
        version: "npm run build && grunt version && git add -A dist && git add CHANGELOG.md bower.json package.json",
        postversion: "git push && git push --tags",
        examples: "node ./examples/server.js",
        coveralls: "cat coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js",
        fix: "eslint --fix lib/**/*.js"
      },
      repository: {
        type: "git",
        url: "https://github.com/axios/axios.git"
      },
      keywords: [
        "xhr",
        "http",
        "ajax",
        "promise",
        "node"
      ],
      author: "Matt Zabriskie",
      license: "MIT",
      bugs: {
        url: "https://github.com/axios/axios/issues"
      },
      homepage: "https://axios-http.com",
      devDependencies: {
        coveralls: "^3.0.0",
        "es6-promise": "^4.2.4",
        grunt: "^1.3.0",
        "grunt-banner": "^0.6.0",
        "grunt-cli": "^1.2.0",
        "grunt-contrib-clean": "^1.1.0",
        "grunt-contrib-watch": "^1.0.0",
        "grunt-eslint": "^23.0.0",
        "grunt-karma": "^4.0.0",
        "grunt-mocha-test": "^0.13.3",
        "grunt-ts": "^6.0.0-beta.19",
        "grunt-webpack": "^4.0.2",
        "istanbul-instrumenter-loader": "^1.0.0",
        "jasmine-core": "^2.4.1",
        karma: "^6.3.2",
        "karma-chrome-launcher": "^3.1.0",
        "karma-firefox-launcher": "^2.1.0",
        "karma-jasmine": "^1.1.1",
        "karma-jasmine-ajax": "^0.1.13",
        "karma-safari-launcher": "^1.0.0",
        "karma-sauce-launcher": "^4.3.6",
        "karma-sinon": "^1.0.5",
        "karma-sourcemap-loader": "^0.3.8",
        "karma-webpack": "^4.0.2",
        "load-grunt-tasks": "^3.5.2",
        minimist: "^1.2.0",
        mocha: "^8.2.1",
        sinon: "^4.5.0",
        "terser-webpack-plugin": "^4.2.3",
        typescript: "^4.0.5",
        "url-search-params": "^0.10.0",
        webpack: "^4.44.2",
        "webpack-dev-server": "^3.11.0"
      },
      browser: {
        "./lib/adapters/http.js": "./lib/adapters/xhr.js"
      },
      jsdelivr: "dist/axios.min.js",
      unpkg: "dist/axios.min.js",
      typings: "./index.d.ts",
      dependencies: {
        "follow-redirects": "^1.14.0"
      },
      bundlesize: [
        {
          path: "./dist/axios.min.js",
          threshold: "5kB"
        }
      ]
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/adapters/http.js
var require_http = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/adapters/http.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    var settle = require_settle();
    var buildFullPath = require_buildFullPath();
    var buildURL = require_buildURL();
    var http = require("http");
    var https = require("https");
    var httpFollow = require_follow_redirects().http;
    var httpsFollow = require_follow_redirects().https;
    var url = require("url");
    var zlib = require("zlib");
    var pkg = require_package();
    var createError = require_createError();
    var enhanceError = require_enhanceError();
    var isHttps = /https:?/;
    function setProxy(options, proxy, location2) {
      options.hostname = proxy.host;
      options.host = proxy.host;
      options.port = proxy.port;
      options.path = location2;
      if (proxy.auth) {
        var base642 = Buffer.from(proxy.auth.username + ":" + proxy.auth.password, "utf8").toString("base64");
        options.headers["Proxy-Authorization"] = "Basic " + base642;
      }
      options.beforeRedirect = function beforeRedirect(redirection) {
        redirection.headers.host = redirection.host;
        setProxy(redirection, proxy, redirection.href);
      };
    }
    module2.exports = function httpAdapter(config) {
      return new Promise(function dispatchHttpRequest(resolvePromise, rejectPromise) {
        var resolve = function resolve2(value) {
          resolvePromise(value);
        };
        var reject = function reject2(value) {
          rejectPromise(value);
        };
        var data = config.data;
        var headers = config.headers;
        if ("User-Agent" in headers || "user-agent" in headers) {
          if (!headers["User-Agent"] && !headers["user-agent"]) {
            delete headers["User-Agent"];
            delete headers["user-agent"];
          }
        } else {
          headers["User-Agent"] = "axios/" + pkg.version;
        }
        if (data && !utils.isStream(data)) {
          if (Buffer.isBuffer(data)) {
          } else if (utils.isArrayBuffer(data)) {
            data = Buffer.from(new Uint8Array(data));
          } else if (utils.isString(data)) {
            data = Buffer.from(data, "utf-8");
          } else {
            return reject(createError(
              "Data after transformation must be a string, an ArrayBuffer, a Buffer, or a Stream",
              config
            ));
          }
          headers["Content-Length"] = data.length;
        }
        var auth = void 0;
        if (config.auth) {
          var username = config.auth.username || "";
          var password = config.auth.password || "";
          auth = username + ":" + password;
        }
        var fullPath = buildFullPath(config.baseURL, config.url);
        var parsed = url.parse(fullPath);
        var protocol = parsed.protocol || "http:";
        if (!auth && parsed.auth) {
          var urlAuth = parsed.auth.split(":");
          var urlUsername = urlAuth[0] || "";
          var urlPassword = urlAuth[1] || "";
          auth = urlUsername + ":" + urlPassword;
        }
        if (auth) {
          delete headers.Authorization;
        }
        var isHttpsRequest = isHttps.test(protocol);
        var agent = isHttpsRequest ? config.httpsAgent : config.httpAgent;
        var options = {
          path: buildURL(parsed.path, config.params, config.paramsSerializer).replace(/^\?/, ""),
          method: config.method.toUpperCase(),
          headers,
          agent,
          agents: { http: config.httpAgent, https: config.httpsAgent },
          auth
        };
        if (config.socketPath) {
          options.socketPath = config.socketPath;
        } else {
          options.hostname = parsed.hostname;
          options.port = parsed.port;
        }
        var proxy = config.proxy;
        if (!proxy && proxy !== false) {
          var proxyEnv = protocol.slice(0, -1) + "_proxy";
          var proxyUrl = process.env[proxyEnv] || process.env[proxyEnv.toUpperCase()];
          if (proxyUrl) {
            var parsedProxyUrl = url.parse(proxyUrl);
            var noProxyEnv = process.env.no_proxy || process.env.NO_PROXY;
            var shouldProxy = true;
            if (noProxyEnv) {
              var noProxy = noProxyEnv.split(",").map(function trim(s) {
                return s.trim();
              });
              shouldProxy = !noProxy.some(function proxyMatch(proxyElement) {
                if (!proxyElement) {
                  return false;
                }
                if (proxyElement === "*") {
                  return true;
                }
                if (proxyElement[0] === "." && parsed.hostname.substr(parsed.hostname.length - proxyElement.length) === proxyElement) {
                  return true;
                }
                return parsed.hostname === proxyElement;
              });
            }
            if (shouldProxy) {
              proxy = {
                host: parsedProxyUrl.hostname,
                port: parsedProxyUrl.port,
                protocol: parsedProxyUrl.protocol
              };
              if (parsedProxyUrl.auth) {
                var proxyUrlAuth = parsedProxyUrl.auth.split(":");
                proxy.auth = {
                  username: proxyUrlAuth[0],
                  password: proxyUrlAuth[1]
                };
              }
            }
          }
        }
        if (proxy) {
          options.headers.host = parsed.hostname + (parsed.port ? ":" + parsed.port : "");
          setProxy(options, proxy, protocol + "//" + parsed.hostname + (parsed.port ? ":" + parsed.port : "") + options.path);
        }
        var transport;
        var isHttpsProxy = isHttpsRequest && (proxy ? isHttps.test(proxy.protocol) : true);
        if (config.transport) {
          transport = config.transport;
        } else if (config.maxRedirects === 0) {
          transport = isHttpsProxy ? https : http;
        } else {
          if (config.maxRedirects) {
            options.maxRedirects = config.maxRedirects;
          }
          transport = isHttpsProxy ? httpsFollow : httpFollow;
        }
        if (config.maxBodyLength > -1) {
          options.maxBodyLength = config.maxBodyLength;
        }
        var req = transport.request(options, function handleResponse(res) {
          if (req.aborted) return;
          var stream = res;
          var lastRequest = res.req || req;
          if (res.statusCode !== 204 && lastRequest.method !== "HEAD" && config.decompress !== false) {
            switch (res.headers["content-encoding"]) {
              /*eslint default-case:0*/
              case "gzip":
              case "compress":
              case "deflate":
                stream = stream.pipe(zlib.createUnzip());
                delete res.headers["content-encoding"];
                break;
            }
          }
          var response = {
            status: res.statusCode,
            statusText: res.statusMessage,
            headers: res.headers,
            config,
            request: lastRequest
          };
          if (config.responseType === "stream") {
            response.data = stream;
            settle(resolve, reject, response);
          } else {
            var responseBuffer = [];
            var totalResponseBytes = 0;
            stream.on("data", function handleStreamData(chunk) {
              responseBuffer.push(chunk);
              totalResponseBytes += chunk.length;
              if (config.maxContentLength > -1 && totalResponseBytes > config.maxContentLength) {
                stream.destroy();
                reject(createError(
                  "maxContentLength size of " + config.maxContentLength + " exceeded",
                  config,
                  null,
                  lastRequest
                ));
              }
            });
            stream.on("error", function handleStreamError(err) {
              if (req.aborted) return;
              reject(enhanceError(err, config, null, lastRequest));
            });
            stream.on("end", function handleStreamEnd() {
              var responseData = Buffer.concat(responseBuffer);
              if (config.responseType !== "arraybuffer") {
                responseData = responseData.toString(config.responseEncoding);
                if (!config.responseEncoding || config.responseEncoding === "utf8") {
                  responseData = utils.stripBOM(responseData);
                }
              }
              response.data = responseData;
              settle(resolve, reject, response);
            });
          }
        });
        req.on("error", function handleRequestError(err) {
          if (req.aborted && err.code !== "ERR_FR_TOO_MANY_REDIRECTS") return;
          reject(enhanceError(err, config, null, req));
        });
        if (config.timeout) {
          var timeout = parseInt(config.timeout, 10);
          if (isNaN(timeout)) {
            reject(createError(
              "error trying to parse `config.timeout` to int",
              config,
              "ERR_PARSE_TIMEOUT",
              req
            ));
            return;
          }
          req.setTimeout(timeout, function handleRequestTimeout() {
            req.abort();
            reject(createError(
              "timeout of " + timeout + "ms exceeded",
              config,
              config.transitional && config.transitional.clarifyTimeoutError ? "ETIMEDOUT" : "ECONNABORTED",
              req
            ));
          });
        }
        if (config.cancelToken) {
          config.cancelToken.promise.then(function onCanceled(cancel) {
            if (req.aborted) return;
            req.abort();
            reject(cancel);
          });
        }
        if (utils.isStream(data)) {
          data.on("error", function handleStreamError(err) {
            reject(enhanceError(err, config, null, req));
          }).pipe(req);
        } else {
          req.end(data);
        }
      });
    };
  }
});

// ../../node_modules/agent-base/dist/helpers.js
var require_helpers = __commonJS({
  "../../node_modules/agent-base/dist/helpers.js"(exports2) {
    "use strict";
    var __createBinding = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __setModuleDefault = exports2 && exports2.__setModuleDefault || (Object.create ? function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    } : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports2 && exports2.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
      }
      __setModuleDefault(result, mod);
      return result;
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.req = exports2.json = exports2.toBuffer = void 0;
    var http = __importStar(require("http"));
    var https = __importStar(require("https"));
    async function toBuffer(stream) {
      let length = 0;
      const chunks = [];
      for await (const chunk of stream) {
        length += chunk.length;
        chunks.push(chunk);
      }
      return Buffer.concat(chunks, length);
    }
    exports2.toBuffer = toBuffer;
    async function json(stream) {
      const buf = await toBuffer(stream);
      const str = buf.toString("utf8");
      try {
        return JSON.parse(str);
      } catch (_err) {
        const err = _err;
        err.message += ` (input: ${str})`;
        throw err;
      }
    }
    exports2.json = json;
    function req(url, opts = {}) {
      const href = typeof url === "string" ? url : url.href;
      const req2 = (href.startsWith("https:") ? https : http).request(url, opts);
      const promise = new Promise((resolve, reject) => {
        req2.once("response", resolve).once("error", reject).end();
      });
      req2.then = promise.then.bind(promise);
      return req2;
    }
    exports2.req = req;
  }
});

// ../../node_modules/agent-base/dist/index.js
var require_dist = __commonJS({
  "../../node_modules/agent-base/dist/index.js"(exports2) {
    "use strict";
    var __createBinding = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __setModuleDefault = exports2 && exports2.__setModuleDefault || (Object.create ? function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    } : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports2 && exports2.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
      }
      __setModuleDefault(result, mod);
      return result;
    };
    var __exportStar = exports2 && exports2.__exportStar || function(m, exports3) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports3, p)) __createBinding(exports3, m, p);
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.Agent = void 0;
    var net = __importStar(require("net"));
    var http = __importStar(require("http"));
    var https_1 = require("https");
    __exportStar(require_helpers(), exports2);
    var INTERNAL = Symbol("AgentBaseInternalState");
    var Agent = class extends http.Agent {
      constructor(opts) {
        super(opts);
        this[INTERNAL] = {};
      }
      /**
       * Determine whether this is an `http` or `https` request.
       */
      isSecureEndpoint(options) {
        if (options) {
          if (typeof options.secureEndpoint === "boolean") {
            return options.secureEndpoint;
          }
          if (typeof options.protocol === "string") {
            return options.protocol === "https:";
          }
        }
        const { stack } = new Error();
        if (typeof stack !== "string")
          return false;
        return stack.split("\n").some((l) => l.indexOf("(https.js:") !== -1 || l.indexOf("node:https:") !== -1);
      }
      // In order to support async signatures in `connect()` and Node's native
      // connection pooling in `http.Agent`, the array of sockets for each origin
      // has to be updated synchronously. This is so the length of the array is
      // accurate when `addRequest()` is next called. We achieve this by creating a
      // fake socket and adding it to `sockets[origin]` and incrementing
      // `totalSocketCount`.
      incrementSockets(name) {
        if (this.maxSockets === Infinity && this.maxTotalSockets === Infinity) {
          return null;
        }
        if (!this.sockets[name]) {
          this.sockets[name] = [];
        }
        const fakeSocket = new net.Socket({ writable: false });
        this.sockets[name].push(fakeSocket);
        this.totalSocketCount++;
        return fakeSocket;
      }
      decrementSockets(name, socket) {
        if (!this.sockets[name] || socket === null) {
          return;
        }
        const sockets = this.sockets[name];
        const index = sockets.indexOf(socket);
        if (index !== -1) {
          sockets.splice(index, 1);
          this.totalSocketCount--;
          if (sockets.length === 0) {
            delete this.sockets[name];
          }
        }
      }
      // In order to properly update the socket pool, we need to call `getName()` on
      // the core `https.Agent` if it is a secureEndpoint.
      getName(options) {
        const secureEndpoint = typeof options.secureEndpoint === "boolean" ? options.secureEndpoint : this.isSecureEndpoint(options);
        if (secureEndpoint) {
          return https_1.Agent.prototype.getName.call(this, options);
        }
        return super.getName(options);
      }
      createSocket(req, options, cb) {
        const connectOpts = {
          ...options,
          secureEndpoint: this.isSecureEndpoint(options)
        };
        const name = this.getName(connectOpts);
        const fakeSocket = this.incrementSockets(name);
        Promise.resolve().then(() => this.connect(req, connectOpts)).then((socket) => {
          this.decrementSockets(name, fakeSocket);
          if (socket instanceof http.Agent) {
            return socket.addRequest(req, connectOpts);
          }
          this[INTERNAL].currentSocket = socket;
          super.createSocket(req, options, cb);
        }, (err) => {
          this.decrementSockets(name, fakeSocket);
          cb(err);
        });
      }
      createConnection() {
        const socket = this[INTERNAL].currentSocket;
        this[INTERNAL].currentSocket = void 0;
        if (!socket) {
          throw new Error("No socket was returned in the `connect()` function");
        }
        return socket;
      }
      get defaultPort() {
        return this[INTERNAL].defaultPort ?? (this.protocol === "https:" ? 443 : 80);
      }
      set defaultPort(v) {
        if (this[INTERNAL]) {
          this[INTERNAL].defaultPort = v;
        }
      }
      get protocol() {
        return this[INTERNAL].protocol ?? (this.isSecureEndpoint() ? "https:" : "http:");
      }
      set protocol(v) {
        if (this[INTERNAL]) {
          this[INTERNAL].protocol = v;
        }
      }
    };
    exports2.Agent = Agent;
  }
});

// ../../node_modules/http-proxy-agent/dist/index.js
var require_dist2 = __commonJS({
  "../../node_modules/http-proxy-agent/dist/index.js"(exports2) {
    "use strict";
    var __createBinding = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __setModuleDefault = exports2 && exports2.__setModuleDefault || (Object.create ? function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    } : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports2 && exports2.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
      }
      __setModuleDefault(result, mod);
      return result;
    };
    var __importDefault = exports2 && exports2.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.HttpProxyAgent = void 0;
    var net = __importStar(require("net"));
    var tls = __importStar(require("tls"));
    var debug_1 = __importDefault(require_src());
    var events_1 = require("events");
    var agent_base_1 = require_dist();
    var url_1 = require("url");
    var debug = (0, debug_1.default)("http-proxy-agent");
    var HttpProxyAgent = class extends agent_base_1.Agent {
      constructor(proxy, opts) {
        super(opts);
        this.proxy = typeof proxy === "string" ? new url_1.URL(proxy) : proxy;
        this.proxyHeaders = opts?.headers ?? {};
        debug("Creating new HttpProxyAgent instance: %o", this.proxy.href);
        const host = (this.proxy.hostname || this.proxy.host).replace(/^\[|\]$/g, "");
        const port = this.proxy.port ? parseInt(this.proxy.port, 10) : this.proxy.protocol === "https:" ? 443 : 80;
        this.connectOpts = {
          ...opts ? omit(opts, "headers") : null,
          host,
          port
        };
      }
      addRequest(req, opts) {
        req._header = null;
        this.setRequestProps(req, opts);
        super.addRequest(req, opts);
      }
      setRequestProps(req, opts) {
        const { proxy } = this;
        const protocol = opts.secureEndpoint ? "https:" : "http:";
        const hostname = req.getHeader("host") || "localhost";
        const base = `${protocol}//${hostname}`;
        const url = new url_1.URL(req.path, base);
        if (opts.port !== 80) {
          url.port = String(opts.port);
        }
        req.path = String(url);
        const headers = typeof this.proxyHeaders === "function" ? this.proxyHeaders() : { ...this.proxyHeaders };
        if (proxy.username || proxy.password) {
          const auth = `${decodeURIComponent(proxy.username)}:${decodeURIComponent(proxy.password)}`;
          headers["Proxy-Authorization"] = `Basic ${Buffer.from(auth).toString("base64")}`;
        }
        if (!headers["Proxy-Connection"]) {
          headers["Proxy-Connection"] = this.keepAlive ? "Keep-Alive" : "close";
        }
        for (const name of Object.keys(headers)) {
          const value = headers[name];
          if (value) {
            req.setHeader(name, value);
          }
        }
      }
      async connect(req, opts) {
        req._header = null;
        if (!req.path.includes("://")) {
          this.setRequestProps(req, opts);
        }
        let first;
        let endOfHeaders;
        debug("Regenerating stored HTTP header string for request");
        req._implicitHeader();
        if (req.outputData && req.outputData.length > 0) {
          debug("Patching connection write() output buffer with updated header");
          first = req.outputData[0].data;
          endOfHeaders = first.indexOf("\r\n\r\n") + 4;
          req.outputData[0].data = req._header + first.substring(endOfHeaders);
          debug("Output buffer: %o", req.outputData[0].data);
        }
        let socket;
        if (this.proxy.protocol === "https:") {
          debug("Creating `tls.Socket`: %o", this.connectOpts);
          socket = tls.connect(this.connectOpts);
        } else {
          debug("Creating `net.Socket`: %o", this.connectOpts);
          socket = net.connect(this.connectOpts);
        }
        await (0, events_1.once)(socket, "connect");
        return socket;
      }
    };
    HttpProxyAgent.protocols = ["http", "https"];
    exports2.HttpProxyAgent = HttpProxyAgent;
    function omit(obj, ...keys) {
      const ret = {};
      let key;
      for (key in obj) {
        if (!keys.includes(key)) {
          ret[key] = obj[key];
        }
      }
      return ret;
    }
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/https-proxy-agent/dist/parse-proxy-response.js
var require_parse_proxy_response = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/https-proxy-agent/dist/parse-proxy-response.js"(exports2) {
    "use strict";
    var __importDefault = exports2 && exports2.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.parseProxyResponse = void 0;
    var debug_1 = __importDefault(require_src());
    var debug = (0, debug_1.default)("https-proxy-agent:parse-proxy-response");
    function parseProxyResponse(socket) {
      return new Promise((resolve, reject) => {
        let buffersLength = 0;
        const buffers = [];
        function read() {
          const b = socket.read();
          if (b)
            ondata(b);
          else
            socket.once("readable", read);
        }
        function cleanup() {
          socket.removeListener("end", onend);
          socket.removeListener("error", onerror);
          socket.removeListener("readable", read);
        }
        function onend() {
          cleanup();
          debug("onend");
          reject(new Error("Proxy connection ended before receiving CONNECT response"));
        }
        function onerror(err) {
          cleanup();
          debug("onerror %o", err);
          reject(err);
        }
        function ondata(b) {
          buffers.push(b);
          buffersLength += b.length;
          const buffered = Buffer.concat(buffers, buffersLength);
          const endOfHeaders = buffered.indexOf("\r\n\r\n");
          if (endOfHeaders === -1) {
            debug("have not received end of HTTP headers yet...");
            read();
            return;
          }
          const headerParts = buffered.slice(0, endOfHeaders).toString("ascii").split("\r\n");
          const firstLine = headerParts.shift();
          if (!firstLine) {
            socket.destroy();
            return reject(new Error("No header received from proxy CONNECT response"));
          }
          const firstLineParts = firstLine.split(" ");
          const statusCode = +firstLineParts[1];
          const statusText = firstLineParts.slice(2).join(" ");
          const headers = {};
          for (const header of headerParts) {
            if (!header)
              continue;
            const firstColon = header.indexOf(":");
            if (firstColon === -1) {
              socket.destroy();
              return reject(new Error(`Invalid header from proxy CONNECT response: "${header}"`));
            }
            const key = header.slice(0, firstColon).toLowerCase();
            const value = header.slice(firstColon + 1).trimStart();
            const current = headers[key];
            if (typeof current === "string") {
              headers[key] = [current, value];
            } else if (Array.isArray(current)) {
              current.push(value);
            } else {
              headers[key] = value;
            }
          }
          debug("got proxy server response: %o %o", firstLine, headers);
          cleanup();
          resolve({
            connect: {
              statusCode,
              statusText,
              headers
            },
            buffered
          });
        }
        socket.on("error", onerror);
        socket.on("end", onend);
        read();
      });
    }
    exports2.parseProxyResponse = parseProxyResponse;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/https-proxy-agent/dist/index.js
var require_dist3 = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/https-proxy-agent/dist/index.js"(exports2) {
    "use strict";
    var __createBinding = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __setModuleDefault = exports2 && exports2.__setModuleDefault || (Object.create ? function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    } : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports2 && exports2.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
      }
      __setModuleDefault(result, mod);
      return result;
    };
    var __importDefault = exports2 && exports2.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.HttpsProxyAgent = void 0;
    var net = __importStar(require("net"));
    var tls = __importStar(require("tls"));
    var assert_1 = __importDefault(require("assert"));
    var debug_1 = __importDefault(require_src());
    var agent_base_1 = require_dist();
    var url_1 = require("url");
    var parse_proxy_response_1 = require_parse_proxy_response();
    var debug = (0, debug_1.default)("https-proxy-agent");
    var HttpsProxyAgent = class extends agent_base_1.Agent {
      constructor(proxy, opts) {
        super(opts);
        this.options = { path: void 0 };
        this.proxy = typeof proxy === "string" ? new url_1.URL(proxy) : proxy;
        this.proxyHeaders = opts?.headers ?? {};
        debug("Creating new HttpsProxyAgent instance: %o", this.proxy.href);
        const host = (this.proxy.hostname || this.proxy.host).replace(/^\[|\]$/g, "");
        const port = this.proxy.port ? parseInt(this.proxy.port, 10) : this.proxy.protocol === "https:" ? 443 : 80;
        this.connectOpts = {
          // Attempt to negotiate http/1.1 for proxy servers that support http/2
          ALPNProtocols: ["http/1.1"],
          ...opts ? omit(opts, "headers") : null,
          host,
          port
        };
      }
      /**
       * Called when the node-core HTTP client library is creating a
       * new HTTP request.
       */
      async connect(req, opts) {
        const { proxy } = this;
        if (!opts.host) {
          throw new TypeError('No "host" provided');
        }
        let socket;
        if (proxy.protocol === "https:") {
          debug("Creating `tls.Socket`: %o", this.connectOpts);
          const servername = this.connectOpts.servername || this.connectOpts.host;
          socket = tls.connect({
            ...this.connectOpts,
            servername
          });
        } else {
          debug("Creating `net.Socket`: %o", this.connectOpts);
          socket = net.connect(this.connectOpts);
        }
        const headers = typeof this.proxyHeaders === "function" ? this.proxyHeaders() : { ...this.proxyHeaders };
        const host = net.isIPv6(opts.host) ? `[${opts.host}]` : opts.host;
        let payload = `CONNECT ${host}:${opts.port} HTTP/1.1\r
`;
        if (proxy.username || proxy.password) {
          const auth = `${decodeURIComponent(proxy.username)}:${decodeURIComponent(proxy.password)}`;
          headers["Proxy-Authorization"] = `Basic ${Buffer.from(auth).toString("base64")}`;
        }
        headers.Host = `${host}:${opts.port}`;
        if (!headers["Proxy-Connection"]) {
          headers["Proxy-Connection"] = this.keepAlive ? "Keep-Alive" : "close";
        }
        for (const name of Object.keys(headers)) {
          payload += `${name}: ${headers[name]}\r
`;
        }
        const proxyResponsePromise = (0, parse_proxy_response_1.parseProxyResponse)(socket);
        socket.write(`${payload}\r
`);
        const { connect, buffered } = await proxyResponsePromise;
        req.emit("proxyConnect", connect);
        this.emit("proxyConnect", connect, req);
        if (connect.statusCode === 200) {
          req.once("socket", resume);
          if (opts.secureEndpoint) {
            debug("Upgrading socket connection to TLS");
            const servername = opts.servername || opts.host;
            return tls.connect({
              ...omit(opts, "host", "path", "port"),
              socket,
              servername
            });
          }
          return socket;
        }
        socket.destroy();
        const fakeSocket = new net.Socket({ writable: false });
        fakeSocket.readable = true;
        req.once("socket", (s) => {
          debug("Replaying proxy buffer for failed request");
          (0, assert_1.default)(s.listenerCount("data") > 0);
          s.push(buffered);
          s.push(null);
        });
        return fakeSocket;
      }
    };
    HttpsProxyAgent.protocols = ["http", "https"];
    exports2.HttpsProxyAgent = HttpsProxyAgent;
    function resume(socket) {
      socket.resume();
    }
    function omit(obj, ...keys) {
      const ret = {};
      let key;
      for (key in obj) {
        if (!keys.includes(key)) {
          ret[key] = obj[key];
        }
      }
      return ret;
    }
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/InterceptorManager.js
var require_InterceptorManager = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/InterceptorManager.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    function InterceptorManager() {
      this.handlers = [];
    }
    InterceptorManager.prototype.use = function use(fulfilled, rejected, options) {
      this.handlers.push({
        fulfilled,
        rejected,
        synchronous: options ? options.synchronous : false,
        runWhen: options ? options.runWhen : null
      });
      return this.handlers.length - 1;
    };
    InterceptorManager.prototype.eject = function eject(id) {
      if (this.handlers[id]) {
        this.handlers[id] = null;
      }
    };
    InterceptorManager.prototype.forEach = function forEach(fn) {
      utils.forEach(this.handlers, function forEachHandler(h) {
        if (h !== null) {
          fn(h);
        }
      });
    };
    module2.exports = InterceptorManager;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/normalizeHeaderName.js
var require_normalizeHeaderName = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/normalizeHeaderName.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    module2.exports = function normalizeHeaderName(headers, normalizedName) {
      utils.forEach(headers, function processHeader(value, name) {
        if (name !== normalizedName && name.toUpperCase() === normalizedName.toUpperCase()) {
          headers[normalizedName] = value;
          delete headers[name];
        }
      });
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/cookies.js
var require_cookies = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/cookies.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    module2.exports = utils.isStandardBrowserEnv() ? (
      // Standard browser envs support document.cookie
      /* @__PURE__ */ function standardBrowserEnv() {
        return {
          write: function write(name, value, expires, path, domain, secure) {
            var cookie = [];
            cookie.push(name + "=" + encodeURIComponent(value));
            if (utils.isNumber(expires)) {
              cookie.push("expires=" + new Date(expires).toGMTString());
            }
            if (utils.isString(path)) {
              cookie.push("path=" + path);
            }
            if (utils.isString(domain)) {
              cookie.push("domain=" + domain);
            }
            if (secure === true) {
              cookie.push("secure");
            }
            document.cookie = cookie.join("; ");
          },
          read: function read(name) {
            var match = document.cookie.match(new RegExp("(^|;\\s*)(" + name + ")=([^;]*)"));
            return match ? decodeURIComponent(match[3]) : null;
          },
          remove: function remove(name) {
            this.write(name, "", Date.now() - 864e5);
          }
        };
      }()
    ) : (
      // Non standard browser env (web workers, react-native) lack needed support.
      /* @__PURE__ */ function nonStandardBrowserEnv() {
        return {
          write: function write() {
          },
          read: function read() {
            return null;
          },
          remove: function remove() {
          }
        };
      }()
    );
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/parseHeaders.js
var require_parseHeaders = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/parseHeaders.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    var ignoreDuplicateOf = [
      "age",
      "authorization",
      "content-length",
      "content-type",
      "etag",
      "expires",
      "from",
      "host",
      "if-modified-since",
      "if-unmodified-since",
      "last-modified",
      "location",
      "max-forwards",
      "proxy-authorization",
      "referer",
      "retry-after",
      "user-agent"
    ];
    module2.exports = function parseHeaders(headers) {
      var parsed = {};
      var key;
      var val;
      var i;
      if (!headers) {
        return parsed;
      }
      utils.forEach(headers.split("\n"), function parser(line) {
        i = line.indexOf(":");
        key = utils.trim(line.substr(0, i)).toLowerCase();
        val = utils.trim(line.substr(i + 1));
        if (key) {
          if (parsed[key] && ignoreDuplicateOf.indexOf(key) >= 0) {
            return;
          }
          if (key === "set-cookie") {
            parsed[key] = (parsed[key] ? parsed[key] : []).concat([val]);
          } else {
            parsed[key] = parsed[key] ? parsed[key] + ", " + val : val;
          }
        }
      });
      return parsed;
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/isURLSameOrigin.js
var require_isURLSameOrigin = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/isURLSameOrigin.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    module2.exports = utils.isStandardBrowserEnv() ? (
      // Standard browser envs have full support of the APIs needed to test
      // whether the request URL is of the same origin as current location.
      function standardBrowserEnv() {
        var msie = /(msie|trident)/i.test(navigator.userAgent);
        var urlParsingNode = document.createElement("a");
        var originURL;
        function resolveURL(url) {
          var href = url;
          if (msie) {
            urlParsingNode.setAttribute("href", href);
            href = urlParsingNode.href;
          }
          urlParsingNode.setAttribute("href", href);
          return {
            href: urlParsingNode.href,
            protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, "") : "",
            host: urlParsingNode.host,
            search: urlParsingNode.search ? urlParsingNode.search.replace(/^\?/, "") : "",
            hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, "") : "",
            hostname: urlParsingNode.hostname,
            port: urlParsingNode.port,
            pathname: urlParsingNode.pathname.charAt(0) === "/" ? urlParsingNode.pathname : "/" + urlParsingNode.pathname
          };
        }
        originURL = resolveURL(window.location.href);
        return function isURLSameOrigin(requestURL) {
          var parsed = utils.isString(requestURL) ? resolveURL(requestURL) : requestURL;
          return parsed.protocol === originURL.protocol && parsed.host === originURL.host;
        };
      }()
    ) : (
      // Non standard browser envs (web workers, react-native) lack needed support.
      /* @__PURE__ */ function nonStandardBrowserEnv() {
        return function isURLSameOrigin() {
          return true;
        };
      }()
    );
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/adapters/xhr.js
var require_xhr = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/adapters/xhr.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    var settle = require_settle();
    var cookies = require_cookies();
    var buildURL = require_buildURL();
    var buildFullPath = require_buildFullPath();
    var parseHeaders = require_parseHeaders();
    var isURLSameOrigin = require_isURLSameOrigin();
    var createError = require_createError();
    module2.exports = function xhrAdapter(config) {
      return new Promise(function dispatchXhrRequest(resolve, reject) {
        var requestData = config.data;
        var requestHeaders = config.headers;
        var responseType = config.responseType;
        if (utils.isFormData(requestData)) {
          delete requestHeaders["Content-Type"];
        }
        var request = new XMLHttpRequest();
        if (config.auth) {
          var username = config.auth.username || "";
          var password = config.auth.password ? unescape(encodeURIComponent(config.auth.password)) : "";
          requestHeaders.Authorization = "Basic " + btoa(username + ":" + password);
        }
        var fullPath = buildFullPath(config.baseURL, config.url);
        request.open(config.method.toUpperCase(), buildURL(fullPath, config.params, config.paramsSerializer), true);
        request.timeout = config.timeout;
        function onloadend() {
          if (!request) {
            return;
          }
          var responseHeaders = "getAllResponseHeaders" in request ? parseHeaders(request.getAllResponseHeaders()) : null;
          var responseData = !responseType || responseType === "text" || responseType === "json" ? request.responseText : request.response;
          var response = {
            data: responseData,
            status: request.status,
            statusText: request.statusText,
            headers: responseHeaders,
            config,
            request
          };
          settle(resolve, reject, response);
          request = null;
        }
        if ("onloadend" in request) {
          request.onloadend = onloadend;
        } else {
          request.onreadystatechange = function handleLoad() {
            if (!request || request.readyState !== 4) {
              return;
            }
            if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf("file:") === 0)) {
              return;
            }
            setTimeout(onloadend);
          };
        }
        request.onabort = function handleAbort() {
          if (!request) {
            return;
          }
          reject(createError("Request aborted", config, "ECONNABORTED", request));
          request = null;
        };
        request.onerror = function handleError() {
          reject(createError("Network Error", config, null, request));
          request = null;
        };
        request.ontimeout = function handleTimeout() {
          var timeoutErrorMessage = "timeout of " + config.timeout + "ms exceeded";
          if (config.timeoutErrorMessage) {
            timeoutErrorMessage = config.timeoutErrorMessage;
          }
          reject(createError(
            timeoutErrorMessage,
            config,
            config.transitional && config.transitional.clarifyTimeoutError ? "ETIMEDOUT" : "ECONNABORTED",
            request
          ));
          request = null;
        };
        if (utils.isStandardBrowserEnv()) {
          var xsrfValue = (config.withCredentials || isURLSameOrigin(fullPath)) && config.xsrfCookieName ? cookies.read(config.xsrfCookieName) : void 0;
          if (xsrfValue) {
            requestHeaders[config.xsrfHeaderName] = xsrfValue;
          }
        }
        if ("setRequestHeader" in request) {
          utils.forEach(requestHeaders, function setRequestHeader(val, key) {
            if (typeof requestData === "undefined" && key.toLowerCase() === "content-type") {
              delete requestHeaders[key];
            } else {
              request.setRequestHeader(key, val);
            }
          });
        }
        if (!utils.isUndefined(config.withCredentials)) {
          request.withCredentials = !!config.withCredentials;
        }
        if (responseType && responseType !== "json") {
          request.responseType = config.responseType;
        }
        if (typeof config.onDownloadProgress === "function") {
          request.addEventListener("progress", config.onDownloadProgress);
        }
        if (typeof config.onUploadProgress === "function" && request.upload) {
          request.upload.addEventListener("progress", config.onUploadProgress);
        }
        if (config.cancelToken) {
          config.cancelToken.promise.then(function onCanceled(cancel) {
            if (!request) {
              return;
            }
            request.abort();
            reject(cancel);
            request = null;
          });
        }
        if (!requestData) {
          requestData = null;
        }
        request.send(requestData);
      });
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/defaults.js
var require_defaults = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/defaults.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    var normalizeHeaderName = require_normalizeHeaderName();
    var enhanceError = require_enhanceError();
    var DEFAULT_CONTENT_TYPE = {
      "Content-Type": "application/x-www-form-urlencoded"
    };
    function setContentTypeIfUnset(headers, value) {
      if (!utils.isUndefined(headers) && utils.isUndefined(headers["Content-Type"])) {
        headers["Content-Type"] = value;
      }
    }
    function getDefaultAdapter() {
      var adapter;
      if (typeof XMLHttpRequest !== "undefined") {
        adapter = require_xhr();
      } else if (typeof process !== "undefined" && Object.prototype.toString.call(process) === "[object process]") {
        adapter = require_http();
      }
      return adapter;
    }
    function stringifySafely(rawValue, parser, encoder) {
      if (utils.isString(rawValue)) {
        try {
          (parser || JSON.parse)(rawValue);
          return utils.trim(rawValue);
        } catch (e) {
          if (e.name !== "SyntaxError") {
            throw e;
          }
        }
      }
      return (encoder || JSON.stringify)(rawValue);
    }
    var defaults2 = {
      transitional: {
        silentJSONParsing: true,
        forcedJSONParsing: true,
        clarifyTimeoutError: false
      },
      adapter: getDefaultAdapter(),
      transformRequest: [function transformRequest(data, headers) {
        normalizeHeaderName(headers, "Accept");
        normalizeHeaderName(headers, "Content-Type");
        if (utils.isFormData(data) || utils.isArrayBuffer(data) || utils.isBuffer(data) || utils.isStream(data) || utils.isFile(data) || utils.isBlob(data)) {
          return data;
        }
        if (utils.isArrayBufferView(data)) {
          return data.buffer;
        }
        if (utils.isURLSearchParams(data)) {
          setContentTypeIfUnset(headers, "application/x-www-form-urlencoded;charset=utf-8");
          return data.toString();
        }
        if (utils.isObject(data) || headers && headers["Content-Type"] === "application/json") {
          setContentTypeIfUnset(headers, "application/json");
          return stringifySafely(data);
        }
        return data;
      }],
      transformResponse: [function transformResponse(data) {
        var transitional = this.transitional;
        var silentJSONParsing = transitional && transitional.silentJSONParsing;
        var forcedJSONParsing = transitional && transitional.forcedJSONParsing;
        var strictJSONParsing = !silentJSONParsing && this.responseType === "json";
        if (strictJSONParsing || forcedJSONParsing && utils.isString(data) && data.length) {
          try {
            return JSON.parse(data);
          } catch (e) {
            if (strictJSONParsing) {
              if (e.name === "SyntaxError") {
                throw enhanceError(e, this, "E_JSON_PARSE");
              }
              throw e;
            }
          }
        }
        return data;
      }],
      /**
       * A timeout in milliseconds to abort a request. If set to 0 (default) a
       * timeout is not created.
       */
      timeout: 0,
      xsrfCookieName: "XSRF-TOKEN",
      xsrfHeaderName: "X-XSRF-TOKEN",
      maxContentLength: -1,
      maxBodyLength: -1,
      validateStatus: function validateStatus(status) {
        return status >= 200 && status < 300;
      }
    };
    defaults2.headers = {
      common: {
        "Accept": "application/json, text/plain, */*"
      }
    };
    utils.forEach(["delete", "get", "head"], function forEachMethodNoData(method) {
      defaults2.headers[method] = {};
    });
    utils.forEach(["post", "put", "patch"], function forEachMethodWithData(method) {
      defaults2.headers[method] = utils.merge(DEFAULT_CONTENT_TYPE);
    });
    module2.exports = defaults2;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/transformData.js
var require_transformData = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/transformData.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    var defaults2 = require_defaults();
    module2.exports = function transformData(data, headers, fns) {
      var context = this || defaults2;
      utils.forEach(fns, function transform(fn) {
        data = fn.call(context, data, headers);
      });
      return data;
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/cancel/isCancel.js
var require_isCancel = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/cancel/isCancel.js"(exports2, module2) {
    "use strict";
    module2.exports = function isCancel(value) {
      return !!(value && value.__CANCEL__);
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/dispatchRequest.js
var require_dispatchRequest = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/dispatchRequest.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    var transformData = require_transformData();
    var isCancel = require_isCancel();
    var defaults2 = require_defaults();
    function throwIfCancellationRequested(config) {
      if (config.cancelToken) {
        config.cancelToken.throwIfRequested();
      }
    }
    module2.exports = function dispatchRequest(config) {
      throwIfCancellationRequested(config);
      config.headers = config.headers || {};
      config.data = transformData.call(
        config,
        config.data,
        config.headers,
        config.transformRequest
      );
      config.headers = utils.merge(
        config.headers.common || {},
        config.headers[config.method] || {},
        config.headers
      );
      utils.forEach(
        ["delete", "get", "head", "post", "put", "patch", "common"],
        function cleanHeaderConfig(method) {
          delete config.headers[method];
        }
      );
      var adapter = config.adapter || defaults2.adapter;
      return adapter(config).then(function onAdapterResolution(response) {
        throwIfCancellationRequested(config);
        response.data = transformData.call(
          config,
          response.data,
          response.headers,
          config.transformResponse
        );
        return response;
      }, function onAdapterRejection(reason) {
        if (!isCancel(reason)) {
          throwIfCancellationRequested(config);
          if (reason && reason.response) {
            reason.response.data = transformData.call(
              config,
              reason.response.data,
              reason.response.headers,
              config.transformResponse
            );
          }
        }
        return Promise.reject(reason);
      });
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/mergeConfig.js
var require_mergeConfig = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/mergeConfig.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    module2.exports = function mergeConfig(config1, config2) {
      config2 = config2 || {};
      var config = {};
      var valueFromConfig2Keys = ["url", "method", "data"];
      var mergeDeepPropertiesKeys = ["headers", "auth", "proxy", "params"];
      var defaultToConfig2Keys = [
        "baseURL",
        "transformRequest",
        "transformResponse",
        "paramsSerializer",
        "timeout",
        "timeoutMessage",
        "withCredentials",
        "adapter",
        "responseType",
        "xsrfCookieName",
        "xsrfHeaderName",
        "onUploadProgress",
        "onDownloadProgress",
        "decompress",
        "maxContentLength",
        "maxBodyLength",
        "maxRedirects",
        "transport",
        "httpAgent",
        "httpsAgent",
        "cancelToken",
        "socketPath",
        "responseEncoding"
      ];
      var directMergeKeys = ["validateStatus"];
      function getMergedValue(target, source) {
        if (utils.isPlainObject(target) && utils.isPlainObject(source)) {
          return utils.merge(target, source);
        } else if (utils.isPlainObject(source)) {
          return utils.merge({}, source);
        } else if (utils.isArray(source)) {
          return source.slice();
        }
        return source;
      }
      function mergeDeepProperties(prop) {
        if (!utils.isUndefined(config2[prop])) {
          config[prop] = getMergedValue(config1[prop], config2[prop]);
        } else if (!utils.isUndefined(config1[prop])) {
          config[prop] = getMergedValue(void 0, config1[prop]);
        }
      }
      utils.forEach(valueFromConfig2Keys, function valueFromConfig2(prop) {
        if (!utils.isUndefined(config2[prop])) {
          config[prop] = getMergedValue(void 0, config2[prop]);
        }
      });
      utils.forEach(mergeDeepPropertiesKeys, mergeDeepProperties);
      utils.forEach(defaultToConfig2Keys, function defaultToConfig2(prop) {
        if (!utils.isUndefined(config2[prop])) {
          config[prop] = getMergedValue(void 0, config2[prop]);
        } else if (!utils.isUndefined(config1[prop])) {
          config[prop] = getMergedValue(void 0, config1[prop]);
        }
      });
      utils.forEach(directMergeKeys, function merge(prop) {
        if (prop in config2) {
          config[prop] = getMergedValue(config1[prop], config2[prop]);
        } else if (prop in config1) {
          config[prop] = getMergedValue(void 0, config1[prop]);
        }
      });
      var axiosKeys = valueFromConfig2Keys.concat(mergeDeepPropertiesKeys).concat(defaultToConfig2Keys).concat(directMergeKeys);
      var otherKeys = Object.keys(config1).concat(Object.keys(config2)).filter(function filterAxiosKeys(key) {
        return axiosKeys.indexOf(key) === -1;
      });
      utils.forEach(otherKeys, mergeDeepProperties);
      return config;
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/validator.js
var require_validator = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/validator.js"(exports2, module2) {
    "use strict";
    var pkg = require_package();
    var validators = {};
    ["object", "boolean", "number", "function", "string", "symbol"].forEach(function(type, i) {
      validators[type] = function validator(thing) {
        return typeof thing === type || "a" + (i < 1 ? "n " : " ") + type;
      };
    });
    var deprecatedWarnings = {};
    var currentVerArr = pkg.version.split(".");
    function isOlderVersion(version, thanVersion) {
      var pkgVersionArr = thanVersion ? thanVersion.split(".") : currentVerArr;
      var destVer = version.split(".");
      for (var i = 0; i < 3; i++) {
        if (pkgVersionArr[i] > destVer[i]) {
          return true;
        } else if (pkgVersionArr[i] < destVer[i]) {
          return false;
        }
      }
      return false;
    }
    validators.transitional = function transitional(validator, version, message) {
      var isDeprecated = version && isOlderVersion(version);
      function formatMessage(opt, desc) {
        return "[Axios v" + pkg.version + "] Transitional option '" + opt + "'" + desc + (message ? ". " + message : "");
      }
      return function(value, opt, opts) {
        if (validator === false) {
          throw new Error(formatMessage(opt, " has been removed in " + version));
        }
        if (isDeprecated && !deprecatedWarnings[opt]) {
          deprecatedWarnings[opt] = true;
          console.warn(
            formatMessage(
              opt,
              " has been deprecated since v" + version + " and will be removed in the near future"
            )
          );
        }
        return validator ? validator(value, opt, opts) : true;
      };
    };
    function assertOptions(options, schema, allowUnknown) {
      if (typeof options !== "object") {
        throw new TypeError("options must be an object");
      }
      var keys = Object.keys(options);
      var i = keys.length;
      while (i-- > 0) {
        var opt = keys[i];
        var validator = schema[opt];
        if (validator) {
          var value = options[opt];
          var result = value === void 0 || validator(value, opt, options);
          if (result !== true) {
            throw new TypeError("option " + opt + " must be " + result);
          }
          continue;
        }
        if (allowUnknown !== true) {
          throw Error("Unknown option " + opt);
        }
      }
    }
    module2.exports = {
      isOlderVersion,
      assertOptions,
      validators
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/Axios.js
var require_Axios = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/core/Axios.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    var buildURL = require_buildURL();
    var InterceptorManager = require_InterceptorManager();
    var dispatchRequest = require_dispatchRequest();
    var mergeConfig = require_mergeConfig();
    var validator = require_validator();
    var validators = validator.validators;
    function Axios(instanceConfig) {
      this.defaults = instanceConfig;
      this.interceptors = {
        request: new InterceptorManager(),
        response: new InterceptorManager()
      };
    }
    Axios.prototype.request = function request(config) {
      if (typeof config === "string") {
        config = arguments[1] || {};
        config.url = arguments[0];
      } else {
        config = config || {};
      }
      config = mergeConfig(this.defaults, config);
      if (config.method) {
        config.method = config.method.toLowerCase();
      } else if (this.defaults.method) {
        config.method = this.defaults.method.toLowerCase();
      } else {
        config.method = "get";
      }
      var transitional = config.transitional;
      if (transitional !== void 0) {
        validator.assertOptions(transitional, {
          silentJSONParsing: validators.transitional(validators.boolean, "1.0.0"),
          forcedJSONParsing: validators.transitional(validators.boolean, "1.0.0"),
          clarifyTimeoutError: validators.transitional(validators.boolean, "1.0.0")
        }, false);
      }
      var requestInterceptorChain = [];
      var synchronousRequestInterceptors = true;
      this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
        if (typeof interceptor.runWhen === "function" && interceptor.runWhen(config) === false) {
          return;
        }
        synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;
        requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
      });
      var responseInterceptorChain = [];
      this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
        responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
      });
      var promise;
      if (!synchronousRequestInterceptors) {
        var chain = [dispatchRequest, void 0];
        Array.prototype.unshift.apply(chain, requestInterceptorChain);
        chain = chain.concat(responseInterceptorChain);
        promise = Promise.resolve(config);
        while (chain.length) {
          promise = promise.then(chain.shift(), chain.shift());
        }
        return promise;
      }
      var newConfig = config;
      while (requestInterceptorChain.length) {
        var onFulfilled = requestInterceptorChain.shift();
        var onRejected = requestInterceptorChain.shift();
        try {
          newConfig = onFulfilled(newConfig);
        } catch (error) {
          onRejected(error);
          break;
        }
      }
      try {
        promise = dispatchRequest(newConfig);
      } catch (error) {
        return Promise.reject(error);
      }
      while (responseInterceptorChain.length) {
        promise = promise.then(responseInterceptorChain.shift(), responseInterceptorChain.shift());
      }
      return promise;
    };
    Axios.prototype.getUri = function getUri(config) {
      config = mergeConfig(this.defaults, config);
      return buildURL(config.url, config.params, config.paramsSerializer).replace(/^\?/, "");
    };
    utils.forEach(["delete", "get", "head", "options"], function forEachMethodNoData(method) {
      Axios.prototype[method] = function(url, config) {
        return this.request(mergeConfig(config || {}, {
          method,
          url,
          data: (config || {}).data
        }));
      };
    });
    utils.forEach(["post", "put", "patch"], function forEachMethodWithData(method) {
      Axios.prototype[method] = function(url, data, config) {
        return this.request(mergeConfig(config || {}, {
          method,
          url,
          data
        }));
      };
    });
    module2.exports = Axios;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/cancel/Cancel.js
var require_Cancel = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/cancel/Cancel.js"(exports2, module2) {
    "use strict";
    function Cancel(message) {
      this.message = message;
    }
    Cancel.prototype.toString = function toString() {
      return "Cancel" + (this.message ? ": " + this.message : "");
    };
    Cancel.prototype.__CANCEL__ = true;
    module2.exports = Cancel;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/cancel/CancelToken.js
var require_CancelToken = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/cancel/CancelToken.js"(exports2, module2) {
    "use strict";
    var Cancel = require_Cancel();
    function CancelToken(executor) {
      if (typeof executor !== "function") {
        throw new TypeError("executor must be a function.");
      }
      var resolvePromise;
      this.promise = new Promise(function promiseExecutor(resolve) {
        resolvePromise = resolve;
      });
      var token = this;
      executor(function cancel(message) {
        if (token.reason) {
          return;
        }
        token.reason = new Cancel(message);
        resolvePromise(token.reason);
      });
    }
    CancelToken.prototype.throwIfRequested = function throwIfRequested() {
      if (this.reason) {
        throw this.reason;
      }
    };
    CancelToken.source = function source() {
      var cancel;
      var token = new CancelToken(function executor(c) {
        cancel = c;
      });
      return {
        token,
        cancel
      };
    };
    module2.exports = CancelToken;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/spread.js
var require_spread = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/spread.js"(exports2, module2) {
    "use strict";
    module2.exports = function spread(callback) {
      return function wrap(arr2) {
        return callback.apply(null, arr2);
      };
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/isAxiosError.js
var require_isAxiosError = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/helpers/isAxiosError.js"(exports2, module2) {
    "use strict";
    module2.exports = function isAxiosError2(payload) {
      return typeof payload === "object" && payload.isAxiosError === true;
    };
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/axios.js
var require_axios = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/lib/axios.js"(exports2, module2) {
    "use strict";
    var utils = require_utils2();
    var bind = require_bind();
    var Axios = require_Axios();
    var mergeConfig = require_mergeConfig();
    var defaults2 = require_defaults();
    function createInstance(defaultConfig) {
      var context = new Axios(defaultConfig);
      var instance = bind(Axios.prototype.request, context);
      utils.extend(instance, Axios.prototype, context);
      utils.extend(instance, context);
      return instance;
    }
    var axios = createInstance(defaults2);
    axios.Axios = Axios;
    axios.create = function create3(instanceConfig) {
      return createInstance(mergeConfig(axios.defaults, instanceConfig));
    };
    axios.Cancel = require_Cancel();
    axios.CancelToken = require_CancelToken();
    axios.isCancel = require_isCancel();
    axios.all = function all(promises) {
      return Promise.all(promises);
    };
    axios.spread = require_spread();
    axios.isAxiosError = require_isAxiosError();
    module2.exports = axios;
    module2.exports.default = axios;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/axios/index.js
var require_axios2 = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/axios/index.js"(exports2, module2) {
    "use strict";
    module2.exports = require_axios();
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/options.js
var require_options = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/options.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.getUrlHeaders = exports2.buildAllHeaders = exports2.buildOptions = void 0;
    var constants_1 = require_constants();
    var headers_1 = require_headers();
    var utils_1 = require_utils();
    var buildOptions = ({ requestOptions, urlObject, headers, globalConfig = {
      httpMockServer: {
        enabled: false
      }
    } }) => {
      const { url, method, params, fallback } = requestOptions;
      if (params) {
        if (typeof params !== "object") {
          throw new Error("Search params must be an object");
        }
        if (fallback === null || fallback === void 0 ? void 0 : fallback.length) {
          if (method === "GET" && !isValidGetRequest(requestOptions)) {
            const fallbackOptions = getFallbackRequestOptions(fallback);
            if (fallbackOptions) {
              requestOptions = Object.assign(Object.assign({}, requestOptions), fallbackOptions);
              if (fallbackOptions.method === "POST") {
                requestOptions.params = void 0;
              }
            }
          }
        }
      }
      const localConfigOptions = maybeRedirectToMockServer(url, globalConfig);
      const newOptions = Object.assign(Object.assign(Object.assign({}, requestOptions), localConfigOptions), { headers });
      return newOptions;
    };
    exports2.buildOptions = buildOptions;
    var buildAllHeaders = (requestOptions, wixHeadersOpts, globalConfig = {
      httpMockServer: {
        enabled: false
      }
    }, urlObject) => {
      const { url, disableWixHeaders, headers, includeWixHeaders } = requestOptions;
      const composedHeaders = (0, headers_1.composeHeaders)({
        url,
        disableWixHeaders,
        includeWixHeaders,
        wixHeadersOpts
      });
      const originalUrlHeader = (0, exports2.getUrlHeaders)(globalConfig, requestOptions, urlObject);
      return Object.assign(Object.assign(Object.assign({}, composedHeaders), lowerAllJsonKeys(headers)), originalUrlHeader);
    };
    exports2.buildAllHeaders = buildAllHeaders;
    var getUrlHeaders = (globalConfig, requestOptions, urlObject) => {
      const { url, params } = requestOptions;
      const { host, protocol } = urlObject;
      const originalUrlHeader = globalConfig.httpMockServer.enabled ? {
        [constants_1.ORIGINAL_URL_HEADER]: buildUrlFromRequest(url, params),
        [constants_1.ORIGINAL_HOST_HEADER]: (0, utils_1.isAbsoluteUrl)(url) ? (0, utils_1.extractHost)(url) : host,
        [constants_1.ORIGINAL_PROTOCOL_HEADER]: (0, utils_1.isAbsoluteUrl)(url) ? (0, utils_1.extractProtocol)(url) : protocol
      } : {};
      return originalUrlHeader;
    };
    exports2.getUrlHeaders = getUrlHeaders;
    function maybeRedirectToMockServer(url, globalConfig) {
      if (globalConfig.httpMockServer.enabled) {
        const mockUrl = new URL(globalConfig.httpMockServer.mockServerUrl);
        return {
          url: (0, utils_1.changeHost)(url, mockUrl.host)
        };
      }
      return {};
    }
    function buildUrlFromRequest(url, requestParams) {
      const { pathname, searchParams } = new URL(url, "http://unused.com");
      requestParams = requestParams || searchParams;
      if (requestParams) {
        const params = (0, utils_1.toSearchQueryParams)(requestParams) || requestParams.toString();
        const paramsAsString = params ? `?${params}` : "";
        return `${pathname}${paramsAsString}`;
      }
      return pathname;
    }
    function lowerAllJsonKeys(objectToChange) {
      objectToChange = objectToChange || {};
      const result = Object.keys(objectToChange).reduce((prev, key) => Object.assign(Object.assign({}, prev), { [key.toLowerCase()]: objectToChange[key] }), {});
      return result;
    }
    function isValidGetRequest(requestOptions) {
      if (requestOptions.method !== "GET") {
        return false;
      }
      const { url, params } = requestOptions;
      return `${url}${params}`.length < constants_1.MAX_NGINX_URL_LENGTH;
    }
    function getFallbackRequestOptions(fallbackRequests) {
      for (const requestOption of fallbackRequests) {
        if (requestOption.method !== "GET" || isValidGetRequest(requestOption)) {
          return requestOption;
        }
      }
    }
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/http-client.js
var require_http_client = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/http-client.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.createHttpClient = exports2.HttpClient = void 0;
    var tslib_1 = require("tslib");
    var axios_1 = tslib_1.__importDefault(require_axios2());
    var xhr_1 = tslib_1.__importDefault(require_xhr());
    var constants_1 = require_constants();
    var env_util_1 = require_env_util();
    var error_1 = require_error();
    var headers_1 = require_headers();
    var options_1 = require_options();
    var utils_1 = require_utils();
    var HttpClient = class {
      constructor(opts = {}) {
        this.opts = opts;
        this.opts = defaultClientOptions(this.opts);
        this.client = axios_1.default.create(this.opts);
        this.flags = {
          sanitizerPocEnabled: checkSanitizerPocEnabled()
        };
      }
      static isHttpError(payload) {
        return !!(payload === null || payload === void 0 ? void 0 : payload.isWixHttpError);
      }
      setErrorHandler(errorHandler) {
        this.opts.errorHandler = errorHandler;
      }
      async request(requestOptionsOrFactory, overrides) {
        var _a, _b;
        const host = (0, utils_1.resolveHost)(this.opts);
        const protocol = (0, utils_1.resolveProtocol)(this.opts);
        const urlObject = { protocol, host };
        const requestOptions = typeof requestOptionsOrFactory === "function" ? requestOptionsOrFactory({ isSSR: this.opts.isSSR, host }) : requestOptionsOrFactory;
        const signedInstance = (overrides === null || overrides === void 0 ? void 0 : overrides.signedInstance) || await ((_b = (_a = this.opts).getAppToken) === null || _b === void 0 ? void 0 : _b.call(_a)) || "";
        const headers = this.getHeaders(requestOptions, signedInstance, urlObject);
        const options = (0, options_1.buildOptions)({
          requestOptions,
          urlObject,
          headers,
          globalConfig: (0, utils_1.getGlobalConfig)()
        });
        if (this.opts.isSSR) {
          options.baseURL = this.opts.baseURL ? this.opts.baseURL : `${constants_1.SSR_PROTOCOL}://${constants_1.SSR_HOST}`;
        }
        try {
          const res = await this.client.request(options).catch((err) => this.maybeFallbackRequest(err, requestOptions, urlObject, signedInstance));
          return this.transformResponse(res, requestOptions);
        } catch (e) {
          this.handleRequestError(e, options);
        }
      }
      async get(url, requestOptions) {
        const opts = Object.assign(Object.assign({}, requestOptions), { url, method: "GET" });
        return this.request(opts);
      }
      async delete(url, requestOptions) {
        const opts = Object.assign(Object.assign({}, requestOptions), { url, method: "DELETE" });
        return this.request(opts);
      }
      async head(url, requestOptions) {
        const opts = Object.assign(Object.assign({}, requestOptions), { url, method: "HEAD" });
        return this.request(opts);
      }
      async options(url, requestOptions) {
        const opts = Object.assign(Object.assign({}, requestOptions), { url, method: "OPTIONS" });
        return this.request(opts);
      }
      async post(url, data, requestOptions) {
        const opts = Object.assign(Object.assign({}, requestOptions), {
          url,
          data,
          method: "POST"
        });
        return this.request(opts);
      }
      async put(url, data, requestOptions) {
        const opts = Object.assign(Object.assign({}, requestOptions), {
          url,
          data,
          method: "PUT"
        });
        return this.request(opts);
      }
      async patch(url, data, requestOptions) {
        const opts = Object.assign(Object.assign({}, requestOptions), {
          url,
          data,
          method: "PATCH"
        });
        return this.request(opts);
      }
      get CancelToken() {
        return axios_1.default.CancelToken;
      }
      get isCancel() {
        return axios_1.default.isCancel;
      }
      getHeaders(requestOptions, signedInstance, urlObject) {
        if (typeof this.opts.createHeaders === "function") {
          return this.getCustomHeaders(requestOptions, signedInstance, urlObject);
        }
        const wixHeadersOpts = {
          signedInstance,
          artifactId: this.opts.artifactId,
          csrf: false,
          multilingualOptions: this.opts.multilingualOptions
        };
        return (0, options_1.buildAllHeaders)(requestOptions, wixHeadersOpts, (0, utils_1.getGlobalConfig)(), urlObject);
      }
      getCustomHeaders(requestOptions, signedInstance, urlObject) {
        const { url, headers } = requestOptions;
        const authHeader = signedInstance && (0, headers_1.isWixDomain)(url) ? { authorization: signedInstance } : {};
        const customHeaders = this.opts.createHeaders();
        const originalUrlHeader = (0, options_1.getUrlHeaders)((0, utils_1.getGlobalConfig)(), requestOptions, urlObject);
        return Object.assign(Object.assign(Object.assign(Object.assign({}, originalUrlHeader), customHeaders), authHeader), headers);
      }
      handleRequestError(error, requestOptions) {
        const requestError = (0, error_1.createHttpError)(error);
        (0, utils_1.silent)(() => {
          var _a;
          return (_a = requestOptions.onError) === null || _a === void 0 ? void 0 : _a.call(requestOptions, requestError);
        });
        (0, utils_1.silent)(() => {
          var _a;
          return (_a = this.opts.errorHandler) === null || _a === void 0 ? void 0 : _a.handleError(requestError, {
            requestOptions
          });
        });
        throw requestError;
      }
      maybeFallbackRequest(err, requestOptions, urlObject, signedInstance) {
        var _a, _b, _c;
        const shouldFallbackRequest = (0, utils_1.isHttpMockServerEnabled)() && (0, utils_1.shouldAllowUnmocked)() && ((_a = err.response) === null || _a === void 0 ? void 0 : _a.status) === 404 && ((_c = (_b = err.response) === null || _b === void 0 ? void 0 : _b.data) === null || _c === void 0 ? void 0 : _c.msg) === constants_1.NO_SCENARIO_FOUND;
        if (shouldFallbackRequest) {
          const headers = this.getHeaders(requestOptions, signedInstance, urlObject);
          const prodOptions = (0, options_1.buildOptions)({
            requestOptions,
            urlObject,
            headers
          });
          return this.client.request(prodOptions);
        }
        throw err;
      }
      transformResponse(res, requestOptions) {
        const requestId = (0, headers_1.requestIdOrEmptyString)(res);
        const data = transformData(res, requestOptions, this.flags);
        return Object.assign(Object.assign({}, res), {
          data,
          requestId
        });
      }
    };
    exports2.HttpClient = HttpClient;
    HttpClient.CancelToken = axios_1.default.CancelToken;
    HttpClient.isCancel = axios_1.default.isCancel;
    function defaultClientOptions(opts) {
      let adapter = opts.adapter;
      if (!adapter) {
        adapter = xhr_1.default;
        if ((0, env_util_1.isNode)()) {
          adapter = getNodeAdapter();
        }
      }
      return Object.assign({ adapter, isSSR: false }, opts);
    }
    function getNodeAdapter() {
      if (typeof __webpack_require__ === "function") {
        return __non_webpack_require__("axios/lib/adapters/http");
      }
      return require_http();
    }
    function transformData(res, requestOptions, flags) {
      let resData = sanitizeResponse(res, flags);
      if (requestOptions._pickResponseBody) {
        resData = (0, utils_1.extractProp)(res.data, requestOptions._pickResponseBody);
      }
      if (requestOptions._logs) {
        const logs = (0, utils_1.extractProp)(res.data, requestOptions._logs);
        if (logs === null || logs === void 0 ? void 0 : logs.length) {
          logs.map((log) => console.log(...log));
        }
      }
      return resData;
    }
    function sanitizeResponse(res, flags) {
      try {
        if (flags.sanitizerPocEnabled) {
          const start = performance.now();
          const sanitizer = new Sanitizer();
          if (res.headers["content-type"].includes("application/json")) {
            console.log("[+] JSON Response detected, sanitizing data");
            const sanitizeStringValue = function(jsonResponse) {
              try {
                Object.keys(jsonResponse).forEach((key) => {
                  if (typeof jsonResponse[key] === "string") {
                    jsonResponse[key] = sanitizer.sanitizeFor("span", jsonResponse[key]).innerHTML;
                  } else if (typeof jsonResponse[key] === "object") {
                    jsonResponse[key] = sanitizeStringValue(jsonResponse[key]);
                  }
                });
                return jsonResponse;
              } catch (error) {
                console.log(error);
              }
            };
            const end = performance.now();
            console.log(`Time taken: ${end - start} milliseconds.`);
            return sanitizeStringValue(res.data);
          }
        }
      } catch (e) {
        console.log("[+] Sanitizer API not supported: %o", e);
      }
      return res.data;
    }
    function checkSanitizerPocEnabled() {
      var _a;
      try {
        return (0, env_util_1.isBrowser)() && new URLSearchParams(((_a = window === null || window === void 0 ? void 0 : window.location) === null || _a === void 0 ? void 0 : _a.hash) || "").get("sanitizerPOC") === "true";
      } catch (_b) {
        return false;
      }
    }
    function createHttpClient(...args) {
      return new HttpClient(...args);
    }
    exports2.createHttpClient = createHttpClient;
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/http-client-node.js
var require_http_client_node = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/http-client-node.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.HttpClient = exports2.createHttpClient = void 0;
    var tslib_1 = require("tslib");
    var http_1 = tslib_1.__importDefault(require_http());
    var http_proxy_agent_1 = require_dist2();
    var https_proxy_agent_1 = require_dist3();
    var env_util_1 = require_env_util();
    var http_client_1 = require_http_client();
    var MAX_NUMBER_OF_REPORTING_PROXY_METRICS_ATTEMPTS = 3;
    var HttpClientNode = class extends http_client_1.HttpClient {
      constructor(opts = {}) {
        const proxyOptions = resolveProxyOptions(opts.disableSquidProxy);
        super(Object.assign(Object.assign({ adapter: http_1.default }, opts), proxyOptions));
        this.reportProxyMetric(opts.measured, proxyOptions);
      }
      /**
       * Attempts to register the http-client-info gauge.
       * If there is no injectedMeasured and the metrics has not yet been intialized by WNP,
       * will retry in 1 second.
       */
      reportProxyMetric(injectedMeasured, proxyOptions, triesCount = 1) {
        const gaugeName = !(proxyOptions === null || proxyOptions === void 0 ? void 0 : proxyOptions.httpsAgent) ? "NoProxy" : "Proxy";
        const gauge = initHttpClientInfoGauge(gaugeName, injectedMeasured);
        if (gauge) {
          gauge(1);
        } else {
          if (triesCount < MAX_NUMBER_OF_REPORTING_PROXY_METRICS_ATTEMPTS) {
            setTimeout(() => this.reportProxyMetric(injectedMeasured, proxyOptions, triesCount + 1), 1e3);
          }
        }
      }
    };
    exports2.HttpClient = HttpClientNode;
    function createHttpClientNode(...args) {
      return new HttpClientNode(...args);
    }
    exports2.createHttpClient = createHttpClientNode;
    function resolveProxyOptions(disableSquidProxy) {
      const proxyUrlAsString = process.env.WIX_HTTP_CLIENT_PROXY_URL;
      if (!(proxyUrlAsString === null || proxyUrlAsString === void 0 ? void 0 : proxyUrlAsString.length) || disableSquidProxy) {
        return {};
      }
      const fullProxyUrl = resolveProxyUrl(proxyUrlAsString);
      return {
        // please see axios issue - https://github.com/axios/axios/issues/925#issuecomment-359982190
        httpsAgent: new https_proxy_agent_1.HttpsProxyAgent(fullProxyUrl),
        httpAgent: new http_proxy_agent_1.HttpProxyAgent(fullProxyUrl)
      };
    }
    function resolveProxyUrl(proxyUrl) {
      if (proxyUrl.startsWith("http")) {
        return proxyUrl;
      }
      return `http://${proxyUrl}`;
    }
    function initHttpClientInfoGauge(gaugeName, injectedMeasured) {
      const client = injectedMeasured !== null && injectedMeasured !== void 0 ? injectedMeasured : initWixMeasured();
      if (!client) {
        if ((0, env_util_1.isProduction)()) {
          console.warn(`could not extract metrics client, will retry`);
        }
        return;
      }
      return client.collection("name", "http-client-info").gauge(gaugeName);
    }
    function initWixMeasured() {
      var _a, _b;
      const globalContextKey = Symbol.for("wix-visibility-global-context");
      return (_b = (_a = global[globalContextKey]) === null || _a === void 0 ? void 0 : _a.metrics) === null || _b === void 0 ? void 0 : _b.client;
    }
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/types.js
var require_types = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/types.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
  }
});

// ../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/index-node.js
var require_index_node = __commonJS({
  "../../node_modules/@wix/http-client-testkit/node_modules/@wix/http-client/dist/src/index-node.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.HttpClient = exports2.createHttpClient = exports2.HttpError = exports2.createHttpError = exports2.ORIGINAL_URL_HEADER = exports2.ORIGINAL_PROTOCOL_HEADER = exports2.ORIGINAL_HOST_HEADER = void 0;
    var tslib_1 = require("tslib");
    var constants_1 = require_constants();
    Object.defineProperty(exports2, "ORIGINAL_HOST_HEADER", { enumerable: true, get: function() {
      return constants_1.ORIGINAL_HOST_HEADER;
    } });
    Object.defineProperty(exports2, "ORIGINAL_PROTOCOL_HEADER", { enumerable: true, get: function() {
      return constants_1.ORIGINAL_PROTOCOL_HEADER;
    } });
    Object.defineProperty(exports2, "ORIGINAL_URL_HEADER", { enumerable: true, get: function() {
      return constants_1.ORIGINAL_URL_HEADER;
    } });
    var error_1 = require_error();
    Object.defineProperty(exports2, "createHttpError", { enumerable: true, get: function() {
      return error_1.createHttpError;
    } });
    Object.defineProperty(exports2, "HttpError", { enumerable: true, get: function() {
      return error_1.HttpError;
    } });
    var http_client_node_1 = require_http_client_node();
    Object.defineProperty(exports2, "createHttpClient", { enumerable: true, get: function() {
      return http_client_node_1.createHttpClient;
    } });
    Object.defineProperty(exports2, "HttpClient", { enumerable: true, get: function() {
      return http_client_node_1.HttpClient;
    } });
    tslib_1.__exportStar(require_types(), exports2);
  }
});

// ../../node_modules/dedent-js/lib/index.js
var require_lib = __commonJS({
  "../../node_modules/dedent-js/lib/index.js"(exports2, module2) {
    "use strict";
    module2.exports = function dedent3(templateStrings) {
      var values = [];
      for (var _i = 1; _i < arguments.length; _i++) {
        values[_i - 1] = arguments[_i];
      }
      var matches = [];
      var strings = typeof templateStrings === "string" ? [templateStrings] : templateStrings.slice();
      strings[strings.length - 1] = strings[strings.length - 1].replace(/\r?\n([\t ]*)$/, "");
      for (var i = 0; i < strings.length; i++) {
        var match = void 0;
        if (match = strings[i].match(/\n[\t ]+/g)) {
          matches.push.apply(matches, match);
        }
      }
      if (matches.length) {
        var size = Math.min.apply(Math, matches.map(function(value) {
          return value.length - 1;
        }));
        var pattern = new RegExp("\n[	 ]{" + size + "}", "g");
        for (var i = 0; i < strings.length; i++) {
          strings[i] = strings[i].replace(pattern, "\n");
        }
      }
      strings[0] = strings[0].replace(/^\r?\n/, "");
      var string = strings[0];
      for (var i = 0; i < values.length; i++) {
        string += values[i] + strings[i + 1];
      }
      return string;
    };
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/assert.js
var require_assert = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/assert.js"(exports2, module2) {
    "use strict";
    var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function(obj) {
      return typeof obj;
    } : function(obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    function _possibleConstructorReturn(self2, call) {
      if (!self2) {
        throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      }
      return call && (typeof call === "object" || typeof call === "function") ? call : self2;
    }
    function _inherits(subClass, superClass) {
      if (typeof superClass !== "function" && superClass !== null) {
        throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
      }
      subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } });
      if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
    }
    var AssertionError = function(_Error) {
      _inherits(AssertionError2, _Error);
      function AssertionError2(message) {
        _classCallCheck(this, AssertionError2);
        var _this = _possibleConstructorReturn(this, (AssertionError2.__proto__ || Object.getPrototypeOf(AssertionError2)).call(this, message));
        _this.name = _this.constructor.name;
        return _this;
      }
      return AssertionError2;
    }(Error);
    function assertDefined(target, message) {
      if (target === void 0) {
        throw new AssertionError(message);
      }
    }
    function assertObject(target, message) {
      if (target !== void 0 && ((typeof target === "undefined" ? "undefined" : _typeof(target)) !== "object" || Array.isArray(target) || target === null)) {
        throw new AssertionError(message);
      }
    }
    function assertOk(target, message) {
      if (!target) {
        throw new AssertionError(message);
      }
    }
    function assertFunc(target, message) {
      if (target !== void 0 && typeof target !== "function") {
        throw new AssertionError(message);
      }
    }
    function assertBoolean2(target, message) {
      if (target !== void 0 && typeof target !== "boolean") {
        throw new AssertionError(message);
      }
    }
    function assertNumber(target, message) {
      if (target !== void 0 && typeof target !== "number") {
        throw new AssertionError(message);
      }
    }
    function assertArray(target, message) {
      if (typeof Array.isArray === "function") {
        if (!Array.isArray(target)) {
          throw new AssertionError(message);
        }
      } else if (Object.prototype.toString.call(target) !== "[object Array]") {
        throw new AssertionError(message);
      }
    }
    module2.exports.defined = assertDefined;
    module2.exports.object = assertObject;
    module2.exports.ok = assertOk;
    module2.exports.func = assertFunc;
    module2.exports.boolean = assertBoolean2;
    module2.exports.number = assertNumber;
    module2.exports.array = assertArray;
    module2.exports.AssertionError = AssertionError;
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/utils/collections.js
var require_collections = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/utils/collections.js"(exports2, module2) {
    "use strict";
    module2.exports.mapValues = function(collection, iteratee) {
      if (!collection) {
        return {};
      }
      return Object.keys(collection).reduce(function(acc, key) {
        acc[key] = iteratee(collection[key], key, collection);
        return acc;
      }, {});
    };
    module2.exports.filterValues = function(collection, iteratee) {
      if (!collection) {
        return {};
      }
      return Object.keys(collection).reduce(function(acc, key) {
        var keep = iteratee(collection[key], key, collection);
        if (keep) {
          acc[key] = collection[key];
        }
        return acc;
      }, {});
    };
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/utils/promise.js
var require_promise = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/utils/promise.js"(exports2, module2) {
    "use strict";
    module2.exports.timedPromise = function(promise, _ref) {
      var message = _ref.message, timeout = _ref.timeout;
      var timeoutPromise = new Promise(function(resolve, reject) {
        setTimeout(reject, timeout, message ? "Timeout: " + message : "Timeout");
      });
      return Promise.race([promise, timeoutPromise]);
    };
    module2.exports.allAsObject = function(promiseObject) {
      var keys = Object.keys(promiseObject);
      return Promise.all(keys.map(function(key) {
        return promiseObject[key];
      })).then(function(resolved) {
        return resolved.reduce(function(acc, value, i) {
          acc[keys[i]] = value;
          return acc;
        }, {});
      });
    };
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/utils/log.js
var require_log = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/utils/log.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      error: function error() {
        if (console && console.error) {
          var _console;
          (_console = console).error.apply(_console, arguments);
        }
      }
    };
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/utils/debounce.js
var require_debounce = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/utils/debounce.js"(exports2, module2) {
    "use strict";
    function debounce(func, wait, immediate) {
      var timeout = void 0;
      return function() {
        var context = this;
        var args = arguments;
        var later = function later2() {
          timeout = null;
          if (!immediate) {
            func.apply(context, args);
          }
        };
        var callNow = immediate && !timeout;
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
        if (callNow) {
          func.apply(context, args);
        }
      };
    }
    module2.exports = debounce;
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/utils/throttle.js
var require_throttle = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/utils/throttle.js"(exports2, module2) {
    "use strict";
    function throttle(func, wait) {
      var timeout = void 0;
      return function() {
        for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        if (!timeout) {
          timeout = setTimeout(function() {
            func.apply(void 0, args);
            timeout = null;
          }, wait);
        }
      };
    }
    module2.exports = throttle;
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/utils/batch-queue.js
var require_batch_queue = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/utils/batch-queue.js"(exports2, module2) {
    "use strict";
    var _createClass2 = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i = 0; i < props.length; i++) {
          var descriptor = props[i];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    var _slicedToArray = /* @__PURE__ */ function() {
      function sliceIterator(arr2, i) {
        var _arr = [];
        var _n = true;
        var _d = false;
        var _e = void 0;
        try {
          for (var _i = arr2[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) {
            _arr.push(_s.value);
            if (i && _arr.length === i) break;
          }
        } catch (err) {
          _d = true;
          _e = err;
        } finally {
          try {
            if (!_n && _i["return"]) _i["return"]();
          } finally {
            if (_d) throw _e;
          }
        }
        return _arr;
      }
      return function(arr2, i) {
        if (Array.isArray(arr2)) {
          return arr2;
        } else if (Symbol.iterator in Object(arr2)) {
          return sliceIterator(arr2, i);
        } else {
          throw new TypeError("Invalid attempt to destructure non-iterable instance");
        }
      };
    }();
    var _extends = Object.assign || function(target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = arguments[i];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    var debounce = require_debounce();
    var throttle = require_throttle();
    var createEvent = function createEvent2(event, context, startTime) {
      return {
        dt: Date.now() - startTime,
        f: event,
        context
      };
    };
    var createBatch = function createBatch2(events, startTime) {
      return {
        dt: Date.now() - startTime,
        e: events,
        g: {}
      };
    };
    var optimizeBatch = function optimizeBatch2(batch) {
      var fieldCounts = {};
      var batchLen = batch.e.length;
      var events = batch.e.map(function(event) {
        var fields = Object.keys(event.f).map(function(field) {
          var value = event.f[field];
          var key = field + "|" + value;
          fieldCounts[key] = fieldCounts[key] || 0;
          fieldCounts[key]++;
          return [field, value, key];
        });
        return _extends({}, event, {
          f: fields
        });
      });
      var globalFields = {};
      events = events.map(function(event) {
        var fields = event.f.reduce(function(res, _ref) {
          var _ref2 = _slicedToArray(_ref, 3), field = _ref2[0], value = _ref2[1], key = _ref2[2];
          if (fieldCounts[key] === batchLen) {
            globalFields[field] = value;
          } else {
            res[field] = value;
          }
          return res;
        }, {});
        return _extends({}, event, {
          f: fields
        });
      });
      return _extends({}, batch, {
        e: events,
        g: globalFields
      });
    };
    var BatchQueue = function() {
      function BatchQueue2() {
        _classCallCheck(this, BatchQueue2);
        this._initilized = false;
      }
      _createClass2(BatchQueue2, [{
        key: "_reset",
        value: function _reset() {
          var _this = this;
          this._startTime = Date.now();
          this._resolve = null;
          this._promise = new Promise(function(resolve) {
            return _this._resolve = resolve;
          });
        }
      }, {
        key: "init",
        value: function init(_ref3, flushHandler) {
          var _this2 = this;
          var delayMs = _ref3.delayMs, maxBatchSize = _ref3.maxBatchSize, useThrottle = _ref3.useThrottle, optimizeBatch2 = _ref3.optimizeBatch;
          if (this._initilized) {
            return;
          }
          this._maxBatchSize = maxBatchSize;
          this._optimizeBatch = optimizeBatch2;
          this._queue = [];
          this._flushHandler = flushHandler;
          this._flushDebounced = useThrottle ? throttle(function() {
            return _this2.flush();
          }, delayMs) : debounce(function() {
            return _this2.flush();
          }, delayMs);
          this._initilized = true;
          this._reset();
        }
      }, {
        key: "flush",
        value: function flush() {
          if (!this._queue.length) {
            return Promise.resolve();
          }
          var events = this._queue.splice(0, this._queue.length);
          var resolve = this._resolve;
          var startTime = this._startTime;
          this._reset();
          var batch = createBatch(events, startTime);
          if (this._optimizeBatch) {
            batch = optimizeBatch(batch);
          }
          return this._flushHandler(batch).then(resolve);
        }
      }, {
        key: "feed",
        value: function feed(event, context) {
          this._queue.push(createEvent(event, context, this._startTime));
          if (this._queue.length === this._maxBatchSize) {
            return this.flush();
          }
          this._flushDebounced();
          return this._promise;
        }
      }]);
      return BatchQueue2;
    }();
    module2.exports = BatchQueue;
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/consent-policy.js
var require_consent_policy = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/consent-policy.js"(exports2, module2) {
    "use strict";
    var DefaultConsentPolicy = {
      functional: true,
      analytics: true,
      __default: true
    };
    var getPolicy = function getPolicy2(consentPolicyGetter) {
      return typeof consentPolicyGetter === "function" && consentPolicyGetter() || DefaultConsentPolicy;
    };
    var shouldMuteNonEssentials = function shouldMuteNonEssentials2(policy) {
      return policy.functional === false || policy.analytics === false;
    };
    var shouldMuteByCategory = function shouldMuteByCategory2(policy, category) {
      if (category === "essential") {
        return false;
      }
      if (category === "functional" || category === "analytics") {
        return policy[category] === false;
      }
      return shouldMuteNonEssentials(policy);
    };
    module2.exports = {
      shouldMuteNonEssentials,
      shouldMuteByCategory,
      getPolicy
    };
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/bi-logger.js
var require_bi_logger = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/bi-logger.js"(exports2, module2) {
    "use strict";
    var _extends = Object.assign || function(target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = arguments[i];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    var _createClass2 = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i = 0; i < props.length; i++) {
          var descriptor = props[i];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    function _objectWithoutProperties(obj, keys) {
      var target = {};
      for (var i in obj) {
        if (keys.indexOf(i) >= 0) continue;
        if (!Object.prototype.hasOwnProperty.call(obj, i)) continue;
        target[i] = obj[i];
      }
      return target;
    }
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    var assert2 = require_assert();
    var _require = require_collections();
    var mapValues = _require.mapValues;
    var filterValues = _require.filterValues;
    var promise = require_promise();
    var log = require_log();
    var BatchQueue = require_batch_queue();
    var _require2 = require_consent_policy();
    var shouldMuteByCategory = _require2.shouldMuteByCategory;
    var shouldMuteNonEssentials = _require2.shouldMuteNonEssentials;
    var getPolicy = _require2.getPolicy;
    var BiLogger = function() {
      function BiLogger2(options, context) {
        _classCallCheck(this, BiLogger2);
        this._publishers = options.publishers;
        this._validators = options.validators || [];
        this._defaults = options.defaults;
        this._ownDefaults = {};
        this._events = options.events || {};
        this._context = context || {};
        this._defaultValueTimeout = options.defaultValueTimeout || 5e3;
        this._defaultContinueOnFail = options.defaultContinueOnFail || false;
        this._onPublisherFailHandler = options.onPublisherFailHandler || BiLogger2._defaultPublisherFailHandler;
        this._isMuted = options.isMuted || function() {
          return false;
        };
        this._eventTransformer = options.eventTransformer || function(event) {
          return event;
        };
        this._payloadTransformer = options.payloadTransformer || function(payload) {
          return payload;
        };
        this._consentPolicyGetter = options.consentPolicyGetter || function() {
          return null;
        };
        this._nonEssentialDefaults = options.nonEssentialDefaults || {};
        this._maxBatchSize = options.maxBatchSize || 100;
        this._globalBatchQueue = options.globalBatchQueue;
      }
      _createClass2(BiLogger2, [{
        key: "report",
        value: function report(data) {
          assert2.defined(data, "Data must be provided");
          assert2.object(data, "Data must be an object");
          var src = data.src, evid = data.evid, params = data.params, context = _objectWithoutProperties(data, ["src", "evid", "params"]);
          return this.log(_extends({ src, evid }, params), context);
        }
      }, {
        key: "log",
        value: function log2(eventOrKey, eventOrContextOrUndefined, contextOrUndefined) {
          var _this = this;
          assert2.defined(eventOrKey, "Event object or event key must be provided.");
          var _extractEventAndConte = this._extractEventAndContext(eventOrKey, eventOrContextOrUndefined, contextOrUndefined), event = _extractEventAndConte.event, context = _extractEventAndConte.context;
          var policy = getPolicy(this._consentPolicyGetter);
          var fullContext = _extends({}, this._context, context);
          if (this._isMuted() || shouldMuteByCategory(policy, fullContext.category)) {
            return Promise.resolve();
          }
          if (fullContext.useBatch) {
            var queue = this._initQueue(fullContext, policy);
            var transformAndQueue = function transformAndQueue2(_event) {
              var transformedEvent = _this._eventTransformer(_event, fullContext);
              return queue.feed(transformedEvent, fullContext);
            };
            if (this._globalBatchQueue) {
              return this._getDefaults(this._defaults).then(function(defaults2) {
                var fullEvent2 = _extends({}, defaults2, _this._getDynamicNonEssentialDefaults(policy), _this._getStaticNonEssentialDefaults(policy), event, _this._getPolicyFields(policy, fullContext.category));
                return transformAndQueue(fullEvent2);
              });
            } else {
              var fullEvent = _extends({}, this._getDynamicDefaults(this._defaults), this._getDynamicNonEssentialDefaults(policy), event, this._getPolicyFields(policy, fullContext.category));
              return transformAndQueue(fullEvent);
            }
          }
          return this._getDefaults(this._defaults).then(function(defaults2) {
            var fullEvent2 = Object.assign(defaults2, _this._getDynamicNonEssentialDefaults(policy), _this._getStaticNonEssentialDefaults(policy), event, _this._getPolicyFields(policy, fullContext.category));
            var validatorsResult = _this._validators.length === 0 ? true : _this._validators.some(function(validator) {
              return validator.match(fullEvent2) && (validator.execute(fullEvent2) || true);
            });
            if (!validatorsResult) {
              throw new Error("No validator accepted the event. Source: " + fullEvent2.src + " Evid: " + (fullEvent2.evid || fullEvent2.evtId));
            }
            var transformedEvent = _this._eventTransformer(fullEvent2, fullContext);
            transformedEvent = _this._payloadTransformer(transformedEvent, fullContext);
            return _this._send(transformedEvent, fullContext);
          });
        }
      }, {
        key: "flush",
        value: function flush() {
          if (!this._queue) {
            return Promise.resolve();
          }
          return this._queue.flush();
        }
      }, {
        key: "updateDefaults",
        value: function updateDefaults(defaults2) {
          assert2.defined(defaults2, "Defaults must be provided");
          assert2.object(defaults2, "Defaults must be an object");
          Object.assign(this._ownDefaults, defaults2);
          return this;
        }
      }, {
        key: "_send",
        value: function _send(payload) {
          var _this2 = this;
          var context = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
          return Promise.all(this._publishers.map(function(publisher) {
            var cloned = _extends({}, payload);
            return Promise.resolve().then(function() {
              return publisher(cloned, context);
            }).catch(function(error) {
              return _this2._onPublisherFailHandler(error, {
                publisherName: publisher.name,
                payload
              });
            });
          })).then(function() {
            return void 0;
          });
        }
      }, {
        key: "_extractEventAndContext",
        value: function _extractEventAndContext(eventOrKey, eventOrContextOrUndefined, contextOrUndefined) {
          var event = void 0;
          var context = {};
          if (typeof eventOrKey !== "string") {
            event = eventOrKey;
            context = eventOrContextOrUndefined || context;
          } else {
            event = this._events[eventOrKey];
            if (!event) {
              throw new assert2.AssertionError("Event with key '" + eventOrKey + "' not found in event map.");
            }
            if (eventOrContextOrUndefined) {
              event = _extends({}, event, eventOrContextOrUndefined);
              context = contextOrUndefined || context;
            }
          }
          return { event, context };
        }
      }, {
        key: "_initQueue",
        value: function _initQueue(context, policy) {
          var _this3 = this;
          if (this._queue) {
            return this._queue;
          }
          this._queue = this._globalBatchQueue || new BatchQueue();
          var onFlush = function onFlush2(batch) {
            if (!_this3._globalBatchQueue) {
              batch.g = Object.assign(_this3._getStaticDefaults(_this3._defaults), _this3._getStaticNonEssentialDefaults(policy));
            }
            var transformedPayload = _this3._payloadTransformer(batch, context);
            return _this3._send(transformedPayload, context);
          };
          this._queue.init({
            delayMs: context.useBatch === true ? 300 : context.useBatch,
            maxBatchSize: this._maxBatchSize,
            useThrottle: !!this._globalBatchQueue,
            optimizeBatch: !!this._globalBatchQueue
          }, onFlush);
          return this._queue;
        }
      }, {
        key: "_handleDefaultsError",
        value: function _handleDefaultsError(err) {
          if (this._defaultContinueOnFail) {
            log.error(err);
            return null;
          }
          return Promise.reject(err);
        }
      }, {
        key: "_getDynamicNonEssentialDefaults",
        value: function _getDynamicNonEssentialDefaults(policy) {
          if (!shouldMuteNonEssentials(policy)) {
            return this._getDynamicDefaults(this._nonEssentialDefaults);
          }
        }
      }, {
        key: "_getStaticNonEssentialDefaults",
        value: function _getStaticNonEssentialDefaults(policy) {
          if (!shouldMuteNonEssentials(policy)) {
            return this._getStaticDefaults(this._nonEssentialDefaults);
          }
        }
      }, {
        key: "_withOwnDefaults",
        value: function _withOwnDefaults(defaults2) {
          return Object.assign({}, defaults2, this._ownDefaults);
        }
      }, {
        key: "_getDynamicDefaults",
        value: function _getDynamicDefaults(defaults2) {
          defaults2 = this._withOwnDefaults(defaults2);
          var dynamicDefaults = filterValues(defaults2, function(v) {
            return typeof v === "function";
          });
          return mapValues(dynamicDefaults, function(v) {
            return v();
          });
        }
      }, {
        key: "_getStaticDefaults",
        value: function _getStaticDefaults(defaults2) {
          defaults2 = this._withOwnDefaults(defaults2);
          var staticDefaults = filterValues(defaults2, function(v) {
            return typeof v !== "function";
          });
          return staticDefaults;
        }
      }, {
        key: "_getDefaults",
        value: function _getDefaults(defaults2) {
          var _this4 = this;
          defaults2 = this._withOwnDefaults(defaults2);
          if (!defaults2) {
            return Promise.resolve({});
          }
          var promises = mapValues(defaults2, function(value, key) {
            if (typeof value === "function") {
              try {
                value = value();
              } catch (err) {
                return _this4._handleDefaultsError(err);
              }
            }
            if (value && typeof value.then === "function") {
              return promise.timedPromise(value, {
                message: "Cannot get default value '" + key + " for BI Event'",
                timeout: _this4._defaultValueTimeout
              }).catch(function(err) {
                return _this4._handleDefaultsError(err);
              });
            }
            return value;
          });
          return promise.allAsObject(promises);
        }
      }, {
        key: "_encodePolicyValue",
        value: function _encodePolicyValue(policy, key) {
          if (!policy) {
            return 1;
          }
          if (typeof policy[key] === "boolean") {
            return policy[key] ? 1 : 0;
          }
          return policy[key];
        }
      }, {
        key: "_getPolicyFields",
        value: function _getPolicyFields(policy, category) {
          return {
            _isca: this._encodePolicyValue(policy, "analytics"),
            _iscf: this._encodePolicyValue(policy, "functional"),
            _ispd: policy.__default ? 1 : 0,
            _ise: category === "essential" ? 1 : 0
          };
        }
      }], [{
        key: "_defaultPublisherFailHandler",
        value: function _defaultPublisherFailHandler(error, _ref) {
          var publisherName = _ref.publisherName;
          return publisherName;
        }
      }]);
      return BiLogger2;
    }();
    module2.exports = BiLogger;
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/bi-logger-manager.js
var require_bi_logger_manager = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/bi-logger-manager.js"(exports2, module2) {
    "use strict";
    var _createClass2 = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i = 0; i < props.length; i++) {
          var descriptor = props[i];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    var assert2 = require_assert();
    var BiLoggerManager = function() {
      function BiLoggerManager2() {
        _classCallCheck(this, BiLoggerManager2);
        this.reset();
      }
      _createClass2(BiLoggerManager2, [{
        key: "reset",
        value: function reset() {
          this._handlers = [];
        }
      }, {
        key: "onLoggerCreated",
        value: function onLoggerCreated(handler) {
          var _this = this;
          assert2.defined(handler, "Handler must be provided.");
          assert2.func(handler, "Handler must be a function.");
          this._handlers.push(handler);
          return function() {
            var index = _this._handlers.indexOf(handler);
            if (index !== -1) {
              _this._handlers.splice(index, 1);
            }
          };
        }
      }, {
        key: "notifyLoggerCreated",
        value: function notifyLoggerCreated(logger) {
          this._handlers.forEach(function(handler) {
            return handler(logger);
          });
        }
      }]);
      return BiLoggerManager2;
    }();
    module2.exports = {
      manager: new BiLoggerManager(),
      BiLoggerManager
    };
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/bi-logger-factory.js
var require_bi_logger_factory = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/bi-logger-factory.js"(exports2, module2) {
    "use strict";
    var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function(obj) {
      return typeof obj;
    } : function(obj) {
      return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    };
    var _createClass2 = /* @__PURE__ */ function() {
      function defineProperties(target, props) {
        for (var i = 0; i < props.length; i++) {
          var descriptor = props[i];
          descriptor.enumerable = descriptor.enumerable || false;
          descriptor.configurable = true;
          if ("value" in descriptor) descriptor.writable = true;
          Object.defineProperty(target, descriptor.key, descriptor);
        }
      }
      return function(Constructor, protoProps, staticProps) {
        if (protoProps) defineProperties(Constructor.prototype, protoProps);
        if (staticProps) defineProperties(Constructor, staticProps);
        return Constructor;
      };
    }();
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    var assert2 = require_assert();
    var BiLogger = require_bi_logger();
    var biLoggerManager = require_bi_logger_manager();
    var BatchQueue = require_batch_queue();
    var BiLoggerFactory = function() {
      function BiLoggerFactory2() {
        _classCallCheck(this, BiLoggerFactory2);
        this._publishers = [];
        this._validators = [];
        this._defaults = {};
        this._nonEssentialDefaults = {};
        this._events = {};
        this._isMuted = false;
        this._eventTransformer = null;
        this._payloadTransformer = null;
        this._consentPolicyGetter = null;
        this._maxBatchSize = null;
        this._batchQueue = null;
      }
      _createClass2(BiLoggerFactory2, [{
        key: "addPublisher",
        value: function addPublisher(publisher) {
          assert2.defined(publisher, "Publisher must be provided");
          assert2.ok(typeof publisher === "function", "Expected a publisher function");
          this._publishers.push(publisher);
          return this;
        }
      }, {
        key: "addValidator",
        value: function addValidator(validator) {
          assert2.defined(validator, "Validator must be provided");
          assert2.ok((typeof validator === "undefined" ? "undefined" : _typeof(validator)) === "object" && validator, "Expected a validator object");
          assert2.ok(validator.execute && validator.match, "Provided validator does not match the interface");
          this._validators.push(validator);
          return this;
        }
      }, {
        key: "setDefaults",
        value: function setDefaults(defaults2) {
          assert2.defined(defaults2, "Defaults must be provided");
          assert2.object(defaults2, "Defaults must be an object");
          this._defaults = defaults2;
          return this;
        }
      }, {
        key: "updateDefaults",
        value: function updateDefaults(defaults2) {
          assert2.defined(defaults2, "Defaults must be provided");
          assert2.object(defaults2, "Defaults must be an object");
          Object.assign(this._defaults, defaults2);
          return this;
        }
      }, {
        key: "updateNonEssentialDefaults",
        value: function updateNonEssentialDefaults(defaults2) {
          assert2.defined(defaults2, "Non-essential Defaults must be provided");
          assert2.object(defaults2, "Non-essential Defaults must be an object");
          Object.assign(this._nonEssentialDefaults, defaults2);
          return this;
        }
      }, {
        key: "setEvents",
        value: function setEvents(events) {
          assert2.defined(events, "Events must be provided");
          assert2.object(events, "Events must be an object");
          this._events = events;
          return this;
        }
      }, {
        key: "setDefaultValueTimeout",
        value: function setDefaultValueTimeout(defaultValueTimeout) {
          assert2.defined(defaultValueTimeout, "Default Value Timeout must be provided");
          this._defaultValueTimeout = defaultValueTimeout;
          return this;
        }
      }, {
        key: "setDefaultContinueOnFail",
        value: function setDefaultContinueOnFail(defaultContinueOnFail) {
          assert2.defined(defaultContinueOnFail, "Default Continue On Fail must be provided");
          this._defaultContinueOnFail = defaultContinueOnFail;
          return this;
        }
      }, {
        key: "setPublisherFailHandler",
        value: function setPublisherFailHandler(onPublisherFailHandler) {
          assert2.defined(onPublisherFailHandler, "Publisher Fail Handler must be provided");
          this._onPublisherFailHandler = onPublisherFailHandler;
          return this;
        }
      }, {
        key: "setMuted",
        value: function setMuted(isMuted) {
          assert2.defined(isMuted, "Is Muted must be provided");
          assert2.boolean(isMuted, "Is Muted must be a boolean");
          this._isMuted = isMuted;
          return this;
        }
      }, {
        key: "setMaxBatchSize",
        value: function setMaxBatchSize(maxBatchSize) {
          assert2.defined(maxBatchSize, "Max Batch Size must be provided");
          assert2.number(maxBatchSize, "Max Batch Size must be a number");
          assert2.ok(maxBatchSize > 0, "Max Batch Size must be higher than 0");
          this._maxBatchSize = maxBatchSize;
          return this;
        }
      }, {
        key: "setGlobalBatchQueue",
        value: function setGlobalBatchQueue(batchQueue) {
          assert2.defined(batchQueue, "Global Batch Queue must be provided");
          assert2.ok(batchQueue instanceof BatchQueue, "Global Batch Queue must be an instance of BatchQueue");
          this._globalBatchQueue = batchQueue;
          return this;
        }
      }, {
        key: "withEventTransformer",
        value: function withEventTransformer(transformer) {
          assert2.defined(transformer, "Event Transformer must be provided");
          assert2.func(transformer, "Event Transformer must be a function");
          this._eventTransformer = transformer;
          return this;
        }
      }, {
        key: "withPayloadTransformer",
        value: function withPayloadTransformer(transformer) {
          assert2.defined(transformer, "Payload Transformer must be provided");
          assert2.func(transformer, "Payload Transformer must be a function");
          this._payloadTransformer = transformer;
          return this;
        }
      }, {
        key: "withConsentPolicyGetter",
        value: function withConsentPolicyGetter(getter) {
          assert2.defined(getter, "Consent Policy Getter must be provided");
          assert2.func(getter, "Consent Policy Getter must be a function");
          this._consentPolicyGetter = getter;
          return this;
        }
      }, {
        key: "logger",
        value: function logger(context) {
          var _this = this;
          var logger2 = new BiLogger({
            publishers: this._publishers,
            validators: this._validators,
            defaults: this._defaults,
            events: this._events,
            defaultValueTimeout: this._defaultValueTimeout,
            defaultContinueOnFail: this._defaultContinueOnFail,
            onPublisherFailHandler: this._onPublisherFailHandler,
            isMuted: function isMuted() {
              return _this._isMuted;
            },
            eventTransformer: this._eventTransformer,
            payloadTransformer: this._payloadTransformer,
            consentPolicyGetter: this._consentPolicyGetter,
            nonEssentialDefaults: this._nonEssentialDefaults,
            maxBatchSize: this._maxBatchSize,
            globalBatchQueue: this._globalBatchQueue
          }, context);
          biLoggerManager.manager.notifyLoggerCreated(logger2);
          return logger2;
        }
      }]);
      return BiLoggerFactory2;
    }();
    module2.exports = BiLoggerFactory;
  }
});

// ../../node_modules/@wix/wix-bi-logger-client/dist/index.js
var require_dist4 = __commonJS({
  "../../node_modules/@wix/wix-bi-logger-client/dist/index.js"(exports2, module2) {
    "use strict";
    var BiLoggerClientFactory = require_bi_logger_factory();
    var BiLogger = require_bi_logger();
    var biLoggerManager = require_bi_logger_manager();
    var BatchQueue = require_batch_queue();
    module2.exports.BiLoggerFactory = BiLoggerClientFactory;
    module2.exports.BiLogger = BiLogger;
    module2.exports.BiLoggerManager = biLoggerManager.BiLoggerManager;
    module2.exports.factory = function() {
      return new BiLoggerClientFactory();
    };
    module2.exports.manager = biLoggerManager.manager;
    module2.exports.createBatchQueue = function() {
      return new BatchQueue();
    };
  }
});

// ../../node_modules/@wix/consent-policy-client-accessor/dist/es/src/consts.js
var METHODS, PROPS_ACCESS, ALLOW_ALL_POLICY;
var init_consts = __esm({
  "../../node_modules/@wix/consent-policy-client-accessor/dist/es/src/consts.js"() {
    "use strict";
    METHODS = {
      policy: "getCurrentConsentPolicy",
      header: "_getConsentPolicyHeader",
      isMethod: true
    };
    PROPS_ACCESS = {
      policy: "consentPolicy",
      header: "consentPolicyHeader",
      isMethod: false
    };
    ALLOW_ALL_POLICY = {
      essential: true,
      dataToThirdParty: true,
      advertising: true,
      functional: true,
      analytics: true
    };
  }
});

// ../../node_modules/@wix/consent-policy-client-accessor/dist/es/src/consent-policy-accessor.js
function getGlobalAccessor() {
  [getGlobalThis, getGlobal, getSelf, getWindow].forEach(function(globalAccessFunc) {
    try {
      if (!globalContext) {
        globalContext = globalAccessFunc();
      }
    } catch (e) {
    }
  });
  return globalContext;
}
function getWindow() {
  return window;
}
function getSelf() {
  return self;
}
function getGlobal() {
  return global;
}
function getGlobalThis() {
  return globalThis;
}
function getConsentPolicyManager(env3) {
  if (env3 === void 0) {
    env3 = void 0;
  }
  if (env3) {
    globalContext = env3;
  }
  if (!globalContext && !env3) {
    globalContext = getGlobalAccessor();
  }
  var consentPolicyManager;
  try {
    if (typeof globalContext.commonConfig === "object" && globalContext.commonConfig.consentPolicy) {
      consentPolicyManager = wrapConsentPolicy(globalContext.commonConfig, PROPS_ACCESS);
    }
    if (!consentPolicyManager && typeof globalContext.consentPolicyManager === "object") {
      consentPolicyManager = wrapConsentPolicy(globalContext.consentPolicyManager, METHODS);
    }
    if (!consentPolicyManager && typeof globalContext.Wix === "object" && typeof globalContext.Wix.Utils === "object" && typeof globalContext.Wix.Utils.getCurrentConsentPolicy === "function") {
      consentPolicyManager = wrapConsentPolicy(globalContext.Wix.Utils, METHODS);
    }
  } catch (e) {
  }
  return consentPolicyManager;
}
function wrapConsentPolicy(accessor, conf) {
  return {
    getCurrentConsentPolicy: function() {
      var extractor = accessor[conf.policy];
      return conf.isMethod ? extractor() : extractor;
    },
    _getConsentPolicyHeader: function() {
      var extractor = accessor[conf.header];
      return conf.isMethod ? extractor() : extractor;
    },
    deleteReference: !conf.isMethod
    // If it is an object pointer, there is no promise the same object will be updated.
  };
}
var globalContext;
var init_consent_policy_accessor = __esm({
  "../../node_modules/@wix/consent-policy-client-accessor/dist/es/src/consent-policy-accessor.js"() {
    "use strict";
    init_consts();
  }
});

// ../../node_modules/@wix/consent-policy-client-accessor/dist/es/src/index.js
var src_exports = {};
__export(src_exports, {
  ConsentPolicyAccessor: () => ConsentPolicyAccessor
});
var ConsentPolicyAccessor;
var init_src = __esm({
  "../../node_modules/@wix/consent-policy-client-accessor/dist/es/src/index.js"() {
    "use strict";
    init_consent_policy_accessor();
    init_consts();
    ConsentPolicyAccessor = /** @class */
    function() {
      function ConsentPolicyAccessor2(env3) {
        if (env3) {
          this.env = env3;
        }
        this.consentPolicyAccess = getConsentPolicyManager(this.env);
      }
      ConsentPolicyAccessor2.prototype.clearReference = function() {
        if (this.consentPolicyAccess && this.consentPolicyAccess.deleteReference) {
          this.consentPolicyAccess = void 0;
        }
      };
      ConsentPolicyAccessor2.prototype.getCurrentConsentPolicy = function() {
        if (!this.consentPolicyAccess) {
          this.consentPolicyAccess = getConsentPolicyManager(this.env);
        }
        var policyObj = this.consentPolicyAccess && this.consentPolicyAccess.getCurrentConsentPolicy() || ALLOW_ALL_POLICY;
        this.clearReference();
        return policyObj.policy ? policyObj.policy : policyObj;
      };
      ConsentPolicyAccessor2.prototype.getConsentPolicyHeader = function(valueOnly) {
        if (valueOnly === void 0) {
          valueOnly = false;
        }
        if (!this.consentPolicyAccess) {
          this.consentPolicyAccess = getConsentPolicyManager(this.env);
        }
        var consentPolicyHeaderObj = this.consentPolicyAccess && this.consentPolicyAccess._getConsentPolicyHeader() || {
          "consent-policy": ""
        };
        this.clearReference();
        return valueOnly ? consentPolicyHeaderObj["consent-policy"] : consentPolicyHeaderObj;
      };
      return ConsentPolicyAccessor2;
    }();
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/errors.js
var require_errors = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/errors.js"(exports2) {
    "use strict";
    var __extends = exports2 && exports2.__extends || /* @__PURE__ */ function() {
      var extendStatics = function(d, b) {
        extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
          d2.__proto__ = b2;
        } || function(d2, b2) {
          for (var p in b2) if (Object.prototype.hasOwnProperty.call(b2, p)) d2[p] = b2[p];
        };
        return extendStatics(d, b);
      };
      return function(d, b) {
        if (typeof b !== "function" && b !== null)
          throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() {
          this.constructor = d;
        }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
      };
    }();
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.APINotSupportedError = exports2.AssertionError = void 0;
    var AssertionError = (
      /** @class */
      function(_super) {
        __extends(AssertionError2, _super);
        function AssertionError2(message) {
          var _this = _super.call(this, message) || this;
          _this.name = _this.constructor.name;
          return _this;
        }
        return AssertionError2;
      }(Error)
    );
    exports2.AssertionError = AssertionError;
    var APINotSupportedError = (
      /** @class */
      function(_super) {
        __extends(APINotSupportedError2, _super);
        function APINotSupportedError2(message) {
          var _this = _super.call(this, message) || this;
          _this.name = _this.constructor.name;
          return _this;
        }
        return APINotSupportedError2;
      }(Error)
    );
    exports2.APINotSupportedError = APINotSupportedError;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/assert.js
var require_assert2 = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/assert.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.ok = exports2.object = exports2.defined = void 0;
    var errors_1 = require_errors();
    var defined = function(target, message) {
      if (target === void 0) {
        throw new errors_1.AssertionError(message);
      }
    };
    exports2.defined = defined;
    var object = function(target, message) {
      if (target !== void 0 && (typeof target !== "object" || Array.isArray(target) || target === null)) {
        throw new errors_1.AssertionError(message);
      }
    };
    exports2.object = object;
    var ok = function(target, message) {
      if (!target) {
        throw new errors_1.AssertionError(message);
      }
    };
    exports2.ok = ok;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/browser.js
var require_browser2 = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/browser.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.readCookie = exports2.sendBeacon = exports2.getWindowSize = exports2.getDesktopSize = exports2.now = void 0;
    function now2() {
      return typeof performance !== "undefined" && performance && performance.now ? performance.now() : -1;
    }
    exports2.now = now2;
    function getDesktopSize(window2) {
      var width = window2.screen && window2.screen.width || 0;
      var height = window2.screen && window2.screen.height || 0;
      return "".concat(width, "x").concat(height);
    }
    exports2.getDesktopSize = getDesktopSize;
    function getWindowSize(window2) {
      var width = 0;
      var height = 0;
      if (window2.innerWidth) {
        width = window2.innerWidth;
        height = window2.innerHeight;
      } else if (window2.document) {
        if (window2.document.documentElement && window2.document.documentElement.clientWidth) {
          width = window2.document.documentElement.clientWidth;
          height = window2.document.documentElement.clientHeight;
        } else if (window2.document.body && window2.document.body.clientWidth) {
          width = window2.document.body.clientWidth;
          height = window2.document.body.clientHeight;
        }
      }
      return "".concat(width, "x").concat(height);
    }
    exports2.getWindowSize = getWindowSize;
    function sendBeacon2(url, data) {
      if (typeof navigator === "undefined" || !navigator || !navigator.sendBeacon) {
        return false;
      }
      return navigator.sendBeacon(url, data);
    }
    exports2.sendBeacon = sendBeacon2;
    function readCookie2(cookieName) {
      if (typeof document === "undefined") {
        return null;
      }
      if (window.__ENABLE_COOKIE_READ_OPTIMIZATION__ && window.__BI_cookie_cache && window.__BI_cookie_cache[cookieName] !== void 0) {
        return window.__BI_cookie_cache[cookieName];
      }
      var cookies = document.cookie.split(";");
      for (var _i = 0, cookies_1 = cookies; _i < cookies_1.length; _i++) {
        var cookie = cookies_1[_i];
        var pair = cookie.split("=");
        var name_1 = pair[0];
        var value = pair[1];
        while (name_1[0] === " ") {
          name_1 = name_1.substr(1);
        }
        if (name_1 === cookieName) {
          if (window.__ENABLE_COOKIE_READ_OPTIMIZATION__) {
            if (!window.__BI_cookie_cache) {
              window.__BI_cookie_cache = {};
            }
            window.__BI_cookie_cache[cookieName] = value;
          }
          return value;
        }
      }
      return "";
    }
    exports2.readCookie = readCookie2;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/constants.js
var require_constants2 = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/constants.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.DefaultCommonConfig = exports2.BrandToHostMap = exports2.HeadlessHost = exports2.DefaultBrand = exports2.EventContextMap = exports2.BackofficeDomains = exports2.LoggerVersion = void 0;
    exports2.LoggerVersion = "2.0.985|C";
    exports2.BackofficeDomains = [".wix.com", ".editorx.com"];
    var EventContextMap;
    (function(EventContextMap2) {
      EventContextMap2["msid"] = "_msid";
      EventContextMap2["clientId"] = "_client_id";
      EventContextMap2["uuid"] = "_uuid";
      EventContextMap2["visitorId"] = "_visitorId";
      EventContextMap2["siteMemberId"] = "_siteMemberId";
      EventContextMap2["brandId"] = "_brandId";
      EventContextMap2["siteBranchId"] = "_siteBranchId";
      EventContextMap2["ms"] = "_ms";
      EventContextMap2["lv"] = "_lv";
      EventContextMap2["isHeadless"] = "_isHeadless";
      EventContextMap2["hostingPlatform"] = "_hostingPlatform";
    })(EventContextMap = exports2.EventContextMap || (exports2.EventContextMap = {}));
    exports2.DefaultBrand = "wix";
    exports2.HeadlessHost = "VIEWER_HEADLESS";
    exports2.BrandToHostMap = {
      wix: "wix",
      editorx: "editorx"
    };
    exports2.DefaultCommonConfig = {
      brand: exports2.DefaultBrand
    };
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/env.js
var require_env = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/env.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.getGlobal = exports2.getWindowIfTop = exports2.isBackoffice = exports2.isWebWorker = exports2.getHost = exports2.setHost = void 0;
    var constants_1 = require_constants2();
    var host;
    var hostByDomain;
    function setHost(_host) {
      host = _host;
    }
    exports2.setHost = setHost;
    function getHost(brand) {
      if (brand === void 0) {
        brand = constants_1.DefaultBrand;
      }
      if (host) {
        return host;
      }
      var hostByDomainOrBrand = "";
      if (typeof hostByDomain === "undefined") {
        hostByDomain = getGlobal2(function(window2) {
          if (!window2.location || !window2.location.hostname) {
            return null;
          }
          var _a = window2.location.hostname.match(/\.(wix|editorx)\.com$/) || [], domain = _a[1];
          return domain || null;
        }, null);
      }
      hostByDomainOrBrand = hostByDomain || constants_1.BrandToHostMap[brand] || constants_1.BrandToHostMap[constants_1.DefaultBrand];
      return "frog.".concat(hostByDomainOrBrand, ".com");
    }
    exports2.getHost = getHost;
    function isWebWorker() {
      return typeof WorkerGlobalScope !== "undefined" && self instanceof WorkerGlobalScope;
    }
    exports2.isWebWorker = isWebWorker;
    function isBackoffice() {
      return getWindowIfTop(function(_a) {
        var document2 = _a.document;
        return constants_1.BackofficeDomains.some(function(domain) {
          return document2.location.host.indexOf(domain) !== -1;
        });
      });
    }
    exports2.isBackoffice = isBackoffice;
    function getWindowIfTop(cb) {
      if (typeof window !== "undefined" && window) {
        try {
          return window.top === window.self ? cb ? cb(window) : window : null;
        } catch (e) {
          return null;
        }
      }
      return null;
    }
    exports2.getWindowIfTop = getWindowIfTop;
    function getGlobal2(cb, defaultValue) {
      if (defaultValue === void 0) {
        defaultValue = null;
      }
      if (typeof self !== "undefined" && self) {
        return cb(self) || defaultValue;
      }
      return defaultValue;
    }
    exports2.getGlobal = getGlobal2;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/utils.js
var require_utils3 = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/utils.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.guid = exports2.buildBiUrl = exports2.getCookies = void 0;
    var browser_1 = require_browser2();
    var env_1 = require_env();
    var cacheKillerCounter = 0;
    function getCookies(params) {
      return Object.keys(params).reduce(function(res, param) {
        var _a = typeof params[param] === "string" ? { name: params[param] } : params[param], name = _a.name, _b = _a.transform, transform = _b === void 0 ? function(x) {
          return x;
        } : _b;
        var value = transform((0, browser_1.readCookie)(name));
        if (value) {
          res[param] = value;
        }
        return res;
      }, {});
    }
    exports2.getCookies = getCookies;
    function buildBiUrl(_a, params, context) {
      var _b = _a.host, host = _b === void 0 ? "" : _b, _c = _a.endpoint, endpoint = _c === void 0 ? "" : _c;
      if (context === void 0) {
        context = {};
      }
      host = typeof host === "function" ? host() : host;
      host = host || (0, env_1.getHost)();
      endpoint = context.endpoint || endpoint;
      if (!context.useBatch) {
        params._ = "" + (/* @__PURE__ */ new Date()).getTime() + cacheKillerCounter++;
      }
      var pairs = context.useBatch ? [] : Object.keys(params).map(function(key) {
        return "".concat(encodeURIComponent(key), "=").concat(encodeURIComponent(params[key]));
      }, []);
      return ["//".concat(host, "/").concat(endpoint)].concat(pairs.length ? pairs.join("&") : []).join("?");
    }
    exports2.buildBiUrl = buildBiUrl;
    function guid() {
      return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function(c) {
        var r = Math.random() * 16 | 0;
        var v = c === "x" ? r : r & 3 | 8;
        return v.toString(16);
      });
    }
    exports2.guid = guid;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/types.js
var require_types2 = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/types.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.Errors = exports2.EventCategories = exports2.PublishMethods = void 0;
    var PublishMethods;
    (function(PublishMethods2) {
      PublishMethods2["Auto"] = "auto";
      PublishMethods2["PostMessage"] = "postMessage";
      PublishMethods2["Fetch"] = "fetch";
    })(PublishMethods = exports2.PublishMethods || (exports2.PublishMethods = {}));
    var EventCategories;
    (function(EventCategories2) {
      EventCategories2["Essential"] = "essential";
      EventCategories2["Functional"] = "functional";
      EventCategories2["Analytics"] = "analytics";
    })(EventCategories = exports2.EventCategories || (exports2.EventCategories = {}));
    var Errors;
    (function(Errors2) {
      Errors2[Errors2["Unsupported"] = 0] = "Unsupported";
    })(Errors = exports2.Errors || (exports2.Errors = {}));
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/transport.js
var require_transport = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/transport.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.postMessageTransport = exports2.xhrTransport = exports2.fetchTransport = exports2.pixelTransport = exports2.beaconTransport = void 0;
    var browser_1 = require_browser2();
    var errors_1 = require_errors();
    function beaconTransport(url, params, sendInBody) {
      if (sendInBody === void 0) {
        sendInBody = false;
      }
      return new Promise(function(resolve, reject) {
        return (0, browser_1.sendBeacon)(url, sendInBody ? JSON.stringify(params) : void 0) ? resolve() : reject(new Error("Transport Error: Cannot send bi using beacon"));
      });
    }
    exports2.beaconTransport = beaconTransport;
    function pixelTransport(url, image) {
      return new Promise(function(resolve, reject) {
        var biImage = image || new window.Image(0, 0);
        biImage.onload = function() {
          return resolve();
        };
        biImage.onerror = function() {
          return reject(new Error("Transport Error: Cannot send bi using pixel"));
        };
        biImage.src = url;
      });
    }
    exports2.pixelTransport = pixelTransport;
    function fetchTransport(url, params, method, fetchFunction) {
      if (method === void 0) {
        method = "GET";
      }
      if (typeof fetchFunction === "undefined") {
        if (typeof fetch === "undefined") {
          return Promise.reject(new errors_1.APINotSupportedError("fetch"));
        }
        fetchFunction = fetch;
      }
      var options = {
        method,
        credentials: "include"
      };
      if (method === "POST") {
        options.body = JSON.stringify(params);
      } else {
        options.keepalive = true;
      }
      return fetchFunction(url, options).then(function(response) {
        if (!response.ok) {
          throw Error("Transport Error: Cannot send bi using fetch. Status: ".concat(response.status));
        }
      });
    }
    exports2.fetchTransport = fetchTransport;
    function xhrTransport(url, params, method) {
      if (method === void 0) {
        method = "GET";
      }
      return new Promise(function(resolve, reject) {
        var xhr = new XMLHttpRequest();
        xhr.open(method, "".concat(location.protocol).concat(url));
        xhr.onload = resolve;
        xhr.onerror = function() {
          reject(new Error("Transport Error: Cannot send bi using xhr."));
        };
        xhr.withCredentials = true;
        if (method === "POST") {
          xhr.send(JSON.stringify(params));
        } else {
          xhr.send();
        }
      });
    }
    exports2.xhrTransport = xhrTransport;
    function postMessageTransport(message, postMessage) {
      if (postMessage === void 0) {
        postMessage = self.postMessage;
      }
      var messageArgs = [message];
      if (typeof WorkerGlobalScope === "undefined") {
        messageArgs.push("*");
      }
      return postMessage.apply(self, messageArgs);
    }
    exports2.postMessageTransport = postMessageTransport;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/publishers.js
var require_publishers = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/publishers.js"(exports2) {
    "use strict";
    var __assign = exports2 && exports2.__assign || function() {
      __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
        }
        return t;
      };
      return __assign.apply(this, arguments);
    };
    var __rest = exports2 && exports2.__rest || function(s, e) {
      var t = {};
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
      if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
          if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
            t[p[i]] = s[p[i]];
        }
      return t;
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.getTransformers = exports2.getPublisher = exports2.postMessagePublisher = exports2.xhrPublisher = exports2.fetchPublisher = exports2.beaconPublisher = exports2.imagePublisher = exports2.resolvePublisher = void 0;
    var utils_1 = require_utils3();
    var env_1 = require_env();
    var errors_1 = require_errors();
    var types_1 = require_types2();
    var transport_1 = require_transport();
    var getHttpMethod = function(context) {
      return context.useBatch ? "POST" : "GET";
    };
    var resolvePublisher = function(options, publishFunctions) {
      var publisherFactory;
      var publishFunction;
      if (options.publishMethod === types_1.PublishMethods.PostMessage) {
        publisherFactory = postMessagePublisher;
        publishFunction = publishFunctions && publishFunctions[types_1.PublishMethods.PostMessage];
      } else if (options.publishMethod === types_1.PublishMethods.Fetch) {
        publisherFactory = fetchPublisher;
        publishFunction = publishFunctions && publishFunctions[types_1.PublishMethods.Fetch];
      } else if ((0, env_1.isWebWorker)()) {
        publisherFactory = fetchPublisher;
      } else {
        publisherFactory = beaconPublisher;
      }
      return publisherFactory(options, publishFunction);
    };
    exports2.resolvePublisher = resolvePublisher;
    function imagePublisher(options) {
      if (options === void 0) {
        options = {};
      }
      return function image(params, context) {
        if (context === void 0) {
          context = {};
        }
        if (context.useBatch) {
          throw new errors_1.APINotSupportedError("Can't use image publisher to send batched events.");
        }
        var url = (0, utils_1.buildBiUrl)(options, params, context);
        return (0, transport_1.pixelTransport)(url, context.image);
      };
    }
    exports2.imagePublisher = imagePublisher;
    function beaconPublisher(options) {
      if (options === void 0) {
        options = {};
      }
      return function beacon(params, context) {
        if (context === void 0) {
          context = {};
        }
        var url = (0, utils_1.buildBiUrl)(options, params, context);
        return (0, transport_1.beaconTransport)(url, params, !!context.useBatch).catch(function() {
          var method = getHttpMethod(context);
          return context.useBatch ? (0, transport_1.fetchTransport)(url, params, method).catch(function(error) {
            return error instanceof errors_1.APINotSupportedError ? (0, transport_1.xhrTransport)(url, params, method) : Promise.reject(error);
          }) : (0, transport_1.pixelTransport)(url, context.image);
        });
      };
    }
    exports2.beaconPublisher = beaconPublisher;
    function fetchPublisher(options, fetchFunction) {
      if (options === void 0) {
        options = {};
      }
      return function fetch2(params, context) {
        if (context === void 0) {
          context = {};
        }
        var url = (0, utils_1.buildBiUrl)(options, params, context);
        var method = getHttpMethod(context);
        return (0, transport_1.fetchTransport)(url, params, method, fetchFunction).catch(function(error) {
          return error instanceof errors_1.APINotSupportedError ? (0, transport_1.xhrTransport)(url, params, method) : Promise.reject(error);
        });
      };
    }
    exports2.fetchPublisher = fetchPublisher;
    function xhrPublisher(options) {
      if (options === void 0) {
        options = {};
      }
      return function xhr(params, context) {
        if (context === void 0) {
          context = {};
        }
        var url = (0, utils_1.buildBiUrl)(options, params, context);
        var method = getHttpMethod(context);
        return (0, transport_1.xhrTransport)(url, params, method);
      };
    }
    exports2.xhrPublisher = xhrPublisher;
    function postMessagePublisher(options, publishFunction) {
      if (options === void 0) {
        options = {};
      }
      return function postMessage(params) {
        return (0, transport_1.postMessageTransport)(params, publishFunction);
      };
    }
    exports2.postMessagePublisher = postMessagePublisher;
    function getPublisher(options, publishFunctions) {
      return (0, exports2.resolvePublisher)(options, publishFunctions);
    }
    exports2.getPublisher = getPublisher;
    var getTransformers = function(factoryOptions, transformer) {
      var _eventTransformer, _payloadTransformer;
      if (transformer) {
        if (typeof transformer !== "function" && transformer.postMessage && factoryOptions.publishMethod === types_1.PublishMethods.PostMessage) {
          _payloadTransformer = transformer.postMessage;
        } else if (typeof transformer === "function") {
          _eventTransformer = transformer;
        }
      }
      var eventTransformer = _eventTransformer;
      var payloadTransformer = function(params, publisherContext) {
        if (publisherContext.useBatch) {
          var getEndpoint_1 = function(context) {
            return (context === null || context === void 0 ? void 0 : context.endpoint) || publisherContext.endpoint || factoryOptions.endpoint;
          };
          params.e = params.e.map(function(e) {
            var context = e.context, event = __rest(e, ["context"]);
            var res = __assign(__assign({}, event), { f: __assign(__assign({}, event.f), { _rp: getEndpoint_1(context) }) });
            return res;
          });
        }
        return _payloadTransformer ? _payloadTransformer(params) : params;
      };
      return { eventTransformer, payloadTransformer };
    };
    exports2.getTransformers = getTransformers;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/common-config.js
var require_common_config = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/common-config.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.getWixHost = exports2.getBranchId = exports2.getBrand = exports2.getCommonConfigValue = void 0;
    var env_1 = require_env();
    var constants_1 = require_constants2();
    var resolveCommonConfig = function() {
      return (0, env_1.getGlobal)(function(g) {
        return g.Wix && g.Wix.Utils && g.Wix.Utils.commonConfig || g.commonConfig;
      });
    };
    var getCommonConfigValue = function(key, commonConfig2) {
      if (commonConfig2 === void 0) {
        commonConfig2 = resolveCommonConfig();
      }
      commonConfig2 = commonConfig2 && typeof commonConfig2.getAll === "function" ? commonConfig2.getAll() : commonConfig2;
      if (!commonConfig2 || typeof commonConfig2[key] === "undefined") {
        return constants_1.DefaultCommonConfig[key];
      }
      return commonConfig2[key];
    };
    exports2.getCommonConfigValue = getCommonConfigValue;
    var getBrand = function(commonConfigGetter) {
      return (0, exports2.getCommonConfigValue)("brand", commonConfigGetter());
    };
    exports2.getBrand = getBrand;
    var getBranchId = function(commonConfigGetter) {
      return (0, exports2.getCommonConfigValue)("branchId", commonConfigGetter());
    };
    exports2.getBranchId = getBranchId;
    var getWixHost = function(commonConfigGetter) {
      return (0, exports2.getCommonConfigValue)("host", commonConfigGetter());
    };
    exports2.getWixHost = getWixHost;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/event-defaults.js
var require_event_defaults = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/event-defaults.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.getCommonDefaults = exports2.getUserDefaults = exports2.transformDefaults = void 0;
    var constants_1 = require_constants2();
    var browser_1 = require_browser2();
    var common_config_1 = require_common_config();
    var utils_1 = require_utils3();
    var transformDefaults = function(context) {
      return Object.keys(context).reduce(function(res, key) {
        var mappedKey = constants_1.EventContextMap[key] || key;
        res[mappedKey] = context[key];
        return res;
      }, {});
    };
    exports2.transformDefaults = transformDefaults;
    var getUserDefaults = function() {
      return (0, utils_1.getCookies)({
        clientId: "_wixCIDX",
        uuid: {
          name: "_wixUIDX",
          transform: function(value) {
            return typeof value === "string" && value.split("|")[1];
          }
        }
      });
    };
    exports2.getUserDefaults = getUserDefaults;
    var getCommonDefaults = function(commonConfigGetter, consentPolicyGetter) {
      return (0, exports2.transformDefaults)({
        brandId: function() {
          return (0, common_config_1.getBrand)(commonConfigGetter);
        },
        siteBranchId: function() {
          return (0, common_config_1.getBranchId)(commonConfigGetter);
        },
        ms: function() {
          return Math.round((0, browser_1.now)());
        },
        isHeadless: function() {
          return resolveIsHeadless(commonConfigGetter);
        },
        hostingPlatform: function() {
          return (0, common_config_1.getWixHost)(commonConfigGetter);
        },
        lv: constants_1.LoggerVersion
      });
    };
    exports2.getCommonDefaults = getCommonDefaults;
    var resolveIsHeadless = function(commonConfigGetter) {
      return (0, common_config_1.getWixHost)(commonConfigGetter) === constants_1.HeadlessHost || void 0;
    };
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/logger.js
var require_logger = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/logger.js"(exports2) {
    "use strict";
    var __assign = exports2 && exports2.__assign || function() {
      __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
        }
        return t;
      };
      return __assign.apply(this, arguments);
    };
    var __createBinding = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __setModuleDefault = exports2 && exports2.__setModuleDefault || (Object.create ? function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    } : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports2 && exports2.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
      }
      __setModuleDefault(result, mod);
      return result;
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.BiLoggerClientFactory = exports2.createBatchQueue = exports2.manager = exports2.factory = exports2.Factory = exports2.setHost = exports2.EventCategories = exports2.PublishMethods = void 0;
    var wix_bi_logger_client_1 = require_dist4();
    var consent_policy_client_accessor_1 = (init_src(), __toCommonJS(src_exports));
    var assert2 = __importStar(require_assert2());
    var publishers_1 = require_publishers();
    var common_config_1 = require_common_config();
    var env_1 = require_env();
    var types_1 = require_types2();
    var event_defaults_1 = require_event_defaults();
    var types_2 = require_types2();
    Object.defineProperty(exports2, "PublishMethods", { enumerable: true, get: function() {
      return types_2.PublishMethods;
    } });
    Object.defineProperty(exports2, "EventCategories", { enumerable: true, get: function() {
      return types_2.EventCategories;
    } });
    var env_2 = require_env();
    Object.defineProperty(exports2, "setHost", { enumerable: true, get: function() {
      return env_2.setHost;
    } });
    var Factory = (
      /** @class */
      function() {
        function Factory2(options) {
          if (options === void 0) {
            options = {};
          }
          this.options = options;
          this.commonConfigGetter = function() {
            return void 0;
          };
          this.initialized = false;
          assert2.ok(!options.publishMethod || Object.keys(types_1.PublishMethods).map(function(key) {
            return types_1.PublishMethods[key];
          }).indexOf(options.publishMethod) !== -1, 'Unsupported publish method "'.concat(options.publishMethod, '"'));
          Factory2.consentPolicyAccessor = Factory2.consentPolicyAccessor || new consent_policy_client_accessor_1.ConsentPolicyAccessor();
          this.loggerClientFactory = (0, wix_bi_logger_client_1.factory)();
        }
        Factory2.prototype.initFactory = function() {
          var _this = this;
          if (this.initialized) {
            return this.loggerClientFactory;
          }
          this.initialized = true;
          this.updateDefaults((0, event_defaults_1.getCommonDefaults)(this.commonConfigGetter, Factory2.consentPolicyAccessor));
          this.withUserContext((0, event_defaults_1.getUserDefaults)());
          var options = __assign(__assign({}, this.options), { host: this.options.host || function() {
            return (0, env_1.getHost)((0, common_config_1.getBrand)(_this.commonConfigGetter));
          } });
          var publisher = (0, publishers_1.getPublisher)(options, this.publishFunctions);
          var _a = (0, publishers_1.getTransformers)(options, this.transformer), eventTransformer = _a.eventTransformer, payloadTransformer = _a.payloadTransformer;
          var baseFactory = this.loggerClientFactory.addPublisher(publisher).withConsentPolicyGetter(function() {
            return Factory2.consentPolicyAccessor.getCurrentConsentPolicy();
          });
          if (eventTransformer) {
            baseFactory.withEventTransformer(eventTransformer);
          }
          if (payloadTransformer) {
            baseFactory.withPayloadTransformer(payloadTransformer);
          }
          return baseFactory;
        };
        Factory2.prototype.withTransformer = function(transformer) {
          assert2.defined(transformer, "Transformer must be provided");
          assert2.ok(typeof transformer === "function" || transformer && typeof transformer[types_1.PublishMethods.PostMessage] === "function", "Valid transformer must be provided");
          this.transformer = transformer;
          return this;
        };
        Factory2.prototype.withPublishFunction = function(publishFunctions) {
          assert2.defined(publishFunctions, "Publish functions object must be provided");
          assert2.ok(this.options.publishMethod && this.options.publishMethod !== types_1.PublishMethods.Auto, "Publish function can be used only when using a custom publish method");
          assert2.ok(publishFunctions && typeof publishFunctions[this.options.publishMethod] === "function", "Valid publish function must be provided");
          this.publishFunctions = publishFunctions;
          return this;
        };
        Factory2.prototype.withUserContext = function(context) {
          assert2.defined(context, "User context object must be provided");
          this.updateDefaults((0, event_defaults_1.transformDefaults)(context));
          return this;
        };
        Factory2.prototype.withUoUContext = function(context) {
          assert2.defined(context, "UoU context object must be provided");
          this.updateDefaults((0, event_defaults_1.transformDefaults)(context));
          return this;
        };
        Factory2.prototype.withNonEssentialContext = function(context) {
          assert2.defined(context, "Non-essential context object must be provided");
          this.loggerClientFactory.updateNonEssentialDefaults((0, event_defaults_1.transformDefaults)(context));
          return this;
        };
        Factory2.prototype.withCommonConfigGetter = function(commonConfigGetter) {
          assert2.defined(commonConfigGetter, "Common config getter must be provided");
          assert2.ok(typeof commonConfigGetter === "function", "Common config getter must be a function");
          this.commonConfigGetter = commonConfigGetter;
          return this;
        };
        Factory2.prototype.updateDefaults = function(params) {
          this.loggerClientFactory.updateDefaults(params);
          return this;
        };
        Factory2.prototype.setMuted = function(isMuted) {
          this.loggerClientFactory.setMuted(isMuted);
          return this;
        };
        Factory2.prototype.setEvents = function(events) {
          this.loggerClientFactory.setEvents(events);
          return this;
        };
        Factory2.prototype.setGlobalBatchQueue = function(batchQueue) {
          this.loggerClientFactory.setGlobalBatchQueue(batchQueue);
          return this;
        };
        Factory2.prototype.onError = function(handler) {
          this.loggerClientFactory.setPublisherFailHandler(handler);
          return this;
        };
        Factory2.prototype.logger = function(options) {
          if (options === void 0) {
            options = {};
          }
          var _a = this.options, endpoint = _a.endpoint, useBatch = _a.useBatch;
          var logger = this.initFactory().logger(__assign({ endpoint, useBatch }, options));
          return logger;
        };
        return Factory2;
      }()
    );
    exports2.Factory = Factory;
    var factory2 = function(options) {
      if (options === void 0) {
        options = {};
      }
      return new Factory(options);
    };
    exports2.factory = factory2;
    exports2.manager = wix_bi_logger_client_1.manager;
    exports2.createBatchQueue = wix_bi_logger_client_1.createBatchQueue;
    exports2.BiLoggerClientFactory = wix_bi_logger_client_1.BiLoggerFactory;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/navigator.js
var require_navigator = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/navigator.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.listen = void 0;
    var env_1 = require_env();
    function hook(obj, fnName, cbHook) {
      if (!obj || !obj[fnName]) {
        return;
      }
      var original = obj[fnName];
      obj[fnName] = function() {
        cbHook.apply(null, arguments);
        return original.apply(obj, arguments);
      };
    }
    function onLoad(cb) {
      setTimeout(function() {
        return (0, env_1.getWindowIfTop)(function(window2) {
          return cb(window2.location.href);
        });
      });
    }
    function onPushState(cb) {
      (0, env_1.getWindowIfTop)(function(_a) {
        var history = _a.history;
        return ["pushState", "replaceState"].forEach(function(fnName) {
          hook(history, fnName, function(_, __, url) {
            return cb(url);
          });
        });
      });
    }
    function onPopState(cb) {
      (0, env_1.getWindowIfTop)(function(window2) {
        return window2.addEventListener("popstate", function() {
          return cb(window2.location.href);
        });
      });
    }
    function listen(cb) {
      (0, env_1.getWindowIfTop)(function(window2) {
        var fromUrl = window2.document.referrer;
        [onLoad, onPushState, onPopState].forEach(function(event) {
          return event(function(toUrl) {
            cb(fromUrl, toUrl);
            fromUrl = toUrl;
          });
        });
      });
    }
    exports2.listen = listen;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/lib/tracker.js
var require_tracker = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/lib/tracker.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.track = void 0;
    var utils_1 = require_utils3();
    var navigator_1 = require_navigator();
    var env_1 = require_env();
    var browser_1 = require_browser2();
    var IS_TRACKING = "__isTrackingPageViews__";
    var ENDPOINT = "p";
    var SOURCE = 19;
    var EVID = 3;
    function shouldTrack(force) {
      return force || !(0, env_1.isWebWorker)() && (0, env_1.isBackoffice)() && (0, env_1.getWindowIfTop)(function(window2) {
        return !window2[IS_TRACKING];
      });
    }
    function setIsTracking() {
      return (0, env_1.getWindowIfTop)(function(window2) {
        return window2[IS_TRACKING] = true;
      });
    }
    function getLogger(loggerFactory) {
      return loggerFactory({ endpoint: ENDPOINT }).updateDefaults({ src: SOURCE, evid: EVID, vsi: (0, utils_1.guid)() }).logger();
    }
    function trackBrowsingSession(loggerFactory) {
      var logger = getLogger(loggerFactory);
      var firstInSession = 1;
      (0, navigator_1.listen)(function(fromUrl, toUrl) {
        var _a = (0, env_1.getWindowIfTop)(function(window2) {
          return {
            sr: (0, browser_1.getDesktopSize)(window2),
            wr: (0, browser_1.getWindowSize)(window2)
          };
        }), sr = _a.sr, wr = _a.wr;
        logger.log({ from: fromUrl, to: toUrl, fis: firstInSession, sr, wr });
        firstInSession = 0;
      });
    }
    function track(loggerFactory, options) {
      if (options === void 0) {
        options = { force: false };
      }
      if (shouldTrack(options.force)) {
        setIsTracking();
        trackBrowsingSession(loggerFactory);
      }
    }
    exports2.track = track;
  }
});

// ../../node_modules/@wix/web-bi-logger/dist/src/tracking-logger.js
var require_tracking_logger = __commonJS({
  "../../node_modules/@wix/web-bi-logger/dist/src/tracking-logger.js"(exports2) {
    "use strict";
    var __createBinding = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __setModuleDefault = exports2 && exports2.__setModuleDefault || (Object.create ? function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    } : function(o, v) {
      o["default"] = v;
    });
    var __importStar = exports2 && exports2.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) {
        for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
      }
      __setModuleDefault(result, mod);
      return result;
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.BiLoggerClientFactory = exports2.EventCategories = exports2.createBatchQueue = exports2.manager = exports2.setHost = exports2.factory = void 0;
    var wix_bi_logger_client_1 = require_dist4();
    var tracker = __importStar(require_tracker());
    var logger = __importStar(require_logger());
    var factory2 = function(options) {
      if (options === void 0) {
        options = {};
      }
      var trackSession = options.trackSession || typeof options.trackSession === "undefined";
      if (trackSession) {
        tracker.track(logger.factory, { force: trackSession === "force" });
      }
      return logger.factory(options);
    };
    exports2.factory = factory2;
    exports2.setHost = logger.setHost;
    exports2.manager = wix_bi_logger_client_1.manager;
    exports2.createBatchQueue = wix_bi_logger_client_1.createBatchQueue;
    exports2.EventCategories = logger.EventCategories;
    exports2.BiLoggerClientFactory = logger.BiLoggerClientFactory;
    exports2.default = {
      factory: exports2.factory,
      setHost: logger.setHost,
      manager: wix_bi_logger_client_1.manager,
      createBatchQueue: wix_bi_logger_client_1.createBatchQueue,
      EventCategories: logger.EventCategories,
      BiLoggerClientFactory: logger.BiLoggerClientFactory
    };
  }
});

// ../../node_modules/@babel/runtime/helpers/typeof.js
var require_typeof = __commonJS({
  "../../node_modules/@babel/runtime/helpers/typeof.js"(exports2, module2) {
    "use strict";
    function _typeof(o) {
      "@babel/helpers - typeof";
      return module2.exports = _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
        return typeof o2;
      } : function(o2) {
        return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
      }, module2.exports.__esModule = true, module2.exports["default"] = module2.exports, _typeof(o);
    }
    module2.exports = _typeof, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});

// ../../node_modules/@babel/runtime/helpers/toPrimitive.js
var require_toPrimitive = __commonJS({
  "../../node_modules/@babel/runtime/helpers/toPrimitive.js"(exports2, module2) {
    "use strict";
    var _typeof = require_typeof()["default"];
    function toPrimitive(t, r) {
      if ("object" != _typeof(t) || !t) return t;
      var e = t[Symbol.toPrimitive];
      if (void 0 !== e) {
        var i = e.call(t, r || "default");
        if ("object" != _typeof(i)) return i;
        throw new TypeError("@@toPrimitive must return a primitive value.");
      }
      return ("string" === r ? String : Number)(t);
    }
    module2.exports = toPrimitive, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});

// ../../node_modules/@babel/runtime/helpers/toPropertyKey.js
var require_toPropertyKey = __commonJS({
  "../../node_modules/@babel/runtime/helpers/toPropertyKey.js"(exports2, module2) {
    "use strict";
    var _typeof = require_typeof()["default"];
    var toPrimitive = require_toPrimitive();
    function toPropertyKey(t) {
      var i = toPrimitive(t, "string");
      return "symbol" == _typeof(i) ? i : i + "";
    }
    module2.exports = toPropertyKey, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});

// ../../node_modules/@babel/runtime/helpers/defineProperty.js
var require_defineProperty = __commonJS({
  "../../node_modules/@babel/runtime/helpers/defineProperty.js"(exports2, module2) {
    "use strict";
    var toPropertyKey = require_toPropertyKey();
    function _defineProperty10(e, r, t) {
      return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
        value: t,
        enumerable: true,
        configurable: true,
        writable: true
      }) : e[r] = t, e;
    }
    module2.exports = _defineProperty10, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ar.json
var require_common_errors_ar = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ar.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_bg.json
var require_common_errors_bg = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_bg.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ca.json
var require_common_errors_ca = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ca.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_cs.json
var require_common_errors_cs = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_cs.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Nem\xE1te opr\xE1vn\u011Bn\xED prov\xE9st tuto akci. Po\u017E\xE1dejte o\xA0p\u0159\xEDstup vlastn\xEDka webu.",
      "errorHandler.uncommon-error.NotImplemented": "Tato akce nen\xED moment\xE1ln\u011B dostupn\xE1.",
      "errorHandler.common-error.conflict": "Zm\u011Bny nelze ulo\u017Eit, proto\u017Ee tato str\xE1nka byla aktualizov\xE1na n\u011Bkde jinde. Poznamenejte si zm\u011Bny, obnovte str\xE1nku a zkuste ji upravit znovu.",
      "errorHandler.common-error.permission-denied.app": "Pro tuto akci nem\xE1te opr\xE1vn\u011Bn\xED. Po\u017E\xE1dejte o\xA0p\u0159\xEDstup vlastn\xEDka aplikace.",
      "errorHandler.common-error.permission-denied.workspace": "Pro tuto akci nem\xE1te opr\xE1vn\u011Bn\xED. Po\u017E\xE1dejte o\xA0p\u0159\xEDstup vlastn\xEDka pracovn\xEDho prostoru.",
      "errorHandler.common-error.CheckStatus.cta": "Str\xE1nka se stavem kontroly",
      "errorHandler.common-error.refresh.cta": "Obnovit",
      "errorHandler.common-error.noConnection": "Vypad\xE1 to, \u017Ee jste offline. Zkontrolujte p\u0159ipojen\xED a zkuste to znovu.",
      "errorHandler.common-error.resource-exhausted": "P\u0159\xEDli\u0161 mnoho akc\xED bylo provedeno p\u0159\xEDli\u0161 rychle. Chv\xEDli po\u010Dkejte a zkuste to znovu.",
      "errorHandler.common-error.unauthenticated.cta": "P\u0159ihl\xE1sit se",
      "errorHandler.common-error.goBack.cta": "Vr\xE1tit se",
      "errorHandler.common-error.not-found": "Vypad\xE1 to, \u017Ee tato polo\u017Eka ji\u017E neexistuje.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Ov\u011B\u0159it",
      "errorHandler.common-error.tryAgain.cta": "Zkusit znovu",
      "errorHandler.requestID.tooltip.cta": "Kop\xEDrovat ID",
      "errorHandler.common-error.permission-denied": "Nem\xE1te pot\u0159ebn\xE1 opr\xE1vn\u011Bn\xED. Po\u017E\xE1dejte o\xA0p\u0159\xEDstup vlastn\xEDka.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "M\xE1me technick\xE9 pot\xED\u017Ee. Zkuste to znovu nebo obnovte str\xE1nku.",
      "errorHandler.common-error.unauthenticated": "Jste odhl\xE1\u0161eni. Pro pokra\u010Dov\xE1n\xED se p\u0159ihlaste.",
      "errorHandler.common-error.networkError": "Objevily se pot\xED\u017Ee p\u0159i p\u0159ipojov\xE1n\xED k va\u0161\xED s\xEDti. Zkontrolujte p\u0159ipojen\xED a zkuste to znovu.",
      "errorHandler.common-error.technicalIssue": "M\xE1me technick\xE9 pot\xED\u017Ee. Zkuste to znovu nebo obnovte str\xE1nku.",
      "errorHandler.common-error.captchaRequired": "Pokud chcete pokra\u010Dovat, bude pot\u0159eba ov\u011B\u0159it, zda jste \u010Dlov\u011Bk.",
      "errorHandler.common-error.technicalIssue.generic": "M\xE1me technick\xE9 pot\xED\u017Ee. Zkuste to znovu nebo obnovte str\xE1nku.",
      "errorHandler.common-error.validationBasic": "N\u011Bkter\xE1 pole obsahuj\xED neplatn\xE9 nebo chyb\u011Bj\xEDc\xED \xFAdaje.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Zkop\xEDrov\xE1no!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_da.json
var require_common_errors_da = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_da.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Du har ikke tilladelse til at g\xF8re dette. Anmod om adgang fra hjemmesideejeren.",
      "errorHandler.uncommon-error.NotImplemented": "Denne handling er ikke tilg\xE6ngelig lige nu.",
      "errorHandler.common-error.conflict": "\xC6ndringerne kan ikke gemmes, fordi denne side er blevet \xE6ndret et andet sted. Noter dine \xE6ndringer, genindl\xE6s, og pr\xF8v derefter at redigere igen.",
      "errorHandler.common-error.permission-denied.app": "Du har ikke tilladelse til at udf\xF8re denne handling. Anmod om adgang fra app-ejeren.",
      "errorHandler.common-error.permission-denied.workspace": "Du har ikke tilladelse til at udf\xF8re denne handling. Anmod om adgang fra ejer af workspace.",
      "errorHandler.common-error.CheckStatus.cta": "Tjek statussiden",
      "errorHandler.common-error.refresh.cta": "Genindl\xE6s",
      "errorHandler.common-error.noConnection": "Det ser ud til, at du er offline. Tjek forbindelsen, og pr\xF8v igen.",
      "errorHandler.common-error.resource-exhausted": "For mange handlinger blev udf\xF8rt for hurtigt. Vent et \xF8jeblik og pr\xF8v igen.",
      "errorHandler.common-error.unauthenticated.cta": "Log ind",
      "errorHandler.common-error.goBack.cta": "G\xE5 tilbage",
      "errorHandler.common-error.not-found": "Det ser ud til, at dette ikke eksisterer l\xE6ngere.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Bekr\xE6ft",
      "errorHandler.common-error.tryAgain.cta": "Pr\xF8v igen",
      "errorHandler.requestID.tooltip.cta": "Kopi\xE9r ID",
      "errorHandler.common-error.permission-denied": "Du har ikke de n\xF8dvendige tilladelser. Anmod om adgang fra ejeren.",
      "errorHandler.requestID.tooltip.description": "Hvis du rapporterer denne fejl til kundeservice, skal du dele dette anmodnings-ID, s\xE5 de kan unders\xF8ge sagen.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Der er opst\xE5et et teknisk problem hos os. Pr\xF8v igen, eller opdater.",
      "errorHandler.common-error.unauthenticated": "Du er logget ud. Log ind igen for at forts\xE6tte.",
      "errorHandler.common-error.networkError": "Der opstod et problem med at oprette forbindelse til dit netv\xE6rk. Tjek forbindelsen, og pr\xF8v igen.",
      "errorHandler.common-error.technicalIssue": "Der er opst\xE5et et teknisk problem hos os. Pr\xF8v igen, eller opdater.",
      "errorHandler.common-error.captchaRequired": "Bekr\xE6ft, at du er et menneske for at forts\xE6tte.",
      "errorHandler.common-error.technicalIssue.generic": "Der er opst\xE5et et teknisk problem hos os. Pr\xF8v igen, eller opdater.",
      "errorHandler.common-error.validationBasic": "Nogle felter har ugyldige eller manglende oplysninger.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Kopieret!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_de.json
var require_common_errors_de = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_de.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Du bist nicht zu dieser Aktion berechtigt. Bitte den Website-Eigent\xFCmer um Zugriff.",
      "errorHandler.uncommon-error.NotImplemented": "Diese Aktion ist zurzeit nicht verf\xFCgbar.",
      "errorHandler.common-error.conflict": "Die \xC4nderungen konnten nicht gespeichert werden, da die Seite an anderer Stelle aktualisiert wurde. Mache Notiz von deinen \xC4nderungen, aktualisiere die Seite und versuche erneut, sie zu bearbeiten.",
      "errorHandler.common-error.permission-denied.app": "Du bist zu dieser Aktion nicht berechtigt. Bitte den App-Eigent\xFCmer um Zugriff.",
      "errorHandler.common-error.permission-denied.workspace": "Du bist zu dieser Aktion nicht berechtigt. Bitte den Workspace-Eigent\xFCmer um Zugriff.",
      "errorHandler.common-error.CheckStatus.cta": "Statusseite pr\xFCfen",
      "errorHandler.common-error.refresh.cta": "Aktualisieren",
      "errorHandler.common-error.noConnection": "Du bist offline. \xDCberpr\xFCfe deine Verbindung und versuche es erneut.",
      "errorHandler.common-error.resource-exhausted": "Zu viele Aktionen wurden zu schnell durchgef\xFChrt. Warte kurz und versuche es erneut.",
      "errorHandler.common-error.unauthenticated.cta": "Anmelden",
      "errorHandler.common-error.goBack.cta": "Zur\xFCck",
      "errorHandler.common-error.not-found": "Dieses Element gibt es nicht mehr.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Best\xE4tigen",
      "errorHandler.common-error.tryAgain.cta": "Erneut versuchen",
      "errorHandler.requestID.tooltip.cta": "ID kopieren",
      "errorHandler.common-error.permission-denied": "Du hast nicht die erforderlichen Berechtigungen. Bitte den Eigent\xFCmer um Zugriff.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Es gab ein technisches Problem. Versuche es erneut oder aktualisiere die Seite.",
      "errorHandler.common-error.unauthenticated": "Du bist abgemeldet. Melde dich an, um fortzufahren.",
      "errorHandler.common-error.networkError": "Es gab ein Netzwerkproblem. \xDCberpr\xFCfe deine Verbindung und versuche es erneut.",
      "errorHandler.common-error.technicalIssue": "Es gab ein technisches Problem. Versuche es erneut oder aktualisiere die Seite.",
      "errorHandler.common-error.captchaRequired": "Um fortzufahren, best\xE4tige, dass du kein Roboter bist.",
      "errorHandler.common-error.technicalIssue.generic": "Es gab ein technisches Problem. Versuche es erneut oder aktualisiere die Seite.",
      "errorHandler.common-error.validationBasic": "Einige Felder enthalten ung\xFCltige oder fehlende Informationen.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Kopiert"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_el.json
var require_common_errors_el = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_el.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "\u0391\u03BD\u03B1\u03BD\u03AD\u03C9\u03C3\u03B7",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "\u03A3\u03CD\u03BD\u03B4\u03B5\u03C3\u03B7",
      "errorHandler.common-error.goBack.cta": "\u03A0\u03AF\u03C3\u03C9",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "\u03B5\u03C0\u03B1\u03BB\u03AE\u03B8\u03B5\u03C5\u03C3\u03B7",
      "errorHandler.common-error.tryAgain.cta": "\u03A0\u03C1\u03BF\u03C3\u03C0\u03B1\u03B8\u03AE\u03C3\u03C4\u03B5 \u03BE\u03B1\u03BD\u03AC",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_en.json
var require_common_errors_en = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_en.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.conflict": "Can\u2019t save changes because this page was updated somewhere else. Take note of your changes, then refresh and try editing again.",
      "errorHandler.common-error.resource-exhausted": "Too many actions were done too quickly. Wait a minute and try again.",
      "errorHandler.common-error.not-found": "It looks like this doesn't exist anymore.",
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Request access from the site owner.",
      "errorHandler.common-error.unauthenticated": "You\u2019re logged out. Log back in to continue.",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.common-error.technicalIssue": "There was a technical issue on our end. Try again or refresh.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Request access from the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Request access from the workspace owner.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.permission-denied": "You don't have the necessary permissions. Request access from the owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.uncommon-error.NotImplemented": "This action isn't available right now.",
      "errorHandler.common-error.technicalIssue-unmappedError": "There was a technical issue on our end. Try again or refresh.",
      "errorHandler.common-error.technicalIssue.generic": "There was a technical issue on our end. Try again or refresh.",
      "errorHandler.requestID.tooltip.description": "If you report this error to Customer Care, share this request ID so they can investigate.",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_es.json
var require_common_errors_es = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_es.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "No tienes permiso para realizar esta acci\xF3n. Solicita acceso al propietario del sitio.",
      "errorHandler.uncommon-error.NotImplemented": "Esta acci\xF3n no est\xE1 disponible en este momento.",
      "errorHandler.common-error.conflict": "No se pueden guardar los cambios porque esta p\xE1gina se actualiz\xF3 en otro lugar. Ten en cuenta los cambios, actualiza e intenta editar nuevamente.",
      "errorHandler.common-error.permission-denied.app": "No tienes permiso para realizar esta acci\xF3n. Solicita acceso al propietario de la app.",
      "errorHandler.common-error.permission-denied.workspace": "No tienes permiso para realizar esta acci\xF3n. Solicita acceso al propietario del espacio de trabajo.",
      "errorHandler.common-error.CheckStatus.cta": "Verificar estado de p\xE1gina",
      "errorHandler.common-error.refresh.cta": "Actualizar",
      "errorHandler.common-error.noConnection": "Parece que est\xE1s sin conexi\xF3n. Comprueba tu conexi\xF3n a Internet y vuelve a intentarlo.",
      "errorHandler.common-error.resource-exhausted": "Se realizaron demasiadas acciones en muy poco tiempo. Espera un minuto y vuelve a intentarlo.",
      "errorHandler.common-error.unauthenticated.cta": "Iniciar sesi\xF3n",
      "errorHandler.common-error.goBack.cta": "Volver",
      "errorHandler.common-error.not-found": "Parece que esto ya no existe.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verificar",
      "errorHandler.common-error.tryAgain.cta": "Reintentar",
      "errorHandler.requestID.tooltip.cta": "Copiar ID",
      "errorHandler.common-error.permission-denied": "No tienes los permisos necesarios. Solicita acceso al propietario.",
      "errorHandler.requestID.tooltip.description": "Si informas de este error a Atenci\xF3n al Cliente, comparte este ID de solicitud para que puedan investigarlo.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Hubo un problema t\xE9cnico de nuestra parte. Actualiza o int\xE9ntalo de nuevo.",
      "errorHandler.common-error.unauthenticated": "Se ha cerrado la sesi\xF3n de tu cuenta. Vuelve a iniciar sesi\xF3n para continuar.",
      "errorHandler.common-error.networkError": "Hubo un problema al conectarse a la red. Comprueba tu conexi\xF3n a Internet y vuelve a intentarlo.",
      "errorHandler.common-error.technicalIssue": "Hubo un problema t\xE9cnico de nuestra parte. Actualiza o int\xE9ntalo de nuevo.",
      "errorHandler.common-error.captchaRequired": "Para continuar, verifica que eres humano.",
      "errorHandler.common-error.technicalIssue.generic": "Hubo un problema t\xE9cnico de nuestra parte. Actualiza o int\xE9ntalo de nuevo.",
      "errorHandler.common-error.validationBasic": "Algunos campos no contienen informaci\xF3n v\xE1lida o falta informaci\xF3n.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copiado"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_fi.json
var require_common_errors_fi = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_fi.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_fr.json
var require_common_errors_fr = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_fr.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Vous n'avez pas les autorisations n\xE9cessaires. Demandez l'acc\xE8s au propri\xE9taire du site.",
      "errorHandler.uncommon-error.NotImplemented": "Cette action n'est pas disponible pour le moment.",
      "errorHandler.common-error.conflict": "Impossible d'enregistrer les modifications car cette page a \xE9t\xE9 mise \xE0 jour ailleurs. Prenez note de vos modifications, puis actualisez la page et r\xE9essayez de la modifier.",
      "errorHandler.common-error.permission-denied.app": "Vous n'avez pas l'autorisation n\xE9cessaire pour effectuer cette action. Demandez l'acc\xE8s au propri\xE9taire de l'appli.",
      "errorHandler.common-error.permission-denied.workspace": "Vous n'avez pas l'autorisation n\xE9cessaire pour effectuer cette action. Demandez l'acc\xE8s au propri\xE9taire de l'espace de travail.",
      "errorHandler.common-error.CheckStatus.cta": "Voir la page de statut",
      "errorHandler.common-error.refresh.cta": "Actualiser",
      "errorHandler.common-error.noConnection": "Il semble que vous \xEAtes hors ligne. V\xE9rifiez votre connexion et r\xE9essayez.",
      "errorHandler.common-error.resource-exhausted": "Trop d'actions ont \xE9t\xE9 effectu\xE9es trop rapidement. Patientez une minute puis r\xE9essayez.",
      "errorHandler.common-error.unauthenticated.cta": "Se connecter",
      "errorHandler.common-error.goBack.cta": "Retour",
      "errorHandler.common-error.not-found": "Il semble que cela n'existe plus.",
      "errorHandler.common-error.captchaRequired.verify.cta": "V\xE9rifier",
      "errorHandler.common-error.tryAgain.cta": "R\xE9essayer",
      "errorHandler.requestID.tooltip.cta": "Copier l'ID",
      "errorHandler.common-error.permission-denied": "Vous n'avez pas les autorisations n\xE9cessaires. Demandez l'acc\xE8s au propri\xE9taire.",
      "errorHandler.requestID.tooltip.description": "Si vous signalez cette erreur \xE0 l'Assistance, partagez cet ID de demande pour qu'ils puissent enqu\xEAter.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Nous avons rencontr\xE9 un probl\xE8me technique. Veuillez r\xE9essayer ou actualiser la page.",
      "errorHandler.common-error.unauthenticated": "Vous \xEAtes d\xE9connect\xE9. Reconnectez-vous pour continuer.",
      "errorHandler.common-error.networkError": "Un probl\xE8me de connexion \xE0 votre r\xE9seau est survenu. V\xE9rifiez votre connexion et r\xE9essayez.",
      "errorHandler.common-error.technicalIssue": "Nous avons rencontr\xE9 un probl\xE8me technique. Veuillez r\xE9essayer ou actualiser la page.",
      "errorHandler.common-error.captchaRequired": "Pour continuer, v\xE9rifiez que vous \xEAtes un humain.",
      "errorHandler.common-error.technicalIssue.generic": "Nous avons rencontr\xE9 un probl\xE8me technique. Veuillez r\xE9essayer ou actualiser la page.",
      "errorHandler.common-error.validationBasic": "Certains champs contiennent des informations invalides ou manquantes.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copi\xE9."
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_he.json
var require_common_errors_he = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_he.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_hi.json
var require_common_errors_hi = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_hi.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_hr.json
var require_common_errors_hr = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_hr.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_hu.json
var require_common_errors_hu = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_hu.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_id.json
var require_common_errors_id = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_id.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Anda tidak memiliki izin untuk melakukan ini. Minta akses dari pemilik situs.",
      "errorHandler.uncommon-error.NotImplemented": "Tindakan ini tidak tersedia saat ini.",
      "errorHandler.common-error.conflict": "Tidak dapat menyimpan perubahan karena halaman ini diperbarui di tempat lain. Catat perubahan Anda, lalu muat ulang dan coba edit lagi.",
      "errorHandler.common-error.permission-denied.app": "Anda tidak memiliki izin untuk melakukan tindakan ini. Minta akses dari pemilik aplikasi.",
      "errorHandler.common-error.permission-denied.workspace": "Anda tidak memiliki izin untuk melakukan tindakan ini. Minta akses dari pemilik ruang kerja.",
      "errorHandler.common-error.CheckStatus.cta": "Periksa Halaman Status",
      "errorHandler.common-error.refresh.cta": "Muat Ulang",
      "errorHandler.common-error.noConnection": "Sepertinya Anda sedang offline. Periksa koneksi Anda dan coba lagi.",
      "errorHandler.common-error.resource-exhausted": "Terlalu banyak tindakan yang dilakukan terlalu cepat. Tunggu sebentar dan coba lagi.",
      "errorHandler.common-error.unauthenticated.cta": "Login",
      "errorHandler.common-error.goBack.cta": "Kembali",
      "errorHandler.common-error.not-found": "Sepertinya ini tidak ada lagi.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verifikasi",
      "errorHandler.common-error.tryAgain.cta": "Coba Lagi",
      "errorHandler.requestID.tooltip.cta": "Salin ID",
      "errorHandler.common-error.permission-denied": "Anda tidak memiliki izin yang diperlukan. Minta akses dari pemilik.",
      "errorHandler.requestID.tooltip.description": "Jika Anda melaporkan kesalahan ini ke Layanan Pelanggan, bagikan ID permintaan ini agar mereka dapat menyelidikinya.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Ada masalah teknis di pihak kami. Coba lagi atau muat ulang.",
      "errorHandler.common-error.unauthenticated": "Anda telah logout. Login kembali untuk melanjutkan.",
      "errorHandler.common-error.networkError": "Terjadi masalah saat menghubungkan ke jaringan Anda. Periksa koneksi Anda dan coba lagi.",
      "errorHandler.common-error.technicalIssue": "Ada masalah teknis di pihak kami. Coba lagi atau muat ulang.",
      "errorHandler.common-error.captchaRequired": "Untuk melanjutkan, verifikasi bahwa Anda manusia.",
      "errorHandler.common-error.technicalIssue.generic": "Ada masalah teknis di pihak kami. Coba lagi atau muat ulang.",
      "errorHandler.common-error.validationBasic": "Beberapa kolom memiliki informasi yang tidak valid atau tidak memiliki informasi.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Disalin!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_it.json
var require_common_errors_it = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_it.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Non hai l'autorizzazione per farlo. Richiedi l'accesso al proprietario del sito.",
      "errorHandler.uncommon-error.NotImplemented": "Questa azione non \xE8 disponibile al momento.",
      "errorHandler.common-error.conflict": "Impossibile salvare le modifiche perch\xE9 la pagina \xE8 stata aggiornata altrove. Prendi nota delle modifiche, aggiorna la pagina e riprova.",
      "errorHandler.common-error.permission-denied.app": "Non hai l'autorizzazione per eseguire quest'azione. Richiedi l'accesso al proprietario dell'app.",
      "errorHandler.common-error.permission-denied.workspace": "Non hai l'autorizzazione per eseguire quest'azione. Richiedi l'accesso al proprietario dello spazio di lavoro.",
      "errorHandler.common-error.CheckStatus.cta": "Controlla la pagina dello stato",
      "errorHandler.common-error.refresh.cta": "Aggiorna",
      "errorHandler.common-error.noConnection": "Sembra che tu sia offline. Verifica la connessione e riprova.",
      "errorHandler.common-error.resource-exhausted": "C'\xE8 stato un problema tecnico interno. Riprova o ricarica la pagina.",
      "errorHandler.common-error.unauthenticated.cta": "Accedi",
      "errorHandler.common-error.goBack.cta": "Indietro",
      "errorHandler.common-error.not-found": "Sembra che questo elemento non esista pi\xF9.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verifica",
      "errorHandler.common-error.tryAgain.cta": "Riprova.",
      "errorHandler.requestID.tooltip.cta": "Copia ID",
      "errorHandler.common-error.permission-denied": "Non hai l'autorizzazione. Richiedi l'accesso al proprietario.",
      "errorHandler.requestID.tooltip.description": "Se segnali un errore all'Assistenza clienti, condividi l'ID di questa richiesta per aiutarli a indagare.",
      "errorHandler.common-error.technicalIssue-unmappedError": "C'\xE8 stato un problema tecnico interno. Riprova o ricarica la pagina.",
      "errorHandler.common-error.unauthenticated": "La connessione \xE8 stata interrotta. Accedi di nuovo per continuare.",
      "errorHandler.common-error.networkError": "Si \xE8 verificato un problema di connessione alla rete. Verifica la connessione e riprova.",
      "errorHandler.common-error.technicalIssue": "C'\xE8 stato un problema tecnico interno. Riprova o ricarica la pagina.",
      "errorHandler.common-error.captchaRequired": "Per continuare, conferma di essere umano.",
      "errorHandler.common-error.technicalIssue.generic": "C'\xE8 stato un problema tecnico interno. Riprova o ricarica la pagina.",
      "errorHandler.common-error.validationBasic": "Alcuni campi contengono informazioni non valide o mancanti.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copiato!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ja.json
var require_common_errors_ja = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ja.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "\u3053\u308C\u3092\u884C\u3046\u6A29\u9650\u304C\u3042\u308A\u307E\u305B\u3093\u3002\u30B5\u30A4\u30C8\u6240\u6709\u8005\u306B\u30A2\u30AF\u30BB\u30B9\u6A29\u3092\u30EA\u30AF\u30A8\u30B9\u30C8\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.uncommon-error.NotImplemented": "\u3053\u306E\u30A2\u30AF\u30B7\u30E7\u30F3\u306F\u73FE\u5728\u3054\u5229\u7528\u3044\u305F\u3060\u3051\u307E\u305B\u3093\u3002",
      "errorHandler.common-error.conflict": "\u3053\u306E\u30DA\u30FC\u30B8\u306F\u5225\u306E\u5834\u6240\u3067\u66F4\u65B0\u3055\u308C\u305F\u305F\u3081\u3001\u5909\u66F4\u304C\u4FDD\u5B58\u3055\u308C\u307E\u305B\u3093\u3067\u3057\u305F\u3002\u5909\u66F4\u5185\u5BB9\u3092\u8A18\u9332\u3057\u305F\u4E0A\u3067\u3001\u30DA\u30FC\u30B8\u3092\u518D\u8AAD\u307F\u8FBC\u307F\u3057\u3066\u518D\u5EA6\u304A\u8A66\u3057\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.permission-denied.app": "\u3053\u306E\u64CD\u4F5C\u3092\u884C\u3046\u6A29\u9650\u304C\u3042\u308A\u307E\u305B\u3093\u3002\u30A2\u30D7\u30EA\u306E\u30AA\u30FC\u30CA\u30FC\u306B\u30A2\u30AF\u30BB\u30B9\u6A29\u3092\u30EA\u30AF\u30A8\u30B9\u30C8\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.permission-denied.workspace": "\u3053\u306E\u30A2\u30AF\u30B7\u30E7\u30F3\u3092\u884C\u3046\u6A29\u9650\u304C\u3042\u308A\u307E\u305B\u3093\u3002\u30EF\u30FC\u30AF\u30B9\u30DA\u30FC\u30B9\u30AA\u30FC\u30CA\u30FC\u306B\u30A2\u30AF\u30BB\u30B9\u6A29\u3092\u30EA\u30AF\u30A8\u30B9\u30C8\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.CheckStatus.cta": "\u30B9\u30C6\u30FC\u30BF\u30B9\u30DA\u30FC\u30B8\u3092\u78BA\u8A8D\u3059\u308B",
      "errorHandler.common-error.refresh.cta": "\u518D\u8AAD\u307F\u8FBC\u307F\u3059\u308B",
      "errorHandler.common-error.noConnection": "\u30AA\u30D5\u30E9\u30A4\u30F3\u306E\u3088\u3046\u3067\u3059\u3002\u304A\u624B\u6570\u3067\u3059\u304C\u3001\u63A5\u7D9A\u72B6\u6CC1\u3092\u78BA\u8A8D\u3057\u3066\u3082\u3046\u4E00\u5EA6\u304A\u8A66\u3057\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.resource-exhausted": "\u77ED\u3044\u6642\u9593\u306B\u591A\u304F\u306E\u64CD\u4F5C\u304C\u884C\u308F\u308C\u307E\u3057\u305F\u3002\u304A\u624B\u6570\u3067\u3059\u304C\u3001\u5C11\u3057\u304A\u5F85\u3061\u3044\u305F\u3060\u3044\u3066\u304B\u3089\u3001\u3082\u3046\u4E00\u5EA6\u304A\u8A66\u3057\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.unauthenticated.cta": "\u30ED\u30B0\u30A4\u30F3",
      "errorHandler.common-error.goBack.cta": "\u623B\u308B",
      "errorHandler.common-error.not-found": "\u3053\u306E\u30A2\u30A4\u30C6\u30E0\u306F\u3082\u3046\u5B58\u5728\u3057\u306A\u3044\u3088\u3046\u3067\u3059\u3002",
      "errorHandler.common-error.captchaRequired.verify.cta": "\u78BA\u8A8D\u3059\u308B",
      "errorHandler.common-error.tryAgain.cta": "\u3082\u3046\u4E00\u5EA6\u8A66\u3059",
      "errorHandler.requestID.tooltip.cta": "ID \u3092\u30B3\u30D4\u30FC",
      "errorHandler.common-error.permission-denied": "\u5FC5\u8981\u306A\u6A29\u9650\u304C\u3042\u308A\u307E\u305B\u3093\u3002\u30B5\u30A4\u30C8\u6240\u6709\u8005\u306B\u30A2\u30AF\u30BB\u30B9\u6A29\u3092\u30EA\u30AF\u30A8\u30B9\u30C8\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "\u5F0A\u793E\u5074\u3067\u6280\u8853\u7684\u306A\u554F\u984C\u304C\u767A\u751F\u3057\u307E\u3057\u305F\u3002\u304A\u624B\u6570\u3067\u3059\u304C\u3001\u518D\u5EA6\u304A\u8A66\u3057\u306B\u306A\u308B\u304B\u30DA\u30FC\u30B8\u3092\u518D\u8AAD\u307F\u8FBC\u307F\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.unauthenticated": "\u30ED\u30B0\u30A2\u30A6\u30C8\u3057\u3066\u3044\u307E\u3059\u3002\u7D9A\u884C\u3059\u308B\u306B\u306F\u518D\u5EA6\u30ED\u30B0\u30A4\u30F3\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.networkError": "\u30CD\u30C3\u30C8\u30EF\u30FC\u30AF\u63A5\u7D9A\u4E2D\u306B\u554F\u984C\u304C\u767A\u751F\u3057\u307E\u3057\u305F\u3002\u304A\u624B\u6570\u3067\u3059\u304C\u3001\u63A5\u7D9A\u72B6\u6CC1\u3092\u78BA\u8A8D\u3057\u3066\u3082\u3046\u4E00\u5EA6\u304A\u8A66\u3057\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.technicalIssue": "\u5F0A\u793E\u5074\u3067\u6280\u8853\u7684\u306A\u554F\u984C\u304C\u767A\u751F\u3057\u307E\u3057\u305F\u3002\u304A\u624B\u6570\u3067\u3059\u304C\u3001\u518D\u5EA6\u304A\u8A66\u3057\u306B\u306A\u308B\u304B\u30DA\u30FC\u30B8\u3092\u518D\u8AAD\u307F\u8FBC\u307F\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.captchaRequired": "\u7D9A\u884C\u3059\u308B\u306B\u306F\u3001\u3042\u306A\u305F\u304C\u4EBA\u9593\u3067\u3042\u308B\u3053\u3068\u3092\u78BA\u8A8D\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.technicalIssue.generic": "\u5F0A\u793E\u5074\u3067\u6280\u8853\u7684\u306A\u554F\u984C\u304C\u767A\u751F\u3057\u307E\u3057\u305F\u3002\u304A\u624B\u6570\u3067\u3059\u304C\u3001\u518D\u5EA6\u304A\u8A66\u3057\u306B\u306A\u308B\u304B\u30DA\u30FC\u30B8\u3092\u518D\u8AAD\u307F\u8FBC\u307F\u3057\u3066\u304F\u3060\u3055\u3044\u3002",
      "errorHandler.common-error.validationBasic": "\u4E00\u90E8\u306E\u9805\u76EE\u306B\u306F\u3001\u60C5\u5831\u304C\u7121\u52B9\u307E\u305F\u306F\u4E0D\u8DB3\u3057\u3066\u3044\u307E\u3059\u3002",
      "errorHandler.requestID.tooltip.cta.copied-success": "\u30B3\u30D4\u30FC\u6E08\u307F"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ko.json
var require_common_errors_ko = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ko.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "\uD574\uB2F9 \uAD8C\uD55C\uC774 \uC5C6\uC2B5\uB2C8\uB2E4. \uC0AC\uC774\uD2B8 \uC18C\uC720\uC790\uC5D0\uAC8C \uC561\uC138\uC2A4 \uAD8C\uD55C\uC744 \uC694\uCCAD\uD558\uC138\uC694.",
      "errorHandler.uncommon-error.NotImplemented": "\uD604\uC7AC \uC2E4\uD589\uC774 \uBD88\uAC00\uB2A5\uD55C \uC791\uC5C5\uC785\uB2C8\uB2E4.",
      "errorHandler.common-error.conflict": "\uD398\uC774\uC9C0\uAC00 \uB2E4\uB978 \uACF3\uC5D0\uC11C \uC5C5\uB370\uC774\uD2B8\uB418\uC5B4 \uBCC0\uACBD\uC0AC\uD56D\uC744 \uC800\uC7A5\uD560 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4. \uBCC0\uACBD\uC0AC\uD56D\uC744 \uAE30\uC5B5\uD55C \uB2E4\uC74C \uD398\uC774\uC9C0\uB97C \uC0C8\uB85C\uACE0\uCE68\uD558\uACE0 \uB2E4\uC2DC \uD3B8\uC9D1\uD574\uBCF4\uC138\uC694.",
      "errorHandler.common-error.permission-denied.app": "\uC791\uC5C5 \uAD8C\uD55C\uC774 \uC5C6\uC2B5\uB2C8\uB2E4. \uC571 \uC18C\uC720\uC790\uC5D0\uAC8C \uC561\uC138\uC2A4 \uAD8C\uD55C\uC744 \uC694\uCCAD\uD558\uC138\uC694.",
      "errorHandler.common-error.permission-denied.workspace": "\uC791\uC5C5 \uAD8C\uD55C\uC774 \uC5C6\uC2B5\uB2C8\uB2E4. \uC6CC\uD06C\uC2A4\uD398\uC774\uC2A4 \uC18C\uC720\uC790\uC5D0\uAC8C \uC561\uC138\uC2A4 \uAD8C\uD55C\uC744 \uC694\uCCAD\uD558\uC138\uC694.",
      "errorHandler.common-error.CheckStatus.cta": "\uC0C1\uD0DC \uD398\uC774\uC9C0 \uD655\uC778",
      "errorHandler.common-error.refresh.cta": "\uC0C8\uB85C\uACE0\uCE68",
      "errorHandler.common-error.noConnection": "\uD604\uC7AC \uC624\uD504\uB77C\uC778 \uC0C1\uD0DC\uC785\uB2C8\uB2E4. \uC778\uD130\uB137 \uC5F0\uACB0 \uC0C1\uD0DC\uB97C \uD655\uC778\uD55C \uD6C4 \uB2E4\uC2DC \uC2DC\uB3C4\uD574\uBCF4\uC138\uC694.",
      "errorHandler.common-error.resource-exhausted": "\uC9E7\uC740 \uC2DC\uAC04 \uC548\uC5D0 \uB108\uBB34 \uB9CE\uC740 \uC791\uC5C5\uC774 \uC218\uD589\uB418\uC5C8\uC2B5\uB2C8\uB2E4. \uC7A0\uC2DC \uD6C4\uC5D0 \uB2E4\uC2DC \uC2DC\uB3C4\uD574\uBCF4\uC138\uC694.",
      "errorHandler.common-error.unauthenticated.cta": "\uB85C\uADF8\uC778",
      "errorHandler.common-error.goBack.cta": "\uB3CC\uC544\uAC00\uAE30",
      "errorHandler.common-error.not-found": "\uB354 \uC774\uC0C1 \uC874\uC7AC\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4.",
      "errorHandler.common-error.captchaRequired.verify.cta": "\uC778\uC99D",
      "errorHandler.common-error.tryAgain.cta": "\uB2E4\uC2DC \uC2DC\uB3C4",
      "errorHandler.requestID.tooltip.cta": "ID \uBCF5\uC0AC",
      "errorHandler.common-error.permission-denied": "\uD544\uC694\uD55C \uAD8C\uD55C\uC774 \uC5C6\uC2B5\uB2C8\uB2E4. \uC18C\uC720\uC790\uC5D0\uAC8C \uC561\uC138\uC2A4 \uAD8C\uD55C\uC744 \uC694\uCCAD\uD558\uC138\uC694.",
      "errorHandler.requestID.tooltip.description": "\uACE0\uAC1D\uC9C0\uC6D0\uD300\uC5D0 \uC624\uB958\uB97C \uC2E0\uACE0\uD558\uB294 \uACBD\uC6B0, \uB2F4\uB2F9\uC790\uAC00 \uBB38\uC81C\uB97C \uD655\uC778\uD560 \uC218 \uC788\uB3C4\uB85D \uBCF8 \uC694\uCCAD ID\uB97C \uACF5\uC720\uD574\uC8FC\uC138\uC694.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Wix\uC5D0 \uAE30\uC220\uC801\uC778 \uBB38\uC81C\uAC00 \uBC1C\uC0DD\uD558\uC600\uC2B5\uB2C8\uB2E4. \uB2E4\uC2DC \uC2DC\uB3C4\uD558\uAC70\uB098 \uD398\uC774\uC9C0\uB97C \uC0C8\uB85C\uACE0\uCE68\uD558\uC138\uC694.",
      "errorHandler.common-error.unauthenticated": "\uB85C\uADF8\uC544\uC6C3 \uC0C1\uD0DC\uC785\uB2C8\uB2E4. \uB85C\uADF8\uC778 \uD6C4 \uACC4\uC18D \uC9C4\uD589\uD558\uC138\uC694.",
      "errorHandler.common-error.networkError": "\uC624\uB958\uAC00 \uBC1C\uC0DD\uD558\uC5EC \uB124\uD2B8\uC6CC\uD06C\uC5D0 \uC5F0\uACB0\uD560 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4. \uC778\uD130\uB137 \uC5F0\uACB0 \uC0C1\uD0DC\uB97C \uD655\uC778\uD55C \uD6C4 \uB2E4\uC2DC \uC2DC\uB3C4\uD574\uBCF4\uC138\uC694.",
      "errorHandler.common-error.technicalIssue": "Wix\uC5D0 \uAE30\uC220\uC801\uC778 \uBB38\uC81C\uAC00 \uBC1C\uC0DD\uD558\uC600\uC2B5\uB2C8\uB2E4. \uB2E4\uC2DC \uC2DC\uB3C4\uD558\uAC70\uB098 \uD398\uC774\uC9C0\uB97C \uC0C8\uB85C\uACE0\uCE68\uD558\uC138\uC694.",
      "errorHandler.common-error.captchaRequired": "\uACC4\uC18D \uC9C4\uD589\uD558\uB824\uBA74 \uBCF8\uC778\uC784\uC744 \uC778\uC99D\uD558\uC138\uC694.",
      "errorHandler.common-error.technicalIssue.generic": "Wix\uC5D0 \uAE30\uC220\uC801\uC778 \uBB38\uC81C\uAC00 \uBC1C\uC0DD\uD558\uC600\uC2B5\uB2C8\uB2E4. \uB2E4\uC2DC \uC2DC\uB3C4\uD558\uAC70\uB098 \uD398\uC774\uC9C0\uB97C \uC0C8\uB85C\uACE0\uCE68\uD558\uC138\uC694.",
      "errorHandler.common-error.validationBasic": "\uC77C\uBD80 \uC785\uB825\uB780\uC5D0 \uC720\uD6A8\uD558\uC9C0 \uC54A\uAC70\uB098 \uB204\uB77D\uB41C \uC815\uBCF4\uAC00 \uC788\uC2B5\uB2C8\uB2E4.",
      "errorHandler.requestID.tooltip.cta.copied-success": "\uBCF5\uC0AC\uB428"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_lt.json
var require_common_errors_lt = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_lt.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_lv.json
var require_common_errors_lv = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_lv.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ms.json
var require_common_errors_ms = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ms.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_nl.json
var require_common_errors_nl = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_nl.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Je hebt geen machtiging om dit te doen. Vraag toegang aan bij de website-eigenaar.",
      "errorHandler.uncommon-error.NotImplemented": "Deze actie is momenteel niet beschikbaar.",
      "errorHandler.common-error.conflict": "Kan wijzigingen niet opslaan, omdat deze pagina ergens anders is bijgewerkt. Noteer je wijzigingen, vernieuw de pagina en probeer het opnieuw.",
      "errorHandler.common-error.permission-denied.app": "Je bent niet gemachtigd om deze actie uit te voeren. Vraag toegang aan bij de app-eigenaar.",
      "errorHandler.common-error.permission-denied.workspace": "Je bent niet gemachtigd om deze actie uit te voeren. Vraag toegang aan bij de werkruimte-eigenaar.",
      "errorHandler.common-error.CheckStatus.cta": "Statuspagina controleren",
      "errorHandler.common-error.refresh.cta": "Vernieuwen",
      "errorHandler.common-error.noConnection": "Het lijkt erop dat je offline bent. Controleer je internetverbinding en probeer het opnieuw.",
      "errorHandler.common-error.resource-exhausted": "Er zijn te veel acties te snel uitgevoerd. Wacht even en probeer het opnieuw.",
      "errorHandler.common-error.unauthenticated.cta": "inloggen",
      "errorHandler.common-error.goBack.cta": "Terug",
      "errorHandler.common-error.not-found": "Het lijkt erop dat dit niet meer bestaat.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verifi\xEBren",
      "errorHandler.common-error.tryAgain.cta": "Opnieuw proberen",
      "errorHandler.requestID.tooltip.cta": "ID kopi\xEBren",
      "errorHandler.common-error.permission-denied": "Je hebt de benodigde machtigingen niet. Vraag toegang aan bij de eigenaar.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Er is een technisch probleem opgetreden aan onze kant. Probeer het opnieuw of vernieuw de pagina.",
      "errorHandler.common-error.unauthenticated": "Je bent uitgelogd. Log opnieuw in om verder te gaan.",
      "errorHandler.common-error.networkError": "Er is een probleem opgetreden tijdens het koppelen aan je netwerk. Controleer je internetverbinding en probeer het opnieuw.",
      "errorHandler.common-error.technicalIssue": "Er is een technisch probleem opgetreden aan onze kant. Probeer het opnieuw of vernieuw de pagina.",
      "errorHandler.common-error.captchaRequired": "Om door te gaan, verifieer je dat je een mens bent.",
      "errorHandler.common-error.technicalIssue.generic": "Er is een technisch probleem opgetreden aan onze kant. Probeer het opnieuw of vernieuw de pagina.",
      "errorHandler.common-error.validationBasic": "Sommige velden bevatten ongeldige of ontbrekende informatie.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Gekopieerd"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_no.json
var require_common_errors_no = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_no.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Du har ikke tillatelse til \xE5 gj\xF8re dette. Be om tilgang fra nettstedets eier.",
      "errorHandler.uncommon-error.NotImplemented": "Denne handlingen er ikke tilgjengelig akkurat n\xE5.",
      "errorHandler.common-error.conflict": "Kan ikke lagre endringene fordi denne siden ble oppdatert et annet sted. Merk endringene dine, oppdater deretter og pr\xF8v \xE5 redigere p\xE5 nytt.",
      "errorHandler.common-error.permission-denied.app": "Du har ikke tillatelse til \xE5 utf\xF8re denne handlingen. Be om tilgang fra appens eier.",
      "errorHandler.common-error.permission-denied.workspace": "Du har ikke tillatelse til \xE5 utf\xF8re denne handlingen. Be om tilgang fra eieren av arbeidsomr\xE5det.",
      "errorHandler.common-error.CheckStatus.cta": "Sjekk status-siden",
      "errorHandler.common-error.refresh.cta": "Oppdater",
      "errorHandler.common-error.noConnection": "Det ser ut til at du er frakoblet. Sjekk tilkoblingen og pr\xF8v igjen.",
      "errorHandler.common-error.resource-exhausted": "For mange handlinger ble utf\xF8rt for raskt. Vent et \xF8yeblikk og pr\xF8v igjen.",
      "errorHandler.common-error.unauthenticated.cta": "Logg inn",
      "errorHandler.common-error.goBack.cta": "G\xE5 tilbake",
      "errorHandler.common-error.not-found": "Det ser ut til at dette ikke eksisterer lenger.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verifiser",
      "errorHandler.common-error.tryAgain.cta": "Pr\xF8v p\xE5 nytt",
      "errorHandler.requestID.tooltip.cta": "Kopier ID",
      "errorHandler.common-error.permission-denied": "Du har ikke de n\xF8dvendige tillatelsene. Be om tilgang fra eieren.",
      "errorHandler.requestID.tooltip.description": "Hvis du rapporterer denne feilen til brukerst\xF8tte, m\xE5 du dele denne foresp\xF8rsels-ID-en, slik at de kan unders\xF8ke saken.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Det oppstod et teknisk problem hos oss. Pr\xF8v p\xE5 nytt eller last oppdater.",
      "errorHandler.common-error.unauthenticated": "Du er logget ut. Logg inn igjen for \xE5 fortsette.",
      "errorHandler.common-error.networkError": "Det oppstod et problem med tilkoblingen til nettverket ditt. Sjekk tilkoblingen og pr\xF8v igjen.",
      "errorHandler.common-error.technicalIssue": "Det oppstod et teknisk problem hos oss. Pr\xF8v p\xE5 nytt eller oppdater.",
      "errorHandler.common-error.captchaRequired": "For \xE5 fortsette m\xE5 du verifisere at du er et menneske.",
      "errorHandler.common-error.technicalIssue.generic": "Det oppstod et teknisk problem hos oss. Pr\xF8v p\xE5 nytt eller oppdater.",
      "errorHandler.common-error.validationBasic": "Noen felt har ugyldig eller manglende informasjon.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Kopiert!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_pl.json
var require_common_errors_pl = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_pl.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Nie masz uprawnie\u0144, aby to zrobi\u0107. Popro\u015B w\u0142a\u015Bciciela strony o dost\u0119p.",
      "errorHandler.uncommon-error.NotImplemented": "Wykonanie tego dzia\u0142ania jest w tej chwili niemo\u017Cliwe.",
      "errorHandler.common-error.conflict": "Nie mo\u017Cna zapisa\u0107 zmian, poniewa\u017C ta strona zosta\u0142a zaktualizowana w innym miejscu. Zanotuj zmiany, a nast\u0119pnie od\u015Bwie\u017C i spr\xF3buj edytowa\u0107 ponownie.",
      "errorHandler.common-error.permission-denied.app": "Nie masz uprawnie\u0144 do wykonania tej czynno\u015Bci. Popro\u015B w\u0142a\u015Bciciela aplikacji o dost\u0119p.",
      "errorHandler.common-error.permission-denied.workspace": "Nie masz uprawnie\u0144 do wykonania tej czynno\u015Bci. Popro\u015B w\u0142a\u015Bciciela obszaru roboczego o dost\u0119p.",
      "errorHandler.common-error.CheckStatus.cta": "Sprawd\u017A stron\u0119 statusu",
      "errorHandler.common-error.refresh.cta": "Od\u015Bwie\u017C",
      "errorHandler.common-error.noConnection": "Wygl\u0105da na to, \u017Ce jeste\u015B offline. Sprawd\u017A po\u0142\u0105czenie i spr\xF3buj ponownie.",
      "errorHandler.common-error.resource-exhausted": "Wiele dzia\u0142a\u0144 zosta\u0142o wykonanych zbyt szybko. Zaczekaj minut\u0119 i spr\xF3buj ponownie.",
      "errorHandler.common-error.unauthenticated.cta": "Zaloguj si\u0119",
      "errorHandler.common-error.goBack.cta": "Wr\xF3\u0107",
      "errorHandler.common-error.not-found": "To ju\u017C nie istnieje.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Zweryfikuj",
      "errorHandler.common-error.tryAgain.cta": "Spr\xF3buj ponownie",
      "errorHandler.requestID.tooltip.cta": "Skopiuj identyfikator",
      "errorHandler.common-error.permission-denied": "Nie masz wymaganych uprawnie\u0144. Popro\u015B w\u0142a\u015Bciciela o dost\u0119p.",
      "errorHandler.requestID.tooltip.description": "Je\u015Bli chcesz zg\u0142osi\u0107 ten b\u0142\u0105d do dzia\u0142u obs\u0142ugi klienta, podaj nast\u0119puj\u0105cy identyfikator \u017C\u0105dania, aby mogli przyjrze\u0107 si\u0119 tej sprawie.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Wyst\u0105pi\u0142 problem techniczny po naszej stronie. Spr\xF3buj ponownie lub od\u015Bwie\u017C.",
      "errorHandler.common-error.unauthenticated": "Wylogowano. Zaloguj si\u0119 ponownie, aby kontynuowa\u0107.",
      "errorHandler.common-error.networkError": "Wyst\u0105pi\u0142 problem podczas po\u0142\u0105czenia z Twoj\u0105 sieci\u0105. Sprawd\u017A po\u0142\u0105czenie i spr\xF3buj ponownie.",
      "errorHandler.common-error.technicalIssue": "Wyst\u0105pi\u0142 problem techniczny po naszej stronie. Spr\xF3buj ponownie lub od\u015Bwie\u017C.",
      "errorHandler.common-error.captchaRequired": "Aby kontynuowa\u0107, potwierd\u017A, \u017Ce jeste\u015B cz\u0142owiekiem.",
      "errorHandler.common-error.technicalIssue.generic": "Wyst\u0105pi\u0142 problem techniczny po naszej stronie. Spr\xF3buj ponownie lub od\u015Bwie\u017C.",
      "errorHandler.common-error.validationBasic": "Niekt\xF3re pola zawieraj\u0105 nieprawid\u0142owe lub brakuj\u0105ce informacje.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Skopiowano!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_pt.json
var require_common_errors_pt = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_pt.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Voc\xEA n\xE3o tem permiss\xE3o para fazer isso. Solicite acesso ao propriet\xE1rio do site.",
      "errorHandler.uncommon-error.NotImplemented": "Essa a\xE7\xE3o n\xE3o est\xE1 dispon\xEDvel no momento.",
      "errorHandler.common-error.conflict": "N\xE3o \xE9 poss\xEDvel salvar as altera\xE7\xF5es porque essa p\xE1gina foi atualizada em outro lugar. Anote suas altera\xE7\xF5es, atualize e tente editar novamente.",
      "errorHandler.common-error.permission-denied.app": "Voc\xEA n\xE3o tem permiss\xE3o para fazer essa a\xE7\xE3o. Solicite acesso do propriet\xE1rio do app.",
      "errorHandler.common-error.permission-denied.workspace": "Voc\xEA n\xE3o tem permiss\xE3o para fazer essa a\xE7\xE3o. Solicite acesso ao propriet\xE1rio do espa\xE7o de trabalho.",
      "errorHandler.common-error.CheckStatus.cta": "Verificar p\xE1gina de status",
      "errorHandler.common-error.refresh.cta": "Atualizar",
      "errorHandler.common-error.noConnection": "Parece que voc\xEA est\xE1 offline. Verifique sua conex\xE3o e tente novamente.",
      "errorHandler.common-error.resource-exhausted": "Muitas a\xE7\xF5es foram feitas muito rapidamente. Aguarde alguns minutos e tente novamente.",
      "errorHandler.common-error.unauthenticated.cta": "Fazer login",
      "errorHandler.common-error.goBack.cta": "Voltar",
      "errorHandler.common-error.not-found": "Parece que isso n\xE3o existe mais.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verificar",
      "errorHandler.common-error.tryAgain.cta": "Tentar novamente",
      "errorHandler.requestID.tooltip.cta": "Copiar ID",
      "errorHandler.common-error.permission-denied": "Voc\xEA n\xE3o tem as permiss\xF5es necess\xE1rias. Solicite acesso ao propriet\xE1rio.",
      "errorHandler.requestID.tooltip.description": "Se voc\xEA for reportar esse erro ao Apoio ao Cliente, compartilhe esse ID de solicita\xE7\xE3o para que eles possam investigar.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Houve um problema t\xE9cnico no nosso sistema. Tente novamente ou atualize.",
      "errorHandler.common-error.unauthenticated": "Voc\xEA est\xE1 desconectado. Fa\xE7a login novamente para continuar.",
      "errorHandler.common-error.networkError": "Houve um problema na conex\xE3o com sua rede. Verifique sua conex\xE3o e tente novamente.",
      "errorHandler.common-error.technicalIssue": "Houve um problema t\xE9cnico no nosso sistema. Tente novamente ou atualize.",
      "errorHandler.common-error.captchaRequired": "Para continuar, confirme que voc\xEA \xE9 humano.",
      "errorHandler.common-error.technicalIssue.generic": "Houve um problema t\xE9cnico no nosso sistema. Tente novamente ou atualize.",
      "errorHandler.common-error.validationBasic": "Alguns campos t\xEAm informa\xE7\xF5es inv\xE1lidas ou ausentes.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copiado"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ro.json
var require_common_errors_ro = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ro.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ru.json
var require_common_errors_ru = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_ru.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "\u0423 \u0432\u0430\u0441 \u043D\u0435\u0442 \u0440\u0430\u0437\u0440\u0435\u0448\u0435\u043D\u0438\u044F \u043D\u0430 \u044D\u0442\u043E \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0435. \u0417\u0430\u043F\u0440\u043E\u0441\u0438\u0442\u0435 \u0434\u043E\u0441\u0442\u0443\u043F \u0443 \u0432\u043B\u0430\u0434\u0435\u043B\u044C\u0446\u0430 \u0441\u0430\u0439\u0442\u0430.",
      "errorHandler.uncommon-error.NotImplemented": "\u0421\u0435\u0439\u0447\u0430\u0441 \u044D\u0442\u043E \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0435 \u043D\u0435\u0434\u043E\u0441\u0442\u0443\u043F\u043D\u043E.",
      "errorHandler.common-error.conflict": "\u041D\u0435\u0432\u043E\u0437\u043C\u043E\u0436\u043D\u043E \u0441\u043E\u0445\u0440\u0430\u043D\u0438\u0442\u044C \u0438\u0437\u043C\u0435\u043D\u0435\u043D\u0438\u044F, \u043F\u043E\u0441\u043A\u043E\u043B\u044C\u043A\u0443 \u044D\u0442\u0430 \u0441\u0442\u0440\u0430\u043D\u0438\u0446\u0430 \u0431\u044B\u043B\u0430 \u043E\u0431\u043D\u043E\u0432\u043B\u0435\u043D\u0430 \u0432 \u0434\u0440\u0443\u0433\u043E\u043C \u043C\u0435\u0441\u0442\u0435. \u041E\u0431\u0440\u0430\u0442\u0438\u0442\u0435 \u0432\u043D\u0438\u043C\u0430\u043D\u0438\u0435 \u043D\u0430 \u0438\u0437\u043C\u0435\u043D\u0435\u043D\u0438\u044F, \u0437\u0430\u0442\u0435\u043C \u043E\u0431\u043D\u043E\u0432\u0438\u0442\u0435 \u0441\u0442\u0440\u0430\u043D\u0438\u0446\u0443 \u0438 \u043F\u043E\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0440\u0435\u0434\u0430\u043A\u0442\u0438\u0440\u043E\u0432\u0430\u0442\u044C \u0441\u043D\u043E\u0432\u0430.",
      "errorHandler.common-error.permission-denied.app": "\u0423 \u0432\u0430\u0441 \u043D\u0435\u0442 \u0440\u0430\u0437\u0440\u0435\u0448\u0435\u043D\u0438\u044F \u0434\u043B\u044F \u044D\u0442\u043E\u0433\u043E \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u044F. \u0417\u0430\u043F\u0440\u043E\u0441\u0438\u0442\u0435 \u0434\u043E\u0441\u0442\u0443\u043F \u0443 \u0432\u043B\u0430\u0434\u0435\u043B\u044C\u0446\u0430 \u043F\u0440\u0438\u043B\u043E\u0436\u0435\u043D\u0438\u044F.",
      "errorHandler.common-error.permission-denied.workspace": "\u0423 \u0432\u0430\u0441 \u043D\u0435\u0442 \u0440\u0430\u0437\u0440\u0435\u0448\u0435\u043D\u0438\u044F \u0434\u043B\u044F \u044D\u0442\u043E\u0433\u043E \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u044F. \u0417\u0430\u043F\u0440\u043E\u0441\u0438\u0442\u0435 \u0434\u043E\u0441\u0442\u0443\u043F \u0443 \u0432\u043B\u0430\u0434\u0435\u043B\u044C\u0446\u0430 \u0440\u0430\u0431\u043E\u0447\u0435\u0433\u043E \u043F\u0440\u043E\u0441\u0442\u0440\u0430\u043D\u0441\u0442\u0432\u0430.",
      "errorHandler.common-error.CheckStatus.cta": "\u041F\u0440\u043E\u0432\u0435\u0440\u0438\u0442\u044C \u0441\u0442\u0440\u0430\u043D\u0438\u0446\u0443 \u0441\u0442\u0430\u0442\u0443\u0441\u0430",
      "errorHandler.common-error.refresh.cta": "\u041E\u0431\u043D\u043E\u0432\u0438\u0442\u044C",
      "errorHandler.common-error.noConnection": "\u041F\u043E\u0445\u043E\u0436\u0435, \u0432\u044B \u043E\u0444\u0444\u043B\u0430\u0439\u043D. \u041F\u0440\u043E\u0432\u0435\u0440\u044C\u0442\u0435 \u0441\u043E\u0435\u0434\u0438\u043D\u0435\u043D\u0438\u0435 \u0438 \u043F\u043E\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0441\u043D\u043E\u0432\u0430.",
      "errorHandler.common-error.resource-exhausted": "\u0421\u043B\u0438\u0448\u043A\u043E\u043C \u043C\u043D\u043E\u0433\u043E \u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0439 \u0432\u044B\u043F\u043E\u043B\u043D\u0435\u043D\u043E \u0441\u043B\u0438\u0448\u043A\u043E\u043C \u0431\u044B\u0441\u0442\u0440\u043E. \u041F\u043E\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0441\u043D\u043E\u0432\u0430 \u0447\u0435\u0440\u0435\u0437 \u043C\u0438\u043D\u0443\u0442\u0443.",
      "errorHandler.common-error.unauthenticated.cta": "\u0412\u0445\u043E\u0434",
      "errorHandler.common-error.goBack.cta": "\u041D\u0430\u0437\u0430\u0434",
      "errorHandler.common-error.not-found": "\u041F\u043E\u0445\u043E\u0436\u0435, \u044D\u0442\u043E\u0439 \u043E\u043F\u0446\u0438\u0438 \u0431\u043E\u043B\u044C\u0448\u0435 \u043D\u0435\u0442.",
      "errorHandler.common-error.captchaRequired.verify.cta": "\u041F\u043E\u0434\u0442\u0432\u0435\u0440\u0434\u0438\u0442\u044C",
      "errorHandler.common-error.tryAgain.cta": "\u041F\u043E\u043F\u0440\u043E\u0431\u043E\u0432\u0430\u0442\u044C \u0435\u0449\u0435 \u0440\u0430\u0437",
      "errorHandler.requestID.tooltip.cta": "\u041A\u043E\u043F\u0438\u0440\u043E\u0432\u0430\u0442\u044C ID",
      "errorHandler.common-error.permission-denied": "\u0423 \u0432\u0430\u0441 \u043D\u0435\u0442 \u043D\u0435\u043E\u0431\u0445\u043E\u0434\u0438\u043C\u044B\u0445 \u0440\u0430\u0437\u0440\u0435\u0448\u0435\u043D\u0438\u0439. \u0417\u0430\u043F\u0440\u043E\u0441\u0438\u0442\u0435 \u0434\u043E\u0441\u0442\u0443\u043F \u0443 \u0432\u043B\u0430\u0434\u0435\u043B\u044C\u0446\u0430.",
      "errorHandler.requestID.tooltip.description": "\u0415\u0441\u043B\u0438 \u0432\u044B \u0441\u043E\u043E\u0431\u0449\u0438\u0442\u0435 \u043E\u0431 \u043E\u0448\u0438\u0431\u043A\u0435 \u043A\u043E\u043C\u0430\u043D\u0434\u0435 \u043F\u043E\u0434\u0434\u0435\u0440\u0436\u043A\u0438, \u043E\u0442\u043F\u0440\u0430\u0432\u044C\u0442\u0435 \u044D\u0442\u043E\u0442 \u0438\u0434\u0435\u043D\u0442\u0438\u0444\u0438\u043A\u0430\u0442\u043E\u0440 \u0437\u0430\u043F\u0440\u043E\u0441\u0430, \u0447\u0442\u043E\u0431\u044B \u043E\u043D\u0438 \u043C\u043E\u0433\u043B\u0438 \u0440\u0430\u0437\u043E\u0431\u0440\u0430\u0442\u044C\u0441\u044F.",
      "errorHandler.common-error.technicalIssue-unmappedError": "\u0412\u043E\u0437\u043D\u0438\u043A\u043B\u0430 \u0442\u0435\u0445\u043D\u0438\u0447\u0435\u0441\u043A\u0430\u044F \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430 \u043D\u0430 \u043D\u0430\u0448\u0435\u0439 \u0441\u0442\u043E\u0440\u043E\u043D\u0435. \u041F\u043E\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0435\u0449\u0435 \u0440\u0430\u0437 \u0438\u043B\u0438 \u043E\u0431\u043D\u043E\u0432\u0438\u0442\u0435 \u0441\u0442\u0440\u0430\u043D\u0438\u0446\u0443.",
      "errorHandler.common-error.unauthenticated": "\u0412\u044B \u0432\u044B\u0448\u043B\u0438 \u0438\u0437 \u0430\u043A\u043A\u0430\u0443\u043D\u0442\u0430. \u0412\u043E\u0439\u0434\u0438\u0442\u0435 \u0441\u043D\u043E\u0432\u0430, \u0447\u0442\u043E\u0431\u044B \u043F\u0440\u043E\u0434\u043E\u043B\u0436\u0438\u0442\u044C.",
      "errorHandler.common-error.networkError": "\u0412\u043E\u0437\u043D\u0438\u043A\u043B\u0430 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430 \u043F\u0440\u0438 \u043F\u043E\u0434\u043A\u043B\u044E\u0447\u0435\u043D\u0438\u0438 \u043A \u0432\u0430\u0448\u0435\u0439 \u0441\u0435\u0442\u0438. \u041F\u0440\u043E\u0432\u0435\u0440\u044C\u0442\u0435 \u043F\u043E\u0434\u043A\u043B\u044E\u0447\u0435\u043D\u0438\u0435 \u0438 \u043F\u043E\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0441\u043D\u043E\u0432\u0430.",
      "errorHandler.common-error.technicalIssue": "\u0412\u043E\u0437\u043D\u0438\u043A\u043B\u0430 \u0442\u0435\u0445\u043D\u0438\u0447\u0435\u0441\u043A\u0430\u044F \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430 \u043D\u0430 \u043D\u0430\u0448\u0435\u0439 \u0441\u0442\u043E\u0440\u043E\u043D\u0435. \u041F\u043E\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0435\u0449\u0435 \u0440\u0430\u0437 \u0438\u043B\u0438 \u043E\u0431\u043D\u043E\u0432\u0438\u0442\u0435 \u0441\u0442\u0440\u0430\u043D\u0438\u0446\u0443.",
      "errorHandler.common-error.captchaRequired": "\u0427\u0442\u043E\u0431\u044B \u043F\u0440\u043E\u0434\u043E\u043B\u0436\u0438\u0442\u044C, \u043F\u043E\u0434\u0442\u0432\u0435\u0440\u0434\u0438\u0442\u0435, \u0447\u0442\u043E \u0432\u044B \u0447\u0435\u043B\u043E\u0432\u0435\u043A.",
      "errorHandler.common-error.technicalIssue.generic": "\u0412\u043E\u0437\u043D\u0438\u043A\u043B\u0430 \u0442\u0435\u0445\u043D\u0438\u0447\u0435\u0441\u043A\u0430\u044F \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430 \u043D\u0430 \u043D\u0430\u0448\u0435\u0439 \u0441\u0442\u043E\u0440\u043E\u043D\u0435. \u041F\u043E\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0435\u0449\u0435 \u0440\u0430\u0437 \u0438\u043B\u0438 \u043E\u0431\u043D\u043E\u0432\u0438\u0442\u0435 \u0441\u0442\u0440\u0430\u043D\u0438\u0446\u0443.",
      "errorHandler.common-error.validationBasic": "\u0412 \u043D\u0435\u043A\u043E\u0442\u043E\u0440\u044B\u0445 \u043F\u043E\u043B\u044F\u0445 \u0443\u043A\u0430\u0437\u0430\u043D\u0430 \u043D\u0435\u0434\u0435\u0439\u0441\u0442\u0432\u0438\u0442\u0435\u043B\u044C\u043D\u0430\u044F \u0438\u043D\u0444\u043E\u0440\u043C\u0430\u0446\u0438\u044F, \u0438\u043B\u0438 \u043E\u043D\u0438 \u043D\u0435 \u0437\u0430\u043F\u043E\u043B\u043D\u0435\u043D\u044B.",
      "errorHandler.requestID.tooltip.cta.copied-success": "\u0421\u043A\u043E\u043F\u0438\u0440\u043E\u0432\u0430\u043D\u043E!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_sk.json
var require_common_errors_sk = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_sk.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_sl.json
var require_common_errors_sl = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_sl.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_sv.json
var require_common_errors_sv = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_sv.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Du har inte beh\xF6righet till detta. Beg\xE4r \xE5tkomst fr\xE5n sid\xE4garen.",
      "errorHandler.uncommon-error.NotImplemented": "Den h\xE4r \xE5tg\xE4rden \xE4r inte tillg\xE4nglig just nu.",
      "errorHandler.common-error.conflict": "Det g\xE5r inte att spara \xE4ndringar eftersom den h\xE4r sidan har uppdaterats n\xE5gon annanstans. Kom ih\xE5g dina \xE4ndringar. Uppdatera sedan sidan och f\xF6rs\xF6k att redigera igen.",
      "errorHandler.common-error.permission-denied.app": "Du har inte beh\xF6righet att utf\xF6ra den h\xE4r \xE5tg\xE4rden. Beg\xE4r \xE5tkomst fr\xE5n app\xE4garen.",
      "errorHandler.common-error.permission-denied.workspace": "Du har inte beh\xF6righet att utf\xF6ra den h\xE4r \xE5tg\xE4rden. Beg\xE4r \xE5tkomst fr\xE5n arbetsytans \xE4gare.",
      "errorHandler.common-error.CheckStatus.cta": "Kontrollera statussidan",
      "errorHandler.common-error.refresh.cta": "Uppdatera",
      "errorHandler.common-error.noConnection": "Det verkar som att du \xE4r offline. Kontrollera din anslutning och f\xF6rs\xF6k igen.",
      "errorHandler.common-error.resource-exhausted": "F\xF6r m\xE5nga \xE5tg\xE4rder gjordes f\xF6r snabbt. V\xE4nta en stund och f\xF6rs\xF6k sedan igen.",
      "errorHandler.common-error.unauthenticated.cta": "Logga in",
      "errorHandler.common-error.goBack.cta": "G\xE5 tillbaka",
      "errorHandler.common-error.not-found": "Det h\xE4r objektet verkar inte finnas l\xE4ngre.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Bekr\xE4fta",
      "errorHandler.common-error.tryAgain.cta": "F\xF6rs\xF6k igen",
      "errorHandler.requestID.tooltip.cta": "Kopiera ID",
      "errorHandler.common-error.permission-denied": "Du har inte de n\xF6dv\xE4ndiga beh\xF6righeterna. Beg\xE4r \xE5tkomst fr\xE5n \xE4garen.",
      "errorHandler.requestID.tooltip.description": "Om du rapporterar det h\xE4r felet till kundtj\xE4nst kan du dela detta f\xF6rfr\xE5gnings-ID s\xE5 att de kan unders\xF6ka.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Det uppstod ett tekniskt problem p\xE5 v\xE5r sida. F\xF6rs\xF6k igen eller uppdatera.",
      "errorHandler.common-error.unauthenticated": "Du \xE4r utloggad. Logga in igen f\xF6r att forts\xE4tta.",
      "errorHandler.common-error.networkError": "Det uppstod ett problem med att ansluta till ditt n\xE4tverk. Kontrollera din anslutning och f\xF6rs\xF6k igen.",
      "errorHandler.common-error.technicalIssue": "Det uppstod ett tekniskt problem p\xE5 v\xE5r sida. F\xF6rs\xF6k igen eller uppdatera.",
      "errorHandler.common-error.captchaRequired": "Bekr\xE4fta att du inte \xE4r en robot f\xF6r att forts\xE4tta.",
      "errorHandler.common-error.technicalIssue.generic": "Det uppstod ett tekniskt problem p\xE5 v\xE5r sida. F\xF6rs\xF6k igen eller uppdatera.",
      "errorHandler.common-error.validationBasic": "Vissa f\xE4lt har uppgifter som \xE4r ogiltiga eller saknas.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Kopierad!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_th.json
var require_common_errors_th = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_th.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "\u0E04\u0E38\u0E13\u0E44\u0E21\u0E48\u0E44\u0E14\u0E49\u0E23\u0E31\u0E1A\u0E2D\u0E19\u0E38\u0E0D\u0E32\u0E15\u0E43\u0E2B\u0E49\u0E14\u0E33\u0E40\u0E19\u0E34\u0E19\u0E01\u0E32\u0E23\u0E19\u0E35\u0E49 \u0E42\u0E1B\u0E23\u0E14\u0E02\u0E2D\u0E2D\u0E19\u0E38\u0E0D\u0E32\u0E15\u0E08\u0E32\u0E01\u0E40\u0E08\u0E49\u0E32\u0E02\u0E2D\u0E07\u0E40\u0E27\u0E47\u0E1A\u0E44\u0E0B\u0E15\u0E4C\u0E40\u0E1E\u0E37\u0E48\u0E2D\u0E40\u0E02\u0E49\u0E32\u0E16\u0E36\u0E07",
      "errorHandler.uncommon-error.NotImplemented": "\u0E44\u0E21\u0E48\u0E2A\u0E32\u0E21\u0E32\u0E23\u0E16\u0E14\u0E33\u0E40\u0E19\u0E34\u0E19\u0E01\u0E32\u0E23\u0E19\u0E35\u0E49\u0E44\u0E14\u0E49\u0E43\u0E19\u0E02\u0E13\u0E30\u0E19\u0E35\u0E49",
      "errorHandler.common-error.conflict": "\u0E44\u0E21\u0E48\u0E2A\u0E32\u0E21\u0E32\u0E23\u0E16\u0E1A\u0E31\u0E19\u0E17\u0E36\u0E01\u0E01\u0E32\u0E23\u0E41\u0E01\u0E49\u0E44\u0E02\u0E40\u0E19\u0E37\u0E48\u0E2D\u0E07\u0E08\u0E32\u0E01\u0E21\u0E35\u0E1C\u0E39\u0E49\u0E2D\u0E37\u0E48\u0E19\u0E2D\u0E31\u0E1B\u0E40\u0E14\u0E15\u0E2B\u0E19\u0E49\u0E32\u0E40\u0E1E\u0E08\u0E19\u0E35\u0E49 \u0E08\u0E14\u0E1A\u0E31\u0E19\u0E17\u0E36\u0E01\u0E2A\u0E34\u0E48\u0E07\u0E17\u0E35\u0E48\u0E04\u0E38\u0E13\u0E41\u0E01\u0E49\u0E44\u0E02 \u0E08\u0E32\u0E01\u0E19\u0E31\u0E49\u0E19\u0E23\u0E35\u0E40\u0E1F\u0E23\u0E0A\u0E41\u0E25\u0E49\u0E27\u0E25\u0E2D\u0E07\u0E41\u0E01\u0E49\u0E44\u0E02\u0E2D\u0E35\u0E01\u0E04\u0E23\u0E31\u0E49\u0E07",
      "errorHandler.common-error.permission-denied.app": "\u0E04\u0E38\u0E13\u0E44\u0E21\u0E48\u0E44\u0E14\u0E49\u0E23\u0E31\u0E1A\u0E2D\u0E19\u0E38\u0E0D\u0E32\u0E15\u0E43\u0E2B\u0E49\u0E14\u0E33\u0E40\u0E19\u0E34\u0E19\u0E01\u0E32\u0E23\u0E19\u0E35\u0E49 \u0E42\u0E1B\u0E23\u0E14\u0E02\u0E2D\u0E2D\u0E19\u0E38\u0E0D\u0E32\u0E15\u0E08\u0E32\u0E01\u0E40\u0E08\u0E49\u0E32\u0E02\u0E2D\u0E07\u0E41\u0E2D\u0E1B\u0E40\u0E1E\u0E37\u0E48\u0E2D\u0E40\u0E02\u0E49\u0E32\u0E16\u0E36\u0E07",
      "errorHandler.common-error.permission-denied.workspace": "\u0E04\u0E38\u0E13\u0E44\u0E21\u0E48\u0E44\u0E14\u0E49\u0E23\u0E31\u0E1A\u0E2D\u0E19\u0E38\u0E0D\u0E32\u0E15\u0E43\u0E2B\u0E49\u0E14\u0E33\u0E40\u0E19\u0E34\u0E19\u0E01\u0E32\u0E23\u0E19\u0E35\u0E49 \u0E42\u0E1B\u0E23\u0E14\u0E02\u0E2D\u0E2D\u0E19\u0E38\u0E0D\u0E32\u0E15\u0E08\u0E32\u0E01\u0E40\u0E08\u0E49\u0E32\u0E02\u0E2D\u0E07\u0E1E\u0E37\u0E49\u0E19\u0E17\u0E35\u0E48\u0E17\u0E33\u0E07\u0E32\u0E19\u0E40\u0E1E\u0E37\u0E48\u0E2D\u0E40\u0E02\u0E49\u0E32\u0E16\u0E36\u0E07",
      "errorHandler.common-error.CheckStatus.cta": "\u0E15\u0E23\u0E27\u0E08\u0E2A\u0E2D\u0E1A\u0E2B\u0E19\u0E49\u0E32\u0E40\u0E1E\u0E08\u0E2A\u0E16\u0E32\u0E19\u0E30",
      "errorHandler.common-error.refresh.cta": "\u0E23\u0E35\u0E40\u0E1F\u0E23\u0E0A",
      "errorHandler.common-error.noConnection": "\u0E14\u0E39\u0E40\u0E2B\u0E21\u0E37\u0E2D\u0E19\u0E04\u0E38\u0E13\u0E01\u0E33\u0E25\u0E31\u0E07\u0E2D\u0E2D\u0E1F\u0E44\u0E25\u0E19\u0E4C \u0E42\u0E1B\u0E23\u0E14\u0E15\u0E23\u0E27\u0E08\u0E2A\u0E2D\u0E1A\u0E01\u0E32\u0E23\u0E40\u0E0A\u0E37\u0E48\u0E2D\u0E21\u0E15\u0E48\u0E2D\u0E2D\u0E34\u0E19\u0E40\u0E17\u0E2D\u0E23\u0E4C\u0E40\u0E19\u0E47\u0E15\u0E08\u0E32\u0E01\u0E19\u0E31\u0E49\u0E19\u0E25\u0E2D\u0E07\u0E2D\u0E35\u0E01\u0E04\u0E23\u0E31\u0E49\u0E07",
      "errorHandler.common-error.resource-exhausted": "\u0E21\u0E35\u0E01\u0E32\u0E23\u0E14\u0E33\u0E40\u0E19\u0E34\u0E19\u0E01\u0E32\u0E23\u0E21\u0E32\u0E01\u0E40\u0E01\u0E34\u0E19\u0E44\u0E1B\u0E43\u0E19\u0E40\u0E27\u0E25\u0E32\u0E2D\u0E31\u0E19\u0E23\u0E27\u0E14\u0E40\u0E23\u0E47\u0E27 \u0E42\u0E1B\u0E23\u0E14\u0E23\u0E2D\u0E2A\u0E31\u0E01\u0E04\u0E23\u0E39\u0E48\u0E41\u0E25\u0E49\u0E27\u0E25\u0E2D\u0E07\u0E2D\u0E35\u0E01\u0E04\u0E23\u0E31\u0E49\u0E07",
      "errorHandler.common-error.unauthenticated.cta": "\u0E40\u0E02\u0E49\u0E32\u0E2A\u0E39\u0E48\u0E23\u0E30\u0E1A\u0E1A",
      "errorHandler.common-error.goBack.cta": "\u0E22\u0E49\u0E2D\u0E19\u0E01\u0E25\u0E31\u0E1A",
      "errorHandler.common-error.not-found": "\u0E14\u0E39\u0E40\u0E2B\u0E21\u0E37\u0E2D\u0E19\u0E27\u0E48\u0E32\u0E44\u0E21\u0E48\u0E21\u0E35\u0E02\u0E49\u0E2D\u0E21\u0E39\u0E25\u0E19\u0E35\u0E49\u0E41\u0E25\u0E49\u0E27",
      "errorHandler.common-error.captchaRequired.verify.cta": "\u0E22\u0E37\u0E19\u0E22\u0E31\u0E19",
      "errorHandler.common-error.tryAgain.cta": "\u0E25\u0E2D\u0E07\u0E2D\u0E35\u0E01\u0E04\u0E23\u0E31\u0E49\u0E07",
      "errorHandler.requestID.tooltip.cta": "\u0E04\u0E31\u0E14\u0E25\u0E2D\u0E01 ID",
      "errorHandler.common-error.permission-denied": "\u0E04\u0E38\u0E13\u0E44\u0E21\u0E48\u0E44\u0E14\u0E49\u0E23\u0E31\u0E1A\u0E2D\u0E19\u0E38\u0E0D\u0E32\u0E15\u0E15\u0E32\u0E21\u0E17\u0E35\u0E48\u0E08\u0E33\u0E40\u0E1B\u0E47\u0E19 \u0E42\u0E1B\u0E23\u0E14\u0E02\u0E2D\u0E2D\u0E19\u0E38\u0E0D\u0E32\u0E15\u0E08\u0E32\u0E01\u0E40\u0E08\u0E49\u0E32\u0E02\u0E2D\u0E07\u0E40\u0E1E\u0E37\u0E48\u0E2D\u0E40\u0E02\u0E49\u0E32\u0E16\u0E36\u0E07",
      "errorHandler.requestID.tooltip.description": "\u0E2B\u0E32\u0E01\u0E04\u0E38\u0E13\u0E23\u0E32\u0E22\u0E07\u0E32\u0E19\u0E1B\u0E31\u0E0D\u0E2B\u0E32\u0E19\u0E35\u0E49\u0E43\u0E2B\u0E49\u0E1D\u0E48\u0E32\u0E22\u0E14\u0E39\u0E41\u0E25\u0E25\u0E39\u0E01\u0E04\u0E49\u0E32\u0E17\u0E23\u0E32\u0E1A \u0E42\u0E1B\u0E23\u0E14\u0E41\u0E08\u0E49\u0E07 ID \u0E04\u0E33\u0E02\u0E2D\u0E19\u0E35\u0E49\u0E40\u0E1E\u0E37\u0E48\u0E2D\u0E43\u0E2B\u0E49\u0E1E\u0E27\u0E01\u0E40\u0E02\u0E32\u0E15\u0E23\u0E27\u0E08\u0E2A\u0E2D\u0E1A\u0E44\u0E14\u0E49",
      "errorHandler.common-error.technicalIssue-unmappedError": "\u0E1E\u0E1A\u0E1B\u0E31\u0E0D\u0E2B\u0E32\u0E17\u0E32\u0E07\u0E40\u0E17\u0E04\u0E19\u0E34\u0E04\u0E43\u0E19\u0E2A\u0E48\u0E27\u0E19\u0E02\u0E2D\u0E07\u0E40\u0E23\u0E32 \u0E25\u0E2D\u0E07\u0E2D\u0E35\u0E01\u0E04\u0E23\u0E31\u0E49\u0E07\u0E2B\u0E23\u0E37\u0E2D\u0E23\u0E35\u0E40\u0E1F\u0E23\u0E0A",
      "errorHandler.common-error.unauthenticated": "\u0E04\u0E38\u0E13\u0E2D\u0E2D\u0E01\u0E08\u0E32\u0E01\u0E23\u0E30\u0E1A\u0E1A\u0E41\u0E25\u0E49\u0E27 \u0E42\u0E1B\u0E23\u0E14\u0E40\u0E02\u0E49\u0E32\u0E2A\u0E39\u0E48\u0E23\u0E30\u0E1A\u0E1A\u0E43\u0E2B\u0E21\u0E48\u0E40\u0E1E\u0E37\u0E48\u0E2D\u0E14\u0E33\u0E40\u0E19\u0E34\u0E19\u0E01\u0E32\u0E23\u0E15\u0E48\u0E2D",
      "errorHandler.common-error.networkError": "\u0E1E\u0E1A\u0E1B\u0E31\u0E0D\u0E2B\u0E32\u0E43\u0E19\u0E01\u0E32\u0E23\u0E40\u0E0A\u0E37\u0E48\u0E2D\u0E21\u0E15\u0E48\u0E2D\u0E01\u0E31\u0E1A\u0E40\u0E04\u0E23\u0E37\u0E2D\u0E02\u0E48\u0E32\u0E22\u0E02\u0E2D\u0E07\u0E04\u0E38\u0E13 \u0E42\u0E1B\u0E23\u0E14\u0E15\u0E23\u0E27\u0E08\u0E2A\u0E2D\u0E1A\u0E01\u0E32\u0E23\u0E40\u0E0A\u0E37\u0E48\u0E2D\u0E21\u0E15\u0E48\u0E2D\u0E2D\u0E34\u0E19\u0E40\u0E17\u0E2D\u0E23\u0E4C\u0E40\u0E19\u0E47\u0E15\u0E08\u0E32\u0E01\u0E19\u0E31\u0E49\u0E19\u0E25\u0E2D\u0E07\u0E2D\u0E35\u0E01\u0E04\u0E23\u0E31\u0E49\u0E07",
      "errorHandler.common-error.technicalIssue": "\u0E1E\u0E1A\u0E1B\u0E31\u0E0D\u0E2B\u0E32\u0E17\u0E32\u0E07\u0E40\u0E17\u0E04\u0E19\u0E34\u0E04\u0E43\u0E19\u0E2A\u0E48\u0E27\u0E19\u0E02\u0E2D\u0E07\u0E40\u0E23\u0E32 \u0E25\u0E2D\u0E07\u0E2D\u0E35\u0E01\u0E04\u0E23\u0E31\u0E49\u0E07\u0E2B\u0E23\u0E37\u0E2D\u0E23\u0E35\u0E40\u0E1F\u0E23\u0E0A",
      "errorHandler.common-error.captchaRequired": "\u0E42\u0E1B\u0E23\u0E14\u0E22\u0E37\u0E19\u0E22\u0E31\u0E19\u0E27\u0E48\u0E32\u0E04\u0E38\u0E13\u0E40\u0E1B\u0E47\u0E19\u0E21\u0E19\u0E38\u0E29\u0E22\u0E4C \u0E40\u0E1E\u0E37\u0E48\u0E2D\u0E14\u0E33\u0E40\u0E19\u0E34\u0E19\u0E01\u0E32\u0E23\u0E15\u0E48\u0E2D",
      "errorHandler.common-error.technicalIssue.generic": "\u0E1E\u0E1A\u0E1B\u0E31\u0E0D\u0E2B\u0E32\u0E17\u0E32\u0E07\u0E40\u0E17\u0E04\u0E19\u0E34\u0E04\u0E43\u0E19\u0E2A\u0E48\u0E27\u0E19\u0E02\u0E2D\u0E07\u0E40\u0E23\u0E32 \u0E25\u0E2D\u0E07\u0E2D\u0E35\u0E01\u0E04\u0E23\u0E31\u0E49\u0E07\u0E2B\u0E23\u0E37\u0E2D\u0E23\u0E35\u0E40\u0E1F\u0E23\u0E0A",
      "errorHandler.common-error.validationBasic": "\u0E02\u0E49\u0E2D\u0E21\u0E39\u0E25\u0E43\u0E19\u0E1A\u0E32\u0E07\u0E0A\u0E48\u0E2D\u0E07\u0E44\u0E21\u0E48\u0E16\u0E39\u0E01\u0E15\u0E49\u0E2D\u0E07\u0E2B\u0E23\u0E37\u0E2D\u0E44\u0E21\u0E48\u0E04\u0E23\u0E1A\u0E16\u0E49\u0E27\u0E19",
      "errorHandler.requestID.tooltip.cta.copied-success": "\u0E04\u0E31\u0E14\u0E25\u0E2D\u0E01\u0E41\u0E25\u0E49\u0E27 !"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_tl.json
var require_common_errors_tl = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_tl.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "You don't have permission to do this. Contact the site owner.",
      "errorHandler.uncommon-error.NotImplemented": "Unfortunately, this action isn\u2019t available.",
      "errorHandler.common-error.conflict": "This was recently updated somewhere else. Refresh the page to get the latest version.",
      "errorHandler.common-error.permission-denied.app": "You don't have permission to perform this action. Contact the app owner.",
      "errorHandler.common-error.permission-denied.workspace": "You don't have permission to perform this action. Contact the workspace owner.",
      "errorHandler.common-error.CheckStatus.cta": "Check Status Page",
      "errorHandler.common-error.refresh.cta": "Refresh",
      "errorHandler.common-error.noConnection": "Looks like you're offline. Check your connection and try again.",
      "errorHandler.common-error.resource-exhausted": "This action couldn't be completed due to a technical issue. Wait a minute and try again. ",
      "errorHandler.common-error.unauthenticated.cta": "Log In",
      "errorHandler.common-error.goBack.cta": "Go Back",
      "errorHandler.common-error.not-found": "It looks like this item doesn't exist anymore.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Verify",
      "errorHandler.common-error.tryAgain.cta": "Try Again",
      "errorHandler.requestID.tooltip.cta": "Copy ID",
      "errorHandler.common-error.permission-denied": "You don't have permission to perform this action. Request access from the owner.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "This action wasn't completed due to a technical issue. Try again or refresh.",
      "errorHandler.common-error.unauthenticated": "Your session has expired. Refresh the page to log in again.",
      "errorHandler.common-error.networkError": "There was an issue connecting to your network. Check your connection and try again.",
      "errorHandler.common-error.technicalIssue": "This action wasn't completed due to a technical issue.",
      "errorHandler.common-error.captchaRequired": "To continue, verify you are human.",
      "errorHandler.common-error.technicalIssue.generic": "This action wasn't completed due to a technical issue. Try again.",
      "errorHandler.common-error.validationBasic": "Some fields have invalid or missing information.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Copied!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_tr.json
var require_common_errors_tr = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_tr.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "Bu eylem i\xE7in izniniz yok. Site sahibinden izin isteyin.",
      "errorHandler.uncommon-error.NotImplemented": "Bu eylem \u015Fu anda kullan\u0131lam\u0131yor.",
      "errorHandler.common-error.conflict": "Bu sayfa ba\u015Fka bir yerde g\xFCncellendi\u011Fi i\xE7in de\u011Fi\u015Fiklikler kaydedilemiyor. De\u011Fi\u015Fikliklerinizi not al\u0131n ve sayfay\u0131 yenileyip tekrar d\xFCzenlemeyi deneyin.",
      "errorHandler.common-error.permission-denied.app": "Bu eylemi ger\xE7ekle\u015Ftirme izniniz yok. Uygulama sahibinden izin isteyin.",
      "errorHandler.common-error.permission-denied.workspace": "Bu eylemi ger\xE7ekle\u015Ftirme izniniz yok. \xC7al\u0131\u015Fma alan\u0131 sahibinden izin isteyin.",
      "errorHandler.common-error.CheckStatus.cta": "Durum Sayfas\u0131",
      "errorHandler.common-error.refresh.cta": "Yenile",
      "errorHandler.common-error.noConnection": "\xC7evrim d\u0131\u015F\u0131s\u0131n\u0131z. Ba\u011Flant\u0131n\u0131z\u0131 kontrol edip tekrar deneyin.",
      "errorHandler.common-error.resource-exhausted": "K\u0131sa s\xFCrede \xE7ok fazla eylem ger\xE7ekle\u015Ftirildi. Bir dakika bekleyip tekrar deneyin.",
      "errorHandler.common-error.unauthenticated.cta": "Giri\u015F Yap",
      "errorHandler.common-error.goBack.cta": "Geri D\xF6n",
      "errorHandler.common-error.not-found": "Bu \xF6ge art\u0131k mevcut de\u011Fil.",
      "errorHandler.common-error.captchaRequired.verify.cta": "Do\u011Frula",
      "errorHandler.common-error.tryAgain.cta": "Tekrar Dene",
      "errorHandler.requestID.tooltip.cta": "Kimli\u011Fi kopyala",
      "errorHandler.common-error.permission-denied": "Gerekli izinleriniz yok. Site sahibinden eri\u015Fim isteyin.",
      "errorHandler.requestID.tooltip.description": "Bu hatay\u0131 m\xFC\u015Fteri hizmetlerine bildirirseniz ara\u015Ft\u0131r\u0131labilmesi i\xE7in bu talep kimli\u011Fini payla\u015F\u0131n.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Bizim taraf\u0131m\u0131zda teknik bir sorun olu\u015Ftu. Tekrar deneyin veya ekran\u0131 yenileyin.",
      "errorHandler.common-error.unauthenticated": "\xC7\u0131k\u0131\u015F yapt\u0131n\u0131z. Devam etmek i\xE7in tekrar giri\u015F yap\u0131n.",
      "errorHandler.common-error.networkError": "A\u011F\u0131n\u0131za ba\u011Flan\u0131l\u0131rken bir sorun olu\u015Ftu. Ba\u011Flant\u0131n\u0131z\u0131 kontrol edip tekrar deneyin.",
      "errorHandler.common-error.technicalIssue": "Bizim taraf\u0131m\u0131zda teknik bir sorun olu\u015Ftu. Tekrar deneyin veya ekran\u0131 yenileyin.",
      "errorHandler.common-error.captchaRequired": "Devam etmek i\xE7in insan oldu\u011Funuzu do\u011Frulay\u0131n.",
      "errorHandler.common-error.technicalIssue.generic": "Bizim taraf\u0131m\u0131zda teknik bir sorun olu\u015Ftu. Tekrar deneyin veya ekran\u0131 yenileyin.",
      "errorHandler.common-error.validationBasic": "Baz\u0131 alanlardaki bilgiler ge\xE7ersiz veya eksik.",
      "errorHandler.requestID.tooltip.cta.copied-success": "Kopyaland\u0131!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_uk.json
var require_common_errors_uk = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_uk.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "\u0423 \u0432\u0430\u0441 \u043D\u0435\u043C\u0430\u0454 \u0434\u043E\u0437\u0432\u043E\u043B\u0443 \u0432\u0438\u043A\u043E\u043D\u0443\u0432\u0430\u0442\u0438 \u0446\u044E \u0434\u0456\u044E. \u041F\u043E\u043F\u0440\u043E\u0441\u0456\u0442\u044C \u0432\u043B\u0430\u0441\u043D\u0438\u043A\u0430 \u0441\u0430\u0439\u0442\u0443 \u043D\u0430\u0434\u0430\u0442\u0438 \u0432\u0430\u043C \u0434\u043E\u0441\u0442\u0443\u043F.",
      "errorHandler.uncommon-error.NotImplemented": "\u0426\u044F \u0434\u0456\u044F \u0437\u0430\u0440\u0430\u0437 \u043D\u0435\u0434\u043E\u0441\u0442\u0443\u043F\u043D\u0430.",
      "errorHandler.common-error.conflict": "\u041D\u0435 \u0432\u0434\u0430\u0454\u0442\u044C\u0441\u044F \u0437\u0431\u0435\u0440\u0435\u0433\u0442\u0438 \u0437\u043C\u0456\u043D\u0438, \u0431\u043E \u0446\u044E \u0441\u0442\u043E\u0440\u0456\u043D\u043A\u0443 \u043E\u043D\u043E\u0432\u043B\u0435\u043D\u043E \u0432 \u0456\u043D\u0448\u043E\u043C\u0443 \u043C\u0456\u0441\u0446\u0456. \u0417\u0430\u043F\u0438\u0448\u0456\u0442\u044C \u0437\u043C\u0456\u043D\u0438, \u043E\u043D\u043E\u0432\u0456\u0442\u044C \u0456 \u0441\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0432\u0456\u0434\u0440\u0435\u0434\u0430\u0433\u0443\u0432\u0430\u0442\u0438 \u0437\u043D\u043E\u0432\u0443.",
      "errorHandler.common-error.permission-denied.app": "\u0423 \u0432\u0430\u0441 \u043D\u0435\u043C\u0430\u0454 \u0434\u043E\u0437\u0432\u043E\u043B\u0443 \u0432\u0438\u043A\u043E\u043D\u0443\u0432\u0430\u0442\u0438 \u0446\u044E \u0434\u0456\u044E. \u041F\u043E\u043F\u0440\u043E\u0441\u0456\u0442\u044C \u0432\u043B\u0430\u0441\u043D\u0438\u043A\u0430 \u0434\u043E\u0434\u0430\u0442\u043A\u0430 \u043D\u0430\u0434\u0430\u0442\u0438 \u0432\u0430\u043C \u0434\u043E\u0441\u0442\u0443\u043F.",
      "errorHandler.common-error.permission-denied.workspace": "\u0423 \u0432\u0430\u0441 \u043D\u0435\u043C\u0430\u0454 \u0434\u043E\u0437\u0432\u043E\u043B\u0443 \u0432\u0438\u043A\u043E\u043D\u0443\u0432\u0430\u0442\u0438 \u0446\u044E \u0434\u0456\u044E. \u041F\u043E\u043F\u0440\u043E\u0441\u0456\u0442\u044C \u0432\u043B\u0430\u0441\u043D\u0438\u043A\u0430 \u0440\u043E\u0431\u043E\u0447\u043E\u0433\u043E \u043F\u0440\u043E\u0441\u0442\u043E\u0440\u0443 \u043D\u0430\u0434\u0430\u0442\u0438 \u0432\u0430\u043C \u0434\u043E\u0441\u0442\u0443\u043F.",
      "errorHandler.common-error.CheckStatus.cta": "\u041F\u0435\u0440\u0435\u0432\u0456\u0440\u0442\u0435 \u0441\u0442\u043E\u0440\u0456\u043D\u043A\u0443 \u0441\u0442\u0430\u0442\u0443\u0441\u0443",
      "errorHandler.common-error.refresh.cta": "\u041F\u0435\u0440\u0435\u0437\u0430\u0432\u0430\u043D\u0442\u0430\u0436\u0438\u0442\u0438",
      "errorHandler.common-error.noConnection": "\u0421\u0445\u043E\u0436\u0435, \u0449\u043E \u0432\u0438 \u043D\u0435 \u0432 \u043C\u0435\u0440\u0435\u0436\u0456. \u041F\u0435\u0440\u0435\u0432\u0456\u0440\u0442\u0435 \u043F\u0456\u0434\u043A\u043B\u044E\u0447\u0435\u043D\u043D\u044F \u0434\u043E \u0456\u043D\u0442\u0435\u0440\u043D\u0435\u0442\u0443 \u0442\u0430 \u043F\u043E\u0432\u0442\u043E\u0440\u0456\u0442\u044C \u0441\u043F\u0440\u043E\u0431\u0443.",
      "errorHandler.common-error.resource-exhausted": "\u0417\u0430\u0431\u0430\u0433\u0430\u0442\u043E \u0434\u0456\u0439 \u0432\u0438\u043A\u043E\u043D\u0430\u043D\u043E \u0437\u0430\u0448\u0432\u0438\u0434\u043A\u043E. \u041F\u043E\u0447\u0435\u043A\u0430\u0439\u0442\u0435 \u0445\u0432\u0438\u043B\u0438\u043D\u0443 \u0442\u0430 \u0441\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0437\u043D\u043E\u0432\u0443.",
      "errorHandler.common-error.unauthenticated.cta": "\u0423\u0432\u0456\u0439\u0434\u0456\u0442\u044C",
      "errorHandler.common-error.goBack.cta": "\u041F\u043E\u0432\u0435\u0440\u043D\u0443\u0442\u0438\u0441\u044F",
      "errorHandler.common-error.not-found": "\u0421\u0445\u043E\u0436\u0435, \u0449\u043E \u0446\u044C\u043E\u0433\u043E \u0431\u0456\u043B\u044C\u0448\u0435 \u043D\u0435 \u0456\u0441\u043D\u0443\u0454.",
      "errorHandler.common-error.captchaRequired.verify.cta": "\u041F\u0456\u0434\u0442\u0432\u0435\u0440\u0434\u0438\u0442\u0438",
      "errorHandler.common-error.tryAgain.cta": "\u0421\u043F\u0440\u043E\u0431\u0443\u0432\u0430\u0442\u0438 \u0437\u043D\u043E\u0432\u0443",
      "errorHandler.requestID.tooltip.cta": "\u041A\u043E\u043F\u0456\u044E\u0432\u0430\u0442\u0438 \u0456\u0434\u0435\u043D\u0442\u0438\u0444\u0456\u043A\u0430\u0442\u043E\u0440",
      "errorHandler.common-error.permission-denied": "\u0423 \u0432\u0430\u0441 \u043D\u0435\u043C\u0430\u0454 \u043F\u043E\u0442\u0440\u0456\u0431\u043D\u0438\u0445 \u0434\u043E\u0437\u0432\u043E\u043B\u0456\u0432. \u041F\u043E\u043F\u0440\u043E\u0441\u0456\u0442\u044C \u0432\u043B\u0430\u0441\u043D\u0438\u043A\u0430 \u043D\u0430\u0434\u0430\u0442\u0438 \u0432\u0430\u043C \u0434\u043E\u0441\u0442\u0443\u043F.",
      "errorHandler.requestID.tooltip.description": "If the issue continues, contact Customer Care and share this Request ID with them so they can investigate.",
      "errorHandler.common-error.technicalIssue-unmappedError": "\u0423 \u043D\u0430\u0441 \u0432\u0438\u043D\u0438\u043A\u043B\u0430 \u0442\u0435\u0445\u043D\u0456\u0447\u043D\u0430 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430. \u0421\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0437\u043D\u043E\u0432\u0443 \u0430\u0431\u043E \u043E\u043D\u043E\u0432\u0456\u0442\u044C.",
      "errorHandler.common-error.unauthenticated": "\u0412\u0438 \u0432\u0438\u0439\u0448\u043B\u0438 \u0437 \u0430\u043A\u0430\u0443\u043D\u0442\u0430. \u0423\u0432\u0456\u0439\u0434\u0456\u0442\u044C, \u0449\u043E\u0431 \u043F\u0440\u043E\u0434\u043E\u0432\u0436\u0438\u0442\u0438.",
      "errorHandler.common-error.networkError": "\u0412\u0438\u043D\u0438\u043A\u043B\u0430 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430 \u0437 \u043F\u0456\u0434\u043A\u043B\u044E\u0447\u0435\u043D\u043D\u044F\u043C \u0434\u043E \u0432\u0430\u0448\u043E\u0457 \u043C\u0435\u0440\u0435\u0436\u0456. \u041F\u0435\u0440\u0435\u0432\u0456\u0440\u0442\u0435 \u043F\u0456\u0434\u043A\u043B\u044E\u0447\u0435\u043D\u043D\u044F \u0434\u043E \u0456\u043D\u0442\u0435\u0440\u043D\u0435\u0442\u0443 \u0442\u0430 \u043F\u043E\u0432\u0442\u043E\u0440\u0456\u0442\u044C \u0441\u043F\u0440\u043E\u0431\u0443.",
      "errorHandler.common-error.technicalIssue": "\u0423 \u043D\u0430\u0441 \u0432\u0438\u043D\u0438\u043A\u043B\u0430 \u0442\u0435\u0445\u043D\u0456\u0447\u043D\u0430 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430. \u0421\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0437\u043D\u043E\u0432\u0443 \u0430\u0431\u043E \u043E\u043D\u043E\u0432\u0456\u0442\u044C.",
      "errorHandler.common-error.captchaRequired": "\u0429\u043E\u0431 \u043F\u0440\u043E\u0434\u043E\u0432\u0436\u0438\u0442\u0438, \u043F\u0456\u0434\u0442\u0432\u0435\u0440\u0434\u044C\u0442\u0435, \u0449\u043E \u0432\u0438 \u043B\u044E\u0434\u0438\u043D\u0430.",
      "errorHandler.common-error.technicalIssue.generic": "\u0423 \u043D\u0430\u0441 \u0432\u0438\u043D\u0438\u043A\u043B\u0430 \u0442\u0435\u0445\u043D\u0456\u0447\u043D\u0430 \u043F\u0440\u043E\u0431\u043B\u0435\u043C\u0430. \u0421\u043F\u0440\u043E\u0431\u0443\u0439\u0442\u0435 \u0437\u043D\u043E\u0432\u0443 \u0430\u0431\u043E \u043E\u043D\u043E\u0432\u0456\u0442\u044C.",
      "errorHandler.common-error.validationBasic": "\u0406\u043D\u0444\u043E\u0440\u043C\u0430\u0446\u0456\u044F \u0432 \u0434\u0435\u044F\u043A\u0438\u0445 \u043F\u043E\u043B\u044F\u0445 \u0432\u0456\u0434\u0441\u0443\u0442\u043D\u044F \u0430\u0431\u043E \u043D\u0435\u0434\u0456\u0439\u0441\u043D\u0430.",
      "errorHandler.requestID.tooltip.cta.copied-success": "\u0421\u043A\u043E\u043F\u0456\u0439\u043E\u0432\u0430\u043D\u043E"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_vi.json
var require_common_errors_vi = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_vi.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "B\u1EA1n kh\xF4ng c\xF3 quy\u1EC1n th\u1EF1c hi\u1EC7n thao t\xE1c n\xE0y. Y\xEAu c\u1EA7u quy\u1EC1n truy c\u1EADp t\u1EEB ch\u1EE7 trang web.",
      "errorHandler.uncommon-error.NotImplemented": "H\xE0nh \u0111\u1ED9ng n\xE0y hi\u1EC7n kh\xF4ng kh\u1EA3 d\u1EE5ng.",
      "errorHandler.common-error.conflict": "Kh\xF4ng th\u1EC3 l\u01B0u thay \u0111\u1ED5i v\xEC trang n\xE0y \u0111\xE3 \u0111\u01B0\u1EE3c c\u1EADp nh\u1EADt \u1EDF m\u1ED9t n\u01A1i kh\xE1c. Ghi ch\xFA l\u1EA1i c\xE1c thay \u0111\u1ED5i c\u1EE7a b\u1EA1n, sau \u0111\xF3 l\xE0m m\u1EDBi v\xE0 th\u1EED ch\u1EC9nh s\u1EEDa l\u1EA1i.",
      "errorHandler.common-error.permission-denied.app": "B\u1EA1n kh\xF4ng c\xF3 quy\u1EC1n th\u1EF1c hi\u1EC7n h\xE0nh \u0111\u1ED9ng n\xE0y. H\xE3y c\u1EA7u quy\u1EC1n truy c\u1EADp t\u1EEB ch\u1EE7 s\u1EDF h\u1EEFu \u1EE9ng d\u1EE5ng.",
      "errorHandler.common-error.permission-denied.workspace": "B\u1EA1n kh\xF4ng c\xF3 quy\u1EC1n th\u1EF1c hi\u1EC7n h\xE0nh \u0111\u1ED9ng n\xE0y. H\xE3y y\xEAu c\u1EA7u quy\u1EC1n truy c\u1EADp t\u1EEB ch\u1EE7 s\u1EDF h\u1EEFu v\xF9ng l\xE0m vi\u1EC7c.",
      "errorHandler.common-error.CheckStatus.cta": "Ki\u1EC3m tra trang tr\u1EA1ng th\xE1i",
      "errorHandler.common-error.refresh.cta": "L\xE0m m\u1EDBi",
      "errorHandler.common-error.noConnection": "C\xF3 v\u1EBB b\u1EA1n \u0111ang ngo\u1EA1i tuy\u1EBFn. H\xE3y ki\u1EC3m tra k\u1EBFt n\u1ED1i r\u1ED3i th\u1EED l\u1EA1i.",
      "errorHandler.common-error.resource-exhausted": "\u0110\xE3 th\u1EF1c hi\u1EC7n qu\xE1 nhi\u1EC1u thao t\xE1c trong th\u1EDDi gian ng\u1EAFn. H\xE3y \u0111\u1EE3i m\u1ED9t l\xE1t v\xE0 th\u1EED l\u1EA1i.",
      "errorHandler.common-error.unauthenticated.cta": "\u0110\u0103ng nh\u1EADp",
      "errorHandler.common-error.goBack.cta": "Quay l\u1EA1i",
      "errorHandler.common-error.not-found": "C\xF3 v\u1EBB nh\u01B0 n\u1ED9i dung n\xE0y kh\xF4ng t\u1ED3n t\u1EA1i n\u1EEFa.",
      "errorHandler.common-error.captchaRequired.verify.cta": "x\xE1c minh",
      "errorHandler.common-error.tryAgain.cta": "Th\u1EED l\u1EA1i",
      "errorHandler.requestID.tooltip.cta": "Sao ch\xE9p ID",
      "errorHandler.common-error.permission-denied": "B\u1EA1n kh\xF4ng c\xF3 \u0111\u1EE7 c\xE1c quy\u1EC1n c\u1EA7n thi\u1EBFt. H\xE3y y\xEAu c\u1EA7u quy\u1EC1n truy c\u1EADp t\u1EEB ch\u1EE7 s\u1EDF h\u1EEFu.",
      "errorHandler.requestID.tooltip.description": "N\u1EBFu b\u1EA1n b\xE1o c\xE1o l\u1ED7i n\xE0y cho b\u1ED9 ph\u1EADn Ch\u0103m s\xF3c kh\xE1ch h\xE0ng, h\xE3y chia s\u1EBB ID y\xEAu c\u1EA7u n\xE0y \u0111\u1EC3 h\u1ECD c\xF3 th\u1EC3 \u0111i\u1EC1u tra.",
      "errorHandler.common-error.technicalIssue-unmappedError": "Ch\xFAng t\xF4i \u0111\xE3 g\u1EB7p ph\u1EA3i v\u1EA5n \u0111\u1EC1 k\u1EF9 thu\u1EADt. H\xE3y th\u1EED l\u1EA1i ho\u1EB7c l\xE0m m\u1EDBi.",
      "errorHandler.common-error.unauthenticated": "B\u1EA1n \u0111\xE3 \u0111\u0103ng xu\u1EA5t. \u0110\u0103ng nh\u1EADp l\u1EA1i \u0111\u1EC3 ti\u1EBFp t\u1EE5c.",
      "errorHandler.common-error.networkError": "\u0110\xE3 x\u1EA3y ra s\u1EF1 c\u1ED1 khi k\u1EBFt n\u1ED1i v\u1EDBi m\u1EA1ng c\u1EE7a b\u1EA1n. H\xE3y ki\u1EC3m tra k\u1EBFt n\u1ED1i r\u1ED3i th\u1EED l\u1EA1i.",
      "errorHandler.common-error.technicalIssue": "Ch\xFAng t\xF4i \u0111\xE3 g\u1EB7p ph\u1EA3i v\u1EA5n \u0111\u1EC1 k\u1EF9 thu\u1EADt. H\xE3y th\u1EED l\u1EA1i ho\u1EB7c l\xE0m m\u1EDBi.",
      "errorHandler.common-error.captchaRequired": "\u0110\u1EC3 ti\u1EBFp t\u1EE5c, h\xE3y x\xE1c minh b\u1EA1n l\xE0 con ng\u01B0\u1EDDi.",
      "errorHandler.common-error.technicalIssue.generic": "Ch\xFAng t\xF4i \u0111\xE3 g\u1EB7p ph\u1EA3i v\u1EA5n \u0111\u1EC1 k\u1EF9 thu\u1EADt. H\xE3y th\u1EED l\u1EA1i ho\u1EB7c l\xE0m m\u1EDBi.",
      "errorHandler.common-error.validationBasic": "M\u1ED9t s\u1ED1 tr\u01B0\u1EDDng c\xF3 th\xF4ng tin kh\xF4ng h\u1EE3p l\u1EC7 ho\u1EB7c thi\u1EBFu th\xF4ng tin.",
      "errorHandler.requestID.tooltip.cta.copied-success": "\u0110\xE3 sao ch\xE9p!"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_zh.json
var require_common_errors_zh = __commonJS({
  "../../node_modules/@wix/error-handler/dist/esm/assets/locale/common-errors/common-errors_zh.json"(exports2, module2) {
    module2.exports = {
      "errorHandler.common-error.permission-denied.site": "\u60A8\u6C92\u6709\u57F7\u884C\u6B64\u64CD\u4F5C\u7684\u6B0A\u9650\u3002\u8ACB\u5411\u7DB2\u7AD9\u8CA0\u8CAC\u4EBA\u8ACB\u6C42\u5B58\u53D6\u6B0A\u9650\u3002",
      "errorHandler.uncommon-error.NotImplemented": "\u6B64\u64CD\u4F5C\u66AB\u4E0D\u53EF\u7528\u3002",
      "errorHandler.common-error.conflict": "\u7121\u6CD5\u5132\u5B58\u8B8A\u66F4\uFF0C\u56E0\u70BA\u9801\u9762\u5DF2\u5728\u5225\u8655\u66F4\u65B0\u3002\u8ACB\u8A18\u4E0B\u8B8A\u66F4\uFF0C\u7136\u5F8C\u91CD\u65B0\u6574\u7406\u4E26\u5617\u8A66\u518D\u6B21\u7DE8\u8F2F\u3002",
      "errorHandler.common-error.permission-denied.app": "\u60A8\u6C92\u6709\u6B0A\u9650\u57F7\u884C\u9019\u500B\u64CD\u4F5C\u3002\u8ACB\u5411\u61C9\u7528\u7A0B\u5F0F\u8CA0\u8CAC\u4EBA\u8ACB\u6C42\u5B58\u53D6\u6B0A\u9650\u3002",
      "errorHandler.common-error.permission-denied.workspace": "\u60A8\u6C92\u6709\u6B0A\u9650\u57F7\u884C\u9019\u500B\u64CD\u4F5C\u3002\u8ACB\u5411\u5DE5\u4F5C\u7A7A\u9593\u8CA0\u8CAC\u4EBA\u8ACB\u6C42\u5B58\u53D6\u6B0A\u9650\u3002",
      "errorHandler.common-error.CheckStatus.cta": "\u6AA2\u67E5\u72C0\u614B\u9801\u9762",
      "errorHandler.common-error.refresh.cta": "\u91CD\u65B0\u6574\u7406",
      "errorHandler.common-error.noConnection": "\u60A8\u4F3C\u4E4E\u5DF2\u96E2\u7DDA\u3002\u8ACB\u6AA2\u67E5\u9023\u7DDA\u4E26\u518D\u8A66\u4E00\u6B21\u3002",
      "errorHandler.common-error.resource-exhausted": "\u904E\u5FEB\u5B8C\u6210\u904E\u591A\u64CD\u4F5C\u3002\u8ACB\u7A0D\u5019\u7247\u523B\uFF0C\u7136\u5F8C\u518D\u8A66\u4E00\u6B21\u3002",
      "errorHandler.common-error.unauthenticated.cta": "\u767B\u5165",
      "errorHandler.common-error.goBack.cta": "\u8FD4\u56DE",
      "errorHandler.common-error.not-found": "\u9019\u4F3C\u4E4E\u4E0D\u518D\u5B58\u5728\u3002",
      "errorHandler.common-error.captchaRequired.verify.cta": "\u9A57\u8B49",
      "errorHandler.common-error.tryAgain.cta": "\u518D\u8A66\u4E00\u6B21",
      "errorHandler.requestID.tooltip.cta": "\u8907\u88FD ID",
      "errorHandler.common-error.permission-denied": "\u60A8\u6C92\u6709\u5FC5\u8981\u7684\u6B0A\u9650\u3002\u8ACB\u5411\u8CA0\u8CAC\u4EBA\u8ACB\u6C42\u5B58\u53D6\u6B0A\u9650\u3002",
      "errorHandler.requestID.tooltip.description": "\u5982\u679C\u60A8\u5411\u9867\u5BA2\u670D\u52D9\u5831\u544A\u6B64\u932F\u8AA4\uFF0C\u8ACB\u63D0\u4F9B\u6B64\u8ACB\u6C42 ID \u4EE5\u4FBF\u8ABF\u67E5\u3002",
      "errorHandler.common-error.technicalIssue-unmappedError": "\u6211\u5011\u9019\u908A\u767C\u751F\u6280\u8853\u554F\u984C\u3002\u8ACB\u518D\u8A66\u4E00\u6B21\u6216\u8005\u91CD\u65B0\u6574\u7406\u3002",
      "errorHandler.common-error.unauthenticated": "\u60A8\u5DF2\u767B\u51FA\u3002\u82E5\u8981\u7E7C\u7E8C\uFF0C\u8ACB\u91CD\u65B0\u767B\u5165\u3002",
      "errorHandler.common-error.networkError": "\u9023\u7D50\u5230\u7DB2\u8DEF\u6642\u767C\u751F\u554F\u984C\u3002\u8ACB\u6AA2\u67E5\u9023\u7DDA\u4E26\u518D\u8A66\u4E00\u6B21\u3002",
      "errorHandler.common-error.technicalIssue": "\u6211\u5011\u9019\u908A\u767C\u751F\u6280\u8853\u554F\u984C\u3002\u8ACB\u518D\u8A66\u4E00\u6B21\u6216\u8005\u91CD\u65B0\u6574\u7406\u3002",
      "errorHandler.common-error.captchaRequired": "\u82E5\u8981\u7E7C\u7E8C\uFF0C\u8ACB\u9A57\u8B49\u60A8\u662F\u4EBA\u985E\u3002",
      "errorHandler.common-error.technicalIssue.generic": "\u6211\u5011\u9019\u908A\u767C\u751F\u6280\u8853\u554F\u984C\u3002\u8ACB\u518D\u8A66\u4E00\u6B21\u6216\u8005\u91CD\u65B0\u6574\u7406\u3002",
      "errorHandler.common-error.validationBasic": "\u67D0\u4E9B\u6B04\u4F4D\u5177\u6709\u7121\u6548\u8CC7\u8A0A\u6216\u7F3A\u5C11\u8CC7\u8A0A\u3002",
      "errorHandler.requestID.tooltip.cta.copied-success": "\u5DF2\u8907\u88FD\uFF01"
    };
  }
});

// ../../node_modules/@wix/error-handler/dist/esm/state/commonErrorsMapping.js
var commonErrorsMapping_exports = {};
__export(commonErrorsMapping_exports, {
  createCommonErrorsMapping: () => createCommonErrorsMapping,
  createGetNetworkErrorShowErrorProps: () => createGetNetworkErrorShowErrorProps,
  getConnectionErrorShowErrorProps: () => getConnectionErrorShowErrorProps,
  getDefaultErrorShowErrorProps: () => getDefaultErrorShowErrorProps,
  setCommonErrors: () => setCommonErrors
});
function createCommonErrorsMapping(t, state) {
  const {
    webWindow
  } = state;
  const location2 = webWindow == null ? void 0 : webWindow.location;
  return {
    applicationError: {
      CAPTCHA_REQUIRED: () => ({
        message: t("errorHandler.common-error.captchaRequired")
      })
    },
    statusCodeError: {
      UNAUTHENTICATED: () => ({
        message: t("errorHandler.common-error.unauthenticated"),
        action: webWindow ? {
          text: t("errorHandler.common-error.unauthenticated.cta"),
          onClick: () => {
            const url = new URL("https://users.wix.com/signin");
            url.searchParams.set("redirectTo", window.location.href);
            webWindow.open == null || webWindow.open(url.href, "_blank");
          }
        } : void 0
      }),
      INVALID_ARGUMENT: () => ({
        message: t("errorHandler.common-error.validationBasic")
      }),
      PERMISSION_DENIED: () => ({
        message: t("errorHandler.common-error.permission-denied.site")
      }),
      NOT_FOUND: () => ({
        message: t("errorHandler.common-error.not-found")
      }),
      ALREADY_EXISTS: () => ({
        message: t("errorHandler.common-error.conflict"),
        action: location2 ? {
          text: t("errorHandler.common-error.refresh.cta"),
          onClick: () => {
            location2.reload();
          }
        } : void 0
      }),
      UNAVAILABLE: () => ({
        message: t("errorHandler.common-error.technicalIssue")
      }),
      RESOURCE_EXHAUSTED: () => ({
        message: t("errorHandler.common-error.resource-exhausted")
      }),
      BAD_GATEWAY: () => ({
        message: t("errorHandler.common-error.technicalIssue")
      }),
      GATEWAY_TIMEOUT: () => ({
        message: t("errorHandler.common-error.technicalIssue")
      }),
      INTERNAL: () => ({
        message: t("errorHandler.common-error.technicalIssue")
      })
    }
  };
}
function getConnectionErrorShowErrorProps(t) {
  return {
    message: t("errorHandler.common-error.noConnection")
  };
}
function getDefaultErrorShowErrorProps(t) {
  return {
    message: t("errorHandler.common-error.technicalIssue")
  };
}
function createGetNetworkErrorShowErrorProps(t) {
  return (errorAccessor) => {
    var _errorAccessor$except;
    const lcExceptionMessage = (_errorAccessor$except = errorAccessor.exceptionMessage) == null ? void 0 : _errorAccessor$except.toLowerCase();
    if (lcExceptionMessage === "network error") {
      return {
        message: t("errorHandler.common-error.networkError")
      };
    }
    if (lcExceptionMessage === "request aborted" || lcExceptionMessage === "timeout exceeded") {
      return {
        message: t("errorHandler.common-error.technicalIssue.generic")
      };
    }
    return {
      message: t("errorHandler.common-error.technicalIssue-unmappedError")
    };
  };
}
async function setCommonErrors(state) {
  const {
    translations,
    errorMonitor
  } = state;
  try {
    const t = await translations.getTranslateFn();
    state.commonErrors = {
      commonErrorsMap: createCommonErrorsMapping(t, state),
      connectionErrorShowErrorProps: getConnectionErrorShowErrorProps(t),
      defaultErrorShowErrorProps: getDefaultErrorShowErrorProps(t),
      getNetworkErrorShowErrorProps: createGetNetworkErrorShowErrorProps(t)
    };
  } catch (e) {
    errorMonitor.captureException(e);
  }
}
var init_commonErrorsMapping = __esm({
  "../../node_modules/@wix/error-handler/dist/esm/state/commonErrorsMapping.js"() {
    "use strict";
  }
});

// src/exports/testkit/index.ts
var testkit_exports = {};
__export(testkit_exports, {
  StandaloneEssentialsTestkit: () => StandaloneEssentialsTestkit,
  TestContext: () => StandaloneEssentialsTestContext,
  testkit: () => testkit
});
module.exports = __toCommonJS(testkit_exports);

// ../../node_modules/@wix/wix-i18n-config/dist/esm/icu/icu.js
var import_intl_messageformat = __toESM(require("intl-messageformat"));

// ../../node_modules/@wix/wix-i18n-config/dist/esm/icu/utils.js
function getLastOfPath(object, path, Empty) {
  function cleanKey(key) {
    return key && key.indexOf("###") > -1 ? key.replace(/###/g, ".") : key;
  }
  function canNotTraverseDeeper() {
    return !object || typeof object === "string";
  }
  const stack = typeof path !== "string" ? [].concat(path) : path.split(".");
  while (stack.length > 1) {
    if (canNotTraverseDeeper())
      return {};
    const key = cleanKey(stack.shift());
    if (!object[key] && Empty)
      object[key] = new Empty();
    object = object[key];
  }
  if (canNotTraverseDeeper())
    return {};
  return {
    obj: object,
    k: cleanKey(stack.shift())
  };
}
function setPath(object, path, newValue) {
  const { obj, k } = getLastOfPath(object, path, Object);
  obj[k] = newValue;
}
function getPath(object, path) {
  const { obj, k } = getLastOfPath(object, path);
  if (!obj)
    return void 0;
  return obj[k];
}
var arr = [];
var each = arr.forEach;
var slice = arr.slice;
function defaults(obj) {
  each.call(slice.call(arguments, 1), function(source) {
    if (source) {
      for (const prop in source) {
        if (obj[prop] === void 0)
          obj[prop] = source[prop];
      }
    }
  });
  return obj;
}

// ../../node_modules/@wix/wix-i18n-config/dist/esm/icu/icu.js
function getDefaults() {
  return {
    memoize: true,
    memoizeFallback: false,
    bindI18n: "",
    bindI18nStore: void 0,
    parseErrorHandler: (_err, _key, res) => {
      return res;
    }
  };
}
var ICU = class {
  constructor(options) {
    this.type = "i18nFormat";
    this.mem = {};
    this.init(null, options);
  }
  init(i18next2, options) {
    const i18nextOptions = i18next2 && i18next2.options && i18next2.options.i18nFormat || {};
    this.options = defaults(i18nextOptions, options, this.options || {}, getDefaults());
    this.formats = this.options?.formats;
    if (i18next2) {
      const { bindI18n, bindI18nStore, memoize } = this.options;
      i18next2.IntlMessageFormat = import_intl_messageformat.default;
      i18next2.ICU = this;
      if (memoize) {
        if (bindI18n) {
          i18next2.on(bindI18n, () => this.clearCache());
        }
        if (bindI18nStore) {
          i18next2.store.on(bindI18nStore, () => this.clearCache());
        }
      }
    }
    if (this.options?.localeData) {
      if (Object.prototype.toString.apply(this.options.localeData) === "[object Array]") {
        this.options.localeData.forEach((ld) => this.addLocaleData(ld));
      } else {
        this.addLocaleData(this.options.localeData);
      }
    }
  }
  addLocaleData(data) {
    const locales = Array.isArray(data) ? data : [data];
    locales.forEach((localeData) => {
      if (localeData && localeData.locale) {
        import_intl_messageformat.default.__addLocaleData(localeData);
      }
    });
  }
  addUserDefinedFormats(formats) {
    this.formats = this.formats ? { ...this.formats, ...formats } : formats;
  }
  parse(res, options, lng, ns, key, info) {
    const hadSuccessfulLookup = info && info.resolved && info.resolved.res;
    const memKey = this.options?.memoize && `${lng}.${ns}.${key.replace(/\./g, "###")}`;
    let fc;
    if (this.options?.memoize) {
      fc = getPath(this.mem, memKey);
    }
    try {
      if (!fc) {
        fc = new import_intl_messageformat.default(res, lng, this.formats);
        if (this.options?.memoize && (this.options.memoizeFallback || !info || hadSuccessfulLookup)) {
          setPath(this.mem, memKey, fc);
        }
      }
      return fc.format(options);
    } catch (err) {
      return this.options?.parseErrorHandler?.(err, key, res, options);
    }
  }
  addLookupKeys(finalKeys) {
    return finalKeys;
  }
  clearCache() {
    this.mem = {};
  }
};
ICU.type = "i18nFormat";
var icu_default = ICU;

// ../../node_modules/@wix/wix-i18n-config/dist/esm/index.js
var import_i18next2 = require("i18next");
var import_react_i18next = require("react-i18next");

// ../../node_modules/@wix/wix-i18n-config/dist/esm/i18n.js
var import_i18next = __toESM(require("i18next"));
function i18n({ locale, asyncMessagesLoader: asyncMessagesLoader2, messages, useSuspense = false, wait, disableAutoInit = false, bindI18n }, { icu } = {}) {
  const config = {
    // Initial language
    lng: locale,
    fallbackLng: "en",
    // Don't use a key separator (no support for nested translation objects)
    keySeparator: false,
    react: {
      useSuspense,
      bindI18n,
      wait
    },
    // @ts-expect-error It's here for backward campatibility - can be removed after old `wix-i18n-config` will no longer be in use.
    // Basically when BM can be loaded without this config - you're good to go.
    _polyfill: { isLoading: false }
  };
  const i18nInstance = import_i18next.default.createInstance(disableAutoInit ? config : void 0);
  if (icu) {
    i18nInstance.use(icu);
  }
  if (asyncMessagesLoader2) {
    i18nInstance.use({
      type: "backend",
      read: async (language, _namespace, callback) => {
        try {
          const translation = await asyncMessagesLoader2(language);
          return callback(null, translation);
        } catch (err) {
          return callback(err, null);
        }
      }
    });
  }
  if (messages) {
    config.resources = { [locale]: { translation: messages } };
  }
  if (!disableAutoInit) {
    i18nInstance.init(config);
  }
  return i18nInstance;
}

// ../../node_modules/@wix/wix-i18n-config/dist/esm/initI18n.js
function initI18n({ icuOptions = {}, ...options }) {
  const { parseErrorHandler = void 0 } = options;
  const icu = new icu_default({
    parseErrorHandler,
    bindI18n: options.bindI18n,
    ...icuOptions
  });
  return i18n(options, { icu });
}

// ../../node_modules/@wix/http-client-testkit/dist/es/src/http-client-mock.js
var import_lodash2 = __toESM(require_lodash());

// ../../node_modules/@wix/http-client-testkit/dist/es/src/errors.js
var import_http_client = __toESM(require_index_node());
var import_dedent_js = __toESM(require_lib());
var NO_SCENARIO_FOUND = "NO_SCENARIO_FOUND";
var SCENARIO_ERROR = "SCENARIO_ERROR";
var CANCEL_ERROR = "CANCEL_ERROR";
var NoMockFoundError = class _NoMockFoundError extends Error {
  constructor(options, scenarios) {
    super(getNoScenarioErrorMessage(options, scenarios));
    this.response = {
      status: 404,
      data: { msg: NO_SCENARIO_FOUND }
    };
    Object.setPrototypeOf(this, _NoMockFoundError.prototype);
  }
};
var CancelError = class _CancelError extends import_http_client.HttpError {
  constructor(errorCode, config, request, response, message, tokenId) {
    super(createHttpError({
      name: CANCEL_ERROR,
      code: CANCEL_ERROR,
      message: _CancelError.getCancellationMessage(message, tokenId),
      config,
      request,
      response
    }));
    Object.setPrototypeOf(this, _CancelError.prototype);
  }
  static getCancellationMessage(reason, tokenId) {
    return `${CANCEL_ERROR} with reason ${reason} and token ${tokenId}`;
  }
};
function createCancelError(...args) {
  return new CancelError(...args);
}
var ScenarioError = class _ScenarioError extends import_http_client.HttpError {
  constructor(errorCode, config, request, response) {
    super(createHttpError({
      name: SCENARIO_ERROR,
      message: `Request failed with status code ${errorCode}`,
      code: SCENARIO_ERROR,
      config,
      request,
      response
    }));
    Object.setPrototypeOf(this, _ScenarioError.prototype);
  }
};
function createScenarioError(...args) {
  return new ScenarioError(...args);
}
function createHttpError({ code, config, request, response, name, message }) {
  return (0, import_http_client.createHttpError)({
    code,
    config,
    request,
    response,
    name,
    message,
    stack: new Error().stack,
    toJSON: () => ({}),
    __CANCEL__: false
  });
}
function getNoScenarioErrorMessage(options, scenarios) {
  return (0, import_dedent_js.default)(`No mock found for scenario:

${getCurrentScenarioMessage(options)}



HINT: Any scenario you define and provide to either createHttpClientMock or HttpMockServer

will be used ONCE (by the first matched request). In case you want a scenario to be persisted

use '.persist()'. For more details see http-client-testkit docs.



Available scenarios:
${getAvailableScenariosMessage(scenarios)}`);
}
function getCurrentScenarioMessage(options) {
  const data = options.data ? "\ndata: " + JSON.stringify(options.data) : "";
  const params = options.params ? `
params: "${options.params}"` : "";
  const headers = Object.keys(options.headers || {}).length ? "\nheaders:" + JSON.stringify(options.headers) : "";
  return (0, import_dedent_js.default)(`url: "${options.url}"${params}
method: "${options.method}"${data}${headers}`);
}
function getAvailableScenariosMessage(scenarios) {
  const availableScenarios = (scenarios === null || scenarios === void 0 ? void 0 : scenarios.scenarios.map((scenario) => {
    var _a;
    return (_a = scenario.createBoundScenarioWithRequest()) === null || _a === void 0 ? void 0 : _a.getAsString();
  })) || [];
  return availableScenarios.join("\n");
}

// ../../node_modules/@wix/http-client-testkit/dist/es/src/host.js
function resolveHost(req) {
  if (req) {
    const origin = req.headers.origin;
    if (origin) {
      return new URL(origin).host;
    }
  }
  if (typeof location !== "undefined") {
    return location.host;
  }
  return "";
}

// ../../node_modules/@wix/http-client-testkit/dist/es/src/http-methods.js
var HttpMethods = {
  GET: "GET",
  POST: "POST",
  HEAD: "HEAD",
  OPTIONS: "OPTIONS",
  DELETE: "DELETE",
  PUT: "PUT",
  PATCH: "PATCH"
};

// ../../node_modules/@wix/http-client-testkit/dist/es/src/http-status.js
function isErrorStatus(status) {
  return status >= 300 && status <= 599;
}

// ../../node_modules/@wix/http-client-testkit/dist/es/src/scenarios/scenarios-collection.js
var ScenariosCollection = class {
  constructor(_scenarios) {
    this._scenarios = _scenarios;
  }
  matchScenario(req, context) {
    const boundScenario = this._scenarios.map((s) => s.createBoundScenarioWithRequest(req, context)).find((bs) => bs === null || bs === void 0 ? void 0 : bs.matchRequest(req));
    const scenario = boundScenario === null || boundScenario === void 0 ? void 0 : boundScenario.scenario;
    if (!(scenario === null || scenario === void 0 ? void 0 : scenario.isPersisted())) {
      this._scenarios = this._scenarios.filter((s) => s !== scenario);
    }
    return boundScenario;
  }
  get scenarios() {
    return this._scenarios;
  }
  resetAll() {
    this._scenarios = [];
  }
  useScenarios(scenarios) {
    this._scenarios = scenarios;
  }
  addScenarios(scenarios) {
    this._scenarios = this._scenarios.concat(scenarios);
  }
};

// ../../node_modules/@wix/http-client-testkit/dist/es/src/scenarios/scenario-utils.js
var import_lodash = __toESM(require_lodash());
var UNDEFINED_BASE = "http://unused.com";

// ../../node_modules/@wix/http-client-testkit/dist/es/src/utils.js
function isRelativeUrl(url) {
  const isAbsoluteUrlRegex = new RegExp("^(?:[a-z]+:)?//", "i");
  return !isAbsoluteUrlRegex.test(url);
}
function toAbsoluteIfPossible(host, url, protocol = "http:", baseUrl = UNDEFINED_BASE) {
  if (isRelativeUrl(url)) {
    const hostToUse = host ? host : baseUrl;
    const absoluteUrl = hostToUse + url;
    return hostToUse.includes("http") ? absoluteUrl : protocol + "//" + absoluteUrl;
  }
  return url;
}

// ../../node_modules/@wix/http-client-testkit/dist/es/src/http-client-mock.js
var HttpClientMock = class {
  constructor(scenarios) {
    this.cancel = { tokenId: 0 };
    this.scenarios = new ScenariosCollection(scenarios);
  }
  async request(requestOptionsOrFactory, overrides) {
    var _a;
    const host = resolveHost();
    const requestOptions = typeof requestOptionsOrFactory === "function" ? requestOptionsOrFactory({ isSSR: false, host }) : requestOptionsOrFactory;
    requestOptions.url = toAbsoluteIfPossible(host, requestOptions.url, void 0, requestOptions.baseURL);
    if (overrides === null || overrides === void 0 ? void 0 : overrides.signedInstance) {
      requestOptions.headers = {
        ...(_a = requestOptions.headers) !== null && _a !== void 0 ? _a : {},
        authorization: overrides.signedInstance
      };
    }
    const boundScenario = this.scenarios.matchScenario(requestOptions);
    if (requestOptions.cancelToken || boundScenario) {
      const { data, status, requestId } = await boundScenario.toResponse(requestOptions, new URLSearchParams(requestOptions.params));
      return this.respond({
        data,
        status,
        statusText: "",
        headers: {},
        config: requestOptions,
        requestId
      });
    }
    throw new NoMockFoundError(requestOptions, this.scenarios);
  }
  async get(url, requestOptions) {
    const opts = {
      ...requestOptions,
      url,
      method: HttpMethods.GET
    };
    return this.request(opts);
  }
  async delete(url, requestOptions) {
    const opts = {
      ...requestOptions,
      url,
      method: HttpMethods.DELETE
    };
    return this.request(opts);
  }
  async head(url, requestOptions) {
    const opts = {
      ...requestOptions,
      url,
      method: HttpMethods.HEAD
    };
    return this.request(opts);
  }
  async options(url, requestOptions) {
    const opts = {
      ...requestOptions,
      url,
      method: HttpMethods.OPTIONS
    };
    return this.request(opts);
  }
  async post(url, data, requestOptions) {
    const opts = {
      ...requestOptions,
      url,
      data,
      method: HttpMethods.POST
    };
    return this.request(opts);
  }
  async put(url, data, requestOptions) {
    const opts = {
      ...requestOptions,
      url,
      data,
      method: HttpMethods.PUT
    };
    return this.request(opts);
  }
  async patch(url, data, requestOptions) {
    const opts = {
      ...requestOptions,
      url,
      data,
      method: HttpMethods.PATCH
    };
    return this.request(opts);
  }
  addScenarios(scenarios) {
    this.scenarios.addScenarios(scenarios);
  }
  reset() {
    this.scenarios.resetAll();
  }
  get CancelToken() {
    const tokenId = Math.floor(Math.random() * 10);
    return {
      // @ts-expect-error
      source: () => {
        return {
          cancel: (reason) => {
            this.cancel.reason = reason;
            this.cancel.tokenId = tokenId;
          },
          token: tokenId
        };
      }
    };
  }
  isCancel(error) {
    return error.message === CancelError.getCancellationMessage(this.cancel.reason, this.cancel.tokenId);
  }
  respond(response) {
    if (this.cancel.tokenId) {
      throw createCancelError(400, response.config, response.request, response, this.cancel.reason, this.cancel.tokenId);
    }
    const { validateStatus } = response.config;
    const isError = (0, import_lodash2.isFunction)(validateStatus) ? (status) => !validateStatus(status) : isErrorStatus;
    if (isError(response.status)) {
      throw createScenarioError(response.status, response.config, response.request, response);
    }
    return Promise.resolve(response);
  }
};
function createHttpClientMock(...args) {
  return new HttpClientMock(...args);
}

// ../../node_modules/@wix/http-client-testkit/dist/es/src/scenarios/scenario-with-request.js
var import_dedent_js2 = __toESM(require_lib());
var import_lodash3 = __toESM(require_lodash());

// ../../node_modules/@wix/http-client-testkit/dist/es/src/scenarios/types.js
var ScenarioType;
(function(ScenarioType2) {
  ScenarioType2[ScenarioType2["When"] = 0] = "When";
  ScenarioType2[ScenarioType2["WhenRequest"] = 1] = "WhenRequest";
})(ScenarioType || (ScenarioType = {}));

// ../../node_modules/@wix/error-monitor/dist/esm/InternalErrorMonitor.js
var import_types4 = require("@sentry/types");

// ../../node_modules/@wix/error-monitor/dist/esm/constants.js
var SENTRY_TAG_NAME_LIMIT = 30;
var SENTRY_TAG_VALUE_LIMIT = 200;
var TAG_WARNING_LIMIT = 20;

// ../../node_modules/@wix/error-monitor/dist/esm/utils/addTags.js
function addTags(scope, appName = "", tags) {
  const sanitizedTags = sanitizeTags(tags, appName);
  Object.keys(sanitizedTags).forEach((tag) => {
    scope.setTag(tag, sanitizedTags[tag]);
  });
}
function sanitizeTags(tags, appName) {
  if (process.env.NODE_ENV === "development") {
    warnInConsoleIfTagIsIncorrect(tags, appName);
  }
  return Object.keys(tags).reduce((acc, key) => {
    const value = tags[key];
    if (value !== void 0) {
      acc[key] = String(value);
    }
    return acc;
  }, {});
}
function warnInConsoleIfTagIsIncorrect(tagsObject, appName = "") {
  Object.keys(tagsObject).forEach((tagKey) => {
    const tagValue = tagsObject[tagKey];
    if (tagKey.length >= SENTRY_TAG_NAME_LIMIT) {
      console.warn(`ErrorMonitor::${appName}:: tag NAME is too long!

 ${tagKey}`);
    }
    const stringValues = typeof tagValue === "object" ? JSON.stringify(tagValue) : String(tagValue);
    if (stringValues && stringValues.length >= SENTRY_TAG_VALUE_LIMIT) {
      const truncatedValue = truncateIfBiggerThen(tagValue, TAG_WARNING_LIMIT);
      console.warn(`ErrorMonitor::${appName}:: tag VALUE is too long!

 ${tagKey}: ${truncatedValue}`);
    }
  });
}
function truncateIfBiggerThen(value, length) {
  if (!value || typeof value === "boolean" || typeof value === "number" || value.length <= length) {
    return value;
  }
  return value.substring(0, length) + "...";
}

// ../../node_modules/@wix/error-monitor/dist/esm/utils/isWixHttpError.js
function isWixHttpError(exception) {
  return exception.isWixHttpError === true;
}

// ../../node_modules/@wix/error-monitor/dist/esm/utils/isAxiosError.js
function isAxiosError(exception) {
  return exception.isAxiosError === true;
}

// ../../node_modules/@wix/error-monitor/dist/esm/utils/extractDetailsFromHttpErrors.js
function extractDetailsFromHttpErrors(error) {
  if (!error || !error.config) {
    return {
      error: error ? JSON.stringify(error) : "error is empty"
    };
  }
  const isWixError = isWixHttpError(error);
  const isAxios = isAxiosError(error);
  if (isWixError || isAxios) {
    return {
      isWixHttpError: isWixError,
      requestId: error.requestId,
      ...error.config && {
        requestConfig: {
          url: error.config.url,
          method: error.config.method,
          ...error.config.params && {
            params: JSON.stringify(error.config.params)
          },
          ...error.config.data && {
            data: JSON.stringify(error.config.data)
          }
        }
      },
      ...error?.response && {
        response: {
          status: error.response.status,
          statusText: error.response.statusText,
          ...error.response.data && {
            data: JSON.stringify(error.response.data)
          }
        }
      }
    };
  }
}

// ../../node_modules/@wix/error-monitor/dist/esm/utils/handleNonErrorException.js
function handleNonErrorException(exception) {
  switch (true) {
    case ("message" in exception && exception?.message): {
      return Object.assign(new Error(exception.message), exception);
    }
    case ("error" in exception && exception?.error?.message): {
      return Object.assign(new Error(exception.error.message), exception?.error);
    }
    case ("exception" in exception && exception?.exception?.message): {
      return Object.assign(new Error(exception.exception.message), exception?.exception);
    }
    default: {
      console.log("ErrorMonitor: please pass instance of an Error, instead error monitor received", exception);
      return exception;
    }
  }
}

// ../../node_modules/@wix/error-monitor/dist/esm/InternalErrorMonitor.js
var InternalErrorMonitor = class {
  constructor(Sentry2, appName = "") {
    this.sentry = Sentry2;
    this.appName = appName;
  }
  captureException(exception, options) {
    if (isWixHttpError(exception) || isAxiosError(exception)) {
      options = {
        ...options,
        contexts: {
          ...options?.contexts,
          httpErrorDetails: extractDetailsFromHttpErrors(exception)
        }
      };
    }
    if (!(exception instanceof Error)) {
      exception = handleNonErrorException(exception);
    }
    if (options) {
      if (options.tags) {
        options.tags = sanitizeTags(options.tags, this.appName);
      }
      const sentry = this.sentry;
      if (this.isHub(sentry)) {
        return sentry.captureException(exception, { captureContext: options });
      } else {
        return sentry.captureException(exception, options);
      }
    } else {
      return this.sentry.captureException(exception);
    }
  }
  captureMessage(message, options) {
    if (options) {
      const { level = import_types4.Severity.Error, tags } = options;
      if (tags) {
        options.tags = sanitizeTags(tags, this.appName);
      }
      const sentry = this.sentry;
      if (this.isHub(sentry)) {
        return sentry.captureMessage(message, level, {
          captureContext: options
        });
      } else {
        return sentry.captureMessage(message, options);
      }
    } else {
      return this.sentry.captureMessage(message);
    }
  }
  /**
   * Added here to comply with already used api (e.g. in business-manager).
   *
   * @deprecated this API is unstable and could be removed in the future, please do not use this if you don't have a reasons for that.
   */
  captureEvent(event) {
    return this.sentry.captureEvent(event);
  }
  addBreadcrumb(breadcrumb) {
    this.sentry.addBreadcrumb(breadcrumb);
  }
  isHub(sentry) {
    return sentry.captureMessage.length === 3;
  }
};

// ../../node_modules/@wix/error-monitor/dist/esm/utils/enrichBreadcrumbsWithDataHooks.js
function enrichBreadcrumbsWithDataHooks(breadcrumb, hint) {
  const { category } = breadcrumb;
  const isUiInteractionBreadcrumb = category && (category === "ui.click" || category === "ui.input");
  const targetElement = hint && hint.event && hint.event.target;
  if (isUiInteractionBreadcrumb && targetElement) {
    const nodeWithDataHook = targetElement.closest("[data-hook]");
    if (nodeWithDataHook) {
      const dataHook = nodeWithDataHook.getAttribute("data-hook");
      if (targetElement === nodeWithDataHook) {
        breadcrumb.message = `${breadcrumb.message} [data-hook="${dataHook}"]`;
      } else {
        breadcrumb.message = `${breadcrumb.message} parent:[data-hook="${dataHook}"]`;
      }
    }
  }
  return breadcrumb;
}

// ../../node_modules/@wix/error-monitor/dist/esm/utils/calculateAppEnvironment.js
function calculateAppEnvironment() {
  if (typeof window === "undefined") {
    return "server";
  }
  const { hostname } = window.location;
  const { userAgent } = window.navigator;
  const { cookie } = window.document;
  const isSled = /(^|;)\s*automation\s*=\s*sled:/.test(cookie);
  const isLocalhost = /localhost|127\.0\.0\.1|::1|\.local|local.wix.com|^$/i.test(hostname);
  const isBot = /Googlebot|AdsBot-Google-Mobile|bingbot|BingPreview|facebookexternalhit|Baiduspider|YandexBot/i.test(userAgent);
  const isNotValidUserAgent = /BonEcho|NewsGator|SeaMonkey|iTunes|Epiphany|Konqueror|Sleipnir|IceWeasel/i.test(userAgent);
  switch (true) {
    case (process.env.NODE_ENV === "development" || isLocalhost):
      return "development";
    case isBot:
      return "bot";
    case isNotValidUserAgent:
      return "invalid_useragent";
    case isSled:
      return "sled";
    default:
      return "production";
  }
}

// ../../node_modules/@wix/error-monitor/dist/esm/utils/prepareOptions.js
function prepareOptions(options) {
  const { dsn, appName = "", version, environment, beforeSend, transport, integrations, artifactId: artifactId2, wixRecorder } = options;
  return {
    dsn,
    environment: environment || calculateAppEnvironment(),
    ...version && { release: `${artifactId2 ?? appName}@${version}` },
    beforeBreadcrumb: enrichBreadcrumbsWithDataHooks,
    beforeSend: async (event, hint) => {
      let newEvent;
      const originalFingerprint = event.fingerprint;
      const isBeforeSendExists = !!beforeSend;
      if (isBeforeSendExists) {
        event.tags = { ...event.tags, customBeforeSend: true };
      }
      try {
        newEvent = isBeforeSendExists ? await beforeSend(event, hint) : event;
      } catch (e) {
        event.contexts = {
          ...event.contexts,
          errorMonitorBuiltInContext: {
            ...event.contexts?.errorMonitorBuiltInContext,
            customBeforeSendError: true
          }
        };
        newEvent = event;
      }
      if (!newEvent) {
        event.fingerprint = originalFingerprint;
        event.contexts = {
          ...event.contexts,
          errorMonitorBuiltInContext: {
            ...event.contexts?.errorMonitorBuiltInContext,
            customBeforeSendReturnNull: true
          }
        };
        event.environment = "unknown";
        return event;
      }
      newEvent.fingerprint = originalFingerprint;
      const availableStacktraces = [
        ...newEvent.exception?.values?.map((error) => error.stacktrace) ?? [],
        newEvent.stacktrace
      ];
      const stackTraceArtifactIds = availableStacktraces.flatMap((stacktrace) => extractArtifactIdsFromStacktrace(stacktrace));
      const urlArtifactId = extractArtifactIdFromParastoragePath([
        newEvent.contexts?.httpErrorDetails?.requestConfig?.url
      ]);
      const detectedArtifactIds = Array.from(/* @__PURE__ */ new Set([...stackTraceArtifactIds, ...urlArtifactId]));
      if (artifactId2 && !detectedArtifactIds.includes(artifactId2) && newEvent.environment === "production") {
        newEvent.environment = "unknown";
      }
      newEvent.contexts = {
        ...newEvent.contexts,
        errorMonitorBuiltInContext: {
          ...newEvent.contexts?.errorMonitorBuiltInContext,
          detectedArtifactIds
        }
      };
      const sessionRecordingUrl = await wixRecorder?.recordUrl();
      if (sessionRecordingUrl) {
        newEvent.tags = {
          ...newEvent.tags,
          sessionRecordingUrl
        };
        if (sessionRecordingUrl.startsWith("https://app.fullstory.com")) {
          newEvent.contexts = {
            ...newEvent.contexts,
            fullStory: {
              fullStoryUrl: sessionRecordingUrl
            }
          };
        } else {
          newEvent.contexts = {
            ...newEvent.contexts,
            errorMonitorBuiltInContext: {
              ...newEvent.contexts?.errorMonitorBuiltInContext,
              sessionRecordingUrl
            }
          };
        }
      }
      return newEvent;
    },
    transport,
    integrations
  };
}
var extractArtifactIdsFromStacktrace = (stacktrace) => {
  const frames = stacktrace?.frames ?? [];
  const allFileNamesFromStack = frames.map((frame) => frame.abs_path || frame.filename).filter((path) => !!path).reverse();
  return extractArtifactIdFromParastoragePath(allFileNamesFromStack);
};
var extractArtifactIdFromParastoragePath = (paths) => {
  const parastorageRegex = /(?:\/services|\/_partials)\/([^/]*)\/((?:0|[1-9]\d*)\.(?:0|[1-9]\d*)\.(?:0|[1-9]\d*)|[a-f0-9]{56})\//i;
  return paths?.map((fileName) => parastorageRegex.exec(fileName ?? "")).filter((matches) => !!matches).map((matches) => matches[1]);
};

// ../../node_modules/@wix/error-monitor/dist/esm/utils/configureScope.js
function configureScope(sentryInstance, options) {
  const { user, appName, tags } = options;
  sentryInstance.configureScope((scope) => {
    if (user) {
      scope.setUser(user);
    }
    if (tags) {
      addTags(scope, appName, tags);
    }
  });
}

// ../../node_modules/@wix/error-monitor/dist/esm/createHub.js
var createSafeSentryOptions = (Sentry2, options = {}) => ({
  ...options,
  transport: options.transport ?? (typeof Sentry2.makeFetchTransport !== "undefined" ? Sentry2.makeFetchTransport : void 0),
  stackParser: options.stackParser ?? (typeof Sentry2.defaultStackParser !== "undefined" ? Sentry2.defaultStackParser : void 0),
  integrations: options.integrations ?? (typeof Sentry2.getDefaultIntegrations === "function" ? Sentry2.getDefaultIntegrations({}) : Sentry2.defaultIntegrations)
});
function createHub(options) {
  const { Sentry: Sentry2 } = options;
  const clientOptions = createSafeSentryOptions(Sentry2, prepareOptions(options));
  const sentryClient = new Sentry2.BrowserClient(clientOptions);
  const sentryHub = new Sentry2.Hub(sentryClient);
  configureScope(sentryHub, options);
  return new InternalErrorMonitor(sentryHub, options.appName);
}

// ../../node_modules/@wix/wix-experiments/dist/es/src/ReadOnlyExperiments.js
var ReadOnlyExperiments = (
  /** @class */
  function() {
    function ReadOnlyExperiments2(options) {
      var _a;
      this.experiments = (_a = options.experiments) !== null && _a !== void 0 ? _a : {};
    }
    ReadOnlyExperiments2.prototype.get = function(key) {
      return this.experiments[key];
    };
    ReadOnlyExperiments2.prototype.enabled = function(key) {
      return this.get(key) === "true";
    };
    ReadOnlyExperiments2.prototype.pending = function() {
      return false;
    };
    ReadOnlyExperiments2.prototype.ready = function() {
      return Promise.resolve();
    };
    ReadOnlyExperiments2.prototype.all = function() {
      return this.experiments;
    };
    return ReadOnlyExperiments2;
  }()
);

// ../../node_modules/@wix/fedops-presets/dist/es/presets/default.json
var default_default = {
  src: 72,
  persistentEndpoint: "performance",
  nonPersistentEndpoint: "fed",
  successRateRoot: "fedops_events",
  performanceRoot: "fedops_performance",
  appLoadStart: {
    eventId: 14
  },
  appLoadFinish: {
    eventId: 11
  },
  loadPhaseStart: {
    eventId: 12
  },
  loadPhaseFinish: {
    eventId: 13
  },
  interactionStart: {
    eventId: 15
  },
  interactionEnd: {
    eventId: 16
  },
  error: {
    eventId: 17
  },
  resource: {
    eventId: 18
  }
};

// ../../node_modules/@wix/fedops-presets/dist/es/presets/ds.json
var ds_default = {
  src: 72,
  persistentEndpoint: "ds-performance",
  nonPersistentEndpoint: "ds-performance",
  successRateRoot: "ds_events",
  performanceRoot: "ds_performance",
  appLoadStart: {
    eventId: 31
  },
  appLoadFinish: {
    eventId: 33
  },
  interactionStart: {
    eventId: 34
  },
  interactionEnd: {
    eventId: 35
  },
  loadPhaseStart: {
    eventId: 38
  },
  loadPhaseFinish: {
    eventId: 32
  },
  error: {
    eventId: 36
  },
  resource: {
    eventId: 37
  }
};

// ../../node_modules/@wix/fedops-presets/dist/es/presets/editor.json
var editor_default = {
  src: 72,
  persistentEndpoint: "editor-performance",
  nonPersistentEndpoint: "editor-performance",
  successRateRoot: "editor_events",
  performanceRoot: "editor_performance",
  appLoadStart: {
    eventId: 41
  },
  appLoadFinish: {
    eventId: 43
  },
  interactionStart: {
    eventId: 44
  },
  interactionEnd: {
    eventId: 45
  },
  loadPhaseStart: {
    eventId: 48
  },
  loadPhaseFinish: {
    eventId: 42
  },
  error: {
    eventId: 46
  },
  resource: {
    eventId: 47
  }
};

// ../../node_modules/@wix/fedops-presets/dist/es/presets/bolt.json
var bolt_default = {
  src: 72,
  persistentEndpoint: "bolt-performance",
  nonPersistentEndpoint: "bolt-performance",
  successRateRoot: "bolt_events",
  performanceRoot: "bolt_performance",
  appLoadStart: {
    eventId: 21
  },
  loadPhaseStart: {
    eventId: 28
  },
  loadPhaseFinish: {
    eventId: 22
  },
  appLoadFinish: {
    eventId: 23
  },
  interactionStart: {
    eventId: 24
  },
  interactionEnd: {
    eventId: 25
  },
  error: {
    eventId: 26
  },
  resource: {
    eventId: 27
  }
};

// ../../node_modules/@wix/fedops-presets/dist/es/presets/ooi.json
var ooi_default = {
  src: 72,
  persistentEndpoint: "ooi-performance",
  nonPersistentEndpoint: "ooi-performance",
  successRateRoot: "fedops_events",
  performanceRoot: "fedops_performance",
  appLoadStart: {
    eventId: 61
  },
  appLoadFinish: {
    eventId: 63
  },
  interactionStart: {
    eventId: 64
  },
  interactionEnd: {
    eventId: 65
  },
  loadPhaseStart: {
    eventId: 68
  },
  loadPhaseFinish: {
    eventId: 62
  },
  error: {
    eventId: 66
  },
  resource: {
    eventId: 67
  }
};

// ../../node_modules/@wix/fedops-presets/dist/es/presets/mobile.json
var mobile_default = {
  src: 72,
  persistentEndpoint: "mobile-performance",
  nonPersistentEndpoint: "mobile-performance",
  successRateRoot: "fedops_events",
  performanceRoot: "fedops_performance",
  appLoadStart: {
    eventId: 51
  },
  appLoadFinish: {
    eventId: 53
  },
  interactionStart: {
    eventId: 54
  },
  interactionEnd: {
    eventId: 55
  },
  loadPhaseStart: {
    eventId: 58
  },
  loadPhaseFinish: {
    eventId: 52
  },
  error: {
    eventId: 56
  },
  resource: {
    eventId: 57
  }
};

// ../../node_modules/@wix/fedops-presets/dist/es/presets/presets-types.js
var presetsTypes = {
  DEFAULT: "DEFAULT",
  DS: "DS",
  EDITOR: "EDITOR",
  OOI: "OOI",
  BOLT: "BOLT",
  MOBILE: "MOBILE"
};

// ../../node_modules/@wix/fedops-presets/dist/es/src/loader.js
function presetLoader(presetType) {
  switch (presetType) {
    case presetsTypes.DEFAULT:
      return default_default;
    case presetsTypes.BOLT:
      return bolt_default;
    case presetsTypes.DS:
      return ds_default;
    case presetsTypes.EDITOR:
      return editor_default;
    case presetsTypes.OOI:
      return ooi_default;
    case presetsTypes.MOBILE:
      return mobile_default;
    default:
      return default_default;
  }
}

// ../../node_modules/@wix/fedops-logger/dist/es/src/env.js
var envFallback = {};
var env = function env2() {
  if (typeof window !== "undefined") {
    return window;
  } else if (typeof globalThis !== "undefined") {
    return globalThis;
  } else if (typeof global !== "undefined") {
    return global;
  } else if (typeof WorkerGlobalScope !== "undefined") {
    return WorkerGlobalScope;
  } else {
    return envFallback;
  }
};

// ../../node_modules/@wix/fedops-logger/dist/es/src/conf.js
var SESSION_ID_STORAGE_KEY = "fedops.logger.sessionId";
var DEFAULT_APP_VERSION = "0.0.0";

// ../../node_modules/@wix/fedops-logger/dist/es/src/reporter/reporter.js
var defaultLoggerFactory = __toESM(require_logger());
var Reporter = /* @__PURE__ */ function() {
  function Reporter2(_ref) {
    var biLoggerFactory = _ref.biLoggerFactory, baseUrl = _ref.baseUrl, preset = _ref.preset, useBatch = _ref.useBatch;
    this._preset = preset;
    var shouldBatch = !(useBatch === false);
    this._factory = biLoggerFactory || defaultLoggerFactory.factory({
      host: baseUrl,
      useBatch: shouldBatch
    });
    this._publisher = this._factory.logger();
    this._nonBatchedPublisher = this._factory.logger({
      useBatch: false
    });
  }
  var _proto = Reporter2.prototype;
  _proto.flush = function flush() {
    this._publisher.flush();
  };
  _proto.report = function report(data, reporterEndpoint, params) {
    if (!data) {
      return null;
    }
    var endpoint = reporterEndpoint || this._preset.nonPersistentEndpoint;
    if (params && params.useBatch === false) {
      return this._nonBatchedPublisher.log(data, {
        endpoint,
        category: "essential"
      });
    }
    return this._publisher.log(data, {
      endpoint,
      category: "essential"
    });
  };
  return Reporter2;
}();

// ../../node_modules/@wix/fedops-logger/dist/es/src/reporter/reporter-factory.js
var ReporterFactory;
function create(_temp) {
  var _ref = _temp === void 0 ? {} : _temp, biLoggerFactory = _ref.biLoggerFactory, baseUrl = _ref.baseUrl, endpoint = _ref.endpoint, preset = _ref.preset, useBatch = _ref.useBatch;
  if (!ReporterFactory) {
    return new Reporter({
      biLoggerFactory,
      baseUrl,
      endpoint,
      preset,
      useBatch
    });
  } else {
    return ReporterFactory({
      preset
    });
  }
}

// ../../node_modules/@wix/fedops-logger/dist/es/src/base-logger.js
init_src();

// ../../node_modules/@wix/fedops-logger/dist/es/src/performance/performance.js
var performance_default = {
  getNavigation: function getNavigation() {
    var _env$performance;
    return ((_env$performance = env().performance) == null ? void 0 : _env$performance.navigation) || {};
  },
  /**
   * get all loaded resources
   */
  getResources: function getResources() {
    var _env$performance2;
    return ((_env$performance2 = env().performance) == null ? void 0 : _env$performance2.getEntriesByType == null ? void 0 : _env$performance2.getEntriesByType("resource")) || [];
  },
  /**
   * get all loaded resources
   */
  getMarks: function getMarks() {
    var _env$performance3;
    return ((_env$performance3 = env().performance) == null ? void 0 : _env$performance3.getEntriesByType == null ? void 0 : _env$performance3.getEntriesByType("mark")) || [];
  },
  getTiming: function getTiming() {
    var _env$performance4;
    return ((_env$performance4 = env().performance) == null ? void 0 : _env$performance4.timing) || {};
  },
  clearResourceTimings: function clearResourceTimings() {
    var _env$performance5;
    (_env$performance5 = env().performance) == null ? void 0 : _env$performance5.clearResourceTimings == null ? void 0 : _env$performance5.clearResourceTimings();
  },
  /**
   * Duration relative to navigation start (env().performance.timing.navigationStart)
   * fallback to: performance.now -> now (nodejs) -> getTime()
   */
  now: function now() {
    var _env$performance6, _performance;
    return ((_env$performance6 = env().performance) == null ? void 0 : _env$performance6.now == null ? void 0 : _env$performance6.now()) || typeof performance !== "undefined" && ((_performance = performance) == null ? void 0 : _performance.now == null ? void 0 : _performance.now()) || (/* @__PURE__ */ new Date()).getTime();
  },
  /**
   * Duration from fetch start to response end
   */
  responseEndTime: function responseEndTime() {
    var _performance2, _performance2$timing, _performance3, _performance3$timing;
    var fetchStart = (_performance2 = performance) == null ? void 0 : (_performance2$timing = _performance2.timing) == null ? void 0 : _performance2$timing.fetchStart;
    var responseEnd = (_performance3 = performance) == null ? void 0 : (_performance3$timing = _performance3.timing) == null ? void 0 : _performance3$timing.responseEnd;
    if (fetchStart && responseEnd) {
      return responseEnd - fetchStart;
    }
  },
  /**
   * Create a performance timestamp
   */
  mark: function mark(name) {
    var _env$performance7;
    (_env$performance7 = env().performance) == null ? void 0 : _env$performance7.mark == null ? void 0 : _env$performance7.mark(name);
  },
  /**
   * Record a performance measurement
   */
  measure: function measure(name, startMark, endMark) {
    var _env$performance8;
    (_env$performance8 = env().performance) == null ? void 0 : _env$performance8.measure == null ? void 0 : _env$performance8.measure(name, startMark, endMark);
  },
  /**
   * Get performance entries by name
   */
  getEntriesByName: function getEntriesByName(name) {
    var _env$performance9;
    return ((_env$performance9 = env().performance) == null ? void 0 : _env$performance9.getEntriesByName == null ? void 0 : _env$performance9.getEntriesByName(name)) || [];
  }
};

// ../../node_modules/@wix/fedops-logger/dist/es/src/global-app-data/global-app-data.js
function getAppData(appName) {
  return ((env().fedops || {}).apps || {})[appName];
}
function initializeFedops() {
  env().fedops = env().fedops || {};
  env().fedops.apps = env().fedops.apps || {};
}
function initializeApp(appName) {
  env().fedops.apps[appName] = env().fedops.apps[appName] || {};
}
function initializeGlobal(appName) {
  initializeFedops();
  initializeApp(appName);
}
function getLoadStartTime(appName) {
  return (getAppData(appName) || {}).startLoadTime || 0;
}
function setLoadStartTime(appName, startLoadTime) {
  return (getAppData(appName) || {}).startLoadTime = startLoadTime;
}
function setSessionId(sessionId) {
  return (env().fedops || {}).sessionId = sessionId;
}

// ../../node_modules/@wix/fedops-logger/dist/es/src/times/times.js
var Times = /* @__PURE__ */ function() {
  function Times2(appName) {
    this.appName = appName;
    this.apps = {};
    this.apps[appName] = {};
  }
  var _proto = Times2.prototype;
  _proto._getKeyForApp = function _getKeyForApp(appId, widgetId) {
    if (appId === void 0) {
      appId = this.appName;
    }
    return widgetId ? appId + "_" + widgetId : appId;
  };
  _proto.setLoadStarted = function setLoadStarted(_temp) {
    var _ref = _temp === void 0 ? {} : _temp, appId = _ref.appId, widgetId = _ref.widgetId;
    var key = this._getKeyForApp(appId, widgetId);
    if (!this.apps[key]) {
      this.apps[key] = {};
    }
    var loadStartTime = performance_default.now();
    this.apps[key].startLoadTime = loadStartTime;
    if (!appId) {
      setLoadStartTime(key, loadStartTime);
    }
  };
  _proto.getLoadStartTime = function getLoadStartTime2(_temp2) {
    var _ref2 = _temp2 === void 0 ? {} : _temp2, appId = _ref2.appId, widgetId = _ref2.widgetId;
    var key = this._getKeyForApp(appId, widgetId);
    var loadStartTime = this.getLoadStartTimeFromInstance(key) || getLoadStartTime(key) || 0;
    return Math.floor(loadStartTime);
  };
  _proto.getLoadStartTimeFromInstance = function getLoadStartTimeFromInstance(key) {
    return this.apps[key] && this.apps[key].startLoadTime;
  };
  _proto.getAppLoadTime = function getAppLoadTime(_temp3) {
    var _ref3 = _temp3 === void 0 ? {} : _temp3, appId = _ref3.appId, widgetId = _ref3.widgetId;
    return Math.floor(performance_default.now() - this.getLoadStartTime({
      appId,
      widgetId
    }));
  };
  _proto.getFirstRequestDuration = function getFirstRequestDuration(_temp4) {
    var _ref4 = _temp4 === void 0 ? {} : _temp4, appId = _ref4.appId, widgetId = _ref4.widgetId;
    return this.getLoadStartTime({
      appId,
      widgetId
    });
  };
  return Times2;
}();

// ../../node_modules/@wix/fedops-logger/dist/es/src/data-items/common/data-item.js
var DataItem = /* @__PURE__ */ function() {
  function DataItem2(data) {
    if (data === void 0) {
      data = void 0;
    }
    this.data = data;
  }
  var _proto = DataItem2.prototype;
  _proto.isActive = function isActive() {
    return true;
  };
  _proto["export"] = function _export() {
    return this.data;
  };
  return DataItem2;
}();

// ../../node_modules/@wix/fedops-logger/dist/es/src/data-source/data-source.js
var DataSource = /* @__PURE__ */ function() {
  function DataSource2(item) {
    var _this = this;
    if (item === void 0) {
      item = null;
    }
    this.items = [];
    if (item) {
      (typeof item.length === "undefined" ? [item] : item).forEach(function(item2) {
        return _this.addItem(item2);
      });
    }
  }
  var _proto = DataSource2.prototype;
  _proto.clone = function clone() {
    return new DataSource2(this.items.slice());
  };
  _proto.addItem = function addItem(item) {
    this.items.push(item instanceof DataItem ? item : new DataItem(item));
    return this;
  };
  _proto.mergeItems = function mergeItems() {
    var res = {};
    for (var i = 0; i < this.items.length; i++) {
      var item = this.items[i];
      if (!item.isActive()) {
        res = null;
        break;
      }
      var data = item["export"]();
      for (var key in data) {
        if (data.hasOwnProperty(key)) {
          res[key] = data[key];
        }
      }
    }
    return res;
  };
  return DataSource2;
}();

// ../../node_modules/@wix/fedops-logger/dist/es/src/storage/storage.js
var storage_default = {
  getItem: function getItem(key) {
    try {
      return env().localStorage && env().localStorage.getItem(key);
    } catch (e) {
    }
  },
  setItem: function setItem(key, value) {
    try {
      return env().localStorage && env().localStorage.setItem(key, value);
    } catch (e) {
    }
  }
};

// ../../node_modules/@wix/fedops-logger/dist/es/src/utils.js
var generateGuid = function generateGuid2() {
  return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function(c) {
    var r = Math.random() * 16 | 0, v = c === "x" ? r : r & 3 | 8;
    return v.toString(16);
  });
};
var phaseNameExtractor = /\[fedops] phase:([^ ]+) ([^ ]+) ?(.*) (started|finished)/;
var getPhaseMark = function getPhaseMark2(markName) {
  var matches = phaseNameExtractor.exec(markName);
  if (matches) {
    return {
      phaseName: matches[1],
      appId: matches[2],
      widgetId: matches[3]
    };
  }
};
var interactionNameExtractor = /\[fedops] ([^ ]+) interaction ([^ ]+) (started|ended)/;
var getInteractionMark = function getInteractionMark2(markName) {
  var matches = interactionNameExtractor.exec(markName);
  if (matches) {
    return {
      appName: matches[1],
      interactionName: matches[2]
    };
  }
};
var getPathnameTemplate = function getPathnameTemplate2(pathname) {
  if (isNumber(pathname)) {
    return ":param";
  }
  return pathname;
};
var isNumber = function isNumber2(str) {
  return !isNaN(parseFloat(str)) && isFinite(str);
};
var extractUrl = function extractUrl2(url) {
  var parsedUrl;
  try {
    parsedUrl = new URL(url);
    return parsedUrl;
  } catch (e) {
    var _env$location$origin, _env, _env$location;
    return {
      pathname: url,
      origin: (_env$location$origin = (_env = env()) == null ? void 0 : (_env$location = _env.location) == null ? void 0 : _env$location.origin) != null ? _env$location$origin : ""
    };
  }
};

// ../../node_modules/@wix/fedops-logger/dist/es/src/session-manager/session-manager.js
var sessionManager = {
  getSessionId: function getSessionId() {
    var res = typeof env() !== "undefined" && env().fedops && env().fedops.sessionId;
    res = res || storage_default.getItem(SESSION_ID_STORAGE_KEY);
    res = res || generateGuid();
    setSessionId(res);
    storage_default.setItem(SESSION_ID_STORAGE_KEY, res);
    return res;
  }
};
var session_manager_default = sessionManager;

// ../../node_modules/@wix/fedops-logger/dist/es/src/loading-phases/loading-phases.js
var LoadingPhases = /* @__PURE__ */ function() {
  function LoadingPhases2(appName, times) {
    this.appName = appName;
    this.phases = /* @__PURE__ */ new Map();
    this.indexToKey = /* @__PURE__ */ new Map();
    this.times = times || new Times(appName);
  }
  LoadingPhases2._getIndexForPhase = function _getIndexForPhase(_ref) {
    var name = _ref.name, _ref$appId = _ref.appId, appId = _ref$appId === void 0 ? "" : _ref$appId, _ref$widgetId = _ref.widgetId, widgetId = _ref$widgetId === void 0 ? "" : _ref$widgetId;
    return name + "_" + appId + "_" + widgetId;
  };
  var _proto = LoadingPhases2.prototype;
  _proto.getAppLoadingPhaseData = function getAppLoadingPhaseData(_ref2) {
    var name = _ref2.name, appId = _ref2.appId, widgetId = _ref2.widgetId;
    return this.phases.get(LoadingPhases2._getIndexForPhase({
      name,
      appId,
      widgetId
    }));
  };
  _proto.saveLoadingPhase = function saveLoadingPhase(_ref3) {
    var name = _ref3.name, appId = _ref3.appId, widgetId = _ref3.widgetId;
    var phaseKey = LoadingPhases2._getIndexForPhase({
      name,
      appId,
      widgetId
    });
    if (!this.phases.has(phaseKey)) {
      var index = this.phases.size;
      this.phases.set(phaseKey, {
        name,
        phaseStartTime: performance_default.now(),
        index
      });
      this.indexToKey.set(index, phaseKey);
    }
  };
  _proto.endLoadingPhase = function endLoadingPhase(_ref4) {
    var name = _ref4.name, appId = _ref4.appId, widgetId = _ref4.widgetId, widgetArray = _ref4.widgetArray;
    var phaseKey = LoadingPhases2._getIndexForPhase({
      name,
      appId,
      widgetId
    });
    if (!this.phases.has(phaseKey) || this.phases.get(phaseKey).duration) {
      return;
    }
    var phase = this.phases.get(phaseKey);
    phase.duration = performance_default.now() - phase.phaseStartTime;
    if (widgetArray) {
      phase.widgetArray = widgetArray;
    }
    if (widgetId) {
      phase.widgetId = widgetId;
    }
    this.phases.set(phaseKey, phase);
  };
  _proto.getNextPhaseToReport = function getNextPhaseToReport() {
    var latestStepIndex = this.phases.size - 1;
    var lastPhaseKey = this.indexToKey.get(latestStepIndex);
    return this.phases.get(lastPhaseKey);
  };
  _proto.getPhasePreviousTo = function getPhasePreviousTo(_ref5) {
    var name = _ref5.name, appId = _ref5.appId, widgetId = _ref5.widgetId;
    var currentPhase = this.phases.get(LoadingPhases2._getIndexForPhase({
      name,
      appId,
      widgetId
    }));
    var previousPhaseIndex = this.indexToKey.get(currentPhase.index - 1);
    return this.phases.get(previousPhaseIndex);
  };
  _proto.getPhases = function getPhases(_temp) {
    var _ref6 = _temp === void 0 ? {} : _temp, appId = _ref6.appId;
    var phaseToNamedPhaseObject = function phaseToNamedPhaseObject2(phase) {
      var _ref7;
      return _ref7 = {}, _ref7[phase.name] = phase, _ref7;
    };
    return Array.from(this.phases).filter(function(_ref8) {
      var phaseKey = _ref8[0];
      var _phaseKey$split = phaseKey.split("_"), appIdOfPhase = _phaseKey$split[1];
      return appId && appIdOfPhase === appId || !appId;
    }).map(function(_ref9) {
      var phase = _ref9[1];
      return phaseToNamedPhaseObject(phase);
    });
  };
  return LoadingPhases2;
}();

// ../../node_modules/@wix/fedops-logger/dist/es/src/data-items/common/bi-app-context-item.js
function _inheritsLoose(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf(subClass, superClass);
}
function _setPrototypeOf(o, p) {
  _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function _setPrototypeOf3(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf(o, p);
}
var BiAppContextDataItem = /* @__PURE__ */ function(_DataItem) {
  _inheritsLoose(BiAppContextDataItem2, _DataItem);
  function BiAppContextDataItem2(_ref) {
    var _this;
    var appId = _ref.appId, widgetId = _ref.widgetId, isServerSide = _ref.isServerSide, widgetArray = _ref.widgetArray;
    _this = _DataItem.call(this) || this;
    _this.data = _this._filterUndefined({
      appId,
      widgetId,
      isServerSide,
      widgetArray
    });
    return _this;
  }
  var _proto = BiAppContextDataItem2.prototype;
  _proto._filterUndefined = function _filterUndefined(contextData) {
    var definedValues = {};
    Object.keys(contextData).forEach(function(key) {
      if (contextData[key]) {
        definedValues[key] = contextData[key];
      }
    });
    return definedValues;
  };
  return BiAppContextDataItem2;
}(DataItem);

// ../../node_modules/@wix/fedops-logger/dist/es/src/data-items/duration/duration-item.js
function _inheritsLoose2(subClass, superClass) {
  subClass.prototype = Object.create(superClass.prototype);
  subClass.prototype.constructor = subClass;
  _setPrototypeOf2(subClass, superClass);
}
function _setPrototypeOf2(o, p) {
  _setPrototypeOf2 = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function _setPrototypeOf3(o2, p2) {
    o2.__proto__ = p2;
    return o2;
  };
  return _setPrototypeOf2(o, p);
}
var DurationDataItem = /* @__PURE__ */ function(_DataItem) {
  _inheritsLoose2(DurationDataItem2, _DataItem);
  function DurationDataItem2(duration) {
    return _DataItem.call(this, {
      duration
    }) || this;
  }
  var _proto = DurationDataItem2.prototype;
  _proto.setFirstRequestDuration = function setFirstRequestDuration(firstRequestDuration) {
    this.data.frd = firstRequestDuration;
    return this;
  };
  return DurationDataItem2;
}(DataItem);

// ../../node_modules/@wix/fedops-logger/dist/es/src/data-items/phases/utils.js
function _createForOfIteratorHelperLoose(o, allowArrayLike) {
  var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"];
  if (it) return (it = it.call(o)).next.bind(it);
  if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") {
    if (it) o = it;
    var i = 0;
    return function() {
      if (i >= o.length) return { done: true };
      return { done: false, value: o[i++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray(o, minLen) {
  if (!o) return;
  if (typeof o === "string") return _arrayLikeToArray(o, minLen);
  var n = Object.prototype.toString.call(o).slice(8, -1);
  if (n === "Object" && o.constructor) n = o.constructor.name;
  if (n === "Map" || n === "Set") return Array.from(o);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen);
}
function _arrayLikeToArray(arr2, len) {
  if (len == null || len > arr2.length) len = arr2.length;
  for (var i = 0, arr22 = new Array(len); i < len; i++) {
    arr22[i] = arr2[i];
  }
  return arr22;
}
function createPhasesCollection(phasesOfApp) {
  var phases = {};
  for (var _iterator = _createForOfIteratorHelperLoose(phasesOfApp), _step; !(_step = _iterator()).done; ) {
    var phase = _step.value;
    deleteIndexFrom(phase);
    phases = Object.assign(phases, phase);
  }
  return phases;
}
function calcPhaseDuration(phaseStartTime) {
  return Math.floor(performance_default.now() - phaseStartTime);
}
function deleteIndexFrom(phase) {
  delete phase[Object.getOwnPropertyNames(phase)[0]].index;
}

// ../../node_modules/@wix/fedops-logger/dist/es/src/data-items/data-items.js
var DataItems = /* @__PURE__ */ function() {
  function DataItems2(sessionId, preset) {
    this._sessionId = sessionId;
    this._preset = preset;
  }
  var _proto = DataItems2.prototype;
  _proto.biAppLoadStart = function biAppLoadStart() {
    return this._biDataItem(this._preset.appLoadStart.eventId);
  };
  _proto.biAppLoadFinish = function biAppLoadFinish() {
    return this._biDataItem(this._preset.appLoadFinish.eventId);
  };
  _proto.biInteractionStart = function biInteractionStart() {
    return this._biDataItem(this._preset.interactionStart.eventId);
  };
  _proto.biInteractionEnd = function biInteractionEnd() {
    return this._biDataItem(this._preset.interactionEnd.eventId);
  };
  _proto.biLoadPhaseStart = function biLoadPhaseStart() {
    return this._biDataItem(this._preset.loadPhaseStart.eventId);
  };
  _proto.biLoadPhaseFinish = function biLoadPhaseFinish() {
    return this._biDataItem(this._preset.loadPhaseFinish.eventId);
  };
  _proto.biHttpRequest = function biHttpRequest() {
    return this._biDataItem(this._preset.httpRequest.eventId);
  };
  _proto.biHttpResponse = function biHttpResponse() {
    return this._biDataItem(this._preset.httpResponse.eventId);
  };
  _proto.biHttpResponseTime = function biHttpResponseTime() {
    return this._biDataItem(this._preset.httpResponseTime.eventId);
  };
  _proto.biBlackbox = function biBlackbox(performanceEntry) {
    var entryTypeToEventId = {
      environment: 500,
      "initial-paint": 501,
      loaded: 502,
      visibility: 503,
      "first-input": 504,
      "page-transition": 505,
      crux: 506,
      "crux-cls": 507,
      responsiveness: 508
    };
    var eventId = entryTypeToEventId[performanceEntry.entryType];
    return eventId && this._biDataItem(eventId);
  };
  _proto.blackboxPerformance = function blackboxPerformance(performanceEntry) {
    return this.dataItem(performanceEntry);
  };
  _proto.webVitalsLoaded = function webVitalsLoaded() {
    return this._biDataItem(29);
  };
  _proto.webVitalsFirstInput = function webVitalsFirstInput() {
    return this._biDataItem(39);
  };
  _proto.biError = function biError() {
    return this._biDataItem(this._preset.error.eventId);
  };
  _proto.appName = function appName(_ref) {
    var _appName = _ref.appName, isServerSide = _ref.isServerSide;
    return this.dataItem({
      appName: isServerSide ? _appName + "_ssr" : _appName
    });
  };
  _proto.artifact = function artifact(_ref2) {
    var artifactId2 = _ref2.id, artifactVersion = _ref2.version, isRollout = _ref2.isRollout;
    return this.dataItem({
      artifactId: artifactId2,
      artifactVersion,
      isRollout
    });
  };
  _proto.appContext = function appContext(params) {
    return new BiAppContextDataItem(params);
  };
  _proto.customParams = function customParams(_customParams) {
    return this.dataItem({
      customParams: _customParams
    });
  };
  _proto.duration = function duration(_duration) {
    return new DurationDataItem(_duration);
  };
  _proto.loadingPhaseCollection = function loadingPhaseCollection(phasesOfApp) {
    var phases = JSON.stringify(createPhasesCollection(phasesOfApp));
    return this.dataItem({
      phases
    });
  };
  _proto.loadingPhaseStart = function loadingPhaseStart(_ref3) {
    var name = _ref3.name;
    return this.dataItem({
      name
    });
  };
  _proto.loadingPhaseFinish = function loadingPhaseFinish(_ref4) {
    var name = _ref4.name, phaseStartTime = _ref4.phaseStartTime;
    var duration = calcPhaseDuration(phaseStartTime);
    return this.dataItem({
      name,
      duration
    });
  };
  _proto.dataItem = function dataItem(params) {
    return new DataItem(params);
  };
  _proto._biDataItem = function _biDataItem(eventId) {
    var data = this._asBiEvent(eventId);
    return this.dataItem(data);
  };
  _proto._asBiEvent = function _asBiEvent(evid) {
    return {
      src: this._preset.src,
      evid,
      session_id: this._sessionId,
      _: (/* @__PURE__ */ new Date()).getTime()
      // cache buster
    };
  };
  return DataItems2;
}();

// ../../node_modules/@wix/fedops-logger/dist/es/src/loading-phases/loading-phases-config.js
var phasesConfigValues = {
  SEND_ON_START: "SEND_ON_START",
  SEND_ON_FINISH: "SEND_START_AND_FINISH",
  SEND_ON_APP_LOADED: "SEND_ON_START",
  SEND_START_AND_FINISH: "SEND_START_AND_FINISH"
};

// ../../node_modules/@wix/fedops-overrides/dist/esm/const.js
var FEDOPS_OVERRIDES_COOKIE = "fedops.logger.defaultOverrides";

// ../../node_modules/@wix/fedops-logger/dist/es/src/cookie-overrides/parse-json-from-node-or-scala-servers.js
function parseJsonFromNodeOrScalaServers(rawPayload) {
  function attemptToParseJsonRecursivelySinceScalaServersSerializeItTwice() {
    try {
      return JSON.parse(JSON.parse(rawPayload));
    } catch (err) {
      return null;
    }
  }
  function attemptURIDecodeAndParseJsonSinceNodeServersUseWithURIEncoding() {
    try {
      return JSON.parse(decodeURIComponent(rawPayload));
    } catch (err) {
      return null;
    }
  }
  return attemptToParseJsonRecursivelySinceScalaServersSerializeItTwice() || attemptURIDecodeAndParseJsonSinceNodeServersUseWithURIEncoding();
}

// ../../node_modules/@wix/fedops-logger/dist/es/src/cookie-overrides/cookie-overrides.js
function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    enumerableOnly && (symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    })), keys.push.apply(keys, symbols);
  }
  return keys;
}
function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = null != arguments[i] ? arguments[i] : {};
    i % 2 ? ownKeys(Object(source), true).forEach(function(key) {
      _defineProperty(target, key, source[key]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function(key) {
      Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
    });
  }
  return target;
}
function _defineProperty(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
  } else {
    obj[key] = value;
  }
  return obj;
}
var FEDOPS_OVERRIDES_WINDOW_IDENTIFIER = "__DEFAULT_FEDOPS_OVERRIDES__";
var CookieOverrides = /* @__PURE__ */ function() {
  function CookieOverrides2() {
    var _this$_getOverridesFr;
    this._overridesCookie = (_this$_getOverridesFr = this._getOverridesFromWindow()) != null ? _this$_getOverridesFr : this._getOverridesFromDocumentCookieIfExists();
  }
  CookieOverrides2.persistGlobalOverrides = function persistGlobalOverrides(overridesObject) {
    env()[FEDOPS_OVERRIDES_WINDOW_IDENTIFIER] = overridesObject;
  };
  var _proto = CookieOverrides2.prototype;
  _proto.getGlobalOverrides = function getGlobalOverrides() {
    return this._overridesCookie;
  };
  _proto.getCookieOverridesForApp = function getCookieOverridesForApp(appName) {
    return Object.assign({}, this._overridesCookie.paramsOverrides, this._overridesCookie.paramsOverridesForApp && this._overridesCookie.paramsOverridesForApp[appName]);
  };
  _proto._getOverridesFromDocumentCookieIfExists = function _getOverridesFromDocumentCookieIfExists() {
    try {
      var overridesCookieContent = getCookie(FEDOPS_OVERRIDES_COOKIE);
      var overridesObject = overridesCookieContent ? parseJsonFromNodeOrScalaServers(overridesCookieContent) : overridesCookieContent;
      if (overridesObject && typeof overridesObject === "object") {
        if (overridesObject.paramsOverridesForApp) {
          overridesObject.paramsOverridesForApp = Object.entries(overridesObject.paramsOverridesForApp).reduce(toOverridesWithoutDots, {});
        }
        CookieOverrides2.persistGlobalOverrides(overridesObject);
        return overridesObject;
      } else {
        return {};
      }
    } catch (err) {
      console.log(err);
      return {};
    }
  };
  _proto._getOverridesFromWindow = function _getOverridesFromWindow() {
    return env()[FEDOPS_OVERRIDES_WINDOW_IDENTIFIER];
  };
  return CookieOverrides2;
}();
function toOverridesWithoutDots(overridesMap, _ref) {
  var _objectSpread22;
  var appName = _ref[0], overrides = _ref[1];
  return _objectSpread(_objectSpread({}, overridesMap), {}, (_objectSpread22 = {}, _objectSpread22[appName.replace(/\./g, "-")] = overrides, _objectSpread22));
}
function getCookie(cookieName) {
  if (!env().document || !env().document.cookie) {
    return "";
  }
  var cookieKeyValuePairs = env().document.cookie.split(";").map(function(c) {
    return c.trim().split("=");
  });
  var _cookieKeyValuePairs$ = cookieKeyValuePairs.filter(function(_ref2) {
    var key = _ref2[0];
    return key === cookieName;
  }), relevantCookie = _cookieKeyValuePairs$[0];
  if (relevantCookie) {
    var value = relevantCookie[1];
    return value;
  } else {
    return "";
  }
}

// ../../node_modules/@wix/fedops-logger/dist/es/src/errors/error-flow-type.js
var APP_LOAD_ERROR_FLOW = "app-load";

// ../../node_modules/@wix/fedops-logger/dist/es/src/http-monitoring-preset.js
var http_monitoring_preset_default = {
  src: 72,
  endpoint: "http-client-poc",
  httpRequest: {
    eventId: 1100
  },
  httpResponse: {
    eventId: 1101
  },
  httpResponseTime: {
    eventId: 1102
  }
};

// ../../node_modules/@wix/fedops-logger/dist/es/src/defer.js
var defer = function defer2() {
  var res, rej;
  var promise = new Promise(function(resolve, reject) {
    res = resolve;
    rej = reject;
  });
  promise.resolve = res;
  promise.reject = rej;
  return promise;
};

// ../../node_modules/@wix/fedops-logger/dist/es/src/sample-rate/sample-rate-lists.js
var VERTICALS_LIST = {
  "1380b703-ce81-ff05-f115-39571d94dfcd_1380bbc4-1485-9d44-4616-92e36b1ead6b": [21, 23],
  "1380b703-ce81-ff05-f115-39571d94dfcd_1380bbc4-1485-9d44-4616-92e36b1ead6b_ssr": [21, 23],
  "14271d6f-ba62-d045-549b-ab972ae1f70e": [22, 28],
  "14271d6f-ba62-d045-549b-ab972ae1f70e_142bb34d-3439-576a-7118-683e690a1e0d": [21, 23],
  "14271d6f-ba62-d045-549b-ab972ae1f70e_142bb34d-3439-576a-7118-683e690a1e0d_ssr": [21, 23],
  "14517e1a-3ff0-af98-408e-2bd6953c36a2": [22, 28],
  "1484cb44-49cd-5b39-9681-75188ab429de": [22, 28],
  "14bcded7-0066-7c35-14d7-466cb3f09103": [22, 28],
  "14cc59bc-f0b7-15b8-e1c7-89ce41d0e0c9": [22, 28],
  "14cc59bc-f0b7-15b8-e1c7-89ce41d0e0c9_members": [21],
  "14cc59bc-f0b7-15b8-e1c7-89ce41d0e0c9_members_ssr": [21],
  "14ce1214-b278-a7e4-1373-00cebd1bef7c": [22, 28],
  "14ce1214-b278-a7e4-1373-00cebd1bef7c_getSubscribers": [21, 23],
  "14ce1214-b278-a7e4-1373-00cebd1bef7c_getSubscribers_ssr": [21, 23],
  "14ce1214-b278-a7e4-1373-00cebd1bef7c_init-phase": [21, 23],
  "14ce1214-b278-a7e4-1373-00cebd1bef7c_init-phase_ssr": [21, 23],
  "14ce1214-b278-a7e4-1373-00cebd1bef7c_wixForms": [21, 23],
  "14ce1214-b278-a7e4-1373-00cebd1bef7c_wixForms_ssr": [21, 23],
  "675bbcef-18d8-41f5-800e-131ec9e08762": [22, 28],
  dataBinding: [22, 28],
  "1484cb44-49cd-5b39-9681-75188ab429de_SearchAppController": [21],
  "1484cb44-49cd-5b39-9681-75188ab429de_SearchAppController_ssr": [21]
};
var PHASES_WHITE_LIST = {
  script_loaded: [22, 28],
  await_controller_promise: [22, 28]
};
var SAMPLED_APP_NAMES = Object.keys(VERTICALS_LIST);
var NON_SAMPLED_PHASES = Object.keys(PHASES_WHITE_LIST);

// ../../node_modules/@wix/fedops-logger/dist/es/src/sample-rate/sample-rate-manager.js
var SampleRateManager = /* @__PURE__ */ function() {
  function SampleRateManager2(enableSampleRateForAppNames) {
    this.enableSampleRateForAppNames = enableSampleRateForAppNames;
  }
  var _proto = SampleRateManager2.prototype;
  _proto.shouldSampleAppNameEvent = function shouldSampleAppNameEvent(appName, eventId, name) {
    var _PHASES_WHITE_LIST$na, _VERTICALS_LIST$appNa;
    var isEventAndNameWhiteListed = (_PHASES_WHITE_LIST$na = PHASES_WHITE_LIST[name]) == null ? void 0 : _PHASES_WHITE_LIST$na.includes(eventId);
    var isAppNameInList = SAMPLED_APP_NAMES.includes(appName);
    if (!isAppNameInList || !this.enableSampleRateForAppNames || isEventAndNameWhiteListed) {
      return false;
    }
    var sampledEventIds = (_VERTICALS_LIST$appNa = VERTICALS_LIST[appName]) != null ? _VERTICALS_LIST$appNa : [];
    var isEventIdInSamplingList = sampledEventIds.includes(eventId);
    return this.enableSampleRateForAppNames && isEventIdInSamplingList;
  };
  return SampleRateManager2;
}();

// ../../node_modules/@wix/fedops-logger/dist/es/src/base-logger.js
function _createForOfIteratorHelperLoose2(o, allowArrayLike) {
  var it = typeof Symbol !== "undefined" && o[Symbol.iterator] || o["@@iterator"];
  if (it) return (it = it.call(o)).next.bind(it);
  if (Array.isArray(o) || (it = _unsupportedIterableToArray2(o)) || allowArrayLike && o && typeof o.length === "number") {
    if (it) o = it;
    var i = 0;
    return function() {
      if (i >= o.length) return { done: true };
      return { done: false, value: o[i++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray2(o, minLen) {
  if (!o) return;
  if (typeof o === "string") return _arrayLikeToArray2(o, minLen);
  var n = Object.prototype.toString.call(o).slice(8, -1);
  if (n === "Object" && o.constructor) n = o.constructor.name;
  if (n === "Map" || n === "Set") return Array.from(o);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray2(o, minLen);
}
function _arrayLikeToArray2(arr2, len) {
  if (len == null || len > arr2.length) len = arr2.length;
  for (var i = 0, arr22 = new Array(len); i < len; i++) {
    arr22[i] = arr2[i];
  }
  return arr22;
}
function ownKeys2(object, enumerableOnly) {
  var keys = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    enumerableOnly && (symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    })), keys.push.apply(keys, symbols);
  }
  return keys;
}
function _objectSpread2(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = null != arguments[i] ? arguments[i] : {};
    i % 2 ? ownKeys2(Object(source), true).forEach(function(key) {
      _defineProperty2(target, key, source[key]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys2(Object(source)).forEach(function(key) {
      Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
    });
  }
  return target;
}
function _defineProperty2(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
  } else {
    obj[key] = value;
  }
  return obj;
}
function _defineProperties(target, props) {
  for (var i = 0; i < props.length; i++) {
    var descriptor = props[i];
    descriptor.enumerable = descriptor.enumerable || false;
    descriptor.configurable = true;
    if ("value" in descriptor) descriptor.writable = true;
    Object.defineProperty(target, descriptor.key, descriptor);
  }
}
function _createClass(Constructor, protoProps, staticProps) {
  if (protoProps) _defineProperties(Constructor.prototype, protoProps);
  if (staticProps) _defineProperties(Constructor, staticProps);
  Object.defineProperty(Constructor, "prototype", { writable: false });
  return Constructor;
}
var BLACKBOX = "wixPerformanceMeasurements";
var BLACKBOX_ENDPOINT = "bpm";
var callHook = function callHook2(hook, hookArgs) {
  if (hook) {
    return hook(hookArgs);
  }
};
var getInteractionLogName = function getInteractionLogName2(interactionName, eventGuid) {
  return eventGuid ? "[guid: " + eventGuid + "] " + interactionName : interactionName;
};
var getInteractionNameFromLog = function getInteractionNameFromLog2(interactionLogName) {
  return interactionLogName.replace(/\[guid:\s[^\]]+\]\s/, "");
};
var ARRAY_TYPE = "[object Array]";
var isArray = function isArray2(possiblyArray) {
  return Object.prototype.toString.call(possiblyArray) === ARRAY_TYPE;
};
var BaseLogger = /* @__PURE__ */ function() {
  function BaseLogger2(appName, appVersion, reporter, params) {
    var defaultParams = {
      appId: null,
      widgetId: null,
      metasiteId: null,
      sessionId: session_manager_default.getSessionId(),
      isServerSide: null,
      disableAutoLoadFinish: false,
      phasesConfig: phasesConfigValues.SEND_ON_START,
      interactionTimeout: null,
      timeoutHook: null,
      startHook: null,
      endHook: null,
      isPersistent: false,
      corrId: generateGuid(),
      presetType: presetsTypes.DEFAULT,
      customParams: {},
      paramsOverrides: {},
      enableSampleRateForAppNames: false
    };
    var paramsWithDefaults = Object.assign({}, defaultParams, params);
    this.appName = appName;
    this.appVersion = appVersion;
    this.appId = paramsWithDefaults.appId;
    this.widgetId = paramsWithDefaults.widgetId;
    this.metasiteId = paramsWithDefaults.metasiteId;
    this.corrId = paramsWithDefaults.corrId;
    this.isServerSide = paramsWithDefaults.isServerSide;
    this.params = {
      sessionId: paramsWithDefaults.sessionId
    };
    this.enableSampleRateForAppNames = paramsWithDefaults.enableSampleRateForAppNames;
    this.httpMonitoringDataItems = new DataItems(this.sessionId, http_monitoring_preset_default);
    this.httpMonitoringReporter = create({
      preset: http_monitoring_preset_default
    });
    this._customParams = Object.assign({}, paramsWithDefaults.customParams);
    this.disableAutoLoadFinish = paramsWithDefaults.disableAutoLoadFinish;
    this.phasesConfig = paramsWithDefaults.phasesConfig;
    this._appLoadedCalled = {};
    this._appStartLoadCalled = {};
    this._presetType = paramsWithDefaults.presetType;
    this._preset = presetLoader(this._presetType);
    this._constructorParamsOverrides = paramsWithDefaults.paramsOverrides;
    this._cookiesParamsOverrides = new CookieOverrides();
    this.dataItems = new DataItems(this.sessionId, this._preset);
    this.reporter = reporter || create({
      preset: this._preset
    });
    this._times = new Times(appName);
    this.loadingPhases = new LoadingPhases(appName, this._times);
    this.sampleRateManager = new SampleRateManager(this.enableSampleRateForAppNames);
    this.dataSourceBase = new DataSource();
    this.dataSourceBase.addItem(this.dataItems.appName({
      appName,
      isServerSide: this.isServerSide
    })).addItem(this.dataItems.dataItem({
      corrId: this.corrId,
      is_rollout: false
    })).addItem(this.dataItems.dataItem(paramsWithDefaults.paramsOverrides));
    if (params && params.artifactData) {
      this.dataSourceBase.addItem(this.dataItems.artifact(params.artifactData));
    }
    if ("is_rollout" in this._constructorParamsOverrides && typeof this._constructorParamsOverrides.is_rollout === "undefined") {
      this._constructorParamsOverrides.is_rollout = false;
    }
    this._outgoingInteractions = {};
    this._outgoingHttpRequests = {};
    this.interactionTimeout = paramsWithDefaults.interactionTimeout;
    this.timeoutHook = paramsWithDefaults.timeoutHook;
    this.startHook = paramsWithDefaults.startHook;
    this.endHook = paramsWithDefaults.endHook;
    this.isPersistent = paramsWithDefaults.isPersistent;
    this._firstErrorFiredInFlow = {};
    this._platformTenantsPromise = defer();
    this._webVitalsLoadedPromise = defer();
    this._webVitalsFirstInputPromise = defer();
    if (paramsWithDefaults.reportBlackbox) {
      this._handleBlackboxPerformance();
    }
    this._reportWebVitalsWhenAvailable();
  }
  var _proto = BaseLogger2.prototype;
  _proto._report = function _report(dataSource, reporterEndpoint, params) {
    var data = dataSource.mergeItems();
    if (this.sampleRateManager.shouldSampleAppNameEvent(data.appName || this.appName, data.evid, data.name)) {
      return;
    }
    return this.reporter.report(data, reporterEndpoint, params);
  };
  _proto._reportIfNotSSR = function _reportIfNotSSR(dataSource, reporterEndpoint, params) {
    if (!this.isServerSide || this.appName.includes("thunderbolt")) {
      return this._report(dataSource, reporterEndpoint, params);
    }
  };
  _proto._reportHttpMonitoring = function _reportHttpMonitoring(dataSource) {
    return this.httpMonitoringReporter.report(dataSource.mergeItems(), http_monitoring_preset_default.endpoint);
  };
  _proto.getAppName = function getAppName() {
    return this.appName;
  };
  _proto.getAppVersion = function getAppVersion2() {
    return this.appVersion;
  };
  _proto.getReporter = function getReporter() {
    return this.reporter;
  };
  _proto.getHttpMonitoringReporter = function getHttpMonitoringReporter() {
    return this.httpMonitoringReporter;
  };
  _proto.getParam = function getParam(paramName) {
    return this.params[paramName];
  };
  _proto._isDisableAutoLoadFinish = function _isDisableAutoLoadFinish() {
    return this.disableAutoLoadFinish;
  };
  _proto.isDisableAutoLoadFinish = function isDisableAutoLoadFinish() {
    return this._isDisableAutoLoadFinish();
  };
  _proto.reportNetworkAnalysis = function reportNetworkAnalysis() {
  };
  _proto._getAppLoadingPhaseMarksForApp = function _getAppLoadingPhaseMarksForApp(_ref) {
    var appId = _ref.appId;
    var allMarks = performance_default.getMarks();
    var appMarks = [];
    allMarks.forEach(function(x) {
      var markData = getPhaseMark(x.name);
      if (markData && markData.appId === appId && !markData.widgetId) {
        var startTime = x.startTime, name = x.name;
        var markWithStartTime = Object.assign({}, {
          startTime,
          name
        }, markData);
        appMarks.push(markWithStartTime);
      }
    });
    return appMarks;
  };
  _proto._getTimesOfPhase = function _getTimesOfPhase(_ref2) {
    var _appPhasesMarks$find, _appPhasesMarks$find2;
    var phaseName = _ref2.phaseName, appPhasesMarks = _ref2.appPhasesMarks, appId = _ref2.appId;
    var startMarkTime = (_appPhasesMarks$find = appPhasesMarks.find(function(mark2) {
      return mark2.name === "[fedops] phase:" + phaseName + " " + appId + " started";
    })) == null ? void 0 : _appPhasesMarks$find.startTime;
    var finishMarkTime = (_appPhasesMarks$find2 = appPhasesMarks.find(function(mark2) {
      return mark2.name === "[fedops] phase:" + phaseName + " " + appId + " finished";
    })) == null ? void 0 : _appPhasesMarks$find2.startTime;
    return {
      startMarkTime,
      finishMarkTime
    };
  };
  _proto._getPhaseNamesFromMarks = function _getPhaseNamesFromMarks(marks) {
    return new Set(marks.map(function(mark2) {
      return mark2.phaseName;
    }));
  };
  _proto.reportAppPhasesNetworkAnalysis = function reportAppPhasesNetworkAnalysis() {
  };
  _proto.appLoadStarted = function appLoadStarted(_temp) {
    var _ref3 = _temp === void 0 ? {} : _temp, appId = _ref3.appId, paramsOverrides = _ref3.paramsOverrides;
    if (appId && this._appStartLoadCalled[appId] || !appId && this._appStartLoadCalled[this.appName]) {
      return;
    }
    BaseLogger2._markAppLoadStarted(this.appName);
    this._appStartLoadCalled[appId || this.appName] = true;
    this._times.setLoadStarted({
      appId
    });
    var dataSource = this.dataSource.addItem(this.dataItems.biAppLoadStart()).addItem(this.dataItems.appContext({
      appId: appId || this.appId,
      widgetId: this.widgetId,
      isServerSide: this.isServerSide
    })).addItem(this._getDataItemWithDefaultParamsOverrides({
      appName: appId
    })).addItem(this.dataItems.dataItem(paramsOverrides));
    if (appId) {
      this._changeAppNameForEvent(dataSource, appId);
    }
    return this._report(dataSource, this._getEndpoint());
  };
  _proto.httpRequest = function httpRequest(_ref4) {
    var artifactId2 = _ref4.artifactId, method = _ref4.method, url = _ref4.url;
    var urlTemplate = this._getUrlTemplate(url);
    this._outgoingHttpRequests[urlTemplate] = {
      timestamp: performance_default.now()
    };
    var dataSource = this.dataSource.addItem(this.httpMonitoringDataItems.biHttpRequest()).addItem({
      artifactId: artifactId2,
      method,
      url: urlTemplate
    });
    this._reportHttpMonitoring(dataSource);
  };
  _proto.httpResponse = function httpResponse(_ref5) {
    var artifactId2 = _ref5.artifactId, method = _ref5.method, statusCode = _ref5.statusCode, url = _ref5.url;
    var urlTemplate = this._getUrlTemplate(url);
    if (!this._outgoingHttpRequests[urlTemplate]) {
      throw new Error("To use httpResponse you must use httpRequest first");
    }
    var startTime = this._outgoingHttpRequests[urlTemplate].timestamp;
    var endTime = performance_default.now();
    var responseTime = Math.floor(endTime - startTime);
    var dataSource = this.dataSource.addItem(this.httpMonitoringDataItems.biHttpResponse()).addItem({
      artifactId: artifactId2,
      method,
      responseTime,
      status_code: statusCode,
      url: urlTemplate
    });
    this._httpResponseTime({
      artifactId: artifactId2,
      method,
      responseTime,
      url: urlTemplate
    });
    this._reportHttpMonitoring(dataSource);
  };
  _proto._httpResponseTime = function _httpResponseTime(_ref6) {
    var artifactId2 = _ref6.artifactId, method = _ref6.method, responseTime = _ref6.responseTime, url = _ref6.url;
    var dataSource = this.dataSource.addItem(this.httpMonitoringDataItems.biHttpResponseTime()).addItem({
      artifactId: artifactId2,
      method,
      responseTime,
      url
    });
    return this._reportHttpMonitoring(dataSource);
  };
  _proto._getUrlTemplate = function _getUrlTemplate(url) {
    var _extractUrl = extractUrl(url), origin = _extractUrl.origin, pathname = _extractUrl.pathname;
    var convertedPathname = pathname.split("/").filter(function(p) {
      return !!p;
    }).map(function(p) {
      return getPathnameTemplate(p);
    }).join("/");
    return origin + "/" + convertedPathname;
  };
  _proto._shouldAddCustomParams = function _shouldAddCustomParams(customParams) {
    var isEmptyObject = function isEmptyObject2(obj) {
      return obj && Object.keys(obj).length === 0 && obj.constructor === Object;
    };
    var bothCustomParamsAreEmpty = isEmptyObject(this._customParams) && isEmptyObject(customParams);
    var consentPolicyAccessor = new ConsentPolicyAccessor();
    var _consentPolicyAccesso = consentPolicyAccessor.getCurrentConsentPolicy(), functional = _consentPolicyAccesso.functional, analytics = _consentPolicyAccesso.analytics;
    var consentAllowsCustomParams = functional && analytics;
    return !bothCustomParamsAreEmpty && consentAllowsCustomParams;
  };
  _proto._addCustomParamsToEvent = function _addCustomParamsToEvent(dataSource, customParams) {
    var customParamsObj = typeof customParams === "string" ? JSON.parse(customParams) : customParams;
    customParams = Object.assign({}, this._customParams, customParamsObj);
    if (Object.keys(customParams).length > 0) {
      var stringifiedCustomParams = JSON.stringify(customParams);
      dataSource.addItem(this.dataItems.customParams(stringifiedCustomParams));
    }
  };
  _proto.appLoaded = function appLoaded(_temp2) {
    var _ref7 = _temp2 === void 0 ? {} : _temp2, appId = _ref7.appId, customParams = _ref7.customParams, paramsOverrides = _ref7.paramsOverrides;
    if (appId && this._appLoadedCalled[appId] || !appId && this._appLoadedCalled[this.appName]) {
      return;
    }
    BaseLogger2._clearLoadTimeout();
    BaseLogger2._markAndMeasureAppLoad(this.appName);
    this._appLoadedCalled[appId || this.appName] = true;
    this._sendLastAppLoadPhaseIfNeeded();
    var dataSource = this.dataSource.addItem(this.dataItems.biAppLoadFinish()).addItem(this.dataItems.appContext({
      appId: appId || this.appId,
      widgetId: this.widgetId,
      isServerSide: this.isServerSide
    })).addItem(this.dataItems.duration(this._times.getAppLoadTime({
      appId
    })).setFirstRequestDuration(this._times.getFirstRequestDuration())).addItem(this._getDataItemWithDefaultParamsOverrides({
      appName: appId
    })).addItem(this.dataItems.dataItem(paramsOverrides));
    if (appId) {
      this._changeAppNameForEvent(dataSource, appId);
    }
    if (this._shouldAddCustomParams(customParams)) {
      this._addCustomParamsToEvent(dataSource, customParams);
    }
    return this._report(dataSource, this._getEndpoint());
  };
  _proto.appLoadingPhaseStart = function appLoadingPhaseStart(name, _temp3) {
    var _ref8 = _temp3 === void 0 ? {} : _temp3, appId = _ref8.appId, widgetId = _ref8.widgetId, paramsOverrides = _ref8.paramsOverrides;
    this.loadingPhases.saveLoadingPhase({
      name,
      appId,
      widgetId
    });
    this._sendPreviousPhaseIfNeeded(name, {
      appId,
      widgetId,
      paramsOverrides
    });
    performance_default.mark("[fedops] " + name + " started");
    performance_default.mark("[fedops] phase:" + name + " " + appId + (widgetId ? " " + widgetId : "") + " started");
    var dataSource = this.dataSource.addItem(this.dataItems.biLoadPhaseStart()).addItem(this.dataItems.appContext({
      appId,
      widgetId
    })).addItem(this.dataItems.loadingPhaseStart({
      name
    })).addItem(this._getDataItemWithDefaultParamsOverrides({
      appName: appId
    })).addItem(this.dataItems.dataItem(paramsOverrides));
    if (appId) {
      this._changeAppNameForEvent(dataSource, appId);
    }
    this._report(dataSource, this._getEndpoint());
  };
  _proto._changeAppNameForEvent = function _changeAppNameForEvent(dataSource, appName) {
    dataSource.addItem(this.dataItems.appName({
      appName,
      isServerSide: this.isServerSide
    }));
  };
  _proto._sendPreviousPhaseIfNeeded = function _sendPreviousPhaseIfNeeded(name, _temp4) {
    var _ref9 = _temp4 === void 0 ? {} : _temp4, appId = _ref9.appId, widgetId = _ref9.widgetId, paramsOverrides = _ref9.paramsOverrides;
    var previousPhase = this.loadingPhases.getPhasePreviousTo({
      name,
      appId,
      widgetId
    });
    if (previousPhase && this.phasesConfig === phasesConfigValues.SEND_ON_START) {
      performance_default.mark("[fedops] " + previousPhase.name + " finished");
      var loadingPhaseDataItem = this.dataItems.loadingPhaseFinish(previousPhase);
      var loadingPhaseBiItem = this.dataItems.biLoadPhaseFinish();
      var overridesDataItem = this.dataItems.dataItem(paramsOverrides);
      var dataSource = this.dataSource.addItem(loadingPhaseDataItem).addItem(loadingPhaseBiItem).addItem(this._getDataItemWithDefaultParamsOverrides({
        appName: appId
      })).addItem(overridesDataItem);
      this._report(dataSource);
    }
  };
  _proto.appLoadingPhaseFinish = function appLoadingPhaseFinish(name, _temp5, _temp6) {
    var _ref10 = _temp5 === void 0 ? {} : _temp5, appId = _ref10.appId, widgetId = _ref10.widgetId, widgetArray = _ref10.widgetArray, paramsOverrides = _ref10.paramsOverrides;
    var _ref11 = _temp6 === void 0 ? {} : _temp6, endHook = _ref11.endHook;
    if (this.phasesConfig === phasesConfigValues.SEND_ON_START) {
      throw new Error('To use appLoadingPhaseFinish you must use "phasesConfig: SEND_START_AND_FINISH" setting');
    }
    performance_default.mark("[fedops] " + name + " finished");
    performance_default.mark("[fedops] phase:" + name + " " + appId + (widgetId ? " " + widgetId : "") + " finished");
    var loadingPhase = this.loadingPhases.getAppLoadingPhaseData({
      name,
      appId,
      widgetId
    });
    if (!loadingPhase) {
      throw new Error("Cannot report end of a phase that wasn't started. Phase " + name + " doesn't exist");
    }
    var loadingPhaseDataItem = this.dataItems.loadingPhaseFinish(loadingPhase);
    var loadingPhaseBiItem = this.dataItems.biLoadPhaseFinish();
    var defaultParamsOverridesDataItem = this._getDataItemWithDefaultParamsOverrides({
      appName: appId
    });
    var overridesDataItem = this.dataItems.dataItem(paramsOverrides);
    var eventContextDataItem = this.dataItems.appContext({
      appId,
      widgetId,
      widgetArray,
      isServerSide: this.isServerSide
    });
    callHook(endHook || this.endHook, {
      name: loadingPhase.name,
      duration: Math.floor(performance_default.now() - loadingPhase.phaseStartTime)
    });
    var dataSource = this.dataSource.addItem(loadingPhaseDataItem).addItem(loadingPhaseBiItem).addItem(eventContextDataItem).addItem(defaultParamsOverridesDataItem).addItem(overridesDataItem);
    if (appId) {
      this._changeAppNameForEvent(dataSource, appId);
    }
    return this._report(dataSource, this._getEndpoint());
  };
  _proto._getEndpoint = function _getEndpoint() {
    return this.isPersistent ? this._preset.persistentEndpoint : this._preset.nonPersistentEndpoint;
  };
  _proto._getDataItemWithDefaultParamsOverrides = function _getDataItemWithDefaultParamsOverrides(_temp7) {
    var _ref12 = _temp7 === void 0 ? {} : _temp7, _ref12$appName = _ref12.appName, appName = _ref12$appName === void 0 ? null : _ref12$appName;
    return this.dataItems.dataItem(_objectSpread2(_objectSpread2({}, this._cookiesParamsOverrides.getCookieOverridesForApp(appName || this.appName)), this._constructorParamsOverrides));
  };
  _proto._sendLastAppLoadPhaseIfNeeded = function _sendLastAppLoadPhaseIfNeeded() {
    var appLoadingPhase = this.loadingPhases.getNextPhaseToReport();
    var shouldReportLastPhase = appLoadingPhase && this.phasesConfig === phasesConfigValues.SEND_ON_START;
    if (shouldReportLastPhase) {
      this._report(this.dataSource.addItem(this.dataItems.loadingPhaseFinish(appLoadingPhase)).addItem(this.dataItems.biLoadPhaseFinish()));
    }
  };
  BaseLogger2._clearLoadTimeout = function _clearLoadTimeout() {
    if (env() && env().fedops && typeof env().fedops.clearLoadTimeout === "function") {
      env().fedops.clearLoadTimeout();
    }
  };
  _proto.clearResourceTimings = function clearResourceTimings2() {
    performance_default.clearResourceTimings();
  };
  BaseLogger2._markAppLoadStarted = function _markAppLoadStarted(appName) {
    performance_default.mark("[fedops] " + appName + " app-load-started");
  };
  BaseLogger2._markAndMeasureAppLoad = function _markAndMeasureAppLoad(appName) {
    performance_default.mark("[fedops] " + appName + " app-loaded");
    try {
      performance_default.measure("[fedops] " + appName + " app-loaded", "[fedops] " + appName + " app-load-started", "[fedops] " + appName + " app-loaded");
    } catch (e) {
    }
  };
  _proto._markInteractionStarted = function _markInteractionStarted(interactionName) {
    performance_default.mark("[fedops] " + this.appName + " interaction " + interactionName + " started");
  };
  _proto._markAndMeasureInteractionEnded = function _markAndMeasureInteractionEnded(interactionName, outgoingInteraction) {
    if (outgoingInteraction) {
      performance_default.mark("[fedops] " + this.appName + " interaction " + interactionName + " ended");
      try {
        performance_default.measure("[fedops] " + interactionName + " duration", "[fedops] " + this.appName + " interaction " + interactionName + " started", "[fedops] " + this.appName + " interaction " + interactionName + " ended");
      } catch (e) {
      }
    }
  };
  _proto.interactionStarted = function interactionStarted(interactionName, _temp8) {
    var _this = this;
    var _ref13 = _temp8 === void 0 ? {} : _temp8, eventGuid = _ref13.eventGuid, stack = _ref13.stack, timeOverride = _ref13.timeOverride, interactionTimeout = _ref13.interactionTimeout, startHook = _ref13.startHook, timeoutHook = _ref13.timeoutHook, customParams = _ref13.customParams, paramsOverrides = _ref13.paramsOverrides;
    this._markInteractionStarted(interactionName);
    var scheduleTimeoutEvent = function scheduleTimeoutEvent2() {
      if (interactionTimeout || _this.interactionTimeout) {
        return setTimeout(function() {
          var dataSource2 = _this.dataSource.addItem({
            interactionName
          }).addItem({
            errorType: "timeout"
          }).addItem(_this.dataItems.biError());
          _this._reportIfNotSSR(dataSource2);
          callHook(timeoutHook || _this.timeoutHook, {
            name: interactionName,
            timeout: interactionTimeout || _this.interactionTimeout
          });
        }, interactionTimeout || _this.interactionTimeout);
      }
    };
    var dataSource = this.dataSource.addItem({
      name: interactionName
    }).addItem(this.dataItems.biInteractionStart()).addItem(this._getDataItemWithDefaultParamsOverrides()).addItem(this.dataItems.dataItem(paramsOverrides));
    if (this._shouldAddCustomParams(customParams)) {
      this._addCustomParamsToEvent(dataSource, customParams);
    }
    var interactionLogName = getInteractionLogName(interactionName, eventGuid);
    var outGoingInteraction = {
      timestamp: timeOverride ? timeOverride : performance_default.now(),
      timeout: interactionTimeout || this.interactionTimeout
    };
    if (stack) {
      this._outgoingInteractions[interactionLogName] = this._outgoingInteractions[interactionLogName] || [];
      this._outgoingInteractions[interactionLogName].push(outGoingInteraction);
    } else {
      this._outgoingInteractions[interactionLogName] = outGoingInteraction;
    }
    this._reportIfNotSSR(dataSource);
    callHook(startHook || this.startHook, {
      name: interactionName
    });
    var timeoutId = scheduleTimeoutEvent();
    return {
      timeoutId
    };
  };
  _proto.interactionEnded = function interactionEnded(interactionName, _temp9) {
    var _ref14 = _temp9 === void 0 ? {} : _temp9, eventGuid = _ref14.eventGuid, timeOverride = _ref14.timeOverride, timeoutId = _ref14.timeoutId, endHook = _ref14.endHook, customParams = _ref14.customParams, paramsOverrides = _ref14.paramsOverrides;
    var interactionLogName = getInteractionLogName(interactionName, eventGuid);
    var outgoingInteraction = isArray(this._outgoingInteractions[interactionLogName]) ? this._outgoingInteractions[interactionLogName].pop() : this._outgoingInteractions[interactionLogName];
    this._markAndMeasureInteractionEnded(interactionName, outgoingInteraction);
    if (timeoutId) {
      clearTimeout(timeoutId);
    }
    var interaction = outgoingInteraction || this._getInteractionFromWindowIfPresent(interactionName);
    var endTime = timeOverride ? timeOverride : performance_default.now();
    var duration = interaction ? Math.floor(endTime - interaction.timestamp) : "";
    callHook(endHook || this.endHook, {
      name: interactionName,
      duration,
      timeout: outgoingInteraction && outgoingInteraction.timeout
    });
    var dataSource = this.dataSource.addItem({
      name: interactionName
    }).addItem({
      duration
    }).addItem(this.dataItems.biInteractionEnd()).addItem(this._getDataItemWithDefaultParamsOverrides()).addItem(this.dataItems.dataItem(paramsOverrides));
    if (this._shouldAddCustomParams(customParams)) {
      this._addCustomParamsToEvent(dataSource, customParams);
    }
    if (!isArray(this._outgoingInteractions[interactionLogName]) || !this._outgoingInteractions[interactionLogName].length) {
      delete this._outgoingInteractions[interactionLogName];
    }
    this._reportIfNotSSR(dataSource);
  };
  _proto._getInteractionsObjectFromWindow = function _getInteractionsObjectFromWindow() {
    var _env, _env$fedops, _env$fedops$apps, _env$fedops$apps$this;
    return ((_env = env()) == null ? void 0 : (_env$fedops = _env.fedops) == null ? void 0 : (_env$fedops$apps = _env$fedops.apps) == null ? void 0 : (_env$fedops$apps$this = _env$fedops$apps[this.appName]) == null ? void 0 : _env$fedops$apps$this.interactions) || {};
  };
  _proto._getInteractionFromWindowIfPresent = function _getInteractionFromWindowIfPresent(interactionName) {
    return this._getInteractionsObjectFromWindow()[interactionName];
  };
  _proto.flush = function flush() {
    this.reporter.flush();
  };
  _proto._handleBlackboxPerformance = function _handleBlackboxPerformance() {
    var _this2 = this;
    var actualEnv = env();
    var wixPerformanceMeasurements = actualEnv[BLACKBOX];
    if (wixPerformanceMeasurements) {
      this._handleBlackboxPerformanceEntries(actualEnv, wixPerformanceMeasurements);
    }
    if (actualEnv.addEventListener) {
      actualEnv.addEventListener(BLACKBOX, function(_ref15) {
        var detail = _ref15.detail;
        return _this2._handleBlackboxPerformanceEntries(actualEnv, detail);
      });
    }
  };
  _proto._handleBlackboxPerformanceEntries = function _handleBlackboxPerformanceEntries(actualEnv, measurements) {
    var _this3 = this;
    measurements.filter(function(_ref16) {
      var then = _ref16.then;
      return then;
    }).forEach(function(performanceEntryPromise) {
      performanceEntryPromise.then(function(performanceEntry) {
        switch (performanceEntry.entryType) {
          case "loaded":
            _this3._webVitalsLoadedPromise.resolve(performanceEntry);
            break;
          case "first-input":
            _this3._webVitalsFirstInputPromise.resolve(performanceEntry);
            break;
          case "page-transitions":
          case "crux-cls-s":
          case "responsiveness":
            if (performanceEntry.eventName && actualEnv.addEventListener) {
              actualEnv.addEventListener(performanceEntry.eventName, function(_ref17) {
                var detail = _ref17.detail;
                return _this3._sendBlackboxMeasurement(detail);
              });
            }
            return;
        }
        _this3._sendBlackboxMeasurement(performanceEntry);
      });
    });
  };
  _proto._sendBlackboxMeasurement = function _sendBlackboxMeasurement(measurement) {
    var blackboxBiDataItem = this.dataItems.biBlackbox(measurement);
    if (blackboxBiDataItem) {
      var blackboxDataItem = this.dataItems.blackboxPerformance(measurement);
      var dataSource = new DataSource().addItem(blackboxBiDataItem).addItem(blackboxDataItem);
      var params;
      if (measurement.entryType === "loaded") {
        params = {
          useBatch: false
        };
      }
      this._report(dataSource, BLACKBOX_ENDPOINT, params);
      if (measurement.entryType === "crux") {
        this.flush();
      }
    }
  };
  _proto.getLoggerForWidget = function getLoggerForWidget(_ref18) {
    var appName = _ref18.appName, appId = _ref18.appId, widgetId = _ref18.widgetId, version = _ref18.version, timeoutHook = _ref18.timeoutHook, startHook = _ref18.startHook, endHook = _ref18.endHook, useGlobalLogger = _ref18.useGlobalLogger, paramsOverrides = _ref18.paramsOverrides, phasesConfig = _ref18.phasesConfig;
    var widgetAppName = appName || appId + "_" + widgetId;
    var widgetVersion = version || this.getAppVersion();
    var reporter = useGlobalLogger ? this.reporter : create({
      biLoggerFactory: this.reporter._factory,
      preset: this._preset
    });
    var config = {
      isServerSide: this.isServerSide,
      appId,
      widgetId,
      sessionId: this.sessionId,
      phasesConfig: phasesConfig || phasesConfigValues.SEND_ON_START,
      isPersistent: this.isPersistent,
      timeoutHook,
      startHook,
      endHook,
      customParams: this._customParams,
      presetType: this._presetType,
      paramsOverrides: _objectSpread2(_objectSpread2({}, this._constructorParamsOverrides), paramsOverrides),
      enableSampleRateForAppNames: this.enableSampleRateForAppNames
    };
    return new BaseLogger2(widgetAppName, widgetVersion, reporter, config);
  };
  _proto._getUnReportedErrorFlows = function _getUnReportedErrorFlows(flows) {
    var _this4 = this;
    return flows.filter(function(flow) {
      return !_this4._firstErrorFiredInFlow[flow];
    });
  };
  _proto.reportErrorThrownOncePerFlow = function reportErrorThrownOncePerFlow() {
    var _this5 = this;
    var flows = this._getFlowsOfError();
    var unReportedErrorFlows = this._getUnReportedErrorFlows(flows);
    if (unReportedErrorFlows.length) {
      unReportedErrorFlows.forEach(function(flow) {
        _this5._firstErrorFiredInFlow[flow] = true;
        var dataSource = _this5.dataSource.addItem({
          interactionName: flow
        }).addItem(_this5.dataItems.biError());
        _this5._report(dataSource);
      });
    }
  };
  _proto._errorHappenedInOneOfAppLoadFlows = function _errorHappenedInOneOfAppLoadFlows() {
    return this._errorHappenedInAppLoadFlow() || this._errorHappenedInAppLoadingPhasesFlow();
  };
  _proto._errorHappenedInAppLoadFlow = function _errorHappenedInAppLoadFlow() {
    var appLoadStarted = this._getAppLoadStartedIndication();
    var appLoaded = this._getAppLoadedIndication();
    return appLoadStarted && !appLoaded;
  };
  _proto._getAppLoadStartedIndication = function _getAppLoadStartedIndication() {
    var _performance$getEntri;
    var appLoadStartedMarkName = "[fedops] " + this.appName + " app-load-started";
    var appLoadStartedMark = (_performance$getEntri = performance_default.getEntriesByName(appLoadStartedMarkName)) == null ? void 0 : _performance$getEntri[0];
    var appLoadStartedTime = this._times.getLoadStartTime();
    return appLoadStartedMark || appLoadStartedTime;
  };
  _proto._getAppLoadedIndication = function _getAppLoadedIndication() {
    var _performance$getEntri2;
    var appLoadedMarkName = "[fedops] " + this.appName + " app-loaded";
    var appLoadedMark = (_performance$getEntri2 = performance_default.getEntriesByName(appLoadedMarkName)) == null ? void 0 : _performance$getEntri2[0];
    return appLoadedMark || this._appLoadedCalled[this.appName];
  };
  _proto._errorHappenedInAppLoadingPhasesFlow = function _errorHappenedInAppLoadingPhasesFlow(_temp10) {
    var _ref19 = _temp10 === void 0 ? {
      appId: this.appName
    } : _temp10, appId = _ref19.appId;
    var appPhasesMarks = this._getAppLoadingPhaseMarksForApp({
      appId
    });
    var phaseNames = this._getPhaseNamesFromMarks(appPhasesMarks);
    for (var _iterator = _createForOfIteratorHelperLoose2(phaseNames), _step; !(_step = _iterator()).done; ) {
      var phaseName = _step.value;
      var _this$_getTimesOfPhas = this._getTimesOfPhase({
        phaseName,
        appPhasesMarks,
        appId
      }), startMarkTime = _this$_getTimesOfPhas.startMarkTime, finishMarkTime = _this$_getTimesOfPhas.finishMarkTime;
      if (startMarkTime && !finishMarkTime) {
        return true;
      }
    }
    return false;
  };
  _proto._getFlowsOfError = function _getFlowsOfError() {
    var flowsWithErrors = this._errorHappenedInOneOfAppLoadFlows() ? [APP_LOAD_ERROR_FLOW] : [];
    return [].concat(flowsWithErrors, this._getInteractionNamesWhereErrorHappened());
  };
  _proto._getInteractionNamesWhereErrorHappened = function _getInteractionNamesWhereErrorHappened() {
    var interactionsFromInstance = this._getInteractionNamesWhereErrorHappenedFromInstance();
    if (interactionsFromInstance.length) {
      return interactionsFromInstance;
    }
    var interactionsFromWindow = Object.keys(this._getInteractionsObjectFromWindow());
    if (interactionsFromWindow.length) {
      return interactionsFromWindow;
    }
    return this._getInteractionNamesWhereErrorHappenedFromPerformanceMarks();
  };
  _proto._getInteractionNamesWhereErrorHappenedFromInstance = function _getInteractionNamesWhereErrorHappenedFromInstance() {
    return Object.keys(this._outgoingInteractions).map(function(logName) {
      return getInteractionNameFromLog(logName);
    });
  };
  _proto._getInteractionNamesWhereErrorHappenedFromPerformanceMarks = function _getInteractionNamesWhereErrorHappenedFromPerformanceMarks() {
    var interactionMarks = this._getInteractionMarksOfCurrentApp();
    return BaseLogger2._getInteractionsThatDidntEndFromMarks(interactionMarks);
  };
  _proto._getInteractionMarksOfCurrentApp = function _getInteractionMarksOfCurrentApp() {
    var _this6 = this;
    var allMarks = performance_default.getMarks();
    return allMarks.reduce(function(interactionMarksOfCurrentApp, mark2) {
      var markData = getInteractionMark(mark2.name);
      if (markData && markData.appName === _this6.appName) {
        interactionMarksOfCurrentApp[markData.interactionName] = interactionMarksOfCurrentApp[markData.interactionName] || [];
        interactionMarksOfCurrentApp[markData.interactionName].push({
          startTime: mark2.startTime,
          name: mark2.name
        });
      }
      return interactionMarksOfCurrentApp;
    }, {});
  };
  BaseLogger2._getInteractionsThatDidntEndFromMarks = function _getInteractionsThatDidntEndFromMarks(interactionsMarks) {
    var unFinishedInteractions = [];
    for (var interactionName in interactionsMarks) {
      if (interactionsMarks.hasOwnProperty(interactionName)) {
        var _BaseLogger$_countSta = BaseLogger2._countStartedEndedInteractionMarks(interactionsMarks[interactionName]), startedInteractions = _BaseLogger$_countSta.startedInteractions, endedInteractions = _BaseLogger$_countSta.endedInteractions;
        var noInteractionMarks = !startedInteractions && !endedInteractions;
        var moreStartedOrEndedInteractions = startedInteractions - endedInteractions !== 0;
        if (noInteractionMarks || moreStartedOrEndedInteractions) {
          unFinishedInteractions.push(interactionName);
        }
      }
    }
    return unFinishedInteractions;
  };
  BaseLogger2._countStartedEndedInteractionMarks = function _countStartedEndedInteractionMarks(interactionsMarks) {
    var startedInteractions = 0;
    var endedInteractions = 0;
    interactionsMarks.forEach(function(mark2) {
      if (mark2.name.includes("started")) {
        startedInteractions++;
      } else if (mark2.name.includes("ended")) {
        endedInteractions++;
      }
    });
    return {
      startedInteractions,
      endedInteractions
    };
  };
  _proto.registerPlatformTenants = function registerPlatformTenants(appNames) {
    this._platformTenantsPromise.resolve(appNames);
  };
  _proto._reportWebVitalsWhenAvailable = function _reportWebVitalsWhenAvailable() {
    var _this7 = this;
    Promise.all([this._webVitalsLoadedPromise, this._platformTenantsPromise]).then(function(_ref20) {
      var webVitalsLoaded = _ref20[0], platformTenantAppNames = _ref20[1];
      var cls = webVitalsLoaded.cls, lcp = webVitalsLoaded.lcp;
      platformTenantAppNames.forEach(function(appName) {
        var dataSource = _this7.dataSource.addItem(_this7.dataItems.webVitalsLoaded()).addItem(_this7.dataItems.appName({
          appName
        })).addItem({
          cls,
          lcp
        });
        _this7._report(dataSource);
      });
    });
    Promise.all([this._webVitalsFirstInputPromise, this._platformTenantsPromise]).then(function(_ref21) {
      var webVitalsFirstInput = _ref21[0], platformTenantAppNames = _ref21[1];
      var delay = webVitalsFirstInput.delay;
      platformTenantAppNames.forEach(function(appName) {
        var dataSource = _this7.dataSource.addItem(_this7.dataItems.webVitalsFirstInput()).addItem(_this7.dataItems.appName({
          appName
        })).addItem({
          fid: delay
        });
        _this7._report(dataSource);
      });
    });
  };
  _createClass(BaseLogger2, [{
    key: "dataSource",
    get: function get3() {
      return this.dataSourceBase.clone();
    }
  }, {
    key: "sessionId",
    get: function get3() {
      return this.getParam("sessionId");
    },
    set: function set3(sessionId) {
      this.params["sessionId"] = sessionId;
    }
  }]);
  return BaseLogger2;
}();

// ../../node_modules/@wix/fedops-logger/dist/es/src/logger-factory.js
function ownKeys3(object, enumerableOnly) {
  var keys = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    enumerableOnly && (symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    })), keys.push.apply(keys, symbols);
  }
  return keys;
}
function _objectSpread3(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = null != arguments[i] ? arguments[i] : {};
    i % 2 ? ownKeys3(Object(source), true).forEach(function(key) {
      _defineProperty3(target, key, source[key]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys3(Object(source)).forEach(function(key) {
      Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
    });
  }
  return target;
}
function _defineProperty3(obj, key, value) {
  if (key in obj) {
    Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true });
  } else {
    obj[key] = value;
  }
  return obj;
}
function getAppVersion() {
  return env().__CI_APP_VERSION__ || DEFAULT_APP_VERSION;
}
function create2(appName, params) {
  if (params === void 0) {
    params = {};
  }
  if (!appName) {
    return null;
  }
  var appNameWithoutDots = appName.replace(/\./g, "-").toLowerCase();
  initializeGlobal(appNameWithoutDots);
  var fullParams = _objectSpread3(_objectSpread3({}, params), {}, {
    preset: presetLoader(params.presetType)
  });
  var reporter = create(fullParams);
  return new BaseLogger(appNameWithoutDots, getAppVersion(), reporter, fullParams);
}

// ../../node_modules/@wix/fe-essentials/dist/esm/testkit/EssentialsPartialMockDelegate.js
var import_web_bi_logger = __toESM(require_tracking_logger());
var Sentry = __toESM(require("@sentry/browser"));

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/ErrorHandler.js
var import_defineProperty6 = __toESM(require_defineProperty());

// ../../node_modules/@wix/error-handler-core/@wix/bi-logger-os-data/dist/esm/v2/index.js
function httpClientErrorMappingSrc176Evid100(params) {
  return { evid: 100, src: 176, endpoint: "", params };
}
function getResolvedErrorSrc176Evid101(params) {
  return { evid: 101, src: 176, endpoint: "", params };
}
function showErrorSrc176Evid102(params) {
  return { evid: 102, src: 176, endpoint: "", params };
}
function retryErrorSrc176Evid103(params) {
  return { evid: 103, src: 176, endpoint: "", params };
}
function httpClientErrorSrc176Evid404(params) {
  return { evid: 404, src: 176, endpoint: "", params };
}

// ../../node_modules/@wix/error-handler/dist/esm/util/withoutDefaults.js
function withoutDefaults(fn) {
  return fn;
}

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/reportErrorBi.js
function reportErrorBi(errorHandler, params) {
  var _webWindow$location;
  const {
    errorAccessor,
    requestOptions
  } = params;
  const {
    biLogger,
    onlineManager,
    webWindow,
    environment,
    biDefaultsOverrides
  } = errorHandler;
  const {
    statusCode,
    errorData,
    errorDetails,
    errorMessage,
    exceptionMessage,
    httpError,
    error
  } = errorAccessor;
  const config = httpError == null ? void 0 : httpError.config;
  biLogger.report(withoutDefaults(httpClientErrorSrc176Evid404)({
    artifact_id: environment.artifactId,
    full_artifact_id: environment.fullArtifactId,
    hostingEnvironment: biDefaultsOverrides == null ? void 0 : biDefaultsOverrides.hostingEnvironment,
    app_id: biDefaultsOverrides == null ? void 0 : biDefaultsOverrides.app_id,
    artifactVersion: environment.version,
    error_code: statusCode,
    error_json: errorData ? JSON.stringify(errorData) : void 0,
    exception_message: exceptionMessage,
    error_message: errorMessage,
    client_error_code: errorAccessor == null ? void 0 : errorAccessor.clientCode,
    error_type: errorDetails != null && errorDetails.applicationError ? "applicationError" : errorDetails != null && errorDetails.validationError ? "validationError" : void 0,
    request_id: httpError == null ? void 0 : httpError.requestId,
    hasInternetConnection: onlineManager.onLine,
    pageUrl: webWindow == null || (_webWindow$location = webWindow.location) == null ? void 0 : _webWindow$location.href,
    session_id: error == null ? void 0 : error._errorHandlerSessionId,
    serverUrl: (config == null ? void 0 : config.url) || (requestOptions == null ? void 0 : requestOptions.url),
    method: (config == null ? void 0 : config.method) || (requestOptions == null ? void 0 : requestOptions.method) || "unknown",
    entity_fqdn: config == null ? void 0 : config.entityFqdn,
    method_fqn: config == null ? void 0 : config.methodFqn,
    server_artifactid: config == null ? void 0 : config.artifactId
  }));
}

// ../../node_modules/@wix/error-handler/dist/esm/state/ErrorResolution.js
var import_defineProperty = __toESM(require_defineProperty());

// ../../node_modules/@wix/error-handler/dist/esm/util/statusCodeNumericToText.js
var statusCodeNumericToText = {
  400: "INVALID_ARGUMENT",
  401: "UNAUTHENTICATED",
  403: "PERMISSION_DENIED",
  404: "NOT_FOUND",
  409: "ALREADY_EXISTS",
  428: "FAILED_PRECONDITION",
  429: "RESOURCE_EXHAUSTED",
  499: "CANCELED",
  500: "INTERNAL",
  501: "NOT_IMPLEMENTED",
  502: "BAD_GATEWAY",
  503: "UNAVAILABLE"
};

// ../../node_modules/@wix/error-handler/dist/esm/util/resolveFromMapping.js
function resolveFromMapping(params) {
  const {
    errorAccessor,
    errorCodesMap
  } = params;
  const {
    applicationError,
    statusCode,
    error,
    validationError
  } = errorAccessor;
  if (applicationError) {
    var _errorCodesMap$applic;
    const {
      code
    } = applicationError;
    const applicationErrorMapping = (_errorCodesMap$applic = errorCodesMap.applicationError) == null ? void 0 : _errorCodesMap$applic[code];
    if (applicationErrorMapping === false) {
      return null;
    }
    if (applicationErrorMapping) {
      return applicationErrorMapping(applicationError, error);
    }
  }
  if (errorCodesMap.validationError) {
    var _validationError$fiel;
    const rules = Object.keys(errorCodesMap.validationError);
    const firstValidationError = validationError == null || (_validationError$fiel = validationError.fieldViolations) == null ? void 0 : _validationError$fiel.find((_ref) => {
      let {
        ruleName
      } = _ref;
      return rules.includes(ruleName);
    });
    if (firstValidationError != null && firstValidationError.ruleName) {
      var _errorCodesMap$valida;
      const validationErrorMapping = (_errorCodesMap$valida = errorCodesMap.validationError) == null ? void 0 : _errorCodesMap$valida[firstValidationError.ruleName];
      if (validationErrorMapping) {
        return validationErrorMapping(error);
      }
    }
  }
  const statusCodeText = statusCode != null ? statusCodeNumericToText[statusCode] : null;
  const statusCodeErrorMap = {
    ...errorCodesMap.serverError ? {
      INTERNAL: errorCodesMap.serverError,
      NOT_IMPLEMENTED: errorCodesMap.serverError,
      BAD_GATEWAY: errorCodesMap.serverError,
      UNAVAILABLE: errorCodesMap.serverError
    } : {},
    ...errorCodesMap.statusCodeError
  };
  const statusCodeErrorMappingFunction = statusCodeText != null ? statusCodeErrorMap[statusCodeText] : null;
  if (statusCodeErrorMappingFunction) {
    return statusCodeErrorMappingFunction(error);
  }
  return null;
}

// ../../node_modules/@wix/error-handler/dist/esm/state/ErrorResolution.js
var ErrorResolution = class {
  constructor(params) {
    (0, import_defineProperty.default)(this, "errorAccessor", void 0);
    (0, import_defineProperty.default)(this, "errorCodesMap", void 0);
    (0, import_defineProperty.default)(this, "commonErrorsMapState", void 0);
    (0, import_defineProperty.default)(this, "onlineManager", void 0);
    (0, import_defineProperty.default)(this, "consumerResolvedError", void 0);
    (0, import_defineProperty.default)(this, "commonResolvedError", void 0);
    this.commonErrorsMapState = params.commonErrorsMapState;
    this.errorAccessor = params.errorAccessor;
    this.errorCodesMap = params.errorCodesMap;
    this.onlineManager = params.onlineManager;
  }
  get commonErrors() {
    return this.commonErrorsMapState.commonErrors;
  }
  _resolveNetworkError() {
    const {
      onlineManager,
      commonErrors,
      errorAccessor
    } = this;
    if (!commonErrors) {
      return null;
    }
    if (onlineManager.onLine) {
      return commonErrors.getNetworkErrorShowErrorProps(errorAccessor);
    }
    return commonErrors.connectionErrorShowErrorProps;
  }
  _resolveCommonError() {
    const {
      errorAccessor,
      commonErrors,
      errorCodesMap
    } = this;
    const {
      error,
      httpError,
      validationError
    } = errorAccessor;
    if (httpError == null) {
      return this._resolveNetworkError();
    }
    if (!commonErrors) {
      return null;
    }
    if (validationError) {
      var _commonErrors$commonE;
      return (_commonErrors$commonE = commonErrors.commonErrorsMap.statusCodeError) == null || _commonErrors$commonE.INVALID_ARGUMENT == null ? void 0 : _commonErrors$commonE.INVALID_ARGUMENT(error);
    }
    const resolvedError = resolveFromMapping({
      errorAccessor,
      errorCodesMap: commonErrors.commonErrorsMap
    });
    if (!resolvedError) {
      return (errorCodesMap == null || errorCodesMap.serverError == null ? void 0 : errorCodesMap.serverError(error)) ?? commonErrors.defaultErrorShowErrorProps;
    }
    return resolvedError;
  }
  _resolveConsumerError() {
    const {
      errorCodesMap,
      errorAccessor
    } = this;
    const {
      httpError
    } = errorAccessor;
    if (httpError == null || errorCodesMap == null) {
      return null;
    }
    return resolveFromMapping({
      errorAccessor,
      errorCodesMap
    });
  }
  resolve() {
    this.consumerResolvedError = this._resolveConsumerError();
    if (this.consumerResolvedError != null) {
      return;
    }
    this.commonResolvedError = this._resolveCommonError();
  }
  get errorToShow() {
    return this.consumerResolvedError ?? this.commonResolvedError;
  }
};

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/createErrorResolution.js
function createErrorResolution(errorHandler, params) {
  const {
    commonErrorsMapState,
    onlineManager
  } = errorHandler;
  const {
    errorAccessor,
    errorCodesMap
  } = params;
  const errorResolution = new ErrorResolution({
    errorAccessor,
    errorCodesMap,
    commonErrorsMapState,
    onlineManager
  });
  errorResolution.resolve();
  return errorResolution;
}

// ../../node_modules/@wix/error-handler/dist/esm/util/createBiLoggerWithDefaults.js
function createBiLoggerWithDefaults(params) {
  return typeof params.biLoggerOrFactory === "function" ? params.biLoggerOrFactory().logger() : params.biLoggerOrFactory;
}

// ../../node_modules/@wix/error-handler/dist/esm/state/ErrorAccessor.js
var import_defineProperty2 = __toESM(require_defineProperty());

// ../../node_modules/@wix/error-handler/dist/esm/util/httpErrorDecoders.js
function isObject2(thing) {
  return thing != null;
}
function isHttpError(err) {
  return isObject2(err) && err.response != null;
}
function responseDataHasMessage(data) {
  return isObject2(data) && data.message != null;
}
function responseDataHasDetails(data) {
  return isObject2(data) && data.details != null;
}

// ../../node_modules/@wix/error-handler/dist/esm/util/sdkErrorDecoders.js
function isObject3(thing) {
  return thing != null;
}
function isSdkError(err) {
  return isObject3(err) && err.status != null;
}
function toMinimalHttpError(err) {
  return {
    response: {
      data: {
        details: err.details
      },
      status: err.status
    },
    requestId: err.requestId
  };
}

// ../../node_modules/@wix/error-handler/dist/esm/state/ErrorAccessor.js
var ErrorAccessor = class {
  constructor(params) {
    (0, import_defineProperty2.default)(this, "inputError", void 0);
    (0, import_defineProperty2.default)(this, "_error", void 0);
    (0, import_defineProperty2.default)(this, "_httpError", void 0);
    (0, import_defineProperty2.default)(this, "_errorDetails", void 0);
    this.inputError = params.error;
  }
  get error() {
    return this._error ??= this.inputError instanceof Error ? this.inputError : null;
  }
  /**
   * The message of the JS error, for example: `Network Error`
   */
  get exceptionMessage() {
    var _this$error;
    return (_this$error = this.error) == null ? void 0 : _this$error.message;
  }
  /**
   * Error object from the http client, including the http response
   */
  get httpError() {
    const {
      inputError
    } = this;
    return this._httpError ??= isHttpError(inputError) ? inputError : isSdkError(inputError) ? toMinimalHttpError(inputError) : null;
  }
  /**
   * errno symbolic name** like `ENETUNREACH`, Not http status code
   * This code is how the browser represents the error
   */
  get clientCode() {
    var _this$httpError;
    return (_this$httpError = this.httpError) == null ? void 0 : _this$httpError.code;
  }
  /**
   * The http response of the error
   */
  get errorResponse() {
    var _this$httpError2;
    return (_this$httpError2 = this.httpError) == null ? void 0 : _this$httpError2.response;
  }
  /**
   * The http status code of the http response, numeric value (e.g. 404)
   */
  get statusCode() {
    var _this$errorResponse;
    return (_this$errorResponse = this.errorResponse) == null ? void 0 : _this$errorResponse.status;
  }
  /**
   * The data (JSON) of the http response
   */
  get errorData() {
    var _this$errorResponse2;
    return (_this$errorResponse2 = this.errorResponse) == null ? void 0 : _this$errorResponse2.data;
  }
  /**
   * The details of the error, if available
   */
  get errorDetails() {
    const {
      errorData
    } = this;
    return this._errorDetails ??= responseDataHasDetails(errorData) ? errorData.details : null;
  }
  get errorMessage() {
    const {
      errorData
    } = this;
    return responseDataHasMessage(errorData) ? errorData.message : void 0;
  }
  get applicationError() {
    var _this$errorDetails;
    return (_this$errorDetails = this.errorDetails) == null ? void 0 : _this$errorDetails.applicationError;
  }
  get validationError() {
    var _this$errorDetails2;
    return (_this$errorDetails2 = this.errorDetails) == null ? void 0 : _this$errorDetails2.validationError;
  }
};

// ../../node_modules/@wix/error-handler/dist/esm/state/TranslationsState.js
var import_defineProperty3 = __toESM(require_defineProperty());

// ../../node_modules/@wix/error-handler/dist/esm/services/translationsImportsMap.js
var translationsImportsMap = {
  ar: () => Promise.resolve().then(() => __toESM(require_common_errors_ar())),
  bg: () => Promise.resolve().then(() => __toESM(require_common_errors_bg())),
  ca: () => Promise.resolve().then(() => __toESM(require_common_errors_ca())),
  cs: () => Promise.resolve().then(() => __toESM(require_common_errors_cs())),
  da: () => Promise.resolve().then(() => __toESM(require_common_errors_da())),
  de: () => Promise.resolve().then(() => __toESM(require_common_errors_de())),
  el: () => Promise.resolve().then(() => __toESM(require_common_errors_el())),
  en: () => Promise.resolve().then(() => __toESM(require_common_errors_en())),
  es: () => Promise.resolve().then(() => __toESM(require_common_errors_es())),
  fi: () => Promise.resolve().then(() => __toESM(require_common_errors_fi())),
  fr: () => Promise.resolve().then(() => __toESM(require_common_errors_fr())),
  he: () => Promise.resolve().then(() => __toESM(require_common_errors_he())),
  hi: () => Promise.resolve().then(() => __toESM(require_common_errors_hi())),
  hr: () => Promise.resolve().then(() => __toESM(require_common_errors_hr())),
  hu: () => Promise.resolve().then(() => __toESM(require_common_errors_hu())),
  id: () => Promise.resolve().then(() => __toESM(require_common_errors_id())),
  it: () => Promise.resolve().then(() => __toESM(require_common_errors_it())),
  ja: () => Promise.resolve().then(() => __toESM(require_common_errors_ja())),
  ko: () => Promise.resolve().then(() => __toESM(require_common_errors_ko())),
  lt: () => Promise.resolve().then(() => __toESM(require_common_errors_lt())),
  lv: () => Promise.resolve().then(() => __toESM(require_common_errors_lv())),
  ms: () => Promise.resolve().then(() => __toESM(require_common_errors_ms())),
  nl: () => Promise.resolve().then(() => __toESM(require_common_errors_nl())),
  no: () => Promise.resolve().then(() => __toESM(require_common_errors_no())),
  pl: () => Promise.resolve().then(() => __toESM(require_common_errors_pl())),
  pt: () => Promise.resolve().then(() => __toESM(require_common_errors_pt())),
  ro: () => Promise.resolve().then(() => __toESM(require_common_errors_ro())),
  ru: () => Promise.resolve().then(() => __toESM(require_common_errors_ru())),
  sk: () => Promise.resolve().then(() => __toESM(require_common_errors_sk())),
  sl: () => Promise.resolve().then(() => __toESM(require_common_errors_sl())),
  sv: () => Promise.resolve().then(() => __toESM(require_common_errors_sv())),
  th: () => Promise.resolve().then(() => __toESM(require_common_errors_th())),
  tl: () => Promise.resolve().then(() => __toESM(require_common_errors_tl())),
  tr: () => Promise.resolve().then(() => __toESM(require_common_errors_tr())),
  uk: () => Promise.resolve().then(() => __toESM(require_common_errors_uk())),
  vi: () => Promise.resolve().then(() => __toESM(require_common_errors_vi())),
  zh: () => Promise.resolve().then(() => __toESM(require_common_errors_zh()))
};

// ../../node_modules/@wix/error-handler/dist/esm/state/asyncMessagesLoader.js
var resolveMessages = (messages) => typeof messages.default === "object" ? messages.default : messages;
var asyncMessagesLoader = async (locale) => {
  const factory2 = translationsImportsMap[locale];
  if (factory2 == null) {
    throw new Error(`@wix/cairo: Failed to find translations for locale '${locale}'`);
  }
  return resolveMessages(await factory2());
};

// ../../node_modules/@wix/error-handler/dist/esm/state/TranslationsState.js
var TranslationsState = class {
  constructor(_params) {
    (0, import_defineProperty3.default)(this, "i18n", void 0);
    (0, import_defineProperty3.default)(this, "_initPromise", void 0);
    (0, import_defineProperty3.default)(this, "_ready", void 0);
    (0, import_defineProperty3.default)(this, "translate", (key, params) => this.i18n.t(key, params));
    (0, import_defineProperty3.default)(this, "getTranslateFn", async () => {
      await this.getInitPromise();
      return this.translate;
    });
    this.i18n = _params.createI18n({
      asyncMessagesLoader,
      disableAutoInit: true
    });
    this._initPromise = new Promise(async (resolve) => {
      let failedLoading = false;
      const onFailedLoading = (lng, _ns, err) => {
        if (lng !== "en") {
          return;
        }
        this.i18n.off("failedLoading", onFailedLoading);
        failedLoading = true;
        resolve(err);
      };
      this.i18n.on("failedLoading", onFailedLoading);
      try {
        await this.i18n.init();
        if (failedLoading) {
          return;
        }
        resolve(null);
        this._ready = true;
      } catch (e) {
        resolve(e);
      }
    });
  }
  async getInitPromise() {
    const error = await this._initPromise;
    if (error) {
      throw error;
    }
  }
};

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/reportErrorResolutionBi.js
function reportErrorResolutionBi(errorHandler, params) {
  var _errorCodesMap$applic;
  const {
    errorAccessor,
    errorCodesMap
  } = params;
  const {
    biLogger
  } = errorHandler;
  const {
    error,
    applicationError
  } = errorAccessor;
  const code = applicationError == null ? void 0 : applicationError.code;
  const applicationErrorMapping = errorCodesMap == null || (_errorCodesMap$applic = errorCodesMap.applicationError) == null ? void 0 : _errorCodesMap$applic[code];
  biLogger.report(withoutDefaults(httpClientErrorMappingSrc176Evid100)({
    error_opt_out: applicationErrorMapping === false,
    is_override_error: String(errorCodesMap != null),
    session_id: error == null ? void 0 : error._errorHandlerSessionId
  }));
}

// ../../node_modules/@wix/error-handler/node_modules/uuid/dist/esm-node/stringify.js
var byteToHex = [];
for (let i = 0; i < 256; ++i) {
  byteToHex.push((i + 256).toString(16).slice(1));
}
function unsafeStringify(arr2, offset = 0) {
  return (byteToHex[arr2[offset + 0]] + byteToHex[arr2[offset + 1]] + byteToHex[arr2[offset + 2]] + byteToHex[arr2[offset + 3]] + "-" + byteToHex[arr2[offset + 4]] + byteToHex[arr2[offset + 5]] + "-" + byteToHex[arr2[offset + 6]] + byteToHex[arr2[offset + 7]] + "-" + byteToHex[arr2[offset + 8]] + byteToHex[arr2[offset + 9]] + "-" + byteToHex[arr2[offset + 10]] + byteToHex[arr2[offset + 11]] + byteToHex[arr2[offset + 12]] + byteToHex[arr2[offset + 13]] + byteToHex[arr2[offset + 14]] + byteToHex[arr2[offset + 15]]).toLowerCase();
}

// ../../node_modules/@wix/error-handler/node_modules/uuid/dist/esm-node/rng.js
var import_node_crypto = __toESM(require("crypto"));
var rnds8Pool = new Uint8Array(256);
var poolPtr = rnds8Pool.length;
function rng() {
  if (poolPtr > rnds8Pool.length - 16) {
    import_node_crypto.default.randomFillSync(rnds8Pool);
    poolPtr = 0;
  }
  return rnds8Pool.slice(poolPtr, poolPtr += 16);
}

// ../../node_modules/@wix/error-handler/node_modules/uuid/dist/esm-node/native.js
var import_node_crypto2 = __toESM(require("crypto"));
var native_default = {
  randomUUID: import_node_crypto2.default.randomUUID
};

// ../../node_modules/@wix/error-handler/node_modules/uuid/dist/esm-node/v4.js
function v4(options, buf, offset) {
  if (native_default.randomUUID && !buf && !options) {
    return native_default.randomUUID();
  }
  options = options || {};
  const rnds = options.random || (options.rng || rng)();
  rnds[6] = rnds[6] & 15 | 64;
  rnds[8] = rnds[8] & 63 | 128;
  if (buf) {
    offset = offset || 0;
    for (let i = 0; i < 16; ++i) {
      buf[offset + i] = rnds[i];
    }
    return buf;
  }
  return unsafeStringify(rnds);
}
var v4_default = v4;

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/reportGetResolvedErrorBi.js
function reportGetResolvedErrorBi(errorHandler, params) {
  const {
    message,
    action,
    errorAccessor,
    errorResolution
  } = params;
  const {
    biLogger,
    commonErrorsMapState,
    translations
  } = errorHandler;
  const {
    error
  } = errorAccessor;
  const {
    consumerResolvedError
  } = errorResolution ?? {};
  biLogger.report(withoutDefaults(getResolvedErrorSrc176Evid101)({
    session_id: error == null ? void 0 : error._errorHandlerSessionId,
    translated_message: message,
    action_text: action == null ? void 0 : action.text,
    is_consumer_resolved_error: consumerResolvedError != null,
    common_errors_load_failed: String(commonErrorsMapState.commonErrors == null),
    isEn: translations.i18n.language === "en"
  }));
}

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/reportShowErrorBi.js
function reportShowErrorBi(errorHandler, params) {
  var _errorAccessor$error;
  const {
    message,
    action,
    errorAccessor
  } = params;
  const {
    biLogger,
    translations
  } = errorHandler;
  biLogger.report(withoutDefaults(showErrorSrc176Evid102)({
    session_id: (_errorAccessor$error = errorAccessor.error) == null ? void 0 : _errorAccessor$error._errorHandlerSessionId,
    displayed_message: message,
    displayed_action: action == null ? void 0 : action.text,
    action_was_provided: String((action == null ? void 0 : action.onClick) != null),
    isEn: translations.i18n.language === "en"
  }));
}

// ../../node_modules/@wix/error-handler/dist/esm/state/CommonErrorsMapState.js
var import_defineProperty4 = __toESM(require_defineProperty());

// ../../node_modules/@wix/error-handler/dist/esm/state/getErrorCodesMap.js
var getErrorCodesMap = () => Promise.resolve().then(() => (init_commonErrorsMapping(), commonErrorsMapping_exports));

// ../../node_modules/@wix/error-handler/dist/esm/state/CommonErrorsMapState.js
var CommonErrorsMapState = class {
  constructor(params) {
    (0, import_defineProperty4.default)(this, "translations", void 0);
    (0, import_defineProperty4.default)(this, "errorMonitor", void 0);
    (0, import_defineProperty4.default)(this, "webWindow", void 0);
    (0, import_defineProperty4.default)(this, "getErrorCodesMap", void 0);
    (0, import_defineProperty4.default)(this, "_initPromise", void 0);
    (0, import_defineProperty4.default)(this, "commonErrors", void 0);
    this.translations = params.translations;
    this.errorMonitor = params.errorMonitor;
    this.webWindow = params.webWindow;
    this.getErrorCodesMap = params.getErrorCodesMap ?? getErrorCodesMap;
    this._initPromise = this._init();
  }
  async getInitPromise() {
    const error = await this._initPromise;
    if (error) {
      throw error;
    }
  }
  async _init() {
    try {
      const {
        setCommonErrors: setCommonErrors2
      } = await this.getErrorCodesMap();
      await setCommonErrors2(this);
      return null;
    } catch (e) {
      return e;
    }
  }
};

// ../../node_modules/@wix/error-handler/dist/esm/fallbackMessage.js
var fallbackMessage = "This action wasn't completed due to a technical issue.";

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/reportErrorRetryBi.js
function reportErrorRetryBi(errorHandler, params) {
  const {
    biLogger
  } = errorHandler;
  const {
    errorAccessor
  } = params;
  const {
    error
  } = errorAccessor;
  const sessionId = error == null ? void 0 : error._errorHandlerSessionId;
  if (!sessionId) {
    return;
  }
  biLogger.report(withoutDefaults(retryErrorSrc176Evid103)({
    session_id: sessionId
  }));
}

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/ErrorHandlerV2.js
var import_defineProperty5 = __toESM(require_defineProperty());
var ErrorHandlerV2 = class {
  constructor(_errorHandler) {
    (0, import_defineProperty5.default)(this, "errorHandler", void 0);
    (0, import_defineProperty5.default)(this, "withErrorHandler", async (fn, errorCodesMap) => {
      return this.errorHandler.withErrorHandler(fn, {
        errorCodesMap: {
          validationError: errorCodesMap,
          statusCodeError: errorCodesMap,
          applicationError: Object.entries(errorCodesMap).reduce((acc, _ref) => {
            let [key, value] = _ref;
            if (value) {
              acc[key] = (_, error) => value(error);
            }
            return acc;
          }, {}),
          serverError: errorCodesMap.serverError
        }
      });
    });
    (0, import_defineProperty5.default)(this, "getResolvedError", (error) => {
      const {
        errorHandler
      } = this;
      const {
        errorMonitor
      } = errorHandler;
      try {
        const errorAccessor = new ErrorAccessor({
          error
        });
        const {
          httpError,
          error: e
        } = errorAccessor;
        const errorResolution = errorHandler._resolveError(errorAccessor);
        const resolvedError = e == null ? void 0 : e._errorHandlerResolvedError;
        const message = (resolvedError == null ? void 0 : resolvedError.message) ?? fallbackMessage;
        const action = resolvedError == null ? void 0 : resolvedError.action;
        const requestId = (resolvedError == null ? void 0 : resolvedError.requestId) ?? (httpError == null ? void 0 : httpError.requestId);
        reportGetResolvedErrorBi(errorHandler, {
          message,
          action,
          errorAccessor,
          errorResolution
        });
        return {
          message,
          action,
          requestId,
          ...resolvedError
        };
      } catch (e) {
        console.error(e);
        errorMonitor.captureException(e);
        throw e;
      }
    });
    (0, import_defineProperty5.default)(this, "showError", (error, props) => {
      this.errorHandler.showError(error, props);
    });
    (0, import_defineProperty5.default)(this, "reportRetryAttempt", (error) => {
      this.errorHandler.reportRetryAttempt(error);
    });
    (0, import_defineProperty5.default)(this, "handleError", (error, options) => {
      this.errorHandler.handleError(error, options);
    });
    this.errorHandler = _errorHandler;
  }
};

// ../../node_modules/@wix/error-handler/dist/esm/util/dedupeRequestId.js
function dedupeRequestId(requestId) {
  var _requestId$split$;
  if (requestId == null) {
    return requestId;
  }
  return (_requestId$split$ = requestId.split(",")[0]) == null ? void 0 : _requestId$split$.trim();
}

// ../../node_modules/@wix/error-handler/dist/esm/error-handler/ErrorHandler.js
var ErrorHandler = class {
  constructor(params) {
    var _this = this;
    (0, import_defineProperty6.default)(this, "environment", void 0);
    (0, import_defineProperty6.default)(this, "biDefaultsOverrides", void 0);
    (0, import_defineProperty6.default)(this, "biLogger", void 0);
    (0, import_defineProperty6.default)(this, "errorMonitor", void 0);
    (0, import_defineProperty6.default)(this, "experiments", void 0);
    (0, import_defineProperty6.default)(this, "platformShowError", void 0);
    (0, import_defineProperty6.default)(this, "translations", void 0);
    (0, import_defineProperty6.default)(this, "commonErrorsMapState", void 0);
    (0, import_defineProperty6.default)(this, "onlineManager", void 0);
    (0, import_defineProperty6.default)(this, "webWindow", void 0);
    (0, import_defineProperty6.default)(this, "_skipWaitTranslations", void 0);
    (0, import_defineProperty6.default)(this, "v2", void 0);
    (0, import_defineProperty6.default)(this, "handleError", function(error, options) {
      if (options === void 0) {
        options = {};
      }
      if (_this._isErrorHandlerDisabled()) {
        return;
      }
      const {
        errorMonitor
      } = _this;
      try {
        const errorAccessor = new ErrorAccessor({
          error
        });
        _this._setErrorHandlerSessionId(errorAccessor);
        reportErrorBi(_this, {
          errorAccessor,
          requestOptions: options.requestOptions
        });
      } catch (e) {
        console.error(e);
        errorMonitor.captureException(e);
      }
    });
    (0, import_defineProperty6.default)(this, "withErrorHandler", async (fn, options) => {
      try {
        return await fn();
      } catch (error) {
        this._setErrorHandlerErrorCodesMap(error, options);
        if (process.env.NODE_ENV === "test" && !this._skipWaitTranslations) {
          await this.commonErrorsMapState.getInitPromise();
        }
        throw error;
      }
    });
    (0, import_defineProperty6.default)(this, "getResolvedError", (error) => {
      const {
        errorMonitor
      } = this;
      try {
        const errorAccessor = new ErrorAccessor({
          error
        });
        const {
          applicationError,
          validationError,
          httpError,
          error: e
        } = errorAccessor;
        const errorResolution = this._resolveError(errorAccessor);
        const resolvedError = e == null ? void 0 : e._errorHandlerResolvedError;
        const message = (resolvedError == null ? void 0 : resolvedError.message) ?? fallbackMessage;
        reportGetResolvedErrorBi(this, {
          message,
          action: resolvedError == null ? void 0 : resolvedError.action,
          errorAccessor,
          errorResolution
        });
        return {
          message,
          action: resolvedError == null ? void 0 : resolvedError.action,
          validationError,
          applicationError,
          httpError,
          requestId: dedupeRequestId(httpError == null ? void 0 : httpError.requestId)
        };
      } catch (e) {
        console.error(e);
        errorMonitor.captureException(e);
        throw e;
      }
    });
    (0, import_defineProperty6.default)(this, "reportRetryAttempt", (error) => {
      const {
        errorMonitor
      } = this;
      try {
        const errorAccessor = new ErrorAccessor({
          error
        });
        reportErrorRetryBi(this, {
          errorAccessor
        });
      } catch (e) {
        console.error(e);
        errorMonitor.captureException(e);
      }
    });
    (0, import_defineProperty6.default)(this, "showError", (error, props) => {
      const {
        platformShowError,
        errorMonitor
      } = this;
      if (!platformShowError) {
        return;
      }
      try {
        var _errorAccessor$httpEr;
        const errorAccessor = new ErrorAccessor({
          error
        });
        const {
          error: e
        } = errorAccessor;
        this._resolveError(errorAccessor);
        const resolvedError = e == null ? void 0 : e._errorHandlerResolvedError;
        const showErrorProps = {
          ...resolvedError,
          ...props
        };
        const message = (props == null ? void 0 : props.message) ?? (resolvedError == null ? void 0 : resolvedError.message) ?? fallbackMessage;
        const action = (props == null ? void 0 : props.action) ?? (resolvedError == null ? void 0 : resolvedError.action);
        reportShowErrorBi(this, {
          message,
          action,
          errorAccessor
        });
        platformShowError({
          ...showErrorProps,
          requestId: dedupeRequestId((_errorAccessor$httpEr = errorAccessor.httpError) == null ? void 0 : _errorAccessor$httpEr.requestId),
          message,
          action: action != null && action.onClick ? {
            text: action.text,
            onClick: action.onClick
          } : void 0
        });
      } catch (e) {
        console.error(e);
        errorMonitor.captureException(e);
      }
    });
    this.environment = params.environment;
    this.biDefaultsOverrides = params.biDefaultsOverrides;
    this.biLogger = createBiLoggerWithDefaults(params);
    this.errorMonitor = params.createErrorMonitor({
      dsn: "https://831e1d96e7944c6aae0c9ed9d6babd35@sentry.wixpress.com/5896"
    });
    this.experiments = params.createExperiments == null ? void 0 : params.createExperiments({
      scopes: ["error-handler"]
    });
    this.platformShowError = params.showError;
    this.translations = new TranslationsState(params);
    this.onlineManager = params.onlineManager ?? (typeof navigator !== "undefined" ? navigator : {
      onLine: true
    });
    this.webWindow = params.webWindow ?? (typeof window !== "undefined" ? window : void 0);
    this.commonErrorsMapState = new CommonErrorsMapState(this);
    this.v2 = new ErrorHandlerV2(this);
  }
  _setErrorHandlerSessionId(errorAccessor) {
    const {
      error
    } = errorAccessor;
    if (!error) {
      return;
    }
    error._errorHandlerSessionId = v4_default();
  }
  _setErrorHandlerResolvedError(errorAccessor, errorResolution) {
    const {
      error
    } = errorAccessor;
    if (!error) {
      return;
    }
    error._errorHandlerResolvedError = errorResolution.errorToShow;
  }
  _setErrorHandlerErrorCodesMap(error, options) {
    if (options === void 0) {
      options = {};
    }
    const {
      errorCodesMap
    } = options;
    const errorAccessor = new ErrorAccessor({
      error
    });
    reportErrorResolutionBi(this, {
      errorAccessor,
      errorCodesMap
    });
    const {
      error: e
    } = errorAccessor;
    if (!e) {
      return;
    }
    e._errorHandlerErrorCodesMap = errorCodesMap;
  }
  _isErrorHandlerDisabled() {
    const {
      experiments
    } = this;
    return experiments != null && !experiments.enabled("specs.os.EnableErrorHandlerInEditor");
  }
  _resolveError(errorAccessor, options) {
    var _errorAccessor$error;
    if (options === void 0) {
      options = {};
    }
    if (this._isErrorHandlerDisabled()) {
      return;
    }
    const errorCodesMap = options.errorCodesMap ?? ((_errorAccessor$error = errorAccessor.error) == null ? void 0 : _errorAccessor$error._errorHandlerErrorCodesMap);
    const errorResolution = createErrorResolution(this, {
      errorAccessor,
      errorCodesMap
    });
    this._setErrorHandlerResolvedError(errorAccessor, errorResolution);
    return errorResolution;
  }
  toJSON() {
    return {};
  }
};

// ../../node_modules/@wix/fe-essentials/dist/esm/core/assertEnabledAPI.js
function assertEnabledAPI(value, message) {
  if (process.env.NODE_ENV !== "production" && value == null) {
    throw new Error(message);
  }
}

// ../../node_modules/@wix/fe-essentials/dist/esm/core/EssentialsPartialGetters.js
var EssentialsPartialGetters = class {
  getExperiments(fallback) {
    const experiments = this.experiments ?? fallback?.experiments;
    assertEnabledAPI(experiments, "Experiments were used without being correctly initialized! Make sure you pass `{ experiments: { scopes: [...] } }` to the `createEssentials` function.");
    return experiments;
  }
  getFedopsLogger(fallback) {
    const fedopsLogger = this.fedopsLogger ?? fallback?.fedopsLogger;
    assertEnabledAPI(fedopsLogger, "Fedops was used without being correctly initialized! Make sure you pass`{ fedops: { appName: '...' } }` to the `createEssentials` function.");
    return fedopsLogger;
  }
  getErrorMonitor(fallback) {
    const errorMonitor = this.errorMonitor ?? fallback?.errorMonitor;
    assertEnabledAPI(errorMonitor, "ErrorMonitor was used without being correctly initialized! Make sure you pass `{ errorMonitor: { ... } }` to the `createEssentials` function.");
    return errorMonitor;
  }
  getI18n(fallback) {
    const _i18n = this.i18n ?? fallback?.i18n;
    assertEnabledAPI(_i18n, "i18n was used without being correctly initialized! Make sure you pass `{ i18n: { ... } }` to the `createEssentials` function.");
    return _i18n;
  }
  getBILoggerFactory(fallback) {
    const biLoggerFactory = this.biLoggerFactory ?? fallback?.biLoggerFactory;
    assertEnabledAPI(biLoggerFactory, "BILoggerFactory was not initialized");
    return biLoggerFactory;
  }
  getBILogger(fallback) {
    const biLogger = this.biLogger ?? fallback?.biLogger;
    assertEnabledAPI(biLogger, "BILogger was not initialized");
    return biLogger;
  }
  getHttpClient(fallback) {
    const httpClient = this.httpClient ?? fallback?.httpClient;
    assertEnabledAPI(httpClient, "HttpClient was not initialized");
    return httpClient;
  }
  getEnvironment(fallback) {
    const environment = fallback?.environment;
    assertEnabledAPI(environment, "Environment was not initialized");
    return environment;
  }
  getErrorHandler(fallback) {
    const errorHandler = fallback?.errorHandler;
    assertEnabledAPI(errorHandler, "ErrorHandler was not initialized");
    return errorHandler;
  }
  getDataCapsule(fallback) {
    const dataCapsule = this.dataCapsule ?? fallback?.dataCapsule;
    assertEnabledAPI(dataCapsule, "DataCapsule was used without being correctly initialized! Make sure you pass `{ dataCapsule: { ... } }` to the `createEssentials` function.");
    return dataCapsule;
  }
  getCreateCurrencyFormatter(fallback) {
    const createCurrencyFormatter = this.createCurrencyFormatter ?? fallback?.createCurrencyFormatter;
    assertEnabledAPI(createCurrencyFormatter, "CreateCurrencyFormatter was not initialized");
    return createCurrencyFormatter;
  }
  getFormatAddress(fallback) {
    const formatAddress = this.formatAddress ?? fallback?.formatAddress;
    assertEnabledAPI(formatAddress, "formatAddress was not initialized");
    return formatAddress;
  }
  getAbTranslate(fallback) {
    const abTranslate = this.abTranslate ?? fallback?.abTranslate;
    assertEnabledAPI(abTranslate, "abTranslate was not initialized");
    return abTranslate;
  }
  getPanoramaClient(fallback) {
    const panoramaClient = this.panoramaClient ?? fallback?.panoramaClient;
    assertEnabledAPI(panoramaClient, "panoramaClient was not initialized");
    return panoramaClient;
  }
};

// ../../node_modules/@wix/data-capsule/dist/esm/base-storage.js
var BaseStorage = class _BaseStorage {
  static verify(strategy) {
    if (strategy instanceof _BaseStorage) {
      return strategy;
    } else {
      throw new Error("This class must extend BaseStorage!");
    }
  }
  extendScope(scope) {
    return scope;
  }
};

// ../../node_modules/@wix/data-capsule/dist/esm/data-capsule.js
var DataCapsule = class extends BaseStorage {
  constructor({ strategy, ...options }) {
    super();
    this.storageStrategy = BaseStorage.verify(strategy);
    this._options = options;
  }
  async setItem(key, value, options) {
    const validOptions = this._buildValidateOptions(this._options, options);
    return this.storageStrategy.setItem(key, value, validOptions);
  }
  getItem(key, options) {
    const validOptions = this._buildValidateOptions(this._options, options);
    return this.storageStrategy.getItem(key, validOptions);
  }
  removeItem(key, options) {
    const validOptions = this._buildValidateOptions(this._options, options);
    return this.storageStrategy.removeItem(key, validOptions);
  }
  getAllItems(options) {
    const validOptions = this._buildValidateOptions(this._options, options);
    return this.storageStrategy.getAllItems(validOptions);
  }
  _buildValidateOptions(capsuleOptions, options) {
    options = { ...capsuleOptions, ...options };
    options.scope = this.storageStrategy.extendScope(options.scope);
    if (!validateNamespace(options)) {
      throw new Error("namespace is required and should be a string");
    }
    return options;
  }
};
function validateNamespace(options) {
  return typeof options.namespace === "string";
}

// ../../node_modules/@wix/data-capsule/dist/esm/utils/constants.js
var errors = {
  NOT_FOUND: new Error("Key was not found in capsule"),
  SERVER_ERROR: new Error("Failed to perform operarion on server"),
  LOCAL_STORAGE_UNSUPPORTED: new Error("LocalStorage is not supported"),
  COOKIE_CONSENT_DISALLOWED: new Error("The item cannot be set because the user has not approved the category it belongs to")
};
var NOT_FOUND = errors.NOT_FOUND;
var SERVER_ERROR = errors.SERVER_ERROR;
var LOCAL_STORAGE_UNSUPPORTED = errors.LOCAL_STORAGE_UNSUPPORTED;
var COOKIE_CONSENT_DISALLOWED = errors.COOKIE_CONSENT_DISALLOWED;

// ../../node_modules/@wix/data-capsule/dist/esm/strategies/in-memory-storage.js
var InMemoryStorageStrategy = class extends BaseStorage {
  constructor() {
    super();
    this.memoryMap = {};
  }
  setItem(key, value, options) {
    if (typeof this.memoryMap[options.namespace] === "undefined") {
      this.memoryMap[options.namespace] = {};
    }
    this.memoryMap[options.namespace][key] = value;
    return Promise.resolve();
  }
  getItem(key, options) {
    const data = this.memoryMap[options.namespace] && this.memoryMap[options.namespace][key];
    return typeof data !== "undefined" ? Promise.resolve(data) : Promise.reject(NOT_FOUND);
  }
  removeItem(key, options) {
    delete this.memoryMap[options.namespace][key];
    return Promise.resolve();
  }
  getAllItems(options) {
    return Promise.resolve(this.memoryMap[options.namespace]);
  }
};

// ../../node_modules/@wix/fe-essentials/dist/esm/testkit/EssentialsPartialMockDelegate.js
var EssentialsPartialMockDelegate = class extends EssentialsPartialGetters {
  constructor({ httpClient: httpClientOptions, i18n: i18nOptions, errorMonitor: errorMonitorOptions, experiments: experimentsOptions, fedops: fedopsOptions, dataCapsule: dataCapsuleOptions, errorHandler: errorHandlerOptions, parent }) {
    super();
    this.panoramaClient = null;
    const environment = this.getEnvironment(parent);
    this.httpClient = httpClientOptions ? createHttpClientMock(httpClientOptions.scenarios ?? []) : null;
    this.dataCapsule = dataCapsuleOptions ? (() => {
      const { namespace } = dataCapsuleOptions;
      return new DataCapsule({
        namespace,
        strategy: new InMemoryStorageStrategy()
      });
    })() : null;
    this.experiments = experimentsOptions ? new ReadOnlyExperiments({ experiments: experimentsOptions.bag }) : null;
    this.fedopsLogger = fedopsOptions ? create2(fedopsOptions.appName ?? environment.artifactId) : null;
    this.errorMonitor = errorMonitorOptions ? createHub({
      Sentry,
      integrations: [],
      environment: "test",
      dsn: "https://examplePublicKey@o0.ingest.sentry.io/0",
      ...errorMonitorOptions
    }) : null;
    this.i18n = i18nOptions ? initI18n({
      locale: environment.language,
      messages: typeof i18nOptions.messages === "function" ? i18nOptions.messages(environment.language) : i18nOptions.messages,
      asyncMessagesLoader: i18nOptions.asyncMessagesLoader,
      disableAutoInit: true
    }) : null;
    this.biLoggerFactory = import_web_bi_logger.factory;
    this.errorHandler = errorHandlerOptions ? new ErrorHandler({
      environment,
      biLoggerOrFactory: this.biLoggerFactory,
      createExperiments: () => new ReadOnlyExperiments({ experiments: experimentsOptions?.bag }),
      createI18n: (opts) => initI18n({
        locale: environment.language,
        ...opts
      }),
      createErrorMonitor: (opts) => createHub({
        Sentry,
        integrations: [],
        ...opts
      }),
      ...errorHandlerOptions
    }) : null;
  }
};

// ../../node_modules/@wix/fe-essentials/dist/esm/testkit/toMockOptions.js
function toMockOptions({ errorMonitor, experiments, i18n: i18n2, fedops, dataCapsule }) {
  return {
    ...errorMonitor && {
      errorMonitor: {
        dsn: errorMonitor.dsn,
        transport: errorMonitor.transport
      }
    },
    ...experiments && {
      experiments: {
        bag: "bag" in experiments ? experiments.bag : {}
      }
    },
    ...i18n2 && {
      i18n: {
        messages: i18n2.messages,
        asyncMessagesLoader: i18n2.asyncMessagesLoader
      }
    },
    ...fedops && {
      fedops: {
        appName: fedops.appName
      }
    },
    ...dataCapsule && {
      dataCapsule: {
        namespace: dataCapsule.namespace
      }
    }
  };
}

// ../../node_modules/@wix/fe-essentials/dist/esm/testkit/ChildEssentialsMock.js
var ChildEssentialsMock = class _ChildEssentialsMock {
  constructor({ parent, ...options }) {
    this.createChildEssentials = (options2 = {}) => {
      return new _ChildEssentialsMock({
        ...toMockOptions(options2),
        parent: this
      });
    };
    this._parent = parent;
    this._delegate = new EssentialsPartialMockDelegate({
      ...options,
      parent: this._parent
    });
  }
  get asPartial() {
    return this._delegate;
  }
  get experiments() {
    return this._delegate.getExperiments(this._parent);
  }
  get fedopsLogger() {
    return this._delegate.getFedopsLogger(this._parent);
  }
  get errorMonitor() {
    return this._delegate.getErrorMonitor(this._parent);
  }
  get i18n() {
    return this._delegate.getI18n(this._parent);
  }
  get biLoggerFactory() {
    return this._delegate.getBILoggerFactory(this._parent);
  }
  get biLogger() {
    return this._delegate.getBILogger(this._parent);
  }
  get httpClient() {
    return this._delegate.getHttpClient(this._parent);
  }
  get errorHandler() {
    return this._delegate.getErrorHandler(this._parent);
  }
  get dataCapsule() {
    return this._delegate.getDataCapsule(this._parent);
  }
  get createCurrencyFormatter() {
    return this._delegate.getCreateCurrencyFormatter(this._parent);
  }
  get formatAddress() {
    return this._delegate.getFormatAddress(this._parent);
  }
  get abTranslate() {
    return this._delegate.getAbTranslate(this._parent);
  }
  get environment() {
    return this._parent.environment;
  }
  get panoramaClient() {
    return this._delegate.getPanoramaClient(this._parent);
  }
};

// ../../node_modules/@wix/fe-essentials/dist/esm/testkit/EssentialsTestkitBrowser.js
var EssentialsTestkitBrowser = class {
  get sentryTestkit() {
    throw new Error("Sentry testkit is not supported in the browser");
  }
  get sentryTransport() {
    return void 0;
  }
  get fedopsLoggerTestkit() {
    throw new Error("Fedops testkit is not supported in the browser");
  }
  get biLoggerTestkit() {
    throw new Error("BI testkit is not supported in the browser");
  }
  get packageJson() {
    return {
      name: "EssentialsTestkitBrowser",
      version: "0.0.0",
      wix: {
        artifact: {
          artifactId: "EssentialsTestkitBrowser"
        }
      }
    };
  }
  beforeAndAfter() {
    throw new Error("Jest APIs are not supported in the browser");
  }
  async beforeAll() {
    return {
      afterAll: async () => {
      }
    };
  }
  async afterEach() {
  }
};

// src/testkit/StandaloneRootEssentialsMock.ts
var StandaloneRootEssentialsMock = class {
  constructor({
    httpClient: httpClientOptions,
    dataCapsule: dataCapsuleOptions,
    fedops: fedopsOptions,
    environment: environmentOptions,
    ...options
  }) {
    this.createChildEssentials = (options = {}) => {
      return new ChildEssentialsMock({
        ...toMockOptions(options),
        parent: this
      });
    };
    this.createChildEssentialsMock = (options = {}) => {
      return new ChildEssentialsMock({
        ...options,
        parent: this
      });
    };
    this._environment = {
      ...environmentOptions
    };
    this._delegate = new EssentialsPartialMockDelegate({
      ...options,
      httpClient: {
        ...httpClientOptions
      },
      dataCapsule: {
        namespace: this.environment.artifactId,
        ...dataCapsuleOptions
      },
      ...fedopsOptions && {
        fedops: {
          ...fedopsOptions,
          appName: this.environment.artifactId
        }
      },
      parent: {
        environment: this._environment
      }
    });
    this.asPartial = {
      environment: this._environment,
      ...this._delegate
    };
  }
  get experiments() {
    return this._delegate.getExperiments();
  }
  get fedopsLogger() {
    return this._delegate.getFedopsLogger();
  }
  get errorMonitor() {
    return this._delegate.getErrorMonitor();
  }
  get errorHandler() {
    return this._delegate.getErrorHandler();
  }
  get i18n() {
    return this._delegate.getI18n();
  }
  get biLoggerFactory() {
    return this._delegate.getBILoggerFactory();
  }
  get biLogger() {
    return this._delegate.getBILogger();
  }
  get httpClient() {
    return this._delegate.getHttpClient();
  }
  get dataCapsule() {
    return this._delegate.getDataCapsule();
  }
  get environment() {
    return this._environment;
  }
  get createCurrencyFormatter() {
    return this._delegate.getCreateCurrencyFormatter();
  }
  get formatAddress() {
    return this._delegate.getFormatAddress();
  }
  get abTranslate() {
    return this._delegate.getAbTranslate();
  }
  get panoramaClient() {
    return this._delegate.getPanoramaClient();
  }
};

// src/testkit/createTestComponent.tsx
var import_react10 = __toESM(require("react"), 1);

// ../../node_modules/@wix/fe-essentials/dist/esm/createRequiredContext.js
var import_react = require("react");
function assert(contentName, value) {
  if (process.env.NODE_ENV !== "production" && !value) {
    throw new Error(`${contentName}: value was not provided`);
  }
}
function createRequiredContext(contentName) {
  const Context11 = (0, import_react.createContext)(null);
  const Provider11 = Context11.Provider;
  const Consumer11 = Context11.Consumer;
  const useContext11 = () => {
    const value = (0, import_react.useContext)(Context11);
    assert(contentName, value);
    return value;
  };
  return {
    Context: Context11,
    Provider: Provider11,
    Consumer: Consumer11,
    useContext: useContext11
  };
}

// ../../node_modules/@wix/fe-essentials/dist/esm/react/EnvironmentProvider.js
var { useContext, Provider, Context, Consumer } = createRequiredContext("Environment");
var EnvironmentProvider = Provider;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/DataCapsuleProvider.js
var { useContext: useContext2, Provider: Provider2, Context: Context2, Consumer: Consumer2 } = createRequiredContext("DataCapsuleProvider");
var DataCapsuleProvider = Provider2;

// ../../node_modules/@wix/error-monitor/dist/esm/react/ErrorMonitorProvider.js
var import_react2 = __toESM(require("react"));
var ErrorMonitorContext = (0, import_react2.createContext)(null);
var ErrorMonitorConsumer = ErrorMonitorContext.Consumer;
var ErrorMonitorProvider = ({ errorMonitor, children }) => {
  const value = (0, import_react2.useMemo)(() => ({ errorMonitor }), [errorMonitor]);
  return import_react2.default.createElement(ErrorMonitorContext.Provider, { value: value.errorMonitor, children });
};

// ../../node_modules/@wix/fe-essentials/dist/esm/react/HttpClientProvider.js
var { useContext: useContext3, Provider: Provider3, Context: Context3, Consumer: Consumer3 } = createRequiredContext("HttpClientProvider");
var HttpClientProvider = Provider3;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/FedopsProvider.js
var { useContext: useContext4, Provider: Provider4, Context: Context4, Consumer: Consumer4 } = createRequiredContext("FedopsProvider");
var FedopsProvider = Provider4;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/BILoggerProvider.js
var { useContext: useContext5, Provider: Provider5, Context: Context5, Consumer: Consumer5 } = createRequiredContext("BILoggerProvider");
var BILoggerProvider = Provider5;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/PanoramaProvider.js
var { useContext: useContext6, Provider: Provider6, Context: Context6, Consumer: Consumer6 } = createRequiredContext("PanoramaProvider");
var PanoramaProvider = Provider6;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/EssentialsProvider.js
var { useContext: useContext7, Provider: Provider7, Context: Context7, Consumer: Consumer7 } = createRequiredContext("EssentialsProvider");
var EssentialsProvider = Provider7;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/EssentialsWrapperProvider.js
var import_react6 = __toESM(require("react"));

// ../../node_modules/@wix/wix-experiments-react/dist/es/src/ExperimentsContext.js
var import_react3 = __toESM(require("react"));
var ExperimentsContext = import_react3.default.createContext(null);

// ../../node_modules/@wix/wix-experiments-react/dist/es/src/PureExperimentsProvider.js
var import_react5 = __toESM(require("react"));

// ../../node_modules/@wix/wix-experiments-react/dist/es/src/useReadyExperiments.js
var import_react4 = require("react");
var useReadyExperiments = function(experiments) {
  var _a = (0, import_react4.useState)(!experiments.pending()), ready = _a[0], setReady = _a[1];
  (0, import_react4.useEffect)(function() {
    var mounted = true;
    if (!ready) {
      void experiments.ready().then(function() {
        if (mounted) {
          setReady(true);
        }
      }).catch(function() {
      });
    }
    return function() {
      mounted = false;
    };
  }, [ready, experiments]);
  return {
    ready,
    promise: experiments.ready(),
    experimentsInstance: experiments
  };
};

// ../../node_modules/@wix/wix-experiments-react/dist/es/src/PureExperimentsProvider.js
var PureExperimentsProvider = function(_a) {
  var experiments = _a.experiments, children = _a.children;
  var value = useReadyExperiments(experiments);
  return import_react5.default.createElement(ExperimentsContext.Provider, { value }, children);
};

// ../../node_modules/@wix/fe-essentials/dist/esm/react/CurrencyFormatterProvider.js
var { useContext: useContext8, Provider: Provider8, Context: Context8, Consumer: Consumer8 } = createRequiredContext("CurrencyFormatter");
var CurrencyFormatterProvider = Provider8;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/AddressFormatterProvider.js
var { useContext: useContext9, Provider: Provider9, Context: Context9, Consumer: Consumer9 } = createRequiredContext("AddressFormatter");
var AddressFormatterProvider = Provider9;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/ErrorHandlerProvider.js
var { useContext: useContext10, Provider: Provider10, Context: Context10, Consumer: Consumer10 } = createRequiredContext("ErrorHandlerProvider");
var ErrorHandlerProvider = Provider10;

// ../../node_modules/@wix/fe-essentials/dist/esm/react/EssentialsWrapperProvider.js
var EssentialsWrapperProvider = ({ essentials, children }) => {
  const { httpClient, errorMonitor, experiments, i18n: i18n2, environment, fedopsLogger, dataCapsule, createCurrencyFormatter, formatAddress, panoramaClient, biLogger, errorHandler } = essentials.asPartial;
  let child = import_react6.default.createElement(EssentialsProvider, { value: essentials }, children);
  if (environment) {
    child = import_react6.default.createElement(EnvironmentProvider, { value: environment }, child);
  }
  if (httpClient) {
    child = import_react6.default.createElement(HttpClientProvider, { value: httpClient }, child);
  }
  if (dataCapsule) {
    child = import_react6.default.createElement(DataCapsuleProvider, { value: dataCapsule }, child);
  }
  if (errorMonitor) {
    child = import_react6.default.createElement(ErrorMonitorProvider, { errorMonitor }, child);
  }
  if (fedopsLogger) {
    child = import_react6.default.createElement(FedopsProvider, { value: fedopsLogger }, child);
  }
  if (experiments) {
    child = import_react6.default.createElement(PureExperimentsProvider, { experiments }, child);
  }
  if (i18n2) {
    child = import_react6.default.createElement(import_react_i18next.I18nextProvider, { i18n: i18n2 }, child);
  }
  if (createCurrencyFormatter) {
    child = import_react6.default.createElement(CurrencyFormatterProvider, { value: createCurrencyFormatter }, child);
  }
  if (formatAddress) {
    child = import_react6.default.createElement(AddressFormatterProvider, { value: formatAddress }, child);
  }
  if (errorHandler) {
    child = import_react6.default.createElement(ErrorHandlerProvider, { value: errorHandler }, child);
  }
  if (panoramaClient) {
    child = import_react6.default.createElement(PanoramaProvider, { value: panoramaClient }, child);
  }
  if (biLogger) {
    child = import_react6.default.createElement(BILoggerProvider, { value: biLogger }, child);
  }
  return child;
};

// src/react/RootEssentialsWrapperProvider.tsx
var import_react8 = __toESM(require("react"), 1);
var RootEssentialsWrapperProvider = ({
  essentials,
  children
}) => {
  return /* @__PURE__ */ import_react8.default.createElement(EssentialsWrapperProvider, { essentials }, children);
};

// src/testkit/createTestComponent.tsx
function createTestComponent(WrapperComponent, {
  essentials,
  rootEssentials
}) {
  return function TestComponent(props) {
    return /* @__PURE__ */ import_react10.default.createElement(RootEssentialsWrapperProvider, { essentials: rootEssentials }, /* @__PURE__ */ import_react10.default.createElement(EssentialsWrapperProvider, { essentials }, /* @__PURE__ */ import_react10.default.createElement(WrapperComponent, { ...props })));
  };
}

// src/testkit/StandaloneEssentialsTestContext.ts
var StandaloneEssentialsTestContext = class {
  constructor(testkit2, params) {
    this.testkit = testkit2;
    this.essentials = params.essentials;
  }
  get asPartial() {
    return this.essentials.asPartial;
  }
  get biLoggerFactory() {
    return this.essentials.biLoggerFactory;
  }
  get biLogger() {
    return this.essentials.biLogger;
  }
  createChildEssentials(options) {
    return this.essentials.createChildEssentials(options);
  }
  get environment() {
    return this.essentials.environment;
  }
  get errorMonitor() {
    return this.essentials.errorMonitor;
  }
  get errorHandler() {
    return this.essentials.errorHandler;
  }
  get experiments() {
    return this.essentials.experiments;
  }
  get fedopsLogger() {
    return this.essentials.fedopsLogger;
  }
  get httpClient() {
    return this.essentials.httpClient;
  }
  get dataCapsule() {
    return this.essentials.dataCapsule;
  }
  get i18n() {
    return this.essentials.i18n;
  }
  get sentryTestkit() {
    return this.testkit.sentryTestkit;
  }
  get fedopsTestkit() {
    return this.testkit.fedopsTestkit;
  }
  get biTestkit() {
    return this.testkit.biTestkit;
  }
  get createCurrencyFormatter() {
    return this.essentials.createCurrencyFormatter;
  }
  get formatAddress() {
    return this.essentials.formatAddress;
  }
  get abTranslate() {
    return this.essentials.abTranslate;
  }
  get panoramaClient() {
    throw new Error(
      "Panorama Client is not implemented in Standalone Essentials"
    );
  }
};

// src/testkit/StandaloneEssentialsTestkit.ts
var StandaloneEssentialsTestkit = class {
  constructor(params) {
    this.essentialsTestkit = params.essentialsTestkit;
  }
  get sentryTestkit() {
    return this.essentialsTestkit.sentryTestkit;
  }
  get fedopsTestkit() {
    return this.essentialsTestkit.fedopsLoggerTestkit;
  }
  get biTestkit() {
    return this.essentialsTestkit.biLoggerTestkit;
  }
  get packageJson() {
    return this.essentialsTestkit.packageJson;
  }
  beforeAndAfter() {
    this.essentialsTestkit.beforeAndAfter();
  }
  async beforeAll() {
    return {
      afterAll: async () => {
      }
    };
  }
  async afterEach() {
  }
  _createTestEssentials(options = {}) {
    const { packageJson } = this;
    const {
      environment: environmentOptions,
      httpClient: httpClientOptions,
      fedops: fedopsOptions,
      dataCapsule: dataCapsuleOptions
    } = options;
    const rootEssentials = new StandaloneRootEssentialsMock({
      environment: {
        language: "en",
        artifactId: packageJson.wix?.artifact?.artifactId ?? packageJson.name,
        version: packageJson.version,
        ...environmentOptions
      },
      httpClient: httpClientOptions,
      fedops: fedopsOptions
    });
    const essentials = rootEssentials.createChildEssentialsMock({
      i18n: {
        messages: {},
        ...options.i18n
      },
      errorMonitor: {
        transport: this.essentialsTestkit.sentryTransport,
        ...options.errorMonitor
      },
      experiments: {
        bag: {},
        ...options.experiments
      },
      dataCapsule: dataCapsuleOptions
    });
    const { i18n: i18n2 } = essentials.asPartial;
    if (i18n2) {
      i18n2.init();
    }
    return { rootEssentials, essentials };
  }
  getTestContext(options) {
    const { essentials } = this._createTestEssentials(options);
    return new StandaloneEssentialsTestContext(this, {
      essentials
    });
  }
  getComponent(WrappedComponent, options = {}) {
    const { rootEssentials, essentials } = this._createTestEssentials(options);
    return {
      TestComponent: createTestComponent(WrappedComponent, {
        rootEssentials,
        essentials
      }),
      context: new StandaloneEssentialsTestContext(this, {
        essentials
      })
    };
  }
};

// src/testkit/index.ts
var testkit = new StandaloneEssentialsTestkit({
  essentialsTestkit: new EssentialsTestkitBrowser()
});
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  StandaloneEssentialsTestkit,
  TestContext,
  testkit
});
/*! Bundled license information:

lodash/lodash.js:
  (**
   * @license
   * Lodash <https://lodash.com/>
   * Copyright OpenJS Foundation and other contributors <https://openjsf.org/>
   * Released under MIT license <https://lodash.com/license>
   * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
   * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
   *)

base-64/base64.js:
  (*! https://mths.be/base64 v1.0.0 by @mathias | MIT license *)
*/
//# sourceMappingURL=index.js.map