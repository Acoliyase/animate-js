export const hasPage = (obj) => {
    return typeof obj === 'object' && obj !== null && 'page' in obj;
};
export const hasRoot = (obj) => {
    return typeof obj === 'object' && obj !== null && 'root' in obj;
};
export const hasSelector = (obj) => {
    return typeof obj === 'object' && obj !== null && 'selector' in obj && typeof obj.selector === 'string';
};
export const hasElement = (obj) => {
    return typeof obj === 'object' && obj !== null && 'element' in obj;
};
//# sourceMappingURL=configShapeChecks.js.map