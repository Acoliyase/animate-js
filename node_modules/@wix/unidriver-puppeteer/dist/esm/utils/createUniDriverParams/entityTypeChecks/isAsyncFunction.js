import { isPromise } from './isPromise';
export const isAsyncFunction = (checkedParams) => {
    if (typeof checkedParams !== 'function') {
        return false;
    }
    if (checkedParams.constructor.name === 'AsyncFunction') {
        return true;
    }
    if (checkedParams.toString().startsWith('async')) {
        return true;
    }
    try {
        const result = checkedParams();
        return isPromise(result);
    }
    catch {
        return true;
    }
};
//# sourceMappingURL=isAsyncFunction.js.map