export const enterText = async (base, value, options) => {
    const { disabled, readonly } = await base.evaluate((_base) => {
        return {
            disabled: _base.disabled,
            readonly: _base.readOnly,
        };
    });
    const { clear, delay } = options;
    if (disabled || readonly) {
        return;
    }
    await base.focus();
    if (clear) {
        // Select all input text
        await base.click({ clickCount: 3 });
        await base.press('Backspace');
    }
    await base.type(value, {
        delay,
    });
};
//# sourceMappingURL=enterText.js.map