import { UniDriverClassList } from './UniDriverClassList';
import { createUniDriverParams } from './utils/createUniDriverParams/createUniDriverParams';
export const puppeteerUniDriverList = (params) => {
    return new UniDriverClassList(
    // @ts-expect-error incompatible types, but we know don't care about actual type of `base` here
    createUniDriverParams(params, async (base, selector) => {
        return base.$$(selector);
    }));
};
//# sourceMappingURL=puppeteerUniDriverList.js.map