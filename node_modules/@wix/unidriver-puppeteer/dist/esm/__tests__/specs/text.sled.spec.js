import { setupWithAwaiter } from '../setup';
import { runTest } from '@wix/unidriver-common/testing';
describe('Adapter Specific', () => {
    describe('text', () => {
        it(`checking text return value when the text is empty`, async () => {
            await runTest(setupWithAwaiter, undefined, { story: 'empty-label' }, async ({ driver }) => {
                const labelText = await driver.$$('.label').text();
                expect(labelText[0]).toBe('');
            });
        });
        it(`checking text return value when the text is not empty`, async () => {
            await runTest(setupWithAwaiter, undefined, { story: 'label' }, async ({ driver }) => {
                const labelText = await driver.$$('.label').text();
                expect(labelText[0]).toBe('hello');
            });
        });
        it(`checking text return value when the component doesn't have textContent property`, async () => {
            await runTest(setupWithAwaiter, undefined, { story: 'with-initial-text' }, async ({ driver }) => {
                const textThatDoesNotExists = await driver.$('header input').text();
                expect(textThatDoesNotExists).toBe('');
            });
        });
    });
});
//# sourceMappingURL=text.sled.spec.js.map