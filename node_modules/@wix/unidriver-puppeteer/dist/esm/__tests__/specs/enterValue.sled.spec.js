import { setupWithAwaiter } from '../setup';
import { runTest } from '@wix/unidriver-common/testing';
describe('Adapter Specific', () => {
    describe('enterValue', () => {
        it(`doesn't attempt to clear value when "shouldClear" is false`, async () => {
            await runTest(setupWithAwaiter, undefined, { story: 'with-initial-text' }, async ({ driver }) => {
                await driver.$('header input').enterValue(' world!', { shouldClear: false });
                expect(await driver.$('header input').value()).toBe('hello world!');
            });
        });
    });
});
//# sourceMappingURL=enterValue.sled.spec.js.map