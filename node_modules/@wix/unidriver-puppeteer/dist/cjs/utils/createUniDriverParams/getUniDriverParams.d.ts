import { type PuppeteerAdapterParams } from '../../puppeteerUniDriver';
import { type ElementHandle, type PageOrFrame } from '../../types';
export type UniDriverParams = {
    selector: string;
    page: PageOrFrame;
    base: PageOrFrame | ElementHandle | (() => ElementHandle) | (() => Promise<ElementHandle>);
};
export declare const getUniDriverParams: (config: PuppeteerAdapterParams, querySelector: (base: ElementHandle, selector: string) => Promise<ElementHandle>) => UniDriverParams;
//# sourceMappingURL=getUniDriverParams.d.ts.map