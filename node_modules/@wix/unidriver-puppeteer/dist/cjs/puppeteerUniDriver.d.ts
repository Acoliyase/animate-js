import { type UniDriver } from '@wix/unidriver-core';
import { type ElementHandle, type PageOrFrame } from './types';
import { type AdapterParams } from '@wix/unidriver-common';
export type PuppeteerAdapterParams<T = ElementHandle | null, P = PageOrFrame> = {
    /** @deprecated use `root` instead of `page`, root can be any of ElementHandle|Page|Frame */
    page: PageOrFrame;
    selector?: string;
} | (() => {
    /** @deprecated use `root` instead of `page`, root can be any of ElementHandle|Page|Frame */
    page: PageOrFrame;
    selector?: string;
}) | (() => Promise<{
    /** @deprecated use `root` instead of `page`, root can be any of ElementHandle|Page|Frame */
    page: PageOrFrame;
    selector?: string;
}>) | (() => Promise<{
    /** @deprecated use `root` instead of `page`, root can be any of ElementHandle|Page|Frame */
    page?: PageOrFrame;
    selector?: string;
} | T>) | AdapterParams<T, P>;
export declare const puppeteerUniDriver: (params: PuppeteerAdapterParams) => UniDriver;
//# sourceMappingURL=puppeteerUniDriver.d.ts.map