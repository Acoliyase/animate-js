import { type RootElement } from './utils/createUniDriverParams/createUniDriverParams';
import { type ElementHandle, type PageOrFrame } from './types';
import { type UniDriver } from '@wix/unidriver-core';
import { type AdapterParams } from '@wix/unidriver-common';
export type PupAdapterParams<T = ElementHandle, P = PageOrFrame> = {
    page: PageOrFrame;
    element?: RootElement;
    selector?: string;
} | (() => {
    page: PageOrFrame;
    element?: RootElement;
    selector?: string;
}) | (() => Promise<{
    page: PageOrFrame;
    element?: RootElement;
    selector?: string;
}>) | (() => Promise<{
    page?: PageOrFrame;
    element?: RootElement;
    selector?: string;
} | T>) | AdapterParams<T, P>;
/** @deprecated use `puppeteerUniDriver` instead */
export declare const pupUniDriver: (params: PupAdapterParams) => UniDriver;
//# sourceMappingURL=pupUniDriver.d.ts.map