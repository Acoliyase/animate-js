"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var CategoryList_driver_exports = {};
__export(CategoryList_driver_exports, {
  default: () => CategoryListDriver
});
module.exports = __toCommonJS(CategoryList_driver_exports);
var import_react = require("@testing-library/react");
var import_dataHooks = __toESM(require("../../dataHooks"));
const getAllByDataHook = (baseElement, dataHook) => baseElement.querySelectorAll(`[data-hook="${dataHook}"]`);
const getByDataHook = (baseElement, dataHook) => baseElement.querySelector(`[data-hook="${dataHook}"]`);
const getSearchInput = (baseElement) => getByDataHook(baseElement, import_dataHooks.default.categorySearchInput);
const getCategoryRows = (baseElement) => getAllByDataHook(baseElement, import_dataHooks.default.categoryTableCell);
class CategoryListDriver {
  constructor(baseElement) {
    this.getAmountOfCategoryTitles = () => getAllByDataHook(this.baseElement, import_dataHooks.default.categoryTableTitle).length;
    this.getAmountOfCategoryRows = () => getCategoryRows(this.baseElement).length;
    this.getRowText = (index) => {
      const categoryRows = getCategoryRows(this.baseElement);
      const row = categoryRows.item(index);
      return row.textContent;
    };
    this.search = (query) => {
      const searchInput = getSearchInput(this.baseElement);
      const inputEvent = import_react.createEvent.input(searchInput, {
        target: { value: query }
      });
      (0, import_react.fireEvent)(searchInput, inputEvent);
    };
    this.baseElement = baseElement;
  }
}
