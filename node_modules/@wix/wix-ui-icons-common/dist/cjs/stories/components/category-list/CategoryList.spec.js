"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var React = __toESM(require("react"));
var import_react = require("@testing-library/react");
var import_CategoryList = __toESM(require("./CategoryList"));
var import_fixtures = require("../../fixtures");
var import_dataHooks = __toESM(require("../../dataHooks"));
var import_CategoryList2 = __toESM(require("./CategoryList.driver"));
const mapIconToRow = ({ title }) => [
  /* @__PURE__ */ React.createElement("div", { "data-hook": import_dataHooks.default.categoryTableCell }, title)
];
describe("mapIconsToCategories", () => {
  it("splits icons into categories", () => {
    const categories = (0, import_CategoryList.mapIconsToCategories)(import_fixtures.iconsMetadata);
    expect(categories).toHaveLength(2);
    const [actionsCategory, generalCategory] = categories;
    const [edit, confirm, languages] = import_fixtures.iconsMetadata;
    expect(actionsCategory.iconsMetadata).toMatchObject([edit, confirm]);
    expect(generalCategory.iconsMetadata).toMatchObject([languages]);
  });
  it("returns categories with titles", () => {
    const categories = (0, import_CategoryList.mapIconsToCategories)(import_fixtures.iconsMetadata);
    const [actionsCategory, generalCategory] = categories;
    expect(actionsCategory.title).toBe("Actions");
    expect(generalCategory.title).toBe("General");
  });
});
describe("<CategoryList/>", () => {
  it("renders all categories", () => {
    const { baseElement } = (0, import_react.render)(
      /* @__PURE__ */ React.createElement(
        import_CategoryList.default,
        {
          ...{ iconsMetadata: import_fixtures.iconsMetadata, tableHeaderTitles: import_fixtures.tableHeaderTitles, mapIconToRow, searchKeys: import_fixtures.searchKeys }
        }
      )
    );
    const driver = new import_CategoryList2.default(baseElement);
    expect(driver.getAmountOfCategoryTitles()).toBe(2);
    expect(driver.getAmountOfCategoryRows()).toBe(3);
  });
  it("can be searched by query", async () => {
    const { baseElement } = (0, import_react.render)(
      /* @__PURE__ */ React.createElement(
        import_CategoryList.default,
        {
          ...{ iconsMetadata: import_fixtures.iconsMetadata, tableHeaderTitles: import_fixtures.tableHeaderTitles, searchKeys: import_fixtures.searchKeys, mapIconToRow }
        }
      )
    );
    const driver = new import_CategoryList2.default(baseElement);
    expect(driver.getAmountOfCategoryRows()).toBe(3);
    const [edit] = import_fixtures.iconsMetadata;
    const query = edit.title;
    driver.search(query);
    await (0, import_react.waitFor)(() => {
      expect(driver.getAmountOfCategoryRows()).toBe(1);
    });
    expect(driver.getRowText(0)).toBe("Edit");
  });
});
