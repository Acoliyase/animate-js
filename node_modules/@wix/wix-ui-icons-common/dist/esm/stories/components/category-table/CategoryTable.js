import * as React from "react";
import { useMemo } from "react";
import { table as Table } from "@wix/wix-storybook-utils/dist/cjs/Sections/views/table";
import sectionStyles from "@wix/wix-storybook-utils/dist/cjs/Sections/styles.scss";
import dataHooks from "../../dataHooks";
const CategoryTable = ({
  title,
  tableHeaderTitles,
  iconsMetadata,
  mapIconToRow
}) => {
  const rows = useMemo(
    () => iconsMetadata.map(mapIconToRow),
    [iconsMetadata, mapIconToRow]
  );
  return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
    "h2",
    {
      "data-hook": dataHooks.categoryTableTitle,
      className: sectionStyles["section-title"]
    },
    title
  ), /* @__PURE__ */ React.createElement(Table, { rows, headerTitles: tableHeaderTitles, transparentHeader: true }));
};
var CategoryTable_default = CategoryTable;
export {
  CategoryTable_default as default
};
