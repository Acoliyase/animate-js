interface ManagerInstance {
    stop: boolean;
    opacity: number;
    environment: {
        loadBackground(url: string): void;
        loadTextureAsLightmap(url: string): void;
        addByDirectionalLights(intensity?: number, color?: number): {
            remove(): void;
        };
        sun(intensity?: number, color?: number): {
            remove(): void;
        };
        addAmbientLight(intensity?: number, color?: number): {
            remove(): void;
        };
        loadHDR(url: string): Promise<{
            remove(): void;
        }>;
    };
    model: {
        load3DModel(url: string): Promise<{
            remove(): void;
            punctualLights: boolean;
        }>;
        addGround(opacity?: number): {
            remove(): void;
        };
        transform: {
            setRotation(x: number | undefined, y: number | undefined, z: number | undefined): void;
            setPosition(x: number | undefined, y: number | undefined, z: number | undefined): void;
            setScale(x: number | undefined, y: number | undefined, z: number | undefined): void;
        };
    };
    camera: {
        enablePan: boolean;
        enableZoom: boolean;
        enableRotate: boolean;
        enableAutoRotate: boolean;
    };
    dispose(): void;
}
export declare function createSceneManager(container: HTMLElement, canvas: HTMLCanvasElement): ManagerInstance;
export type SceneManager = ReturnType<typeof createSceneManager>;
export declare function useSceneManager(): {
    sceneManager: ManagerInstance | null;
    render: (container: HTMLElement, canvas: HTMLCanvasElement) => ManagerInstance;
};
export {};
//# sourceMappingURL=3dManager.d.ts.map