"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const react_1 = tslib_1.__importDefault(require("react"));
const itemView_js_1 = tslib_1.__importDefault(require("../item/itemView.js"));
const pro_gallery_lib_1 = require("pro-gallery-lib");
class GroupView extends react_1.default.Component {
    constructor(props) {
        super(props);
        this.displayName = 'GroupView';
        this.dom = [];
        this.state = {};
    }
    createDom(visible, empty) {
        return this.props.items.map((item) => {
            const props = item.renderProps(Object.assign(Object.assign({}, this.props.galleryConfig), { visible }));
            return react_1.default.createElement(itemView_js_1.default, Object.assign(Object.assign({}, props), { disableItemFocus: this.props.disableItemFocus, type: (empty !== null && empty !== void 0 ? empty : false) ? 'dummy' : props.type }));
        });
    }
    shouldRender() {
        const { items } = this.props;
        if (!items || !items.length || !items[0]) {
            return false;
        }
        return true;
    }
    isVisible() {
        const { items, galleryConfig } = this.props;
        if (this.props.allowLoop) {
            const { idx } = items[items.length - 1];
            const { activeIndex, totalItemsCount } = galleryConfig;
            const distance = activeIndex - idx;
            const padding = Math.floor(totalItemsCount / 2);
            return Math.abs(distance) <= padding;
        }
        return true;
    }
    render() {
        const { options } = this.props.galleryConfig; //v5 TODO - why is this not from the options in the gallery. this comes from the layouter
        const isRTL = options[pro_gallery_lib_1.optionsMap.behaviourParams.gallery.layoutDirection] ===
            pro_gallery_lib_1.GALLERY_CONSTS[pro_gallery_lib_1.optionsMap.behaviourParams.gallery.layoutDirection].RIGHT_TO_LEFT;
        return this.shouldRender() ? (react_1.default.createElement("div", { key: `group_${this.props.idx}_${this.props.items[0].id}`, "data-hook": 'group-view', className: `group-view group-view-${this.props.idx}`, style: {
                '--group-top': this.props.top + 'px',
                '--group-left': isRTL ? 'auto' : this.props.left + 'px',
                '--group-width': this.props.width + 'px',
                '--group-right': !isRTL ? 'auto' : this.props.left + 'px',
            }, "aria-hidden": this.props.ariaHidden }, this.createDom(this.isVisible(), this.props.shouldRenderEmpty))) : null;
    }
}
exports.default = GroupView;
//# sourceMappingURL=groupView.js.map