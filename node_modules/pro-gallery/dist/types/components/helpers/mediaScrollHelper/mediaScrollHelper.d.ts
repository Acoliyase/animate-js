import { SetItemIdx, UpdateGalleryData, SetScroll, HandleEvents, GetPlayTrigger } from './types.js';
declare class VideoScrollHelper {
    setPlayingItem: SetItemIdx;
    readonly isItemSupported: (item: any) => boolean;
    readonly getPlayTrigger: GetPlayTrigger;
    private scrollBase;
    private items;
    private currentPlayingIdx;
    private currentItemCount;
    private playTrigger?;
    private scrollDirection?;
    private lastVideoPlayed;
    private readonly itemRatingMap;
    private galleryWidth?;
    private videoLoop?;
    private top;
    private left;
    constructor(setPlayingItem: SetItemIdx, isItemSupported: (item: any) => boolean, getPlayTrigger: GetPlayTrigger);
    readonly updateGalleryStructure: UpdateGalleryData;
    readonly handleEvent: HandleEvents;
    private readonly shouldTriggerAction;
    private readonly itemHovered;
    private readonly itemClicked;
    readonly onScroll: SetScroll;
    private readonly videoEnded;
    private readonly videoPlayed;
    private readonly videoErrorReported;
    readonly initializePlayState: () => void;
    private readonly autoPlayNextItemByRating;
    private readonly play;
    readonly stop: (idx?: number) => void;
    private readonly onPlayingIdxChange;
    private readonly setPlayingIdx;
    private readonly isCurrentItemStillVisible;
    private readonly isVisible;
    private readonly shouldTrigger;
    readonly findItem: (idx: number) => any;
}
export default VideoScrollHelper;
//# sourceMappingURL=mediaScrollHelper.d.ts.map